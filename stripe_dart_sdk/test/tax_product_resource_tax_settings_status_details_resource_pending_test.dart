import 'package:test/test.dart';
import 'package:stripe_dart_sdk/stripe_dart_sdk.dart';

// tests for TaxProductResourceTaxSettingsStatusDetailsResourcePending
void main() {
  final instance = TaxProductResourceTaxSettingsStatusDetailsResourcePendingBuilder();
  // TODO add properties to the builder and call build()

  group(TaxProductResourceTaxSettingsStatusDetailsResourcePending, () {
    // The list of missing fields that are required to perform calculations. It includes the entry `head_office` when the status is `pending`. It is recommended to set the optional values even if they aren't listed as required for calculating taxes. Calculations can fail if missing fields aren't explicitly provided on every call.
    // BuiltList<String> missingFields
    test('to test the property `missingFields`', () async {
      // TODO
    });

  });
}
