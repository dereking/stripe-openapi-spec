//
// AUTO-GENERATED FILE, DO NOT MODIFY!
//
// @dart=2.18

// ignore_for_file: unused_element, unused_import
// ignore_for_file: always_put_required_named_parameters_first
// ignore_for_file: constant_identifier_names
// ignore_for_file: lines_longer_than_80_chars

part of openapi.api;


class DefaultApi {
  DefaultApi([ApiClient? apiClient]) : apiClient = apiClient ?? defaultApiClient;

  final ApiClient apiClient;

  /// Delete an account
  ///
  /// <p>With <a href=\"/connect\">Connect</a>, you can delete accounts you manage.</p>  <p>Test-mode accounts can be deleted at any time.</p>  <p>Live-mode accounts that have access to the standard dashboard and Stripe is responsible for negative account balances cannot be deleted, which includes Standard accounts. All other Live-mode accounts, can be deleted when all <a href=\"/api/balance/balance_object\">balances</a> are zero.</p>  <p>If you want to delete your own account, use the <a href=\"https://dashboard.stripe.com/settings/account\">account information tab in your account settings</a> instead.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  Future<Response> deleteAccountsAccountWithHttpInfo(String account,) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/accounts/{account}'
      .replaceAll('{account}', account);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'DELETE',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Delete an account
  ///
  /// <p>With <a href=\"/connect\">Connect</a>, you can delete accounts you manage.</p>  <p>Test-mode accounts can be deleted at any time.</p>  <p>Live-mode accounts that have access to the standard dashboard and Stripe is responsible for negative account balances cannot be deleted, which includes Standard accounts. All other Live-mode accounts, can be deleted when all <a href=\"/api/balance/balance_object\">balances</a> are zero.</p>  <p>If you want to delete your own account, use the <a href=\"https://dashboard.stripe.com/settings/account\">account information tab in your account settings</a> instead.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  Future<DeletedAccount?> deleteAccountsAccount(String account,) async {
    final response = await deleteAccountsAccountWithHttpInfo(account,);
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'DeletedAccount',) as DeletedAccount;
    
    }
    return null;
  }

  /// Delete an external account
  ///
  /// <p>Delete a specified external account for a given account.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] id (required):
  ///   Unique identifier for the external account to be deleted.
  Future<Response> deleteAccountsAccountBankAccountsIdWithHttpInfo(String account, String id,) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/accounts/{account}/bank_accounts/{id}'
      .replaceAll('{account}', account)
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'DELETE',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Delete an external account
  ///
  /// <p>Delete a specified external account for a given account.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] id (required):
  ///   Unique identifier for the external account to be deleted.
  Future<DeletedExternalAccount?> deleteAccountsAccountBankAccountsId(String account, String id,) async {
    final response = await deleteAccountsAccountBankAccountsIdWithHttpInfo(account, id,);
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'DeletedExternalAccount',) as DeletedExternalAccount;
    
    }
    return null;
  }

  /// Delete an external account
  ///
  /// <p>Delete a specified external account for a given account.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] id (required):
  ///   Unique identifier for the external account to be deleted.
  Future<Response> deleteAccountsAccountExternalAccountsIdWithHttpInfo(String account, String id,) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/accounts/{account}/external_accounts/{id}'
      .replaceAll('{account}', account)
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'DELETE',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Delete an external account
  ///
  /// <p>Delete a specified external account for a given account.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] id (required):
  ///   Unique identifier for the external account to be deleted.
  Future<DeletedExternalAccount?> deleteAccountsAccountExternalAccountsId(String account, String id,) async {
    final response = await deleteAccountsAccountExternalAccountsIdWithHttpInfo(account, id,);
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'DeletedExternalAccount',) as DeletedExternalAccount;
    
    }
    return null;
  }

  /// Delete a person
  ///
  /// <p>Deletes an existing person’s relationship to the account’s legal entity. Any person with a relationship for an account can be deleted through the API, except if the person is the <code>account_opener</code>. If your integration is using the <code>executive</code> parameter, you cannot delete the only verified <code>executive</code> on file.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] person (required):
  Future<Response> deleteAccountsAccountPeoplePersonWithHttpInfo(String account, String person,) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/accounts/{account}/people/{person}'
      .replaceAll('{account}', account)
      .replaceAll('{person}', person);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'DELETE',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Delete a person
  ///
  /// <p>Deletes an existing person’s relationship to the account’s legal entity. Any person with a relationship for an account can be deleted through the API, except if the person is the <code>account_opener</code>. If your integration is using the <code>executive</code> parameter, you cannot delete the only verified <code>executive</code> on file.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] person (required):
  Future<DeletedPerson?> deleteAccountsAccountPeoplePerson(String account, String person,) async {
    final response = await deleteAccountsAccountPeoplePersonWithHttpInfo(account, person,);
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'DeletedPerson',) as DeletedPerson;
    
    }
    return null;
  }

  /// Delete a person
  ///
  /// <p>Deletes an existing person’s relationship to the account’s legal entity. Any person with a relationship for an account can be deleted through the API, except if the person is the <code>account_opener</code>. If your integration is using the <code>executive</code> parameter, you cannot delete the only verified <code>executive</code> on file.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] person (required):
  Future<Response> deleteAccountsAccountPersonsPersonWithHttpInfo(String account, String person,) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/accounts/{account}/persons/{person}'
      .replaceAll('{account}', account)
      .replaceAll('{person}', person);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'DELETE',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Delete a person
  ///
  /// <p>Deletes an existing person’s relationship to the account’s legal entity. Any person with a relationship for an account can be deleted through the API, except if the person is the <code>account_opener</code>. If your integration is using the <code>executive</code> parameter, you cannot delete the only verified <code>executive</code> on file.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] person (required):
  Future<DeletedPerson?> deleteAccountsAccountPersonsPerson(String account, String person,) async {
    final response = await deleteAccountsAccountPersonsPersonWithHttpInfo(account, person,);
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'DeletedPerson',) as DeletedPerson;
    
    }
    return null;
  }

  /// <p>Delete an apple pay domain.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] domain (required):
  Future<Response> deleteApplePayDomainsDomainWithHttpInfo(String domain,) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/apple_pay/domains/{domain}'
      .replaceAll('{domain}', domain);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'DELETE',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// <p>Delete an apple pay domain.</p>
  ///
  /// Parameters:
  ///
  /// * [String] domain (required):
  Future<DeletedApplePayDomain?> deleteApplePayDomainsDomain(String domain,) async {
    final response = await deleteApplePayDomainsDomainWithHttpInfo(domain,);
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'DeletedApplePayDomain',) as DeletedApplePayDomain;
    
    }
    return null;
  }

  /// Delete a coupon
  ///
  /// <p>You can delete coupons via the <a href=\"https://dashboard.stripe.com/coupons\">coupon management</a> page of the Stripe dashboard. However, deleting a coupon does not affect any customers who have already applied the coupon; it means that new customers can’t redeem the coupon. You can also delete coupons via the API.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] coupon (required):
  Future<Response> deleteCouponsCouponWithHttpInfo(String coupon,) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/coupons/{coupon}'
      .replaceAll('{coupon}', coupon);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'DELETE',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Delete a coupon
  ///
  /// <p>You can delete coupons via the <a href=\"https://dashboard.stripe.com/coupons\">coupon management</a> page of the Stripe dashboard. However, deleting a coupon does not affect any customers who have already applied the coupon; it means that new customers can’t redeem the coupon. You can also delete coupons via the API.</p>
  ///
  /// Parameters:
  ///
  /// * [String] coupon (required):
  Future<DeletedCoupon?> deleteCouponsCoupon(String coupon,) async {
    final response = await deleteCouponsCouponWithHttpInfo(coupon,);
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'DeletedCoupon',) as DeletedCoupon;
    
    }
    return null;
  }

  /// Delete a customer
  ///
  /// <p>Permanently deletes a customer. It cannot be undone. Also immediately cancels any active subscriptions on the customer.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  Future<Response> deleteCustomersCustomerWithHttpInfo(String customer,) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}'
      .replaceAll('{customer}', customer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'DELETE',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Delete a customer
  ///
  /// <p>Permanently deletes a customer. It cannot be undone. Also immediately cancels any active subscriptions on the customer.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  Future<DeletedCustomer?> deleteCustomersCustomer(String customer,) async {
    final response = await deleteCustomersCustomerWithHttpInfo(customer,);
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'DeletedCustomer',) as DeletedCustomer;
    
    }
    return null;
  }

  /// Delete a customer source
  ///
  /// <p>Delete a specified source for a given customer.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> deleteCustomersCustomerBankAccountsIdWithHttpInfo(String customer, String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/bank_accounts/{id}'
      .replaceAll('{customer}', customer)
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'DELETE',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Delete a customer source
  ///
  /// <p>Delete a specified source for a given customer.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<DeleteCustomersCustomerBankAccountsId200Response?> deleteCustomersCustomerBankAccountsId(String customer, String id, { List<String>? expand, }) async {
    final response = await deleteCustomersCustomerBankAccountsIdWithHttpInfo(customer, id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'DeleteCustomersCustomerBankAccountsId200Response',) as DeleteCustomersCustomerBankAccountsId200Response;
    
    }
    return null;
  }

  /// Delete a customer source
  ///
  /// <p>Delete a specified source for a given customer.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> deleteCustomersCustomerCardsIdWithHttpInfo(String customer, String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/cards/{id}'
      .replaceAll('{customer}', customer)
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'DELETE',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Delete a customer source
  ///
  /// <p>Delete a specified source for a given customer.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<DeleteCustomersCustomerBankAccountsId200Response?> deleteCustomersCustomerCardsId(String customer, String id, { List<String>? expand, }) async {
    final response = await deleteCustomersCustomerCardsIdWithHttpInfo(customer, id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'DeleteCustomersCustomerBankAccountsId200Response',) as DeleteCustomersCustomerBankAccountsId200Response;
    
    }
    return null;
  }

  /// Delete a customer discount
  ///
  /// <p>Removes the currently applied discount on a customer.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  Future<Response> deleteCustomersCustomerDiscountWithHttpInfo(String customer,) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/discount'
      .replaceAll('{customer}', customer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'DELETE',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Delete a customer discount
  ///
  /// <p>Removes the currently applied discount on a customer.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  Future<DeletedDiscount?> deleteCustomersCustomerDiscount(String customer,) async {
    final response = await deleteCustomersCustomerDiscountWithHttpInfo(customer,);
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'DeletedDiscount',) as DeletedDiscount;
    
    }
    return null;
  }

  /// Delete a customer source
  ///
  /// <p>Delete a specified source for a given customer.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> deleteCustomersCustomerSourcesIdWithHttpInfo(String customer, String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/sources/{id}'
      .replaceAll('{customer}', customer)
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'DELETE',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Delete a customer source
  ///
  /// <p>Delete a specified source for a given customer.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<DeleteCustomersCustomerBankAccountsId200Response?> deleteCustomersCustomerSourcesId(String customer, String id, { List<String>? expand, }) async {
    final response = await deleteCustomersCustomerSourcesIdWithHttpInfo(customer, id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'DeleteCustomersCustomerBankAccountsId200Response',) as DeleteCustomersCustomerBankAccountsId200Response;
    
    }
    return null;
  }

  /// Cancel a subscription
  ///
  /// <p>Cancels a customer’s subscription. If you set the <code>at_period_end</code> parameter to <code>true</code>, the subscription will remain active until the end of the period, at which point it will be canceled and not renewed. Otherwise, with the default <code>false</code> value, the subscription is terminated immediately. In either case, the customer will not be charged again for the subscription.</p>  <p>Note, however, that any pending invoice items that you’ve created will still be charged for at the end of the period, unless manually <a href=\"#delete_invoiceitem\">deleted</a>. If you’ve set the subscription to cancel at the end of the period, any pending prorations will also be left in place and collected at the end of the period. But if the subscription is set to cancel immediately, pending prorations will be removed.</p>  <p>By default, upon subscription cancellation, Stripe will stop automatic collection of all finalized invoices for the customer. This is intended to prevent unexpected payment attempts after the customer has canceled a subscription. However, you can resume automatic collection of the invoices manually after subscription cancellation to have us proceed. Or, you could check for unpaid invoices before allowing the customer to cancel the subscription at all.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] subscriptionExposedId (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [bool] invoiceNow:
  ///   Can be set to `true` if `at_period_end` is not set to `true`. Will generate a final invoice that invoices for any un-invoiced metered usage and new/pending proration invoice items.
  ///
  /// * [bool] prorate:
  ///   Can be set to `true` if `at_period_end` is not set to `true`. Will generate a proration invoice item that credits remaining unused time until the subscription period end.
  Future<Response> deleteCustomersCustomerSubscriptionsSubscriptionExposedIdWithHttpInfo(String customer, String subscriptionExposedId, { List<String>? expand, bool? invoiceNow, bool? prorate, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/subscriptions/{subscription_exposed_id}'
      .replaceAll('{customer}', customer)
      .replaceAll('{subscription_exposed_id}', subscriptionExposedId);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (invoiceNow != null) {
      formParams[r'invoice_now'] = parameterToString(invoiceNow);
    }
    if (prorate != null) {
      formParams[r'prorate'] = parameterToString(prorate);
    }

    return apiClient.invokeAPI(
      path,
      'DELETE',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Cancel a subscription
  ///
  /// <p>Cancels a customer’s subscription. If you set the <code>at_period_end</code> parameter to <code>true</code>, the subscription will remain active until the end of the period, at which point it will be canceled and not renewed. Otherwise, with the default <code>false</code> value, the subscription is terminated immediately. In either case, the customer will not be charged again for the subscription.</p>  <p>Note, however, that any pending invoice items that you’ve created will still be charged for at the end of the period, unless manually <a href=\"#delete_invoiceitem\">deleted</a>. If you’ve set the subscription to cancel at the end of the period, any pending prorations will also be left in place and collected at the end of the period. But if the subscription is set to cancel immediately, pending prorations will be removed.</p>  <p>By default, upon subscription cancellation, Stripe will stop automatic collection of all finalized invoices for the customer. This is intended to prevent unexpected payment attempts after the customer has canceled a subscription. However, you can resume automatic collection of the invoices manually after subscription cancellation to have us proceed. Or, you could check for unpaid invoices before allowing the customer to cancel the subscription at all.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] subscriptionExposedId (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [bool] invoiceNow:
  ///   Can be set to `true` if `at_period_end` is not set to `true`. Will generate a final invoice that invoices for any un-invoiced metered usage and new/pending proration invoice items.
  ///
  /// * [bool] prorate:
  ///   Can be set to `true` if `at_period_end` is not set to `true`. Will generate a proration invoice item that credits remaining unused time until the subscription period end.
  Future<Subscription?> deleteCustomersCustomerSubscriptionsSubscriptionExposedId(String customer, String subscriptionExposedId, { List<String>? expand, bool? invoiceNow, bool? prorate, }) async {
    final response = await deleteCustomersCustomerSubscriptionsSubscriptionExposedIdWithHttpInfo(customer, subscriptionExposedId,  expand: expand, invoiceNow: invoiceNow, prorate: prorate, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Subscription',) as Subscription;
    
    }
    return null;
  }

  /// Delete a customer discount
  ///
  /// <p>Removes the currently applied discount on a customer.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] subscriptionExposedId (required):
  Future<Response> deleteCustomersCustomerSubscriptionsSubscriptionExposedIdDiscountWithHttpInfo(String customer, String subscriptionExposedId,) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/subscriptions/{subscription_exposed_id}/discount'
      .replaceAll('{customer}', customer)
      .replaceAll('{subscription_exposed_id}', subscriptionExposedId);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'DELETE',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Delete a customer discount
  ///
  /// <p>Removes the currently applied discount on a customer.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] subscriptionExposedId (required):
  Future<DeletedDiscount?> deleteCustomersCustomerSubscriptionsSubscriptionExposedIdDiscount(String customer, String subscriptionExposedId,) async {
    final response = await deleteCustomersCustomerSubscriptionsSubscriptionExposedIdDiscountWithHttpInfo(customer, subscriptionExposedId,);
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'DeletedDiscount',) as DeletedDiscount;
    
    }
    return null;
  }

  /// Delete a Customer tax ID
  ///
  /// <p>Deletes an existing <code>tax_id</code> object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] id (required):
  Future<Response> deleteCustomersCustomerTaxIdsIdWithHttpInfo(String customer, String id,) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/tax_ids/{id}'
      .replaceAll('{customer}', customer)
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'DELETE',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Delete a Customer tax ID
  ///
  /// <p>Deletes an existing <code>tax_id</code> object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] id (required):
  Future<DeletedTaxId?> deleteCustomersCustomerTaxIdsId(String customer, String id,) async {
    final response = await deleteCustomersCustomerTaxIdsIdWithHttpInfo(customer, id,);
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'DeletedTaxId',) as DeletedTaxId;
    
    }
    return null;
  }

  /// Immediately invalidate an ephemeral key
  ///
  /// <p>Invalidates a short-lived API key for a given resource.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] key (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> deleteEphemeralKeysKeyWithHttpInfo(String key, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/ephemeral_keys/{key}'
      .replaceAll('{key}', key);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'DELETE',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Immediately invalidate an ephemeral key
  ///
  /// <p>Invalidates a short-lived API key for a given resource.</p>
  ///
  /// Parameters:
  ///
  /// * [String] key (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<EphemeralKey?> deleteEphemeralKeysKey(String key, { List<String>? expand, }) async {
    final response = await deleteEphemeralKeysKeyWithHttpInfo(key,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'EphemeralKey',) as EphemeralKey;
    
    }
    return null;
  }

  /// Delete an invoice item
  ///
  /// <p>Deletes an invoice item, removing it from an invoice. Deleting invoice items is only possible when they’re not attached to invoices, or if it’s attached to a draft invoice.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] invoiceitem (required):
  Future<Response> deleteInvoiceitemsInvoiceitemWithHttpInfo(String invoiceitem,) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/invoiceitems/{invoiceitem}'
      .replaceAll('{invoiceitem}', invoiceitem);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'DELETE',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Delete an invoice item
  ///
  /// <p>Deletes an invoice item, removing it from an invoice. Deleting invoice items is only possible when they’re not attached to invoices, or if it’s attached to a draft invoice.</p>
  ///
  /// Parameters:
  ///
  /// * [String] invoiceitem (required):
  Future<DeletedInvoiceitem?> deleteInvoiceitemsInvoiceitem(String invoiceitem,) async {
    final response = await deleteInvoiceitemsInvoiceitemWithHttpInfo(invoiceitem,);
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'DeletedInvoiceitem',) as DeletedInvoiceitem;
    
    }
    return null;
  }

  /// Delete a draft invoice
  ///
  /// <p>Permanently deletes a one-off invoice draft. This cannot be undone. Attempts to delete invoices that are no longer in a draft state will fail; once an invoice has been finalized or if an invoice is for a subscription, it must be <a href=\"#void_invoice\">voided</a>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  Future<Response> deleteInvoicesInvoiceWithHttpInfo(String invoice,) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/invoices/{invoice}'
      .replaceAll('{invoice}', invoice);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'DELETE',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Delete a draft invoice
  ///
  /// <p>Permanently deletes a one-off invoice draft. This cannot be undone. Attempts to delete invoices that are no longer in a draft state will fail; once an invoice has been finalized or if an invoice is for a subscription, it must be <a href=\"#void_invoice\">voided</a>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  Future<DeletedInvoice?> deleteInvoicesInvoice(String invoice,) async {
    final response = await deleteInvoicesInvoiceWithHttpInfo(invoice,);
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'DeletedInvoice',) as DeletedInvoice;
    
    }
    return null;
  }

  /// Delete a plan
  ///
  /// <p>Deleting plans means new subscribers can’t be added. Existing subscribers aren’t affected.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] plan (required):
  Future<Response> deletePlansPlanWithHttpInfo(String plan,) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/plans/{plan}'
      .replaceAll('{plan}', plan);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'DELETE',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Delete a plan
  ///
  /// <p>Deleting plans means new subscribers can’t be added. Existing subscribers aren’t affected.</p>
  ///
  /// Parameters:
  ///
  /// * [String] plan (required):
  Future<DeletedPlan?> deletePlansPlan(String plan,) async {
    final response = await deletePlansPlanWithHttpInfo(plan,);
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'DeletedPlan',) as DeletedPlan;
    
    }
    return null;
  }

  /// Delete a product
  ///
  /// <p>Delete a product. Deleting a product is only possible if it has no prices associated with it. Additionally, deleting a product with <code>type=good</code> is only possible if it has no SKUs associated with it.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  Future<Response> deleteProductsIdWithHttpInfo(String id,) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/products/{id}'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'DELETE',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Delete a product
  ///
  /// <p>Delete a product. Deleting a product is only possible if it has no prices associated with it. Additionally, deleting a product with <code>type=good</code> is only possible if it has no SKUs associated with it.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  Future<DeletedProduct?> deleteProductsId(String id,) async {
    final response = await deleteProductsIdWithHttpInfo(id,);
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'DeletedProduct',) as DeletedProduct;
    
    }
    return null;
  }

  /// Remove a feature from a product
  ///
  /// <p>Deletes the feature attachment to a product</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [String] product (required):
  Future<Response> deleteProductsProductFeaturesIdWithHttpInfo(String id, String product,) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/products/{product}/features/{id}'
      .replaceAll('{id}', id)
      .replaceAll('{product}', product);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'DELETE',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Remove a feature from a product
  ///
  /// <p>Deletes the feature attachment to a product</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [String] product (required):
  Future<DeletedProductFeature?> deleteProductsProductFeaturesId(String id, String product,) async {
    final response = await deleteProductsProductFeaturesIdWithHttpInfo(id, product,);
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'DeletedProductFeature',) as DeletedProductFeature;
    
    }
    return null;
  }

  /// Delete a value list item
  ///
  /// <p>Deletes a <code>ValueListItem</code> object, removing it from its parent value list.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] item (required):
  Future<Response> deleteRadarValueListItemsItemWithHttpInfo(String item,) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/radar/value_list_items/{item}'
      .replaceAll('{item}', item);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'DELETE',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Delete a value list item
  ///
  /// <p>Deletes a <code>ValueListItem</code> object, removing it from its parent value list.</p>
  ///
  /// Parameters:
  ///
  /// * [String] item (required):
  Future<DeletedRadarValueListItem?> deleteRadarValueListItemsItem(String item,) async {
    final response = await deleteRadarValueListItemsItemWithHttpInfo(item,);
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'DeletedRadarValueListItem',) as DeletedRadarValueListItem;
    
    }
    return null;
  }

  /// Delete a value list
  ///
  /// <p>Deletes a <code>ValueList</code> object, also deleting any items contained within the value list. To be deleted, a value list must not be referenced in any rules.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] valueList (required):
  Future<Response> deleteRadarValueListsValueListWithHttpInfo(String valueList,) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/radar/value_lists/{value_list}'
      .replaceAll('{value_list}', valueList);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'DELETE',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Delete a value list
  ///
  /// <p>Deletes a <code>ValueList</code> object, also deleting any items contained within the value list. To be deleted, a value list must not be referenced in any rules.</p>
  ///
  /// Parameters:
  ///
  /// * [String] valueList (required):
  Future<DeletedRadarValueList?> deleteRadarValueListsValueList(String valueList,) async {
    final response = await deleteRadarValueListsValueListWithHttpInfo(valueList,);
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'DeletedRadarValueList',) as DeletedRadarValueList;
    
    }
    return null;
  }

  /// Delete a subscription item
  ///
  /// <p>Deletes an item from the subscription. Removing a subscription item from a subscription will not cancel the subscription.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] item (required):
  ///
  /// * [bool] clearUsage:
  ///   Delete all usage for the given subscription item. Allowed only when the current plan's `usage_type` is `metered`.
  ///
  /// * [String] prorationBehavior:
  ///   Determines how to handle [prorations](https://stripe.com/docs/billing/subscriptions/prorations) when the billing cycle changes (e.g., when switching plans, resetting `billing_cycle_anchor=now`, or starting a trial), or if an item's `quantity` changes. The default value is `create_prorations`.
  ///
  /// * [int] prorationDate:
  ///   If set, the proration will be calculated as though the subscription was updated at the given time. This can be used to apply the same proration that was previewed with the [upcoming invoice](https://stripe.com/docs/api#retrieve_customer_invoice) endpoint.
  Future<Response> deleteSubscriptionItemsItemWithHttpInfo(String item, { bool? clearUsage, String? prorationBehavior, int? prorationDate, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/subscription_items/{item}'
      .replaceAll('{item}', item);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (clearUsage != null) {
      formParams[r'clear_usage'] = parameterToString(clearUsage);
    }
    if (prorationBehavior != null) {
      formParams[r'proration_behavior'] = parameterToString(prorationBehavior);
    }
    if (prorationDate != null) {
      formParams[r'proration_date'] = parameterToString(prorationDate);
    }

    return apiClient.invokeAPI(
      path,
      'DELETE',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Delete a subscription item
  ///
  /// <p>Deletes an item from the subscription. Removing a subscription item from a subscription will not cancel the subscription.</p>
  ///
  /// Parameters:
  ///
  /// * [String] item (required):
  ///
  /// * [bool] clearUsage:
  ///   Delete all usage for the given subscription item. Allowed only when the current plan's `usage_type` is `metered`.
  ///
  /// * [String] prorationBehavior:
  ///   Determines how to handle [prorations](https://stripe.com/docs/billing/subscriptions/prorations) when the billing cycle changes (e.g., when switching plans, resetting `billing_cycle_anchor=now`, or starting a trial), or if an item's `quantity` changes. The default value is `create_prorations`.
  ///
  /// * [int] prorationDate:
  ///   If set, the proration will be calculated as though the subscription was updated at the given time. This can be used to apply the same proration that was previewed with the [upcoming invoice](https://stripe.com/docs/api#retrieve_customer_invoice) endpoint.
  Future<DeletedSubscriptionItem?> deleteSubscriptionItemsItem(String item, { bool? clearUsage, String? prorationBehavior, int? prorationDate, }) async {
    final response = await deleteSubscriptionItemsItemWithHttpInfo(item,  clearUsage: clearUsage, prorationBehavior: prorationBehavior, prorationDate: prorationDate, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'DeletedSubscriptionItem',) as DeletedSubscriptionItem;
    
    }
    return null;
  }

  /// Cancel a subscription
  ///
  /// <p>Cancels a customer’s subscription immediately. The customer won’t be charged again for the subscription. After it’s canceled, you can no longer update the subscription or its <a href=\"/metadata\">metadata</a>.</p>  <p>Any pending invoice items that you’ve created are still charged at the end of the period, unless manually <a href=\"#delete_invoiceitem\">deleted</a>. If you’ve set the subscription to cancel at the end of the period, any pending prorations are also left in place and collected at the end of the period. But if the subscription is set to cancel immediately, pending prorations are removed if <code>invoice_now</code> and <code>prorate</code> are both set to true.</p>  <p>By default, upon subscription cancellation, Stripe stops automatic collection of all finalized invoices for the customer. This is intended to prevent unexpected payment attempts after the customer has canceled a subscription. However, you can resume automatic collection of the invoices manually after subscription cancellation to have us proceed. Or, you could check for unpaid invoices before allowing the customer to cancel the subscription at all.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] subscriptionExposedId (required):
  ///
  /// * [CancellationDetailsParam] cancellationDetails:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [bool] invoiceNow:
  ///   Will generate a final invoice that invoices for any un-invoiced metered usage and new/pending proration invoice items. Defaults to `false`.
  ///
  /// * [bool] prorate:
  ///   Will generate a proration invoice item that credits remaining unused time until the subscription period end. Defaults to `false`.
  Future<Response> deleteSubscriptionsSubscriptionExposedIdWithHttpInfo(String subscriptionExposedId, { CancellationDetailsParam? cancellationDetails, List<String>? expand, bool? invoiceNow, bool? prorate, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/subscriptions/{subscription_exposed_id}'
      .replaceAll('{subscription_exposed_id}', subscriptionExposedId);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (cancellationDetails != null) {
      formParams[r'cancellation_details'] = parameterToString(cancellationDetails);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (invoiceNow != null) {
      formParams[r'invoice_now'] = parameterToString(invoiceNow);
    }
    if (prorate != null) {
      formParams[r'prorate'] = parameterToString(prorate);
    }

    return apiClient.invokeAPI(
      path,
      'DELETE',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Cancel a subscription
  ///
  /// <p>Cancels a customer’s subscription immediately. The customer won’t be charged again for the subscription. After it’s canceled, you can no longer update the subscription or its <a href=\"/metadata\">metadata</a>.</p>  <p>Any pending invoice items that you’ve created are still charged at the end of the period, unless manually <a href=\"#delete_invoiceitem\">deleted</a>. If you’ve set the subscription to cancel at the end of the period, any pending prorations are also left in place and collected at the end of the period. But if the subscription is set to cancel immediately, pending prorations are removed if <code>invoice_now</code> and <code>prorate</code> are both set to true.</p>  <p>By default, upon subscription cancellation, Stripe stops automatic collection of all finalized invoices for the customer. This is intended to prevent unexpected payment attempts after the customer has canceled a subscription. However, you can resume automatic collection of the invoices manually after subscription cancellation to have us proceed. Or, you could check for unpaid invoices before allowing the customer to cancel the subscription at all.</p>
  ///
  /// Parameters:
  ///
  /// * [String] subscriptionExposedId (required):
  ///
  /// * [CancellationDetailsParam] cancellationDetails:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [bool] invoiceNow:
  ///   Will generate a final invoice that invoices for any un-invoiced metered usage and new/pending proration invoice items. Defaults to `false`.
  ///
  /// * [bool] prorate:
  ///   Will generate a proration invoice item that credits remaining unused time until the subscription period end. Defaults to `false`.
  Future<Subscription?> deleteSubscriptionsSubscriptionExposedId(String subscriptionExposedId, { CancellationDetailsParam? cancellationDetails, List<String>? expand, bool? invoiceNow, bool? prorate, }) async {
    final response = await deleteSubscriptionsSubscriptionExposedIdWithHttpInfo(subscriptionExposedId,  cancellationDetails: cancellationDetails, expand: expand, invoiceNow: invoiceNow, prorate: prorate, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Subscription',) as Subscription;
    
    }
    return null;
  }

  /// Delete a subscription discount
  ///
  /// <p>Removes the currently applied discount on a subscription.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] subscriptionExposedId (required):
  Future<Response> deleteSubscriptionsSubscriptionExposedIdDiscountWithHttpInfo(String subscriptionExposedId,) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/subscriptions/{subscription_exposed_id}/discount'
      .replaceAll('{subscription_exposed_id}', subscriptionExposedId);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'DELETE',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Delete a subscription discount
  ///
  /// <p>Removes the currently applied discount on a subscription.</p>
  ///
  /// Parameters:
  ///
  /// * [String] subscriptionExposedId (required):
  Future<DeletedDiscount?> deleteSubscriptionsSubscriptionExposedIdDiscount(String subscriptionExposedId,) async {
    final response = await deleteSubscriptionsSubscriptionExposedIdDiscountWithHttpInfo(subscriptionExposedId,);
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'DeletedDiscount',) as DeletedDiscount;
    
    }
    return null;
  }

  /// Delete a tax ID
  ///
  /// <p>Deletes an existing account or customer <code>tax_id</code> object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  Future<Response> deleteTaxIdsIdWithHttpInfo(String id,) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/tax_ids/{id}'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'DELETE',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Delete a tax ID
  ///
  /// <p>Deletes an existing account or customer <code>tax_id</code> object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  Future<DeletedTaxId?> deleteTaxIdsId(String id,) async {
    final response = await deleteTaxIdsIdWithHttpInfo(id,);
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'DeletedTaxId',) as DeletedTaxId;
    
    }
    return null;
  }

  /// Delete a Configuration
  ///
  /// <p>Deletes a <code>Configuration</code> object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] configuration (required):
  Future<Response> deleteTerminalConfigurationsConfigurationWithHttpInfo(String configuration,) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/terminal/configurations/{configuration}'
      .replaceAll('{configuration}', configuration);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'DELETE',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Delete a Configuration
  ///
  /// <p>Deletes a <code>Configuration</code> object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] configuration (required):
  Future<DeletedTerminalConfiguration?> deleteTerminalConfigurationsConfiguration(String configuration,) async {
    final response = await deleteTerminalConfigurationsConfigurationWithHttpInfo(configuration,);
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'DeletedTerminalConfiguration',) as DeletedTerminalConfiguration;
    
    }
    return null;
  }

  /// Delete a Location
  ///
  /// <p>Deletes a <code>Location</code> object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] location (required):
  Future<Response> deleteTerminalLocationsLocationWithHttpInfo(String location,) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/terminal/locations/{location}'
      .replaceAll('{location}', location);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'DELETE',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Delete a Location
  ///
  /// <p>Deletes a <code>Location</code> object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] location (required):
  Future<DeletedTerminalLocation?> deleteTerminalLocationsLocation(String location,) async {
    final response = await deleteTerminalLocationsLocationWithHttpInfo(location,);
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'DeletedTerminalLocation',) as DeletedTerminalLocation;
    
    }
    return null;
  }

  /// Delete a Reader
  ///
  /// <p>Deletes a <code>Reader</code> object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] reader (required):
  Future<Response> deleteTerminalReadersReaderWithHttpInfo(String reader,) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/terminal/readers/{reader}'
      .replaceAll('{reader}', reader);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'DELETE',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Delete a Reader
  ///
  /// <p>Deletes a <code>Reader</code> object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] reader (required):
  Future<DeletedTerminalReader?> deleteTerminalReadersReader(String reader,) async {
    final response = await deleteTerminalReadersReaderWithHttpInfo(reader,);
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'DeletedTerminalReader',) as DeletedTerminalReader;
    
    }
    return null;
  }

  /// Delete a test clock
  ///
  /// <p>Deletes a test clock.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] testClock (required):
  Future<Response> deleteTestHelpersTestClocksTestClockWithHttpInfo(String testClock,) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/test_clocks/{test_clock}'
      .replaceAll('{test_clock}', testClock);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'DELETE',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Delete a test clock
  ///
  /// <p>Deletes a test clock.</p>
  ///
  /// Parameters:
  ///
  /// * [String] testClock (required):
  Future<DeletedTestHelpersTestClock?> deleteTestHelpersTestClocksTestClock(String testClock,) async {
    final response = await deleteTestHelpersTestClocksTestClockWithHttpInfo(testClock,);
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'DeletedTestHelpersTestClock',) as DeletedTestHelpersTestClock;
    
    }
    return null;
  }

  /// Delete a webhook endpoint
  ///
  /// <p>You can also delete webhook endpoints via the <a href=\"https://dashboard.stripe.com/account/webhooks\">webhook endpoint management</a> page of the Stripe dashboard.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] webhookEndpoint (required):
  Future<Response> deleteWebhookEndpointsWebhookEndpointWithHttpInfo(String webhookEndpoint,) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/webhook_endpoints/{webhook_endpoint}'
      .replaceAll('{webhook_endpoint}', webhookEndpoint);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'DELETE',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Delete a webhook endpoint
  ///
  /// <p>You can also delete webhook endpoints via the <a href=\"https://dashboard.stripe.com/account/webhooks\">webhook endpoint management</a> page of the Stripe dashboard.</p>
  ///
  /// Parameters:
  ///
  /// * [String] webhookEndpoint (required):
  Future<DeletedWebhookEndpoint?> deleteWebhookEndpointsWebhookEndpoint(String webhookEndpoint,) async {
    final response = await deleteWebhookEndpointsWebhookEndpointWithHttpInfo(webhookEndpoint,);
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'DeletedWebhookEndpoint',) as DeletedWebhookEndpoint;
    
    }
    return null;
  }

  /// Retrieve account
  ///
  /// <p>Retrieves the details of an account.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getAccountWithHttpInfo({ List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/account';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve account
  ///
  /// <p>Retrieves the details of an account.</p>
  ///
  /// Parameters:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Account?> getAccount({ List<String>? expand, }) async {
    final response = await getAccountWithHttpInfo( expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Account',) as Account;
    
    }
    return null;
  }

  /// List all connected accounts
  ///
  /// <p>Returns a list of accounts connected to your platform via <a href=\"/docs/connect\">Connect</a>. If you’re not a platform, the list is empty.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return connected accounts that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getAccountsWithHttpInfo({ GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/accounts';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all connected accounts
  ///
  /// <p>Returns a list of accounts connected to your platform via <a href=\"/docs/connect\">Connect</a>. If you’re not a platform, the list is empty.</p>
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return connected accounts that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<AccountList?> getAccounts({ GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getAccountsWithHttpInfo( created: created, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'AccountList',) as AccountList;
    
    }
    return null;
  }

  /// Retrieve account
  ///
  /// <p>Retrieves the details of an account.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getAccountsAccountWithHttpInfo(String account, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/accounts/{account}'
      .replaceAll('{account}', account);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve account
  ///
  /// <p>Retrieves the details of an account.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Account?> getAccountsAccount(String account, { List<String>? expand, }) async {
    final response = await getAccountsAccountWithHttpInfo(account,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Account',) as Account;
    
    }
    return null;
  }

  /// Retrieve an external account
  ///
  /// <p>Retrieve a specified external account for a given account.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] id (required):
  ///   Unique identifier for the external account to be retrieved.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getAccountsAccountBankAccountsIdWithHttpInfo(String account, String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/accounts/{account}/bank_accounts/{id}'
      .replaceAll('{account}', account)
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve an external account
  ///
  /// <p>Retrieve a specified external account for a given account.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] id (required):
  ///   Unique identifier for the external account to be retrieved.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<ExternalAccount?> getAccountsAccountBankAccountsId(String account, String id, { List<String>? expand, }) async {
    final response = await getAccountsAccountBankAccountsIdWithHttpInfo(account, id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ExternalAccount',) as ExternalAccount;
    
    }
    return null;
  }

  /// List all account capabilities
  ///
  /// <p>Returns a list of capabilities associated with the account. The capabilities are returned sorted by creation date, with the most recent capability appearing first.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getAccountsAccountCapabilitiesWithHttpInfo(String account, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/accounts/{account}/capabilities'
      .replaceAll('{account}', account);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all account capabilities
  ///
  /// <p>Returns a list of capabilities associated with the account. The capabilities are returned sorted by creation date, with the most recent capability appearing first.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<ListAccountCapability?> getAccountsAccountCapabilities(String account, { List<String>? expand, }) async {
    final response = await getAccountsAccountCapabilitiesWithHttpInfo(account,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ListAccountCapability',) as ListAccountCapability;
    
    }
    return null;
  }

  /// Retrieve an Account Capability
  ///
  /// <p>Retrieves information about the specified Account Capability.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] capability (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getAccountsAccountCapabilitiesCapabilityWithHttpInfo(String account, String capability, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/accounts/{account}/capabilities/{capability}'
      .replaceAll('{account}', account)
      .replaceAll('{capability}', capability);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve an Account Capability
  ///
  /// <p>Retrieves information about the specified Account Capability.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] capability (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Capability?> getAccountsAccountCapabilitiesCapability(String account, String capability, { List<String>? expand, }) async {
    final response = await getAccountsAccountCapabilitiesCapabilityWithHttpInfo(account, capability,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Capability',) as Capability;
    
    }
    return null;
  }

  /// List all external accounts
  ///
  /// <p>List external accounts for an account.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] object:
  ///   Filter external accounts according to a particular object type.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getAccountsAccountExternalAccountsWithHttpInfo(String account, { String? endingBefore, List<String>? expand, int? limit, String? object, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/accounts/{account}/external_accounts'
      .replaceAll('{account}', account);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (object != null) {
      queryParams.addAll(_queryParams('', 'object', object));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all external accounts
  ///
  /// <p>List external accounts for an account.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] object:
  ///   Filter external accounts according to a particular object type.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<ExternalAccountList?> getAccountsAccountExternalAccounts(String account, { String? endingBefore, List<String>? expand, int? limit, String? object, String? startingAfter, }) async {
    final response = await getAccountsAccountExternalAccountsWithHttpInfo(account,  endingBefore: endingBefore, expand: expand, limit: limit, object: object, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ExternalAccountList',) as ExternalAccountList;
    
    }
    return null;
  }

  /// Retrieve an external account
  ///
  /// <p>Retrieve a specified external account for a given account.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] id (required):
  ///   Unique identifier for the external account to be retrieved.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getAccountsAccountExternalAccountsIdWithHttpInfo(String account, String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/accounts/{account}/external_accounts/{id}'
      .replaceAll('{account}', account)
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve an external account
  ///
  /// <p>Retrieve a specified external account for a given account.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] id (required):
  ///   Unique identifier for the external account to be retrieved.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<ExternalAccount?> getAccountsAccountExternalAccountsId(String account, String id, { List<String>? expand, }) async {
    final response = await getAccountsAccountExternalAccountsIdWithHttpInfo(account, id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ExternalAccount',) as ExternalAccount;
    
    }
    return null;
  }

  /// List all persons
  ///
  /// <p>Returns a list of people associated with the account’s legal entity. The people are returned sorted by creation date, with the most recent people appearing first.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [AllPeopleRelationshipSpecs] relationship:
  ///   Filters on the list of people returned based on the person's relationship to the account's company.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getAccountsAccountPeopleWithHttpInfo(String account, { String? endingBefore, List<String>? expand, int? limit, AllPeopleRelationshipSpecs? relationship, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/accounts/{account}/people'
      .replaceAll('{account}', account);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (relationship != null) {
      queryParams.addAll(_queryParams('', 'relationship', relationship));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all persons
  ///
  /// <p>Returns a list of people associated with the account’s legal entity. The people are returned sorted by creation date, with the most recent people appearing first.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [AllPeopleRelationshipSpecs] relationship:
  ///   Filters on the list of people returned based on the person's relationship to the account's company.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<PersonList?> getAccountsAccountPeople(String account, { String? endingBefore, List<String>? expand, int? limit, AllPeopleRelationshipSpecs? relationship, String? startingAfter, }) async {
    final response = await getAccountsAccountPeopleWithHttpInfo(account,  endingBefore: endingBefore, expand: expand, limit: limit, relationship: relationship, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PersonList',) as PersonList;
    
    }
    return null;
  }

  /// Retrieve a person
  ///
  /// <p>Retrieves an existing person.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] person (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getAccountsAccountPeoplePersonWithHttpInfo(String account, String person, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/accounts/{account}/people/{person}'
      .replaceAll('{account}', account)
      .replaceAll('{person}', person);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a person
  ///
  /// <p>Retrieves an existing person.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] person (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Person?> getAccountsAccountPeoplePerson(String account, String person, { List<String>? expand, }) async {
    final response = await getAccountsAccountPeoplePersonWithHttpInfo(account, person,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Person',) as Person;
    
    }
    return null;
  }

  /// List all persons
  ///
  /// <p>Returns a list of people associated with the account’s legal entity. The people are returned sorted by creation date, with the most recent people appearing first.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [AllPeopleRelationshipSpecs] relationship:
  ///   Filters on the list of people returned based on the person's relationship to the account's company.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getAccountsAccountPersonsWithHttpInfo(String account, { String? endingBefore, List<String>? expand, int? limit, AllPeopleRelationshipSpecs? relationship, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/accounts/{account}/persons'
      .replaceAll('{account}', account);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (relationship != null) {
      queryParams.addAll(_queryParams('', 'relationship', relationship));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all persons
  ///
  /// <p>Returns a list of people associated with the account’s legal entity. The people are returned sorted by creation date, with the most recent people appearing first.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [AllPeopleRelationshipSpecs] relationship:
  ///   Filters on the list of people returned based on the person's relationship to the account's company.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<PersonList?> getAccountsAccountPersons(String account, { String? endingBefore, List<String>? expand, int? limit, AllPeopleRelationshipSpecs? relationship, String? startingAfter, }) async {
    final response = await getAccountsAccountPersonsWithHttpInfo(account,  endingBefore: endingBefore, expand: expand, limit: limit, relationship: relationship, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PersonList',) as PersonList;
    
    }
    return null;
  }

  /// Retrieve a person
  ///
  /// <p>Retrieves an existing person.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] person (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getAccountsAccountPersonsPersonWithHttpInfo(String account, String person, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/accounts/{account}/persons/{person}'
      .replaceAll('{account}', account)
      .replaceAll('{person}', person);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a person
  ///
  /// <p>Retrieves an existing person.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] person (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Person?> getAccountsAccountPersonsPerson(String account, String person, { List<String>? expand, }) async {
    final response = await getAccountsAccountPersonsPersonWithHttpInfo(account, person,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Person',) as Person;
    
    }
    return null;
  }

  /// <p>List apple pay domains.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] domainName:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getApplePayDomainsWithHttpInfo({ String? domainName, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/apple_pay/domains';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (domainName != null) {
      queryParams.addAll(_queryParams('', 'domain_name', domainName));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// <p>List apple pay domains.</p>
  ///
  /// Parameters:
  ///
  /// * [String] domainName:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<ApplePayDomainList?> getApplePayDomains({ String? domainName, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getApplePayDomainsWithHttpInfo( domainName: domainName, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ApplePayDomainList',) as ApplePayDomainList;
    
    }
    return null;
  }

  /// <p>Retrieve an apple pay domain.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] domain (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getApplePayDomainsDomainWithHttpInfo(String domain, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/apple_pay/domains/{domain}'
      .replaceAll('{domain}', domain);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// <p>Retrieve an apple pay domain.</p>
  ///
  /// Parameters:
  ///
  /// * [String] domain (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<ApplePayDomain?> getApplePayDomainsDomain(String domain, { List<String>? expand, }) async {
    final response = await getApplePayDomainsDomainWithHttpInfo(domain,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ApplePayDomain',) as ApplePayDomain;
    
    }
    return null;
  }

  /// List all application fees
  ///
  /// <p>Returns a list of application fees you’ve previously collected. The application fees are returned in sorted order, with the most recent fees appearing first.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] charge:
  ///   Only return application fees for the charge specified by this charge ID.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return applications fees that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getApplicationFeesWithHttpInfo({ String? charge, GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/application_fees';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (charge != null) {
      queryParams.addAll(_queryParams('', 'charge', charge));
    }
    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all application fees
  ///
  /// <p>Returns a list of application fees you’ve previously collected. The application fees are returned in sorted order, with the most recent fees appearing first.</p>
  ///
  /// Parameters:
  ///
  /// * [String] charge:
  ///   Only return application fees for the charge specified by this charge ID.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return applications fees that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<PlatformEarningList?> getApplicationFees({ String? charge, GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getApplicationFeesWithHttpInfo( charge: charge, created: created, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PlatformEarningList',) as PlatformEarningList;
    
    }
    return null;
  }

  /// Retrieve an application fee refund
  ///
  /// <p>By default, you can see the 10 most recent refunds stored directly on the application fee object, but you can also retrieve details about a specific refund stored on the application fee.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] fee (required):
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getApplicationFeesFeeRefundsIdWithHttpInfo(String fee, String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/application_fees/{fee}/refunds/{id}'
      .replaceAll('{fee}', fee)
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve an application fee refund
  ///
  /// <p>By default, you can see the 10 most recent refunds stored directly on the application fee object, but you can also retrieve details about a specific refund stored on the application fee.</p>
  ///
  /// Parameters:
  ///
  /// * [String] fee (required):
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<FeeRefund?> getApplicationFeesFeeRefundsId(String fee, String id, { List<String>? expand, }) async {
    final response = await getApplicationFeesFeeRefundsIdWithHttpInfo(fee, id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'FeeRefund',) as FeeRefund;
    
    }
    return null;
  }

  /// Retrieve an application fee
  ///
  /// <p>Retrieves the details of an application fee that your account has collected. The same information is returned when refunding the application fee.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getApplicationFeesIdWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/application_fees/{id}'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve an application fee
  ///
  /// <p>Retrieves the details of an application fee that your account has collected. The same information is returned when refunding the application fee.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<ApplicationFee?> getApplicationFeesId(String id, { List<String>? expand, }) async {
    final response = await getApplicationFeesIdWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ApplicationFee',) as ApplicationFee;
    
    }
    return null;
  }

  /// List all application fee refunds
  ///
  /// <p>You can see a list of the refunds belonging to a specific application fee. Note that the 10 most recent refunds are always available by default on the application fee object. If you need more than those 10, you can use this API method and the <code>limit</code> and <code>starting_after</code> parameters to page through additional refunds.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getApplicationFeesIdRefundsWithHttpInfo(String id, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/application_fees/{id}/refunds'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all application fee refunds
  ///
  /// <p>You can see a list of the refunds belonging to a specific application fee. Note that the 10 most recent refunds are always available by default on the application fee object. If you need more than those 10, you can use this API method and the <code>limit</code> and <code>starting_after</code> parameters to page through additional refunds.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<FeeRefundList?> getApplicationFeesIdRefunds(String id, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getApplicationFeesIdRefundsWithHttpInfo(id,  endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'FeeRefundList',) as FeeRefundList;
    
    }
    return null;
  }

  /// List secrets
  ///
  /// <p>List all secrets stored on the given scope.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [ScopeParam] scope (required):
  ///   Specifies the scoping of the secret. Requests originating from UI extensions can only access account-scoped secrets or secrets scoped to their own user.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getAppsSecretsWithHttpInfo(ScopeParam scope, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/apps/secrets';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
      queryParams.addAll(_queryParams('', 'scope', scope));
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List secrets
  ///
  /// <p>List all secrets stored on the given scope.</p>
  ///
  /// Parameters:
  ///
  /// * [ScopeParam] scope (required):
  ///   Specifies the scoping of the secret. Requests originating from UI extensions can only access account-scoped secrets or secrets scoped to their own user.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<SecretServiceResourceSecretList?> getAppsSecrets(ScopeParam scope, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getAppsSecretsWithHttpInfo(scope,  endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'SecretServiceResourceSecretList',) as SecretServiceResourceSecretList;
    
    }
    return null;
  }

  /// Find a Secret
  ///
  /// <p>Finds a secret in the secret store by name and scope.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] name (required):
  ///   A name for the secret that's unique within the scope.
  ///
  /// * [ScopeParam] scope (required):
  ///   Specifies the scoping of the secret. Requests originating from UI extensions can only access account-scoped secrets or secrets scoped to their own user.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getAppsSecretsFindWithHttpInfo(String name, ScopeParam scope, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/apps/secrets/find';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
      queryParams.addAll(_queryParams('', 'name', name));
      queryParams.addAll(_queryParams('', 'scope', scope));

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Find a Secret
  ///
  /// <p>Finds a secret in the secret store by name and scope.</p>
  ///
  /// Parameters:
  ///
  /// * [String] name (required):
  ///   A name for the secret that's unique within the scope.
  ///
  /// * [ScopeParam] scope (required):
  ///   Specifies the scoping of the secret. Requests originating from UI extensions can only access account-scoped secrets or secrets scoped to their own user.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<AppsSecret?> getAppsSecretsFind(String name, ScopeParam scope, { List<String>? expand, }) async {
    final response = await getAppsSecretsFindWithHttpInfo(name, scope,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'AppsSecret',) as AppsSecret;
    
    }
    return null;
  }

  /// Retrieve balance
  ///
  /// <p>Retrieves the current account balance, based on the authentication that was used to make the request.  For a sample request, see <a href=\"/docs/connect/account-balances#accounting-for-negative-balances\">Accounting for negative balances</a>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getBalanceWithHttpInfo({ List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/balance';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve balance
  ///
  /// <p>Retrieves the current account balance, based on the authentication that was used to make the request.  For a sample request, see <a href=\"/docs/connect/account-balances#accounting-for-negative-balances\">Accounting for negative balances</a>.</p>
  ///
  /// Parameters:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Balance?> getBalance({ List<String>? expand, }) async {
    final response = await getBalanceWithHttpInfo( expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Balance',) as Balance;
    
    }
    return null;
  }

  /// List all balance transactions
  ///
  /// <p>Returns a list of transactions that have contributed to the Stripe account balance (e.g., charges, transfers, and so forth). The transactions are returned in sorted order, with the most recent transactions appearing first.</p>  <p>Note that this endpoint was previously called “Balance history” and used the path <code>/v1/balance/history</code>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return transactions that were created during the given date interval.
  ///
  /// * [String] currency:
  ///   Only return transactions in a certain currency. Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] payout:
  ///   For automatic Stripe payouts only, only returns transactions that were paid out on the specified payout ID.
  ///
  /// * [String] source_:
  ///   Only returns the original transaction.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] type:
  ///   Only returns transactions of the given type. One of: `adjustment`, `advance`, `advance_funding`, `anticipation_repayment`, `application_fee`, `application_fee_refund`, `charge`, `climate_order_purchase`, `climate_order_refund`, `connect_collection_transfer`, `contribution`, `issuing_authorization_hold`, `issuing_authorization_release`, `issuing_dispute`, `issuing_transaction`, `obligation_outbound`, `obligation_reversal_inbound`, `payment`, `payment_failure_refund`, `payment_network_reserve_hold`, `payment_network_reserve_release`, `payment_refund`, `payment_reversal`, `payment_unreconciled`, `payout`, `payout_cancel`, `payout_failure`, `payout_minimum_balance_hold`, `payout_minimum_balance_release`, `refund`, `refund_failure`, `reserve_transaction`, `reserved_funds`, `stripe_fee`, `stripe_fx_fee`, `stripe_balance_payment_debit`, `stripe_balance_payment_debit_reversal`, `tax_fee`, `topup`, `topup_reversal`, `transfer`, `transfer_cancel`, `transfer_failure`, or `transfer_refund`.
  Future<Response> getBalanceHistoryWithHttpInfo({ GetAccountsCreatedParameter? created, String? currency, String? endingBefore, List<String>? expand, int? limit, String? payout, String? source_, String? startingAfter, String? type, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/balance/history';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (currency != null) {
      queryParams.addAll(_queryParams('', 'currency', currency));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (payout != null) {
      queryParams.addAll(_queryParams('', 'payout', payout));
    }
    if (source_ != null) {
      queryParams.addAll(_queryParams('', 'source', source_));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (type != null) {
      queryParams.addAll(_queryParams('', 'type', type));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all balance transactions
  ///
  /// <p>Returns a list of transactions that have contributed to the Stripe account balance (e.g., charges, transfers, and so forth). The transactions are returned in sorted order, with the most recent transactions appearing first.</p>  <p>Note that this endpoint was previously called “Balance history” and used the path <code>/v1/balance/history</code>.</p>
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return transactions that were created during the given date interval.
  ///
  /// * [String] currency:
  ///   Only return transactions in a certain currency. Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] payout:
  ///   For automatic Stripe payouts only, only returns transactions that were paid out on the specified payout ID.
  ///
  /// * [String] source_:
  ///   Only returns the original transaction.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] type:
  ///   Only returns transactions of the given type. One of: `adjustment`, `advance`, `advance_funding`, `anticipation_repayment`, `application_fee`, `application_fee_refund`, `charge`, `climate_order_purchase`, `climate_order_refund`, `connect_collection_transfer`, `contribution`, `issuing_authorization_hold`, `issuing_authorization_release`, `issuing_dispute`, `issuing_transaction`, `obligation_outbound`, `obligation_reversal_inbound`, `payment`, `payment_failure_refund`, `payment_network_reserve_hold`, `payment_network_reserve_release`, `payment_refund`, `payment_reversal`, `payment_unreconciled`, `payout`, `payout_cancel`, `payout_failure`, `payout_minimum_balance_hold`, `payout_minimum_balance_release`, `refund`, `refund_failure`, `reserve_transaction`, `reserved_funds`, `stripe_fee`, `stripe_fx_fee`, `stripe_balance_payment_debit`, `stripe_balance_payment_debit_reversal`, `tax_fee`, `topup`, `topup_reversal`, `transfer`, `transfer_cancel`, `transfer_failure`, or `transfer_refund`.
  Future<BalanceTransactionsList?> getBalanceHistory({ GetAccountsCreatedParameter? created, String? currency, String? endingBefore, List<String>? expand, int? limit, String? payout, String? source_, String? startingAfter, String? type, }) async {
    final response = await getBalanceHistoryWithHttpInfo( created: created, currency: currency, endingBefore: endingBefore, expand: expand, limit: limit, payout: payout, source_: source_, startingAfter: startingAfter, type: type, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BalanceTransactionsList',) as BalanceTransactionsList;
    
    }
    return null;
  }

  /// Retrieve a balance transaction
  ///
  /// <p>Retrieves the balance transaction with the given ID.</p>  <p>Note that this endpoint previously used the path <code>/v1/balance/history/:id</code>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getBalanceHistoryIdWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/balance/history/{id}'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a balance transaction
  ///
  /// <p>Retrieves the balance transaction with the given ID.</p>  <p>Note that this endpoint previously used the path <code>/v1/balance/history/:id</code>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<BalanceTransaction?> getBalanceHistoryId(String id, { List<String>? expand, }) async {
    final response = await getBalanceHistoryIdWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BalanceTransaction',) as BalanceTransaction;
    
    }
    return null;
  }

  /// List all balance transactions
  ///
  /// <p>Returns a list of transactions that have contributed to the Stripe account balance (e.g., charges, transfers, and so forth). The transactions are returned in sorted order, with the most recent transactions appearing first.</p>  <p>Note that this endpoint was previously called “Balance history” and used the path <code>/v1/balance/history</code>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return transactions that were created during the given date interval.
  ///
  /// * [String] currency:
  ///   Only return transactions in a certain currency. Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] payout:
  ///   For automatic Stripe payouts only, only returns transactions that were paid out on the specified payout ID.
  ///
  /// * [String] source_:
  ///   Only returns the original transaction.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] type:
  ///   Only returns transactions of the given type. One of: `adjustment`, `advance`, `advance_funding`, `anticipation_repayment`, `application_fee`, `application_fee_refund`, `charge`, `climate_order_purchase`, `climate_order_refund`, `connect_collection_transfer`, `contribution`, `issuing_authorization_hold`, `issuing_authorization_release`, `issuing_dispute`, `issuing_transaction`, `obligation_outbound`, `obligation_reversal_inbound`, `payment`, `payment_failure_refund`, `payment_network_reserve_hold`, `payment_network_reserve_release`, `payment_refund`, `payment_reversal`, `payment_unreconciled`, `payout`, `payout_cancel`, `payout_failure`, `payout_minimum_balance_hold`, `payout_minimum_balance_release`, `refund`, `refund_failure`, `reserve_transaction`, `reserved_funds`, `stripe_fee`, `stripe_fx_fee`, `stripe_balance_payment_debit`, `stripe_balance_payment_debit_reversal`, `tax_fee`, `topup`, `topup_reversal`, `transfer`, `transfer_cancel`, `transfer_failure`, or `transfer_refund`.
  Future<Response> getBalanceTransactionsWithHttpInfo({ GetAccountsCreatedParameter? created, String? currency, String? endingBefore, List<String>? expand, int? limit, String? payout, String? source_, String? startingAfter, String? type, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/balance_transactions';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (currency != null) {
      queryParams.addAll(_queryParams('', 'currency', currency));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (payout != null) {
      queryParams.addAll(_queryParams('', 'payout', payout));
    }
    if (source_ != null) {
      queryParams.addAll(_queryParams('', 'source', source_));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (type != null) {
      queryParams.addAll(_queryParams('', 'type', type));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all balance transactions
  ///
  /// <p>Returns a list of transactions that have contributed to the Stripe account balance (e.g., charges, transfers, and so forth). The transactions are returned in sorted order, with the most recent transactions appearing first.</p>  <p>Note that this endpoint was previously called “Balance history” and used the path <code>/v1/balance/history</code>.</p>
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return transactions that were created during the given date interval.
  ///
  /// * [String] currency:
  ///   Only return transactions in a certain currency. Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] payout:
  ///   For automatic Stripe payouts only, only returns transactions that were paid out on the specified payout ID.
  ///
  /// * [String] source_:
  ///   Only returns the original transaction.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] type:
  ///   Only returns transactions of the given type. One of: `adjustment`, `advance`, `advance_funding`, `anticipation_repayment`, `application_fee`, `application_fee_refund`, `charge`, `climate_order_purchase`, `climate_order_refund`, `connect_collection_transfer`, `contribution`, `issuing_authorization_hold`, `issuing_authorization_release`, `issuing_dispute`, `issuing_transaction`, `obligation_outbound`, `obligation_reversal_inbound`, `payment`, `payment_failure_refund`, `payment_network_reserve_hold`, `payment_network_reserve_release`, `payment_refund`, `payment_reversal`, `payment_unreconciled`, `payout`, `payout_cancel`, `payout_failure`, `payout_minimum_balance_hold`, `payout_minimum_balance_release`, `refund`, `refund_failure`, `reserve_transaction`, `reserved_funds`, `stripe_fee`, `stripe_fx_fee`, `stripe_balance_payment_debit`, `stripe_balance_payment_debit_reversal`, `tax_fee`, `topup`, `topup_reversal`, `transfer`, `transfer_cancel`, `transfer_failure`, or `transfer_refund`.
  Future<BalanceTransactionsList?> getBalanceTransactions({ GetAccountsCreatedParameter? created, String? currency, String? endingBefore, List<String>? expand, int? limit, String? payout, String? source_, String? startingAfter, String? type, }) async {
    final response = await getBalanceTransactionsWithHttpInfo( created: created, currency: currency, endingBefore: endingBefore, expand: expand, limit: limit, payout: payout, source_: source_, startingAfter: startingAfter, type: type, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BalanceTransactionsList',) as BalanceTransactionsList;
    
    }
    return null;
  }

  /// Retrieve a balance transaction
  ///
  /// <p>Retrieves the balance transaction with the given ID.</p>  <p>Note that this endpoint previously used the path <code>/v1/balance/history/:id</code>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getBalanceTransactionsIdWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/balance_transactions/{id}'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a balance transaction
  ///
  /// <p>Retrieves the balance transaction with the given ID.</p>  <p>Note that this endpoint previously used the path <code>/v1/balance/history/:id</code>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<BalanceTransaction?> getBalanceTransactionsId(String id, { List<String>? expand, }) async {
    final response = await getBalanceTransactionsIdWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BalanceTransaction',) as BalanceTransaction;
    
    }
    return null;
  }

  /// List billing alerts
  ///
  /// <p>Lists billing active and inactive alerts</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] alertType:
  ///   Filter results to only include this type of alert.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] meter:
  ///   Filter results to only include alerts with the given meter.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getBillingAlertsWithHttpInfo({ String? alertType, String? endingBefore, List<String>? expand, int? limit, String? meter, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/billing/alerts';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (alertType != null) {
      queryParams.addAll(_queryParams('', 'alert_type', alertType));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (meter != null) {
      queryParams.addAll(_queryParams('', 'meter', meter));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List billing alerts
  ///
  /// <p>Lists billing active and inactive alerts</p>
  ///
  /// Parameters:
  ///
  /// * [String] alertType:
  ///   Filter results to only include this type of alert.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] meter:
  ///   Filter results to only include alerts with the given meter.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<ThresholdsResourceAlertList?> getBillingAlerts({ String? alertType, String? endingBefore, List<String>? expand, int? limit, String? meter, String? startingAfter, }) async {
    final response = await getBillingAlertsWithHttpInfo( alertType: alertType, endingBefore: endingBefore, expand: expand, limit: limit, meter: meter, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ThresholdsResourceAlertList',) as ThresholdsResourceAlertList;
    
    }
    return null;
  }

  /// Retrieve a billing alert
  ///
  /// <p>Retrieves a billing alert given an ID</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getBillingAlertsIdWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/billing/alerts/{id}'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a billing alert
  ///
  /// <p>Retrieves a billing alert given an ID</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<BillingAlert?> getBillingAlertsId(String id, { List<String>? expand, }) async {
    final response = await getBillingAlertsIdWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BillingAlert',) as BillingAlert;
    
    }
    return null;
  }

  /// Retrieve the credit balance summary for a customer
  ///
  /// <p>Retrieves the credit balance summary for a customer.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///   The customer for which to fetch credit balance summary.
  ///
  /// * [BalanceSummaryFilterParam] filter (required):
  ///   The filter criteria for the credit balance summary.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getBillingCreditBalanceSummaryWithHttpInfo(String customer, BalanceSummaryFilterParam filter, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/billing/credit_balance_summary';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

      queryParams.addAll(_queryParams('', 'customer', customer));
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
      queryParams.addAll(_queryParams('', 'filter', filter));

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve the credit balance summary for a customer
  ///
  /// <p>Retrieves the credit balance summary for a customer.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///   The customer for which to fetch credit balance summary.
  ///
  /// * [BalanceSummaryFilterParam] filter (required):
  ///   The filter criteria for the credit balance summary.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<BillingCreditBalanceSummary?> getBillingCreditBalanceSummary(String customer, BalanceSummaryFilterParam filter, { List<String>? expand, }) async {
    final response = await getBillingCreditBalanceSummaryWithHttpInfo(customer, filter,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BillingCreditBalanceSummary',) as BillingCreditBalanceSummary;
    
    }
    return null;
  }

  /// List credit balance transactions
  ///
  /// <p>Retrieve a list of credit balance transactions.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///   The customer for which to fetch credit balance transactions.
  ///
  /// * [String] creditGrant:
  ///   The credit grant for which to fetch credit balance transactions.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getBillingCreditBalanceTransactionsWithHttpInfo(String customer, { String? creditGrant, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/billing/credit_balance_transactions';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (creditGrant != null) {
      queryParams.addAll(_queryParams('', 'credit_grant', creditGrant));
    }
      queryParams.addAll(_queryParams('', 'customer', customer));
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List credit balance transactions
  ///
  /// <p>Retrieve a list of credit balance transactions.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///   The customer for which to fetch credit balance transactions.
  ///
  /// * [String] creditGrant:
  ///   The credit grant for which to fetch credit balance transactions.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<BillingCreditGrantsResourceBalanceTransactionList?> getBillingCreditBalanceTransactions(String customer, { String? creditGrant, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getBillingCreditBalanceTransactionsWithHttpInfo(customer,  creditGrant: creditGrant, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BillingCreditGrantsResourceBalanceTransactionList',) as BillingCreditGrantsResourceBalanceTransactionList;
    
    }
    return null;
  }

  /// Retrieve a credit balance transaction
  ///
  /// <p>Retrieves a credit balance transaction.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///   Unique identifier for the object.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getBillingCreditBalanceTransactionsIdWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/billing/credit_balance_transactions/{id}'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a credit balance transaction
  ///
  /// <p>Retrieves a credit balance transaction.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///   Unique identifier for the object.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<BillingCreditBalanceTransaction?> getBillingCreditBalanceTransactionsId(String id, { List<String>? expand, }) async {
    final response = await getBillingCreditBalanceTransactionsIdWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BillingCreditBalanceTransaction',) as BillingCreditBalanceTransaction;
    
    }
    return null;
  }

  /// List credit grants
  ///
  /// <p>Retrieve a list of credit grants.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer:
  ///   Only return credit grants for this customer.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getBillingCreditGrantsWithHttpInfo({ String? customer, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/billing/credit_grants';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (customer != null) {
      queryParams.addAll(_queryParams('', 'customer', customer));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List credit grants
  ///
  /// <p>Retrieve a list of credit grants.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer:
  ///   Only return credit grants for this customer.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<BillingCreditGrantsResourceCreditGrantList?> getBillingCreditGrants({ String? customer, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getBillingCreditGrantsWithHttpInfo( customer: customer, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BillingCreditGrantsResourceCreditGrantList',) as BillingCreditGrantsResourceCreditGrantList;
    
    }
    return null;
  }

  /// Retrieve a credit grant
  ///
  /// <p>Retrieves a credit grant.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///   Unique identifier for the object.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getBillingCreditGrantsIdWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/billing/credit_grants/{id}'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a credit grant
  ///
  /// <p>Retrieves a credit grant.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///   Unique identifier for the object.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<BillingCreditGrant?> getBillingCreditGrantsId(String id, { List<String>? expand, }) async {
    final response = await getBillingCreditGrantsIdWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BillingCreditGrant',) as BillingCreditGrant;
    
    }
    return null;
  }

  /// List billing meters
  ///
  /// <p>Retrieve a list of billing meters.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Filter results to only include meters with the given status.
  Future<Response> getBillingMetersWithHttpInfo({ String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? status, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/billing/meters';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (status != null) {
      queryParams.addAll(_queryParams('', 'status', status));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List billing meters
  ///
  /// <p>Retrieve a list of billing meters.</p>
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Filter results to only include meters with the given status.
  Future<BillingMeterResourceBillingMeterList?> getBillingMeters({ String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? status, }) async {
    final response = await getBillingMetersWithHttpInfo( endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, status: status, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BillingMeterResourceBillingMeterList',) as BillingMeterResourceBillingMeterList;
    
    }
    return null;
  }

  /// Retrieve a billing meter
  ///
  /// <p>Retrieves a billing meter given an ID.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///   Unique identifier for the object.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getBillingMetersIdWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/billing/meters/{id}'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a billing meter
  ///
  /// <p>Retrieves a billing meter given an ID.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///   Unique identifier for the object.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<BillingMeter?> getBillingMetersId(String id, { List<String>? expand, }) async {
    final response = await getBillingMetersIdWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BillingMeter',) as BillingMeter;
    
    }
    return null;
  }

  /// List billing meter event summaries
  ///
  /// <p>Retrieve a list of billing meter event summaries.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///   The customer for which to fetch event summaries.
  ///
  /// * [int] endTime (required):
  ///   The timestamp from when to stop aggregating meter events (exclusive). Must be aligned with minute boundaries.
  ///
  /// * [String] id (required):
  ///   Unique identifier for the object.
  ///
  /// * [int] startTime (required):
  ///   The timestamp from when to start aggregating meter events (inclusive). Must be aligned with minute boundaries.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] valueGroupingWindow:
  ///   Specifies what granularity to use when generating event summaries. If not specified, a single event summary would be returned for the specified time range. For hourly granularity, start and end times must align with hour boundaries (e.g., 00:00, 01:00, ..., 23:00). For daily granularity, start and end times must align with UTC day boundaries (00:00 UTC).
  Future<Response> getBillingMetersIdEventSummariesWithHttpInfo(String customer, int endTime, String id, int startTime, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? valueGroupingWindow, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/billing/meters/{id}/event_summaries'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

      queryParams.addAll(_queryParams('', 'customer', customer));
      queryParams.addAll(_queryParams('', 'end_time', endTime));
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
      queryParams.addAll(_queryParams('', 'start_time', startTime));
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (valueGroupingWindow != null) {
      queryParams.addAll(_queryParams('', 'value_grouping_window', valueGroupingWindow));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List billing meter event summaries
  ///
  /// <p>Retrieve a list of billing meter event summaries.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///   The customer for which to fetch event summaries.
  ///
  /// * [int] endTime (required):
  ///   The timestamp from when to stop aggregating meter events (exclusive). Must be aligned with minute boundaries.
  ///
  /// * [String] id (required):
  ///   Unique identifier for the object.
  ///
  /// * [int] startTime (required):
  ///   The timestamp from when to start aggregating meter events (inclusive). Must be aligned with minute boundaries.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] valueGroupingWindow:
  ///   Specifies what granularity to use when generating event summaries. If not specified, a single event summary would be returned for the specified time range. For hourly granularity, start and end times must align with hour boundaries (e.g., 00:00, 01:00, ..., 23:00). For daily granularity, start and end times must align with UTC day boundaries (00:00 UTC).
  Future<BillingMeterResourceBillingMeterEventSummaryList?> getBillingMetersIdEventSummaries(String customer, int endTime, String id, int startTime, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? valueGroupingWindow, }) async {
    final response = await getBillingMetersIdEventSummariesWithHttpInfo(customer, endTime, id, startTime,  endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, valueGroupingWindow: valueGroupingWindow, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BillingMeterResourceBillingMeterEventSummaryList',) as BillingMeterResourceBillingMeterEventSummaryList;
    
    }
    return null;
  }

  /// List portal configurations
  ///
  /// <p>Returns a list of configurations that describe the functionality of the customer portal.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [bool] active:
  ///   Only return configurations that are active or inactive (e.g., pass `true` to only list active configurations).
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [bool] isDefault:
  ///   Only return the default or non-default configurations (e.g., pass `true` to only list the default configuration).
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getBillingPortalConfigurationsWithHttpInfo({ bool? active, String? endingBefore, List<String>? expand, bool? isDefault, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/billing_portal/configurations';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (active != null) {
      queryParams.addAll(_queryParams('', 'active', active));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (isDefault != null) {
      queryParams.addAll(_queryParams('', 'is_default', isDefault));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List portal configurations
  ///
  /// <p>Returns a list of configurations that describe the functionality of the customer portal.</p>
  ///
  /// Parameters:
  ///
  /// * [bool] active:
  ///   Only return configurations that are active or inactive (e.g., pass `true` to only list active configurations).
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [bool] isDefault:
  ///   Only return the default or non-default configurations (e.g., pass `true` to only list the default configuration).
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<PortalPublicResourceConfigurationList?> getBillingPortalConfigurations({ bool? active, String? endingBefore, List<String>? expand, bool? isDefault, int? limit, String? startingAfter, }) async {
    final response = await getBillingPortalConfigurationsWithHttpInfo( active: active, endingBefore: endingBefore, expand: expand, isDefault: isDefault, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PortalPublicResourceConfigurationList',) as PortalPublicResourceConfigurationList;
    
    }
    return null;
  }

  /// Retrieve a portal configuration
  ///
  /// <p>Retrieves a configuration that describes the functionality of the customer portal.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] configuration (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getBillingPortalConfigurationsConfigurationWithHttpInfo(String configuration, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/billing_portal/configurations/{configuration}'
      .replaceAll('{configuration}', configuration);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a portal configuration
  ///
  /// <p>Retrieves a configuration that describes the functionality of the customer portal.</p>
  ///
  /// Parameters:
  ///
  /// * [String] configuration (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<BillingPortalConfiguration?> getBillingPortalConfigurationsConfiguration(String configuration, { List<String>? expand, }) async {
    final response = await getBillingPortalConfigurationsConfigurationWithHttpInfo(configuration,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BillingPortalConfiguration',) as BillingPortalConfiguration;
    
    }
    return null;
  }

  /// List all charges
  ///
  /// <p>Returns a list of charges you’ve previously created. The charges are returned in sorted order, with the most recent charges appearing first.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return charges that were created during the given date interval.
  ///
  /// * [String] customer:
  ///   Only return charges for the customer specified by this customer ID.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] paymentIntent:
  ///   Only return charges that were created by the PaymentIntent specified by this PaymentIntent ID.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] transferGroup:
  ///   Only return charges for this transfer group, limited to 100.
  Future<Response> getChargesWithHttpInfo({ GetAccountsCreatedParameter? created, String? customer, String? endingBefore, List<String>? expand, int? limit, String? paymentIntent, String? startingAfter, String? transferGroup, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/charges';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (customer != null) {
      queryParams.addAll(_queryParams('', 'customer', customer));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (paymentIntent != null) {
      queryParams.addAll(_queryParams('', 'payment_intent', paymentIntent));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (transferGroup != null) {
      queryParams.addAll(_queryParams('', 'transfer_group', transferGroup));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all charges
  ///
  /// <p>Returns a list of charges you’ve previously created. The charges are returned in sorted order, with the most recent charges appearing first.</p>
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return charges that were created during the given date interval.
  ///
  /// * [String] customer:
  ///   Only return charges for the customer specified by this customer ID.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] paymentIntent:
  ///   Only return charges that were created by the PaymentIntent specified by this PaymentIntent ID.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] transferGroup:
  ///   Only return charges for this transfer group, limited to 100.
  Future<ChargeList?> getCharges({ GetAccountsCreatedParameter? created, String? customer, String? endingBefore, List<String>? expand, int? limit, String? paymentIntent, String? startingAfter, String? transferGroup, }) async {
    final response = await getChargesWithHttpInfo( created: created, customer: customer, endingBefore: endingBefore, expand: expand, limit: limit, paymentIntent: paymentIntent, startingAfter: startingAfter, transferGroup: transferGroup, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ChargeList',) as ChargeList;
    
    }
    return null;
  }

  /// Retrieve a charge
  ///
  /// <p>Retrieves the details of a charge that has previously been created. Supply the unique charge ID that was returned from your previous request, and Stripe will return the corresponding charge information. The same information is returned when creating or refunding the charge.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] charge (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getChargesChargeWithHttpInfo(String charge, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/charges/{charge}'
      .replaceAll('{charge}', charge);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a charge
  ///
  /// <p>Retrieves the details of a charge that has previously been created. Supply the unique charge ID that was returned from your previous request, and Stripe will return the corresponding charge information. The same information is returned when creating or refunding the charge.</p>
  ///
  /// Parameters:
  ///
  /// * [String] charge (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Charge?> getChargesCharge(String charge, { List<String>? expand, }) async {
    final response = await getChargesChargeWithHttpInfo(charge,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Charge',) as Charge;
    
    }
    return null;
  }

  /// <p>Retrieve a dispute for a specified charge.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] charge (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getChargesChargeDisputeWithHttpInfo(String charge, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/charges/{charge}/dispute'
      .replaceAll('{charge}', charge);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// <p>Retrieve a dispute for a specified charge.</p>
  ///
  /// Parameters:
  ///
  /// * [String] charge (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Dispute?> getChargesChargeDispute(String charge, { List<String>? expand, }) async {
    final response = await getChargesChargeDisputeWithHttpInfo(charge,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Dispute',) as Dispute;
    
    }
    return null;
  }

  /// List all refunds
  ///
  /// <p>You can see a list of the refunds belonging to a specific charge. Note that the 10 most recent refunds are always available by default on the charge object. If you need more than those 10, you can use this API method and the <code>limit</code> and <code>starting_after</code> parameters to page through additional refunds.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] charge (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getChargesChargeRefundsWithHttpInfo(String charge, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/charges/{charge}/refunds'
      .replaceAll('{charge}', charge);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all refunds
  ///
  /// <p>You can see a list of the refunds belonging to a specific charge. Note that the 10 most recent refunds are always available by default on the charge object. If you need more than those 10, you can use this API method and the <code>limit</code> and <code>starting_after</code> parameters to page through additional refunds.</p>
  ///
  /// Parameters:
  ///
  /// * [String] charge (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<RefundList?> getChargesChargeRefunds(String charge, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getChargesChargeRefundsWithHttpInfo(charge,  endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'RefundList',) as RefundList;
    
    }
    return null;
  }

  /// <p>Retrieves the details of an existing refund.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] charge (required):
  ///
  /// * [String] refund (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getChargesChargeRefundsRefundWithHttpInfo(String charge, String refund, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/charges/{charge}/refunds/{refund}'
      .replaceAll('{charge}', charge)
      .replaceAll('{refund}', refund);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// <p>Retrieves the details of an existing refund.</p>
  ///
  /// Parameters:
  ///
  /// * [String] charge (required):
  ///
  /// * [String] refund (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Refund?> getChargesChargeRefundsRefund(String charge, String refund, { List<String>? expand, }) async {
    final response = await getChargesChargeRefundsRefundWithHttpInfo(charge, refund,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Refund',) as Refund;
    
    }
    return null;
  }

  /// Search charges
  ///
  /// <p>Search for charges you’ve previously created using Stripe’s <a href=\"/docs/search#search-query-language\">Search Query Language</a>. Don’t use search in read-after-write flows where strict consistency is necessary. Under normal operating conditions, data is searchable in less than a minute. Occasionally, propagation of new or updated data can be up to an hour behind during outages. Search functionality is not available to merchants in India.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] query (required):
  ///   The search query string. See [search query language](https://stripe.com/docs/search#search-query-language) and the list of supported [query fields for charges](https://stripe.com/docs/search#query-fields-for-charges).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] page:
  ///   A cursor for pagination across multiple pages of results. Don't include this parameter on the first call. Use the next_page value returned in a previous response to request subsequent results.
  Future<Response> getChargesSearchWithHttpInfo(String query, { List<String>? expand, int? limit, String? page, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/charges/search';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (page != null) {
      queryParams.addAll(_queryParams('', 'page', page));
    }
      queryParams.addAll(_queryParams('', 'query', query));

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Search charges
  ///
  /// <p>Search for charges you’ve previously created using Stripe’s <a href=\"/docs/search#search-query-language\">Search Query Language</a>. Don’t use search in read-after-write flows where strict consistency is necessary. Under normal operating conditions, data is searchable in less than a minute. Occasionally, propagation of new or updated data can be up to an hour behind during outages. Search functionality is not available to merchants in India.</p>
  ///
  /// Parameters:
  ///
  /// * [String] query (required):
  ///   The search query string. See [search query language](https://stripe.com/docs/search#search-query-language) and the list of supported [query fields for charges](https://stripe.com/docs/search#query-fields-for-charges).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] page:
  ///   A cursor for pagination across multiple pages of results. Don't include this parameter on the first call. Use the next_page value returned in a previous response to request subsequent results.
  Future<SearchResult?> getChargesSearch(String query, { List<String>? expand, int? limit, String? page, }) async {
    final response = await getChargesSearchWithHttpInfo(query,  expand: expand, limit: limit, page: page, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'SearchResult',) as SearchResult;
    
    }
    return null;
  }

  /// List all Checkout Sessions
  ///
  /// <p>Returns a list of Checkout Sessions.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return Checkout Sessions that were created during the given date interval.
  ///
  /// * [String] customer:
  ///   Only return the Checkout Sessions for the Customer specified.
  ///
  /// * [CustomerDetailsParams] customerDetails:
  ///   Only return the Checkout Sessions for the Customer details specified.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] paymentIntent:
  ///   Only return the Checkout Session for the PaymentIntent specified.
  ///
  /// * [String] paymentLink:
  ///   Only return the Checkout Sessions for the Payment Link specified.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return the Checkout Sessions matching the given status.
  ///
  /// * [String] subscription:
  ///   Only return the Checkout Session for the subscription specified.
  Future<Response> getCheckoutSessionsWithHttpInfo({ GetAccountsCreatedParameter? created, String? customer, CustomerDetailsParams? customerDetails, String? endingBefore, List<String>? expand, int? limit, String? paymentIntent, String? paymentLink, String? startingAfter, String? status, String? subscription, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/checkout/sessions';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (customer != null) {
      queryParams.addAll(_queryParams('', 'customer', customer));
    }
    if (customerDetails != null) {
      queryParams.addAll(_queryParams('', 'customer_details', customerDetails));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (paymentIntent != null) {
      queryParams.addAll(_queryParams('', 'payment_intent', paymentIntent));
    }
    if (paymentLink != null) {
      queryParams.addAll(_queryParams('', 'payment_link', paymentLink));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (status != null) {
      queryParams.addAll(_queryParams('', 'status', status));
    }
    if (subscription != null) {
      queryParams.addAll(_queryParams('', 'subscription', subscription));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all Checkout Sessions
  ///
  /// <p>Returns a list of Checkout Sessions.</p>
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return Checkout Sessions that were created during the given date interval.
  ///
  /// * [String] customer:
  ///   Only return the Checkout Sessions for the Customer specified.
  ///
  /// * [CustomerDetailsParams] customerDetails:
  ///   Only return the Checkout Sessions for the Customer details specified.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] paymentIntent:
  ///   Only return the Checkout Session for the PaymentIntent specified.
  ///
  /// * [String] paymentLink:
  ///   Only return the Checkout Sessions for the Payment Link specified.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return the Checkout Sessions matching the given status.
  ///
  /// * [String] subscription:
  ///   Only return the Checkout Session for the subscription specified.
  Future<PaymentPagesCheckoutSessionList?> getCheckoutSessions({ GetAccountsCreatedParameter? created, String? customer, CustomerDetailsParams? customerDetails, String? endingBefore, List<String>? expand, int? limit, String? paymentIntent, String? paymentLink, String? startingAfter, String? status, String? subscription, }) async {
    final response = await getCheckoutSessionsWithHttpInfo( created: created, customer: customer, customerDetails: customerDetails, endingBefore: endingBefore, expand: expand, limit: limit, paymentIntent: paymentIntent, paymentLink: paymentLink, startingAfter: startingAfter, status: status, subscription: subscription, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentPagesCheckoutSessionList',) as PaymentPagesCheckoutSessionList;
    
    }
    return null;
  }

  /// Retrieve a Checkout Session
  ///
  /// <p>Retrieves a Checkout Session object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] session (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getCheckoutSessionsSessionWithHttpInfo(String session, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/checkout/sessions/{session}'
      .replaceAll('{session}', session);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a Checkout Session
  ///
  /// <p>Retrieves a Checkout Session object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] session (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<CheckoutSession?> getCheckoutSessionsSession(String session, { List<String>? expand, }) async {
    final response = await getCheckoutSessionsSessionWithHttpInfo(session,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'CheckoutSession',) as CheckoutSession;
    
    }
    return null;
  }

  /// Retrieve a Checkout Session's line items
  ///
  /// <p>When retrieving a Checkout Session, there is an includable <strong>line_items</strong> property containing the first handful of those items. There is also a URL where you can retrieve the full (paginated) list of line items.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] session (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getCheckoutSessionsSessionLineItemsWithHttpInfo(String session, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/checkout/sessions/{session}/line_items'
      .replaceAll('{session}', session);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a Checkout Session's line items
  ///
  /// <p>When retrieving a Checkout Session, there is an includable <strong>line_items</strong> property containing the first handful of those items. There is also a URL where you can retrieve the full (paginated) list of line items.</p>
  ///
  /// Parameters:
  ///
  /// * [String] session (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<PaymentPagesCheckoutSessionListLineItems?> getCheckoutSessionsSessionLineItems(String session, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getCheckoutSessionsSessionLineItemsWithHttpInfo(session,  endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentPagesCheckoutSessionListLineItems',) as PaymentPagesCheckoutSessionListLineItems;
    
    }
    return null;
  }

  /// List orders
  ///
  /// <p>Lists all Climate order objects. The orders are returned sorted by creation date, with the most recently created orders appearing first.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getClimateOrdersWithHttpInfo({ String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/climate/orders';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List orders
  ///
  /// <p>Lists all Climate order objects. The orders are returned sorted by creation date, with the most recently created orders appearing first.</p>
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<ClimateRemovalsOrdersList?> getClimateOrders({ String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getClimateOrdersWithHttpInfo( endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ClimateRemovalsOrdersList',) as ClimateRemovalsOrdersList;
    
    }
    return null;
  }

  /// Retrieve an order
  ///
  /// <p>Retrieves the details of a Climate order object with the given ID.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] order (required):
  ///   Unique identifier of the order.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getClimateOrdersOrderWithHttpInfo(String order, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/climate/orders/{order}'
      .replaceAll('{order}', order);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve an order
  ///
  /// <p>Retrieves the details of a Climate order object with the given ID.</p>
  ///
  /// Parameters:
  ///
  /// * [String] order (required):
  ///   Unique identifier of the order.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<ClimateOrder?> getClimateOrdersOrder(String order, { List<String>? expand, }) async {
    final response = await getClimateOrdersOrderWithHttpInfo(order,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ClimateOrder',) as ClimateOrder;
    
    }
    return null;
  }

  /// List products
  ///
  /// <p>Lists all available Climate product objects.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getClimateProductsWithHttpInfo({ String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/climate/products';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List products
  ///
  /// <p>Lists all available Climate product objects.</p>
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<ClimateRemovalsProductsList?> getClimateProducts({ String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getClimateProductsWithHttpInfo( endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ClimateRemovalsProductsList',) as ClimateRemovalsProductsList;
    
    }
    return null;
  }

  /// Retrieve a product
  ///
  /// <p>Retrieves the details of a Climate product with the given ID.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] product (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getClimateProductsProductWithHttpInfo(String product, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/climate/products/{product}'
      .replaceAll('{product}', product);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a product
  ///
  /// <p>Retrieves the details of a Climate product with the given ID.</p>
  ///
  /// Parameters:
  ///
  /// * [String] product (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<ClimateProduct?> getClimateProductsProduct(String product, { List<String>? expand, }) async {
    final response = await getClimateProductsProductWithHttpInfo(product,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ClimateProduct',) as ClimateProduct;
    
    }
    return null;
  }

  /// List suppliers
  ///
  /// <p>Lists all available Climate supplier objects.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getClimateSuppliersWithHttpInfo({ String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/climate/suppliers';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List suppliers
  ///
  /// <p>Lists all available Climate supplier objects.</p>
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<ClimateRemovalsSuppliersList?> getClimateSuppliers({ String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getClimateSuppliersWithHttpInfo( endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ClimateRemovalsSuppliersList',) as ClimateRemovalsSuppliersList;
    
    }
    return null;
  }

  /// Retrieve a supplier
  ///
  /// <p>Retrieves a Climate supplier object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] supplier (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getClimateSuppliersSupplierWithHttpInfo(String supplier, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/climate/suppliers/{supplier}'
      .replaceAll('{supplier}', supplier);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a supplier
  ///
  /// <p>Retrieves a Climate supplier object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] supplier (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<ClimateSupplier?> getClimateSuppliersSupplier(String supplier, { List<String>? expand, }) async {
    final response = await getClimateSuppliersSupplierWithHttpInfo(supplier,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ClimateSupplier',) as ClimateSupplier;
    
    }
    return null;
  }

  /// Retrieve a ConfirmationToken
  ///
  /// <p>Retrieves an existing ConfirmationToken object</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] confirmationToken (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getConfirmationTokensConfirmationTokenWithHttpInfo(String confirmationToken, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/confirmation_tokens/{confirmation_token}'
      .replaceAll('{confirmation_token}', confirmationToken);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a ConfirmationToken
  ///
  /// <p>Retrieves an existing ConfirmationToken object</p>
  ///
  /// Parameters:
  ///
  /// * [String] confirmationToken (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<ConfirmationToken?> getConfirmationTokensConfirmationToken(String confirmationToken, { List<String>? expand, }) async {
    final response = await getConfirmationTokensConfirmationTokenWithHttpInfo(confirmationToken,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ConfirmationToken',) as ConfirmationToken;
    
    }
    return null;
  }

  /// List Country Specs
  ///
  /// <p>Lists all Country Spec objects available in the API.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getCountrySpecsWithHttpInfo({ String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/country_specs';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List Country Specs
  ///
  /// <p>Lists all Country Spec objects available in the API.</p>
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<CountrySpecList?> getCountrySpecs({ String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getCountrySpecsWithHttpInfo( endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'CountrySpecList',) as CountrySpecList;
    
    }
    return null;
  }

  /// Retrieve a Country Spec
  ///
  /// <p>Returns a Country Spec for a given Country code.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] country (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getCountrySpecsCountryWithHttpInfo(String country, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/country_specs/{country}'
      .replaceAll('{country}', country);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a Country Spec
  ///
  /// <p>Returns a Country Spec for a given Country code.</p>
  ///
  /// Parameters:
  ///
  /// * [String] country (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<CountrySpec?> getCountrySpecsCountry(String country, { List<String>? expand, }) async {
    final response = await getCountrySpecsCountryWithHttpInfo(country,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'CountrySpec',) as CountrySpec;
    
    }
    return null;
  }

  /// List all coupons
  ///
  /// <p>Returns a list of your coupons.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with a number of different query options.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getCouponsWithHttpInfo({ GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/coupons';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all coupons
  ///
  /// <p>Returns a list of your coupons.</p>
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with a number of different query options.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<CouponsResourceCouponList?> getCoupons({ GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getCouponsWithHttpInfo( created: created, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'CouponsResourceCouponList',) as CouponsResourceCouponList;
    
    }
    return null;
  }

  /// Retrieve a coupon
  ///
  /// <p>Retrieves the coupon with the given ID.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] coupon (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getCouponsCouponWithHttpInfo(String coupon, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/coupons/{coupon}'
      .replaceAll('{coupon}', coupon);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a coupon
  ///
  /// <p>Retrieves the coupon with the given ID.</p>
  ///
  /// Parameters:
  ///
  /// * [String] coupon (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Coupon?> getCouponsCoupon(String coupon, { List<String>? expand, }) async {
    final response = await getCouponsCouponWithHttpInfo(coupon,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Coupon',) as Coupon;
    
    }
    return null;
  }

  /// List all credit notes
  ///
  /// <p>Returns a list of credit notes.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return credit notes that were created during the given date interval.
  ///
  /// * [String] customer:
  ///   Only return credit notes for the customer specified by this customer ID.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] invoice:
  ///   Only return credit notes for the invoice specified by this invoice ID.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getCreditNotesWithHttpInfo({ GetAccountsCreatedParameter? created, String? customer, String? endingBefore, List<String>? expand, String? invoice, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/credit_notes';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (customer != null) {
      queryParams.addAll(_queryParams('', 'customer', customer));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (invoice != null) {
      queryParams.addAll(_queryParams('', 'invoice', invoice));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all credit notes
  ///
  /// <p>Returns a list of credit notes.</p>
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return credit notes that were created during the given date interval.
  ///
  /// * [String] customer:
  ///   Only return credit notes for the customer specified by this customer ID.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] invoice:
  ///   Only return credit notes for the invoice specified by this invoice ID.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<CreditNotesList?> getCreditNotes({ GetAccountsCreatedParameter? created, String? customer, String? endingBefore, List<String>? expand, String? invoice, int? limit, String? startingAfter, }) async {
    final response = await getCreditNotesWithHttpInfo( created: created, customer: customer, endingBefore: endingBefore, expand: expand, invoice: invoice, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'CreditNotesList',) as CreditNotesList;
    
    }
    return null;
  }

  /// Retrieve a credit note's line items
  ///
  /// <p>When retrieving a credit note, you’ll get a <strong>lines</strong> property containing the first handful of those items. There is also a URL where you can retrieve the full (paginated) list of line items.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] creditNote (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getCreditNotesCreditNoteLinesWithHttpInfo(String creditNote, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/credit_notes/{credit_note}/lines'
      .replaceAll('{credit_note}', creditNote);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a credit note's line items
  ///
  /// <p>When retrieving a credit note, you’ll get a <strong>lines</strong> property containing the first handful of those items. There is also a URL where you can retrieve the full (paginated) list of line items.</p>
  ///
  /// Parameters:
  ///
  /// * [String] creditNote (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<CreditNoteLinesList?> getCreditNotesCreditNoteLines(String creditNote, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getCreditNotesCreditNoteLinesWithHttpInfo(creditNote,  endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'CreditNoteLinesList',) as CreditNoteLinesList;
    
    }
    return null;
  }

  /// Retrieve a credit note
  ///
  /// <p>Retrieves the credit note object with the given identifier.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getCreditNotesIdWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/credit_notes/{id}'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a credit note
  ///
  /// <p>Retrieves the credit note object with the given identifier.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<CreditNote?> getCreditNotesId(String id, { List<String>? expand, }) async {
    final response = await getCreditNotesIdWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'CreditNote',) as CreditNote;
    
    }
    return null;
  }

  /// Preview a credit note
  ///
  /// <p>Get a preview of a credit note without creating it.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  ///   ID of the invoice.
  ///
  /// * [int] amount:
  ///   The integer amount in cents (or local equivalent) representing the total amount of the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided.
  ///
  /// * [int] creditAmount:
  ///   The integer amount in cents (or local equivalent) representing the amount to credit the customer's balance, which will be automatically applied to their next invoice.
  ///
  /// * [int] effectiveAt:
  ///   The date when this credit note is in effect. Same as `created` unless overwritten. When defined, this value replaces the system-generated 'Date of issue' printed on the credit note PDF.
  ///
  /// * [String] emailType:
  ///   Type of email to send to the customer, one of `credit_note` or `none` and the default is `credit_note`.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [List<CreditNoteLineItemParams>] lines:
  ///   Line items that make up the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided.
  ///
  /// * [String] memo:
  ///   The credit note's memo appears on the credit note PDF.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [int] outOfBandAmount:
  ///   The integer amount in cents (or local equivalent) representing the amount that is credited outside of Stripe.
  ///
  /// * [String] reason:
  ///   Reason for issuing this credit note, one of `duplicate`, `fraudulent`, `order_change`, or `product_unsatisfactory`
  ///
  /// * [int] refundAmount:
  ///   The integer amount in cents (or local equivalent) representing the amount to refund. If set, a refund will be created for the charge associated with the invoice.
  ///
  /// * [List<CreditNoteRefundParams>] refunds:
  ///   Refunds to link to this credit note.
  ///
  /// * [CreditNoteShippingCost1] shippingCost:
  ///   When shipping_cost contains the shipping_rate from the invoice, the shipping_cost is included in the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided.
  Future<Response> getCreditNotesPreviewWithHttpInfo(String invoice, { int? amount, int? creditAmount, int? effectiveAt, String? emailType, List<String>? expand, List<CreditNoteLineItemParams>? lines, String? memo, Map<String, String>? metadata, int? outOfBandAmount, String? reason, int? refundAmount, List<CreditNoteRefundParams>? refunds, CreditNoteShippingCost1? shippingCost, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/credit_notes/preview';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (amount != null) {
      queryParams.addAll(_queryParams('', 'amount', amount));
    }
    if (creditAmount != null) {
      queryParams.addAll(_queryParams('', 'credit_amount', creditAmount));
    }
    if (effectiveAt != null) {
      queryParams.addAll(_queryParams('', 'effective_at', effectiveAt));
    }
    if (emailType != null) {
      queryParams.addAll(_queryParams('', 'email_type', emailType));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
      queryParams.addAll(_queryParams('', 'invoice', invoice));
    if (lines != null) {
      queryParams.addAll(_queryParams('csv', 'lines', lines));
    }
    if (memo != null) {
      queryParams.addAll(_queryParams('', 'memo', memo));
    }
    if (metadata != null) {
      queryParams.addAll(_queryParams('', 'metadata', metadata));
    }
    if (outOfBandAmount != null) {
      queryParams.addAll(_queryParams('', 'out_of_band_amount', outOfBandAmount));
    }
    if (reason != null) {
      queryParams.addAll(_queryParams('', 'reason', reason));
    }
    if (refundAmount != null) {
      queryParams.addAll(_queryParams('', 'refund_amount', refundAmount));
    }
    if (refunds != null) {
      queryParams.addAll(_queryParams('csv', 'refunds', refunds));
    }
    if (shippingCost != null) {
      queryParams.addAll(_queryParams('', 'shipping_cost', shippingCost));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Preview a credit note
  ///
  /// <p>Get a preview of a credit note without creating it.</p>
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  ///   ID of the invoice.
  ///
  /// * [int] amount:
  ///   The integer amount in cents (or local equivalent) representing the total amount of the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided.
  ///
  /// * [int] creditAmount:
  ///   The integer amount in cents (or local equivalent) representing the amount to credit the customer's balance, which will be automatically applied to their next invoice.
  ///
  /// * [int] effectiveAt:
  ///   The date when this credit note is in effect. Same as `created` unless overwritten. When defined, this value replaces the system-generated 'Date of issue' printed on the credit note PDF.
  ///
  /// * [String] emailType:
  ///   Type of email to send to the customer, one of `credit_note` or `none` and the default is `credit_note`.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [List<CreditNoteLineItemParams>] lines:
  ///   Line items that make up the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided.
  ///
  /// * [String] memo:
  ///   The credit note's memo appears on the credit note PDF.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [int] outOfBandAmount:
  ///   The integer amount in cents (or local equivalent) representing the amount that is credited outside of Stripe.
  ///
  /// * [String] reason:
  ///   Reason for issuing this credit note, one of `duplicate`, `fraudulent`, `order_change`, or `product_unsatisfactory`
  ///
  /// * [int] refundAmount:
  ///   The integer amount in cents (or local equivalent) representing the amount to refund. If set, a refund will be created for the charge associated with the invoice.
  ///
  /// * [List<CreditNoteRefundParams>] refunds:
  ///   Refunds to link to this credit note.
  ///
  /// * [CreditNoteShippingCost1] shippingCost:
  ///   When shipping_cost contains the shipping_rate from the invoice, the shipping_cost is included in the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided.
  Future<CreditNote?> getCreditNotesPreview(String invoice, { int? amount, int? creditAmount, int? effectiveAt, String? emailType, List<String>? expand, List<CreditNoteLineItemParams>? lines, String? memo, Map<String, String>? metadata, int? outOfBandAmount, String? reason, int? refundAmount, List<CreditNoteRefundParams>? refunds, CreditNoteShippingCost1? shippingCost, }) async {
    final response = await getCreditNotesPreviewWithHttpInfo(invoice,  amount: amount, creditAmount: creditAmount, effectiveAt: effectiveAt, emailType: emailType, expand: expand, lines: lines, memo: memo, metadata: metadata, outOfBandAmount: outOfBandAmount, reason: reason, refundAmount: refundAmount, refunds: refunds, shippingCost: shippingCost, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'CreditNote',) as CreditNote;
    
    }
    return null;
  }

  /// Retrieve a credit note preview's line items
  ///
  /// <p>When retrieving a credit note preview, you’ll get a <strong>lines</strong> property containing the first handful of those items. This URL you can retrieve the full (paginated) list of line items.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  ///   ID of the invoice.
  ///
  /// * [int] amount:
  ///   The integer amount in cents (or local equivalent) representing the total amount of the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided.
  ///
  /// * [int] creditAmount:
  ///   The integer amount in cents (or local equivalent) representing the amount to credit the customer's balance, which will be automatically applied to their next invoice.
  ///
  /// * [int] effectiveAt:
  ///   The date when this credit note is in effect. Same as `created` unless overwritten. When defined, this value replaces the system-generated 'Date of issue' printed on the credit note PDF.
  ///
  /// * [String] emailType:
  ///   Type of email to send to the customer, one of `credit_note` or `none` and the default is `credit_note`.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [List<CreditNoteLineItemParams>] lines:
  ///   Line items that make up the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided.
  ///
  /// * [String] memo:
  ///   The credit note's memo appears on the credit note PDF.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [int] outOfBandAmount:
  ///   The integer amount in cents (or local equivalent) representing the amount that is credited outside of Stripe.
  ///
  /// * [String] reason:
  ///   Reason for issuing this credit note, one of `duplicate`, `fraudulent`, `order_change`, or `product_unsatisfactory`
  ///
  /// * [int] refundAmount:
  ///   The integer amount in cents (or local equivalent) representing the amount to refund. If set, a refund will be created for the charge associated with the invoice.
  ///
  /// * [List<CreditNoteRefundParams>] refunds:
  ///   Refunds to link to this credit note.
  ///
  /// * [CreditNoteShippingCost1] shippingCost:
  ///   When shipping_cost contains the shipping_rate from the invoice, the shipping_cost is included in the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getCreditNotesPreviewLinesWithHttpInfo(String invoice, { int? amount, int? creditAmount, int? effectiveAt, String? emailType, String? endingBefore, List<String>? expand, int? limit, List<CreditNoteLineItemParams>? lines, String? memo, Map<String, String>? metadata, int? outOfBandAmount, String? reason, int? refundAmount, List<CreditNoteRefundParams>? refunds, CreditNoteShippingCost1? shippingCost, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/credit_notes/preview/lines';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (amount != null) {
      queryParams.addAll(_queryParams('', 'amount', amount));
    }
    if (creditAmount != null) {
      queryParams.addAll(_queryParams('', 'credit_amount', creditAmount));
    }
    if (effectiveAt != null) {
      queryParams.addAll(_queryParams('', 'effective_at', effectiveAt));
    }
    if (emailType != null) {
      queryParams.addAll(_queryParams('', 'email_type', emailType));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
      queryParams.addAll(_queryParams('', 'invoice', invoice));
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (lines != null) {
      queryParams.addAll(_queryParams('csv', 'lines', lines));
    }
    if (memo != null) {
      queryParams.addAll(_queryParams('', 'memo', memo));
    }
    if (metadata != null) {
      queryParams.addAll(_queryParams('', 'metadata', metadata));
    }
    if (outOfBandAmount != null) {
      queryParams.addAll(_queryParams('', 'out_of_band_amount', outOfBandAmount));
    }
    if (reason != null) {
      queryParams.addAll(_queryParams('', 'reason', reason));
    }
    if (refundAmount != null) {
      queryParams.addAll(_queryParams('', 'refund_amount', refundAmount));
    }
    if (refunds != null) {
      queryParams.addAll(_queryParams('csv', 'refunds', refunds));
    }
    if (shippingCost != null) {
      queryParams.addAll(_queryParams('', 'shipping_cost', shippingCost));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a credit note preview's line items
  ///
  /// <p>When retrieving a credit note preview, you’ll get a <strong>lines</strong> property containing the first handful of those items. This URL you can retrieve the full (paginated) list of line items.</p>
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  ///   ID of the invoice.
  ///
  /// * [int] amount:
  ///   The integer amount in cents (or local equivalent) representing the total amount of the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided.
  ///
  /// * [int] creditAmount:
  ///   The integer amount in cents (or local equivalent) representing the amount to credit the customer's balance, which will be automatically applied to their next invoice.
  ///
  /// * [int] effectiveAt:
  ///   The date when this credit note is in effect. Same as `created` unless overwritten. When defined, this value replaces the system-generated 'Date of issue' printed on the credit note PDF.
  ///
  /// * [String] emailType:
  ///   Type of email to send to the customer, one of `credit_note` or `none` and the default is `credit_note`.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [List<CreditNoteLineItemParams>] lines:
  ///   Line items that make up the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided.
  ///
  /// * [String] memo:
  ///   The credit note's memo appears on the credit note PDF.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [int] outOfBandAmount:
  ///   The integer amount in cents (or local equivalent) representing the amount that is credited outside of Stripe.
  ///
  /// * [String] reason:
  ///   Reason for issuing this credit note, one of `duplicate`, `fraudulent`, `order_change`, or `product_unsatisfactory`
  ///
  /// * [int] refundAmount:
  ///   The integer amount in cents (or local equivalent) representing the amount to refund. If set, a refund will be created for the charge associated with the invoice.
  ///
  /// * [List<CreditNoteRefundParams>] refunds:
  ///   Refunds to link to this credit note.
  ///
  /// * [CreditNoteShippingCost1] shippingCost:
  ///   When shipping_cost contains the shipping_rate from the invoice, the shipping_cost is included in the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<CreditNoteLinesList?> getCreditNotesPreviewLines(String invoice, { int? amount, int? creditAmount, int? effectiveAt, String? emailType, String? endingBefore, List<String>? expand, int? limit, List<CreditNoteLineItemParams>? lines, String? memo, Map<String, String>? metadata, int? outOfBandAmount, String? reason, int? refundAmount, List<CreditNoteRefundParams>? refunds, CreditNoteShippingCost1? shippingCost, String? startingAfter, }) async {
    final response = await getCreditNotesPreviewLinesWithHttpInfo(invoice,  amount: amount, creditAmount: creditAmount, effectiveAt: effectiveAt, emailType: emailType, endingBefore: endingBefore, expand: expand, limit: limit, lines: lines, memo: memo, metadata: metadata, outOfBandAmount: outOfBandAmount, reason: reason, refundAmount: refundAmount, refunds: refunds, shippingCost: shippingCost, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'CreditNoteLinesList',) as CreditNoteLinesList;
    
    }
    return null;
  }

  /// List all customers
  ///
  /// <p>Returns a list of your customers. The customers are returned sorted by creation date, with the most recent customers appearing first.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return customers that were created during the given date interval.
  ///
  /// * [String] email:
  ///   A case-sensitive filter on the list based on the customer's `email` field. The value must be a string.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] testClock:
  ///   Provides a list of customers that are associated with the specified test clock. The response will not include customers with test clocks if this parameter is not set.
  Future<Response> getCustomersWithHttpInfo({ GetAccountsCreatedParameter? created, String? email, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? testClock, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (email != null) {
      queryParams.addAll(_queryParams('', 'email', email));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (testClock != null) {
      queryParams.addAll(_queryParams('', 'test_clock', testClock));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all customers
  ///
  /// <p>Returns a list of your customers. The customers are returned sorted by creation date, with the most recent customers appearing first.</p>
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return customers that were created during the given date interval.
  ///
  /// * [String] email:
  ///   A case-sensitive filter on the list based on the customer's `email` field. The value must be a string.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] testClock:
  ///   Provides a list of customers that are associated with the specified test clock. The response will not include customers with test clocks if this parameter is not set.
  Future<CustomerResourceCustomerList?> getCustomers({ GetAccountsCreatedParameter? created, String? email, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? testClock, }) async {
    final response = await getCustomersWithHttpInfo( created: created, email: email, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, testClock: testClock, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'CustomerResourceCustomerList',) as CustomerResourceCustomerList;
    
    }
    return null;
  }

  /// Retrieve a customer
  ///
  /// <p>Retrieves a Customer object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getCustomersCustomerWithHttpInfo(String customer, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}'
      .replaceAll('{customer}', customer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a customer
  ///
  /// <p>Retrieves a Customer object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<GetCustomersCustomer200Response?> getCustomersCustomer(String customer, { List<String>? expand, }) async {
    final response = await getCustomersCustomerWithHttpInfo(customer,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'GetCustomersCustomer200Response',) as GetCustomersCustomer200Response;
    
    }
    return null;
  }

  /// List customer balance transactions
  ///
  /// <p>Returns a list of transactions that updated the customer’s <a href=\"/docs/billing/customer/balance\">balances</a>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getCustomersCustomerBalanceTransactionsWithHttpInfo(String customer, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/balance_transactions'
      .replaceAll('{customer}', customer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List customer balance transactions
  ///
  /// <p>Returns a list of transactions that updated the customer’s <a href=\"/docs/billing/customer/balance\">balances</a>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<CustomerBalanceTransactionList?> getCustomersCustomerBalanceTransactions(String customer, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getCustomersCustomerBalanceTransactionsWithHttpInfo(customer,  endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'CustomerBalanceTransactionList',) as CustomerBalanceTransactionList;
    
    }
    return null;
  }

  /// Retrieve a customer balance transaction
  ///
  /// <p>Retrieves a specific customer balance transaction that updated the customer’s <a href=\"/docs/billing/customer/balance\">balances</a>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] transaction (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getCustomersCustomerBalanceTransactionsTransactionWithHttpInfo(String customer, String transaction, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/balance_transactions/{transaction}'
      .replaceAll('{customer}', customer)
      .replaceAll('{transaction}', transaction);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a customer balance transaction
  ///
  /// <p>Retrieves a specific customer balance transaction that updated the customer’s <a href=\"/docs/billing/customer/balance\">balances</a>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] transaction (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<CustomerBalanceTransaction?> getCustomersCustomerBalanceTransactionsTransaction(String customer, String transaction, { List<String>? expand, }) async {
    final response = await getCustomersCustomerBalanceTransactionsTransactionWithHttpInfo(customer, transaction,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'CustomerBalanceTransaction',) as CustomerBalanceTransaction;
    
    }
    return null;
  }

  /// List all bank accounts
  ///
  /// <p>You can see a list of the bank accounts belonging to a Customer. Note that the 10 most recent sources are always available by default on the Customer. If you need more than those 10, you can use this API method and the <code>limit</code> and <code>starting_after</code> parameters to page through additional bank accounts.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getCustomersCustomerBankAccountsWithHttpInfo(String customer, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/bank_accounts'
      .replaceAll('{customer}', customer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all bank accounts
  ///
  /// <p>You can see a list of the bank accounts belonging to a Customer. Note that the 10 most recent sources are always available by default on the Customer. If you need more than those 10, you can use this API method and the <code>limit</code> and <code>starting_after</code> parameters to page through additional bank accounts.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<BankAccountList?> getCustomersCustomerBankAccounts(String customer, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getCustomersCustomerBankAccountsWithHttpInfo(customer,  endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BankAccountList',) as BankAccountList;
    
    }
    return null;
  }

  /// Retrieve a bank account
  ///
  /// <p>By default, you can see the 10 most recent sources stored on a Customer directly on the object, but you can also retrieve details about a specific bank account stored on the Stripe account.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getCustomersCustomerBankAccountsIdWithHttpInfo(String customer, String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/bank_accounts/{id}'
      .replaceAll('{customer}', customer)
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a bank account
  ///
  /// <p>By default, you can see the 10 most recent sources stored on a Customer directly on the object, but you can also retrieve details about a specific bank account stored on the Stripe account.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<BankAccount?> getCustomersCustomerBankAccountsId(String customer, String id, { List<String>? expand, }) async {
    final response = await getCustomersCustomerBankAccountsIdWithHttpInfo(customer, id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BankAccount',) as BankAccount;
    
    }
    return null;
  }

  /// List all cards
  ///
  /// <p>You can see a list of the cards belonging to a customer. Note that the 10 most recent sources are always available on the <code>Customer</code> object. If you need more than those 10, you can use this API method and the <code>limit</code> and <code>starting_after</code> parameters to page through additional cards.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getCustomersCustomerCardsWithHttpInfo(String customer, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/cards'
      .replaceAll('{customer}', customer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all cards
  ///
  /// <p>You can see a list of the cards belonging to a customer. Note that the 10 most recent sources are always available on the <code>Customer</code> object. If you need more than those 10, you can use this API method and the <code>limit</code> and <code>starting_after</code> parameters to page through additional cards.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<CardList?> getCustomersCustomerCards(String customer, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getCustomersCustomerCardsWithHttpInfo(customer,  endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'CardList',) as CardList;
    
    }
    return null;
  }

  /// Retrieve a card
  ///
  /// <p>You can always see the 10 most recent cards directly on a customer; this method lets you retrieve details about a specific card stored on the customer.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getCustomersCustomerCardsIdWithHttpInfo(String customer, String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/cards/{id}'
      .replaceAll('{customer}', customer)
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a card
  ///
  /// <p>You can always see the 10 most recent cards directly on a customer; this method lets you retrieve details about a specific card stored on the customer.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Card?> getCustomersCustomerCardsId(String customer, String id, { List<String>? expand, }) async {
    final response = await getCustomersCustomerCardsIdWithHttpInfo(customer, id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Card',) as Card;
    
    }
    return null;
  }

  /// Retrieve a cash balance
  ///
  /// <p>Retrieves a customer’s cash balance.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getCustomersCustomerCashBalanceWithHttpInfo(String customer, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/cash_balance'
      .replaceAll('{customer}', customer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a cash balance
  ///
  /// <p>Retrieves a customer’s cash balance.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<CashBalance?> getCustomersCustomerCashBalance(String customer, { List<String>? expand, }) async {
    final response = await getCustomersCustomerCashBalanceWithHttpInfo(customer,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'CashBalance',) as CashBalance;
    
    }
    return null;
  }

  /// List cash balance transactions
  ///
  /// <p>Returns a list of transactions that modified the customer’s <a href=\"/docs/payments/customer-balance\">cash balance</a>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getCustomersCustomerCashBalanceTransactionsWithHttpInfo(String customer, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/cash_balance_transactions'
      .replaceAll('{customer}', customer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List cash balance transactions
  ///
  /// <p>Returns a list of transactions that modified the customer’s <a href=\"/docs/payments/customer-balance\">cash balance</a>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<CustomerCashBalanceTransactionList?> getCustomersCustomerCashBalanceTransactions(String customer, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getCustomersCustomerCashBalanceTransactionsWithHttpInfo(customer,  endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'CustomerCashBalanceTransactionList',) as CustomerCashBalanceTransactionList;
    
    }
    return null;
  }

  /// Retrieve a cash balance transaction
  ///
  /// <p>Retrieves a specific cash balance transaction, which updated the customer’s <a href=\"/docs/payments/customer-balance\">cash balance</a>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] transaction (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getCustomersCustomerCashBalanceTransactionsTransactionWithHttpInfo(String customer, String transaction, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/cash_balance_transactions/{transaction}'
      .replaceAll('{customer}', customer)
      .replaceAll('{transaction}', transaction);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a cash balance transaction
  ///
  /// <p>Retrieves a specific cash balance transaction, which updated the customer’s <a href=\"/docs/payments/customer-balance\">cash balance</a>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] transaction (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<CustomerCashBalanceTransaction?> getCustomersCustomerCashBalanceTransactionsTransaction(String customer, String transaction, { List<String>? expand, }) async {
    final response = await getCustomersCustomerCashBalanceTransactionsTransactionWithHttpInfo(customer, transaction,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'CustomerCashBalanceTransaction',) as CustomerCashBalanceTransaction;
    
    }
    return null;
  }

  /// 
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getCustomersCustomerDiscountWithHttpInfo(String customer, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/discount'
      .replaceAll('{customer}', customer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// 
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Discount?> getCustomersCustomerDiscount(String customer, { List<String>? expand, }) async {
    final response = await getCustomersCustomerDiscountWithHttpInfo(customer,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Discount',) as Discount;
    
    }
    return null;
  }

  /// List a Customer's PaymentMethods
  ///
  /// <p>Returns a list of PaymentMethods for a given Customer</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] allowRedisplay:
  ///   This field indicates whether this payment method can be shown again to its customer in a checkout flow. Stripe products such as Checkout and Elements use this field to determine whether a payment method can be shown as a saved payment method in a checkout flow.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] type:
  ///   An optional filter on the list, based on the object `type` field. Without the filter, the list includes all current and future payment method types. If your integration expects only one type of payment method in the response, make sure to provide a type value in the request.
  Future<Response> getCustomersCustomerPaymentMethodsWithHttpInfo(String customer, { String? allowRedisplay, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? type, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/payment_methods'
      .replaceAll('{customer}', customer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (allowRedisplay != null) {
      queryParams.addAll(_queryParams('', 'allow_redisplay', allowRedisplay));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (type != null) {
      queryParams.addAll(_queryParams('', 'type', type));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List a Customer's PaymentMethods
  ///
  /// <p>Returns a list of PaymentMethods for a given Customer</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] allowRedisplay:
  ///   This field indicates whether this payment method can be shown again to its customer in a checkout flow. Stripe products such as Checkout and Elements use this field to determine whether a payment method can be shown as a saved payment method in a checkout flow.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] type:
  ///   An optional filter on the list, based on the object `type` field. Without the filter, the list includes all current and future payment method types. If your integration expects only one type of payment method in the response, make sure to provide a type value in the request.
  Future<CustomerPaymentMethodResourceList?> getCustomersCustomerPaymentMethods(String customer, { String? allowRedisplay, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? type, }) async {
    final response = await getCustomersCustomerPaymentMethodsWithHttpInfo(customer,  allowRedisplay: allowRedisplay, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, type: type, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'CustomerPaymentMethodResourceList',) as CustomerPaymentMethodResourceList;
    
    }
    return null;
  }

  /// Retrieve a Customer's PaymentMethod
  ///
  /// <p>Retrieves a PaymentMethod object for a given Customer.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] paymentMethod (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getCustomersCustomerPaymentMethodsPaymentMethodWithHttpInfo(String customer, String paymentMethod, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/payment_methods/{payment_method}'
      .replaceAll('{customer}', customer)
      .replaceAll('{payment_method}', paymentMethod);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a Customer's PaymentMethod
  ///
  /// <p>Retrieves a PaymentMethod object for a given Customer.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] paymentMethod (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<PaymentMethod?> getCustomersCustomerPaymentMethodsPaymentMethod(String customer, String paymentMethod, { List<String>? expand, }) async {
    final response = await getCustomersCustomerPaymentMethodsPaymentMethodWithHttpInfo(customer, paymentMethod,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentMethod',) as PaymentMethod;
    
    }
    return null;
  }

  /// <p>List sources for a specified customer.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] object:
  ///   Filter sources according to a particular object type.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getCustomersCustomerSourcesWithHttpInfo(String customer, { String? endingBefore, List<String>? expand, int? limit, String? object, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/sources'
      .replaceAll('{customer}', customer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (object != null) {
      queryParams.addAll(_queryParams('', 'object', object));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// <p>List sources for a specified customer.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] object:
  ///   Filter sources according to a particular object type.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<ApmsSourcesSourceList?> getCustomersCustomerSources(String customer, { String? endingBefore, List<String>? expand, int? limit, String? object, String? startingAfter, }) async {
    final response = await getCustomersCustomerSourcesWithHttpInfo(customer,  endingBefore: endingBefore, expand: expand, limit: limit, object: object, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ApmsSourcesSourceList',) as ApmsSourcesSourceList;
    
    }
    return null;
  }

  /// <p>Retrieve a specified source for a given customer.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getCustomersCustomerSourcesIdWithHttpInfo(String customer, String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/sources/{id}'
      .replaceAll('{customer}', customer)
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// <p>Retrieve a specified source for a given customer.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<PaymentSource?> getCustomersCustomerSourcesId(String customer, String id, { List<String>? expand, }) async {
    final response = await getCustomersCustomerSourcesIdWithHttpInfo(customer, id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentSource',) as PaymentSource;
    
    }
    return null;
  }

  /// List active subscriptions
  ///
  /// <p>You can see a list of the customer’s active subscriptions. Note that the 10 most recent active subscriptions are always available by default on the customer object. If you need more than those 10, you can use the limit and starting_after parameters to page through additional subscriptions.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getCustomersCustomerSubscriptionsWithHttpInfo(String customer, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/subscriptions'
      .replaceAll('{customer}', customer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List active subscriptions
  ///
  /// <p>You can see a list of the customer’s active subscriptions. Note that the 10 most recent active subscriptions are always available by default on the customer object. If you need more than those 10, you can use the limit and starting_after parameters to page through additional subscriptions.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<SubscriptionList?> getCustomersCustomerSubscriptions(String customer, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getCustomersCustomerSubscriptionsWithHttpInfo(customer,  endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'SubscriptionList',) as SubscriptionList;
    
    }
    return null;
  }

  /// Retrieve a subscription
  ///
  /// <p>Retrieves the subscription with the given ID.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] subscriptionExposedId (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getCustomersCustomerSubscriptionsSubscriptionExposedIdWithHttpInfo(String customer, String subscriptionExposedId, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/subscriptions/{subscription_exposed_id}'
      .replaceAll('{customer}', customer)
      .replaceAll('{subscription_exposed_id}', subscriptionExposedId);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a subscription
  ///
  /// <p>Retrieves the subscription with the given ID.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] subscriptionExposedId (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Subscription?> getCustomersCustomerSubscriptionsSubscriptionExposedId(String customer, String subscriptionExposedId, { List<String>? expand, }) async {
    final response = await getCustomersCustomerSubscriptionsSubscriptionExposedIdWithHttpInfo(customer, subscriptionExposedId,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Subscription',) as Subscription;
    
    }
    return null;
  }

  /// 
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] subscriptionExposedId (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getCustomersCustomerSubscriptionsSubscriptionExposedIdDiscountWithHttpInfo(String customer, String subscriptionExposedId, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/subscriptions/{subscription_exposed_id}/discount'
      .replaceAll('{customer}', customer)
      .replaceAll('{subscription_exposed_id}', subscriptionExposedId);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// 
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] subscriptionExposedId (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Discount?> getCustomersCustomerSubscriptionsSubscriptionExposedIdDiscount(String customer, String subscriptionExposedId, { List<String>? expand, }) async {
    final response = await getCustomersCustomerSubscriptionsSubscriptionExposedIdDiscountWithHttpInfo(customer, subscriptionExposedId,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Discount',) as Discount;
    
    }
    return null;
  }

  /// List all Customer tax IDs
  ///
  /// <p>Returns a list of tax IDs for a customer.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getCustomersCustomerTaxIdsWithHttpInfo(String customer, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/tax_ids'
      .replaceAll('{customer}', customer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all Customer tax IDs
  ///
  /// <p>Returns a list of tax IDs for a customer.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<TaxIDsList?> getCustomersCustomerTaxIds(String customer, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getCustomersCustomerTaxIdsWithHttpInfo(customer,  endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TaxIDsList',) as TaxIDsList;
    
    }
    return null;
  }

  /// Retrieve a Customer tax ID
  ///
  /// <p>Retrieves the <code>tax_id</code> object with the given identifier.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getCustomersCustomerTaxIdsIdWithHttpInfo(String customer, String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/tax_ids/{id}'
      .replaceAll('{customer}', customer)
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a Customer tax ID
  ///
  /// <p>Retrieves the <code>tax_id</code> object with the given identifier.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TaxId?> getCustomersCustomerTaxIdsId(String customer, String id, { List<String>? expand, }) async {
    final response = await getCustomersCustomerTaxIdsIdWithHttpInfo(customer, id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TaxId',) as TaxId;
    
    }
    return null;
  }

  /// Search customers
  ///
  /// <p>Search for customers you’ve previously created using Stripe’s <a href=\"/docs/search#search-query-language\">Search Query Language</a>. Don’t use search in read-after-write flows where strict consistency is necessary. Under normal operating conditions, data is searchable in less than a minute. Occasionally, propagation of new or updated data can be up to an hour behind during outages. Search functionality is not available to merchants in India.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] query (required):
  ///   The search query string. See [search query language](https://stripe.com/docs/search#search-query-language) and the list of supported [query fields for customers](https://stripe.com/docs/search#query-fields-for-customers).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] page:
  ///   A cursor for pagination across multiple pages of results. Don't include this parameter on the first call. Use the next_page value returned in a previous response to request subsequent results.
  Future<Response> getCustomersSearchWithHttpInfo(String query, { List<String>? expand, int? limit, String? page, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/search';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (page != null) {
      queryParams.addAll(_queryParams('', 'page', page));
    }
      queryParams.addAll(_queryParams('', 'query', query));

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Search customers
  ///
  /// <p>Search for customers you’ve previously created using Stripe’s <a href=\"/docs/search#search-query-language\">Search Query Language</a>. Don’t use search in read-after-write flows where strict consistency is necessary. Under normal operating conditions, data is searchable in less than a minute. Occasionally, propagation of new or updated data can be up to an hour behind during outages. Search functionality is not available to merchants in India.</p>
  ///
  /// Parameters:
  ///
  /// * [String] query (required):
  ///   The search query string. See [search query language](https://stripe.com/docs/search#search-query-language) and the list of supported [query fields for customers](https://stripe.com/docs/search#query-fields-for-customers).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] page:
  ///   A cursor for pagination across multiple pages of results. Don't include this parameter on the first call. Use the next_page value returned in a previous response to request subsequent results.
  Future<SearchResult1?> getCustomersSearch(String query, { List<String>? expand, int? limit, String? page, }) async {
    final response = await getCustomersSearchWithHttpInfo(query,  expand: expand, limit: limit, page: page, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'SearchResult1',) as SearchResult1;
    
    }
    return null;
  }

  /// List all disputes
  ///
  /// <p>Returns a list of your disputes.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] charge:
  ///   Only return disputes associated to the charge specified by this charge ID.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return disputes that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] paymentIntent:
  ///   Only return disputes associated to the PaymentIntent specified by this PaymentIntent ID.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getDisputesWithHttpInfo({ String? charge, GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? paymentIntent, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/disputes';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (charge != null) {
      queryParams.addAll(_queryParams('', 'charge', charge));
    }
    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (paymentIntent != null) {
      queryParams.addAll(_queryParams('', 'payment_intent', paymentIntent));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all disputes
  ///
  /// <p>Returns a list of your disputes.</p>
  ///
  /// Parameters:
  ///
  /// * [String] charge:
  ///   Only return disputes associated to the charge specified by this charge ID.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return disputes that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] paymentIntent:
  ///   Only return disputes associated to the PaymentIntent specified by this PaymentIntent ID.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<DisputeList?> getDisputes({ String? charge, GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? paymentIntent, String? startingAfter, }) async {
    final response = await getDisputesWithHttpInfo( charge: charge, created: created, endingBefore: endingBefore, expand: expand, limit: limit, paymentIntent: paymentIntent, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'DisputeList',) as DisputeList;
    
    }
    return null;
  }

  /// Retrieve a dispute
  ///
  /// <p>Retrieves the dispute with the given ID.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] dispute (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getDisputesDisputeWithHttpInfo(String dispute, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/disputes/{dispute}'
      .replaceAll('{dispute}', dispute);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a dispute
  ///
  /// <p>Retrieves the dispute with the given ID.</p>
  ///
  /// Parameters:
  ///
  /// * [String] dispute (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Dispute?> getDisputesDispute(String dispute, { List<String>? expand, }) async {
    final response = await getDisputesDisputeWithHttpInfo(dispute,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Dispute',) as Dispute;
    
    }
    return null;
  }

  /// List all active entitlements
  ///
  /// <p>Retrieve a list of active entitlements for a customer</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///   The ID of the customer.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getEntitlementsActiveEntitlementsWithHttpInfo(String customer, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/entitlements/active_entitlements';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

      queryParams.addAll(_queryParams('', 'customer', customer));
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all active entitlements
  ///
  /// <p>Retrieve a list of active entitlements for a customer</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///   The ID of the customer.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<EntitlementsResourceCustomerEntitlementList?> getEntitlementsActiveEntitlements(String customer, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getEntitlementsActiveEntitlementsWithHttpInfo(customer,  endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'EntitlementsResourceCustomerEntitlementList',) as EntitlementsResourceCustomerEntitlementList;
    
    }
    return null;
  }

  /// Retrieve an active entitlement
  ///
  /// <p>Retrieve an active entitlement</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///   The ID of the entitlement.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getEntitlementsActiveEntitlementsIdWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/entitlements/active_entitlements/{id}'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve an active entitlement
  ///
  /// <p>Retrieve an active entitlement</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///   The ID of the entitlement.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<EntitlementsActiveEntitlement?> getEntitlementsActiveEntitlementsId(String id, { List<String>? expand, }) async {
    final response = await getEntitlementsActiveEntitlementsIdWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'EntitlementsActiveEntitlement',) as EntitlementsActiveEntitlement;
    
    }
    return null;
  }

  /// List all features
  ///
  /// <p>Retrieve a list of features</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [bool] archived:
  ///   If set, filter results to only include features with the given archive status.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] lookupKey:
  ///   If set, filter results to only include features with the given lookup_key.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getEntitlementsFeaturesWithHttpInfo({ bool? archived, String? endingBefore, List<String>? expand, int? limit, String? lookupKey, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/entitlements/features';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (archived != null) {
      queryParams.addAll(_queryParams('', 'archived', archived));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (lookupKey != null) {
      queryParams.addAll(_queryParams('', 'lookup_key', lookupKey));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all features
  ///
  /// <p>Retrieve a list of features</p>
  ///
  /// Parameters:
  ///
  /// * [bool] archived:
  ///   If set, filter results to only include features with the given archive status.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] lookupKey:
  ///   If set, filter results to only include features with the given lookup_key.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<EntitlementsResourceFeatureList?> getEntitlementsFeatures({ bool? archived, String? endingBefore, List<String>? expand, int? limit, String? lookupKey, String? startingAfter, }) async {
    final response = await getEntitlementsFeaturesWithHttpInfo( archived: archived, endingBefore: endingBefore, expand: expand, limit: limit, lookupKey: lookupKey, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'EntitlementsResourceFeatureList',) as EntitlementsResourceFeatureList;
    
    }
    return null;
  }

  /// Retrieve a feature
  ///
  /// <p>Retrieves a feature</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///   The ID of the feature.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getEntitlementsFeaturesIdWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/entitlements/features/{id}'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a feature
  ///
  /// <p>Retrieves a feature</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///   The ID of the feature.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<EntitlementsFeature?> getEntitlementsFeaturesId(String id, { List<String>? expand, }) async {
    final response = await getEntitlementsFeaturesIdWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'EntitlementsFeature',) as EntitlementsFeature;
    
    }
    return null;
  }

  /// List all events
  ///
  /// <p>List events, going back up to 30 days. Each event data is rendered according to Stripe API version at its creation time, specified in <a href=\"https://docs.stripe.com/api/events/object\">event object</a> <code>api_version</code> attribute (not according to your current Stripe API version or <code>Stripe-Version</code> header).</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return events that were created during the given date interval.
  ///
  /// * [bool] deliverySuccess:
  ///   Filter events by whether all webhooks were successfully delivered. If false, events which are still pending or have failed all delivery attempts to a webhook endpoint will be returned.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] type:
  ///   A string containing a specific event name, or group of events using * as a wildcard. The list will be filtered to include only events with a matching event property.
  ///
  /// * [List<String>] types:
  ///   An array of up to 20 strings containing specific event names. The list will be filtered to include only events with a matching event property. You may pass either `type` or `types`, but not both.
  Future<Response> getEventsWithHttpInfo({ GetAccountsCreatedParameter? created, bool? deliverySuccess, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? type, List<String>? types, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/events';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (deliverySuccess != null) {
      queryParams.addAll(_queryParams('', 'delivery_success', deliverySuccess));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (type != null) {
      queryParams.addAll(_queryParams('', 'type', type));
    }
    if (types != null) {
      queryParams.addAll(_queryParams('csv', 'types', types));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all events
  ///
  /// <p>List events, going back up to 30 days. Each event data is rendered according to Stripe API version at its creation time, specified in <a href=\"https://docs.stripe.com/api/events/object\">event object</a> <code>api_version</code> attribute (not according to your current Stripe API version or <code>Stripe-Version</code> header).</p>
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return events that were created during the given date interval.
  ///
  /// * [bool] deliverySuccess:
  ///   Filter events by whether all webhooks were successfully delivered. If false, events which are still pending or have failed all delivery attempts to a webhook endpoint will be returned.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] type:
  ///   A string containing a specific event name, or group of events using * as a wildcard. The list will be filtered to include only events with a matching event property.
  ///
  /// * [List<String>] types:
  ///   An array of up to 20 strings containing specific event names. The list will be filtered to include only events with a matching event property. You may pass either `type` or `types`, but not both.
  Future<NotificationEventList?> getEvents({ GetAccountsCreatedParameter? created, bool? deliverySuccess, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? type, List<String>? types, }) async {
    final response = await getEventsWithHttpInfo( created: created, deliverySuccess: deliverySuccess, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, type: type, types: types, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'NotificationEventList',) as NotificationEventList;
    
    }
    return null;
  }

  /// Retrieve an event
  ///
  /// <p>Retrieves the details of an event if it was created in the last 30 days. Supply the unique identifier of the event, which you might have received in a webhook.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getEventsIdWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/events/{id}'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve an event
  ///
  /// <p>Retrieves the details of an event if it was created in the last 30 days. Supply the unique identifier of the event, which you might have received in a webhook.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Event?> getEventsId(String id, { List<String>? expand, }) async {
    final response = await getEventsIdWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Event',) as Event;
    
    }
    return null;
  }

  /// List all exchange rates
  ///
  /// <p>Returns a list of objects that contain the rates at which foreign currencies are converted to one another. Only shows the currencies for which Stripe supports.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is the currency that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with the exchange rate for currency X your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and total number of supported payout currencies, and the default is the max.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is the currency that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with the exchange rate for currency X, your subsequent call can include `starting_after=X` in order to fetch the next page of the list.
  Future<Response> getExchangeRatesWithHttpInfo({ String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/exchange_rates';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all exchange rates
  ///
  /// <p>Returns a list of objects that contain the rates at which foreign currencies are converted to one another. Only shows the currencies for which Stripe supports.</p>
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is the currency that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with the exchange rate for currency X your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and total number of supported payout currencies, and the default is the max.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is the currency that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with the exchange rate for currency X, your subsequent call can include `starting_after=X` in order to fetch the next page of the list.
  Future<ExchangeRateList?> getExchangeRates({ String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getExchangeRatesWithHttpInfo( endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ExchangeRateList',) as ExchangeRateList;
    
    }
    return null;
  }

  /// Retrieve an exchange rate
  ///
  /// <p>Retrieves the exchange rates from the given currency to every supported currency.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] rateId (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getExchangeRatesRateIdWithHttpInfo(String rateId, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/exchange_rates/{rate_id}'
      .replaceAll('{rate_id}', rateId);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve an exchange rate
  ///
  /// <p>Retrieves the exchange rates from the given currency to every supported currency.</p>
  ///
  /// Parameters:
  ///
  /// * [String] rateId (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<ExchangeRate?> getExchangeRatesRateId(String rateId, { List<String>? expand, }) async {
    final response = await getExchangeRatesRateIdWithHttpInfo(rateId,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ExchangeRate',) as ExchangeRate;
    
    }
    return null;
  }

  /// List all file links
  ///
  /// <p>Returns a list of file links.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return links that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [bool] expired:
  ///   Filter links by their expiration status. By default, Stripe returns all links.
  ///
  /// * [String] file:
  ///   Only return links for the given file.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getFileLinksWithHttpInfo({ GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, bool? expired, String? file, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/file_links';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (expired != null) {
      queryParams.addAll(_queryParams('', 'expired', expired));
    }
    if (file != null) {
      queryParams.addAll(_queryParams('', 'file', file));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all file links
  ///
  /// <p>Returns a list of file links.</p>
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return links that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [bool] expired:
  ///   Filter links by their expiration status. By default, Stripe returns all links.
  ///
  /// * [String] file:
  ///   Only return links for the given file.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<FileResourceFileLinkList?> getFileLinks({ GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, bool? expired, String? file, int? limit, String? startingAfter, }) async {
    final response = await getFileLinksWithHttpInfo( created: created, endingBefore: endingBefore, expand: expand, expired: expired, file: file, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'FileResourceFileLinkList',) as FileResourceFileLinkList;
    
    }
    return null;
  }

  /// Retrieve a file link
  ///
  /// <p>Retrieves the file link with the given ID.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] link (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getFileLinksLinkWithHttpInfo(String link, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/file_links/{link}'
      .replaceAll('{link}', link);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a file link
  ///
  /// <p>Retrieves the file link with the given ID.</p>
  ///
  /// Parameters:
  ///
  /// * [String] link (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<FileLink?> getFileLinksLink(String link, { List<String>? expand, }) async {
    final response = await getFileLinksLinkWithHttpInfo(link,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'FileLink',) as FileLink;
    
    }
    return null;
  }

  /// List all files
  ///
  /// <p>Returns a list of the files that your account has access to. Stripe sorts and returns the files by their creation dates, placing the most recently created files at the top.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return files that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] purpose:
  ///   Filter queries by the file purpose. If you don't provide a purpose, the queries return unfiltered files.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getFilesWithHttpInfo({ GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? purpose, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/files';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (purpose != null) {
      queryParams.addAll(_queryParams('', 'purpose', purpose));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all files
  ///
  /// <p>Returns a list of the files that your account has access to. Stripe sorts and returns the files by their creation dates, placing the most recently created files at the top.</p>
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return files that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] purpose:
  ///   Filter queries by the file purpose. If you don't provide a purpose, the queries return unfiltered files.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<FileResourceFileList?> getFiles({ GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? purpose, String? startingAfter, }) async {
    final response = await getFilesWithHttpInfo( created: created, endingBefore: endingBefore, expand: expand, limit: limit, purpose: purpose, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'FileResourceFileList',) as FileResourceFileList;
    
    }
    return null;
  }

  /// Retrieve a file
  ///
  /// <p>Retrieves the details of an existing file object. After you supply a unique file ID, Stripe returns the corresponding file object. Learn how to <a href=\"/docs/file-upload#download-file-contents\">access file contents</a>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] file (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getFilesFileWithHttpInfo(String file, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/files/{file}'
      .replaceAll('{file}', file);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a file
  ///
  /// <p>Retrieves the details of an existing file object. After you supply a unique file ID, Stripe returns the corresponding file object. Learn how to <a href=\"/docs/file-upload#download-file-contents\">access file contents</a>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] file (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<MultipartFile?> getFilesFile(String file, { List<String>? expand, }) async {
    final response = await getFilesFileWithHttpInfo(file,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'MultipartFile',) as MultipartFile;
    
    }
    return null;
  }

  /// List Accounts
  ///
  /// <p>Returns a list of Financial Connections <code>Account</code> objects.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [AccountholderParams] accountHolder:
  ///   If present, only return accounts that belong to the specified account holder. `account_holder[customer]` and `account_holder[account]` are mutually exclusive.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] session:
  ///   If present, only return accounts that were collected as part of the given session.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getFinancialConnectionsAccountsWithHttpInfo({ AccountholderParams? accountHolder, String? endingBefore, List<String>? expand, int? limit, String? session, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/financial_connections/accounts';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (accountHolder != null) {
      queryParams.addAll(_queryParams('', 'account_holder', accountHolder));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (session != null) {
      queryParams.addAll(_queryParams('', 'session', session));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List Accounts
  ///
  /// <p>Returns a list of Financial Connections <code>Account</code> objects.</p>
  ///
  /// Parameters:
  ///
  /// * [AccountholderParams] accountHolder:
  ///   If present, only return accounts that belong to the specified account holder. `account_holder[customer]` and `account_holder[account]` are mutually exclusive.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] session:
  ///   If present, only return accounts that were collected as part of the given session.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<BankConnectionsResourceLinkedAccountList?> getFinancialConnectionsAccounts({ AccountholderParams? accountHolder, String? endingBefore, List<String>? expand, int? limit, String? session, String? startingAfter, }) async {
    final response = await getFinancialConnectionsAccountsWithHttpInfo( accountHolder: accountHolder, endingBefore: endingBefore, expand: expand, limit: limit, session: session, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BankConnectionsResourceLinkedAccountList',) as BankConnectionsResourceLinkedAccountList;
    
    }
    return null;
  }

  /// Retrieve an Account
  ///
  /// <p>Retrieves the details of an Financial Connections <code>Account</code>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getFinancialConnectionsAccountsAccountWithHttpInfo(String account, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/financial_connections/accounts/{account}'
      .replaceAll('{account}', account);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve an Account
  ///
  /// <p>Retrieves the details of an Financial Connections <code>Account</code>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<FinancialConnectionsAccount?> getFinancialConnectionsAccountsAccount(String account, { List<String>? expand, }) async {
    final response = await getFinancialConnectionsAccountsAccountWithHttpInfo(account,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'FinancialConnectionsAccount',) as FinancialConnectionsAccount;
    
    }
    return null;
  }

  /// List Account Owners
  ///
  /// <p>Lists all owners for a given <code>Account</code></p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] ownership (required):
  ///   The ID of the ownership object to fetch owners from.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getFinancialConnectionsAccountsAccountOwnersWithHttpInfo(String account, String ownership, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/financial_connections/accounts/{account}/owners'
      .replaceAll('{account}', account);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
      queryParams.addAll(_queryParams('', 'ownership', ownership));
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List Account Owners
  ///
  /// <p>Lists all owners for a given <code>Account</code></p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] ownership (required):
  ///   The ID of the ownership object to fetch owners from.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<BankConnectionsResourceOwnerList?> getFinancialConnectionsAccountsAccountOwners(String account, String ownership, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getFinancialConnectionsAccountsAccountOwnersWithHttpInfo(account, ownership,  endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BankConnectionsResourceOwnerList',) as BankConnectionsResourceOwnerList;
    
    }
    return null;
  }

  /// Retrieve a Session
  ///
  /// <p>Retrieves the details of a Financial Connections <code>Session</code></p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] session (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getFinancialConnectionsSessionsSessionWithHttpInfo(String session, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/financial_connections/sessions/{session}'
      .replaceAll('{session}', session);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a Session
  ///
  /// <p>Retrieves the details of a Financial Connections <code>Session</code></p>
  ///
  /// Parameters:
  ///
  /// * [String] session (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<FinancialConnectionsSession?> getFinancialConnectionsSessionsSession(String session, { List<String>? expand, }) async {
    final response = await getFinancialConnectionsSessionsSessionWithHttpInfo(session,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'FinancialConnectionsSession',) as FinancialConnectionsSession;
    
    }
    return null;
  }

  /// List Transactions
  ///
  /// <p>Returns a list of Financial Connections <code>Transaction</code> objects.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///   The ID of the Financial Connections Account whose transactions will be retrieved.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [GetAccountsCreatedParameter] transactedAt:
  ///   A filter on the list based on the object `transacted_at` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with the following options:
  ///
  /// * [TransactionRefreshParams] transactionRefresh:
  ///   A filter on the list based on the object `transaction_refresh` field. The value can be a dictionary with the following options:
  Future<Response> getFinancialConnectionsTransactionsWithHttpInfo(String account, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, GetAccountsCreatedParameter? transactedAt, TransactionRefreshParams? transactionRefresh, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/financial_connections/transactions';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

      queryParams.addAll(_queryParams('', 'account', account));
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (transactedAt != null) {
      queryParams.addAll(_queryParams('', 'transacted_at', transactedAt));
    }
    if (transactionRefresh != null) {
      queryParams.addAll(_queryParams('', 'transaction_refresh', transactionRefresh));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List Transactions
  ///
  /// <p>Returns a list of Financial Connections <code>Transaction</code> objects.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///   The ID of the Financial Connections Account whose transactions will be retrieved.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [GetAccountsCreatedParameter] transactedAt:
  ///   A filter on the list based on the object `transacted_at` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with the following options:
  ///
  /// * [TransactionRefreshParams] transactionRefresh:
  ///   A filter on the list based on the object `transaction_refresh` field. The value can be a dictionary with the following options:
  Future<BankConnectionsResourceTransactionList?> getFinancialConnectionsTransactions(String account, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, GetAccountsCreatedParameter? transactedAt, TransactionRefreshParams? transactionRefresh, }) async {
    final response = await getFinancialConnectionsTransactionsWithHttpInfo(account,  endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, transactedAt: transactedAt, transactionRefresh: transactionRefresh, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BankConnectionsResourceTransactionList',) as BankConnectionsResourceTransactionList;
    
    }
    return null;
  }

  /// Retrieve a Transaction
  ///
  /// <p>Retrieves the details of a Financial Connections <code>Transaction</code></p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] transaction (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getFinancialConnectionsTransactionsTransactionWithHttpInfo(String transaction, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/financial_connections/transactions/{transaction}'
      .replaceAll('{transaction}', transaction);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a Transaction
  ///
  /// <p>Retrieves the details of a Financial Connections <code>Transaction</code></p>
  ///
  /// Parameters:
  ///
  /// * [String] transaction (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<FinancialConnectionsTransaction?> getFinancialConnectionsTransactionsTransaction(String transaction, { List<String>? expand, }) async {
    final response = await getFinancialConnectionsTransactionsTransactionWithHttpInfo(transaction,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'FinancialConnectionsTransaction',) as FinancialConnectionsTransaction;
    
    }
    return null;
  }

  /// List all ForwardingRequests
  ///
  /// <p>Lists all ForwardingRequest objects.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [CreatedParam] created:
  ///   Similar to other List endpoints, filters results based on created timestamp. You can pass gt, gte, lt, and lte timestamp values.
  ///
  /// * [String] endingBefore:
  ///   A pagination cursor to fetch the previous page of the list. The value must be a ForwardingRequest ID.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A pagination cursor to fetch the next page of the list. The value must be a ForwardingRequest ID.
  Future<Response> getForwardingRequestsWithHttpInfo({ CreatedParam? created, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/forwarding/requests';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all ForwardingRequests
  ///
  /// <p>Lists all ForwardingRequest objects.</p>
  ///
  /// Parameters:
  ///
  /// * [CreatedParam] created:
  ///   Similar to other List endpoints, filters results based on created timestamp. You can pass gt, gte, lt, and lte timestamp values.
  ///
  /// * [String] endingBefore:
  ///   A pagination cursor to fetch the previous page of the list. The value must be a ForwardingRequest ID.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A pagination cursor to fetch the next page of the list. The value must be a ForwardingRequest ID.
  Future<ForwardingRequestList?> getForwardingRequests({ CreatedParam? created, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getForwardingRequestsWithHttpInfo( created: created, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ForwardingRequestList',) as ForwardingRequestList;
    
    }
    return null;
  }

  /// Retrieve a ForwardingRequest
  ///
  /// <p>Retrieves a ForwardingRequest object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getForwardingRequestsIdWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/forwarding/requests/{id}'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a ForwardingRequest
  ///
  /// <p>Retrieves a ForwardingRequest object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<ForwardingRequest?> getForwardingRequestsId(String id, { List<String>? expand, }) async {
    final response = await getForwardingRequestsIdWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ForwardingRequest',) as ForwardingRequest;
    
    }
    return null;
  }

  /// List VerificationReports
  ///
  /// <p>List all verification reports.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] clientReferenceId:
  ///   A string to reference this user. This can be a customer ID, a session ID, or similar, and can be used to reconcile this verification with your internal systems.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return VerificationReports that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] type:
  ///   Only return VerificationReports of this type
  ///
  /// * [String] verificationSession:
  ///   Only return VerificationReports created by this VerificationSession ID. It is allowed to provide a VerificationIntent ID.
  Future<Response> getIdentityVerificationReportsWithHttpInfo({ String? clientReferenceId, GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? type, String? verificationSession, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/identity/verification_reports';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (clientReferenceId != null) {
      queryParams.addAll(_queryParams('', 'client_reference_id', clientReferenceId));
    }
    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (type != null) {
      queryParams.addAll(_queryParams('', 'type', type));
    }
    if (verificationSession != null) {
      queryParams.addAll(_queryParams('', 'verification_session', verificationSession));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List VerificationReports
  ///
  /// <p>List all verification reports.</p>
  ///
  /// Parameters:
  ///
  /// * [String] clientReferenceId:
  ///   A string to reference this user. This can be a customer ID, a session ID, or similar, and can be used to reconcile this verification with your internal systems.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return VerificationReports that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] type:
  ///   Only return VerificationReports of this type
  ///
  /// * [String] verificationSession:
  ///   Only return VerificationReports created by this VerificationSession ID. It is allowed to provide a VerificationIntent ID.
  Future<GelatoVerificationReportList?> getIdentityVerificationReports({ String? clientReferenceId, GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? type, String? verificationSession, }) async {
    final response = await getIdentityVerificationReportsWithHttpInfo( clientReferenceId: clientReferenceId, created: created, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, type: type, verificationSession: verificationSession, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'GelatoVerificationReportList',) as GelatoVerificationReportList;
    
    }
    return null;
  }

  /// Retrieve a VerificationReport
  ///
  /// <p>Retrieves an existing VerificationReport</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] report (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getIdentityVerificationReportsReportWithHttpInfo(String report, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/identity/verification_reports/{report}'
      .replaceAll('{report}', report);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a VerificationReport
  ///
  /// <p>Retrieves an existing VerificationReport</p>
  ///
  /// Parameters:
  ///
  /// * [String] report (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<IdentityVerificationReport?> getIdentityVerificationReportsReport(String report, { List<String>? expand, }) async {
    final response = await getIdentityVerificationReportsReportWithHttpInfo(report,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IdentityVerificationReport',) as IdentityVerificationReport;
    
    }
    return null;
  }

  /// List VerificationSessions
  ///
  /// <p>Returns a list of VerificationSessions</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] clientReferenceId:
  ///   A string to reference this user. This can be a customer ID, a session ID, or similar, and can be used to reconcile this verification with your internal systems.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return VerificationSessions that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] relatedCustomer:
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return VerificationSessions with this status. [Learn more about the lifecycle of sessions](https://stripe.com/docs/identity/how-sessions-work).
  Future<Response> getIdentityVerificationSessionsWithHttpInfo({ String? clientReferenceId, GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? relatedCustomer, String? startingAfter, String? status, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/identity/verification_sessions';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (clientReferenceId != null) {
      queryParams.addAll(_queryParams('', 'client_reference_id', clientReferenceId));
    }
    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (relatedCustomer != null) {
      queryParams.addAll(_queryParams('', 'related_customer', relatedCustomer));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (status != null) {
      queryParams.addAll(_queryParams('', 'status', status));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List VerificationSessions
  ///
  /// <p>Returns a list of VerificationSessions</p>
  ///
  /// Parameters:
  ///
  /// * [String] clientReferenceId:
  ///   A string to reference this user. This can be a customer ID, a session ID, or similar, and can be used to reconcile this verification with your internal systems.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return VerificationSessions that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] relatedCustomer:
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return VerificationSessions with this status. [Learn more about the lifecycle of sessions](https://stripe.com/docs/identity/how-sessions-work).
  Future<GelatoVerificationSessionList?> getIdentityVerificationSessions({ String? clientReferenceId, GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? relatedCustomer, String? startingAfter, String? status, }) async {
    final response = await getIdentityVerificationSessionsWithHttpInfo( clientReferenceId: clientReferenceId, created: created, endingBefore: endingBefore, expand: expand, limit: limit, relatedCustomer: relatedCustomer, startingAfter: startingAfter, status: status, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'GelatoVerificationSessionList',) as GelatoVerificationSessionList;
    
    }
    return null;
  }

  /// Retrieve a VerificationSession
  ///
  /// <p>Retrieves the details of a VerificationSession that was previously created.</p>  <p>When the session status is <code>requires_input</code>, you can use this method to retrieve a valid <code>client_secret</code> or <code>url</code> to allow re-submission.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] session (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getIdentityVerificationSessionsSessionWithHttpInfo(String session, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/identity/verification_sessions/{session}'
      .replaceAll('{session}', session);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a VerificationSession
  ///
  /// <p>Retrieves the details of a VerificationSession that was previously created.</p>  <p>When the session status is <code>requires_input</code>, you can use this method to retrieve a valid <code>client_secret</code> or <code>url</code> to allow re-submission.</p>
  ///
  /// Parameters:
  ///
  /// * [String] session (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<IdentityVerificationSession?> getIdentityVerificationSessionsSession(String session, { List<String>? expand, }) async {
    final response = await getIdentityVerificationSessionsSessionWithHttpInfo(session,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IdentityVerificationSession',) as IdentityVerificationSession;
    
    }
    return null;
  }

  /// List all payments for an invoice
  ///
  /// <p>When retrieving an invoice, there is an includable payments property containing the first handful of those items. There is also a URL where you can retrieve the full (paginated) list of payments.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] invoice:
  ///   The identifier of the invoice whose payments to return.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [PaymentParam] payment:
  ///   The payment details of the invoice payments to return.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   The status of the invoice payments to return.
  Future<Response> getInvoicePaymentsWithHttpInfo({ String? endingBefore, List<String>? expand, String? invoice, int? limit, PaymentParam? payment, String? startingAfter, String? status, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/invoice_payments';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (invoice != null) {
      queryParams.addAll(_queryParams('', 'invoice', invoice));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (payment != null) {
      queryParams.addAll(_queryParams('', 'payment', payment));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (status != null) {
      queryParams.addAll(_queryParams('', 'status', status));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all payments for an invoice
  ///
  /// <p>When retrieving an invoice, there is an includable payments property containing the first handful of those items. There is also a URL where you can retrieve the full (paginated) list of payments.</p>
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] invoice:
  ///   The identifier of the invoice whose payments to return.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [PaymentParam] payment:
  ///   The payment details of the invoice payments to return.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   The status of the invoice payments to return.
  Future<InvoicesPaymentsListInvoicePayments?> getInvoicePayments({ String? endingBefore, List<String>? expand, String? invoice, int? limit, PaymentParam? payment, String? startingAfter, String? status, }) async {
    final response = await getInvoicePaymentsWithHttpInfo( endingBefore: endingBefore, expand: expand, invoice: invoice, limit: limit, payment: payment, startingAfter: startingAfter, status: status, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'InvoicesPaymentsListInvoicePayments',) as InvoicesPaymentsListInvoicePayments;
    
    }
    return null;
  }

  /// Retrieve an InvoicePayment
  ///
  /// <p>Retrieves the invoice payment with the given ID.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] invoicePayment (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getInvoicePaymentsInvoicePaymentWithHttpInfo(String invoicePayment, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/invoice_payments/{invoice_payment}'
      .replaceAll('{invoice_payment}', invoicePayment);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve an InvoicePayment
  ///
  /// <p>Retrieves the invoice payment with the given ID.</p>
  ///
  /// Parameters:
  ///
  /// * [String] invoicePayment (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<InvoicePayment?> getInvoicePaymentsInvoicePayment(String invoicePayment, { List<String>? expand, }) async {
    final response = await getInvoicePaymentsInvoicePaymentWithHttpInfo(invoicePayment,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'InvoicePayment',) as InvoicePayment;
    
    }
    return null;
  }

  /// List all invoice rendering templates
  ///
  /// <p>List all templates, ordered by creation date, with the most recently created template appearing first.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  Future<Response> getInvoiceRenderingTemplatesWithHttpInfo({ String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? status, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/invoice_rendering_templates';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (status != null) {
      queryParams.addAll(_queryParams('', 'status', status));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all invoice rendering templates
  ///
  /// <p>List all templates, ordered by creation date, with the most recently created template appearing first.</p>
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  Future<InvoiceRenderingTemplatesList?> getInvoiceRenderingTemplates({ String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? status, }) async {
    final response = await getInvoiceRenderingTemplatesWithHttpInfo( endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, status: status, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'InvoiceRenderingTemplatesList',) as InvoiceRenderingTemplatesList;
    
    }
    return null;
  }

  /// Retrieve an invoice rendering template
  ///
  /// <p>Retrieves an invoice rendering template with the given ID. It by default returns the latest version of the template. Optionally, specify a version to see previous versions.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] template (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] version:
  Future<Response> getInvoiceRenderingTemplatesTemplateWithHttpInfo(String template, { List<String>? expand, int? version, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/invoice_rendering_templates/{template}'
      .replaceAll('{template}', template);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (version != null) {
      queryParams.addAll(_queryParams('', 'version', version));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve an invoice rendering template
  ///
  /// <p>Retrieves an invoice rendering template with the given ID. It by default returns the latest version of the template. Optionally, specify a version to see previous versions.</p>
  ///
  /// Parameters:
  ///
  /// * [String] template (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] version:
  Future<InvoiceRenderingTemplate?> getInvoiceRenderingTemplatesTemplate(String template, { List<String>? expand, int? version, }) async {
    final response = await getInvoiceRenderingTemplatesTemplateWithHttpInfo(template,  expand: expand, version: version, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'InvoiceRenderingTemplate',) as InvoiceRenderingTemplate;
    
    }
    return null;
  }

  /// List all invoice items
  ///
  /// <p>Returns a list of your invoice items. Invoice items are returned sorted by creation date, with the most recently created invoice items appearing first.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return invoice items that were created during the given date interval.
  ///
  /// * [String] customer:
  ///   The identifier of the customer whose invoice items to return. If none is provided, all invoice items will be returned.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] invoice:
  ///   Only return invoice items belonging to this invoice. If none is provided, all invoice items will be returned. If specifying an invoice, no customer identifier is needed.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [bool] pending:
  ///   Set to `true` to only show pending invoice items, which are not yet attached to any invoices. Set to `false` to only show invoice items already attached to invoices. If unspecified, no filter is applied.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getInvoiceitemsWithHttpInfo({ GetAccountsCreatedParameter? created, String? customer, String? endingBefore, List<String>? expand, String? invoice, int? limit, bool? pending, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/invoiceitems';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (customer != null) {
      queryParams.addAll(_queryParams('', 'customer', customer));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (invoice != null) {
      queryParams.addAll(_queryParams('', 'invoice', invoice));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (pending != null) {
      queryParams.addAll(_queryParams('', 'pending', pending));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all invoice items
  ///
  /// <p>Returns a list of your invoice items. Invoice items are returned sorted by creation date, with the most recently created invoice items appearing first.</p>
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return invoice items that were created during the given date interval.
  ///
  /// * [String] customer:
  ///   The identifier of the customer whose invoice items to return. If none is provided, all invoice items will be returned.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] invoice:
  ///   Only return invoice items belonging to this invoice. If none is provided, all invoice items will be returned. If specifying an invoice, no customer identifier is needed.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [bool] pending:
  ///   Set to `true` to only show pending invoice items, which are not yet attached to any invoices. Set to `false` to only show invoice items already attached to invoices. If unspecified, no filter is applied.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<InvoicesItemsList?> getInvoiceitems({ GetAccountsCreatedParameter? created, String? customer, String? endingBefore, List<String>? expand, String? invoice, int? limit, bool? pending, String? startingAfter, }) async {
    final response = await getInvoiceitemsWithHttpInfo( created: created, customer: customer, endingBefore: endingBefore, expand: expand, invoice: invoice, limit: limit, pending: pending, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'InvoicesItemsList',) as InvoicesItemsList;
    
    }
    return null;
  }

  /// Retrieve an invoice item
  ///
  /// <p>Retrieves the invoice item with the given ID.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] invoiceitem (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getInvoiceitemsInvoiceitemWithHttpInfo(String invoiceitem, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/invoiceitems/{invoiceitem}'
      .replaceAll('{invoiceitem}', invoiceitem);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve an invoice item
  ///
  /// <p>Retrieves the invoice item with the given ID.</p>
  ///
  /// Parameters:
  ///
  /// * [String] invoiceitem (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Invoiceitem?> getInvoiceitemsInvoiceitem(String invoiceitem, { List<String>? expand, }) async {
    final response = await getInvoiceitemsInvoiceitemWithHttpInfo(invoiceitem,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Invoiceitem',) as Invoiceitem;
    
    }
    return null;
  }

  /// List all invoices
  ///
  /// <p>You can list all invoices, or list the invoices for a specific customer. The invoices are returned sorted by creation date, with the most recently created invoices appearing first.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] collectionMethod:
  ///   The collection method of the invoice to retrieve. Either `charge_automatically` or `send_invoice`.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return invoices that were created during the given date interval.
  ///
  /// * [String] customer:
  ///   Only return invoices for the customer specified by this customer ID.
  ///
  /// * [GetAccountsCreatedParameter] dueDate:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   The status of the invoice, one of `draft`, `open`, `paid`, `uncollectible`, or `void`. [Learn more](https://stripe.com/docs/billing/invoices/workflow#workflow-overview)
  ///
  /// * [String] subscription:
  ///   Only return invoices for the subscription specified by this subscription ID.
  Future<Response> getInvoicesWithHttpInfo({ String? collectionMethod, GetAccountsCreatedParameter? created, String? customer, GetAccountsCreatedParameter? dueDate, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? status, String? subscription, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/invoices';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (collectionMethod != null) {
      queryParams.addAll(_queryParams('', 'collection_method', collectionMethod));
    }
    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (customer != null) {
      queryParams.addAll(_queryParams('', 'customer', customer));
    }
    if (dueDate != null) {
      queryParams.addAll(_queryParams('', 'due_date', dueDate));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (status != null) {
      queryParams.addAll(_queryParams('', 'status', status));
    }
    if (subscription != null) {
      queryParams.addAll(_queryParams('', 'subscription', subscription));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all invoices
  ///
  /// <p>You can list all invoices, or list the invoices for a specific customer. The invoices are returned sorted by creation date, with the most recently created invoices appearing first.</p>
  ///
  /// Parameters:
  ///
  /// * [String] collectionMethod:
  ///   The collection method of the invoice to retrieve. Either `charge_automatically` or `send_invoice`.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return invoices that were created during the given date interval.
  ///
  /// * [String] customer:
  ///   Only return invoices for the customer specified by this customer ID.
  ///
  /// * [GetAccountsCreatedParameter] dueDate:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   The status of the invoice, one of `draft`, `open`, `paid`, `uncollectible`, or `void`. [Learn more](https://stripe.com/docs/billing/invoices/workflow#workflow-overview)
  ///
  /// * [String] subscription:
  ///   Only return invoices for the subscription specified by this subscription ID.
  Future<InvoicesResourceList?> getInvoices({ String? collectionMethod, GetAccountsCreatedParameter? created, String? customer, GetAccountsCreatedParameter? dueDate, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? status, String? subscription, }) async {
    final response = await getInvoicesWithHttpInfo( collectionMethod: collectionMethod, created: created, customer: customer, dueDate: dueDate, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, status: status, subscription: subscription, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'InvoicesResourceList',) as InvoicesResourceList;
    
    }
    return null;
  }

  /// Retrieve an invoice
  ///
  /// <p>Retrieves the invoice with the given ID.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getInvoicesInvoiceWithHttpInfo(String invoice, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/invoices/{invoice}'
      .replaceAll('{invoice}', invoice);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve an invoice
  ///
  /// <p>Retrieves the invoice with the given ID.</p>
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Invoice?> getInvoicesInvoice(String invoice, { List<String>? expand, }) async {
    final response = await getInvoicesInvoiceWithHttpInfo(invoice,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Invoice',) as Invoice;
    
    }
    return null;
  }

  /// Retrieve an invoice's line items
  ///
  /// <p>When retrieving an invoice, you’ll get a <strong>lines</strong> property containing the total count of line items and the first handful of those items. There is also a URL where you can retrieve the full (paginated) list of line items.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getInvoicesInvoiceLinesWithHttpInfo(String invoice, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/invoices/{invoice}/lines'
      .replaceAll('{invoice}', invoice);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve an invoice's line items
  ///
  /// <p>When retrieving an invoice, you’ll get a <strong>lines</strong> property containing the total count of line items and the first handful of those items. There is also a URL where you can retrieve the full (paginated) list of line items.</p>
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<InvoiceLinesList?> getInvoicesInvoiceLines(String invoice, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getInvoicesInvoiceLinesWithHttpInfo(invoice,  endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'InvoiceLinesList',) as InvoiceLinesList;
    
    }
    return null;
  }

  /// Search invoices
  ///
  /// <p>Search for invoices you’ve previously created using Stripe’s <a href=\"/docs/search#search-query-language\">Search Query Language</a>. Don’t use search in read-after-write flows where strict consistency is necessary. Under normal operating conditions, data is searchable in less than a minute. Occasionally, propagation of new or updated data can be up to an hour behind during outages. Search functionality is not available to merchants in India.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] query (required):
  ///   The search query string. See [search query language](https://stripe.com/docs/search#search-query-language) and the list of supported [query fields for invoices](https://stripe.com/docs/search#query-fields-for-invoices).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] page:
  ///   A cursor for pagination across multiple pages of results. Don't include this parameter on the first call. Use the next_page value returned in a previous response to request subsequent results.
  Future<Response> getInvoicesSearchWithHttpInfo(String query, { List<String>? expand, int? limit, String? page, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/invoices/search';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (page != null) {
      queryParams.addAll(_queryParams('', 'page', page));
    }
      queryParams.addAll(_queryParams('', 'query', query));

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Search invoices
  ///
  /// <p>Search for invoices you’ve previously created using Stripe’s <a href=\"/docs/search#search-query-language\">Search Query Language</a>. Don’t use search in read-after-write flows where strict consistency is necessary. Under normal operating conditions, data is searchable in less than a minute. Occasionally, propagation of new or updated data can be up to an hour behind during outages. Search functionality is not available to merchants in India.</p>
  ///
  /// Parameters:
  ///
  /// * [String] query (required):
  ///   The search query string. See [search query language](https://stripe.com/docs/search#search-query-language) and the list of supported [query fields for invoices](https://stripe.com/docs/search#query-fields-for-invoices).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] page:
  ///   A cursor for pagination across multiple pages of results. Don't include this parameter on the first call. Use the next_page value returned in a previous response to request subsequent results.
  Future<SearchResult2?> getInvoicesSearch(String query, { List<String>? expand, int? limit, String? page, }) async {
    final response = await getInvoicesSearchWithHttpInfo(query,  expand: expand, limit: limit, page: page, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'SearchResult2',) as SearchResult2;
    
    }
    return null;
  }

  /// List all authorizations
  ///
  /// <p>Returns a list of Issuing <code>Authorization</code> objects. The objects are sorted in descending order by creation date, with the most recently created object appearing first.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] card:
  ///   Only return authorizations that belong to the given card.
  ///
  /// * [String] cardholder:
  ///   Only return authorizations that belong to the given cardholder.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return authorizations that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return authorizations with the given status. One of `pending`, `closed`, or `reversed`.
  Future<Response> getIssuingAuthorizationsWithHttpInfo({ String? card, String? cardholder, GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? status, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/issuing/authorizations';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (card != null) {
      queryParams.addAll(_queryParams('', 'card', card));
    }
    if (cardholder != null) {
      queryParams.addAll(_queryParams('', 'cardholder', cardholder));
    }
    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (status != null) {
      queryParams.addAll(_queryParams('', 'status', status));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all authorizations
  ///
  /// <p>Returns a list of Issuing <code>Authorization</code> objects. The objects are sorted in descending order by creation date, with the most recently created object appearing first.</p>
  ///
  /// Parameters:
  ///
  /// * [String] card:
  ///   Only return authorizations that belong to the given card.
  ///
  /// * [String] cardholder:
  ///   Only return authorizations that belong to the given cardholder.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return authorizations that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return authorizations with the given status. One of `pending`, `closed`, or `reversed`.
  Future<IssuingAuthorizationList?> getIssuingAuthorizations({ String? card, String? cardholder, GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? status, }) async {
    final response = await getIssuingAuthorizationsWithHttpInfo( card: card, cardholder: cardholder, created: created, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, status: status, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingAuthorizationList',) as IssuingAuthorizationList;
    
    }
    return null;
  }

  /// Retrieve an authorization
  ///
  /// <p>Retrieves an Issuing <code>Authorization</code> object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] authorization (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getIssuingAuthorizationsAuthorizationWithHttpInfo(String authorization, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/issuing/authorizations/{authorization}'
      .replaceAll('{authorization}', authorization);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve an authorization
  ///
  /// <p>Retrieves an Issuing <code>Authorization</code> object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] authorization (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<IssuingAuthorization?> getIssuingAuthorizationsAuthorization(String authorization, { List<String>? expand, }) async {
    final response = await getIssuingAuthorizationsAuthorizationWithHttpInfo(authorization,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingAuthorization',) as IssuingAuthorization;
    
    }
    return null;
  }

  /// List all cardholders
  ///
  /// <p>Returns a list of Issuing <code>Cardholder</code> objects. The objects are sorted in descending order by creation date, with the most recently created object appearing first.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return cardholders that were created during the given date interval.
  ///
  /// * [String] email:
  ///   Only return cardholders that have the given email address.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] phoneNumber:
  ///   Only return cardholders that have the given phone number.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return cardholders that have the given status. One of `active`, `inactive`, or `blocked`.
  ///
  /// * [String] type:
  ///   Only return cardholders that have the given type. One of `individual` or `company`.
  Future<Response> getIssuingCardholdersWithHttpInfo({ GetAccountsCreatedParameter? created, String? email, String? endingBefore, List<String>? expand, int? limit, String? phoneNumber, String? startingAfter, String? status, String? type, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/issuing/cardholders';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (email != null) {
      queryParams.addAll(_queryParams('', 'email', email));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (phoneNumber != null) {
      queryParams.addAll(_queryParams('', 'phone_number', phoneNumber));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (status != null) {
      queryParams.addAll(_queryParams('', 'status', status));
    }
    if (type != null) {
      queryParams.addAll(_queryParams('', 'type', type));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all cardholders
  ///
  /// <p>Returns a list of Issuing <code>Cardholder</code> objects. The objects are sorted in descending order by creation date, with the most recently created object appearing first.</p>
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return cardholders that were created during the given date interval.
  ///
  /// * [String] email:
  ///   Only return cardholders that have the given email address.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] phoneNumber:
  ///   Only return cardholders that have the given phone number.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return cardholders that have the given status. One of `active`, `inactive`, or `blocked`.
  ///
  /// * [String] type:
  ///   Only return cardholders that have the given type. One of `individual` or `company`.
  Future<IssuingCardholderList?> getIssuingCardholders({ GetAccountsCreatedParameter? created, String? email, String? endingBefore, List<String>? expand, int? limit, String? phoneNumber, String? startingAfter, String? status, String? type, }) async {
    final response = await getIssuingCardholdersWithHttpInfo( created: created, email: email, endingBefore: endingBefore, expand: expand, limit: limit, phoneNumber: phoneNumber, startingAfter: startingAfter, status: status, type: type, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingCardholderList',) as IssuingCardholderList;
    
    }
    return null;
  }

  /// Retrieve a cardholder
  ///
  /// <p>Retrieves an Issuing <code>Cardholder</code> object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] cardholder (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getIssuingCardholdersCardholderWithHttpInfo(String cardholder, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/issuing/cardholders/{cardholder}'
      .replaceAll('{cardholder}', cardholder);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a cardholder
  ///
  /// <p>Retrieves an Issuing <code>Cardholder</code> object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] cardholder (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<IssuingCardholder?> getIssuingCardholdersCardholder(String cardholder, { List<String>? expand, }) async {
    final response = await getIssuingCardholdersCardholderWithHttpInfo(cardholder,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingCardholder',) as IssuingCardholder;
    
    }
    return null;
  }

  /// List all cards
  ///
  /// <p>Returns a list of Issuing <code>Card</code> objects. The objects are sorted in descending order by creation date, with the most recently created object appearing first.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] cardholder:
  ///   Only return cards belonging to the Cardholder with the provided ID.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return cards that were issued during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [int] expMonth:
  ///   Only return cards that have the given expiration month.
  ///
  /// * [int] expYear:
  ///   Only return cards that have the given expiration year.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] last4:
  ///   Only return cards that have the given last four digits.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] personalizationDesign:
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return cards that have the given status. One of `active`, `inactive`, or `canceled`.
  ///
  /// * [String] type:
  ///   Only return cards that have the given type. One of `virtual` or `physical`.
  Future<Response> getIssuingCardsWithHttpInfo({ String? cardholder, GetAccountsCreatedParameter? created, String? endingBefore, int? expMonth, int? expYear, List<String>? expand, String? last4, int? limit, String? personalizationDesign, String? startingAfter, String? status, String? type, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/issuing/cards';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (cardholder != null) {
      queryParams.addAll(_queryParams('', 'cardholder', cardholder));
    }
    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expMonth != null) {
      queryParams.addAll(_queryParams('', 'exp_month', expMonth));
    }
    if (expYear != null) {
      queryParams.addAll(_queryParams('', 'exp_year', expYear));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (last4 != null) {
      queryParams.addAll(_queryParams('', 'last4', last4));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (personalizationDesign != null) {
      queryParams.addAll(_queryParams('', 'personalization_design', personalizationDesign));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (status != null) {
      queryParams.addAll(_queryParams('', 'status', status));
    }
    if (type != null) {
      queryParams.addAll(_queryParams('', 'type', type));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all cards
  ///
  /// <p>Returns a list of Issuing <code>Card</code> objects. The objects are sorted in descending order by creation date, with the most recently created object appearing first.</p>
  ///
  /// Parameters:
  ///
  /// * [String] cardholder:
  ///   Only return cards belonging to the Cardholder with the provided ID.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return cards that were issued during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [int] expMonth:
  ///   Only return cards that have the given expiration month.
  ///
  /// * [int] expYear:
  ///   Only return cards that have the given expiration year.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] last4:
  ///   Only return cards that have the given last four digits.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] personalizationDesign:
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return cards that have the given status. One of `active`, `inactive`, or `canceled`.
  ///
  /// * [String] type:
  ///   Only return cards that have the given type. One of `virtual` or `physical`.
  Future<IssuingCardList?> getIssuingCards({ String? cardholder, GetAccountsCreatedParameter? created, String? endingBefore, int? expMonth, int? expYear, List<String>? expand, String? last4, int? limit, String? personalizationDesign, String? startingAfter, String? status, String? type, }) async {
    final response = await getIssuingCardsWithHttpInfo( cardholder: cardholder, created: created, endingBefore: endingBefore, expMonth: expMonth, expYear: expYear, expand: expand, last4: last4, limit: limit, personalizationDesign: personalizationDesign, startingAfter: startingAfter, status: status, type: type, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingCardList',) as IssuingCardList;
    
    }
    return null;
  }

  /// Retrieve a card
  ///
  /// <p>Retrieves an Issuing <code>Card</code> object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] card (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getIssuingCardsCardWithHttpInfo(String card, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/issuing/cards/{card}'
      .replaceAll('{card}', card);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a card
  ///
  /// <p>Retrieves an Issuing <code>Card</code> object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] card (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<IssuingCard?> getIssuingCardsCard(String card, { List<String>? expand, }) async {
    final response = await getIssuingCardsCardWithHttpInfo(card,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingCard',) as IssuingCard;
    
    }
    return null;
  }

  /// List all disputes
  ///
  /// <p>Returns a list of Issuing <code>Dispute</code> objects. The objects are sorted in descending order by creation date, with the most recently created object appearing first.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return Issuing disputes that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Select Issuing disputes with the given status.
  ///
  /// * [String] transaction:
  ///   Select the Issuing dispute for the given transaction.
  Future<Response> getIssuingDisputesWithHttpInfo({ GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? status, String? transaction, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/issuing/disputes';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (status != null) {
      queryParams.addAll(_queryParams('', 'status', status));
    }
    if (transaction != null) {
      queryParams.addAll(_queryParams('', 'transaction', transaction));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all disputes
  ///
  /// <p>Returns a list of Issuing <code>Dispute</code> objects. The objects are sorted in descending order by creation date, with the most recently created object appearing first.</p>
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return Issuing disputes that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Select Issuing disputes with the given status.
  ///
  /// * [String] transaction:
  ///   Select the Issuing dispute for the given transaction.
  Future<IssuingDisputeList?> getIssuingDisputes({ GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? status, String? transaction, }) async {
    final response = await getIssuingDisputesWithHttpInfo( created: created, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, status: status, transaction: transaction, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingDisputeList',) as IssuingDisputeList;
    
    }
    return null;
  }

  /// Retrieve a dispute
  ///
  /// <p>Retrieves an Issuing <code>Dispute</code> object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] dispute (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getIssuingDisputesDisputeWithHttpInfo(String dispute, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/issuing/disputes/{dispute}'
      .replaceAll('{dispute}', dispute);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a dispute
  ///
  /// <p>Retrieves an Issuing <code>Dispute</code> object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] dispute (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<IssuingDispute?> getIssuingDisputesDispute(String dispute, { List<String>? expand, }) async {
    final response = await getIssuingDisputesDisputeWithHttpInfo(dispute,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingDispute',) as IssuingDispute;
    
    }
    return null;
  }

  /// List all personalization designs
  ///
  /// <p>Returns a list of personalization design objects. The objects are sorted in descending order by creation date, with the most recently created object appearing first.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [List<String>] lookupKeys:
  ///   Only return personalization designs with the given lookup keys.
  ///
  /// * [PreferencesListParam] preferences:
  ///   Only return personalization designs with the given preferences.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return personalization designs with the given status.
  Future<Response> getIssuingPersonalizationDesignsWithHttpInfo({ String? endingBefore, List<String>? expand, int? limit, List<String>? lookupKeys, PreferencesListParam? preferences, String? startingAfter, String? status, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/issuing/personalization_designs';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (lookupKeys != null) {
      queryParams.addAll(_queryParams('csv', 'lookup_keys', lookupKeys));
    }
    if (preferences != null) {
      queryParams.addAll(_queryParams('', 'preferences', preferences));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (status != null) {
      queryParams.addAll(_queryParams('', 'status', status));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all personalization designs
  ///
  /// <p>Returns a list of personalization design objects. The objects are sorted in descending order by creation date, with the most recently created object appearing first.</p>
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [List<String>] lookupKeys:
  ///   Only return personalization designs with the given lookup keys.
  ///
  /// * [PreferencesListParam] preferences:
  ///   Only return personalization designs with the given preferences.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return personalization designs with the given status.
  Future<IssuingPersonalizationDesignList?> getIssuingPersonalizationDesigns({ String? endingBefore, List<String>? expand, int? limit, List<String>? lookupKeys, PreferencesListParam? preferences, String? startingAfter, String? status, }) async {
    final response = await getIssuingPersonalizationDesignsWithHttpInfo( endingBefore: endingBefore, expand: expand, limit: limit, lookupKeys: lookupKeys, preferences: preferences, startingAfter: startingAfter, status: status, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingPersonalizationDesignList',) as IssuingPersonalizationDesignList;
    
    }
    return null;
  }

  /// Retrieve a personalization design
  ///
  /// <p>Retrieves a personalization design object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] personalizationDesign (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getIssuingPersonalizationDesignsPersonalizationDesignWithHttpInfo(String personalizationDesign, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/issuing/personalization_designs/{personalization_design}'
      .replaceAll('{personalization_design}', personalizationDesign);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a personalization design
  ///
  /// <p>Retrieves a personalization design object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] personalizationDesign (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<IssuingPersonalizationDesign?> getIssuingPersonalizationDesignsPersonalizationDesign(String personalizationDesign, { List<String>? expand, }) async {
    final response = await getIssuingPersonalizationDesignsPersonalizationDesignWithHttpInfo(personalizationDesign,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingPersonalizationDesign',) as IssuingPersonalizationDesign;
    
    }
    return null;
  }

  /// List all physical bundles
  ///
  /// <p>Returns a list of physical bundle objects. The objects are sorted in descending order by creation date, with the most recently created object appearing first.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return physical bundles with the given status.
  ///
  /// * [String] type:
  ///   Only return physical bundles with the given type.
  Future<Response> getIssuingPhysicalBundlesWithHttpInfo({ String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? status, String? type, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/issuing/physical_bundles';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (status != null) {
      queryParams.addAll(_queryParams('', 'status', status));
    }
    if (type != null) {
      queryParams.addAll(_queryParams('', 'type', type));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all physical bundles
  ///
  /// <p>Returns a list of physical bundle objects. The objects are sorted in descending order by creation date, with the most recently created object appearing first.</p>
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return physical bundles with the given status.
  ///
  /// * [String] type:
  ///   Only return physical bundles with the given type.
  Future<IssuingPhysicalBundleList?> getIssuingPhysicalBundles({ String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? status, String? type, }) async {
    final response = await getIssuingPhysicalBundlesWithHttpInfo( endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, status: status, type: type, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingPhysicalBundleList',) as IssuingPhysicalBundleList;
    
    }
    return null;
  }

  /// Retrieve a physical bundle
  ///
  /// <p>Retrieves a physical bundle object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] physicalBundle (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getIssuingPhysicalBundlesPhysicalBundleWithHttpInfo(String physicalBundle, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/issuing/physical_bundles/{physical_bundle}'
      .replaceAll('{physical_bundle}', physicalBundle);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a physical bundle
  ///
  /// <p>Retrieves a physical bundle object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] physicalBundle (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<IssuingPhysicalBundle?> getIssuingPhysicalBundlesPhysicalBundle(String physicalBundle, { List<String>? expand, }) async {
    final response = await getIssuingPhysicalBundlesPhysicalBundleWithHttpInfo(physicalBundle,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingPhysicalBundle',) as IssuingPhysicalBundle;
    
    }
    return null;
  }

  /// Retrieve a settlement
  ///
  /// <p>Retrieves an Issuing <code>Settlement</code> object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] settlement (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getIssuingSettlementsSettlementWithHttpInfo(String settlement, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/issuing/settlements/{settlement}'
      .replaceAll('{settlement}', settlement);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a settlement
  ///
  /// <p>Retrieves an Issuing <code>Settlement</code> object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] settlement (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<IssuingSettlement?> getIssuingSettlementsSettlement(String settlement, { List<String>? expand, }) async {
    final response = await getIssuingSettlementsSettlementWithHttpInfo(settlement,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingSettlement',) as IssuingSettlement;
    
    }
    return null;
  }

  /// List all issuing tokens for card
  ///
  /// <p>Lists all Issuing <code>Token</code> objects for a given card.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] card (required):
  ///   The Issuing card identifier to list tokens for.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return Issuing tokens that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Select Issuing tokens with the given status.
  Future<Response> getIssuingTokensWithHttpInfo(String card, { GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? status, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/issuing/tokens';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

      queryParams.addAll(_queryParams('', 'card', card));
    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (status != null) {
      queryParams.addAll(_queryParams('', 'status', status));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all issuing tokens for card
  ///
  /// <p>Lists all Issuing <code>Token</code> objects for a given card.</p>
  ///
  /// Parameters:
  ///
  /// * [String] card (required):
  ///   The Issuing card identifier to list tokens for.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return Issuing tokens that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Select Issuing tokens with the given status.
  Future<IssuingNetworkTokenList?> getIssuingTokens(String card, { GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? status, }) async {
    final response = await getIssuingTokensWithHttpInfo(card,  created: created, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, status: status, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingNetworkTokenList',) as IssuingNetworkTokenList;
    
    }
    return null;
  }

  /// Retrieve an issuing token
  ///
  /// <p>Retrieves an Issuing <code>Token</code> object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] token (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getIssuingTokensTokenWithHttpInfo(String token, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/issuing/tokens/{token}'
      .replaceAll('{token}', token);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve an issuing token
  ///
  /// <p>Retrieves an Issuing <code>Token</code> object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] token (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<IssuingToken?> getIssuingTokensToken(String token, { List<String>? expand, }) async {
    final response = await getIssuingTokensTokenWithHttpInfo(token,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingToken',) as IssuingToken;
    
    }
    return null;
  }

  /// List all transactions
  ///
  /// <p>Returns a list of Issuing <code>Transaction</code> objects. The objects are sorted in descending order by creation date, with the most recently created object appearing first.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] card:
  ///   Only return transactions that belong to the given card.
  ///
  /// * [String] cardholder:
  ///   Only return transactions that belong to the given cardholder.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return transactions that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] type:
  ///   Only return transactions that have the given type. One of `capture` or `refund`.
  Future<Response> getIssuingTransactionsWithHttpInfo({ String? card, String? cardholder, GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? type, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/issuing/transactions';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (card != null) {
      queryParams.addAll(_queryParams('', 'card', card));
    }
    if (cardholder != null) {
      queryParams.addAll(_queryParams('', 'cardholder', cardholder));
    }
    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (type != null) {
      queryParams.addAll(_queryParams('', 'type', type));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all transactions
  ///
  /// <p>Returns a list of Issuing <code>Transaction</code> objects. The objects are sorted in descending order by creation date, with the most recently created object appearing first.</p>
  ///
  /// Parameters:
  ///
  /// * [String] card:
  ///   Only return transactions that belong to the given card.
  ///
  /// * [String] cardholder:
  ///   Only return transactions that belong to the given cardholder.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return transactions that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] type:
  ///   Only return transactions that have the given type. One of `capture` or `refund`.
  Future<IssuingTransactionList?> getIssuingTransactions({ String? card, String? cardholder, GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? type, }) async {
    final response = await getIssuingTransactionsWithHttpInfo( card: card, cardholder: cardholder, created: created, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, type: type, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingTransactionList',) as IssuingTransactionList;
    
    }
    return null;
  }

  /// Retrieve a transaction
  ///
  /// <p>Retrieves an Issuing <code>Transaction</code> object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] transaction (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getIssuingTransactionsTransactionWithHttpInfo(String transaction, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/issuing/transactions/{transaction}'
      .replaceAll('{transaction}', transaction);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a transaction
  ///
  /// <p>Retrieves an Issuing <code>Transaction</code> object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] transaction (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<IssuingTransaction?> getIssuingTransactionsTransaction(String transaction, { List<String>? expand, }) async {
    final response = await getIssuingTransactionsTransactionWithHttpInfo(transaction,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingTransaction',) as IssuingTransaction;
    
    }
    return null;
  }

  /// Retrieve a Session
  ///
  /// <p>Retrieves the details of a Financial Connections <code>Session</code></p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] session (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getLinkAccountSessionsSessionWithHttpInfo(String session, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/link_account_sessions/{session}'
      .replaceAll('{session}', session);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a Session
  ///
  /// <p>Retrieves the details of a Financial Connections <code>Session</code></p>
  ///
  /// Parameters:
  ///
  /// * [String] session (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<FinancialConnectionsSession?> getLinkAccountSessionsSession(String session, { List<String>? expand, }) async {
    final response = await getLinkAccountSessionsSessionWithHttpInfo(session,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'FinancialConnectionsSession',) as FinancialConnectionsSession;
    
    }
    return null;
  }

  /// List Accounts
  ///
  /// <p>Returns a list of Financial Connections <code>Account</code> objects.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [AccountholderParams] accountHolder:
  ///   If present, only return accounts that belong to the specified account holder. `account_holder[customer]` and `account_holder[account]` are mutually exclusive.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] session:
  ///   If present, only return accounts that were collected as part of the given session.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getLinkedAccountsWithHttpInfo({ AccountholderParams? accountHolder, String? endingBefore, List<String>? expand, int? limit, String? session, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/linked_accounts';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (accountHolder != null) {
      queryParams.addAll(_queryParams('', 'account_holder', accountHolder));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (session != null) {
      queryParams.addAll(_queryParams('', 'session', session));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List Accounts
  ///
  /// <p>Returns a list of Financial Connections <code>Account</code> objects.</p>
  ///
  /// Parameters:
  ///
  /// * [AccountholderParams] accountHolder:
  ///   If present, only return accounts that belong to the specified account holder. `account_holder[customer]` and `account_holder[account]` are mutually exclusive.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] session:
  ///   If present, only return accounts that were collected as part of the given session.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<BankConnectionsResourceLinkedAccountList?> getLinkedAccounts({ AccountholderParams? accountHolder, String? endingBefore, List<String>? expand, int? limit, String? session, String? startingAfter, }) async {
    final response = await getLinkedAccountsWithHttpInfo( accountHolder: accountHolder, endingBefore: endingBefore, expand: expand, limit: limit, session: session, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BankConnectionsResourceLinkedAccountList',) as BankConnectionsResourceLinkedAccountList;
    
    }
    return null;
  }

  /// Retrieve an Account
  ///
  /// <p>Retrieves the details of an Financial Connections <code>Account</code>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getLinkedAccountsAccountWithHttpInfo(String account, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/linked_accounts/{account}'
      .replaceAll('{account}', account);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve an Account
  ///
  /// <p>Retrieves the details of an Financial Connections <code>Account</code>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<FinancialConnectionsAccount?> getLinkedAccountsAccount(String account, { List<String>? expand, }) async {
    final response = await getLinkedAccountsAccountWithHttpInfo(account,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'FinancialConnectionsAccount',) as FinancialConnectionsAccount;
    
    }
    return null;
  }

  /// List Account Owners
  ///
  /// <p>Lists all owners for a given <code>Account</code></p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] ownership (required):
  ///   The ID of the ownership object to fetch owners from.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getLinkedAccountsAccountOwnersWithHttpInfo(String account, String ownership, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/linked_accounts/{account}/owners'
      .replaceAll('{account}', account);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
      queryParams.addAll(_queryParams('', 'ownership', ownership));
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List Account Owners
  ///
  /// <p>Lists all owners for a given <code>Account</code></p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] ownership (required):
  ///   The ID of the ownership object to fetch owners from.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<BankConnectionsResourceOwnerList?> getLinkedAccountsAccountOwners(String account, String ownership, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getLinkedAccountsAccountOwnersWithHttpInfo(account, ownership,  endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BankConnectionsResourceOwnerList',) as BankConnectionsResourceOwnerList;
    
    }
    return null;
  }

  /// Retrieve a Mandate
  ///
  /// <p>Retrieves a Mandate object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] mandate (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getMandatesMandateWithHttpInfo(String mandate, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/mandates/{mandate}'
      .replaceAll('{mandate}', mandate);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a Mandate
  ///
  /// <p>Retrieves a Mandate object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] mandate (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Mandate?> getMandatesMandate(String mandate, { List<String>? expand, }) async {
    final response = await getMandatesMandateWithHttpInfo(mandate,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Mandate',) as Mandate;
    
    }
    return null;
  }

  /// List all PaymentIntents
  ///
  /// <p>Returns a list of PaymentIntents.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp or a dictionary with a number of different query options.
  ///
  /// * [String] customer:
  ///   Only return PaymentIntents for the customer that this customer ID specifies.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getPaymentIntentsWithHttpInfo({ GetAccountsCreatedParameter? created, String? customer, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payment_intents';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (customer != null) {
      queryParams.addAll(_queryParams('', 'customer', customer));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all PaymentIntents
  ///
  /// <p>Returns a list of PaymentIntents.</p>
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp or a dictionary with a number of different query options.
  ///
  /// * [String] customer:
  ///   Only return PaymentIntents for the customer that this customer ID specifies.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<PaymentFlowsPaymentIntentList?> getPaymentIntents({ GetAccountsCreatedParameter? created, String? customer, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getPaymentIntentsWithHttpInfo( created: created, customer: customer, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentFlowsPaymentIntentList',) as PaymentFlowsPaymentIntentList;
    
    }
    return null;
  }

  /// Retrieve a PaymentIntent
  ///
  /// <p>Retrieves the details of a PaymentIntent that has previously been created. </p>  <p>You can retrieve a PaymentIntent client-side using a publishable key when the <code>client_secret</code> is in the query string. </p>  <p>If you retrieve a PaymentIntent with a publishable key, it only returns a subset of properties. Refer to the <a href=\"#payment_intent_object\">payment intent</a> object reference for more details.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] intent (required):
  ///
  /// * [String] clientSecret:
  ///   The client secret of the PaymentIntent. We require it if you use a publishable key to retrieve the source.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getPaymentIntentsIntentWithHttpInfo(String intent, { String? clientSecret, List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payment_intents/{intent}'
      .replaceAll('{intent}', intent);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (clientSecret != null) {
      queryParams.addAll(_queryParams('', 'client_secret', clientSecret));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a PaymentIntent
  ///
  /// <p>Retrieves the details of a PaymentIntent that has previously been created. </p>  <p>You can retrieve a PaymentIntent client-side using a publishable key when the <code>client_secret</code> is in the query string. </p>  <p>If you retrieve a PaymentIntent with a publishable key, it only returns a subset of properties. Refer to the <a href=\"#payment_intent_object\">payment intent</a> object reference for more details.</p>
  ///
  /// Parameters:
  ///
  /// * [String] intent (required):
  ///
  /// * [String] clientSecret:
  ///   The client secret of the PaymentIntent. We require it if you use a publishable key to retrieve the source.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<PaymentIntent?> getPaymentIntentsIntent(String intent, { String? clientSecret, List<String>? expand, }) async {
    final response = await getPaymentIntentsIntentWithHttpInfo(intent,  clientSecret: clientSecret, expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentIntent',) as PaymentIntent;
    
    }
    return null;
  }

  /// Search PaymentIntents
  ///
  /// <p>Search for PaymentIntents you’ve previously created using Stripe’s <a href=\"/docs/search#search-query-language\">Search Query Language</a>. Don’t use search in read-after-write flows where strict consistency is necessary. Under normal operating conditions, data is searchable in less than a minute. Occasionally, propagation of new or updated data can be up to an hour behind during outages. Search functionality is not available to merchants in India.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] query (required):
  ///   The search query string. See [search query language](https://stripe.com/docs/search#search-query-language) and the list of supported [query fields for payment intents](https://stripe.com/docs/search#query-fields-for-payment-intents).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] page:
  ///   A cursor for pagination across multiple pages of results. Don't include this parameter on the first call. Use the next_page value returned in a previous response to request subsequent results.
  Future<Response> getPaymentIntentsSearchWithHttpInfo(String query, { List<String>? expand, int? limit, String? page, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payment_intents/search';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (page != null) {
      queryParams.addAll(_queryParams('', 'page', page));
    }
      queryParams.addAll(_queryParams('', 'query', query));

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Search PaymentIntents
  ///
  /// <p>Search for PaymentIntents you’ve previously created using Stripe’s <a href=\"/docs/search#search-query-language\">Search Query Language</a>. Don’t use search in read-after-write flows where strict consistency is necessary. Under normal operating conditions, data is searchable in less than a minute. Occasionally, propagation of new or updated data can be up to an hour behind during outages. Search functionality is not available to merchants in India.</p>
  ///
  /// Parameters:
  ///
  /// * [String] query (required):
  ///   The search query string. See [search query language](https://stripe.com/docs/search#search-query-language) and the list of supported [query fields for payment intents](https://stripe.com/docs/search#query-fields-for-payment-intents).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] page:
  ///   A cursor for pagination across multiple pages of results. Don't include this parameter on the first call. Use the next_page value returned in a previous response to request subsequent results.
  Future<SearchResult3?> getPaymentIntentsSearch(String query, { List<String>? expand, int? limit, String? page, }) async {
    final response = await getPaymentIntentsSearchWithHttpInfo(query,  expand: expand, limit: limit, page: page, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'SearchResult3',) as SearchResult3;
    
    }
    return null;
  }

  /// List all payment links
  ///
  /// <p>Returns a list of your payment links.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [bool] active:
  ///   Only return payment links that are active or inactive (e.g., pass `false` to list all inactive payment links).
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getPaymentLinksWithHttpInfo({ bool? active, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payment_links';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (active != null) {
      queryParams.addAll(_queryParams('', 'active', active));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all payment links
  ///
  /// <p>Returns a list of your payment links.</p>
  ///
  /// Parameters:
  ///
  /// * [bool] active:
  ///   Only return payment links that are active or inactive (e.g., pass `false` to list all inactive payment links).
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<PaymentLinksResourcePaymentLinkList?> getPaymentLinks({ bool? active, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getPaymentLinksWithHttpInfo( active: active, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentLinksResourcePaymentLinkList',) as PaymentLinksResourcePaymentLinkList;
    
    }
    return null;
  }

  /// Retrieve payment link
  ///
  /// <p>Retrieve a payment link.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] paymentLink (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getPaymentLinksPaymentLinkWithHttpInfo(String paymentLink, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payment_links/{payment_link}'
      .replaceAll('{payment_link}', paymentLink);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve payment link
  ///
  /// <p>Retrieve a payment link.</p>
  ///
  /// Parameters:
  ///
  /// * [String] paymentLink (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<PaymentLink?> getPaymentLinksPaymentLink(String paymentLink, { List<String>? expand, }) async {
    final response = await getPaymentLinksPaymentLinkWithHttpInfo(paymentLink,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentLink',) as PaymentLink;
    
    }
    return null;
  }

  /// Retrieve a payment link's line items
  ///
  /// <p>When retrieving a payment link, there is an includable <strong>line_items</strong> property containing the first handful of those items. There is also a URL where you can retrieve the full (paginated) list of line items.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] paymentLink (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getPaymentLinksPaymentLinkLineItemsWithHttpInfo(String paymentLink, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payment_links/{payment_link}/line_items'
      .replaceAll('{payment_link}', paymentLink);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a payment link's line items
  ///
  /// <p>When retrieving a payment link, there is an includable <strong>line_items</strong> property containing the first handful of those items. There is also a URL where you can retrieve the full (paginated) list of line items.</p>
  ///
  /// Parameters:
  ///
  /// * [String] paymentLink (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<PaymentLinksResourceListLineItems?> getPaymentLinksPaymentLinkLineItems(String paymentLink, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getPaymentLinksPaymentLinkLineItemsWithHttpInfo(paymentLink,  endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentLinksResourceListLineItems',) as PaymentLinksResourceListLineItems;
    
    }
    return null;
  }

  /// List payment method configurations
  ///
  /// <p>List payment method configurations</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [GetPaymentMethodConfigurationsApplicationParameter] application:
  ///   The Connect application to filter by.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getPaymentMethodConfigurationsWithHttpInfo({ GetPaymentMethodConfigurationsApplicationParameter? application, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payment_method_configurations';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (application != null) {
      queryParams.addAll(_queryParams('', 'application', application));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List payment method configurations
  ///
  /// <p>List payment method configurations</p>
  ///
  /// Parameters:
  ///
  /// * [GetPaymentMethodConfigurationsApplicationParameter] application:
  ///   The Connect application to filter by.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<PaymentMethodConfigResourcePaymentMethodConfigurationsList?> getPaymentMethodConfigurations({ GetPaymentMethodConfigurationsApplicationParameter? application, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getPaymentMethodConfigurationsWithHttpInfo( application: application, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentMethodConfigResourcePaymentMethodConfigurationsList',) as PaymentMethodConfigResourcePaymentMethodConfigurationsList;
    
    }
    return null;
  }

  /// Retrieve payment method configuration
  ///
  /// <p>Retrieve payment method configuration</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] configuration (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getPaymentMethodConfigurationsConfigurationWithHttpInfo(String configuration, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payment_method_configurations/{configuration}'
      .replaceAll('{configuration}', configuration);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve payment method configuration
  ///
  /// <p>Retrieve payment method configuration</p>
  ///
  /// Parameters:
  ///
  /// * [String] configuration (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<PaymentMethodConfiguration?> getPaymentMethodConfigurationsConfiguration(String configuration, { List<String>? expand, }) async {
    final response = await getPaymentMethodConfigurationsConfigurationWithHttpInfo(configuration,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentMethodConfiguration',) as PaymentMethodConfiguration;
    
    }
    return null;
  }

  /// List payment method domains
  ///
  /// <p>Lists the details of existing payment method domains.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] domainName:
  ///   The domain name that this payment method domain object represents.
  ///
  /// * [bool] enabled:
  ///   Whether this payment method domain is enabled. If the domain is not enabled, payment methods will not appear in Elements or Embedded Checkout
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getPaymentMethodDomainsWithHttpInfo({ String? domainName, bool? enabled, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payment_method_domains';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (domainName != null) {
      queryParams.addAll(_queryParams('', 'domain_name', domainName));
    }
    if (enabled != null) {
      queryParams.addAll(_queryParams('', 'enabled', enabled));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List payment method domains
  ///
  /// <p>Lists the details of existing payment method domains.</p>
  ///
  /// Parameters:
  ///
  /// * [String] domainName:
  ///   The domain name that this payment method domain object represents.
  ///
  /// * [bool] enabled:
  ///   Whether this payment method domain is enabled. If the domain is not enabled, payment methods will not appear in Elements or Embedded Checkout
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<PaymentMethodDomainResourcePaymentMethodDomainList?> getPaymentMethodDomains({ String? domainName, bool? enabled, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getPaymentMethodDomainsWithHttpInfo( domainName: domainName, enabled: enabled, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentMethodDomainResourcePaymentMethodDomainList',) as PaymentMethodDomainResourcePaymentMethodDomainList;
    
    }
    return null;
  }

  /// Retrieve a payment method domain
  ///
  /// <p>Retrieves the details of an existing payment method domain.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] paymentMethodDomain (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getPaymentMethodDomainsPaymentMethodDomainWithHttpInfo(String paymentMethodDomain, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payment_method_domains/{payment_method_domain}'
      .replaceAll('{payment_method_domain}', paymentMethodDomain);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a payment method domain
  ///
  /// <p>Retrieves the details of an existing payment method domain.</p>
  ///
  /// Parameters:
  ///
  /// * [String] paymentMethodDomain (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<PaymentMethodDomain?> getPaymentMethodDomainsPaymentMethodDomain(String paymentMethodDomain, { List<String>? expand, }) async {
    final response = await getPaymentMethodDomainsPaymentMethodDomainWithHttpInfo(paymentMethodDomain,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentMethodDomain',) as PaymentMethodDomain;
    
    }
    return null;
  }

  /// List PaymentMethods
  ///
  /// <p>Returns a list of PaymentMethods for Treasury flows. If you want to list the PaymentMethods attached to a Customer for payments, you should use the <a href=\"/docs/api/payment_methods/customer_list\">List a Customer’s PaymentMethods</a> API instead.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer:
  ///   The ID of the customer whose PaymentMethods will be retrieved.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] type:
  ///   An optional filter on the list, based on the object `type` field. Without the filter, the list includes all current and future payment method types. If your integration expects only one type of payment method in the response, make sure to provide a type value in the request.
  Future<Response> getPaymentMethodsWithHttpInfo({ String? customer, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? type, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payment_methods';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (customer != null) {
      queryParams.addAll(_queryParams('', 'customer', customer));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (type != null) {
      queryParams.addAll(_queryParams('', 'type', type));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List PaymentMethods
  ///
  /// <p>Returns a list of PaymentMethods for Treasury flows. If you want to list the PaymentMethods attached to a Customer for payments, you should use the <a href=\"/docs/api/payment_methods/customer_list\">List a Customer’s PaymentMethods</a> API instead.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer:
  ///   The ID of the customer whose PaymentMethods will be retrieved.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] type:
  ///   An optional filter on the list, based on the object `type` field. Without the filter, the list includes all current and future payment method types. If your integration expects only one type of payment method in the response, make sure to provide a type value in the request.
  Future<PaymentFlowsPaymentMethodList?> getPaymentMethods({ String? customer, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? type, }) async {
    final response = await getPaymentMethodsWithHttpInfo( customer: customer, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, type: type, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentFlowsPaymentMethodList',) as PaymentFlowsPaymentMethodList;
    
    }
    return null;
  }

  /// Retrieve a PaymentMethod
  ///
  /// <p>Retrieves a PaymentMethod object attached to the StripeAccount. To retrieve a payment method attached to a Customer, you should use <a href=\"/docs/api/payment_methods/customer\">Retrieve a Customer’s PaymentMethods</a></p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] paymentMethod (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getPaymentMethodsPaymentMethodWithHttpInfo(String paymentMethod, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payment_methods/{payment_method}'
      .replaceAll('{payment_method}', paymentMethod);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a PaymentMethod
  ///
  /// <p>Retrieves a PaymentMethod object attached to the StripeAccount. To retrieve a payment method attached to a Customer, you should use <a href=\"/docs/api/payment_methods/customer\">Retrieve a Customer’s PaymentMethods</a></p>
  ///
  /// Parameters:
  ///
  /// * [String] paymentMethod (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<PaymentMethod?> getPaymentMethodsPaymentMethod(String paymentMethod, { List<String>? expand, }) async {
    final response = await getPaymentMethodsPaymentMethodWithHttpInfo(paymentMethod,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentMethod',) as PaymentMethod;
    
    }
    return null;
  }

  /// List all payouts
  ///
  /// <p>Returns a list of existing payouts sent to third-party bank accounts or payouts that Stripe sent to you. The payouts return in sorted order, with the most recently created payouts appearing first.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] arrivalDate:
  ///   Only return payouts that are expected to arrive during the given date interval.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return payouts that were created during the given date interval.
  ///
  /// * [String] destination:
  ///   The ID of an external account - only return payouts sent to this external account.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return payouts that have the given status: `pending`, `paid`, `failed`, or `canceled`.
  Future<Response> getPayoutsWithHttpInfo({ GetAccountsCreatedParameter? arrivalDate, GetAccountsCreatedParameter? created, String? destination, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? status, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payouts';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (arrivalDate != null) {
      queryParams.addAll(_queryParams('', 'arrival_date', arrivalDate));
    }
    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (destination != null) {
      queryParams.addAll(_queryParams('', 'destination', destination));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (status != null) {
      queryParams.addAll(_queryParams('', 'status', status));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all payouts
  ///
  /// <p>Returns a list of existing payouts sent to third-party bank accounts or payouts that Stripe sent to you. The payouts return in sorted order, with the most recently created payouts appearing first.</p>
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] arrivalDate:
  ///   Only return payouts that are expected to arrive during the given date interval.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return payouts that were created during the given date interval.
  ///
  /// * [String] destination:
  ///   The ID of an external account - only return payouts sent to this external account.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return payouts that have the given status: `pending`, `paid`, `failed`, or `canceled`.
  Future<PayoutList?> getPayouts({ GetAccountsCreatedParameter? arrivalDate, GetAccountsCreatedParameter? created, String? destination, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? status, }) async {
    final response = await getPayoutsWithHttpInfo( arrivalDate: arrivalDate, created: created, destination: destination, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, status: status, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PayoutList',) as PayoutList;
    
    }
    return null;
  }

  /// Retrieve a payout
  ///
  /// <p>Retrieves the details of an existing payout. Supply the unique payout ID from either a payout creation request or the payout list. Stripe returns the corresponding payout information.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] payout (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getPayoutsPayoutWithHttpInfo(String payout, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payouts/{payout}'
      .replaceAll('{payout}', payout);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a payout
  ///
  /// <p>Retrieves the details of an existing payout. Supply the unique payout ID from either a payout creation request or the payout list. Stripe returns the corresponding payout information.</p>
  ///
  /// Parameters:
  ///
  /// * [String] payout (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Payout?> getPayoutsPayout(String payout, { List<String>? expand, }) async {
    final response = await getPayoutsPayoutWithHttpInfo(payout,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Payout',) as Payout;
    
    }
    return null;
  }

  /// List all plans
  ///
  /// <p>Returns a list of your plans.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [bool] active:
  ///   Only return plans that are active or inactive (e.g., pass `false` to list all inactive plans).
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with a number of different query options.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] product:
  ///   Only return plans for the given product.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getPlansWithHttpInfo({ bool? active, GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? product, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/plans';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (active != null) {
      queryParams.addAll(_queryParams('', 'active', active));
    }
    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (product != null) {
      queryParams.addAll(_queryParams('', 'product', product));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all plans
  ///
  /// <p>Returns a list of your plans.</p>
  ///
  /// Parameters:
  ///
  /// * [bool] active:
  ///   Only return plans that are active or inactive (e.g., pass `false` to list all inactive plans).
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with a number of different query options.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] product:
  ///   Only return plans for the given product.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<PlanList?> getPlans({ bool? active, GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? product, String? startingAfter, }) async {
    final response = await getPlansWithHttpInfo( active: active, created: created, endingBefore: endingBefore, expand: expand, limit: limit, product: product, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PlanList',) as PlanList;
    
    }
    return null;
  }

  /// Retrieve a plan
  ///
  /// <p>Retrieves the plan with the given ID.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] plan (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getPlansPlanWithHttpInfo(String plan, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/plans/{plan}'
      .replaceAll('{plan}', plan);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a plan
  ///
  /// <p>Retrieves the plan with the given ID.</p>
  ///
  /// Parameters:
  ///
  /// * [String] plan (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Plan?> getPlansPlan(String plan, { List<String>? expand, }) async {
    final response = await getPlansPlanWithHttpInfo(plan,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Plan',) as Plan;
    
    }
    return null;
  }

  /// List all prices
  ///
  /// <p>Returns a list of your active prices, excluding <a href=\"/docs/products-prices/pricing-models#inline-pricing\">inline prices</a>. For the list of inactive prices, set <code>active</code> to false.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [bool] active:
  ///   Only return prices that are active or inactive (e.g., pass `false` to list all inactive prices).
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with a number of different query options.
  ///
  /// * [String] currency:
  ///   Only return prices for the given currency.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [List<String>] lookupKeys:
  ///   Only return the price with these lookup_keys, if any exist. You can specify up to 10 lookup_keys.
  ///
  /// * [String] product:
  ///   Only return prices for the given product.
  ///
  /// * [AllPricesRecurringParams] recurring:
  ///   Only return prices with these recurring fields.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] type:
  ///   Only return prices of type `recurring` or `one_time`.
  Future<Response> getPricesWithHttpInfo({ bool? active, GetAccountsCreatedParameter? created, String? currency, String? endingBefore, List<String>? expand, int? limit, List<String>? lookupKeys, String? product, AllPricesRecurringParams? recurring, String? startingAfter, String? type, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/prices';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (active != null) {
      queryParams.addAll(_queryParams('', 'active', active));
    }
    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (currency != null) {
      queryParams.addAll(_queryParams('', 'currency', currency));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (lookupKeys != null) {
      queryParams.addAll(_queryParams('csv', 'lookup_keys', lookupKeys));
    }
    if (product != null) {
      queryParams.addAll(_queryParams('', 'product', product));
    }
    if (recurring != null) {
      queryParams.addAll(_queryParams('', 'recurring', recurring));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (type != null) {
      queryParams.addAll(_queryParams('', 'type', type));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all prices
  ///
  /// <p>Returns a list of your active prices, excluding <a href=\"/docs/products-prices/pricing-models#inline-pricing\">inline prices</a>. For the list of inactive prices, set <code>active</code> to false.</p>
  ///
  /// Parameters:
  ///
  /// * [bool] active:
  ///   Only return prices that are active or inactive (e.g., pass `false` to list all inactive prices).
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with a number of different query options.
  ///
  /// * [String] currency:
  ///   Only return prices for the given currency.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [List<String>] lookupKeys:
  ///   Only return the price with these lookup_keys, if any exist. You can specify up to 10 lookup_keys.
  ///
  /// * [String] product:
  ///   Only return prices for the given product.
  ///
  /// * [AllPricesRecurringParams] recurring:
  ///   Only return prices with these recurring fields.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] type:
  ///   Only return prices of type `recurring` or `one_time`.
  Future<PriceList?> getPrices({ bool? active, GetAccountsCreatedParameter? created, String? currency, String? endingBefore, List<String>? expand, int? limit, List<String>? lookupKeys, String? product, AllPricesRecurringParams? recurring, String? startingAfter, String? type, }) async {
    final response = await getPricesWithHttpInfo( active: active, created: created, currency: currency, endingBefore: endingBefore, expand: expand, limit: limit, lookupKeys: lookupKeys, product: product, recurring: recurring, startingAfter: startingAfter, type: type, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PriceList',) as PriceList;
    
    }
    return null;
  }

  /// Retrieve a price
  ///
  /// <p>Retrieves the price with the given ID.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] price (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getPricesPriceWithHttpInfo(String price, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/prices/{price}'
      .replaceAll('{price}', price);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a price
  ///
  /// <p>Retrieves the price with the given ID.</p>
  ///
  /// Parameters:
  ///
  /// * [String] price (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Price?> getPricesPrice(String price, { List<String>? expand, }) async {
    final response = await getPricesPriceWithHttpInfo(price,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Price',) as Price;
    
    }
    return null;
  }

  /// Search prices
  ///
  /// <p>Search for prices you’ve previously created using Stripe’s <a href=\"/docs/search#search-query-language\">Search Query Language</a>. Don’t use search in read-after-write flows where strict consistency is necessary. Under normal operating conditions, data is searchable in less than a minute. Occasionally, propagation of new or updated data can be up to an hour behind during outages. Search functionality is not available to merchants in India.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] query (required):
  ///   The search query string. See [search query language](https://stripe.com/docs/search#search-query-language) and the list of supported [query fields for prices](https://stripe.com/docs/search#query-fields-for-prices).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] page:
  ///   A cursor for pagination across multiple pages of results. Don't include this parameter on the first call. Use the next_page value returned in a previous response to request subsequent results.
  Future<Response> getPricesSearchWithHttpInfo(String query, { List<String>? expand, int? limit, String? page, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/prices/search';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (page != null) {
      queryParams.addAll(_queryParams('', 'page', page));
    }
      queryParams.addAll(_queryParams('', 'query', query));

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Search prices
  ///
  /// <p>Search for prices you’ve previously created using Stripe’s <a href=\"/docs/search#search-query-language\">Search Query Language</a>. Don’t use search in read-after-write flows where strict consistency is necessary. Under normal operating conditions, data is searchable in less than a minute. Occasionally, propagation of new or updated data can be up to an hour behind during outages. Search functionality is not available to merchants in India.</p>
  ///
  /// Parameters:
  ///
  /// * [String] query (required):
  ///   The search query string. See [search query language](https://stripe.com/docs/search#search-query-language) and the list of supported [query fields for prices](https://stripe.com/docs/search#query-fields-for-prices).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] page:
  ///   A cursor for pagination across multiple pages of results. Don't include this parameter on the first call. Use the next_page value returned in a previous response to request subsequent results.
  Future<SearchResult4?> getPricesSearch(String query, { List<String>? expand, int? limit, String? page, }) async {
    final response = await getPricesSearchWithHttpInfo(query,  expand: expand, limit: limit, page: page, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'SearchResult4',) as SearchResult4;
    
    }
    return null;
  }

  /// List all products
  ///
  /// <p>Returns a list of your products. The products are returned sorted by creation date, with the most recently created products appearing first.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [bool] active:
  ///   Only return products that are active or inactive (e.g., pass `false` to list all inactive products).
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return products that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [List<String>] ids:
  ///   Only return products with the given IDs. Cannot be used with [starting_after](https://stripe.com/docs/api#list_products-starting_after) or [ending_before](https://stripe.com/docs/api#list_products-ending_before).
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [bool] shippable:
  ///   Only return products that can be shipped (i.e., physical, not digital products).
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] url:
  ///   Only return products with the given url.
  Future<Response> getProductsWithHttpInfo({ bool? active, GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, List<String>? ids, int? limit, bool? shippable, String? startingAfter, String? url, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/products';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (active != null) {
      queryParams.addAll(_queryParams('', 'active', active));
    }
    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (ids != null) {
      queryParams.addAll(_queryParams('csv', 'ids', ids));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (shippable != null) {
      queryParams.addAll(_queryParams('', 'shippable', shippable));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (url != null) {
      queryParams.addAll(_queryParams('', 'url', url));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all products
  ///
  /// <p>Returns a list of your products. The products are returned sorted by creation date, with the most recently created products appearing first.</p>
  ///
  /// Parameters:
  ///
  /// * [bool] active:
  ///   Only return products that are active or inactive (e.g., pass `false` to list all inactive products).
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return products that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [List<String>] ids:
  ///   Only return products with the given IDs. Cannot be used with [starting_after](https://stripe.com/docs/api#list_products-starting_after) or [ending_before](https://stripe.com/docs/api#list_products-ending_before).
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [bool] shippable:
  ///   Only return products that can be shipped (i.e., physical, not digital products).
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] url:
  ///   Only return products with the given url.
  Future<ProductList?> getProducts({ bool? active, GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, List<String>? ids, int? limit, bool? shippable, String? startingAfter, String? url, }) async {
    final response = await getProductsWithHttpInfo( active: active, created: created, endingBefore: endingBefore, expand: expand, ids: ids, limit: limit, shippable: shippable, startingAfter: startingAfter, url: url, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ProductList',) as ProductList;
    
    }
    return null;
  }

  /// Retrieve a product
  ///
  /// <p>Retrieves the details of an existing product. Supply the unique product ID from either a product creation request or the product list, and Stripe will return the corresponding product information.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getProductsIdWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/products/{id}'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a product
  ///
  /// <p>Retrieves the details of an existing product. Supply the unique product ID from either a product creation request or the product list, and Stripe will return the corresponding product information.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Product?> getProductsId(String id, { List<String>? expand, }) async {
    final response = await getProductsIdWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Product',) as Product;
    
    }
    return null;
  }

  /// List all features attached to a product
  ///
  /// <p>Retrieve a list of features for a product</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] product (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getProductsProductFeaturesWithHttpInfo(String product, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/products/{product}/features'
      .replaceAll('{product}', product);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all features attached to a product
  ///
  /// <p>Retrieve a list of features for a product</p>
  ///
  /// Parameters:
  ///
  /// * [String] product (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<EntitlementsResourceProductFeatureList?> getProductsProductFeatures(String product, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getProductsProductFeaturesWithHttpInfo(product,  endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'EntitlementsResourceProductFeatureList',) as EntitlementsResourceProductFeatureList;
    
    }
    return null;
  }

  /// Retrieve a product_feature
  ///
  /// <p>Retrieves a product_feature, which represents a feature attachment to a product</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///   The ID of the product_feature.
  ///
  /// * [String] product (required):
  ///   The ID of the product.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getProductsProductFeaturesIdWithHttpInfo(String id, String product, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/products/{product}/features/{id}'
      .replaceAll('{id}', id)
      .replaceAll('{product}', product);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a product_feature
  ///
  /// <p>Retrieves a product_feature, which represents a feature attachment to a product</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///   The ID of the product_feature.
  ///
  /// * [String] product (required):
  ///   The ID of the product.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<ProductFeature?> getProductsProductFeaturesId(String id, String product, { List<String>? expand, }) async {
    final response = await getProductsProductFeaturesIdWithHttpInfo(id, product,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ProductFeature',) as ProductFeature;
    
    }
    return null;
  }

  /// Search products
  ///
  /// <p>Search for products you’ve previously created using Stripe’s <a href=\"/docs/search#search-query-language\">Search Query Language</a>. Don’t use search in read-after-write flows where strict consistency is necessary. Under normal operating conditions, data is searchable in less than a minute. Occasionally, propagation of new or updated data can be up to an hour behind during outages. Search functionality is not available to merchants in India.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] query (required):
  ///   The search query string. See [search query language](https://stripe.com/docs/search#search-query-language) and the list of supported [query fields for products](https://stripe.com/docs/search#query-fields-for-products).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] page:
  ///   A cursor for pagination across multiple pages of results. Don't include this parameter on the first call. Use the next_page value returned in a previous response to request subsequent results.
  Future<Response> getProductsSearchWithHttpInfo(String query, { List<String>? expand, int? limit, String? page, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/products/search';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (page != null) {
      queryParams.addAll(_queryParams('', 'page', page));
    }
      queryParams.addAll(_queryParams('', 'query', query));

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Search products
  ///
  /// <p>Search for products you’ve previously created using Stripe’s <a href=\"/docs/search#search-query-language\">Search Query Language</a>. Don’t use search in read-after-write flows where strict consistency is necessary. Under normal operating conditions, data is searchable in less than a minute. Occasionally, propagation of new or updated data can be up to an hour behind during outages. Search functionality is not available to merchants in India.</p>
  ///
  /// Parameters:
  ///
  /// * [String] query (required):
  ///   The search query string. See [search query language](https://stripe.com/docs/search#search-query-language) and the list of supported [query fields for products](https://stripe.com/docs/search#query-fields-for-products).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] page:
  ///   A cursor for pagination across multiple pages of results. Don't include this parameter on the first call. Use the next_page value returned in a previous response to request subsequent results.
  Future<SearchResult5?> getProductsSearch(String query, { List<String>? expand, int? limit, String? page, }) async {
    final response = await getProductsSearchWithHttpInfo(query,  expand: expand, limit: limit, page: page, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'SearchResult5',) as SearchResult5;
    
    }
    return null;
  }

  /// List all promotion codes
  ///
  /// <p>Returns a list of your promotion codes.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [bool] active:
  ///   Filter promotion codes by whether they are active.
  ///
  /// * [String] code:
  ///   Only return promotion codes that have this case-insensitive code.
  ///
  /// * [String] coupon:
  ///   Only return promotion codes for this coupon.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with a number of different query options.
  ///
  /// * [String] customer:
  ///   Only return promotion codes that are restricted to this customer.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getPromotionCodesWithHttpInfo({ bool? active, String? code, String? coupon, GetAccountsCreatedParameter? created, String? customer, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/promotion_codes';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (active != null) {
      queryParams.addAll(_queryParams('', 'active', active));
    }
    if (code != null) {
      queryParams.addAll(_queryParams('', 'code', code));
    }
    if (coupon != null) {
      queryParams.addAll(_queryParams('', 'coupon', coupon));
    }
    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (customer != null) {
      queryParams.addAll(_queryParams('', 'customer', customer));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all promotion codes
  ///
  /// <p>Returns a list of your promotion codes.</p>
  ///
  /// Parameters:
  ///
  /// * [bool] active:
  ///   Filter promotion codes by whether they are active.
  ///
  /// * [String] code:
  ///   Only return promotion codes that have this case-insensitive code.
  ///
  /// * [String] coupon:
  ///   Only return promotion codes for this coupon.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with a number of different query options.
  ///
  /// * [String] customer:
  ///   Only return promotion codes that are restricted to this customer.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<PromotionCodesResourcePromotionCodeList?> getPromotionCodes({ bool? active, String? code, String? coupon, GetAccountsCreatedParameter? created, String? customer, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getPromotionCodesWithHttpInfo( active: active, code: code, coupon: coupon, created: created, customer: customer, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PromotionCodesResourcePromotionCodeList',) as PromotionCodesResourcePromotionCodeList;
    
    }
    return null;
  }

  /// Retrieve a promotion code
  ///
  /// <p>Retrieves the promotion code with the given ID. In order to retrieve a promotion code by the customer-facing <code>code</code> use <a href=\"/docs/api/promotion_codes/list\">list</a> with the desired <code>code</code>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] promotionCode (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getPromotionCodesPromotionCodeWithHttpInfo(String promotionCode, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/promotion_codes/{promotion_code}'
      .replaceAll('{promotion_code}', promotionCode);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a promotion code
  ///
  /// <p>Retrieves the promotion code with the given ID. In order to retrieve a promotion code by the customer-facing <code>code</code> use <a href=\"/docs/api/promotion_codes/list\">list</a> with the desired <code>code</code>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] promotionCode (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<PromotionCode?> getPromotionCodesPromotionCode(String promotionCode, { List<String>? expand, }) async {
    final response = await getPromotionCodesPromotionCodeWithHttpInfo(promotionCode,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PromotionCode',) as PromotionCode;
    
    }
    return null;
  }

  /// List all quotes
  ///
  /// <p>Returns a list of your quotes.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer:
  ///   The ID of the customer whose quotes will be retrieved.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   The status of the quote.
  ///
  /// * [String] testClock:
  ///   Provides a list of quotes that are associated with the specified test clock. The response will not include quotes with test clocks if this and the customer parameter is not set.
  Future<Response> getQuotesWithHttpInfo({ String? customer, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? status, String? testClock, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/quotes';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (customer != null) {
      queryParams.addAll(_queryParams('', 'customer', customer));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (status != null) {
      queryParams.addAll(_queryParams('', 'status', status));
    }
    if (testClock != null) {
      queryParams.addAll(_queryParams('', 'test_clock', testClock));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all quotes
  ///
  /// <p>Returns a list of your quotes.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer:
  ///   The ID of the customer whose quotes will be retrieved.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   The status of the quote.
  ///
  /// * [String] testClock:
  ///   Provides a list of quotes that are associated with the specified test clock. The response will not include quotes with test clocks if this and the customer parameter is not set.
  Future<QuotesResourceQuoteList?> getQuotes({ String? customer, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? status, String? testClock, }) async {
    final response = await getQuotesWithHttpInfo( customer: customer, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, status: status, testClock: testClock, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'QuotesResourceQuoteList',) as QuotesResourceQuoteList;
    
    }
    return null;
  }

  /// Retrieve a quote
  ///
  /// <p>Retrieves the quote with the given ID.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] quote (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getQuotesQuoteWithHttpInfo(String quote, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/quotes/{quote}'
      .replaceAll('{quote}', quote);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a quote
  ///
  /// <p>Retrieves the quote with the given ID.</p>
  ///
  /// Parameters:
  ///
  /// * [String] quote (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Quote?> getQuotesQuote(String quote, { List<String>? expand, }) async {
    final response = await getQuotesQuoteWithHttpInfo(quote,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Quote',) as Quote;
    
    }
    return null;
  }

  /// Retrieve a quote's upfront line items
  ///
  /// <p>When retrieving a quote, there is an includable <a href=\"https://stripe.com/docs/api/quotes/object#quote_object-computed-upfront-line_items\"><strong>computed.upfront.line_items</strong></a> property containing the first handful of those items. There is also a URL where you can retrieve the full (paginated) list of upfront line items.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] quote (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getQuotesQuoteComputedUpfrontLineItemsWithHttpInfo(String quote, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/quotes/{quote}/computed_upfront_line_items'
      .replaceAll('{quote}', quote);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a quote's upfront line items
  ///
  /// <p>When retrieving a quote, there is an includable <a href=\"https://stripe.com/docs/api/quotes/object#quote_object-computed-upfront-line_items\"><strong>computed.upfront.line_items</strong></a> property containing the first handful of those items. There is also a URL where you can retrieve the full (paginated) list of upfront line items.</p>
  ///
  /// Parameters:
  ///
  /// * [String] quote (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<QuotesResourceListLineItems?> getQuotesQuoteComputedUpfrontLineItems(String quote, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getQuotesQuoteComputedUpfrontLineItemsWithHttpInfo(quote,  endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'QuotesResourceListLineItems',) as QuotesResourceListLineItems;
    
    }
    return null;
  }

  /// Retrieve a quote's line items
  ///
  /// <p>When retrieving a quote, there is an includable <strong>line_items</strong> property containing the first handful of those items. There is also a URL where you can retrieve the full (paginated) list of line items.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] quote (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getQuotesQuoteLineItemsWithHttpInfo(String quote, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/quotes/{quote}/line_items'
      .replaceAll('{quote}', quote);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a quote's line items
  ///
  /// <p>When retrieving a quote, there is an includable <strong>line_items</strong> property containing the first handful of those items. There is also a URL where you can retrieve the full (paginated) list of line items.</p>
  ///
  /// Parameters:
  ///
  /// * [String] quote (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<QuotesResourceListLineItems?> getQuotesQuoteLineItems(String quote, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getQuotesQuoteLineItemsWithHttpInfo(quote,  endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'QuotesResourceListLineItems',) as QuotesResourceListLineItems;
    
    }
    return null;
  }

  /// Download quote PDF
  ///
  /// <p>Download the PDF for a finalized quote. Explanation for special handling can be found <a href=\"https://docs.stripe.com/quotes/overview#quote_pdf\">here</a></p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] quote (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getQuotesQuotePdfWithHttpInfo(String quote, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/quotes/{quote}/pdf'
      .replaceAll('{quote}', quote);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Download quote PDF
  ///
  /// <p>Download the PDF for a finalized quote. Explanation for special handling can be found <a href=\"https://docs.stripe.com/quotes/overview#quote_pdf\">here</a></p>
  ///
  /// Parameters:
  ///
  /// * [String] quote (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<MultipartFile?> getQuotesQuotePdf(String quote, { List<String>? expand, }) async {
    final response = await getQuotesQuotePdfWithHttpInfo(quote,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'MultipartFile',) as MultipartFile;
    
    }
    return null;
  }

  /// List all early fraud warnings
  ///
  /// <p>Returns a list of early fraud warnings.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] charge:
  ///   Only return early fraud warnings for the charge specified by this charge ID.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return early fraud warnings that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] paymentIntent:
  ///   Only return early fraud warnings for charges that were created by the PaymentIntent specified by this PaymentIntent ID.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getRadarEarlyFraudWarningsWithHttpInfo({ String? charge, GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? paymentIntent, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/radar/early_fraud_warnings';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (charge != null) {
      queryParams.addAll(_queryParams('', 'charge', charge));
    }
    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (paymentIntent != null) {
      queryParams.addAll(_queryParams('', 'payment_intent', paymentIntent));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all early fraud warnings
  ///
  /// <p>Returns a list of early fraud warnings.</p>
  ///
  /// Parameters:
  ///
  /// * [String] charge:
  ///   Only return early fraud warnings for the charge specified by this charge ID.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return early fraud warnings that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] paymentIntent:
  ///   Only return early fraud warnings for charges that were created by the PaymentIntent specified by this PaymentIntent ID.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<RadarEarlyFraudWarningList?> getRadarEarlyFraudWarnings({ String? charge, GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? paymentIntent, String? startingAfter, }) async {
    final response = await getRadarEarlyFraudWarningsWithHttpInfo( charge: charge, created: created, endingBefore: endingBefore, expand: expand, limit: limit, paymentIntent: paymentIntent, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'RadarEarlyFraudWarningList',) as RadarEarlyFraudWarningList;
    
    }
    return null;
  }

  /// Retrieve an early fraud warning
  ///
  /// <p>Retrieves the details of an early fraud warning that has previously been created. </p>  <p>Please refer to the <a href=\"#early_fraud_warning_object\">early fraud warning</a> object reference for more details.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] earlyFraudWarning (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getRadarEarlyFraudWarningsEarlyFraudWarningWithHttpInfo(String earlyFraudWarning, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/radar/early_fraud_warnings/{early_fraud_warning}'
      .replaceAll('{early_fraud_warning}', earlyFraudWarning);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve an early fraud warning
  ///
  /// <p>Retrieves the details of an early fraud warning that has previously been created. </p>  <p>Please refer to the <a href=\"#early_fraud_warning_object\">early fraud warning</a> object reference for more details.</p>
  ///
  /// Parameters:
  ///
  /// * [String] earlyFraudWarning (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<RadarEarlyFraudWarning?> getRadarEarlyFraudWarningsEarlyFraudWarning(String earlyFraudWarning, { List<String>? expand, }) async {
    final response = await getRadarEarlyFraudWarningsEarlyFraudWarningWithHttpInfo(earlyFraudWarning,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'RadarEarlyFraudWarning',) as RadarEarlyFraudWarning;
    
    }
    return null;
  }

  /// List all value list items
  ///
  /// <p>Returns a list of <code>ValueListItem</code> objects. The objects are sorted in descending order by creation date, with the most recently created object appearing first.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] valueList (required):
  ///   Identifier for the parent value list this item belongs to.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return items that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] value:
  ///   Return items belonging to the parent list whose value matches the specified value (using an \"is like\" match).
  Future<Response> getRadarValueListItemsWithHttpInfo(String valueList, { GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? value, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/radar/value_list_items';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (value != null) {
      queryParams.addAll(_queryParams('', 'value', value));
    }
      queryParams.addAll(_queryParams('', 'value_list', valueList));

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all value list items
  ///
  /// <p>Returns a list of <code>ValueListItem</code> objects. The objects are sorted in descending order by creation date, with the most recently created object appearing first.</p>
  ///
  /// Parameters:
  ///
  /// * [String] valueList (required):
  ///   Identifier for the parent value list this item belongs to.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return items that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] value:
  ///   Return items belonging to the parent list whose value matches the specified value (using an \"is like\" match).
  Future<RadarListListItemList?> getRadarValueListItems(String valueList, { GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? value, }) async {
    final response = await getRadarValueListItemsWithHttpInfo(valueList,  created: created, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, value: value, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'RadarListListItemList',) as RadarListListItemList;
    
    }
    return null;
  }

  /// Retrieve a value list item
  ///
  /// <p>Retrieves a <code>ValueListItem</code> object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] item (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getRadarValueListItemsItemWithHttpInfo(String item, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/radar/value_list_items/{item}'
      .replaceAll('{item}', item);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a value list item
  ///
  /// <p>Retrieves a <code>ValueListItem</code> object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] item (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<RadarValueListItem?> getRadarValueListItemsItem(String item, { List<String>? expand, }) async {
    final response = await getRadarValueListItemsItemWithHttpInfo(item,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'RadarValueListItem',) as RadarValueListItem;
    
    }
    return null;
  }

  /// List all value lists
  ///
  /// <p>Returns a list of <code>ValueList</code> objects. The objects are sorted in descending order by creation date, with the most recently created object appearing first.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] alias:
  ///   The alias used to reference the value list when writing rules.
  ///
  /// * [String] contains:
  ///   A value contained within a value list - returns all value lists containing this value.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return value lists that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getRadarValueListsWithHttpInfo({ String? alias, String? contains, GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/radar/value_lists';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (alias != null) {
      queryParams.addAll(_queryParams('', 'alias', alias));
    }
    if (contains != null) {
      queryParams.addAll(_queryParams('', 'contains', contains));
    }
    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all value lists
  ///
  /// <p>Returns a list of <code>ValueList</code> objects. The objects are sorted in descending order by creation date, with the most recently created object appearing first.</p>
  ///
  /// Parameters:
  ///
  /// * [String] alias:
  ///   The alias used to reference the value list when writing rules.
  ///
  /// * [String] contains:
  ///   A value contained within a value list - returns all value lists containing this value.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return value lists that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<RadarListListList?> getRadarValueLists({ String? alias, String? contains, GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getRadarValueListsWithHttpInfo( alias: alias, contains: contains, created: created, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'RadarListListList',) as RadarListListList;
    
    }
    return null;
  }

  /// Retrieve a value list
  ///
  /// <p>Retrieves a <code>ValueList</code> object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] valueList (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getRadarValueListsValueListWithHttpInfo(String valueList, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/radar/value_lists/{value_list}'
      .replaceAll('{value_list}', valueList);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a value list
  ///
  /// <p>Retrieves a <code>ValueList</code> object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] valueList (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<RadarValueList?> getRadarValueListsValueList(String valueList, { List<String>? expand, }) async {
    final response = await getRadarValueListsValueListWithHttpInfo(valueList,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'RadarValueList',) as RadarValueList;
    
    }
    return null;
  }

  /// List all refunds
  ///
  /// <p>Returns a list of all refunds you created. We return the refunds in sorted order, with the most recent refunds appearing first. The 10 most recent refunds are always available by default on the Charge object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] charge:
  ///   Only return refunds for the charge specified by this charge ID.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return refunds that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] paymentIntent:
  ///   Only return refunds for the PaymentIntent specified by this ID.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getRefundsWithHttpInfo({ String? charge, GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? paymentIntent, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/refunds';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (charge != null) {
      queryParams.addAll(_queryParams('', 'charge', charge));
    }
    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (paymentIntent != null) {
      queryParams.addAll(_queryParams('', 'payment_intent', paymentIntent));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all refunds
  ///
  /// <p>Returns a list of all refunds you created. We return the refunds in sorted order, with the most recent refunds appearing first. The 10 most recent refunds are always available by default on the Charge object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] charge:
  ///   Only return refunds for the charge specified by this charge ID.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return refunds that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] paymentIntent:
  ///   Only return refunds for the PaymentIntent specified by this ID.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<APIMethodRefundList?> getRefunds({ String? charge, GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? paymentIntent, String? startingAfter, }) async {
    final response = await getRefundsWithHttpInfo( charge: charge, created: created, endingBefore: endingBefore, expand: expand, limit: limit, paymentIntent: paymentIntent, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'APIMethodRefundList',) as APIMethodRefundList;
    
    }
    return null;
  }

  /// Retrieve a refund
  ///
  /// <p>Retrieves the details of an existing refund.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] refund (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getRefundsRefundWithHttpInfo(String refund, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/refunds/{refund}'
      .replaceAll('{refund}', refund);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a refund
  ///
  /// <p>Retrieves the details of an existing refund.</p>
  ///
  /// Parameters:
  ///
  /// * [String] refund (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Refund?> getRefundsRefund(String refund, { List<String>? expand, }) async {
    final response = await getRefundsRefundWithHttpInfo(refund,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Refund',) as Refund;
    
    }
    return null;
  }

  /// List all Report Runs
  ///
  /// <p>Returns a list of Report Runs, with the most recent appearing first.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return Report Runs that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getReportingReportRunsWithHttpInfo({ GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/reporting/report_runs';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all Report Runs
  ///
  /// <p>Returns a list of Report Runs, with the most recent appearing first.</p>
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return Report Runs that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<FinancialReportingFinanceReportRunList?> getReportingReportRuns({ GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getReportingReportRunsWithHttpInfo( created: created, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'FinancialReportingFinanceReportRunList',) as FinancialReportingFinanceReportRunList;
    
    }
    return null;
  }

  /// Retrieve a Report Run
  ///
  /// <p>Retrieves the details of an existing Report Run.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] reportRun (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getReportingReportRunsReportRunWithHttpInfo(String reportRun, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/reporting/report_runs/{report_run}'
      .replaceAll('{report_run}', reportRun);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a Report Run
  ///
  /// <p>Retrieves the details of an existing Report Run.</p>
  ///
  /// Parameters:
  ///
  /// * [String] reportRun (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<ReportingReportRun?> getReportingReportRunsReportRun(String reportRun, { List<String>? expand, }) async {
    final response = await getReportingReportRunsReportRunWithHttpInfo(reportRun,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ReportingReportRun',) as ReportingReportRun;
    
    }
    return null;
  }

  /// List all Report Types
  ///
  /// <p>Returns a full list of Report Types.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getReportingReportTypesWithHttpInfo({ List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/reporting/report_types';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all Report Types
  ///
  /// <p>Returns a full list of Report Types.</p>
  ///
  /// Parameters:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<FinancialReportingFinanceReportTypeList?> getReportingReportTypes({ List<String>? expand, }) async {
    final response = await getReportingReportTypesWithHttpInfo( expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'FinancialReportingFinanceReportTypeList',) as FinancialReportingFinanceReportTypeList;
    
    }
    return null;
  }

  /// Retrieve a Report Type
  ///
  /// <p>Retrieves the details of a Report Type. (Certain report types require a <a href=\"https://stripe.com/docs/keys#test-live-modes\">live-mode API key</a>.)</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] reportType (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getReportingReportTypesReportTypeWithHttpInfo(String reportType, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/reporting/report_types/{report_type}'
      .replaceAll('{report_type}', reportType);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a Report Type
  ///
  /// <p>Retrieves the details of a Report Type. (Certain report types require a <a href=\"https://stripe.com/docs/keys#test-live-modes\">live-mode API key</a>.)</p>
  ///
  /// Parameters:
  ///
  /// * [String] reportType (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<ReportingReportType?> getReportingReportTypesReportType(String reportType, { List<String>? expand, }) async {
    final response = await getReportingReportTypesReportTypeWithHttpInfo(reportType,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ReportingReportType',) as ReportingReportType;
    
    }
    return null;
  }

  /// List all open reviews
  ///
  /// <p>Returns a list of <code>Review</code> objects that have <code>open</code> set to <code>true</code>. The objects are sorted in descending order by creation date, with the most recently created object appearing first.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return reviews that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getReviewsWithHttpInfo({ GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/reviews';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all open reviews
  ///
  /// <p>Returns a list of <code>Review</code> objects that have <code>open</code> set to <code>true</code>. The objects are sorted in descending order by creation date, with the most recently created object appearing first.</p>
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return reviews that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<RadarReviewList?> getReviews({ GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getReviewsWithHttpInfo( created: created, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'RadarReviewList',) as RadarReviewList;
    
    }
    return null;
  }

  /// Retrieve a review
  ///
  /// <p>Retrieves a <code>Review</code> object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] review (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getReviewsReviewWithHttpInfo(String review, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/reviews/{review}'
      .replaceAll('{review}', review);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a review
  ///
  /// <p>Retrieves a <code>Review</code> object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] review (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Review?> getReviewsReview(String review, { List<String>? expand, }) async {
    final response = await getReviewsReviewWithHttpInfo(review,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Review',) as Review;
    
    }
    return null;
  }

  /// List all SetupAttempts
  ///
  /// <p>Returns a list of SetupAttempts that associate with a provided SetupIntent.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] setupIntent (required):
  ///   Only return SetupAttempts created by the SetupIntent specified by this ID.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp or a dictionary with a number of different query options.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getSetupAttemptsWithHttpInfo(String setupIntent, { GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/setup_attempts';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
      queryParams.addAll(_queryParams('', 'setup_intent', setupIntent));
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all SetupAttempts
  ///
  /// <p>Returns a list of SetupAttempts that associate with a provided SetupIntent.</p>
  ///
  /// Parameters:
  ///
  /// * [String] setupIntent (required):
  ///   Only return SetupAttempts created by the SetupIntent specified by this ID.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp or a dictionary with a number of different query options.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<PaymentFlowsSetupIntentSetupAttemptList?> getSetupAttempts(String setupIntent, { GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getSetupAttemptsWithHttpInfo(setupIntent,  created: created, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentFlowsSetupIntentSetupAttemptList',) as PaymentFlowsSetupIntentSetupAttemptList;
    
    }
    return null;
  }

  /// List all SetupIntents
  ///
  /// <p>Returns a list of SetupIntents.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [bool] attachToSelf:
  ///   If present, the SetupIntent's payment method will be attached to the in-context Stripe Account.  It can only be used for this Stripe Account’s own money movement flows like InboundTransfer and OutboundTransfers. It cannot be set to true when setting up a PaymentMethod for a Customer, and defaults to false when attaching a PaymentMethod to a Customer.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with a number of different query options.
  ///
  /// * [String] customer:
  ///   Only return SetupIntents for the customer specified by this customer ID.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] paymentMethod:
  ///   Only return SetupIntents that associate with the specified payment method.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getSetupIntentsWithHttpInfo({ bool? attachToSelf, GetAccountsCreatedParameter? created, String? customer, String? endingBefore, List<String>? expand, int? limit, String? paymentMethod, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/setup_intents';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (attachToSelf != null) {
      queryParams.addAll(_queryParams('', 'attach_to_self', attachToSelf));
    }
    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (customer != null) {
      queryParams.addAll(_queryParams('', 'customer', customer));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (paymentMethod != null) {
      queryParams.addAll(_queryParams('', 'payment_method', paymentMethod));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all SetupIntents
  ///
  /// <p>Returns a list of SetupIntents.</p>
  ///
  /// Parameters:
  ///
  /// * [bool] attachToSelf:
  ///   If present, the SetupIntent's payment method will be attached to the in-context Stripe Account.  It can only be used for this Stripe Account’s own money movement flows like InboundTransfer and OutboundTransfers. It cannot be set to true when setting up a PaymentMethod for a Customer, and defaults to false when attaching a PaymentMethod to a Customer.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with a number of different query options.
  ///
  /// * [String] customer:
  ///   Only return SetupIntents for the customer specified by this customer ID.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] paymentMethod:
  ///   Only return SetupIntents that associate with the specified payment method.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<PaymentFlowsSetupIntentList?> getSetupIntents({ bool? attachToSelf, GetAccountsCreatedParameter? created, String? customer, String? endingBefore, List<String>? expand, int? limit, String? paymentMethod, String? startingAfter, }) async {
    final response = await getSetupIntentsWithHttpInfo( attachToSelf: attachToSelf, created: created, customer: customer, endingBefore: endingBefore, expand: expand, limit: limit, paymentMethod: paymentMethod, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentFlowsSetupIntentList',) as PaymentFlowsSetupIntentList;
    
    }
    return null;
  }

  /// Retrieve a SetupIntent
  ///
  /// <p>Retrieves the details of a SetupIntent that has previously been created. </p>  <p>Client-side retrieval using a publishable key is allowed when the <code>client_secret</code> is provided in the query string. </p>  <p>When retrieved with a publishable key, only a subset of properties will be returned. Please refer to the <a href=\"#setup_intent_object\">SetupIntent</a> object reference for more details.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] intent (required):
  ///
  /// * [String] clientSecret:
  ///   The client secret of the SetupIntent. We require this string if you use a publishable key to retrieve the SetupIntent.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getSetupIntentsIntentWithHttpInfo(String intent, { String? clientSecret, List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/setup_intents/{intent}'
      .replaceAll('{intent}', intent);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (clientSecret != null) {
      queryParams.addAll(_queryParams('', 'client_secret', clientSecret));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a SetupIntent
  ///
  /// <p>Retrieves the details of a SetupIntent that has previously been created. </p>  <p>Client-side retrieval using a publishable key is allowed when the <code>client_secret</code> is provided in the query string. </p>  <p>When retrieved with a publishable key, only a subset of properties will be returned. Please refer to the <a href=\"#setup_intent_object\">SetupIntent</a> object reference for more details.</p>
  ///
  /// Parameters:
  ///
  /// * [String] intent (required):
  ///
  /// * [String] clientSecret:
  ///   The client secret of the SetupIntent. We require this string if you use a publishable key to retrieve the SetupIntent.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<SetupIntent?> getSetupIntentsIntent(String intent, { String? clientSecret, List<String>? expand, }) async {
    final response = await getSetupIntentsIntentWithHttpInfo(intent,  clientSecret: clientSecret, expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'SetupIntent',) as SetupIntent;
    
    }
    return null;
  }

  /// List all shipping rates
  ///
  /// <p>Returns a list of your shipping rates.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [bool] active:
  ///   Only return shipping rates that are active or inactive.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with a number of different query options.
  ///
  /// * [String] currency:
  ///   Only return shipping rates for the given currency.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getShippingRatesWithHttpInfo({ bool? active, GetAccountsCreatedParameter? created, String? currency, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/shipping_rates';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (active != null) {
      queryParams.addAll(_queryParams('', 'active', active));
    }
    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (currency != null) {
      queryParams.addAll(_queryParams('', 'currency', currency));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all shipping rates
  ///
  /// <p>Returns a list of your shipping rates.</p>
  ///
  /// Parameters:
  ///
  /// * [bool] active:
  ///   Only return shipping rates that are active or inactive.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with a number of different query options.
  ///
  /// * [String] currency:
  ///   Only return shipping rates for the given currency.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<ShippingResourcesShippingRateList?> getShippingRates({ bool? active, GetAccountsCreatedParameter? created, String? currency, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getShippingRatesWithHttpInfo( active: active, created: created, currency: currency, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ShippingResourcesShippingRateList',) as ShippingResourcesShippingRateList;
    
    }
    return null;
  }

  /// Retrieve a shipping rate
  ///
  /// <p>Returns the shipping rate object with the given ID.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] shippingRateToken (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getShippingRatesShippingRateTokenWithHttpInfo(String shippingRateToken, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/shipping_rates/{shipping_rate_token}'
      .replaceAll('{shipping_rate_token}', shippingRateToken);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a shipping rate
  ///
  /// <p>Returns the shipping rate object with the given ID.</p>
  ///
  /// Parameters:
  ///
  /// * [String] shippingRateToken (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<ShippingRate?> getShippingRatesShippingRateToken(String shippingRateToken, { List<String>? expand, }) async {
    final response = await getShippingRatesShippingRateTokenWithHttpInfo(shippingRateToken,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ShippingRate',) as ShippingRate;
    
    }
    return null;
  }

  /// List all scheduled query runs
  ///
  /// <p>Returns a list of scheduled query runs.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getSigmaScheduledQueryRunsWithHttpInfo({ String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/sigma/scheduled_query_runs';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all scheduled query runs
  ///
  /// <p>Returns a list of scheduled query runs.</p>
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<SigmaScheduledQueryRunList?> getSigmaScheduledQueryRuns({ String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getSigmaScheduledQueryRunsWithHttpInfo( endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'SigmaScheduledQueryRunList',) as SigmaScheduledQueryRunList;
    
    }
    return null;
  }

  /// Retrieve a scheduled query run
  ///
  /// <p>Retrieves the details of an scheduled query run.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] scheduledQueryRun (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getSigmaScheduledQueryRunsScheduledQueryRunWithHttpInfo(String scheduledQueryRun, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/sigma/scheduled_query_runs/{scheduled_query_run}'
      .replaceAll('{scheduled_query_run}', scheduledQueryRun);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a scheduled query run
  ///
  /// <p>Retrieves the details of an scheduled query run.</p>
  ///
  /// Parameters:
  ///
  /// * [String] scheduledQueryRun (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<ScheduledQueryRun?> getSigmaScheduledQueryRunsScheduledQueryRun(String scheduledQueryRun, { List<String>? expand, }) async {
    final response = await getSigmaScheduledQueryRunsScheduledQueryRunWithHttpInfo(scheduledQueryRun,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ScheduledQueryRun',) as ScheduledQueryRun;
    
    }
    return null;
  }

  /// Retrieve a source
  ///
  /// <p>Retrieves an existing source object. Supply the unique source ID from a source creation request and Stripe will return the corresponding up-to-date source object information.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] source_ (required):
  ///
  /// * [String] clientSecret:
  ///   The client secret of the source. Required if a publishable key is used to retrieve the source.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getSourcesSourceWithHttpInfo(String source_, { String? clientSecret, List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/sources/{source}'
      .replaceAll('{source}', source_);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (clientSecret != null) {
      queryParams.addAll(_queryParams('', 'client_secret', clientSecret));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a source
  ///
  /// <p>Retrieves an existing source object. Supply the unique source ID from a source creation request and Stripe will return the corresponding up-to-date source object information.</p>
  ///
  /// Parameters:
  ///
  /// * [String] source_ (required):
  ///
  /// * [String] clientSecret:
  ///   The client secret of the source. Required if a publishable key is used to retrieve the source.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<ModelSource?> getSourcesSource(String source_, { String? clientSecret, List<String>? expand, }) async {
    final response = await getSourcesSourceWithHttpInfo(source_,  clientSecret: clientSecret, expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ModelSource',) as ModelSource;
    
    }
    return null;
  }

  /// Retrieve a Source MandateNotification
  ///
  /// <p>Retrieves a new Source MandateNotification.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] mandateNotification (required):
  ///
  /// * [String] source_ (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getSourcesSourceMandateNotificationsMandateNotificationWithHttpInfo(String mandateNotification, String source_, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/sources/{source}/mandate_notifications/{mandate_notification}'
      .replaceAll('{mandate_notification}', mandateNotification)
      .replaceAll('{source}', source_);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a Source MandateNotification
  ///
  /// <p>Retrieves a new Source MandateNotification.</p>
  ///
  /// Parameters:
  ///
  /// * [String] mandateNotification (required):
  ///
  /// * [String] source_ (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<SourceMandateNotification?> getSourcesSourceMandateNotificationsMandateNotification(String mandateNotification, String source_, { List<String>? expand, }) async {
    final response = await getSourcesSourceMandateNotificationsMandateNotificationWithHttpInfo(mandateNotification, source_,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'SourceMandateNotification',) as SourceMandateNotification;
    
    }
    return null;
  }

  /// <p>List source transactions for a given source.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] source_ (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getSourcesSourceSourceTransactionsWithHttpInfo(String source_, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/sources/{source}/source_transactions'
      .replaceAll('{source}', source_);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// <p>List source transactions for a given source.</p>
  ///
  /// Parameters:
  ///
  /// * [String] source_ (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<ApmsSourcesSourceTransactionList?> getSourcesSourceSourceTransactions(String source_, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getSourcesSourceSourceTransactionsWithHttpInfo(source_,  endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ApmsSourcesSourceTransactionList',) as ApmsSourcesSourceTransactionList;
    
    }
    return null;
  }

  /// Retrieve a source transaction
  ///
  /// <p>Retrieve an existing source transaction object. Supply the unique source ID from a source creation request and the source transaction ID and Stripe will return the corresponding up-to-date source object information.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] source_ (required):
  ///
  /// * [String] sourceTransaction (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getSourcesSourceSourceTransactionsSourceTransactionWithHttpInfo(String source_, String sourceTransaction, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/sources/{source}/source_transactions/{source_transaction}'
      .replaceAll('{source}', source_)
      .replaceAll('{source_transaction}', sourceTransaction);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a source transaction
  ///
  /// <p>Retrieve an existing source transaction object. Supply the unique source ID from a source creation request and the source transaction ID and Stripe will return the corresponding up-to-date source object information.</p>
  ///
  /// Parameters:
  ///
  /// * [String] source_ (required):
  ///
  /// * [String] sourceTransaction (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<SourceTransaction?> getSourcesSourceSourceTransactionsSourceTransaction(String source_, String sourceTransaction, { List<String>? expand, }) async {
    final response = await getSourcesSourceSourceTransactionsSourceTransactionWithHttpInfo(source_, sourceTransaction,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'SourceTransaction',) as SourceTransaction;
    
    }
    return null;
  }

  /// List all subscription items
  ///
  /// <p>Returns a list of your subscription items for a given subscription.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] subscription (required):
  ///   The ID of the subscription whose items will be retrieved.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getSubscriptionItemsWithHttpInfo(String subscription, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/subscription_items';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
      queryParams.addAll(_queryParams('', 'subscription', subscription));

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all subscription items
  ///
  /// <p>Returns a list of your subscription items for a given subscription.</p>
  ///
  /// Parameters:
  ///
  /// * [String] subscription (required):
  ///   The ID of the subscription whose items will be retrieved.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<SubscriptionsItemsSubscriptionItemList?> getSubscriptionItems(String subscription, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getSubscriptionItemsWithHttpInfo(subscription,  endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'SubscriptionsItemsSubscriptionItemList',) as SubscriptionsItemsSubscriptionItemList;
    
    }
    return null;
  }

  /// Retrieve a subscription item
  ///
  /// <p>Retrieves the subscription item with the given ID.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] item (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getSubscriptionItemsItemWithHttpInfo(String item, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/subscription_items/{item}'
      .replaceAll('{item}', item);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a subscription item
  ///
  /// <p>Retrieves the subscription item with the given ID.</p>
  ///
  /// Parameters:
  ///
  /// * [String] item (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<SubscriptionItem?> getSubscriptionItemsItem(String item, { List<String>? expand, }) async {
    final response = await getSubscriptionItemsItemWithHttpInfo(item,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'SubscriptionItem',) as SubscriptionItem;
    
    }
    return null;
  }

  /// List all schedules
  ///
  /// <p>Retrieves the list of your subscription schedules.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] canceledAt:
  ///   Only return subscription schedules that were created canceled the given date interval.
  ///
  /// * [GetAccountsCreatedParameter] completedAt:
  ///   Only return subscription schedules that completed during the given date interval.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return subscription schedules that were created during the given date interval.
  ///
  /// * [String] customer:
  ///   Only return subscription schedules for the given customer.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [GetAccountsCreatedParameter] releasedAt:
  ///   Only return subscription schedules that were released during the given date interval.
  ///
  /// * [bool] scheduled:
  ///   Only return subscription schedules that have not started yet.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getSubscriptionSchedulesWithHttpInfo({ GetAccountsCreatedParameter? canceledAt, GetAccountsCreatedParameter? completedAt, GetAccountsCreatedParameter? created, String? customer, String? endingBefore, List<String>? expand, int? limit, GetAccountsCreatedParameter? releasedAt, bool? scheduled, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/subscription_schedules';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (canceledAt != null) {
      queryParams.addAll(_queryParams('', 'canceled_at', canceledAt));
    }
    if (completedAt != null) {
      queryParams.addAll(_queryParams('', 'completed_at', completedAt));
    }
    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (customer != null) {
      queryParams.addAll(_queryParams('', 'customer', customer));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (releasedAt != null) {
      queryParams.addAll(_queryParams('', 'released_at', releasedAt));
    }
    if (scheduled != null) {
      queryParams.addAll(_queryParams('', 'scheduled', scheduled));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all schedules
  ///
  /// <p>Retrieves the list of your subscription schedules.</p>
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] canceledAt:
  ///   Only return subscription schedules that were created canceled the given date interval.
  ///
  /// * [GetAccountsCreatedParameter] completedAt:
  ///   Only return subscription schedules that completed during the given date interval.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return subscription schedules that were created during the given date interval.
  ///
  /// * [String] customer:
  ///   Only return subscription schedules for the given customer.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [GetAccountsCreatedParameter] releasedAt:
  ///   Only return subscription schedules that were released during the given date interval.
  ///
  /// * [bool] scheduled:
  ///   Only return subscription schedules that have not started yet.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<SubscriptionSchedulesResourceScheduleList?> getSubscriptionSchedules({ GetAccountsCreatedParameter? canceledAt, GetAccountsCreatedParameter? completedAt, GetAccountsCreatedParameter? created, String? customer, String? endingBefore, List<String>? expand, int? limit, GetAccountsCreatedParameter? releasedAt, bool? scheduled, String? startingAfter, }) async {
    final response = await getSubscriptionSchedulesWithHttpInfo( canceledAt: canceledAt, completedAt: completedAt, created: created, customer: customer, endingBefore: endingBefore, expand: expand, limit: limit, releasedAt: releasedAt, scheduled: scheduled, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'SubscriptionSchedulesResourceScheduleList',) as SubscriptionSchedulesResourceScheduleList;
    
    }
    return null;
  }

  /// Retrieve a schedule
  ///
  /// <p>Retrieves the details of an existing subscription schedule. You only need to supply the unique subscription schedule identifier that was returned upon subscription schedule creation.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] schedule (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getSubscriptionSchedulesScheduleWithHttpInfo(String schedule, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/subscription_schedules/{schedule}'
      .replaceAll('{schedule}', schedule);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a schedule
  ///
  /// <p>Retrieves the details of an existing subscription schedule. You only need to supply the unique subscription schedule identifier that was returned upon subscription schedule creation.</p>
  ///
  /// Parameters:
  ///
  /// * [String] schedule (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<SubscriptionSchedule?> getSubscriptionSchedulesSchedule(String schedule, { List<String>? expand, }) async {
    final response = await getSubscriptionSchedulesScheduleWithHttpInfo(schedule,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'SubscriptionSchedule',) as SubscriptionSchedule;
    
    }
    return null;
  }

  /// List subscriptions
  ///
  /// <p>By default, returns a list of subscriptions that have not been canceled. In order to list canceled subscriptions, specify <code>status=canceled</code>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [AutomaticTaxFilterParams] automaticTax:
  ///   Filter subscriptions by their automatic tax settings.
  ///
  /// * [String] collectionMethod:
  ///   The collection method of the subscriptions to retrieve. Either `charge_automatically` or `send_invoice`.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return subscriptions that were created during the given date interval.
  ///
  /// * [GetAccountsCreatedParameter] currentPeriodEnd:
  ///   Only return subscriptions whose current_period_end falls within the given date interval.
  ///
  /// * [GetAccountsCreatedParameter] currentPeriodStart:
  ///   Only return subscriptions whose current_period_start falls within the given date interval.
  ///
  /// * [String] customer:
  ///   The ID of the customer whose subscriptions will be retrieved.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] price:
  ///   Filter for subscriptions that contain this recurring price ID.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   The status of the subscriptions to retrieve. Passing in a value of `canceled` will return all canceled subscriptions, including those belonging to deleted customers. Pass `ended` to find subscriptions that are canceled and subscriptions that are expired due to [incomplete payment](https://stripe.com/docs/billing/subscriptions/overview#subscription-statuses). Passing in a value of `all` will return subscriptions of all statuses. If no value is supplied, all subscriptions that have not been canceled are returned.
  ///
  /// * [String] testClock:
  ///   Filter for subscriptions that are associated with the specified test clock. The response will not include subscriptions with test clocks if this and the customer parameter is not set.
  Future<Response> getSubscriptionsWithHttpInfo({ AutomaticTaxFilterParams? automaticTax, String? collectionMethod, GetAccountsCreatedParameter? created, GetAccountsCreatedParameter? currentPeriodEnd, GetAccountsCreatedParameter? currentPeriodStart, String? customer, String? endingBefore, List<String>? expand, int? limit, String? price, String? startingAfter, String? status, String? testClock, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/subscriptions';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (automaticTax != null) {
      queryParams.addAll(_queryParams('', 'automatic_tax', automaticTax));
    }
    if (collectionMethod != null) {
      queryParams.addAll(_queryParams('', 'collection_method', collectionMethod));
    }
    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (currentPeriodEnd != null) {
      queryParams.addAll(_queryParams('', 'current_period_end', currentPeriodEnd));
    }
    if (currentPeriodStart != null) {
      queryParams.addAll(_queryParams('', 'current_period_start', currentPeriodStart));
    }
    if (customer != null) {
      queryParams.addAll(_queryParams('', 'customer', customer));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (price != null) {
      queryParams.addAll(_queryParams('', 'price', price));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (status != null) {
      queryParams.addAll(_queryParams('', 'status', status));
    }
    if (testClock != null) {
      queryParams.addAll(_queryParams('', 'test_clock', testClock));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List subscriptions
  ///
  /// <p>By default, returns a list of subscriptions that have not been canceled. In order to list canceled subscriptions, specify <code>status=canceled</code>.</p>
  ///
  /// Parameters:
  ///
  /// * [AutomaticTaxFilterParams] automaticTax:
  ///   Filter subscriptions by their automatic tax settings.
  ///
  /// * [String] collectionMethod:
  ///   The collection method of the subscriptions to retrieve. Either `charge_automatically` or `send_invoice`.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return subscriptions that were created during the given date interval.
  ///
  /// * [GetAccountsCreatedParameter] currentPeriodEnd:
  ///   Only return subscriptions whose current_period_end falls within the given date interval.
  ///
  /// * [GetAccountsCreatedParameter] currentPeriodStart:
  ///   Only return subscriptions whose current_period_start falls within the given date interval.
  ///
  /// * [String] customer:
  ///   The ID of the customer whose subscriptions will be retrieved.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] price:
  ///   Filter for subscriptions that contain this recurring price ID.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   The status of the subscriptions to retrieve. Passing in a value of `canceled` will return all canceled subscriptions, including those belonging to deleted customers. Pass `ended` to find subscriptions that are canceled and subscriptions that are expired due to [incomplete payment](https://stripe.com/docs/billing/subscriptions/overview#subscription-statuses). Passing in a value of `all` will return subscriptions of all statuses. If no value is supplied, all subscriptions that have not been canceled are returned.
  ///
  /// * [String] testClock:
  ///   Filter for subscriptions that are associated with the specified test clock. The response will not include subscriptions with test clocks if this and the customer parameter is not set.
  Future<SubscriptionsSubscriptionList?> getSubscriptions({ AutomaticTaxFilterParams? automaticTax, String? collectionMethod, GetAccountsCreatedParameter? created, GetAccountsCreatedParameter? currentPeriodEnd, GetAccountsCreatedParameter? currentPeriodStart, String? customer, String? endingBefore, List<String>? expand, int? limit, String? price, String? startingAfter, String? status, String? testClock, }) async {
    final response = await getSubscriptionsWithHttpInfo( automaticTax: automaticTax, collectionMethod: collectionMethod, created: created, currentPeriodEnd: currentPeriodEnd, currentPeriodStart: currentPeriodStart, customer: customer, endingBefore: endingBefore, expand: expand, limit: limit, price: price, startingAfter: startingAfter, status: status, testClock: testClock, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'SubscriptionsSubscriptionList',) as SubscriptionsSubscriptionList;
    
    }
    return null;
  }

  /// Search subscriptions
  ///
  /// <p>Search for subscriptions you’ve previously created using Stripe’s <a href=\"/docs/search#search-query-language\">Search Query Language</a>. Don’t use search in read-after-write flows where strict consistency is necessary. Under normal operating conditions, data is searchable in less than a minute. Occasionally, propagation of new or updated data can be up to an hour behind during outages. Search functionality is not available to merchants in India.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] query (required):
  ///   The search query string. See [search query language](https://stripe.com/docs/search#search-query-language) and the list of supported [query fields for subscriptions](https://stripe.com/docs/search#query-fields-for-subscriptions).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] page:
  ///   A cursor for pagination across multiple pages of results. Don't include this parameter on the first call. Use the next_page value returned in a previous response to request subsequent results.
  Future<Response> getSubscriptionsSearchWithHttpInfo(String query, { List<String>? expand, int? limit, String? page, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/subscriptions/search';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (page != null) {
      queryParams.addAll(_queryParams('', 'page', page));
    }
      queryParams.addAll(_queryParams('', 'query', query));

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Search subscriptions
  ///
  /// <p>Search for subscriptions you’ve previously created using Stripe’s <a href=\"/docs/search#search-query-language\">Search Query Language</a>. Don’t use search in read-after-write flows where strict consistency is necessary. Under normal operating conditions, data is searchable in less than a minute. Occasionally, propagation of new or updated data can be up to an hour behind during outages. Search functionality is not available to merchants in India.</p>
  ///
  /// Parameters:
  ///
  /// * [String] query (required):
  ///   The search query string. See [search query language](https://stripe.com/docs/search#search-query-language) and the list of supported [query fields for subscriptions](https://stripe.com/docs/search#query-fields-for-subscriptions).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] page:
  ///   A cursor for pagination across multiple pages of results. Don't include this parameter on the first call. Use the next_page value returned in a previous response to request subsequent results.
  Future<SearchResult6?> getSubscriptionsSearch(String query, { List<String>? expand, int? limit, String? page, }) async {
    final response = await getSubscriptionsSearchWithHttpInfo(query,  expand: expand, limit: limit, page: page, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'SearchResult6',) as SearchResult6;
    
    }
    return null;
  }

  /// Retrieve a subscription
  ///
  /// <p>Retrieves the subscription with the given ID.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] subscriptionExposedId (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getSubscriptionsSubscriptionExposedIdWithHttpInfo(String subscriptionExposedId, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/subscriptions/{subscription_exposed_id}'
      .replaceAll('{subscription_exposed_id}', subscriptionExposedId);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a subscription
  ///
  /// <p>Retrieves the subscription with the given ID.</p>
  ///
  /// Parameters:
  ///
  /// * [String] subscriptionExposedId (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Subscription?> getSubscriptionsSubscriptionExposedId(String subscriptionExposedId, { List<String>? expand, }) async {
    final response = await getSubscriptionsSubscriptionExposedIdWithHttpInfo(subscriptionExposedId,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Subscription',) as Subscription;
    
    }
    return null;
  }

  /// Retrieve a Tax Calculation
  ///
  /// <p>Retrieves a Tax <code>Calculation</code> object, if the calculation hasn’t expired.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] calculation (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getTaxCalculationsCalculationWithHttpInfo(String calculation, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/tax/calculations/{calculation}'
      .replaceAll('{calculation}', calculation);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a Tax Calculation
  ///
  /// <p>Retrieves a Tax <code>Calculation</code> object, if the calculation hasn’t expired.</p>
  ///
  /// Parameters:
  ///
  /// * [String] calculation (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TaxCalculation?> getTaxCalculationsCalculation(String calculation, { List<String>? expand, }) async {
    final response = await getTaxCalculationsCalculationWithHttpInfo(calculation,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TaxCalculation',) as TaxCalculation;
    
    }
    return null;
  }

  /// Retrieve a calculation's line items
  ///
  /// <p>Retrieves the line items of a tax calculation as a collection, if the calculation hasn’t expired.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] calculation (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getTaxCalculationsCalculationLineItemsWithHttpInfo(String calculation, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/tax/calculations/{calculation}/line_items'
      .replaceAll('{calculation}', calculation);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a calculation's line items
  ///
  /// <p>Retrieves the line items of a tax calculation as a collection, if the calculation hasn’t expired.</p>
  ///
  /// Parameters:
  ///
  /// * [String] calculation (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<TaxProductResourceTaxCalculationLineItemList?> getTaxCalculationsCalculationLineItems(String calculation, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getTaxCalculationsCalculationLineItemsWithHttpInfo(calculation,  endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TaxProductResourceTaxCalculationLineItemList',) as TaxProductResourceTaxCalculationLineItemList;
    
    }
    return null;
  }

  /// List all tax codes
  ///
  /// <p>A list of <a href=\"https://stripe.com/docs/tax/tax-categories\">all tax codes available</a> to add to Products in order to allow specific tax calculations.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getTaxCodesWithHttpInfo({ String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/tax_codes';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all tax codes
  ///
  /// <p>A list of <a href=\"https://stripe.com/docs/tax/tax-categories\">all tax codes available</a> to add to Products in order to allow specific tax calculations.</p>
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<TaxProductResourceTaxCodeList?> getTaxCodes({ String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getTaxCodesWithHttpInfo( endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TaxProductResourceTaxCodeList',) as TaxProductResourceTaxCodeList;
    
    }
    return null;
  }

  /// Retrieve a tax code
  ///
  /// <p>Retrieves the details of an existing tax code. Supply the unique tax code ID and Stripe will return the corresponding tax code information.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getTaxCodesIdWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/tax_codes/{id}'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a tax code
  ///
  /// <p>Retrieves the details of an existing tax code. Supply the unique tax code ID and Stripe will return the corresponding tax code information.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TaxCode?> getTaxCodesId(String id, { List<String>? expand, }) async {
    final response = await getTaxCodesIdWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TaxCode',) as TaxCode;
    
    }
    return null;
  }

  /// List all tax IDs
  ///
  /// <p>Returns a list of tax IDs.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [OwnerParams] owner:
  ///   The account or customer the tax ID belongs to. Defaults to `owner[type]=self`.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getTaxIdsWithHttpInfo({ String? endingBefore, List<String>? expand, int? limit, OwnerParams? owner, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/tax_ids';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (owner != null) {
      queryParams.addAll(_queryParams('', 'owner', owner));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all tax IDs
  ///
  /// <p>Returns a list of tax IDs.</p>
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [OwnerParams] owner:
  ///   The account or customer the tax ID belongs to. Defaults to `owner[type]=self`.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<TaxIDsList?> getTaxIds({ String? endingBefore, List<String>? expand, int? limit, OwnerParams? owner, String? startingAfter, }) async {
    final response = await getTaxIdsWithHttpInfo( endingBefore: endingBefore, expand: expand, limit: limit, owner: owner, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TaxIDsList',) as TaxIDsList;
    
    }
    return null;
  }

  /// Retrieve a tax ID
  ///
  /// <p>Retrieves an account or customer <code>tax_id</code> object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getTaxIdsIdWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/tax_ids/{id}'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a tax ID
  ///
  /// <p>Retrieves an account or customer <code>tax_id</code> object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TaxId?> getTaxIdsId(String id, { List<String>? expand, }) async {
    final response = await getTaxIdsIdWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TaxId',) as TaxId;
    
    }
    return null;
  }

  /// List all tax rates
  ///
  /// <p>Returns a list of your tax rates. Tax rates are returned sorted by creation date, with the most recently created tax rates appearing first.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [bool] active:
  ///   Optional flag to filter by tax rates that are either active or inactive (archived).
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Optional range for filtering created date.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [bool] inclusive:
  ///   Optional flag to filter by tax rates that are inclusive (or those that are not inclusive).
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getTaxRatesWithHttpInfo({ bool? active, GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, bool? inclusive, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/tax_rates';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (active != null) {
      queryParams.addAll(_queryParams('', 'active', active));
    }
    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (inclusive != null) {
      queryParams.addAll(_queryParams('', 'inclusive', inclusive));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all tax rates
  ///
  /// <p>Returns a list of your tax rates. Tax rates are returned sorted by creation date, with the most recently created tax rates appearing first.</p>
  ///
  /// Parameters:
  ///
  /// * [bool] active:
  ///   Optional flag to filter by tax rates that are either active or inactive (archived).
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Optional range for filtering created date.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [bool] inclusive:
  ///   Optional flag to filter by tax rates that are inclusive (or those that are not inclusive).
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<TaxRatesList?> getTaxRates({ bool? active, GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, bool? inclusive, int? limit, String? startingAfter, }) async {
    final response = await getTaxRatesWithHttpInfo( active: active, created: created, endingBefore: endingBefore, expand: expand, inclusive: inclusive, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TaxRatesList',) as TaxRatesList;
    
    }
    return null;
  }

  /// Retrieve a tax rate
  ///
  /// <p>Retrieves a tax rate with the given ID</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] taxRate (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getTaxRatesTaxRateWithHttpInfo(String taxRate, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/tax_rates/{tax_rate}'
      .replaceAll('{tax_rate}', taxRate);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a tax rate
  ///
  /// <p>Retrieves a tax rate with the given ID</p>
  ///
  /// Parameters:
  ///
  /// * [String] taxRate (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TaxRate?> getTaxRatesTaxRate(String taxRate, { List<String>? expand, }) async {
    final response = await getTaxRatesTaxRateWithHttpInfo(taxRate,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TaxRate',) as TaxRate;
    
    }
    return null;
  }

  /// List registrations
  ///
  /// <p>Returns a list of Tax <code>Registration</code> objects.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   The status of the Tax Registration.
  Future<Response> getTaxRegistrationsWithHttpInfo({ String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? status, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/tax/registrations';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (status != null) {
      queryParams.addAll(_queryParams('', 'status', status));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List registrations
  ///
  /// <p>Returns a list of Tax <code>Registration</code> objects.</p>
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   The status of the Tax Registration.
  Future<TaxProductRegistrationsResourceTaxRegistrationList?> getTaxRegistrations({ String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? status, }) async {
    final response = await getTaxRegistrationsWithHttpInfo( endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, status: status, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TaxProductRegistrationsResourceTaxRegistrationList',) as TaxProductRegistrationsResourceTaxRegistrationList;
    
    }
    return null;
  }

  /// Retrieve a registration
  ///
  /// <p>Returns a Tax <code>Registration</code> object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getTaxRegistrationsIdWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/tax/registrations/{id}'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a registration
  ///
  /// <p>Returns a Tax <code>Registration</code> object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TaxRegistration?> getTaxRegistrationsId(String id, { List<String>? expand, }) async {
    final response = await getTaxRegistrationsIdWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TaxRegistration',) as TaxRegistration;
    
    }
    return null;
  }

  /// Retrieve settings
  ///
  /// <p>Retrieves Tax <code>Settings</code> for a merchant.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getTaxSettingsWithHttpInfo({ List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/tax/settings';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve settings
  ///
  /// <p>Retrieves Tax <code>Settings</code> for a merchant.</p>
  ///
  /// Parameters:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TaxSettings?> getTaxSettings({ List<String>? expand, }) async {
    final response = await getTaxSettingsWithHttpInfo( expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TaxSettings',) as TaxSettings;
    
    }
    return null;
  }

  /// Retrieve a transaction
  ///
  /// <p>Retrieves a Tax <code>Transaction</code> object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] transaction (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getTaxTransactionsTransactionWithHttpInfo(String transaction, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/tax/transactions/{transaction}'
      .replaceAll('{transaction}', transaction);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a transaction
  ///
  /// <p>Retrieves a Tax <code>Transaction</code> object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] transaction (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TaxTransaction?> getTaxTransactionsTransaction(String transaction, { List<String>? expand, }) async {
    final response = await getTaxTransactionsTransactionWithHttpInfo(transaction,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TaxTransaction',) as TaxTransaction;
    
    }
    return null;
  }

  /// Retrieve a transaction's line items
  ///
  /// <p>Retrieves the line items of a committed standalone transaction as a collection.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] transaction (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getTaxTransactionsTransactionLineItemsWithHttpInfo(String transaction, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/tax/transactions/{transaction}/line_items'
      .replaceAll('{transaction}', transaction);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a transaction's line items
  ///
  /// <p>Retrieves the line items of a committed standalone transaction as a collection.</p>
  ///
  /// Parameters:
  ///
  /// * [String] transaction (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<TaxProductResourceTaxTransactionLineItemList?> getTaxTransactionsTransactionLineItems(String transaction, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getTaxTransactionsTransactionLineItemsWithHttpInfo(transaction,  endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TaxProductResourceTaxTransactionLineItemList',) as TaxProductResourceTaxTransactionLineItemList;
    
    }
    return null;
  }

  /// List all Configurations
  ///
  /// <p>Returns a list of <code>Configuration</code> objects.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [bool] isAccountDefault:
  ///   if present, only return the account default or non-default configurations.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getTerminalConfigurationsWithHttpInfo({ String? endingBefore, List<String>? expand, bool? isAccountDefault, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/terminal/configurations';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (isAccountDefault != null) {
      queryParams.addAll(_queryParams('', 'is_account_default', isAccountDefault));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all Configurations
  ///
  /// <p>Returns a list of <code>Configuration</code> objects.</p>
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [bool] isAccountDefault:
  ///   if present, only return the account default or non-default configurations.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<TerminalConfigurationConfigurationList?> getTerminalConfigurations({ String? endingBefore, List<String>? expand, bool? isAccountDefault, int? limit, String? startingAfter, }) async {
    final response = await getTerminalConfigurationsWithHttpInfo( endingBefore: endingBefore, expand: expand, isAccountDefault: isAccountDefault, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TerminalConfigurationConfigurationList',) as TerminalConfigurationConfigurationList;
    
    }
    return null;
  }

  /// Retrieve a Configuration
  ///
  /// <p>Retrieves a <code>Configuration</code> object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] configuration (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getTerminalConfigurationsConfigurationWithHttpInfo(String configuration, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/terminal/configurations/{configuration}'
      .replaceAll('{configuration}', configuration);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a Configuration
  ///
  /// <p>Retrieves a <code>Configuration</code> object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] configuration (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<GetTerminalConfigurationsConfiguration200Response?> getTerminalConfigurationsConfiguration(String configuration, { List<String>? expand, }) async {
    final response = await getTerminalConfigurationsConfigurationWithHttpInfo(configuration,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'GetTerminalConfigurationsConfiguration200Response',) as GetTerminalConfigurationsConfiguration200Response;
    
    }
    return null;
  }

  /// List all Locations
  ///
  /// <p>Returns a list of <code>Location</code> objects.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getTerminalLocationsWithHttpInfo({ String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/terminal/locations';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all Locations
  ///
  /// <p>Returns a list of <code>Location</code> objects.</p>
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<TerminalLocationLocationList?> getTerminalLocations({ String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getTerminalLocationsWithHttpInfo( endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TerminalLocationLocationList',) as TerminalLocationLocationList;
    
    }
    return null;
  }

  /// Retrieve a Location
  ///
  /// <p>Retrieves a <code>Location</code> object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] location (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getTerminalLocationsLocationWithHttpInfo(String location, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/terminal/locations/{location}'
      .replaceAll('{location}', location);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a Location
  ///
  /// <p>Retrieves a <code>Location</code> object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] location (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<GetTerminalLocationsLocation200Response?> getTerminalLocationsLocation(String location, { List<String>? expand, }) async {
    final response = await getTerminalLocationsLocationWithHttpInfo(location,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'GetTerminalLocationsLocation200Response',) as GetTerminalLocationsLocation200Response;
    
    }
    return null;
  }

  /// List all Readers
  ///
  /// <p>Returns a list of <code>Reader</code> objects.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] deviceType:
  ///   Filters readers by device type
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] location:
  ///   A location ID to filter the response list to only readers at the specific location
  ///
  /// * [String] serialNumber:
  ///   Filters readers by serial number
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   A status filter to filter readers to only offline or online readers
  Future<Response> getTerminalReadersWithHttpInfo({ String? deviceType, String? endingBefore, List<String>? expand, int? limit, String? location, String? serialNumber, String? startingAfter, String? status, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/terminal/readers';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (deviceType != null) {
      queryParams.addAll(_queryParams('', 'device_type', deviceType));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (location != null) {
      queryParams.addAll(_queryParams('', 'location', location));
    }
    if (serialNumber != null) {
      queryParams.addAll(_queryParams('', 'serial_number', serialNumber));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (status != null) {
      queryParams.addAll(_queryParams('', 'status', status));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all Readers
  ///
  /// <p>Returns a list of <code>Reader</code> objects.</p>
  ///
  /// Parameters:
  ///
  /// * [String] deviceType:
  ///   Filters readers by device type
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] location:
  ///   A location ID to filter the response list to only readers at the specific location
  ///
  /// * [String] serialNumber:
  ///   Filters readers by serial number
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   A status filter to filter readers to only offline or online readers
  Future<TerminalReaderRetrieveReader?> getTerminalReaders({ String? deviceType, String? endingBefore, List<String>? expand, int? limit, String? location, String? serialNumber, String? startingAfter, String? status, }) async {
    final response = await getTerminalReadersWithHttpInfo( deviceType: deviceType, endingBefore: endingBefore, expand: expand, limit: limit, location: location, serialNumber: serialNumber, startingAfter: startingAfter, status: status, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TerminalReaderRetrieveReader',) as TerminalReaderRetrieveReader;
    
    }
    return null;
  }

  /// Retrieve a Reader
  ///
  /// <p>Retrieves a <code>Reader</code> object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] reader (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getTerminalReadersReaderWithHttpInfo(String reader, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/terminal/readers/{reader}'
      .replaceAll('{reader}', reader);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a Reader
  ///
  /// <p>Retrieves a <code>Reader</code> object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] reader (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<GetTerminalReadersReader200Response?> getTerminalReadersReader(String reader, { List<String>? expand, }) async {
    final response = await getTerminalReadersReaderWithHttpInfo(reader,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'GetTerminalReadersReader200Response',) as GetTerminalReadersReader200Response;
    
    }
    return null;
  }

  /// List all test clocks
  ///
  /// <p>Returns a list of your test clocks.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getTestHelpersTestClocksWithHttpInfo({ String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/test_clocks';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all test clocks
  ///
  /// <p>Returns a list of your test clocks.</p>
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<BillingClocksResourceBillingClockList?> getTestHelpersTestClocks({ String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getTestHelpersTestClocksWithHttpInfo( endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BillingClocksResourceBillingClockList',) as BillingClocksResourceBillingClockList;
    
    }
    return null;
  }

  /// Retrieve a test clock
  ///
  /// <p>Retrieves a test clock.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] testClock (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getTestHelpersTestClocksTestClockWithHttpInfo(String testClock, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/test_clocks/{test_clock}'
      .replaceAll('{test_clock}', testClock);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a test clock
  ///
  /// <p>Retrieves a test clock.</p>
  ///
  /// Parameters:
  ///
  /// * [String] testClock (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TestHelpersTestClock?> getTestHelpersTestClocksTestClock(String testClock, { List<String>? expand, }) async {
    final response = await getTestHelpersTestClocksTestClockWithHttpInfo(testClock,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TestHelpersTestClock',) as TestHelpersTestClock;
    
    }
    return null;
  }

  /// Retrieve a token
  ///
  /// <p>Retrieves the token with the given ID.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] token (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getTokensTokenWithHttpInfo(String token, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/tokens/{token}'
      .replaceAll('{token}', token);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a token
  ///
  /// <p>Retrieves the token with the given ID.</p>
  ///
  /// Parameters:
  ///
  /// * [String] token (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Token?> getTokensToken(String token, { List<String>? expand, }) async {
    final response = await getTokensTokenWithHttpInfo(token,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Token',) as Token;
    
    }
    return null;
  }

  /// List all top-ups
  ///
  /// <p>Returns a list of top-ups.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] amount:
  ///   A positive integer representing how much to transfer.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with a number of different query options.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return top-ups that have the given status. One of `canceled`, `failed`, `pending` or `succeeded`.
  Future<Response> getTopupsWithHttpInfo({ GetAccountsCreatedParameter? amount, GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? status, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/topups';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (amount != null) {
      queryParams.addAll(_queryParams('', 'amount', amount));
    }
    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (status != null) {
      queryParams.addAll(_queryParams('', 'status', status));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all top-ups
  ///
  /// <p>Returns a list of top-ups.</p>
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] amount:
  ///   A positive integer representing how much to transfer.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   A filter on the list, based on the object `created` field. The value can be a string with an integer Unix timestamp, or it can be a dictionary with a number of different query options.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return top-ups that have the given status. One of `canceled`, `failed`, `pending` or `succeeded`.
  Future<TopupList?> getTopups({ GetAccountsCreatedParameter? amount, GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? status, }) async {
    final response = await getTopupsWithHttpInfo( amount: amount, created: created, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, status: status, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TopupList',) as TopupList;
    
    }
    return null;
  }

  /// Retrieve a top-up
  ///
  /// <p>Retrieves the details of a top-up that has previously been created. Supply the unique top-up ID that was returned from your previous request, and Stripe will return the corresponding top-up information.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] topup (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getTopupsTopupWithHttpInfo(String topup, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/topups/{topup}'
      .replaceAll('{topup}', topup);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a top-up
  ///
  /// <p>Retrieves the details of a top-up that has previously been created. Supply the unique top-up ID that was returned from your previous request, and Stripe will return the corresponding top-up information.</p>
  ///
  /// Parameters:
  ///
  /// * [String] topup (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Topup?> getTopupsTopup(String topup, { List<String>? expand, }) async {
    final response = await getTopupsTopupWithHttpInfo(topup,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Topup',) as Topup;
    
    }
    return null;
  }

  /// List all transfers
  ///
  /// <p>Returns a list of existing transfers sent to connected accounts. The transfers are returned in sorted order, with the most recently created transfers appearing first.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return transfers that were created during the given date interval.
  ///
  /// * [String] destination:
  ///   Only return transfers for the destination specified by this account ID.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] transferGroup:
  ///   Only return transfers with the specified transfer group.
  Future<Response> getTransfersWithHttpInfo({ GetAccountsCreatedParameter? created, String? destination, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? transferGroup, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/transfers';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (destination != null) {
      queryParams.addAll(_queryParams('', 'destination', destination));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (transferGroup != null) {
      queryParams.addAll(_queryParams('', 'transfer_group', transferGroup));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all transfers
  ///
  /// <p>Returns a list of existing transfers sent to connected accounts. The transfers are returned in sorted order, with the most recently created transfers appearing first.</p>
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return transfers that were created during the given date interval.
  ///
  /// * [String] destination:
  ///   Only return transfers for the destination specified by this account ID.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] transferGroup:
  ///   Only return transfers with the specified transfer group.
  Future<TransferList?> getTransfers({ GetAccountsCreatedParameter? created, String? destination, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? transferGroup, }) async {
    final response = await getTransfersWithHttpInfo( created: created, destination: destination, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, transferGroup: transferGroup, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TransferList',) as TransferList;
    
    }
    return null;
  }

  /// List all reversals
  ///
  /// <p>You can see a list of the reversals belonging to a specific transfer. Note that the 10 most recent reversals are always available by default on the transfer object. If you need more than those 10, you can use this API method and the <code>limit</code> and <code>starting_after</code> parameters to page through additional reversals.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getTransfersIdReversalsWithHttpInfo(String id, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/transfers/{id}/reversals'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all reversals
  ///
  /// <p>You can see a list of the reversals belonging to a specific transfer. Note that the 10 most recent reversals are always available by default on the transfer object. If you need more than those 10, you can use this API method and the <code>limit</code> and <code>starting_after</code> parameters to page through additional reversals.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<TransferReversalList?> getTransfersIdReversals(String id, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getTransfersIdReversalsWithHttpInfo(id,  endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TransferReversalList',) as TransferReversalList;
    
    }
    return null;
  }

  /// Retrieve a transfer
  ///
  /// <p>Retrieves the details of an existing transfer. Supply the unique transfer ID from either a transfer creation request or the transfer list, and Stripe will return the corresponding transfer information.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] transfer (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getTransfersTransferWithHttpInfo(String transfer, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/transfers/{transfer}'
      .replaceAll('{transfer}', transfer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a transfer
  ///
  /// <p>Retrieves the details of an existing transfer. Supply the unique transfer ID from either a transfer creation request or the transfer list, and Stripe will return the corresponding transfer information.</p>
  ///
  /// Parameters:
  ///
  /// * [String] transfer (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Transfer?> getTransfersTransfer(String transfer, { List<String>? expand, }) async {
    final response = await getTransfersTransferWithHttpInfo(transfer,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Transfer',) as Transfer;
    
    }
    return null;
  }

  /// Retrieve a reversal
  ///
  /// <p>By default, you can see the 10 most recent reversals stored directly on the transfer object, but you can also retrieve details about a specific reversal stored on the transfer.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [String] transfer (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getTransfersTransferReversalsIdWithHttpInfo(String id, String transfer, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/transfers/{transfer}/reversals/{id}'
      .replaceAll('{id}', id)
      .replaceAll('{transfer}', transfer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a reversal
  ///
  /// <p>By default, you can see the 10 most recent reversals stored directly on the transfer object, but you can also retrieve details about a specific reversal stored on the transfer.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [String] transfer (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TransferReversal?> getTransfersTransferReversalsId(String id, String transfer, { List<String>? expand, }) async {
    final response = await getTransfersTransferReversalsIdWithHttpInfo(id, transfer,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TransferReversal',) as TransferReversal;
    
    }
    return null;
  }

  /// List all CreditReversals
  ///
  /// <p>Returns a list of CreditReversals.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] financialAccount (required):
  ///   Returns objects associated with this FinancialAccount.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] receivedCredit:
  ///   Only return CreditReversals for the ReceivedCredit ID.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return CreditReversals for a given status.
  Future<Response> getTreasuryCreditReversalsWithHttpInfo(String financialAccount, { String? endingBefore, List<String>? expand, int? limit, String? receivedCredit, String? startingAfter, String? status, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/treasury/credit_reversals';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
      queryParams.addAll(_queryParams('', 'financial_account', financialAccount));
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (receivedCredit != null) {
      queryParams.addAll(_queryParams('', 'received_credit', receivedCredit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (status != null) {
      queryParams.addAll(_queryParams('', 'status', status));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all CreditReversals
  ///
  /// <p>Returns a list of CreditReversals.</p>
  ///
  /// Parameters:
  ///
  /// * [String] financialAccount (required):
  ///   Returns objects associated with this FinancialAccount.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] receivedCredit:
  ///   Only return CreditReversals for the ReceivedCredit ID.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return CreditReversals for a given status.
  Future<TreasuryReceivedCreditsResourceCreditReversalList?> getTreasuryCreditReversals(String financialAccount, { String? endingBefore, List<String>? expand, int? limit, String? receivedCredit, String? startingAfter, String? status, }) async {
    final response = await getTreasuryCreditReversalsWithHttpInfo(financialAccount,  endingBefore: endingBefore, expand: expand, limit: limit, receivedCredit: receivedCredit, startingAfter: startingAfter, status: status, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryReceivedCreditsResourceCreditReversalList',) as TreasuryReceivedCreditsResourceCreditReversalList;
    
    }
    return null;
  }

  /// Retrieve a CreditReversal
  ///
  /// <p>Retrieves the details of an existing CreditReversal by passing the unique CreditReversal ID from either the CreditReversal creation request or CreditReversal list</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] creditReversal (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getTreasuryCreditReversalsCreditReversalWithHttpInfo(String creditReversal, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/treasury/credit_reversals/{credit_reversal}'
      .replaceAll('{credit_reversal}', creditReversal);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a CreditReversal
  ///
  /// <p>Retrieves the details of an existing CreditReversal by passing the unique CreditReversal ID from either the CreditReversal creation request or CreditReversal list</p>
  ///
  /// Parameters:
  ///
  /// * [String] creditReversal (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TreasuryCreditReversal?> getTreasuryCreditReversalsCreditReversal(String creditReversal, { List<String>? expand, }) async {
    final response = await getTreasuryCreditReversalsCreditReversalWithHttpInfo(creditReversal,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryCreditReversal',) as TreasuryCreditReversal;
    
    }
    return null;
  }

  /// List all DebitReversals
  ///
  /// <p>Returns a list of DebitReversals.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] financialAccount (required):
  ///   Returns objects associated with this FinancialAccount.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] receivedDebit:
  ///   Only return DebitReversals for the ReceivedDebit ID.
  ///
  /// * [String] resolution:
  ///   Only return DebitReversals for a given resolution.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return DebitReversals for a given status.
  Future<Response> getTreasuryDebitReversalsWithHttpInfo(String financialAccount, { String? endingBefore, List<String>? expand, int? limit, String? receivedDebit, String? resolution, String? startingAfter, String? status, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/treasury/debit_reversals';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
      queryParams.addAll(_queryParams('', 'financial_account', financialAccount));
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (receivedDebit != null) {
      queryParams.addAll(_queryParams('', 'received_debit', receivedDebit));
    }
    if (resolution != null) {
      queryParams.addAll(_queryParams('', 'resolution', resolution));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (status != null) {
      queryParams.addAll(_queryParams('', 'status', status));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all DebitReversals
  ///
  /// <p>Returns a list of DebitReversals.</p>
  ///
  /// Parameters:
  ///
  /// * [String] financialAccount (required):
  ///   Returns objects associated with this FinancialAccount.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] receivedDebit:
  ///   Only return DebitReversals for the ReceivedDebit ID.
  ///
  /// * [String] resolution:
  ///   Only return DebitReversals for a given resolution.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return DebitReversals for a given status.
  Future<TreasuryReceivedDebitsResourceDebitReversalList?> getTreasuryDebitReversals(String financialAccount, { String? endingBefore, List<String>? expand, int? limit, String? receivedDebit, String? resolution, String? startingAfter, String? status, }) async {
    final response = await getTreasuryDebitReversalsWithHttpInfo(financialAccount,  endingBefore: endingBefore, expand: expand, limit: limit, receivedDebit: receivedDebit, resolution: resolution, startingAfter: startingAfter, status: status, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryReceivedDebitsResourceDebitReversalList',) as TreasuryReceivedDebitsResourceDebitReversalList;
    
    }
    return null;
  }

  /// Retrieve a DebitReversal
  ///
  /// <p>Retrieves a DebitReversal object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] debitReversal (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getTreasuryDebitReversalsDebitReversalWithHttpInfo(String debitReversal, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/treasury/debit_reversals/{debit_reversal}'
      .replaceAll('{debit_reversal}', debitReversal);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a DebitReversal
  ///
  /// <p>Retrieves a DebitReversal object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] debitReversal (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TreasuryDebitReversal?> getTreasuryDebitReversalsDebitReversal(String debitReversal, { List<String>? expand, }) async {
    final response = await getTreasuryDebitReversalsDebitReversalWithHttpInfo(debitReversal,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryDebitReversal',) as TreasuryDebitReversal;
    
    }
    return null;
  }

  /// List all FinancialAccounts
  ///
  /// <p>Returns a list of FinancialAccounts.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return FinancialAccounts that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   An object ID cursor for use in pagination.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit ranging from 1 to 100 (defaults to 10).
  ///
  /// * [String] startingAfter:
  ///   An object ID cursor for use in pagination.
  ///
  /// * [String] status:
  ///   Only return FinancialAccounts that have the given status: `open` or `closed`
  Future<Response> getTreasuryFinancialAccountsWithHttpInfo({ GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? status, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/treasury/financial_accounts';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (status != null) {
      queryParams.addAll(_queryParams('', 'status', status));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all FinancialAccounts
  ///
  /// <p>Returns a list of FinancialAccounts.</p>
  ///
  /// Parameters:
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return FinancialAccounts that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   An object ID cursor for use in pagination.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit ranging from 1 to 100 (defaults to 10).
  ///
  /// * [String] startingAfter:
  ///   An object ID cursor for use in pagination.
  ///
  /// * [String] status:
  ///   Only return FinancialAccounts that have the given status: `open` or `closed`
  Future<TreasuryFinancialAccountsResourceFinancialAccountList?> getTreasuryFinancialAccounts({ GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? status, }) async {
    final response = await getTreasuryFinancialAccountsWithHttpInfo( created: created, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, status: status, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryFinancialAccountsResourceFinancialAccountList',) as TreasuryFinancialAccountsResourceFinancialAccountList;
    
    }
    return null;
  }

  /// Retrieve a FinancialAccount
  ///
  /// <p>Retrieves the details of a FinancialAccount.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] financialAccount (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getTreasuryFinancialAccountsFinancialAccountWithHttpInfo(String financialAccount, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/treasury/financial_accounts/{financial_account}'
      .replaceAll('{financial_account}', financialAccount);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a FinancialAccount
  ///
  /// <p>Retrieves the details of a FinancialAccount.</p>
  ///
  /// Parameters:
  ///
  /// * [String] financialAccount (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TreasuryFinancialAccount?> getTreasuryFinancialAccountsFinancialAccount(String financialAccount, { List<String>? expand, }) async {
    final response = await getTreasuryFinancialAccountsFinancialAccountWithHttpInfo(financialAccount,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryFinancialAccount',) as TreasuryFinancialAccount;
    
    }
    return null;
  }

  /// Retrieve FinancialAccount Features
  ///
  /// <p>Retrieves Features information associated with the FinancialAccount.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] financialAccount (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getTreasuryFinancialAccountsFinancialAccountFeaturesWithHttpInfo(String financialAccount, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/treasury/financial_accounts/{financial_account}/features'
      .replaceAll('{financial_account}', financialAccount);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve FinancialAccount Features
  ///
  /// <p>Retrieves Features information associated with the FinancialAccount.</p>
  ///
  /// Parameters:
  ///
  /// * [String] financialAccount (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TreasuryFinancialAccountFeatures?> getTreasuryFinancialAccountsFinancialAccountFeatures(String financialAccount, { List<String>? expand, }) async {
    final response = await getTreasuryFinancialAccountsFinancialAccountFeaturesWithHttpInfo(financialAccount,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryFinancialAccountFeatures',) as TreasuryFinancialAccountFeatures;
    
    }
    return null;
  }

  /// List all InboundTransfers
  ///
  /// <p>Returns a list of InboundTransfers sent from the specified FinancialAccount.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] financialAccount (required):
  ///   Returns objects associated with this FinancialAccount.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return InboundTransfers that have the given status: `processing`, `succeeded`, `failed` or `canceled`.
  Future<Response> getTreasuryInboundTransfersWithHttpInfo(String financialAccount, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? status, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/treasury/inbound_transfers';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
      queryParams.addAll(_queryParams('', 'financial_account', financialAccount));
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (status != null) {
      queryParams.addAll(_queryParams('', 'status', status));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all InboundTransfers
  ///
  /// <p>Returns a list of InboundTransfers sent from the specified FinancialAccount.</p>
  ///
  /// Parameters:
  ///
  /// * [String] financialAccount (required):
  ///   Returns objects associated with this FinancialAccount.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return InboundTransfers that have the given status: `processing`, `succeeded`, `failed` or `canceled`.
  Future<TreasuryInboundTransfersResourceInboundTransferList?> getTreasuryInboundTransfers(String financialAccount, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? status, }) async {
    final response = await getTreasuryInboundTransfersWithHttpInfo(financialAccount,  endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, status: status, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryInboundTransfersResourceInboundTransferList',) as TreasuryInboundTransfersResourceInboundTransferList;
    
    }
    return null;
  }

  /// Retrieve an InboundTransfer
  ///
  /// <p>Retrieves the details of an existing InboundTransfer.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getTreasuryInboundTransfersIdWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/treasury/inbound_transfers/{id}'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve an InboundTransfer
  ///
  /// <p>Retrieves the details of an existing InboundTransfer.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TreasuryInboundTransfer?> getTreasuryInboundTransfersId(String id, { List<String>? expand, }) async {
    final response = await getTreasuryInboundTransfersIdWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryInboundTransfer',) as TreasuryInboundTransfer;
    
    }
    return null;
  }

  /// List all OutboundPayments
  ///
  /// <p>Returns a list of OutboundPayments sent from the specified FinancialAccount.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] financialAccount (required):
  ///   Returns objects associated with this FinancialAccount.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return OutboundPayments that were created during the given date interval.
  ///
  /// * [String] customer:
  ///   Only return OutboundPayments sent to this customer.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return OutboundPayments that have the given status: `processing`, `failed`, `posted`, `returned`, or `canceled`.
  Future<Response> getTreasuryOutboundPaymentsWithHttpInfo(String financialAccount, { GetAccountsCreatedParameter? created, String? customer, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? status, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/treasury/outbound_payments';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (customer != null) {
      queryParams.addAll(_queryParams('', 'customer', customer));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
      queryParams.addAll(_queryParams('', 'financial_account', financialAccount));
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (status != null) {
      queryParams.addAll(_queryParams('', 'status', status));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all OutboundPayments
  ///
  /// <p>Returns a list of OutboundPayments sent from the specified FinancialAccount.</p>
  ///
  /// Parameters:
  ///
  /// * [String] financialAccount (required):
  ///   Returns objects associated with this FinancialAccount.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return OutboundPayments that were created during the given date interval.
  ///
  /// * [String] customer:
  ///   Only return OutboundPayments sent to this customer.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return OutboundPayments that have the given status: `processing`, `failed`, `posted`, `returned`, or `canceled`.
  Future<TreasuryOutboundPaymentsResourceOutboundPaymentList?> getTreasuryOutboundPayments(String financialAccount, { GetAccountsCreatedParameter? created, String? customer, String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? status, }) async {
    final response = await getTreasuryOutboundPaymentsWithHttpInfo(financialAccount,  created: created, customer: customer, endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, status: status, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryOutboundPaymentsResourceOutboundPaymentList',) as TreasuryOutboundPaymentsResourceOutboundPaymentList;
    
    }
    return null;
  }

  /// Retrieve an OutboundPayment
  ///
  /// <p>Retrieves the details of an existing OutboundPayment by passing the unique OutboundPayment ID from either the OutboundPayment creation request or OutboundPayment list.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getTreasuryOutboundPaymentsIdWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/treasury/outbound_payments/{id}'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve an OutboundPayment
  ///
  /// <p>Retrieves the details of an existing OutboundPayment by passing the unique OutboundPayment ID from either the OutboundPayment creation request or OutboundPayment list.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TreasuryOutboundPayment?> getTreasuryOutboundPaymentsId(String id, { List<String>? expand, }) async {
    final response = await getTreasuryOutboundPaymentsIdWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryOutboundPayment',) as TreasuryOutboundPayment;
    
    }
    return null;
  }

  /// List all OutboundTransfers
  ///
  /// <p>Returns a list of OutboundTransfers sent from the specified FinancialAccount.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] financialAccount (required):
  ///   Returns objects associated with this FinancialAccount.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return OutboundTransfers that have the given status: `processing`, `canceled`, `failed`, `posted`, or `returned`.
  Future<Response> getTreasuryOutboundTransfersWithHttpInfo(String financialAccount, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? status, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/treasury/outbound_transfers';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
      queryParams.addAll(_queryParams('', 'financial_account', financialAccount));
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (status != null) {
      queryParams.addAll(_queryParams('', 'status', status));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all OutboundTransfers
  ///
  /// <p>Returns a list of OutboundTransfers sent from the specified FinancialAccount.</p>
  ///
  /// Parameters:
  ///
  /// * [String] financialAccount (required):
  ///   Returns objects associated with this FinancialAccount.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return OutboundTransfers that have the given status: `processing`, `canceled`, `failed`, `posted`, or `returned`.
  Future<TreasuryOutboundTransfersResourceOutboundTransferList?> getTreasuryOutboundTransfers(String financialAccount, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? status, }) async {
    final response = await getTreasuryOutboundTransfersWithHttpInfo(financialAccount,  endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, status: status, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryOutboundTransfersResourceOutboundTransferList',) as TreasuryOutboundTransfersResourceOutboundTransferList;
    
    }
    return null;
  }

  /// Retrieve an OutboundTransfer
  ///
  /// <p>Retrieves the details of an existing OutboundTransfer by passing the unique OutboundTransfer ID from either the OutboundTransfer creation request or OutboundTransfer list.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] outboundTransfer (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getTreasuryOutboundTransfersOutboundTransferWithHttpInfo(String outboundTransfer, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/treasury/outbound_transfers/{outbound_transfer}'
      .replaceAll('{outbound_transfer}', outboundTransfer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve an OutboundTransfer
  ///
  /// <p>Retrieves the details of an existing OutboundTransfer by passing the unique OutboundTransfer ID from either the OutboundTransfer creation request or OutboundTransfer list.</p>
  ///
  /// Parameters:
  ///
  /// * [String] outboundTransfer (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TreasuryOutboundTransfer?> getTreasuryOutboundTransfersOutboundTransfer(String outboundTransfer, { List<String>? expand, }) async {
    final response = await getTreasuryOutboundTransfersOutboundTransferWithHttpInfo(outboundTransfer,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryOutboundTransfer',) as TreasuryOutboundTransfer;
    
    }
    return null;
  }

  /// List all ReceivedCredits
  ///
  /// <p>Returns a list of ReceivedCredits.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] financialAccount (required):
  ///   The FinancialAccount that received the funds.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [LinkedFlowsParam] linkedFlows:
  ///   Only return ReceivedCredits described by the flow.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return ReceivedCredits that have the given status: `succeeded` or `failed`.
  Future<Response> getTreasuryReceivedCreditsWithHttpInfo(String financialAccount, { String? endingBefore, List<String>? expand, int? limit, LinkedFlowsParam? linkedFlows, String? startingAfter, String? status, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/treasury/received_credits';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
      queryParams.addAll(_queryParams('', 'financial_account', financialAccount));
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (linkedFlows != null) {
      queryParams.addAll(_queryParams('', 'linked_flows', linkedFlows));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (status != null) {
      queryParams.addAll(_queryParams('', 'status', status));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all ReceivedCredits
  ///
  /// <p>Returns a list of ReceivedCredits.</p>
  ///
  /// Parameters:
  ///
  /// * [String] financialAccount (required):
  ///   The FinancialAccount that received the funds.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [LinkedFlowsParam] linkedFlows:
  ///   Only return ReceivedCredits described by the flow.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return ReceivedCredits that have the given status: `succeeded` or `failed`.
  Future<TreasuryReceivedCreditsResourceReceivedCreditList?> getTreasuryReceivedCredits(String financialAccount, { String? endingBefore, List<String>? expand, int? limit, LinkedFlowsParam? linkedFlows, String? startingAfter, String? status, }) async {
    final response = await getTreasuryReceivedCreditsWithHttpInfo(financialAccount,  endingBefore: endingBefore, expand: expand, limit: limit, linkedFlows: linkedFlows, startingAfter: startingAfter, status: status, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryReceivedCreditsResourceReceivedCreditList',) as TreasuryReceivedCreditsResourceReceivedCreditList;
    
    }
    return null;
  }

  /// Retrieve a ReceivedCredit
  ///
  /// <p>Retrieves the details of an existing ReceivedCredit by passing the unique ReceivedCredit ID from the ReceivedCredit list.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getTreasuryReceivedCreditsIdWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/treasury/received_credits/{id}'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a ReceivedCredit
  ///
  /// <p>Retrieves the details of an existing ReceivedCredit by passing the unique ReceivedCredit ID from the ReceivedCredit list.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TreasuryReceivedCredit?> getTreasuryReceivedCreditsId(String id, { List<String>? expand, }) async {
    final response = await getTreasuryReceivedCreditsIdWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryReceivedCredit',) as TreasuryReceivedCredit;
    
    }
    return null;
  }

  /// List all ReceivedDebits
  ///
  /// <p>Returns a list of ReceivedDebits.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] financialAccount (required):
  ///   The FinancialAccount that funds were pulled from.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return ReceivedDebits that have the given status: `succeeded` or `failed`.
  Future<Response> getTreasuryReceivedDebitsWithHttpInfo(String financialAccount, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? status, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/treasury/received_debits';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
      queryParams.addAll(_queryParams('', 'financial_account', financialAccount));
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (status != null) {
      queryParams.addAll(_queryParams('', 'status', status));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all ReceivedDebits
  ///
  /// <p>Returns a list of ReceivedDebits.</p>
  ///
  /// Parameters:
  ///
  /// * [String] financialAccount (required):
  ///   The FinancialAccount that funds were pulled from.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return ReceivedDebits that have the given status: `succeeded` or `failed`.
  Future<TreasuryReceivedDebitsResourceReceivedDebitList?> getTreasuryReceivedDebits(String financialAccount, { String? endingBefore, List<String>? expand, int? limit, String? startingAfter, String? status, }) async {
    final response = await getTreasuryReceivedDebitsWithHttpInfo(financialAccount,  endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, status: status, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryReceivedDebitsResourceReceivedDebitList',) as TreasuryReceivedDebitsResourceReceivedDebitList;
    
    }
    return null;
  }

  /// Retrieve a ReceivedDebit
  ///
  /// <p>Retrieves the details of an existing ReceivedDebit by passing the unique ReceivedDebit ID from the ReceivedDebit list</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getTreasuryReceivedDebitsIdWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/treasury/received_debits/{id}'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a ReceivedDebit
  ///
  /// <p>Retrieves the details of an existing ReceivedDebit by passing the unique ReceivedDebit ID from the ReceivedDebit list</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TreasuryReceivedDebit?> getTreasuryReceivedDebitsId(String id, { List<String>? expand, }) async {
    final response = await getTreasuryReceivedDebitsIdWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryReceivedDebit',) as TreasuryReceivedDebit;
    
    }
    return null;
  }

  /// List all TransactionEntries
  ///
  /// <p>Retrieves a list of TransactionEntry objects.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] financialAccount (required):
  ///   Returns objects associated with this FinancialAccount.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return TransactionEntries that were created during the given date interval.
  ///
  /// * [GetAccountsCreatedParameter] effectiveAt:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] orderBy:
  ///   The results are in reverse chronological order by `created` or `effective_at`. The default is `created`.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] transaction:
  ///   Only return TransactionEntries associated with this Transaction.
  Future<Response> getTreasuryTransactionEntriesWithHttpInfo(String financialAccount, { GetAccountsCreatedParameter? created, GetAccountsCreatedParameter? effectiveAt, String? endingBefore, List<String>? expand, int? limit, String? orderBy, String? startingAfter, String? transaction, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/treasury/transaction_entries';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (effectiveAt != null) {
      queryParams.addAll(_queryParams('', 'effective_at', effectiveAt));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
      queryParams.addAll(_queryParams('', 'financial_account', financialAccount));
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (orderBy != null) {
      queryParams.addAll(_queryParams('', 'order_by', orderBy));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (transaction != null) {
      queryParams.addAll(_queryParams('', 'transaction', transaction));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all TransactionEntries
  ///
  /// <p>Retrieves a list of TransactionEntry objects.</p>
  ///
  /// Parameters:
  ///
  /// * [String] financialAccount (required):
  ///   Returns objects associated with this FinancialAccount.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return TransactionEntries that were created during the given date interval.
  ///
  /// * [GetAccountsCreatedParameter] effectiveAt:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] orderBy:
  ///   The results are in reverse chronological order by `created` or `effective_at`. The default is `created`.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] transaction:
  ///   Only return TransactionEntries associated with this Transaction.
  Future<TreasuryTransactionsResourceTransactionEntryList?> getTreasuryTransactionEntries(String financialAccount, { GetAccountsCreatedParameter? created, GetAccountsCreatedParameter? effectiveAt, String? endingBefore, List<String>? expand, int? limit, String? orderBy, String? startingAfter, String? transaction, }) async {
    final response = await getTreasuryTransactionEntriesWithHttpInfo(financialAccount,  created: created, effectiveAt: effectiveAt, endingBefore: endingBefore, expand: expand, limit: limit, orderBy: orderBy, startingAfter: startingAfter, transaction: transaction, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryTransactionsResourceTransactionEntryList',) as TreasuryTransactionsResourceTransactionEntryList;
    
    }
    return null;
  }

  /// Retrieve a TransactionEntry
  ///
  /// <p>Retrieves a TransactionEntry object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getTreasuryTransactionEntriesIdWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/treasury/transaction_entries/{id}'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a TransactionEntry
  ///
  /// <p>Retrieves a TransactionEntry object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TreasuryTransactionEntry?> getTreasuryTransactionEntriesId(String id, { List<String>? expand, }) async {
    final response = await getTreasuryTransactionEntriesIdWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryTransactionEntry',) as TreasuryTransactionEntry;
    
    }
    return null;
  }

  /// List all Transactions
  ///
  /// <p>Retrieves a list of Transaction objects.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] financialAccount (required):
  ///   Returns objects associated with this FinancialAccount.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return Transactions that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] orderBy:
  ///   The results are in reverse chronological order by `created` or `posted_at`. The default is `created`.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return Transactions that have the given status: `open`, `posted`, or `void`.
  ///
  /// * [StatusTransitionTimestampSpecs] statusTransitions:
  ///   A filter for the `status_transitions.posted_at` timestamp. When using this filter, `status=posted` and `order_by=posted_at` must also be specified.
  Future<Response> getTreasuryTransactionsWithHttpInfo(String financialAccount, { GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? orderBy, String? startingAfter, String? status, StatusTransitionTimestampSpecs? statusTransitions, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/treasury/transactions';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (created != null) {
      queryParams.addAll(_queryParams('', 'created', created));
    }
    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
      queryParams.addAll(_queryParams('', 'financial_account', financialAccount));
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (orderBy != null) {
      queryParams.addAll(_queryParams('', 'order_by', orderBy));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }
    if (status != null) {
      queryParams.addAll(_queryParams('', 'status', status));
    }
    if (statusTransitions != null) {
      queryParams.addAll(_queryParams('', 'status_transitions', statusTransitions));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all Transactions
  ///
  /// <p>Retrieves a list of Transaction objects.</p>
  ///
  /// Parameters:
  ///
  /// * [String] financialAccount (required):
  ///   Returns objects associated with this FinancialAccount.
  ///
  /// * [GetAccountsCreatedParameter] created:
  ///   Only return Transactions that were created during the given date interval.
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] orderBy:
  ///   The results are in reverse chronological order by `created` or `posted_at`. The default is `created`.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  ///
  /// * [String] status:
  ///   Only return Transactions that have the given status: `open`, `posted`, or `void`.
  ///
  /// * [StatusTransitionTimestampSpecs] statusTransitions:
  ///   A filter for the `status_transitions.posted_at` timestamp. When using this filter, `status=posted` and `order_by=posted_at` must also be specified.
  Future<TreasuryTransactionsResourceTransactionList?> getTreasuryTransactions(String financialAccount, { GetAccountsCreatedParameter? created, String? endingBefore, List<String>? expand, int? limit, String? orderBy, String? startingAfter, String? status, StatusTransitionTimestampSpecs? statusTransitions, }) async {
    final response = await getTreasuryTransactionsWithHttpInfo(financialAccount,  created: created, endingBefore: endingBefore, expand: expand, limit: limit, orderBy: orderBy, startingAfter: startingAfter, status: status, statusTransitions: statusTransitions, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryTransactionsResourceTransactionList',) as TreasuryTransactionsResourceTransactionList;
    
    }
    return null;
  }

  /// Retrieve a Transaction
  ///
  /// <p>Retrieves the details of an existing Transaction.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getTreasuryTransactionsIdWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/treasury/transactions/{id}'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a Transaction
  ///
  /// <p>Retrieves the details of an existing Transaction.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TreasuryTransaction?> getTreasuryTransactionsId(String id, { List<String>? expand, }) async {
    final response = await getTreasuryTransactionsIdWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryTransaction',) as TreasuryTransaction;
    
    }
    return null;
  }

  /// List all webhook endpoints
  ///
  /// <p>Returns a list of your webhook endpoints.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<Response> getWebhookEndpointsWithHttpInfo({ String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/webhook_endpoints';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (endingBefore != null) {
      queryParams.addAll(_queryParams('', 'ending_before', endingBefore));
    }
    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }
    if (limit != null) {
      queryParams.addAll(_queryParams('', 'limit', limit));
    }
    if (startingAfter != null) {
      queryParams.addAll(_queryParams('', 'starting_after', startingAfter));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// List all webhook endpoints
  ///
  /// <p>Returns a list of your webhook endpoints.</p>
  ///
  /// Parameters:
  ///
  /// * [String] endingBefore:
  ///   A cursor for use in pagination. `ending_before` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, starting with `obj_bar`, your subsequent call can include `ending_before=obj_bar` in order to fetch the previous page of the list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] limit:
  ///   A limit on the number of objects to be returned. Limit can range between 1 and 100, and the default is 10.
  ///
  /// * [String] startingAfter:
  ///   A cursor for use in pagination. `starting_after` is an object ID that defines your place in the list. For instance, if you make a list request and receive 100 objects, ending with `obj_foo`, your subsequent call can include `starting_after=obj_foo` in order to fetch the next page of the list.
  Future<NotificationWebhookEndpointList?> getWebhookEndpoints({ String? endingBefore, List<String>? expand, int? limit, String? startingAfter, }) async {
    final response = await getWebhookEndpointsWithHttpInfo( endingBefore: endingBefore, expand: expand, limit: limit, startingAfter: startingAfter, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'NotificationWebhookEndpointList',) as NotificationWebhookEndpointList;
    
    }
    return null;
  }

  /// Retrieve a webhook endpoint
  ///
  /// <p>Retrieves the webhook endpoint with the given ID.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] webhookEndpoint (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> getWebhookEndpointsWebhookEndpointWithHttpInfo(String webhookEndpoint, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/webhook_endpoints/{webhook_endpoint}'
      .replaceAll('{webhook_endpoint}', webhookEndpoint);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    if (expand != null) {
      queryParams.addAll(_queryParams('csv', 'expand', expand));
    }

    const contentTypes = <String>['application/x-www-form-urlencoded'];


    return apiClient.invokeAPI(
      path,
      'GET',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Retrieve a webhook endpoint
  ///
  /// <p>Retrieves the webhook endpoint with the given ID.</p>
  ///
  /// Parameters:
  ///
  /// * [String] webhookEndpoint (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<WebhookEndpoint?> getWebhookEndpointsWebhookEndpoint(String webhookEndpoint, { List<String>? expand, }) async {
    final response = await getWebhookEndpointsWebhookEndpointWithHttpInfo(webhookEndpoint,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'WebhookEndpoint',) as WebhookEndpoint;
    
    }
    return null;
  }

  /// Create an account link
  ///
  /// <p>Creates an AccountLink object that includes a single-use Stripe URL that the platform can redirect their user to in order to take them through the Connect Onboarding flow.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///   The identifier of the account to create an account link for.
  ///
  /// * [String] type (required):
  ///   The type of account link the user is requesting.  You can create Account Links of type `account_update` only for connected accounts where your platform is responsible for collecting requirements, including Custom accounts. You can't create them for accounts that have access to a Stripe-hosted Dashboard. If you use [Connect embedded components](/connect/get-started-connect-embedded-components), you can include components that allow your connected accounts to update their own information. For an account without Stripe-hosted Dashboard access where Stripe is liable for negative balances, you must use embedded components.
  ///
  /// * [String] collect:
  ///   The collect parameter is deprecated. Use `collection_options` instead.
  ///
  /// * [CollectionOptionsParams] collectionOptions:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] refreshUrl:
  ///   The URL the user will be redirected to if the account link is expired, has been previously-visited, or is otherwise invalid. The URL you specify should attempt to generate a new account link with the same parameters used to create the original account link, then redirect the user to the new account link's URL so they can continue with Connect Onboarding. If a new account link cannot be generated or the redirect fails you should display a useful error to the user.
  ///
  /// * [String] returnUrl:
  ///   The URL that the user will be redirected to upon leaving or completing the linked flow.
  Future<Response> postAccountLinksWithHttpInfo(String account, String type, { String? collect, CollectionOptionsParams? collectionOptions, List<String>? expand, String? refreshUrl, String? returnUrl, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/account_links';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (account != null) {
      formParams[r'account'] = parameterToString(account);
    }
    if (collect != null) {
      formParams[r'collect'] = parameterToString(collect);
    }
    if (collectionOptions != null) {
      formParams[r'collection_options'] = parameterToString(collectionOptions);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (refreshUrl != null) {
      formParams[r'refresh_url'] = parameterToString(refreshUrl);
    }
    if (returnUrl != null) {
      formParams[r'return_url'] = parameterToString(returnUrl);
    }
    if (type != null) {
      formParams[r'type'] = parameterToString(type);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create an account link
  ///
  /// <p>Creates an AccountLink object that includes a single-use Stripe URL that the platform can redirect their user to in order to take them through the Connect Onboarding flow.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///   The identifier of the account to create an account link for.
  ///
  /// * [String] type (required):
  ///   The type of account link the user is requesting.  You can create Account Links of type `account_update` only for connected accounts where your platform is responsible for collecting requirements, including Custom accounts. You can't create them for accounts that have access to a Stripe-hosted Dashboard. If you use [Connect embedded components](/connect/get-started-connect-embedded-components), you can include components that allow your connected accounts to update their own information. For an account without Stripe-hosted Dashboard access where Stripe is liable for negative balances, you must use embedded components.
  ///
  /// * [String] collect:
  ///   The collect parameter is deprecated. Use `collection_options` instead.
  ///
  /// * [CollectionOptionsParams] collectionOptions:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] refreshUrl:
  ///   The URL the user will be redirected to if the account link is expired, has been previously-visited, or is otherwise invalid. The URL you specify should attempt to generate a new account link with the same parameters used to create the original account link, then redirect the user to the new account link's URL so they can continue with Connect Onboarding. If a new account link cannot be generated or the redirect fails you should display a useful error to the user.
  ///
  /// * [String] returnUrl:
  ///   The URL that the user will be redirected to upon leaving or completing the linked flow.
  Future<AccountLink?> postAccountLinks(String account, String type, { String? collect, CollectionOptionsParams? collectionOptions, List<String>? expand, String? refreshUrl, String? returnUrl, }) async {
    final response = await postAccountLinksWithHttpInfo(account, type,  collect: collect, collectionOptions: collectionOptions, expand: expand, refreshUrl: refreshUrl, returnUrl: returnUrl, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'AccountLink',) as AccountLink;
    
    }
    return null;
  }

  /// Create an Account Session
  ///
  /// <p>Creates a AccountSession object that includes a single-use token that the platform can use on their front-end to grant client-side API access.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///   The identifier of the account to create an Account Session for.
  ///
  /// * [AccountSessionCreateComponentsParam] components (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postAccountSessionsWithHttpInfo(String account, AccountSessionCreateComponentsParam components, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/account_sessions';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (account != null) {
      formParams[r'account'] = parameterToString(account);
    }
    if (components != null) {
      formParams[r'components'] = parameterToString(components);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create an Account Session
  ///
  /// <p>Creates a AccountSession object that includes a single-use token that the platform can use on their front-end to grant client-side API access.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///   The identifier of the account to create an Account Session for.
  ///
  /// * [AccountSessionCreateComponentsParam] components (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<AccountSession?> postAccountSessions(String account, AccountSessionCreateComponentsParam components, { List<String>? expand, }) async {
    final response = await postAccountSessionsWithHttpInfo(account, components,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'AccountSession',) as AccountSession;
    
    }
    return null;
  }

  /// <p>With <a href=\"/docs/connect\">Connect</a>, you can create Stripe accounts for your users. To do this, you’ll first need to <a href=\"https://dashboard.stripe.com/account/applications/settings\">register your platform</a>.</p>  <p>If you’ve already collected information for your connected accounts, you <a href=\"/docs/connect/best-practices#onboarding\">can prefill that information</a> when creating the account. Connect Onboarding won’t ask for the prefilled information during account onboarding. You can prefill any information on the account.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] accountToken:
  ///   An [account token](https://stripe.com/docs/api#create_account_token), used to securely provide details to the account.
  ///
  /// * [PostAccountsRequestBankAccount] bankAccount:
  ///
  /// * [BusinessProfileSpecs] businessProfile:
  ///
  /// * [String] businessType:
  ///   The business type. Once you create an [Account Link](/api/account_links) or [Account Session](/api/account_sessions), this property can only be updated for accounts where [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `application`, which includes Custom accounts.
  ///
  /// * [CapabilitiesParam] capabilities:
  ///
  /// * [CompanySpecs] company:
  ///
  /// * [ControllerSpecs] controller:
  ///
  /// * [String] country:
  ///   The country in which the account holder resides, or in which the business is legally established. This should be an ISO 3166-1 alpha-2 country code. For example, if you are in the United States and the business for which you're creating an account is legally represented in Canada, you would use `CA` as the country for the account being created. Available countries include [Stripe's global markets](https://stripe.com/global) as well as countries where [cross-border payouts](https://stripe.com/docs/connect/cross-border-payouts) are supported.
  ///
  /// * [String] defaultCurrency:
  ///   Three-letter ISO currency code representing the default currency for the account. This must be a currency that [Stripe supports in the account's country](https://docs.stripe.com/payouts).
  ///
  /// * [DocumentsSpecs] documents:
  ///
  /// * [String] email:
  ///   The email address of the account holder. This is only to make the account easier to identify to you. If [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `application`, which includes Custom accounts, Stripe doesn't email the account without your consent.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] externalAccount:
  ///   A card or bank account to attach to the account for receiving [payouts](/connect/bank-debit-card-payouts) (you won’t be able to use it for top-ups). You can provide either a token, like the ones returned by [Stripe.js](/js), or a dictionary, as documented in the `external_account` parameter for [bank account](/api#account_create_bank_account) creation. <br><br>By default, providing an external account sets it as the new default external account for its currency, and deletes the old default if one exists. To add additional external accounts without replacing the existing default for the currency, use the [bank account](/api#account_create_bank_account) or [card creation](/api#account_create_card) APIs. After you create an [Account Link](/api/account_links) or [Account Session](/api/account_sessions), this property can only be updated for accounts where [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `application`, which includes Custom accounts.
  ///
  /// * [AccountGroupsSpecs] groups:
  ///
  /// * [IndividualSpecs] individual:
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [SettingsSpecs] settings:
  ///
  /// * [TosAcceptanceSpecs] tosAcceptance:
  ///
  /// * [String] type:
  ///   The type of Stripe account to create. May be one of `custom`, `express` or `standard`.
  Future<Response> postAccountsWithHttpInfo({ String? accountToken, PostAccountsRequestBankAccount? bankAccount, BusinessProfileSpecs? businessProfile, String? businessType, CapabilitiesParam? capabilities, CompanySpecs? company, ControllerSpecs? controller, String? country, String? defaultCurrency, DocumentsSpecs? documents, String? email, List<String>? expand, String? externalAccount, AccountGroupsSpecs? groups, IndividualSpecs? individual, PostAccountsRequestMetadata? metadata, SettingsSpecs? settings, TosAcceptanceSpecs? tosAcceptance, String? type, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/accounts';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (accountToken != null) {
      formParams[r'account_token'] = parameterToString(accountToken);
    }
    if (bankAccount != null) {
      formParams[r'bank_account'] = parameterToString(bankAccount);
    }
    if (businessProfile != null) {
      formParams[r'business_profile'] = parameterToString(businessProfile);
    }
    if (businessType != null) {
      formParams[r'business_type'] = parameterToString(businessType);
    }
    if (capabilities != null) {
      formParams[r'capabilities'] = parameterToString(capabilities);
    }
    if (company != null) {
      formParams[r'company'] = parameterToString(company);
    }
    if (controller != null) {
      formParams[r'controller'] = parameterToString(controller);
    }
    if (country != null) {
      formParams[r'country'] = parameterToString(country);
    }
    if (defaultCurrency != null) {
      formParams[r'default_currency'] = parameterToString(defaultCurrency);
    }
    if (documents != null) {
      formParams[r'documents'] = parameterToString(documents);
    }
    if (email != null) {
      formParams[r'email'] = parameterToString(email);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (externalAccount != null) {
      formParams[r'external_account'] = parameterToString(externalAccount);
    }
    if (groups != null) {
      formParams[r'groups'] = parameterToString(groups);
    }
    if (individual != null) {
      formParams[r'individual'] = parameterToString(individual);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (settings != null) {
      formParams[r'settings'] = parameterToString(settings);
    }
    if (tosAcceptance != null) {
      formParams[r'tos_acceptance'] = parameterToString(tosAcceptance);
    }
    if (type != null) {
      formParams[r'type'] = parameterToString(type);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// <p>With <a href=\"/docs/connect\">Connect</a>, you can create Stripe accounts for your users. To do this, you’ll first need to <a href=\"https://dashboard.stripe.com/account/applications/settings\">register your platform</a>.</p>  <p>If you’ve already collected information for your connected accounts, you <a href=\"/docs/connect/best-practices#onboarding\">can prefill that information</a> when creating the account. Connect Onboarding won’t ask for the prefilled information during account onboarding. You can prefill any information on the account.</p>
  ///
  /// Parameters:
  ///
  /// * [String] accountToken:
  ///   An [account token](https://stripe.com/docs/api#create_account_token), used to securely provide details to the account.
  ///
  /// * [PostAccountsRequestBankAccount] bankAccount:
  ///
  /// * [BusinessProfileSpecs] businessProfile:
  ///
  /// * [String] businessType:
  ///   The business type. Once you create an [Account Link](/api/account_links) or [Account Session](/api/account_sessions), this property can only be updated for accounts where [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `application`, which includes Custom accounts.
  ///
  /// * [CapabilitiesParam] capabilities:
  ///
  /// * [CompanySpecs] company:
  ///
  /// * [ControllerSpecs] controller:
  ///
  /// * [String] country:
  ///   The country in which the account holder resides, or in which the business is legally established. This should be an ISO 3166-1 alpha-2 country code. For example, if you are in the United States and the business for which you're creating an account is legally represented in Canada, you would use `CA` as the country for the account being created. Available countries include [Stripe's global markets](https://stripe.com/global) as well as countries where [cross-border payouts](https://stripe.com/docs/connect/cross-border-payouts) are supported.
  ///
  /// * [String] defaultCurrency:
  ///   Three-letter ISO currency code representing the default currency for the account. This must be a currency that [Stripe supports in the account's country](https://docs.stripe.com/payouts).
  ///
  /// * [DocumentsSpecs] documents:
  ///
  /// * [String] email:
  ///   The email address of the account holder. This is only to make the account easier to identify to you. If [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `application`, which includes Custom accounts, Stripe doesn't email the account without your consent.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] externalAccount:
  ///   A card or bank account to attach to the account for receiving [payouts](/connect/bank-debit-card-payouts) (you won’t be able to use it for top-ups). You can provide either a token, like the ones returned by [Stripe.js](/js), or a dictionary, as documented in the `external_account` parameter for [bank account](/api#account_create_bank_account) creation. <br><br>By default, providing an external account sets it as the new default external account for its currency, and deletes the old default if one exists. To add additional external accounts without replacing the existing default for the currency, use the [bank account](/api#account_create_bank_account) or [card creation](/api#account_create_card) APIs. After you create an [Account Link](/api/account_links) or [Account Session](/api/account_sessions), this property can only be updated for accounts where [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `application`, which includes Custom accounts.
  ///
  /// * [AccountGroupsSpecs] groups:
  ///
  /// * [IndividualSpecs] individual:
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [SettingsSpecs] settings:
  ///
  /// * [TosAcceptanceSpecs] tosAcceptance:
  ///
  /// * [String] type:
  ///   The type of Stripe account to create. May be one of `custom`, `express` or `standard`.
  Future<Account?> postAccounts({ String? accountToken, PostAccountsRequestBankAccount? bankAccount, BusinessProfileSpecs? businessProfile, String? businessType, CapabilitiesParam? capabilities, CompanySpecs? company, ControllerSpecs? controller, String? country, String? defaultCurrency, DocumentsSpecs? documents, String? email, List<String>? expand, String? externalAccount, AccountGroupsSpecs? groups, IndividualSpecs? individual, PostAccountsRequestMetadata? metadata, SettingsSpecs? settings, TosAcceptanceSpecs? tosAcceptance, String? type, }) async {
    final response = await postAccountsWithHttpInfo( accountToken: accountToken, bankAccount: bankAccount, businessProfile: businessProfile, businessType: businessType, capabilities: capabilities, company: company, controller: controller, country: country, defaultCurrency: defaultCurrency, documents: documents, email: email, expand: expand, externalAccount: externalAccount, groups: groups, individual: individual, metadata: metadata, settings: settings, tosAcceptance: tosAcceptance, type: type, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Account',) as Account;
    
    }
    return null;
  }

  /// Update an account
  ///
  /// <p>Updates a <a href=\"/connect/accounts\">connected account</a> by setting the values of the parameters passed. Any parameters not provided are left unchanged.</p>  <p>For accounts where <a href=\"/api/accounts/object#account_object-controller-requirement_collection\">controller.requirement_collection</a> is <code>application</code>, which includes Custom accounts, you can update any information on the account.</p>  <p>For accounts where <a href=\"/api/accounts/object#account_object-controller-requirement_collection\">controller.requirement_collection</a> is <code>stripe</code>, which includes Standard and Express accounts, you can update all information until you create an <a href=\"/api/account_links\">Account Link</a> or <a href=\"/api/account_sessions\">Account Session</a> to start Connect onboarding, after which some properties can no longer be updated.</p>  <p>To update your own account, use the <a href=\"https://dashboard.stripe.com/settings/account\">Dashboard</a>. Refer to our <a href=\"/docs/connect/updating-accounts\">Connect</a> documentation to learn more about updating accounts.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] accountToken:
  ///   An [account token](https://stripe.com/docs/api#create_account_token), used to securely provide details to the account.
  ///
  /// * [BusinessProfileUpdateSpecs] businessProfile:
  ///
  /// * [String] businessType:
  ///   The business type. Once you create an [Account Link](/api/account_links) or [Account Session](/api/account_sessions), this property can only be updated for accounts where [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `application`, which includes Custom accounts.
  ///
  /// * [CapabilitiesParam] capabilities:
  ///
  /// * [CompanyUpdateSpecs] company:
  ///
  /// * [String] defaultCurrency:
  ///   Three-letter ISO currency code representing the default currency for the account. This must be a currency that [Stripe supports in the account's country](https://docs.stripe.com/payouts).
  ///
  /// * [DocumentsSpecs] documents:
  ///
  /// * [String] email:
  ///   The email address of the account holder. This is only to make the account easier to identify to you. If [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `application`, which includes Custom accounts, Stripe doesn't email the account without your consent.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] externalAccount:
  ///   A card or bank account to attach to the account for receiving [payouts](/connect/bank-debit-card-payouts) (you won’t be able to use it for top-ups). You can provide either a token, like the ones returned by [Stripe.js](/js), or a dictionary, as documented in the `external_account` parameter for [bank account](/api#account_create_bank_account) creation. <br><br>By default, providing an external account sets it as the new default external account for its currency, and deletes the old default if one exists. To add additional external accounts without replacing the existing default for the currency, use the [bank account](/api#account_create_bank_account) or [card creation](/api#account_create_card) APIs. After you create an [Account Link](/api/account_links) or [Account Session](/api/account_sessions), this property can only be updated for accounts where [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `application`, which includes Custom accounts.
  ///
  /// * [AccountGroupsSpecs] groups:
  ///
  /// * [IndividualUpdateSpecs] individual:
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [SettingsSpecsUpdate] settings:
  ///
  /// * [TosAcceptanceSpecs] tosAcceptance:
  Future<Response> postAccountsAccountWithHttpInfo(String account, { String? accountToken, BusinessProfileUpdateSpecs? businessProfile, String? businessType, CapabilitiesParam? capabilities, CompanyUpdateSpecs? company, String? defaultCurrency, DocumentsSpecs? documents, String? email, List<String>? expand, String? externalAccount, AccountGroupsSpecs? groups, IndividualUpdateSpecs? individual, PostAccountsRequestMetadata? metadata, SettingsSpecsUpdate? settings, TosAcceptanceSpecs? tosAcceptance, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/accounts/{account}'
      .replaceAll('{account}', account);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (accountToken != null) {
      formParams[r'account_token'] = parameterToString(accountToken);
    }
    if (businessProfile != null) {
      formParams[r'business_profile'] = parameterToString(businessProfile);
    }
    if (businessType != null) {
      formParams[r'business_type'] = parameterToString(businessType);
    }
    if (capabilities != null) {
      formParams[r'capabilities'] = parameterToString(capabilities);
    }
    if (company != null) {
      formParams[r'company'] = parameterToString(company);
    }
    if (defaultCurrency != null) {
      formParams[r'default_currency'] = parameterToString(defaultCurrency);
    }
    if (documents != null) {
      formParams[r'documents'] = parameterToString(documents);
    }
    if (email != null) {
      formParams[r'email'] = parameterToString(email);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (externalAccount != null) {
      formParams[r'external_account'] = parameterToString(externalAccount);
    }
    if (groups != null) {
      formParams[r'groups'] = parameterToString(groups);
    }
    if (individual != null) {
      formParams[r'individual'] = parameterToString(individual);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (settings != null) {
      formParams[r'settings'] = parameterToString(settings);
    }
    if (tosAcceptance != null) {
      formParams[r'tos_acceptance'] = parameterToString(tosAcceptance);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update an account
  ///
  /// <p>Updates a <a href=\"/connect/accounts\">connected account</a> by setting the values of the parameters passed. Any parameters not provided are left unchanged.</p>  <p>For accounts where <a href=\"/api/accounts/object#account_object-controller-requirement_collection\">controller.requirement_collection</a> is <code>application</code>, which includes Custom accounts, you can update any information on the account.</p>  <p>For accounts where <a href=\"/api/accounts/object#account_object-controller-requirement_collection\">controller.requirement_collection</a> is <code>stripe</code>, which includes Standard and Express accounts, you can update all information until you create an <a href=\"/api/account_links\">Account Link</a> or <a href=\"/api/account_sessions\">Account Session</a> to start Connect onboarding, after which some properties can no longer be updated.</p>  <p>To update your own account, use the <a href=\"https://dashboard.stripe.com/settings/account\">Dashboard</a>. Refer to our <a href=\"/docs/connect/updating-accounts\">Connect</a> documentation to learn more about updating accounts.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] accountToken:
  ///   An [account token](https://stripe.com/docs/api#create_account_token), used to securely provide details to the account.
  ///
  /// * [BusinessProfileUpdateSpecs] businessProfile:
  ///
  /// * [String] businessType:
  ///   The business type. Once you create an [Account Link](/api/account_links) or [Account Session](/api/account_sessions), this property can only be updated for accounts where [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `application`, which includes Custom accounts.
  ///
  /// * [CapabilitiesParam] capabilities:
  ///
  /// * [CompanyUpdateSpecs] company:
  ///
  /// * [String] defaultCurrency:
  ///   Three-letter ISO currency code representing the default currency for the account. This must be a currency that [Stripe supports in the account's country](https://docs.stripe.com/payouts).
  ///
  /// * [DocumentsSpecs] documents:
  ///
  /// * [String] email:
  ///   The email address of the account holder. This is only to make the account easier to identify to you. If [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `application`, which includes Custom accounts, Stripe doesn't email the account without your consent.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] externalAccount:
  ///   A card or bank account to attach to the account for receiving [payouts](/connect/bank-debit-card-payouts) (you won’t be able to use it for top-ups). You can provide either a token, like the ones returned by [Stripe.js](/js), or a dictionary, as documented in the `external_account` parameter for [bank account](/api#account_create_bank_account) creation. <br><br>By default, providing an external account sets it as the new default external account for its currency, and deletes the old default if one exists. To add additional external accounts without replacing the existing default for the currency, use the [bank account](/api#account_create_bank_account) or [card creation](/api#account_create_card) APIs. After you create an [Account Link](/api/account_links) or [Account Session](/api/account_sessions), this property can only be updated for accounts where [controller.requirement_collection](/api/accounts/object#account_object-controller-requirement_collection) is `application`, which includes Custom accounts.
  ///
  /// * [AccountGroupsSpecs] groups:
  ///
  /// * [IndividualUpdateSpecs] individual:
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [SettingsSpecsUpdate] settings:
  ///
  /// * [TosAcceptanceSpecs] tosAcceptance:
  Future<Account?> postAccountsAccount(String account, { String? accountToken, BusinessProfileUpdateSpecs? businessProfile, String? businessType, CapabilitiesParam? capabilities, CompanyUpdateSpecs? company, String? defaultCurrency, DocumentsSpecs? documents, String? email, List<String>? expand, String? externalAccount, AccountGroupsSpecs? groups, IndividualUpdateSpecs? individual, PostAccountsRequestMetadata? metadata, SettingsSpecsUpdate? settings, TosAcceptanceSpecs? tosAcceptance, }) async {
    final response = await postAccountsAccountWithHttpInfo(account,  accountToken: accountToken, businessProfile: businessProfile, businessType: businessType, capabilities: capabilities, company: company, defaultCurrency: defaultCurrency, documents: documents, email: email, expand: expand, externalAccount: externalAccount, groups: groups, individual: individual, metadata: metadata, settings: settings, tosAcceptance: tosAcceptance, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Account',) as Account;
    
    }
    return null;
  }

  /// Create an external account
  ///
  /// <p>Create an external account for a given account.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [PostAccountsRequestBankAccount] bankAccount:
  ///
  /// * [bool] defaultForCurrency:
  ///   When set to true, or if this is the first external account added in this currency, this account becomes the default external account for its currency.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] externalAccount:
  ///   A token, like the ones returned by [Stripe.js](https://stripe.com/docs/js) or a dictionary containing a user's external account details (with the options shown below). Please refer to full [documentation](https://stripe.com/docs/api/external_accounts) instead.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  Future<Response> postAccountsAccountBankAccountsWithHttpInfo(String account, { PostAccountsRequestBankAccount? bankAccount, bool? defaultForCurrency, List<String>? expand, String? externalAccount, Map<String, String>? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/accounts/{account}/bank_accounts'
      .replaceAll('{account}', account);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (bankAccount != null) {
      formParams[r'bank_account'] = parameterToString(bankAccount);
    }
    if (defaultForCurrency != null) {
      formParams[r'default_for_currency'] = parameterToString(defaultForCurrency);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (externalAccount != null) {
      formParams[r'external_account'] = parameterToString(externalAccount);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create an external account
  ///
  /// <p>Create an external account for a given account.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [PostAccountsRequestBankAccount] bankAccount:
  ///
  /// * [bool] defaultForCurrency:
  ///   When set to true, or if this is the first external account added in this currency, this account becomes the default external account for its currency.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] externalAccount:
  ///   A token, like the ones returned by [Stripe.js](https://stripe.com/docs/js) or a dictionary containing a user's external account details (with the options shown below). Please refer to full [documentation](https://stripe.com/docs/api/external_accounts) instead.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  Future<ExternalAccount?> postAccountsAccountBankAccounts(String account, { PostAccountsRequestBankAccount? bankAccount, bool? defaultForCurrency, List<String>? expand, String? externalAccount, Map<String, String>? metadata, }) async {
    final response = await postAccountsAccountBankAccountsWithHttpInfo(account,  bankAccount: bankAccount, defaultForCurrency: defaultForCurrency, expand: expand, externalAccount: externalAccount, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ExternalAccount',) as ExternalAccount;
    
    }
    return null;
  }

  /// <p>Updates the metadata, account holder name, account holder type of a bank account belonging to a connected account and optionally sets it as the default for its currency. Other bank account details are not editable by design.</p>  <p>You can only update bank accounts when <a href=\"/api/accounts/object#account_object-controller-requirement_collection\">account.controller.requirement_collection</a> is <code>application</code>, which includes <a href=\"/connect/custom-accounts\">Custom accounts</a>.</p>  <p>You can re-enable a disabled bank account by performing an update call without providing any arguments or changes.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] id (required):
  ///
  /// * [String] accountHolderName:
  ///   The name of the person or business that owns the bank account.
  ///
  /// * [String] accountHolderType:
  ///   The type of entity that holds the account. This can be either `individual` or `company`.
  ///
  /// * [String] accountType:
  ///   The bank account type. This can only be `checking` or `savings` in most countries. In Japan, this can only be `futsu` or `toza`.
  ///
  /// * [String] addressCity:
  ///   City/District/Suburb/Town/Village.
  ///
  /// * [String] addressCountry:
  ///   Billing address country, if provided when creating card.
  ///
  /// * [String] addressLine1:
  ///   Address line 1 (Street address/PO Box/Company name).
  ///
  /// * [String] addressLine2:
  ///   Address line 2 (Apartment/Suite/Unit/Building).
  ///
  /// * [String] addressState:
  ///   State/County/Province/Region.
  ///
  /// * [String] addressZip:
  ///   ZIP or postal code.
  ///
  /// * [bool] defaultForCurrency:
  ///   When set to true, this becomes the default external account for its currency.
  ///
  /// * [ExternalAccountDocumentsParam1] documents:
  ///
  /// * [String] expMonth:
  ///   Two digit number representing the card’s expiration month.
  ///
  /// * [String] expYear:
  ///   Four digit number representing the card’s expiration year.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] name:
  ///   Cardholder name.
  Future<Response> postAccountsAccountBankAccountsIdWithHttpInfo(String account, String id, { String? accountHolderName, String? accountHolderType, String? accountType, String? addressCity, String? addressCountry, String? addressLine1, String? addressLine2, String? addressState, String? addressZip, bool? defaultForCurrency, ExternalAccountDocumentsParam1? documents, String? expMonth, String? expYear, List<String>? expand, PostAccountsRequestMetadata? metadata, String? name, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/accounts/{account}/bank_accounts/{id}'
      .replaceAll('{account}', account)
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (accountHolderName != null) {
      formParams[r'account_holder_name'] = parameterToString(accountHolderName);
    }
    if (accountHolderType != null) {
      formParams[r'account_holder_type'] = parameterToString(accountHolderType);
    }
    if (accountType != null) {
      formParams[r'account_type'] = parameterToString(accountType);
    }
    if (addressCity != null) {
      formParams[r'address_city'] = parameterToString(addressCity);
    }
    if (addressCountry != null) {
      formParams[r'address_country'] = parameterToString(addressCountry);
    }
    if (addressLine1 != null) {
      formParams[r'address_line1'] = parameterToString(addressLine1);
    }
    if (addressLine2 != null) {
      formParams[r'address_line2'] = parameterToString(addressLine2);
    }
    if (addressState != null) {
      formParams[r'address_state'] = parameterToString(addressState);
    }
    if (addressZip != null) {
      formParams[r'address_zip'] = parameterToString(addressZip);
    }
    if (defaultForCurrency != null) {
      formParams[r'default_for_currency'] = parameterToString(defaultForCurrency);
    }
    if (documents != null) {
      formParams[r'documents'] = parameterToString(documents);
    }
    if (expMonth != null) {
      formParams[r'exp_month'] = parameterToString(expMonth);
    }
    if (expYear != null) {
      formParams[r'exp_year'] = parameterToString(expYear);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (name != null) {
      formParams[r'name'] = parameterToString(name);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// <p>Updates the metadata, account holder name, account holder type of a bank account belonging to a connected account and optionally sets it as the default for its currency. Other bank account details are not editable by design.</p>  <p>You can only update bank accounts when <a href=\"/api/accounts/object#account_object-controller-requirement_collection\">account.controller.requirement_collection</a> is <code>application</code>, which includes <a href=\"/connect/custom-accounts\">Custom accounts</a>.</p>  <p>You can re-enable a disabled bank account by performing an update call without providing any arguments or changes.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] id (required):
  ///
  /// * [String] accountHolderName:
  ///   The name of the person or business that owns the bank account.
  ///
  /// * [String] accountHolderType:
  ///   The type of entity that holds the account. This can be either `individual` or `company`.
  ///
  /// * [String] accountType:
  ///   The bank account type. This can only be `checking` or `savings` in most countries. In Japan, this can only be `futsu` or `toza`.
  ///
  /// * [String] addressCity:
  ///   City/District/Suburb/Town/Village.
  ///
  /// * [String] addressCountry:
  ///   Billing address country, if provided when creating card.
  ///
  /// * [String] addressLine1:
  ///   Address line 1 (Street address/PO Box/Company name).
  ///
  /// * [String] addressLine2:
  ///   Address line 2 (Apartment/Suite/Unit/Building).
  ///
  /// * [String] addressState:
  ///   State/County/Province/Region.
  ///
  /// * [String] addressZip:
  ///   ZIP or postal code.
  ///
  /// * [bool] defaultForCurrency:
  ///   When set to true, this becomes the default external account for its currency.
  ///
  /// * [ExternalAccountDocumentsParam1] documents:
  ///
  /// * [String] expMonth:
  ///   Two digit number representing the card’s expiration month.
  ///
  /// * [String] expYear:
  ///   Four digit number representing the card’s expiration year.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] name:
  ///   Cardholder name.
  Future<ExternalAccount?> postAccountsAccountBankAccountsId(String account, String id, { String? accountHolderName, String? accountHolderType, String? accountType, String? addressCity, String? addressCountry, String? addressLine1, String? addressLine2, String? addressState, String? addressZip, bool? defaultForCurrency, ExternalAccountDocumentsParam1? documents, String? expMonth, String? expYear, List<String>? expand, PostAccountsRequestMetadata? metadata, String? name, }) async {
    final response = await postAccountsAccountBankAccountsIdWithHttpInfo(account, id,  accountHolderName: accountHolderName, accountHolderType: accountHolderType, accountType: accountType, addressCity: addressCity, addressCountry: addressCountry, addressLine1: addressLine1, addressLine2: addressLine2, addressState: addressState, addressZip: addressZip, defaultForCurrency: defaultForCurrency, documents: documents, expMonth: expMonth, expYear: expYear, expand: expand, metadata: metadata, name: name, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ExternalAccount',) as ExternalAccount;
    
    }
    return null;
  }

  /// Update an Account Capability
  ///
  /// <p>Updates an existing Account Capability. Request or remove a capability by updating its <code>requested</code> parameter.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] capability (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [bool] requested:
  ///   To request a new capability for an account, pass true. There can be a delay before the requested capability becomes active. If the capability has any activation requirements, the response includes them in the `requirements` arrays.  If a capability isn't permanent, you can remove it from the account by passing false. Some capabilities are permanent after they've been requested. Attempting to remove a permanent capability returns an error.
  Future<Response> postAccountsAccountCapabilitiesCapabilityWithHttpInfo(String account, String capability, { List<String>? expand, bool? requested, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/accounts/{account}/capabilities/{capability}'
      .replaceAll('{account}', account)
      .replaceAll('{capability}', capability);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (requested != null) {
      formParams[r'requested'] = parameterToString(requested);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update an Account Capability
  ///
  /// <p>Updates an existing Account Capability. Request or remove a capability by updating its <code>requested</code> parameter.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] capability (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [bool] requested:
  ///   To request a new capability for an account, pass true. There can be a delay before the requested capability becomes active. If the capability has any activation requirements, the response includes them in the `requirements` arrays.  If a capability isn't permanent, you can remove it from the account by passing false. Some capabilities are permanent after they've been requested. Attempting to remove a permanent capability returns an error.
  Future<Capability?> postAccountsAccountCapabilitiesCapability(String account, String capability, { List<String>? expand, bool? requested, }) async {
    final response = await postAccountsAccountCapabilitiesCapabilityWithHttpInfo(account, capability,  expand: expand, requested: requested, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Capability',) as Capability;
    
    }
    return null;
  }

  /// Create an external account
  ///
  /// <p>Create an external account for a given account.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [PostAccountsRequestBankAccount] bankAccount:
  ///
  /// * [bool] defaultForCurrency:
  ///   When set to true, or if this is the first external account added in this currency, this account becomes the default external account for its currency.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] externalAccount:
  ///   A token, like the ones returned by [Stripe.js](https://stripe.com/docs/js) or a dictionary containing a user's external account details (with the options shown below). Please refer to full [documentation](https://stripe.com/docs/api/external_accounts) instead.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  Future<Response> postAccountsAccountExternalAccountsWithHttpInfo(String account, { PostAccountsRequestBankAccount? bankAccount, bool? defaultForCurrency, List<String>? expand, String? externalAccount, Map<String, String>? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/accounts/{account}/external_accounts'
      .replaceAll('{account}', account);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (bankAccount != null) {
      formParams[r'bank_account'] = parameterToString(bankAccount);
    }
    if (defaultForCurrency != null) {
      formParams[r'default_for_currency'] = parameterToString(defaultForCurrency);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (externalAccount != null) {
      formParams[r'external_account'] = parameterToString(externalAccount);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create an external account
  ///
  /// <p>Create an external account for a given account.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [PostAccountsRequestBankAccount] bankAccount:
  ///
  /// * [bool] defaultForCurrency:
  ///   When set to true, or if this is the first external account added in this currency, this account becomes the default external account for its currency.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] externalAccount:
  ///   A token, like the ones returned by [Stripe.js](https://stripe.com/docs/js) or a dictionary containing a user's external account details (with the options shown below). Please refer to full [documentation](https://stripe.com/docs/api/external_accounts) instead.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  Future<ExternalAccount?> postAccountsAccountExternalAccounts(String account, { PostAccountsRequestBankAccount? bankAccount, bool? defaultForCurrency, List<String>? expand, String? externalAccount, Map<String, String>? metadata, }) async {
    final response = await postAccountsAccountExternalAccountsWithHttpInfo(account,  bankAccount: bankAccount, defaultForCurrency: defaultForCurrency, expand: expand, externalAccount: externalAccount, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ExternalAccount',) as ExternalAccount;
    
    }
    return null;
  }

  /// <p>Updates the metadata, account holder name, account holder type of a bank account belonging to a connected account and optionally sets it as the default for its currency. Other bank account details are not editable by design.</p>  <p>You can only update bank accounts when <a href=\"/api/accounts/object#account_object-controller-requirement_collection\">account.controller.requirement_collection</a> is <code>application</code>, which includes <a href=\"/connect/custom-accounts\">Custom accounts</a>.</p>  <p>You can re-enable a disabled bank account by performing an update call without providing any arguments or changes.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] id (required):
  ///
  /// * [String] accountHolderName:
  ///   The name of the person or business that owns the bank account.
  ///
  /// * [String] accountHolderType:
  ///   The type of entity that holds the account. This can be either `individual` or `company`.
  ///
  /// * [String] accountType:
  ///   The bank account type. This can only be `checking` or `savings` in most countries. In Japan, this can only be `futsu` or `toza`.
  ///
  /// * [String] addressCity:
  ///   City/District/Suburb/Town/Village.
  ///
  /// * [String] addressCountry:
  ///   Billing address country, if provided when creating card.
  ///
  /// * [String] addressLine1:
  ///   Address line 1 (Street address/PO Box/Company name).
  ///
  /// * [String] addressLine2:
  ///   Address line 2 (Apartment/Suite/Unit/Building).
  ///
  /// * [String] addressState:
  ///   State/County/Province/Region.
  ///
  /// * [String] addressZip:
  ///   ZIP or postal code.
  ///
  /// * [bool] defaultForCurrency:
  ///   When set to true, this becomes the default external account for its currency.
  ///
  /// * [ExternalAccountDocumentsParam1] documents:
  ///
  /// * [String] expMonth:
  ///   Two digit number representing the card’s expiration month.
  ///
  /// * [String] expYear:
  ///   Four digit number representing the card’s expiration year.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] name:
  ///   Cardholder name.
  Future<Response> postAccountsAccountExternalAccountsIdWithHttpInfo(String account, String id, { String? accountHolderName, String? accountHolderType, String? accountType, String? addressCity, String? addressCountry, String? addressLine1, String? addressLine2, String? addressState, String? addressZip, bool? defaultForCurrency, ExternalAccountDocumentsParam1? documents, String? expMonth, String? expYear, List<String>? expand, PostAccountsRequestMetadata? metadata, String? name, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/accounts/{account}/external_accounts/{id}'
      .replaceAll('{account}', account)
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (accountHolderName != null) {
      formParams[r'account_holder_name'] = parameterToString(accountHolderName);
    }
    if (accountHolderType != null) {
      formParams[r'account_holder_type'] = parameterToString(accountHolderType);
    }
    if (accountType != null) {
      formParams[r'account_type'] = parameterToString(accountType);
    }
    if (addressCity != null) {
      formParams[r'address_city'] = parameterToString(addressCity);
    }
    if (addressCountry != null) {
      formParams[r'address_country'] = parameterToString(addressCountry);
    }
    if (addressLine1 != null) {
      formParams[r'address_line1'] = parameterToString(addressLine1);
    }
    if (addressLine2 != null) {
      formParams[r'address_line2'] = parameterToString(addressLine2);
    }
    if (addressState != null) {
      formParams[r'address_state'] = parameterToString(addressState);
    }
    if (addressZip != null) {
      formParams[r'address_zip'] = parameterToString(addressZip);
    }
    if (defaultForCurrency != null) {
      formParams[r'default_for_currency'] = parameterToString(defaultForCurrency);
    }
    if (documents != null) {
      formParams[r'documents'] = parameterToString(documents);
    }
    if (expMonth != null) {
      formParams[r'exp_month'] = parameterToString(expMonth);
    }
    if (expYear != null) {
      formParams[r'exp_year'] = parameterToString(expYear);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (name != null) {
      formParams[r'name'] = parameterToString(name);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// <p>Updates the metadata, account holder name, account holder type of a bank account belonging to a connected account and optionally sets it as the default for its currency. Other bank account details are not editable by design.</p>  <p>You can only update bank accounts when <a href=\"/api/accounts/object#account_object-controller-requirement_collection\">account.controller.requirement_collection</a> is <code>application</code>, which includes <a href=\"/connect/custom-accounts\">Custom accounts</a>.</p>  <p>You can re-enable a disabled bank account by performing an update call without providing any arguments or changes.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] id (required):
  ///
  /// * [String] accountHolderName:
  ///   The name of the person or business that owns the bank account.
  ///
  /// * [String] accountHolderType:
  ///   The type of entity that holds the account. This can be either `individual` or `company`.
  ///
  /// * [String] accountType:
  ///   The bank account type. This can only be `checking` or `savings` in most countries. In Japan, this can only be `futsu` or `toza`.
  ///
  /// * [String] addressCity:
  ///   City/District/Suburb/Town/Village.
  ///
  /// * [String] addressCountry:
  ///   Billing address country, if provided when creating card.
  ///
  /// * [String] addressLine1:
  ///   Address line 1 (Street address/PO Box/Company name).
  ///
  /// * [String] addressLine2:
  ///   Address line 2 (Apartment/Suite/Unit/Building).
  ///
  /// * [String] addressState:
  ///   State/County/Province/Region.
  ///
  /// * [String] addressZip:
  ///   ZIP or postal code.
  ///
  /// * [bool] defaultForCurrency:
  ///   When set to true, this becomes the default external account for its currency.
  ///
  /// * [ExternalAccountDocumentsParam1] documents:
  ///
  /// * [String] expMonth:
  ///   Two digit number representing the card’s expiration month.
  ///
  /// * [String] expYear:
  ///   Four digit number representing the card’s expiration year.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] name:
  ///   Cardholder name.
  Future<ExternalAccount?> postAccountsAccountExternalAccountsId(String account, String id, { String? accountHolderName, String? accountHolderType, String? accountType, String? addressCity, String? addressCountry, String? addressLine1, String? addressLine2, String? addressState, String? addressZip, bool? defaultForCurrency, ExternalAccountDocumentsParam1? documents, String? expMonth, String? expYear, List<String>? expand, PostAccountsRequestMetadata? metadata, String? name, }) async {
    final response = await postAccountsAccountExternalAccountsIdWithHttpInfo(account, id,  accountHolderName: accountHolderName, accountHolderType: accountHolderType, accountType: accountType, addressCity: addressCity, addressCountry: addressCountry, addressLine1: addressLine1, addressLine2: addressLine2, addressState: addressState, addressZip: addressZip, defaultForCurrency: defaultForCurrency, documents: documents, expMonth: expMonth, expYear: expYear, expand: expand, metadata: metadata, name: name, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ExternalAccount',) as ExternalAccount;
    
    }
    return null;
  }

  /// Create a login link
  ///
  /// <p>Creates a login link for a connected account to access the Express Dashboard.</p>  <p><strong>You can only create login links for accounts that use the <a href=\"/connect/express-dashboard\">Express Dashboard</a> and are connected to your platform</strong>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postAccountsAccountLoginLinksWithHttpInfo(String account, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/accounts/{account}/login_links'
      .replaceAll('{account}', account);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a login link
  ///
  /// <p>Creates a login link for a connected account to access the Express Dashboard.</p>  <p><strong>You can only create login links for accounts that use the <a href=\"/connect/express-dashboard\">Express Dashboard</a> and are connected to your platform</strong>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<LoginLink?> postAccountsAccountLoginLinks(String account, { List<String>? expand, }) async {
    final response = await postAccountsAccountLoginLinksWithHttpInfo(account,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'LoginLink',) as LoginLink;
    
    }
    return null;
  }

  /// Create a person
  ///
  /// <p>Creates a new person.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [PersonAdditionalTosAcceptancesSpecs] additionalTosAcceptances:
  ///
  /// * [LegalEntityAndKycAddressSpecs1] address:
  ///
  /// * [JapanAddressKanaSpecs1] addressKana:
  ///
  /// * [JapanAddressKanjiSpecs1] addressKanji:
  ///
  /// * [PostAccountsAccountPeopleRequestDob] dob:
  ///
  /// * [PersonDocumentsSpecs] documents:
  ///
  /// * [String] email:
  ///   The person's email address.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] firstName:
  ///   The person's first name.
  ///
  /// * [String] firstNameKana:
  ///   The Kana variation of the person's first name (Japan only).
  ///
  /// * [String] firstNameKanji:
  ///   The Kanji variation of the person's first name (Japan only).
  ///
  /// * [PostAccountsAccountPeopleRequestFullNameAliases] fullNameAliases:
  ///
  /// * [String] gender:
  ///   The person's gender (International regulations require either \\\"male\\\" or \\\"female\\\").
  ///
  /// * [String] idNumber:
  ///   The person's ID number, as appropriate for their country. For example, a social security number in the U.S., social insurance number in Canada, etc. Instead of the number itself, you can also provide a [PII token provided by Stripe.js](https://docs.stripe.com/js/tokens/create_token?type=pii).
  ///
  /// * [String] idNumberSecondary:
  ///   The person's secondary ID number, as appropriate for their country, will be used for enhanced verification checks. In Thailand, this would be the laser code found on the back of an ID card. Instead of the number itself, you can also provide a [PII token provided by Stripe.js](https://docs.stripe.com/js/tokens/create_token?type=pii).
  ///
  /// * [String] lastName:
  ///   The person's last name.
  ///
  /// * [String] lastNameKana:
  ///   The Kana variation of the person's last name (Japan only).
  ///
  /// * [String] lastNameKanji:
  ///   The Kanji variation of the person's last name (Japan only).
  ///
  /// * [String] maidenName:
  ///   The person's maiden name.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] nationality:
  ///   The country where the person is a national. Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)), or \\\"XX\\\" if unavailable.
  ///
  /// * [String] personToken:
  ///   A [person token](https://docs.stripe.com/connect/account-tokens), used to securely provide details to the person.
  ///
  /// * [String] phone:
  ///   The person's phone number.
  ///
  /// * [String] politicalExposure:
  ///   Indicates if the person or any of their representatives, family members, or other closely related persons, declares that they hold or have held an important public job or function, in any jurisdiction.
  ///
  /// * [AddressSpecs1] registeredAddress:
  ///
  /// * [RelationshipSpecs] relationship:
  ///
  /// * [String] ssnLast4:
  ///   The last four digits of the person's Social Security number (U.S. only).
  ///
  /// * [UsCfpbDataSpecs] usCfpbData:
  ///
  /// * [PersonVerificationSpecs1] verification:
  Future<Response> postAccountsAccountPeopleWithHttpInfo(String account, { PersonAdditionalTosAcceptancesSpecs? additionalTosAcceptances, LegalEntityAndKycAddressSpecs1? address, JapanAddressKanaSpecs1? addressKana, JapanAddressKanjiSpecs1? addressKanji, PostAccountsAccountPeopleRequestDob? dob, PersonDocumentsSpecs? documents, String? email, List<String>? expand, String? firstName, String? firstNameKana, String? firstNameKanji, PostAccountsAccountPeopleRequestFullNameAliases? fullNameAliases, String? gender, String? idNumber, String? idNumberSecondary, String? lastName, String? lastNameKana, String? lastNameKanji, String? maidenName, PostAccountsRequestMetadata? metadata, String? nationality, String? personToken, String? phone, String? politicalExposure, AddressSpecs1? registeredAddress, RelationshipSpecs? relationship, String? ssnLast4, UsCfpbDataSpecs? usCfpbData, PersonVerificationSpecs1? verification, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/accounts/{account}/people'
      .replaceAll('{account}', account);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (additionalTosAcceptances != null) {
      formParams[r'additional_tos_acceptances'] = parameterToString(additionalTosAcceptances);
    }
    if (address != null) {
      formParams[r'address'] = parameterToString(address);
    }
    if (addressKana != null) {
      formParams[r'address_kana'] = parameterToString(addressKana);
    }
    if (addressKanji != null) {
      formParams[r'address_kanji'] = parameterToString(addressKanji);
    }
    if (dob != null) {
      formParams[r'dob'] = parameterToString(dob);
    }
    if (documents != null) {
      formParams[r'documents'] = parameterToString(documents);
    }
    if (email != null) {
      formParams[r'email'] = parameterToString(email);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (firstName != null) {
      formParams[r'first_name'] = parameterToString(firstName);
    }
    if (firstNameKana != null) {
      formParams[r'first_name_kana'] = parameterToString(firstNameKana);
    }
    if (firstNameKanji != null) {
      formParams[r'first_name_kanji'] = parameterToString(firstNameKanji);
    }
    if (fullNameAliases != null) {
      formParams[r'full_name_aliases'] = parameterToString(fullNameAliases);
    }
    if (gender != null) {
      formParams[r'gender'] = parameterToString(gender);
    }
    if (idNumber != null) {
      formParams[r'id_number'] = parameterToString(idNumber);
    }
    if (idNumberSecondary != null) {
      formParams[r'id_number_secondary'] = parameterToString(idNumberSecondary);
    }
    if (lastName != null) {
      formParams[r'last_name'] = parameterToString(lastName);
    }
    if (lastNameKana != null) {
      formParams[r'last_name_kana'] = parameterToString(lastNameKana);
    }
    if (lastNameKanji != null) {
      formParams[r'last_name_kanji'] = parameterToString(lastNameKanji);
    }
    if (maidenName != null) {
      formParams[r'maiden_name'] = parameterToString(maidenName);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (nationality != null) {
      formParams[r'nationality'] = parameterToString(nationality);
    }
    if (personToken != null) {
      formParams[r'person_token'] = parameterToString(personToken);
    }
    if (phone != null) {
      formParams[r'phone'] = parameterToString(phone);
    }
    if (politicalExposure != null) {
      formParams[r'political_exposure'] = parameterToString(politicalExposure);
    }
    if (registeredAddress != null) {
      formParams[r'registered_address'] = parameterToString(registeredAddress);
    }
    if (relationship != null) {
      formParams[r'relationship'] = parameterToString(relationship);
    }
    if (ssnLast4 != null) {
      formParams[r'ssn_last_4'] = parameterToString(ssnLast4);
    }
    if (usCfpbData != null) {
      formParams[r'us_cfpb_data'] = parameterToString(usCfpbData);
    }
    if (verification != null) {
      formParams[r'verification'] = parameterToString(verification);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a person
  ///
  /// <p>Creates a new person.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [PersonAdditionalTosAcceptancesSpecs] additionalTosAcceptances:
  ///
  /// * [LegalEntityAndKycAddressSpecs1] address:
  ///
  /// * [JapanAddressKanaSpecs1] addressKana:
  ///
  /// * [JapanAddressKanjiSpecs1] addressKanji:
  ///
  /// * [PostAccountsAccountPeopleRequestDob] dob:
  ///
  /// * [PersonDocumentsSpecs] documents:
  ///
  /// * [String] email:
  ///   The person's email address.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] firstName:
  ///   The person's first name.
  ///
  /// * [String] firstNameKana:
  ///   The Kana variation of the person's first name (Japan only).
  ///
  /// * [String] firstNameKanji:
  ///   The Kanji variation of the person's first name (Japan only).
  ///
  /// * [PostAccountsAccountPeopleRequestFullNameAliases] fullNameAliases:
  ///
  /// * [String] gender:
  ///   The person's gender (International regulations require either \\\"male\\\" or \\\"female\\\").
  ///
  /// * [String] idNumber:
  ///   The person's ID number, as appropriate for their country. For example, a social security number in the U.S., social insurance number in Canada, etc. Instead of the number itself, you can also provide a [PII token provided by Stripe.js](https://docs.stripe.com/js/tokens/create_token?type=pii).
  ///
  /// * [String] idNumberSecondary:
  ///   The person's secondary ID number, as appropriate for their country, will be used for enhanced verification checks. In Thailand, this would be the laser code found on the back of an ID card. Instead of the number itself, you can also provide a [PII token provided by Stripe.js](https://docs.stripe.com/js/tokens/create_token?type=pii).
  ///
  /// * [String] lastName:
  ///   The person's last name.
  ///
  /// * [String] lastNameKana:
  ///   The Kana variation of the person's last name (Japan only).
  ///
  /// * [String] lastNameKanji:
  ///   The Kanji variation of the person's last name (Japan only).
  ///
  /// * [String] maidenName:
  ///   The person's maiden name.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] nationality:
  ///   The country where the person is a national. Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)), or \\\"XX\\\" if unavailable.
  ///
  /// * [String] personToken:
  ///   A [person token](https://docs.stripe.com/connect/account-tokens), used to securely provide details to the person.
  ///
  /// * [String] phone:
  ///   The person's phone number.
  ///
  /// * [String] politicalExposure:
  ///   Indicates if the person or any of their representatives, family members, or other closely related persons, declares that they hold or have held an important public job or function, in any jurisdiction.
  ///
  /// * [AddressSpecs1] registeredAddress:
  ///
  /// * [RelationshipSpecs] relationship:
  ///
  /// * [String] ssnLast4:
  ///   The last four digits of the person's Social Security number (U.S. only).
  ///
  /// * [UsCfpbDataSpecs] usCfpbData:
  ///
  /// * [PersonVerificationSpecs1] verification:
  Future<Person?> postAccountsAccountPeople(String account, { PersonAdditionalTosAcceptancesSpecs? additionalTosAcceptances, LegalEntityAndKycAddressSpecs1? address, JapanAddressKanaSpecs1? addressKana, JapanAddressKanjiSpecs1? addressKanji, PostAccountsAccountPeopleRequestDob? dob, PersonDocumentsSpecs? documents, String? email, List<String>? expand, String? firstName, String? firstNameKana, String? firstNameKanji, PostAccountsAccountPeopleRequestFullNameAliases? fullNameAliases, String? gender, String? idNumber, String? idNumberSecondary, String? lastName, String? lastNameKana, String? lastNameKanji, String? maidenName, PostAccountsRequestMetadata? metadata, String? nationality, String? personToken, String? phone, String? politicalExposure, AddressSpecs1? registeredAddress, RelationshipSpecs? relationship, String? ssnLast4, UsCfpbDataSpecs? usCfpbData, PersonVerificationSpecs1? verification, }) async {
    final response = await postAccountsAccountPeopleWithHttpInfo(account,  additionalTosAcceptances: additionalTosAcceptances, address: address, addressKana: addressKana, addressKanji: addressKanji, dob: dob, documents: documents, email: email, expand: expand, firstName: firstName, firstNameKana: firstNameKana, firstNameKanji: firstNameKanji, fullNameAliases: fullNameAliases, gender: gender, idNumber: idNumber, idNumberSecondary: idNumberSecondary, lastName: lastName, lastNameKana: lastNameKana, lastNameKanji: lastNameKanji, maidenName: maidenName, metadata: metadata, nationality: nationality, personToken: personToken, phone: phone, politicalExposure: politicalExposure, registeredAddress: registeredAddress, relationship: relationship, ssnLast4: ssnLast4, usCfpbData: usCfpbData, verification: verification, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Person',) as Person;
    
    }
    return null;
  }

  /// Update a person
  ///
  /// <p>Updates an existing person.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] person (required):
  ///
  /// * [PersonAdditionalTosAcceptancesSpecs] additionalTosAcceptances:
  ///
  /// * [LegalEntityAndKycAddressSpecs1] address:
  ///
  /// * [JapanAddressKanaSpecs1] addressKana:
  ///
  /// * [JapanAddressKanjiSpecs1] addressKanji:
  ///
  /// * [PostAccountsAccountPeopleRequestDob] dob:
  ///
  /// * [PersonDocumentsSpecs] documents:
  ///
  /// * [String] email:
  ///   The person's email address.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] firstName:
  ///   The person's first name.
  ///
  /// * [String] firstNameKana:
  ///   The Kana variation of the person's first name (Japan only).
  ///
  /// * [String] firstNameKanji:
  ///   The Kanji variation of the person's first name (Japan only).
  ///
  /// * [PostAccountsAccountPeopleRequestFullNameAliases] fullNameAliases:
  ///
  /// * [String] gender:
  ///   The person's gender (International regulations require either \\\"male\\\" or \\\"female\\\").
  ///
  /// * [String] idNumber:
  ///   The person's ID number, as appropriate for their country. For example, a social security number in the U.S., social insurance number in Canada, etc. Instead of the number itself, you can also provide a [PII token provided by Stripe.js](https://docs.stripe.com/js/tokens/create_token?type=pii).
  ///
  /// * [String] idNumberSecondary:
  ///   The person's secondary ID number, as appropriate for their country, will be used for enhanced verification checks. In Thailand, this would be the laser code found on the back of an ID card. Instead of the number itself, you can also provide a [PII token provided by Stripe.js](https://docs.stripe.com/js/tokens/create_token?type=pii).
  ///
  /// * [String] lastName:
  ///   The person's last name.
  ///
  /// * [String] lastNameKana:
  ///   The Kana variation of the person's last name (Japan only).
  ///
  /// * [String] lastNameKanji:
  ///   The Kanji variation of the person's last name (Japan only).
  ///
  /// * [String] maidenName:
  ///   The person's maiden name.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] nationality:
  ///   The country where the person is a national. Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)), or \\\"XX\\\" if unavailable.
  ///
  /// * [String] personToken:
  ///   A [person token](https://docs.stripe.com/connect/account-tokens), used to securely provide details to the person.
  ///
  /// * [String] phone:
  ///   The person's phone number.
  ///
  /// * [String] politicalExposure:
  ///   Indicates if the person or any of their representatives, family members, or other closely related persons, declares that they hold or have held an important public job or function, in any jurisdiction.
  ///
  /// * [AddressSpecs1] registeredAddress:
  ///
  /// * [RelationshipSpecs] relationship:
  ///
  /// * [String] ssnLast4:
  ///   The last four digits of the person's Social Security number (U.S. only).
  ///
  /// * [UsCfpbDataSpecs] usCfpbData:
  ///
  /// * [PersonVerificationSpecs1] verification:
  Future<Response> postAccountsAccountPeoplePersonWithHttpInfo(String account, String person, { PersonAdditionalTosAcceptancesSpecs? additionalTosAcceptances, LegalEntityAndKycAddressSpecs1? address, JapanAddressKanaSpecs1? addressKana, JapanAddressKanjiSpecs1? addressKanji, PostAccountsAccountPeopleRequestDob? dob, PersonDocumentsSpecs? documents, String? email, List<String>? expand, String? firstName, String? firstNameKana, String? firstNameKanji, PostAccountsAccountPeopleRequestFullNameAliases? fullNameAliases, String? gender, String? idNumber, String? idNumberSecondary, String? lastName, String? lastNameKana, String? lastNameKanji, String? maidenName, PostAccountsRequestMetadata? metadata, String? nationality, String? personToken, String? phone, String? politicalExposure, AddressSpecs1? registeredAddress, RelationshipSpecs? relationship, String? ssnLast4, UsCfpbDataSpecs? usCfpbData, PersonVerificationSpecs1? verification, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/accounts/{account}/people/{person}'
      .replaceAll('{account}', account)
      .replaceAll('{person}', person);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (additionalTosAcceptances != null) {
      formParams[r'additional_tos_acceptances'] = parameterToString(additionalTosAcceptances);
    }
    if (address != null) {
      formParams[r'address'] = parameterToString(address);
    }
    if (addressKana != null) {
      formParams[r'address_kana'] = parameterToString(addressKana);
    }
    if (addressKanji != null) {
      formParams[r'address_kanji'] = parameterToString(addressKanji);
    }
    if (dob != null) {
      formParams[r'dob'] = parameterToString(dob);
    }
    if (documents != null) {
      formParams[r'documents'] = parameterToString(documents);
    }
    if (email != null) {
      formParams[r'email'] = parameterToString(email);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (firstName != null) {
      formParams[r'first_name'] = parameterToString(firstName);
    }
    if (firstNameKana != null) {
      formParams[r'first_name_kana'] = parameterToString(firstNameKana);
    }
    if (firstNameKanji != null) {
      formParams[r'first_name_kanji'] = parameterToString(firstNameKanji);
    }
    if (fullNameAliases != null) {
      formParams[r'full_name_aliases'] = parameterToString(fullNameAliases);
    }
    if (gender != null) {
      formParams[r'gender'] = parameterToString(gender);
    }
    if (idNumber != null) {
      formParams[r'id_number'] = parameterToString(idNumber);
    }
    if (idNumberSecondary != null) {
      formParams[r'id_number_secondary'] = parameterToString(idNumberSecondary);
    }
    if (lastName != null) {
      formParams[r'last_name'] = parameterToString(lastName);
    }
    if (lastNameKana != null) {
      formParams[r'last_name_kana'] = parameterToString(lastNameKana);
    }
    if (lastNameKanji != null) {
      formParams[r'last_name_kanji'] = parameterToString(lastNameKanji);
    }
    if (maidenName != null) {
      formParams[r'maiden_name'] = parameterToString(maidenName);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (nationality != null) {
      formParams[r'nationality'] = parameterToString(nationality);
    }
    if (personToken != null) {
      formParams[r'person_token'] = parameterToString(personToken);
    }
    if (phone != null) {
      formParams[r'phone'] = parameterToString(phone);
    }
    if (politicalExposure != null) {
      formParams[r'political_exposure'] = parameterToString(politicalExposure);
    }
    if (registeredAddress != null) {
      formParams[r'registered_address'] = parameterToString(registeredAddress);
    }
    if (relationship != null) {
      formParams[r'relationship'] = parameterToString(relationship);
    }
    if (ssnLast4 != null) {
      formParams[r'ssn_last_4'] = parameterToString(ssnLast4);
    }
    if (usCfpbData != null) {
      formParams[r'us_cfpb_data'] = parameterToString(usCfpbData);
    }
    if (verification != null) {
      formParams[r'verification'] = parameterToString(verification);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a person
  ///
  /// <p>Updates an existing person.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] person (required):
  ///
  /// * [PersonAdditionalTosAcceptancesSpecs] additionalTosAcceptances:
  ///
  /// * [LegalEntityAndKycAddressSpecs1] address:
  ///
  /// * [JapanAddressKanaSpecs1] addressKana:
  ///
  /// * [JapanAddressKanjiSpecs1] addressKanji:
  ///
  /// * [PostAccountsAccountPeopleRequestDob] dob:
  ///
  /// * [PersonDocumentsSpecs] documents:
  ///
  /// * [String] email:
  ///   The person's email address.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] firstName:
  ///   The person's first name.
  ///
  /// * [String] firstNameKana:
  ///   The Kana variation of the person's first name (Japan only).
  ///
  /// * [String] firstNameKanji:
  ///   The Kanji variation of the person's first name (Japan only).
  ///
  /// * [PostAccountsAccountPeopleRequestFullNameAliases] fullNameAliases:
  ///
  /// * [String] gender:
  ///   The person's gender (International regulations require either \\\"male\\\" or \\\"female\\\").
  ///
  /// * [String] idNumber:
  ///   The person's ID number, as appropriate for their country. For example, a social security number in the U.S., social insurance number in Canada, etc. Instead of the number itself, you can also provide a [PII token provided by Stripe.js](https://docs.stripe.com/js/tokens/create_token?type=pii).
  ///
  /// * [String] idNumberSecondary:
  ///   The person's secondary ID number, as appropriate for their country, will be used for enhanced verification checks. In Thailand, this would be the laser code found on the back of an ID card. Instead of the number itself, you can also provide a [PII token provided by Stripe.js](https://docs.stripe.com/js/tokens/create_token?type=pii).
  ///
  /// * [String] lastName:
  ///   The person's last name.
  ///
  /// * [String] lastNameKana:
  ///   The Kana variation of the person's last name (Japan only).
  ///
  /// * [String] lastNameKanji:
  ///   The Kanji variation of the person's last name (Japan only).
  ///
  /// * [String] maidenName:
  ///   The person's maiden name.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] nationality:
  ///   The country where the person is a national. Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)), or \\\"XX\\\" if unavailable.
  ///
  /// * [String] personToken:
  ///   A [person token](https://docs.stripe.com/connect/account-tokens), used to securely provide details to the person.
  ///
  /// * [String] phone:
  ///   The person's phone number.
  ///
  /// * [String] politicalExposure:
  ///   Indicates if the person or any of their representatives, family members, or other closely related persons, declares that they hold or have held an important public job or function, in any jurisdiction.
  ///
  /// * [AddressSpecs1] registeredAddress:
  ///
  /// * [RelationshipSpecs] relationship:
  ///
  /// * [String] ssnLast4:
  ///   The last four digits of the person's Social Security number (U.S. only).
  ///
  /// * [UsCfpbDataSpecs] usCfpbData:
  ///
  /// * [PersonVerificationSpecs1] verification:
  Future<Person?> postAccountsAccountPeoplePerson(String account, String person, { PersonAdditionalTosAcceptancesSpecs? additionalTosAcceptances, LegalEntityAndKycAddressSpecs1? address, JapanAddressKanaSpecs1? addressKana, JapanAddressKanjiSpecs1? addressKanji, PostAccountsAccountPeopleRequestDob? dob, PersonDocumentsSpecs? documents, String? email, List<String>? expand, String? firstName, String? firstNameKana, String? firstNameKanji, PostAccountsAccountPeopleRequestFullNameAliases? fullNameAliases, String? gender, String? idNumber, String? idNumberSecondary, String? lastName, String? lastNameKana, String? lastNameKanji, String? maidenName, PostAccountsRequestMetadata? metadata, String? nationality, String? personToken, String? phone, String? politicalExposure, AddressSpecs1? registeredAddress, RelationshipSpecs? relationship, String? ssnLast4, UsCfpbDataSpecs? usCfpbData, PersonVerificationSpecs1? verification, }) async {
    final response = await postAccountsAccountPeoplePersonWithHttpInfo(account, person,  additionalTosAcceptances: additionalTosAcceptances, address: address, addressKana: addressKana, addressKanji: addressKanji, dob: dob, documents: documents, email: email, expand: expand, firstName: firstName, firstNameKana: firstNameKana, firstNameKanji: firstNameKanji, fullNameAliases: fullNameAliases, gender: gender, idNumber: idNumber, idNumberSecondary: idNumberSecondary, lastName: lastName, lastNameKana: lastNameKana, lastNameKanji: lastNameKanji, maidenName: maidenName, metadata: metadata, nationality: nationality, personToken: personToken, phone: phone, politicalExposure: politicalExposure, registeredAddress: registeredAddress, relationship: relationship, ssnLast4: ssnLast4, usCfpbData: usCfpbData, verification: verification, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Person',) as Person;
    
    }
    return null;
  }

  /// Create a person
  ///
  /// <p>Creates a new person.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [PersonAdditionalTosAcceptancesSpecs] additionalTosAcceptances:
  ///
  /// * [LegalEntityAndKycAddressSpecs1] address:
  ///
  /// * [JapanAddressKanaSpecs1] addressKana:
  ///
  /// * [JapanAddressKanjiSpecs1] addressKanji:
  ///
  /// * [PostAccountsAccountPeopleRequestDob] dob:
  ///
  /// * [PersonDocumentsSpecs] documents:
  ///
  /// * [String] email:
  ///   The person's email address.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] firstName:
  ///   The person's first name.
  ///
  /// * [String] firstNameKana:
  ///   The Kana variation of the person's first name (Japan only).
  ///
  /// * [String] firstNameKanji:
  ///   The Kanji variation of the person's first name (Japan only).
  ///
  /// * [PostAccountsAccountPeopleRequestFullNameAliases] fullNameAliases:
  ///
  /// * [String] gender:
  ///   The person's gender (International regulations require either \\\"male\\\" or \\\"female\\\").
  ///
  /// * [String] idNumber:
  ///   The person's ID number, as appropriate for their country. For example, a social security number in the U.S., social insurance number in Canada, etc. Instead of the number itself, you can also provide a [PII token provided by Stripe.js](https://docs.stripe.com/js/tokens/create_token?type=pii).
  ///
  /// * [String] idNumberSecondary:
  ///   The person's secondary ID number, as appropriate for their country, will be used for enhanced verification checks. In Thailand, this would be the laser code found on the back of an ID card. Instead of the number itself, you can also provide a [PII token provided by Stripe.js](https://docs.stripe.com/js/tokens/create_token?type=pii).
  ///
  /// * [String] lastName:
  ///   The person's last name.
  ///
  /// * [String] lastNameKana:
  ///   The Kana variation of the person's last name (Japan only).
  ///
  /// * [String] lastNameKanji:
  ///   The Kanji variation of the person's last name (Japan only).
  ///
  /// * [String] maidenName:
  ///   The person's maiden name.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] nationality:
  ///   The country where the person is a national. Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)), or \\\"XX\\\" if unavailable.
  ///
  /// * [String] personToken:
  ///   A [person token](https://docs.stripe.com/connect/account-tokens), used to securely provide details to the person.
  ///
  /// * [String] phone:
  ///   The person's phone number.
  ///
  /// * [String] politicalExposure:
  ///   Indicates if the person or any of their representatives, family members, or other closely related persons, declares that they hold or have held an important public job or function, in any jurisdiction.
  ///
  /// * [AddressSpecs1] registeredAddress:
  ///
  /// * [RelationshipSpecs] relationship:
  ///
  /// * [String] ssnLast4:
  ///   The last four digits of the person's Social Security number (U.S. only).
  ///
  /// * [UsCfpbDataSpecs] usCfpbData:
  ///
  /// * [PersonVerificationSpecs1] verification:
  Future<Response> postAccountsAccountPersonsWithHttpInfo(String account, { PersonAdditionalTosAcceptancesSpecs? additionalTosAcceptances, LegalEntityAndKycAddressSpecs1? address, JapanAddressKanaSpecs1? addressKana, JapanAddressKanjiSpecs1? addressKanji, PostAccountsAccountPeopleRequestDob? dob, PersonDocumentsSpecs? documents, String? email, List<String>? expand, String? firstName, String? firstNameKana, String? firstNameKanji, PostAccountsAccountPeopleRequestFullNameAliases? fullNameAliases, String? gender, String? idNumber, String? idNumberSecondary, String? lastName, String? lastNameKana, String? lastNameKanji, String? maidenName, PostAccountsRequestMetadata? metadata, String? nationality, String? personToken, String? phone, String? politicalExposure, AddressSpecs1? registeredAddress, RelationshipSpecs? relationship, String? ssnLast4, UsCfpbDataSpecs? usCfpbData, PersonVerificationSpecs1? verification, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/accounts/{account}/persons'
      .replaceAll('{account}', account);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (additionalTosAcceptances != null) {
      formParams[r'additional_tos_acceptances'] = parameterToString(additionalTosAcceptances);
    }
    if (address != null) {
      formParams[r'address'] = parameterToString(address);
    }
    if (addressKana != null) {
      formParams[r'address_kana'] = parameterToString(addressKana);
    }
    if (addressKanji != null) {
      formParams[r'address_kanji'] = parameterToString(addressKanji);
    }
    if (dob != null) {
      formParams[r'dob'] = parameterToString(dob);
    }
    if (documents != null) {
      formParams[r'documents'] = parameterToString(documents);
    }
    if (email != null) {
      formParams[r'email'] = parameterToString(email);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (firstName != null) {
      formParams[r'first_name'] = parameterToString(firstName);
    }
    if (firstNameKana != null) {
      formParams[r'first_name_kana'] = parameterToString(firstNameKana);
    }
    if (firstNameKanji != null) {
      formParams[r'first_name_kanji'] = parameterToString(firstNameKanji);
    }
    if (fullNameAliases != null) {
      formParams[r'full_name_aliases'] = parameterToString(fullNameAliases);
    }
    if (gender != null) {
      formParams[r'gender'] = parameterToString(gender);
    }
    if (idNumber != null) {
      formParams[r'id_number'] = parameterToString(idNumber);
    }
    if (idNumberSecondary != null) {
      formParams[r'id_number_secondary'] = parameterToString(idNumberSecondary);
    }
    if (lastName != null) {
      formParams[r'last_name'] = parameterToString(lastName);
    }
    if (lastNameKana != null) {
      formParams[r'last_name_kana'] = parameterToString(lastNameKana);
    }
    if (lastNameKanji != null) {
      formParams[r'last_name_kanji'] = parameterToString(lastNameKanji);
    }
    if (maidenName != null) {
      formParams[r'maiden_name'] = parameterToString(maidenName);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (nationality != null) {
      formParams[r'nationality'] = parameterToString(nationality);
    }
    if (personToken != null) {
      formParams[r'person_token'] = parameterToString(personToken);
    }
    if (phone != null) {
      formParams[r'phone'] = parameterToString(phone);
    }
    if (politicalExposure != null) {
      formParams[r'political_exposure'] = parameterToString(politicalExposure);
    }
    if (registeredAddress != null) {
      formParams[r'registered_address'] = parameterToString(registeredAddress);
    }
    if (relationship != null) {
      formParams[r'relationship'] = parameterToString(relationship);
    }
    if (ssnLast4 != null) {
      formParams[r'ssn_last_4'] = parameterToString(ssnLast4);
    }
    if (usCfpbData != null) {
      formParams[r'us_cfpb_data'] = parameterToString(usCfpbData);
    }
    if (verification != null) {
      formParams[r'verification'] = parameterToString(verification);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a person
  ///
  /// <p>Creates a new person.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [PersonAdditionalTosAcceptancesSpecs] additionalTosAcceptances:
  ///
  /// * [LegalEntityAndKycAddressSpecs1] address:
  ///
  /// * [JapanAddressKanaSpecs1] addressKana:
  ///
  /// * [JapanAddressKanjiSpecs1] addressKanji:
  ///
  /// * [PostAccountsAccountPeopleRequestDob] dob:
  ///
  /// * [PersonDocumentsSpecs] documents:
  ///
  /// * [String] email:
  ///   The person's email address.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] firstName:
  ///   The person's first name.
  ///
  /// * [String] firstNameKana:
  ///   The Kana variation of the person's first name (Japan only).
  ///
  /// * [String] firstNameKanji:
  ///   The Kanji variation of the person's first name (Japan only).
  ///
  /// * [PostAccountsAccountPeopleRequestFullNameAliases] fullNameAliases:
  ///
  /// * [String] gender:
  ///   The person's gender (International regulations require either \\\"male\\\" or \\\"female\\\").
  ///
  /// * [String] idNumber:
  ///   The person's ID number, as appropriate for their country. For example, a social security number in the U.S., social insurance number in Canada, etc. Instead of the number itself, you can also provide a [PII token provided by Stripe.js](https://docs.stripe.com/js/tokens/create_token?type=pii).
  ///
  /// * [String] idNumberSecondary:
  ///   The person's secondary ID number, as appropriate for their country, will be used for enhanced verification checks. In Thailand, this would be the laser code found on the back of an ID card. Instead of the number itself, you can also provide a [PII token provided by Stripe.js](https://docs.stripe.com/js/tokens/create_token?type=pii).
  ///
  /// * [String] lastName:
  ///   The person's last name.
  ///
  /// * [String] lastNameKana:
  ///   The Kana variation of the person's last name (Japan only).
  ///
  /// * [String] lastNameKanji:
  ///   The Kanji variation of the person's last name (Japan only).
  ///
  /// * [String] maidenName:
  ///   The person's maiden name.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] nationality:
  ///   The country where the person is a national. Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)), or \\\"XX\\\" if unavailable.
  ///
  /// * [String] personToken:
  ///   A [person token](https://docs.stripe.com/connect/account-tokens), used to securely provide details to the person.
  ///
  /// * [String] phone:
  ///   The person's phone number.
  ///
  /// * [String] politicalExposure:
  ///   Indicates if the person or any of their representatives, family members, or other closely related persons, declares that they hold or have held an important public job or function, in any jurisdiction.
  ///
  /// * [AddressSpecs1] registeredAddress:
  ///
  /// * [RelationshipSpecs] relationship:
  ///
  /// * [String] ssnLast4:
  ///   The last four digits of the person's Social Security number (U.S. only).
  ///
  /// * [UsCfpbDataSpecs] usCfpbData:
  ///
  /// * [PersonVerificationSpecs1] verification:
  Future<Person?> postAccountsAccountPersons(String account, { PersonAdditionalTosAcceptancesSpecs? additionalTosAcceptances, LegalEntityAndKycAddressSpecs1? address, JapanAddressKanaSpecs1? addressKana, JapanAddressKanjiSpecs1? addressKanji, PostAccountsAccountPeopleRequestDob? dob, PersonDocumentsSpecs? documents, String? email, List<String>? expand, String? firstName, String? firstNameKana, String? firstNameKanji, PostAccountsAccountPeopleRequestFullNameAliases? fullNameAliases, String? gender, String? idNumber, String? idNumberSecondary, String? lastName, String? lastNameKana, String? lastNameKanji, String? maidenName, PostAccountsRequestMetadata? metadata, String? nationality, String? personToken, String? phone, String? politicalExposure, AddressSpecs1? registeredAddress, RelationshipSpecs? relationship, String? ssnLast4, UsCfpbDataSpecs? usCfpbData, PersonVerificationSpecs1? verification, }) async {
    final response = await postAccountsAccountPersonsWithHttpInfo(account,  additionalTosAcceptances: additionalTosAcceptances, address: address, addressKana: addressKana, addressKanji: addressKanji, dob: dob, documents: documents, email: email, expand: expand, firstName: firstName, firstNameKana: firstNameKana, firstNameKanji: firstNameKanji, fullNameAliases: fullNameAliases, gender: gender, idNumber: idNumber, idNumberSecondary: idNumberSecondary, lastName: lastName, lastNameKana: lastNameKana, lastNameKanji: lastNameKanji, maidenName: maidenName, metadata: metadata, nationality: nationality, personToken: personToken, phone: phone, politicalExposure: politicalExposure, registeredAddress: registeredAddress, relationship: relationship, ssnLast4: ssnLast4, usCfpbData: usCfpbData, verification: verification, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Person',) as Person;
    
    }
    return null;
  }

  /// Update a person
  ///
  /// <p>Updates an existing person.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] person (required):
  ///
  /// * [PersonAdditionalTosAcceptancesSpecs] additionalTosAcceptances:
  ///
  /// * [LegalEntityAndKycAddressSpecs1] address:
  ///
  /// * [JapanAddressKanaSpecs1] addressKana:
  ///
  /// * [JapanAddressKanjiSpecs1] addressKanji:
  ///
  /// * [PostAccountsAccountPeopleRequestDob] dob:
  ///
  /// * [PersonDocumentsSpecs] documents:
  ///
  /// * [String] email:
  ///   The person's email address.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] firstName:
  ///   The person's first name.
  ///
  /// * [String] firstNameKana:
  ///   The Kana variation of the person's first name (Japan only).
  ///
  /// * [String] firstNameKanji:
  ///   The Kanji variation of the person's first name (Japan only).
  ///
  /// * [PostAccountsAccountPeopleRequestFullNameAliases] fullNameAliases:
  ///
  /// * [String] gender:
  ///   The person's gender (International regulations require either \\\"male\\\" or \\\"female\\\").
  ///
  /// * [String] idNumber:
  ///   The person's ID number, as appropriate for their country. For example, a social security number in the U.S., social insurance number in Canada, etc. Instead of the number itself, you can also provide a [PII token provided by Stripe.js](https://docs.stripe.com/js/tokens/create_token?type=pii).
  ///
  /// * [String] idNumberSecondary:
  ///   The person's secondary ID number, as appropriate for their country, will be used for enhanced verification checks. In Thailand, this would be the laser code found on the back of an ID card. Instead of the number itself, you can also provide a [PII token provided by Stripe.js](https://docs.stripe.com/js/tokens/create_token?type=pii).
  ///
  /// * [String] lastName:
  ///   The person's last name.
  ///
  /// * [String] lastNameKana:
  ///   The Kana variation of the person's last name (Japan only).
  ///
  /// * [String] lastNameKanji:
  ///   The Kanji variation of the person's last name (Japan only).
  ///
  /// * [String] maidenName:
  ///   The person's maiden name.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] nationality:
  ///   The country where the person is a national. Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)), or \\\"XX\\\" if unavailable.
  ///
  /// * [String] personToken:
  ///   A [person token](https://docs.stripe.com/connect/account-tokens), used to securely provide details to the person.
  ///
  /// * [String] phone:
  ///   The person's phone number.
  ///
  /// * [String] politicalExposure:
  ///   Indicates if the person or any of their representatives, family members, or other closely related persons, declares that they hold or have held an important public job or function, in any jurisdiction.
  ///
  /// * [AddressSpecs1] registeredAddress:
  ///
  /// * [RelationshipSpecs] relationship:
  ///
  /// * [String] ssnLast4:
  ///   The last four digits of the person's Social Security number (U.S. only).
  ///
  /// * [UsCfpbDataSpecs] usCfpbData:
  ///
  /// * [PersonVerificationSpecs1] verification:
  Future<Response> postAccountsAccountPersonsPersonWithHttpInfo(String account, String person, { PersonAdditionalTosAcceptancesSpecs? additionalTosAcceptances, LegalEntityAndKycAddressSpecs1? address, JapanAddressKanaSpecs1? addressKana, JapanAddressKanjiSpecs1? addressKanji, PostAccountsAccountPeopleRequestDob? dob, PersonDocumentsSpecs? documents, String? email, List<String>? expand, String? firstName, String? firstNameKana, String? firstNameKanji, PostAccountsAccountPeopleRequestFullNameAliases? fullNameAliases, String? gender, String? idNumber, String? idNumberSecondary, String? lastName, String? lastNameKana, String? lastNameKanji, String? maidenName, PostAccountsRequestMetadata? metadata, String? nationality, String? personToken, String? phone, String? politicalExposure, AddressSpecs1? registeredAddress, RelationshipSpecs? relationship, String? ssnLast4, UsCfpbDataSpecs? usCfpbData, PersonVerificationSpecs1? verification, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/accounts/{account}/persons/{person}'
      .replaceAll('{account}', account)
      .replaceAll('{person}', person);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (additionalTosAcceptances != null) {
      formParams[r'additional_tos_acceptances'] = parameterToString(additionalTosAcceptances);
    }
    if (address != null) {
      formParams[r'address'] = parameterToString(address);
    }
    if (addressKana != null) {
      formParams[r'address_kana'] = parameterToString(addressKana);
    }
    if (addressKanji != null) {
      formParams[r'address_kanji'] = parameterToString(addressKanji);
    }
    if (dob != null) {
      formParams[r'dob'] = parameterToString(dob);
    }
    if (documents != null) {
      formParams[r'documents'] = parameterToString(documents);
    }
    if (email != null) {
      formParams[r'email'] = parameterToString(email);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (firstName != null) {
      formParams[r'first_name'] = parameterToString(firstName);
    }
    if (firstNameKana != null) {
      formParams[r'first_name_kana'] = parameterToString(firstNameKana);
    }
    if (firstNameKanji != null) {
      formParams[r'first_name_kanji'] = parameterToString(firstNameKanji);
    }
    if (fullNameAliases != null) {
      formParams[r'full_name_aliases'] = parameterToString(fullNameAliases);
    }
    if (gender != null) {
      formParams[r'gender'] = parameterToString(gender);
    }
    if (idNumber != null) {
      formParams[r'id_number'] = parameterToString(idNumber);
    }
    if (idNumberSecondary != null) {
      formParams[r'id_number_secondary'] = parameterToString(idNumberSecondary);
    }
    if (lastName != null) {
      formParams[r'last_name'] = parameterToString(lastName);
    }
    if (lastNameKana != null) {
      formParams[r'last_name_kana'] = parameterToString(lastNameKana);
    }
    if (lastNameKanji != null) {
      formParams[r'last_name_kanji'] = parameterToString(lastNameKanji);
    }
    if (maidenName != null) {
      formParams[r'maiden_name'] = parameterToString(maidenName);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (nationality != null) {
      formParams[r'nationality'] = parameterToString(nationality);
    }
    if (personToken != null) {
      formParams[r'person_token'] = parameterToString(personToken);
    }
    if (phone != null) {
      formParams[r'phone'] = parameterToString(phone);
    }
    if (politicalExposure != null) {
      formParams[r'political_exposure'] = parameterToString(politicalExposure);
    }
    if (registeredAddress != null) {
      formParams[r'registered_address'] = parameterToString(registeredAddress);
    }
    if (relationship != null) {
      formParams[r'relationship'] = parameterToString(relationship);
    }
    if (ssnLast4 != null) {
      formParams[r'ssn_last_4'] = parameterToString(ssnLast4);
    }
    if (usCfpbData != null) {
      formParams[r'us_cfpb_data'] = parameterToString(usCfpbData);
    }
    if (verification != null) {
      formParams[r'verification'] = parameterToString(verification);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a person
  ///
  /// <p>Updates an existing person.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] person (required):
  ///
  /// * [PersonAdditionalTosAcceptancesSpecs] additionalTosAcceptances:
  ///
  /// * [LegalEntityAndKycAddressSpecs1] address:
  ///
  /// * [JapanAddressKanaSpecs1] addressKana:
  ///
  /// * [JapanAddressKanjiSpecs1] addressKanji:
  ///
  /// * [PostAccountsAccountPeopleRequestDob] dob:
  ///
  /// * [PersonDocumentsSpecs] documents:
  ///
  /// * [String] email:
  ///   The person's email address.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] firstName:
  ///   The person's first name.
  ///
  /// * [String] firstNameKana:
  ///   The Kana variation of the person's first name (Japan only).
  ///
  /// * [String] firstNameKanji:
  ///   The Kanji variation of the person's first name (Japan only).
  ///
  /// * [PostAccountsAccountPeopleRequestFullNameAliases] fullNameAliases:
  ///
  /// * [String] gender:
  ///   The person's gender (International regulations require either \\\"male\\\" or \\\"female\\\").
  ///
  /// * [String] idNumber:
  ///   The person's ID number, as appropriate for their country. For example, a social security number in the U.S., social insurance number in Canada, etc. Instead of the number itself, you can also provide a [PII token provided by Stripe.js](https://docs.stripe.com/js/tokens/create_token?type=pii).
  ///
  /// * [String] idNumberSecondary:
  ///   The person's secondary ID number, as appropriate for their country, will be used for enhanced verification checks. In Thailand, this would be the laser code found on the back of an ID card. Instead of the number itself, you can also provide a [PII token provided by Stripe.js](https://docs.stripe.com/js/tokens/create_token?type=pii).
  ///
  /// * [String] lastName:
  ///   The person's last name.
  ///
  /// * [String] lastNameKana:
  ///   The Kana variation of the person's last name (Japan only).
  ///
  /// * [String] lastNameKanji:
  ///   The Kanji variation of the person's last name (Japan only).
  ///
  /// * [String] maidenName:
  ///   The person's maiden name.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] nationality:
  ///   The country where the person is a national. Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)), or \\\"XX\\\" if unavailable.
  ///
  /// * [String] personToken:
  ///   A [person token](https://docs.stripe.com/connect/account-tokens), used to securely provide details to the person.
  ///
  /// * [String] phone:
  ///   The person's phone number.
  ///
  /// * [String] politicalExposure:
  ///   Indicates if the person or any of their representatives, family members, or other closely related persons, declares that they hold or have held an important public job or function, in any jurisdiction.
  ///
  /// * [AddressSpecs1] registeredAddress:
  ///
  /// * [RelationshipSpecs] relationship:
  ///
  /// * [String] ssnLast4:
  ///   The last four digits of the person's Social Security number (U.S. only).
  ///
  /// * [UsCfpbDataSpecs] usCfpbData:
  ///
  /// * [PersonVerificationSpecs1] verification:
  Future<Person?> postAccountsAccountPersonsPerson(String account, String person, { PersonAdditionalTosAcceptancesSpecs? additionalTosAcceptances, LegalEntityAndKycAddressSpecs1? address, JapanAddressKanaSpecs1? addressKana, JapanAddressKanjiSpecs1? addressKanji, PostAccountsAccountPeopleRequestDob? dob, PersonDocumentsSpecs? documents, String? email, List<String>? expand, String? firstName, String? firstNameKana, String? firstNameKanji, PostAccountsAccountPeopleRequestFullNameAliases? fullNameAliases, String? gender, String? idNumber, String? idNumberSecondary, String? lastName, String? lastNameKana, String? lastNameKanji, String? maidenName, PostAccountsRequestMetadata? metadata, String? nationality, String? personToken, String? phone, String? politicalExposure, AddressSpecs1? registeredAddress, RelationshipSpecs? relationship, String? ssnLast4, UsCfpbDataSpecs? usCfpbData, PersonVerificationSpecs1? verification, }) async {
    final response = await postAccountsAccountPersonsPersonWithHttpInfo(account, person,  additionalTosAcceptances: additionalTosAcceptances, address: address, addressKana: addressKana, addressKanji: addressKanji, dob: dob, documents: documents, email: email, expand: expand, firstName: firstName, firstNameKana: firstNameKana, firstNameKanji: firstNameKanji, fullNameAliases: fullNameAliases, gender: gender, idNumber: idNumber, idNumberSecondary: idNumberSecondary, lastName: lastName, lastNameKana: lastNameKana, lastNameKanji: lastNameKanji, maidenName: maidenName, metadata: metadata, nationality: nationality, personToken: personToken, phone: phone, politicalExposure: politicalExposure, registeredAddress: registeredAddress, relationship: relationship, ssnLast4: ssnLast4, usCfpbData: usCfpbData, verification: verification, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Person',) as Person;
    
    }
    return null;
  }

  /// Reject an account
  ///
  /// <p>With <a href=\"/connect\">Connect</a>, you can reject accounts that you have flagged as suspicious.</p>  <p>Only accounts where your platform is liable for negative account balances, which includes Custom and Express accounts, can be rejected. Test-mode accounts can be rejected at any time. Live-mode accounts can only be rejected after all balances are zero.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] reason (required):
  ///   The reason for rejecting the account. Can be `fraud`, `terms_of_service`, or `other`.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postAccountsAccountRejectWithHttpInfo(String account, String reason, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/accounts/{account}/reject'
      .replaceAll('{account}', account);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (reason != null) {
      formParams[r'reason'] = parameterToString(reason);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Reject an account
  ///
  /// <p>With <a href=\"/connect\">Connect</a>, you can reject accounts that you have flagged as suspicious.</p>  <p>Only accounts where your platform is liable for negative account balances, which includes Custom and Express accounts, can be rejected. Test-mode accounts can be rejected at any time. Live-mode accounts can only be rejected after all balances are zero.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [String] reason (required):
  ///   The reason for rejecting the account. Can be `fraud`, `terms_of_service`, or `other`.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Account?> postAccountsAccountReject(String account, String reason, { List<String>? expand, }) async {
    final response = await postAccountsAccountRejectWithHttpInfo(account, reason,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Account',) as Account;
    
    }
    return null;
  }

  /// <p>Create an apple pay domain.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] domainName (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postApplePayDomainsWithHttpInfo(String domainName, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/apple_pay/domains';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (domainName != null) {
      formParams[r'domain_name'] = parameterToString(domainName);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// <p>Create an apple pay domain.</p>
  ///
  /// Parameters:
  ///
  /// * [String] domainName (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<ApplePayDomain?> postApplePayDomains(String domainName, { List<String>? expand, }) async {
    final response = await postApplePayDomainsWithHttpInfo(domainName,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ApplePayDomain',) as ApplePayDomain;
    
    }
    return null;
  }

  /// Update an application fee refund
  ///
  /// <p>Updates the specified application fee refund by setting the values of the parameters passed. Any parameters not provided will be left unchanged.</p>  <p>This request only accepts metadata as an argument.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] fee (required):
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<Response> postApplicationFeesFeeRefundsIdWithHttpInfo(String fee, String id, { List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/application_fees/{fee}/refunds/{id}'
      .replaceAll('{fee}', fee)
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update an application fee refund
  ///
  /// <p>Updates the specified application fee refund by setting the values of the parameters passed. Any parameters not provided will be left unchanged.</p>  <p>This request only accepts metadata as an argument.</p>
  ///
  /// Parameters:
  ///
  /// * [String] fee (required):
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<FeeRefund?> postApplicationFeesFeeRefundsId(String fee, String id, { List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    final response = await postApplicationFeesFeeRefundsIdWithHttpInfo(fee, id,  expand: expand, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'FeeRefund',) as FeeRefund;
    
    }
    return null;
  }

  /// 
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [int] amount:
  ///
  /// * [String] directive:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postApplicationFeesIdRefundWithHttpInfo(String id, { int? amount, String? directive, List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/application_fees/{id}/refund'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (directive != null) {
      formParams[r'directive'] = parameterToString(directive);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// 
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [int] amount:
  ///
  /// * [String] directive:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<ApplicationFee?> postApplicationFeesIdRefund(String id, { int? amount, String? directive, List<String>? expand, }) async {
    final response = await postApplicationFeesIdRefundWithHttpInfo(id,  amount: amount, directive: directive, expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ApplicationFee',) as ApplicationFee;
    
    }
    return null;
  }

  /// Create an application fee refund
  ///
  /// <p>Refunds an application fee that has previously been collected but not yet refunded. Funds will be refunded to the Stripe account from which the fee was originally collected.</p>  <p>You can optionally refund only part of an application fee. You can do so multiple times, until the entire fee has been refunded.</p>  <p>Once entirely refunded, an application fee can’t be refunded again. This method will raise an error when called on an already-refunded application fee, or when trying to refund more money than is left on an application fee.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [int] amount:
  ///   A positive integer, in _cents (or local equivalent)_, representing how much of this fee to refund. Can refund only up to the remaining unrefunded amount of the fee.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  Future<Response> postApplicationFeesIdRefundsWithHttpInfo(String id, { int? amount, List<String>? expand, Map<String, String>? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/application_fees/{id}/refunds'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create an application fee refund
  ///
  /// <p>Refunds an application fee that has previously been collected but not yet refunded. Funds will be refunded to the Stripe account from which the fee was originally collected.</p>  <p>You can optionally refund only part of an application fee. You can do so multiple times, until the entire fee has been refunded.</p>  <p>Once entirely refunded, an application fee can’t be refunded again. This method will raise an error when called on an already-refunded application fee, or when trying to refund more money than is left on an application fee.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [int] amount:
  ///   A positive integer, in _cents (or local equivalent)_, representing how much of this fee to refund. Can refund only up to the remaining unrefunded amount of the fee.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  Future<FeeRefund?> postApplicationFeesIdRefunds(String id, { int? amount, List<String>? expand, Map<String, String>? metadata, }) async {
    final response = await postApplicationFeesIdRefundsWithHttpInfo(id,  amount: amount, expand: expand, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'FeeRefund',) as FeeRefund;
    
    }
    return null;
  }

  /// Set a Secret
  ///
  /// <p>Create or replace a secret in the secret store.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] name (required):
  ///   A name for the secret that's unique within the scope.
  ///
  /// * [String] payload (required):
  ///   The plaintext secret value to be stored.
  ///
  /// * [ScopeParam1] scope (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] expiresAt:
  ///   The Unix timestamp for the expiry time of the secret, after which the secret deletes.
  Future<Response> postAppsSecretsWithHttpInfo(String name, String payload, ScopeParam1 scope, { List<String>? expand, int? expiresAt, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/apps/secrets';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (expiresAt != null) {
      formParams[r'expires_at'] = parameterToString(expiresAt);
    }
    if (name != null) {
      formParams[r'name'] = parameterToString(name);
    }
    if (payload != null) {
      formParams[r'payload'] = parameterToString(payload);
    }
    if (scope != null) {
      formParams[r'scope'] = parameterToString(scope);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Set a Secret
  ///
  /// <p>Create or replace a secret in the secret store.</p>
  ///
  /// Parameters:
  ///
  /// * [String] name (required):
  ///   A name for the secret that's unique within the scope.
  ///
  /// * [String] payload (required):
  ///   The plaintext secret value to be stored.
  ///
  /// * [ScopeParam1] scope (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] expiresAt:
  ///   The Unix timestamp for the expiry time of the secret, after which the secret deletes.
  Future<AppsSecret?> postAppsSecrets(String name, String payload, ScopeParam1 scope, { List<String>? expand, int? expiresAt, }) async {
    final response = await postAppsSecretsWithHttpInfo(name, payload, scope,  expand: expand, expiresAt: expiresAt, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'AppsSecret',) as AppsSecret;
    
    }
    return null;
  }

  /// Delete a Secret
  ///
  /// <p>Deletes a secret from the secret store by name and scope.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] name (required):
  ///   A name for the secret that's unique within the scope.
  ///
  /// * [ScopeParam1] scope (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postAppsSecretsDeleteWithHttpInfo(String name, ScopeParam1 scope, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/apps/secrets/delete';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (name != null) {
      formParams[r'name'] = parameterToString(name);
    }
    if (scope != null) {
      formParams[r'scope'] = parameterToString(scope);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Delete a Secret
  ///
  /// <p>Deletes a secret from the secret store by name and scope.</p>
  ///
  /// Parameters:
  ///
  /// * [String] name (required):
  ///   A name for the secret that's unique within the scope.
  ///
  /// * [ScopeParam1] scope (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<AppsSecret?> postAppsSecretsDelete(String name, ScopeParam1 scope, { List<String>? expand, }) async {
    final response = await postAppsSecretsDeleteWithHttpInfo(name, scope,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'AppsSecret',) as AppsSecret;
    
    }
    return null;
  }

  /// Create a billing alert
  ///
  /// <p>Creates a billing alert</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] alertType (required):
  ///   The type of alert to create.
  ///
  /// * [String] title (required):
  ///   The title of the alert.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [UsageThresholdConfig] usageThreshold:
  Future<Response> postBillingAlertsWithHttpInfo(String alertType, String title, { List<String>? expand, UsageThresholdConfig? usageThreshold, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/billing/alerts';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (alertType != null) {
      formParams[r'alert_type'] = parameterToString(alertType);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (title != null) {
      formParams[r'title'] = parameterToString(title);
    }
    if (usageThreshold != null) {
      formParams[r'usage_threshold'] = parameterToString(usageThreshold);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a billing alert
  ///
  /// <p>Creates a billing alert</p>
  ///
  /// Parameters:
  ///
  /// * [String] alertType (required):
  ///   The type of alert to create.
  ///
  /// * [String] title (required):
  ///   The title of the alert.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [UsageThresholdConfig] usageThreshold:
  Future<BillingAlert?> postBillingAlerts(String alertType, String title, { List<String>? expand, UsageThresholdConfig? usageThreshold, }) async {
    final response = await postBillingAlertsWithHttpInfo(alertType, title,  expand: expand, usageThreshold: usageThreshold, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BillingAlert',) as BillingAlert;
    
    }
    return null;
  }

  /// Activate a billing alert
  ///
  /// <p>Reactivates this alert, allowing it to trigger again.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postBillingAlertsIdActivateWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/billing/alerts/{id}/activate'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Activate a billing alert
  ///
  /// <p>Reactivates this alert, allowing it to trigger again.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<BillingAlert?> postBillingAlertsIdActivate(String id, { List<String>? expand, }) async {
    final response = await postBillingAlertsIdActivateWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BillingAlert',) as BillingAlert;
    
    }
    return null;
  }

  /// Archive a billing alert
  ///
  /// <p>Archives this alert, removing it from the list view and APIs. This is non-reversible.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postBillingAlertsIdArchiveWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/billing/alerts/{id}/archive'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Archive a billing alert
  ///
  /// <p>Archives this alert, removing it from the list view and APIs. This is non-reversible.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<BillingAlert?> postBillingAlertsIdArchive(String id, { List<String>? expand, }) async {
    final response = await postBillingAlertsIdArchiveWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BillingAlert',) as BillingAlert;
    
    }
    return null;
  }

  /// Deactivate a billing alert
  ///
  /// <p>Deactivates this alert, preventing it from triggering.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postBillingAlertsIdDeactivateWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/billing/alerts/{id}/deactivate'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Deactivate a billing alert
  ///
  /// <p>Deactivates this alert, preventing it from triggering.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<BillingAlert?> postBillingAlertsIdDeactivate(String id, { List<String>? expand, }) async {
    final response = await postBillingAlertsIdDeactivateWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BillingAlert',) as BillingAlert;
    
    }
    return null;
  }

  /// Create a credit grant
  ///
  /// <p>Creates a credit grant.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [AmountParam] amount (required):
  ///
  /// * [ApplicabilityConfigParam] applicabilityConfig (required):
  ///
  /// * [String] category (required):
  ///   The category of this credit grant.
  ///
  /// * [String] customer (required):
  ///   ID of the customer to receive the billing credits.
  ///
  /// * [int] effectiveAt:
  ///   The time when the billing credits become effective-when they're eligible for use. It defaults to the current timestamp if not specified.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] expiresAt:
  ///   The time when the billing credits expire. If not specified, the billing credits don't expire.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of key-value pairs that you can attach to an object. You can use this to store additional information about the object (for example, cost basis) in a structured format.
  ///
  /// * [String] name:
  ///   A descriptive name shown in the Dashboard.
  ///
  /// * [int] priority:
  ///   The desired priority for applying this credit grant. If not specified, it will be set to the default value of 50. The highest priority is 0 and the lowest is 100.
  Future<Response> postBillingCreditGrantsWithHttpInfo(AmountParam amount, ApplicabilityConfigParam applicabilityConfig, String category, String customer, { int? effectiveAt, List<String>? expand, int? expiresAt, Map<String, String>? metadata, String? name, int? priority, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/billing/credit_grants';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (applicabilityConfig != null) {
      formParams[r'applicability_config'] = parameterToString(applicabilityConfig);
    }
    if (category != null) {
      formParams[r'category'] = parameterToString(category);
    }
    if (customer != null) {
      formParams[r'customer'] = parameterToString(customer);
    }
    if (effectiveAt != null) {
      formParams[r'effective_at'] = parameterToString(effectiveAt);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (expiresAt != null) {
      formParams[r'expires_at'] = parameterToString(expiresAt);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (name != null) {
      formParams[r'name'] = parameterToString(name);
    }
    if (priority != null) {
      formParams[r'priority'] = parameterToString(priority);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a credit grant
  ///
  /// <p>Creates a credit grant.</p>
  ///
  /// Parameters:
  ///
  /// * [AmountParam] amount (required):
  ///
  /// * [ApplicabilityConfigParam] applicabilityConfig (required):
  ///
  /// * [String] category (required):
  ///   The category of this credit grant.
  ///
  /// * [String] customer (required):
  ///   ID of the customer to receive the billing credits.
  ///
  /// * [int] effectiveAt:
  ///   The time when the billing credits become effective-when they're eligible for use. It defaults to the current timestamp if not specified.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] expiresAt:
  ///   The time when the billing credits expire. If not specified, the billing credits don't expire.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of key-value pairs that you can attach to an object. You can use this to store additional information about the object (for example, cost basis) in a structured format.
  ///
  /// * [String] name:
  ///   A descriptive name shown in the Dashboard.
  ///
  /// * [int] priority:
  ///   The desired priority for applying this credit grant. If not specified, it will be set to the default value of 50. The highest priority is 0 and the lowest is 100.
  Future<BillingCreditGrant?> postBillingCreditGrants(AmountParam amount, ApplicabilityConfigParam applicabilityConfig, String category, String customer, { int? effectiveAt, List<String>? expand, int? expiresAt, Map<String, String>? metadata, String? name, int? priority, }) async {
    final response = await postBillingCreditGrantsWithHttpInfo(amount, applicabilityConfig, category, customer,  effectiveAt: effectiveAt, expand: expand, expiresAt: expiresAt, metadata: metadata, name: name, priority: priority, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BillingCreditGrant',) as BillingCreditGrant;
    
    }
    return null;
  }

  /// Update a credit grant
  ///
  /// <p>Updates a credit grant.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///   Unique identifier for the object.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostBillingCreditGrantsIdRequestExpiresAt] expiresAt:
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of key-value pairs you can attach to an object. You can use this to store additional information about the object (for example, cost basis) in a structured format.
  Future<Response> postBillingCreditGrantsIdWithHttpInfo(String id, { List<String>? expand, PostBillingCreditGrantsIdRequestExpiresAt? expiresAt, Map<String, String>? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/billing/credit_grants/{id}'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (expiresAt != null) {
      formParams[r'expires_at'] = parameterToString(expiresAt);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a credit grant
  ///
  /// <p>Updates a credit grant.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///   Unique identifier for the object.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostBillingCreditGrantsIdRequestExpiresAt] expiresAt:
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of key-value pairs you can attach to an object. You can use this to store additional information about the object (for example, cost basis) in a structured format.
  Future<BillingCreditGrant?> postBillingCreditGrantsId(String id, { List<String>? expand, PostBillingCreditGrantsIdRequestExpiresAt? expiresAt, Map<String, String>? metadata, }) async {
    final response = await postBillingCreditGrantsIdWithHttpInfo(id,  expand: expand, expiresAt: expiresAt, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BillingCreditGrant',) as BillingCreditGrant;
    
    }
    return null;
  }

  /// Expire a credit grant
  ///
  /// <p>Expires a credit grant.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///   Unique identifier for the object.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postBillingCreditGrantsIdExpireWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/billing/credit_grants/{id}/expire'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Expire a credit grant
  ///
  /// <p>Expires a credit grant.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///   Unique identifier for the object.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<BillingCreditGrant?> postBillingCreditGrantsIdExpire(String id, { List<String>? expand, }) async {
    final response = await postBillingCreditGrantsIdExpireWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BillingCreditGrant',) as BillingCreditGrant;
    
    }
    return null;
  }

  /// Void a credit grant
  ///
  /// <p>Voids a credit grant.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///   Unique identifier for the object.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postBillingCreditGrantsIdVoidWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/billing/credit_grants/{id}/void'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Void a credit grant
  ///
  /// <p>Voids a credit grant.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///   Unique identifier for the object.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<BillingCreditGrant?> postBillingCreditGrantsIdVoid(String id, { List<String>? expand, }) async {
    final response = await postBillingCreditGrantsIdVoidWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BillingCreditGrant',) as BillingCreditGrant;
    
    }
    return null;
  }

  /// Create a billing meter event adjustment
  ///
  /// <p>Creates a billing meter event adjustment.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] eventName (required):
  ///   The name of the meter event. Corresponds with the `event_name` field on a meter.
  ///
  /// * [String] type (required):
  ///   Specifies whether to cancel a single event or a range of events for a time period. Time period cancellation is not supported yet.
  ///
  /// * [EventAdjustmentCancelSettingsParam] cancel:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postBillingMeterEventAdjustmentsWithHttpInfo(String eventName, String type, { EventAdjustmentCancelSettingsParam? cancel, List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/billing/meter_event_adjustments';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (cancel != null) {
      formParams[r'cancel'] = parameterToString(cancel);
    }
    if (eventName != null) {
      formParams[r'event_name'] = parameterToString(eventName);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (type != null) {
      formParams[r'type'] = parameterToString(type);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a billing meter event adjustment
  ///
  /// <p>Creates a billing meter event adjustment.</p>
  ///
  /// Parameters:
  ///
  /// * [String] eventName (required):
  ///   The name of the meter event. Corresponds with the `event_name` field on a meter.
  ///
  /// * [String] type (required):
  ///   Specifies whether to cancel a single event or a range of events for a time period. Time period cancellation is not supported yet.
  ///
  /// * [EventAdjustmentCancelSettingsParam] cancel:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<BillingMeterEventAdjustment?> postBillingMeterEventAdjustments(String eventName, String type, { EventAdjustmentCancelSettingsParam? cancel, List<String>? expand, }) async {
    final response = await postBillingMeterEventAdjustmentsWithHttpInfo(eventName, type,  cancel: cancel, expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BillingMeterEventAdjustment',) as BillingMeterEventAdjustment;
    
    }
    return null;
  }

  /// Create a billing meter event
  ///
  /// <p>Creates a billing meter event.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] eventName (required):
  ///   The name of the meter event. Corresponds with the `event_name` field on a meter.
  ///
  /// * [Map<String, String>] payload (required):
  ///   The payload of the event. This must contain the fields corresponding to a meter's `customer_mapping.event_payload_key` (default is `stripe_customer_id`) and `value_settings.event_payload_key` (default is `value`). Read more about the [payload](https://docs.stripe.com/billing/subscriptions/usage-based/recording-usage#payload-key-overrides).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] identifier:
  ///   A unique identifier for the event. If not provided, one is generated. We recommend using UUID-like identifiers. We will enforce uniqueness within a rolling period of at least 24 hours. The enforcement of uniqueness primarily addresses issues arising from accidental retries or other problems occurring within extremely brief time intervals. This approach helps prevent duplicate entries and ensures data integrity in high-frequency operations.
  ///
  /// * [int] timestamp:
  ///   The time of the event. Measured in seconds since the Unix epoch. Must be within the past 35 calendar days or up to 5 minutes in the future. Defaults to current timestamp if not specified.
  Future<Response> postBillingMeterEventsWithHttpInfo(String eventName, Map<String, String> payload, { List<String>? expand, String? identifier, int? timestamp, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/billing/meter_events';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (eventName != null) {
      formParams[r'event_name'] = parameterToString(eventName);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (identifier != null) {
      formParams[r'identifier'] = parameterToString(identifier);
    }
    if (payload != null) {
      formParams[r'payload'] = parameterToString(payload);
    }
    if (timestamp != null) {
      formParams[r'timestamp'] = parameterToString(timestamp);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a billing meter event
  ///
  /// <p>Creates a billing meter event.</p>
  ///
  /// Parameters:
  ///
  /// * [String] eventName (required):
  ///   The name of the meter event. Corresponds with the `event_name` field on a meter.
  ///
  /// * [Map<String, String>] payload (required):
  ///   The payload of the event. This must contain the fields corresponding to a meter's `customer_mapping.event_payload_key` (default is `stripe_customer_id`) and `value_settings.event_payload_key` (default is `value`). Read more about the [payload](https://docs.stripe.com/billing/subscriptions/usage-based/recording-usage#payload-key-overrides).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] identifier:
  ///   A unique identifier for the event. If not provided, one is generated. We recommend using UUID-like identifiers. We will enforce uniqueness within a rolling period of at least 24 hours. The enforcement of uniqueness primarily addresses issues arising from accidental retries or other problems occurring within extremely brief time intervals. This approach helps prevent duplicate entries and ensures data integrity in high-frequency operations.
  ///
  /// * [int] timestamp:
  ///   The time of the event. Measured in seconds since the Unix epoch. Must be within the past 35 calendar days or up to 5 minutes in the future. Defaults to current timestamp if not specified.
  Future<BillingMeterEvent?> postBillingMeterEvents(String eventName, Map<String, String> payload, { List<String>? expand, String? identifier, int? timestamp, }) async {
    final response = await postBillingMeterEventsWithHttpInfo(eventName, payload,  expand: expand, identifier: identifier, timestamp: timestamp, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BillingMeterEvent',) as BillingMeterEvent;
    
    }
    return null;
  }

  /// Create a billing meter
  ///
  /// <p>Creates a billing meter.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [AggregationSettingsParam] defaultAggregation (required):
  ///
  /// * [String] displayName (required):
  ///   The meter’s name. Not visible to the customer.
  ///
  /// * [String] eventName (required):
  ///   The name of the meter event to record usage for. Corresponds with the `event_name` field on meter events.
  ///
  /// * [CustomerMappingParam] customerMapping:
  ///
  /// * [String] eventTimeWindow:
  ///   The time window to pre-aggregate meter events for, if any.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [MeterValueSettingsParam] valueSettings:
  Future<Response> postBillingMetersWithHttpInfo(AggregationSettingsParam defaultAggregation, String displayName, String eventName, { CustomerMappingParam? customerMapping, String? eventTimeWindow, List<String>? expand, MeterValueSettingsParam? valueSettings, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/billing/meters';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (customerMapping != null) {
      formParams[r'customer_mapping'] = parameterToString(customerMapping);
    }
    if (defaultAggregation != null) {
      formParams[r'default_aggregation'] = parameterToString(defaultAggregation);
    }
    if (displayName != null) {
      formParams[r'display_name'] = parameterToString(displayName);
    }
    if (eventName != null) {
      formParams[r'event_name'] = parameterToString(eventName);
    }
    if (eventTimeWindow != null) {
      formParams[r'event_time_window'] = parameterToString(eventTimeWindow);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (valueSettings != null) {
      formParams[r'value_settings'] = parameterToString(valueSettings);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a billing meter
  ///
  /// <p>Creates a billing meter.</p>
  ///
  /// Parameters:
  ///
  /// * [AggregationSettingsParam] defaultAggregation (required):
  ///
  /// * [String] displayName (required):
  ///   The meter’s name. Not visible to the customer.
  ///
  /// * [String] eventName (required):
  ///   The name of the meter event to record usage for. Corresponds with the `event_name` field on meter events.
  ///
  /// * [CustomerMappingParam] customerMapping:
  ///
  /// * [String] eventTimeWindow:
  ///   The time window to pre-aggregate meter events for, if any.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [MeterValueSettingsParam] valueSettings:
  Future<BillingMeter?> postBillingMeters(AggregationSettingsParam defaultAggregation, String displayName, String eventName, { CustomerMappingParam? customerMapping, String? eventTimeWindow, List<String>? expand, MeterValueSettingsParam? valueSettings, }) async {
    final response = await postBillingMetersWithHttpInfo(defaultAggregation, displayName, eventName,  customerMapping: customerMapping, eventTimeWindow: eventTimeWindow, expand: expand, valueSettings: valueSettings, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BillingMeter',) as BillingMeter;
    
    }
    return null;
  }

  /// Update a billing meter
  ///
  /// <p>Updates a billing meter.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///   Unique identifier for the object.
  ///
  /// * [String] displayName:
  ///   The meter’s name. Not visible to the customer.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postBillingMetersIdWithHttpInfo(String id, { String? displayName, List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/billing/meters/{id}'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (displayName != null) {
      formParams[r'display_name'] = parameterToString(displayName);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a billing meter
  ///
  /// <p>Updates a billing meter.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///   Unique identifier for the object.
  ///
  /// * [String] displayName:
  ///   The meter’s name. Not visible to the customer.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<BillingMeter?> postBillingMetersId(String id, { String? displayName, List<String>? expand, }) async {
    final response = await postBillingMetersIdWithHttpInfo(id,  displayName: displayName, expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BillingMeter',) as BillingMeter;
    
    }
    return null;
  }

  /// Deactivate a billing meter
  ///
  /// <p>When a meter is deactivated, no more meter events will be accepted for this meter. You can’t attach a deactivated meter to a price.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///   Unique identifier for the object.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postBillingMetersIdDeactivateWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/billing/meters/{id}/deactivate'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Deactivate a billing meter
  ///
  /// <p>When a meter is deactivated, no more meter events will be accepted for this meter. You can’t attach a deactivated meter to a price.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///   Unique identifier for the object.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<BillingMeter?> postBillingMetersIdDeactivate(String id, { List<String>? expand, }) async {
    final response = await postBillingMetersIdDeactivateWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BillingMeter',) as BillingMeter;
    
    }
    return null;
  }

  /// Reactivate a billing meter
  ///
  /// <p>When a meter is reactivated, events for this meter can be accepted and you can attach the meter to a price.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///   Unique identifier for the object.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postBillingMetersIdReactivateWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/billing/meters/{id}/reactivate'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Reactivate a billing meter
  ///
  /// <p>When a meter is reactivated, events for this meter can be accepted and you can attach the meter to a price.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///   Unique identifier for the object.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<BillingMeter?> postBillingMetersIdReactivate(String id, { List<String>? expand, }) async {
    final response = await postBillingMetersIdReactivateWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BillingMeter',) as BillingMeter;
    
    }
    return null;
  }

  /// Create a portal configuration
  ///
  /// <p>Creates a configuration that describes the functionality and behavior of a PortalSession</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [FeaturesCreationParam] features (required):
  ///
  /// * [BusinessProfileCreateParam] businessProfile:
  ///
  /// * [PostBillingPortalConfigurationsRequestDefaultReturnUrl] defaultReturnUrl:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [LoginPageCreateParam] loginPage:
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  Future<Response> postBillingPortalConfigurationsWithHttpInfo(FeaturesCreationParam features, { BusinessProfileCreateParam? businessProfile, PostBillingPortalConfigurationsRequestDefaultReturnUrl? defaultReturnUrl, List<String>? expand, LoginPageCreateParam? loginPage, Map<String, String>? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/billing_portal/configurations';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (businessProfile != null) {
      formParams[r'business_profile'] = parameterToString(businessProfile);
    }
    if (defaultReturnUrl != null) {
      formParams[r'default_return_url'] = parameterToString(defaultReturnUrl);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (features != null) {
      formParams[r'features'] = parameterToString(features);
    }
    if (loginPage != null) {
      formParams[r'login_page'] = parameterToString(loginPage);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a portal configuration
  ///
  /// <p>Creates a configuration that describes the functionality and behavior of a PortalSession</p>
  ///
  /// Parameters:
  ///
  /// * [FeaturesCreationParam] features (required):
  ///
  /// * [BusinessProfileCreateParam] businessProfile:
  ///
  /// * [PostBillingPortalConfigurationsRequestDefaultReturnUrl] defaultReturnUrl:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [LoginPageCreateParam] loginPage:
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  Future<BillingPortalConfiguration?> postBillingPortalConfigurations(FeaturesCreationParam features, { BusinessProfileCreateParam? businessProfile, PostBillingPortalConfigurationsRequestDefaultReturnUrl? defaultReturnUrl, List<String>? expand, LoginPageCreateParam? loginPage, Map<String, String>? metadata, }) async {
    final response = await postBillingPortalConfigurationsWithHttpInfo(features,  businessProfile: businessProfile, defaultReturnUrl: defaultReturnUrl, expand: expand, loginPage: loginPage, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BillingPortalConfiguration',) as BillingPortalConfiguration;
    
    }
    return null;
  }

  /// Update a portal configuration
  ///
  /// <p>Updates a configuration that describes the functionality of the customer portal.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] configuration (required):
  ///
  /// * [bool] active:
  ///   Whether the configuration is active and can be used to create portal sessions.
  ///
  /// * [BusinessProfileUpdateParam] businessProfile:
  ///
  /// * [PostBillingPortalConfigurationsRequestDefaultReturnUrl] defaultReturnUrl:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [FeaturesUpdatingParam] features:
  ///
  /// * [LoginPageUpdateParam] loginPage:
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<Response> postBillingPortalConfigurationsConfigurationWithHttpInfo(String configuration, { bool? active, BusinessProfileUpdateParam? businessProfile, PostBillingPortalConfigurationsRequestDefaultReturnUrl? defaultReturnUrl, List<String>? expand, FeaturesUpdatingParam? features, LoginPageUpdateParam? loginPage, PostAccountsRequestMetadata? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/billing_portal/configurations/{configuration}'
      .replaceAll('{configuration}', configuration);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (active != null) {
      formParams[r'active'] = parameterToString(active);
    }
    if (businessProfile != null) {
      formParams[r'business_profile'] = parameterToString(businessProfile);
    }
    if (defaultReturnUrl != null) {
      formParams[r'default_return_url'] = parameterToString(defaultReturnUrl);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (features != null) {
      formParams[r'features'] = parameterToString(features);
    }
    if (loginPage != null) {
      formParams[r'login_page'] = parameterToString(loginPage);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a portal configuration
  ///
  /// <p>Updates a configuration that describes the functionality of the customer portal.</p>
  ///
  /// Parameters:
  ///
  /// * [String] configuration (required):
  ///
  /// * [bool] active:
  ///   Whether the configuration is active and can be used to create portal sessions.
  ///
  /// * [BusinessProfileUpdateParam] businessProfile:
  ///
  /// * [PostBillingPortalConfigurationsRequestDefaultReturnUrl] defaultReturnUrl:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [FeaturesUpdatingParam] features:
  ///
  /// * [LoginPageUpdateParam] loginPage:
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<BillingPortalConfiguration?> postBillingPortalConfigurationsConfiguration(String configuration, { bool? active, BusinessProfileUpdateParam? businessProfile, PostBillingPortalConfigurationsRequestDefaultReturnUrl? defaultReturnUrl, List<String>? expand, FeaturesUpdatingParam? features, LoginPageUpdateParam? loginPage, PostAccountsRequestMetadata? metadata, }) async {
    final response = await postBillingPortalConfigurationsConfigurationWithHttpInfo(configuration,  active: active, businessProfile: businessProfile, defaultReturnUrl: defaultReturnUrl, expand: expand, features: features, loginPage: loginPage, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BillingPortalConfiguration',) as BillingPortalConfiguration;
    
    }
    return null;
  }

  /// Create a portal session
  ///
  /// <p>Creates a session of the customer portal.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///   The ID of an existing customer.
  ///
  /// * [String] configuration:
  ///   The ID of an existing [configuration](https://stripe.com/docs/api/customer_portal/configuration) to use for this session, describing its functionality and features. If not specified, the session uses the default configuration.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [FlowDataParam] flowData:
  ///
  /// * [String] locale:
  ///   The IETF language tag of the locale customer portal is displayed in. If blank or auto, the customer’s `preferred_locales` or browser’s locale is used.
  ///
  /// * [String] onBehalfOf:
  ///   The `on_behalf_of` account to use for this session. When specified, only subscriptions and invoices with this `on_behalf_of` account appear in the portal. For more information, see the [docs](https://stripe.com/docs/connect/separate-charges-and-transfers#settlement-merchant). Use the [Accounts API](https://stripe.com/docs/api/accounts/object#account_object-settings-branding) to modify the `on_behalf_of` account's branding settings, which the portal displays.
  ///
  /// * [String] returnUrl:
  ///   The default URL to redirect customers to when they click on the portal's link to return to your website.
  Future<Response> postBillingPortalSessionsWithHttpInfo(String customer, { String? configuration, List<String>? expand, FlowDataParam? flowData, String? locale, String? onBehalfOf, String? returnUrl, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/billing_portal/sessions';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (configuration != null) {
      formParams[r'configuration'] = parameterToString(configuration);
    }
    if (customer != null) {
      formParams[r'customer'] = parameterToString(customer);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (flowData != null) {
      formParams[r'flow_data'] = parameterToString(flowData);
    }
    if (locale != null) {
      formParams[r'locale'] = parameterToString(locale);
    }
    if (onBehalfOf != null) {
      formParams[r'on_behalf_of'] = parameterToString(onBehalfOf);
    }
    if (returnUrl != null) {
      formParams[r'return_url'] = parameterToString(returnUrl);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a portal session
  ///
  /// <p>Creates a session of the customer portal.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///   The ID of an existing customer.
  ///
  /// * [String] configuration:
  ///   The ID of an existing [configuration](https://stripe.com/docs/api/customer_portal/configuration) to use for this session, describing its functionality and features. If not specified, the session uses the default configuration.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [FlowDataParam] flowData:
  ///
  /// * [String] locale:
  ///   The IETF language tag of the locale customer portal is displayed in. If blank or auto, the customer’s `preferred_locales` or browser’s locale is used.
  ///
  /// * [String] onBehalfOf:
  ///   The `on_behalf_of` account to use for this session. When specified, only subscriptions and invoices with this `on_behalf_of` account appear in the portal. For more information, see the [docs](https://stripe.com/docs/connect/separate-charges-and-transfers#settlement-merchant). Use the [Accounts API](https://stripe.com/docs/api/accounts/object#account_object-settings-branding) to modify the `on_behalf_of` account's branding settings, which the portal displays.
  ///
  /// * [String] returnUrl:
  ///   The default URL to redirect customers to when they click on the portal's link to return to your website.
  Future<BillingPortalSession?> postBillingPortalSessions(String customer, { String? configuration, List<String>? expand, FlowDataParam? flowData, String? locale, String? onBehalfOf, String? returnUrl, }) async {
    final response = await postBillingPortalSessionsWithHttpInfo(customer,  configuration: configuration, expand: expand, flowData: flowData, locale: locale, onBehalfOf: onBehalfOf, returnUrl: returnUrl, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BillingPortalSession',) as BillingPortalSession;
    
    }
    return null;
  }

  /// <p>This method is no longer recommended—use the <a href=\"/docs/api/payment_intents\">Payment Intents API</a> to initiate a new payment instead. Confirmation of the PaymentIntent creates the <code>Charge</code> object used to request payment.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [int] amount:
  ///   Amount intended to be collected by this payment. A positive integer representing how much to charge in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal) (e.g., 100 cents to charge $1.00 or 100 to charge ¥100, a zero-decimal currency). The minimum amount is $0.50 US or [equivalent in charge currency](https://stripe.com/docs/currencies#minimum-and-maximum-charge-amounts). The amount value supports up to eight digits (e.g., a value of 99999999 for a USD charge of $999,999.99).
  ///
  /// * [int] applicationFee:
  ///
  /// * [int] applicationFeeAmount:
  ///   A fee in cents (or local equivalent) that will be applied to the charge and transferred to the application owner's Stripe account. The request must be made with an OAuth key or the `Stripe-Account` header in order to take an application fee. For more information, see the application fees [documentation](https://stripe.com/docs/connect/direct-charges#collect-fees).
  ///
  /// * [bool] capture:
  ///   Whether to immediately capture the charge. Defaults to `true`. When `false`, the charge issues an authorization (or pre-authorization), and will need to be [captured](https://stripe.com/docs/api#capture_charge) later. Uncaptured charges expire after a set number of days (7 by default). For more information, see the [authorizing charges and settling later](https://stripe.com/docs/charges/placing-a-hold) documentation.
  ///
  /// * [PostChargesRequestCard] card:
  ///
  /// * [String] currency:
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] customer:
  ///   The ID of an existing customer that will be charged in this request.
  ///
  /// * [String] description:
  ///   An arbitrary string which you can attach to a `Charge` object. It is displayed when in the web interface alongside the charge. Note that if you use Stripe to send automatic email receipts to your customers, your receipt emails will include the `description` of the charge(s) that they are describing.
  ///
  /// * [PostChargesRequestDestination] destination:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] onBehalfOf:
  ///   The Stripe account ID for which these funds are intended. Automatically set if you use the `destination` parameter. For details, see [Creating Separate Charges and Transfers](https://stripe.com/docs/connect/separate-charges-and-transfers#settlement-merchant).
  ///
  /// * [RadarOptionsWithHiddenOptions] radarOptions:
  ///
  /// * [String] receiptEmail:
  ///   The email address to which this charge's [receipt](https://stripe.com/docs/dashboard/receipts) will be sent. The receipt will not be sent until the charge is paid, and no receipts will be sent for test mode charges. If this charge is for a [Customer](https://stripe.com/docs/api/customers/object), the email address specified here will override the customer's email address. If `receipt_email` is specified for a charge in live mode, a receipt will be sent regardless of your [email settings](https://dashboard.stripe.com/account/emails).
  ///
  /// * [OptionalFieldsShipping] shipping:
  ///
  /// * [String] source_:
  ///   A payment source to be charged. This can be the ID of a [card](https://stripe.com/docs/api#cards) (i.e., credit or debit card), a [bank account](https://stripe.com/docs/api#bank_accounts), a [source](https://stripe.com/docs/api#sources), a [token](https://stripe.com/docs/api#tokens), or a [connected account](https://stripe.com/docs/connect/account-debits#charging-a-connected-account). For certain sources---namely, [cards](https://stripe.com/docs/api#cards), [bank accounts](https://stripe.com/docs/api#bank_accounts), and attached [sources](https://stripe.com/docs/api#sources)---you must also pass the ID of the associated customer.
  ///
  /// * [String] statementDescriptor:
  ///   For a non-card charge, text that appears on the customer's statement as the statement descriptor. This value overrides the account's default statement descriptor. For information about requirements, including the 22-character limit, see [the Statement Descriptor docs](https://docs.stripe.com/get-started/account/statement-descriptors).  For a card charge, this value is ignored unless you don't specify a `statement_descriptor_suffix`, in which case this value is used as the suffix.
  ///
  /// * [String] statementDescriptorSuffix:
  ///   Provides information about a card charge. Concatenated to the account's [statement descriptor prefix](https://docs.stripe.com/get-started/account/statement-descriptors#static) to form the complete statement descriptor that appears on the customer's statement. If the account has no prefix value, the suffix is concatenated to the account's statement descriptor.
  ///
  /// * [TransferDataSpecs] transferData:
  ///
  /// * [String] transferGroup:
  ///   A string that identifies this transaction as part of a group. For details, see [Grouping transactions](https://stripe.com/docs/connect/separate-charges-and-transfers#transfer-options).
  Future<Response> postChargesWithHttpInfo({ int? amount, int? applicationFee, int? applicationFeeAmount, bool? capture, PostChargesRequestCard? card, String? currency, String? customer, String? description, PostChargesRequestDestination? destination, List<String>? expand, PostAccountsRequestMetadata? metadata, String? onBehalfOf, RadarOptionsWithHiddenOptions? radarOptions, String? receiptEmail, OptionalFieldsShipping? shipping, String? source_, String? statementDescriptor, String? statementDescriptorSuffix, TransferDataSpecs? transferData, String? transferGroup, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/charges';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (applicationFee != null) {
      formParams[r'application_fee'] = parameterToString(applicationFee);
    }
    if (applicationFeeAmount != null) {
      formParams[r'application_fee_amount'] = parameterToString(applicationFeeAmount);
    }
    if (capture != null) {
      formParams[r'capture'] = parameterToString(capture);
    }
    if (card != null) {
      formParams[r'card'] = parameterToString(card);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (customer != null) {
      formParams[r'customer'] = parameterToString(customer);
    }
    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (destination != null) {
      formParams[r'destination'] = parameterToString(destination);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (onBehalfOf != null) {
      formParams[r'on_behalf_of'] = parameterToString(onBehalfOf);
    }
    if (radarOptions != null) {
      formParams[r'radar_options'] = parameterToString(radarOptions);
    }
    if (receiptEmail != null) {
      formParams[r'receipt_email'] = parameterToString(receiptEmail);
    }
    if (shipping != null) {
      formParams[r'shipping'] = parameterToString(shipping);
    }
    if (source_ != null) {
      formParams[r'source'] = parameterToString(source_);
    }
    if (statementDescriptor != null) {
      formParams[r'statement_descriptor'] = parameterToString(statementDescriptor);
    }
    if (statementDescriptorSuffix != null) {
      formParams[r'statement_descriptor_suffix'] = parameterToString(statementDescriptorSuffix);
    }
    if (transferData != null) {
      formParams[r'transfer_data'] = parameterToString(transferData);
    }
    if (transferGroup != null) {
      formParams[r'transfer_group'] = parameterToString(transferGroup);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// <p>This method is no longer recommended—use the <a href=\"/docs/api/payment_intents\">Payment Intents API</a> to initiate a new payment instead. Confirmation of the PaymentIntent creates the <code>Charge</code> object used to request payment.</p>
  ///
  /// Parameters:
  ///
  /// * [int] amount:
  ///   Amount intended to be collected by this payment. A positive integer representing how much to charge in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal) (e.g., 100 cents to charge $1.00 or 100 to charge ¥100, a zero-decimal currency). The minimum amount is $0.50 US or [equivalent in charge currency](https://stripe.com/docs/currencies#minimum-and-maximum-charge-amounts). The amount value supports up to eight digits (e.g., a value of 99999999 for a USD charge of $999,999.99).
  ///
  /// * [int] applicationFee:
  ///
  /// * [int] applicationFeeAmount:
  ///   A fee in cents (or local equivalent) that will be applied to the charge and transferred to the application owner's Stripe account. The request must be made with an OAuth key or the `Stripe-Account` header in order to take an application fee. For more information, see the application fees [documentation](https://stripe.com/docs/connect/direct-charges#collect-fees).
  ///
  /// * [bool] capture:
  ///   Whether to immediately capture the charge. Defaults to `true`. When `false`, the charge issues an authorization (or pre-authorization), and will need to be [captured](https://stripe.com/docs/api#capture_charge) later. Uncaptured charges expire after a set number of days (7 by default). For more information, see the [authorizing charges and settling later](https://stripe.com/docs/charges/placing-a-hold) documentation.
  ///
  /// * [PostChargesRequestCard] card:
  ///
  /// * [String] currency:
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] customer:
  ///   The ID of an existing customer that will be charged in this request.
  ///
  /// * [String] description:
  ///   An arbitrary string which you can attach to a `Charge` object. It is displayed when in the web interface alongside the charge. Note that if you use Stripe to send automatic email receipts to your customers, your receipt emails will include the `description` of the charge(s) that they are describing.
  ///
  /// * [PostChargesRequestDestination] destination:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] onBehalfOf:
  ///   The Stripe account ID for which these funds are intended. Automatically set if you use the `destination` parameter. For details, see [Creating Separate Charges and Transfers](https://stripe.com/docs/connect/separate-charges-and-transfers#settlement-merchant).
  ///
  /// * [RadarOptionsWithHiddenOptions] radarOptions:
  ///
  /// * [String] receiptEmail:
  ///   The email address to which this charge's [receipt](https://stripe.com/docs/dashboard/receipts) will be sent. The receipt will not be sent until the charge is paid, and no receipts will be sent for test mode charges. If this charge is for a [Customer](https://stripe.com/docs/api/customers/object), the email address specified here will override the customer's email address. If `receipt_email` is specified for a charge in live mode, a receipt will be sent regardless of your [email settings](https://dashboard.stripe.com/account/emails).
  ///
  /// * [OptionalFieldsShipping] shipping:
  ///
  /// * [String] source_:
  ///   A payment source to be charged. This can be the ID of a [card](https://stripe.com/docs/api#cards) (i.e., credit or debit card), a [bank account](https://stripe.com/docs/api#bank_accounts), a [source](https://stripe.com/docs/api#sources), a [token](https://stripe.com/docs/api#tokens), or a [connected account](https://stripe.com/docs/connect/account-debits#charging-a-connected-account). For certain sources---namely, [cards](https://stripe.com/docs/api#cards), [bank accounts](https://stripe.com/docs/api#bank_accounts), and attached [sources](https://stripe.com/docs/api#sources)---you must also pass the ID of the associated customer.
  ///
  /// * [String] statementDescriptor:
  ///   For a non-card charge, text that appears on the customer's statement as the statement descriptor. This value overrides the account's default statement descriptor. For information about requirements, including the 22-character limit, see [the Statement Descriptor docs](https://docs.stripe.com/get-started/account/statement-descriptors).  For a card charge, this value is ignored unless you don't specify a `statement_descriptor_suffix`, in which case this value is used as the suffix.
  ///
  /// * [String] statementDescriptorSuffix:
  ///   Provides information about a card charge. Concatenated to the account's [statement descriptor prefix](https://docs.stripe.com/get-started/account/statement-descriptors#static) to form the complete statement descriptor that appears on the customer's statement. If the account has no prefix value, the suffix is concatenated to the account's statement descriptor.
  ///
  /// * [TransferDataSpecs] transferData:
  ///
  /// * [String] transferGroup:
  ///   A string that identifies this transaction as part of a group. For details, see [Grouping transactions](https://stripe.com/docs/connect/separate-charges-and-transfers#transfer-options).
  Future<Charge?> postCharges({ int? amount, int? applicationFee, int? applicationFeeAmount, bool? capture, PostChargesRequestCard? card, String? currency, String? customer, String? description, PostChargesRequestDestination? destination, List<String>? expand, PostAccountsRequestMetadata? metadata, String? onBehalfOf, RadarOptionsWithHiddenOptions? radarOptions, String? receiptEmail, OptionalFieldsShipping? shipping, String? source_, String? statementDescriptor, String? statementDescriptorSuffix, TransferDataSpecs? transferData, String? transferGroup, }) async {
    final response = await postChargesWithHttpInfo( amount: amount, applicationFee: applicationFee, applicationFeeAmount: applicationFeeAmount, capture: capture, card: card, currency: currency, customer: customer, description: description, destination: destination, expand: expand, metadata: metadata, onBehalfOf: onBehalfOf, radarOptions: radarOptions, receiptEmail: receiptEmail, shipping: shipping, source_: source_, statementDescriptor: statementDescriptor, statementDescriptorSuffix: statementDescriptorSuffix, transferData: transferData, transferGroup: transferGroup, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Charge',) as Charge;
    
    }
    return null;
  }

  /// Update a charge
  ///
  /// <p>Updates the specified charge by setting the values of the parameters passed. Any parameters not provided will be left unchanged.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] charge (required):
  ///
  /// * [String] customer:
  ///   The ID of an existing customer that will be associated with this request. This field may only be updated if there is no existing associated customer with this charge.
  ///
  /// * [String] description:
  ///   An arbitrary string which you can attach to a charge object. It is displayed when in the web interface alongside the charge. Note that if you use Stripe to send automatic email receipts to your customers, your receipt emails will include the `description` of the charge(s) that they are describing.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [FraudDetails] fraudDetails:
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] receiptEmail:
  ///   This is the email address that the receipt for this charge will be sent to. If this field is updated, then a new email receipt will be sent to the updated address.
  ///
  /// * [OptionalFieldsShipping] shipping:
  ///
  /// * [String] transferGroup:
  ///   A string that identifies this transaction as part of a group. `transfer_group` may only be provided if it has not been set. See the [Connect documentation](https://stripe.com/docs/connect/separate-charges-and-transfers#transfer-options) for details.
  Future<Response> postChargesChargeWithHttpInfo(String charge, { String? customer, String? description, List<String>? expand, FraudDetails? fraudDetails, PostAccountsRequestMetadata? metadata, String? receiptEmail, OptionalFieldsShipping? shipping, String? transferGroup, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/charges/{charge}'
      .replaceAll('{charge}', charge);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (customer != null) {
      formParams[r'customer'] = parameterToString(customer);
    }
    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (fraudDetails != null) {
      formParams[r'fraud_details'] = parameterToString(fraudDetails);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (receiptEmail != null) {
      formParams[r'receipt_email'] = parameterToString(receiptEmail);
    }
    if (shipping != null) {
      formParams[r'shipping'] = parameterToString(shipping);
    }
    if (transferGroup != null) {
      formParams[r'transfer_group'] = parameterToString(transferGroup);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a charge
  ///
  /// <p>Updates the specified charge by setting the values of the parameters passed. Any parameters not provided will be left unchanged.</p>
  ///
  /// Parameters:
  ///
  /// * [String] charge (required):
  ///
  /// * [String] customer:
  ///   The ID of an existing customer that will be associated with this request. This field may only be updated if there is no existing associated customer with this charge.
  ///
  /// * [String] description:
  ///   An arbitrary string which you can attach to a charge object. It is displayed when in the web interface alongside the charge. Note that if you use Stripe to send automatic email receipts to your customers, your receipt emails will include the `description` of the charge(s) that they are describing.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [FraudDetails] fraudDetails:
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] receiptEmail:
  ///   This is the email address that the receipt for this charge will be sent to. If this field is updated, then a new email receipt will be sent to the updated address.
  ///
  /// * [OptionalFieldsShipping] shipping:
  ///
  /// * [String] transferGroup:
  ///   A string that identifies this transaction as part of a group. `transfer_group` may only be provided if it has not been set. See the [Connect documentation](https://stripe.com/docs/connect/separate-charges-and-transfers#transfer-options) for details.
  Future<Charge?> postChargesCharge(String charge, { String? customer, String? description, List<String>? expand, FraudDetails? fraudDetails, PostAccountsRequestMetadata? metadata, String? receiptEmail, OptionalFieldsShipping? shipping, String? transferGroup, }) async {
    final response = await postChargesChargeWithHttpInfo(charge,  customer: customer, description: description, expand: expand, fraudDetails: fraudDetails, metadata: metadata, receiptEmail: receiptEmail, shipping: shipping, transferGroup: transferGroup, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Charge',) as Charge;
    
    }
    return null;
  }

  /// Capture a payment
  ///
  /// <p>Capture the payment of an existing, uncaptured charge that was created with the <code>capture</code> option set to false.</p>  <p>Uncaptured payments expire a set number of days after they are created (<a href=\"/docs/charges/placing-a-hold\">7 by default</a>), after which they are marked as refunded and capture attempts will fail.</p>  <p>Don’t use this method to capture a PaymentIntent-initiated charge. Use <a href=\"/docs/api/payment_intents/capture\">Capture a PaymentIntent</a>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] charge (required):
  ///
  /// * [int] amount:
  ///   The amount to capture, which must be less than or equal to the original amount.
  ///
  /// * [int] applicationFee:
  ///   An application fee to add on to this charge.
  ///
  /// * [int] applicationFeeAmount:
  ///   An application fee amount to add on to this charge, which must be less than or equal to the original amount.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] receiptEmail:
  ///   The email address to send this charge's receipt to. This will override the previously-specified email address for this charge, if one was set. Receipts will not be sent in test mode.
  ///
  /// * [String] statementDescriptor:
  ///   For a non-card charge, text that appears on the customer's statement as the statement descriptor. This value overrides the account's default statement descriptor. For information about requirements, including the 22-character limit, see [the Statement Descriptor docs](https://docs.stripe.com/get-started/account/statement-descriptors).  For a card charge, this value is ignored unless you don't specify a `statement_descriptor_suffix`, in which case this value is used as the suffix.
  ///
  /// * [String] statementDescriptorSuffix:
  ///   Provides information about a card charge. Concatenated to the account's [statement descriptor prefix](https://docs.stripe.com/get-started/account/statement-descriptors#static) to form the complete statement descriptor that appears on the customer's statement. If the account has no prefix value, the suffix is concatenated to the account's statement descriptor.
  ///
  /// * [TransferDataSpecs1] transferData:
  ///
  /// * [String] transferGroup:
  ///   A string that identifies this transaction as part of a group. `transfer_group` may only be provided if it has not been set. See the [Connect documentation](https://stripe.com/docs/connect/separate-charges-and-transfers#transfer-options) for details.
  Future<Response> postChargesChargeCaptureWithHttpInfo(String charge, { int? amount, int? applicationFee, int? applicationFeeAmount, List<String>? expand, String? receiptEmail, String? statementDescriptor, String? statementDescriptorSuffix, TransferDataSpecs1? transferData, String? transferGroup, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/charges/{charge}/capture'
      .replaceAll('{charge}', charge);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (applicationFee != null) {
      formParams[r'application_fee'] = parameterToString(applicationFee);
    }
    if (applicationFeeAmount != null) {
      formParams[r'application_fee_amount'] = parameterToString(applicationFeeAmount);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (receiptEmail != null) {
      formParams[r'receipt_email'] = parameterToString(receiptEmail);
    }
    if (statementDescriptor != null) {
      formParams[r'statement_descriptor'] = parameterToString(statementDescriptor);
    }
    if (statementDescriptorSuffix != null) {
      formParams[r'statement_descriptor_suffix'] = parameterToString(statementDescriptorSuffix);
    }
    if (transferData != null) {
      formParams[r'transfer_data'] = parameterToString(transferData);
    }
    if (transferGroup != null) {
      formParams[r'transfer_group'] = parameterToString(transferGroup);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Capture a payment
  ///
  /// <p>Capture the payment of an existing, uncaptured charge that was created with the <code>capture</code> option set to false.</p>  <p>Uncaptured payments expire a set number of days after they are created (<a href=\"/docs/charges/placing-a-hold\">7 by default</a>), after which they are marked as refunded and capture attempts will fail.</p>  <p>Don’t use this method to capture a PaymentIntent-initiated charge. Use <a href=\"/docs/api/payment_intents/capture\">Capture a PaymentIntent</a>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] charge (required):
  ///
  /// * [int] amount:
  ///   The amount to capture, which must be less than or equal to the original amount.
  ///
  /// * [int] applicationFee:
  ///   An application fee to add on to this charge.
  ///
  /// * [int] applicationFeeAmount:
  ///   An application fee amount to add on to this charge, which must be less than or equal to the original amount.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] receiptEmail:
  ///   The email address to send this charge's receipt to. This will override the previously-specified email address for this charge, if one was set. Receipts will not be sent in test mode.
  ///
  /// * [String] statementDescriptor:
  ///   For a non-card charge, text that appears on the customer's statement as the statement descriptor. This value overrides the account's default statement descriptor. For information about requirements, including the 22-character limit, see [the Statement Descriptor docs](https://docs.stripe.com/get-started/account/statement-descriptors).  For a card charge, this value is ignored unless you don't specify a `statement_descriptor_suffix`, in which case this value is used as the suffix.
  ///
  /// * [String] statementDescriptorSuffix:
  ///   Provides information about a card charge. Concatenated to the account's [statement descriptor prefix](https://docs.stripe.com/get-started/account/statement-descriptors#static) to form the complete statement descriptor that appears on the customer's statement. If the account has no prefix value, the suffix is concatenated to the account's statement descriptor.
  ///
  /// * [TransferDataSpecs1] transferData:
  ///
  /// * [String] transferGroup:
  ///   A string that identifies this transaction as part of a group. `transfer_group` may only be provided if it has not been set. See the [Connect documentation](https://stripe.com/docs/connect/separate-charges-and-transfers#transfer-options) for details.
  Future<Charge?> postChargesChargeCapture(String charge, { int? amount, int? applicationFee, int? applicationFeeAmount, List<String>? expand, String? receiptEmail, String? statementDescriptor, String? statementDescriptorSuffix, TransferDataSpecs1? transferData, String? transferGroup, }) async {
    final response = await postChargesChargeCaptureWithHttpInfo(charge,  amount: amount, applicationFee: applicationFee, applicationFeeAmount: applicationFeeAmount, expand: expand, receiptEmail: receiptEmail, statementDescriptor: statementDescriptor, statementDescriptorSuffix: statementDescriptorSuffix, transferData: transferData, transferGroup: transferGroup, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Charge',) as Charge;
    
    }
    return null;
  }

  /// 
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] charge (required):
  ///
  /// * [DisputeEvidenceParams] evidence:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [bool] submit:
  ///   Whether to immediately submit evidence to the bank. If `false`, evidence is staged on the dispute. Staged evidence is visible in the API and Dashboard, and can be submitted to the bank by making another request with this attribute set to `true` (the default).
  Future<Response> postChargesChargeDisputeWithHttpInfo(String charge, { DisputeEvidenceParams? evidence, List<String>? expand, PostAccountsRequestMetadata? metadata, bool? submit, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/charges/{charge}/dispute'
      .replaceAll('{charge}', charge);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (evidence != null) {
      formParams[r'evidence'] = parameterToString(evidence);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (submit != null) {
      formParams[r'submit'] = parameterToString(submit);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// 
  ///
  /// Parameters:
  ///
  /// * [String] charge (required):
  ///
  /// * [DisputeEvidenceParams] evidence:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [bool] submit:
  ///   Whether to immediately submit evidence to the bank. If `false`, evidence is staged on the dispute. Staged evidence is visible in the API and Dashboard, and can be submitted to the bank by making another request with this attribute set to `true` (the default).
  Future<Dispute?> postChargesChargeDispute(String charge, { DisputeEvidenceParams? evidence, List<String>? expand, PostAccountsRequestMetadata? metadata, bool? submit, }) async {
    final response = await postChargesChargeDisputeWithHttpInfo(charge,  evidence: evidence, expand: expand, metadata: metadata, submit: submit, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Dispute',) as Dispute;
    
    }
    return null;
  }

  /// 
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] charge (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postChargesChargeDisputeCloseWithHttpInfo(String charge, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/charges/{charge}/dispute/close'
      .replaceAll('{charge}', charge);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// 
  ///
  /// Parameters:
  ///
  /// * [String] charge (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Dispute?> postChargesChargeDisputeClose(String charge, { List<String>? expand, }) async {
    final response = await postChargesChargeDisputeCloseWithHttpInfo(charge,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Dispute',) as Dispute;
    
    }
    return null;
  }

  /// Create a refund
  ///
  /// <p>When you create a new refund, you must specify either a Charge or a PaymentIntent object.</p>  <p>This action refunds a previously created charge that’s not refunded yet. Funds are refunded to the credit or debit card that’s originally charged.</p>  <p>You can optionally refund only part of a charge. You can repeat this until the entire charge is refunded.</p>  <p>After you entirely refund a charge, you can’t refund it again. This method raises an error when it’s called on an already-refunded charge, or when you attempt to refund more money than is left on a charge.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] charge (required):
  ///   The identifier of the charge to refund.
  ///
  /// * [int] amount:
  ///   A positive integer in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal) representing how much of this charge to refund. Can refund only up to the remaining, unrefunded amount of the charge.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] instructionsEmail:
  ///   For payment methods without native refund support (e.g., Konbini, PromptPay), use this email from the customer to receive refund instructions.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] paymentIntent:
  ///   The identifier of the PaymentIntent to refund.
  ///
  /// * [String] reason:
  ///   String indicating the reason for the refund. If set, possible values are `duplicate`, `fraudulent`, and `requested_by_customer`. If you believe the charge to be fraudulent, specifying `fraudulent` as the reason will add the associated card and email to your [block lists](https://stripe.com/docs/radar/lists), and will also help us improve our fraud detection algorithms.
  ///
  /// * [bool] refundApplicationFee:
  ///   Boolean indicating whether the application fee should be refunded when refunding this charge. If a full charge refund is given, the full application fee will be refunded. Otherwise, the application fee will be refunded in an amount proportional to the amount of the charge refunded. An application fee can be refunded only by the application that created the charge.
  ///
  /// * [bool] reverseTransfer:
  ///   Boolean indicating whether the transfer should be reversed when refunding this charge. The transfer will be reversed proportionally to the amount being refunded (either the entire or partial amount).<br><br>A transfer can be reversed only by the application that created the charge.
  Future<Response> postChargesChargeRefundWithHttpInfo(String charge, { int? amount, List<String>? expand, String? instructionsEmail, PostAccountsRequestMetadata? metadata, String? paymentIntent, String? reason, bool? refundApplicationFee, bool? reverseTransfer, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/charges/{charge}/refund'
      .replaceAll('{charge}', charge);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (instructionsEmail != null) {
      formParams[r'instructions_email'] = parameterToString(instructionsEmail);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (paymentIntent != null) {
      formParams[r'payment_intent'] = parameterToString(paymentIntent);
    }
    if (reason != null) {
      formParams[r'reason'] = parameterToString(reason);
    }
    if (refundApplicationFee != null) {
      formParams[r'refund_application_fee'] = parameterToString(refundApplicationFee);
    }
    if (reverseTransfer != null) {
      formParams[r'reverse_transfer'] = parameterToString(reverseTransfer);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a refund
  ///
  /// <p>When you create a new refund, you must specify either a Charge or a PaymentIntent object.</p>  <p>This action refunds a previously created charge that’s not refunded yet. Funds are refunded to the credit or debit card that’s originally charged.</p>  <p>You can optionally refund only part of a charge. You can repeat this until the entire charge is refunded.</p>  <p>After you entirely refund a charge, you can’t refund it again. This method raises an error when it’s called on an already-refunded charge, or when you attempt to refund more money than is left on a charge.</p>
  ///
  /// Parameters:
  ///
  /// * [String] charge (required):
  ///   The identifier of the charge to refund.
  ///
  /// * [int] amount:
  ///   A positive integer in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal) representing how much of this charge to refund. Can refund only up to the remaining, unrefunded amount of the charge.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] instructionsEmail:
  ///   For payment methods without native refund support (e.g., Konbini, PromptPay), use this email from the customer to receive refund instructions.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] paymentIntent:
  ///   The identifier of the PaymentIntent to refund.
  ///
  /// * [String] reason:
  ///   String indicating the reason for the refund. If set, possible values are `duplicate`, `fraudulent`, and `requested_by_customer`. If you believe the charge to be fraudulent, specifying `fraudulent` as the reason will add the associated card and email to your [block lists](https://stripe.com/docs/radar/lists), and will also help us improve our fraud detection algorithms.
  ///
  /// * [bool] refundApplicationFee:
  ///   Boolean indicating whether the application fee should be refunded when refunding this charge. If a full charge refund is given, the full application fee will be refunded. Otherwise, the application fee will be refunded in an amount proportional to the amount of the charge refunded. An application fee can be refunded only by the application that created the charge.
  ///
  /// * [bool] reverseTransfer:
  ///   Boolean indicating whether the transfer should be reversed when refunding this charge. The transfer will be reversed proportionally to the amount being refunded (either the entire or partial amount).<br><br>A transfer can be reversed only by the application that created the charge.
  Future<Charge?> postChargesChargeRefund(String charge, { int? amount, List<String>? expand, String? instructionsEmail, PostAccountsRequestMetadata? metadata, String? paymentIntent, String? reason, bool? refundApplicationFee, bool? reverseTransfer, }) async {
    final response = await postChargesChargeRefundWithHttpInfo(charge,  amount: amount, expand: expand, instructionsEmail: instructionsEmail, metadata: metadata, paymentIntent: paymentIntent, reason: reason, refundApplicationFee: refundApplicationFee, reverseTransfer: reverseTransfer, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Charge',) as Charge;
    
    }
    return null;
  }

  /// Create customer balance refund
  ///
  /// <p>When you create a new refund, you must specify a Charge or a PaymentIntent object on which to create it.</p>  <p>Creating a new refund will refund a charge that has previously been created but not yet refunded. Funds will be refunded to the credit or debit card that was originally charged.</p>  <p>You can optionally refund only part of a charge. You can do so multiple times, until the entire charge has been refunded.</p>  <p>Once entirely refunded, a charge can’t be refunded again. This method will raise an error when called on an already-refunded charge, or when trying to refund more money than is left on a charge.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] charge (required):
  ///   The identifier of the charge to refund.
  ///
  /// * [int] amount:
  ///
  /// * [String] currency:
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] customer:
  ///   Customer whose customer balance to refund from.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] instructionsEmail:
  ///   For payment methods without native refund support (e.g., Konbini, PromptPay), use this email from the customer to receive refund instructions.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] origin:
  ///   Origin of the refund
  ///
  /// * [String] paymentIntent:
  ///   The identifier of the PaymentIntent to refund.
  ///
  /// * [String] reason:
  ///   String indicating the reason for the refund. If set, possible values are `duplicate`, `fraudulent`, and `requested_by_customer`. If you believe the charge to be fraudulent, specifying `fraudulent` as the reason will add the associated card and email to your [block lists](https://stripe.com/docs/radar/lists), and will also help us improve our fraud detection algorithms.
  ///
  /// * [bool] refundApplicationFee:
  ///   Boolean indicating whether the application fee should be refunded when refunding this charge. If a full charge refund is given, the full application fee will be refunded. Otherwise, the application fee will be refunded in an amount proportional to the amount of the charge refunded. An application fee can be refunded only by the application that created the charge.
  ///
  /// * [bool] reverseTransfer:
  ///   Boolean indicating whether the transfer should be reversed when refunding this charge. The transfer will be reversed proportionally to the amount being refunded (either the entire or partial amount).<br><br>A transfer can be reversed only by the application that created the charge.
  Future<Response> postChargesChargeRefundsWithHttpInfo(String charge, { int? amount, String? currency, String? customer, List<String>? expand, String? instructionsEmail, PostAccountsRequestMetadata? metadata, String? origin, String? paymentIntent, String? reason, bool? refundApplicationFee, bool? reverseTransfer, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/charges/{charge}/refunds'
      .replaceAll('{charge}', charge);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (customer != null) {
      formParams[r'customer'] = parameterToString(customer);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (instructionsEmail != null) {
      formParams[r'instructions_email'] = parameterToString(instructionsEmail);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (origin != null) {
      formParams[r'origin'] = parameterToString(origin);
    }
    if (paymentIntent != null) {
      formParams[r'payment_intent'] = parameterToString(paymentIntent);
    }
    if (reason != null) {
      formParams[r'reason'] = parameterToString(reason);
    }
    if (refundApplicationFee != null) {
      formParams[r'refund_application_fee'] = parameterToString(refundApplicationFee);
    }
    if (reverseTransfer != null) {
      formParams[r'reverse_transfer'] = parameterToString(reverseTransfer);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create customer balance refund
  ///
  /// <p>When you create a new refund, you must specify a Charge or a PaymentIntent object on which to create it.</p>  <p>Creating a new refund will refund a charge that has previously been created but not yet refunded. Funds will be refunded to the credit or debit card that was originally charged.</p>  <p>You can optionally refund only part of a charge. You can do so multiple times, until the entire charge has been refunded.</p>  <p>Once entirely refunded, a charge can’t be refunded again. This method will raise an error when called on an already-refunded charge, or when trying to refund more money than is left on a charge.</p>
  ///
  /// Parameters:
  ///
  /// * [String] charge (required):
  ///   The identifier of the charge to refund.
  ///
  /// * [int] amount:
  ///
  /// * [String] currency:
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] customer:
  ///   Customer whose customer balance to refund from.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] instructionsEmail:
  ///   For payment methods without native refund support (e.g., Konbini, PromptPay), use this email from the customer to receive refund instructions.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] origin:
  ///   Origin of the refund
  ///
  /// * [String] paymentIntent:
  ///   The identifier of the PaymentIntent to refund.
  ///
  /// * [String] reason:
  ///   String indicating the reason for the refund. If set, possible values are `duplicate`, `fraudulent`, and `requested_by_customer`. If you believe the charge to be fraudulent, specifying `fraudulent` as the reason will add the associated card and email to your [block lists](https://stripe.com/docs/radar/lists), and will also help us improve our fraud detection algorithms.
  ///
  /// * [bool] refundApplicationFee:
  ///   Boolean indicating whether the application fee should be refunded when refunding this charge. If a full charge refund is given, the full application fee will be refunded. Otherwise, the application fee will be refunded in an amount proportional to the amount of the charge refunded. An application fee can be refunded only by the application that created the charge.
  ///
  /// * [bool] reverseTransfer:
  ///   Boolean indicating whether the transfer should be reversed when refunding this charge. The transfer will be reversed proportionally to the amount being refunded (either the entire or partial amount).<br><br>A transfer can be reversed only by the application that created the charge.
  Future<Refund?> postChargesChargeRefunds(String charge, { int? amount, String? currency, String? customer, List<String>? expand, String? instructionsEmail, PostAccountsRequestMetadata? metadata, String? origin, String? paymentIntent, String? reason, bool? refundApplicationFee, bool? reverseTransfer, }) async {
    final response = await postChargesChargeRefundsWithHttpInfo(charge,  amount: amount, currency: currency, customer: customer, expand: expand, instructionsEmail: instructionsEmail, metadata: metadata, origin: origin, paymentIntent: paymentIntent, reason: reason, refundApplicationFee: refundApplicationFee, reverseTransfer: reverseTransfer, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Refund',) as Refund;
    
    }
    return null;
  }

  /// <p>Update a specified refund.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] charge (required):
  ///
  /// * [String] refund (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [IndividualSpecsMetadata] metadata:
  Future<Response> postChargesChargeRefundsRefundWithHttpInfo(String charge, String refund, { List<String>? expand, IndividualSpecsMetadata? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/charges/{charge}/refunds/{refund}'
      .replaceAll('{charge}', charge)
      .replaceAll('{refund}', refund);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// <p>Update a specified refund.</p>
  ///
  /// Parameters:
  ///
  /// * [String] charge (required):
  ///
  /// * [String] refund (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [IndividualSpecsMetadata] metadata:
  Future<Refund?> postChargesChargeRefundsRefund(String charge, String refund, { List<String>? expand, IndividualSpecsMetadata? metadata, }) async {
    final response = await postChargesChargeRefundsRefundWithHttpInfo(charge, refund,  expand: expand, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Refund',) as Refund;
    
    }
    return null;
  }

  /// Create a Checkout Session
  ///
  /// <p>Creates a Checkout Session object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [AdaptivePricingParams] adaptivePricing:
  ///
  /// * [AfterExpirationParams] afterExpiration:
  ///
  /// * [bool] allowPromotionCodes:
  ///   Enables user redeemable promotion codes.
  ///
  /// * [AutomaticTaxParams] automaticTax:
  ///
  /// * [String] billingAddressCollection:
  ///   Specify whether Checkout should collect the customer's billing address. Defaults to `auto`.
  ///
  /// * [String] cancelUrl:
  ///   If set, Checkout displays a back button and customers will be directed to this URL if they decide to cancel payment and return to your website. This parameter is not allowed if ui_mode is `embedded` or `custom`.
  ///
  /// * [String] clientReferenceId:
  ///   A unique string to reference the Checkout Session. This can be a customer ID, a cart ID, or similar, and can be used to reconcile the session with your internal systems.
  ///
  /// * [ConsentCollectionParams] consentCollection:
  ///
  /// * [String] currency:
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). Required in `setup` mode when `payment_method_types` is not set.
  ///
  /// * [List<CustomFieldParam>] customFields:
  ///   Collect additional information from your customer using custom fields. Up to 3 fields are supported.
  ///
  /// * [CustomTextParam] customText:
  ///
  /// * [String] customer:
  ///   ID of an existing Customer, if one exists. In `payment` mode, the customer’s most recently saved card payment method will be used to prefill the email, name, card details, and billing address on the Checkout page. In `subscription` mode, the customer’s [default payment method](https://stripe.com/docs/api/customers/update#update_customer-invoice_settings-default_payment_method) will be used if it’s a card, otherwise the most recently saved card will be used. A valid billing address, billing name and billing email are required on the payment method for Checkout to prefill the customer's card details.  If the Customer already has a valid [email](https://stripe.com/docs/api/customers/object#customer_object-email) set, the email will be prefilled and not editable in Checkout. If the Customer does not have a valid `email`, Checkout will set the email entered during the session on the Customer.  If blank for Checkout Sessions in `subscription` mode or with `customer_creation` set as `always` in `payment` mode, Checkout will create a new Customer object based on information provided during the payment flow.  You can set [`payment_intent_data.setup_future_usage`](https://stripe.com/docs/api/checkout/sessions/create#create_checkout_session-payment_intent_data-setup_future_usage) to have Checkout automatically attach the payment method to the Customer you pass in for future reuse.
  ///
  /// * [String] customerCreation:
  ///   Configure whether a Checkout Session creates a [Customer](https://stripe.com/docs/api/customers) during Session confirmation.  When a Customer is not created, you can still retrieve email, address, and other customer data entered in Checkout with [customer_details](https://stripe.com/docs/api/checkout/sessions/object#checkout_session_object-customer_details).  Sessions that don't create Customers instead are grouped by [guest customers](https://stripe.com/docs/payments/checkout/guest-customers) in the Dashboard. Promotion codes limited to first time customers will return invalid for these Sessions.  Can only be set in `payment` and `setup` mode.
  ///
  /// * [String] customerEmail:
  ///   If provided, this value will be used when the Customer object is created. If not provided, customers will be asked to enter their email address. Use this parameter to prefill customer data if you already have an email on file. To access information about the customer once a session is complete, use the `customer` field.
  ///
  /// * [CustomerUpdateParams] customerUpdate:
  ///
  /// * [List<DiscountParams>] discounts:
  ///   The coupon or promotion code to apply to this Session. Currently, only up to one may be specified.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] expiresAt:
  ///   The Epoch time in seconds at which the Checkout Session will expire. It can be anywhere from 30 minutes to 24 hours after Checkout Session creation. By default, this value is 24 hours from creation.
  ///
  /// * [InvoiceCreationParams] invoiceCreation:
  ///
  /// * [List<LineItemParams>] lineItems:
  ///   A list of items the customer is purchasing. Use this parameter to pass one-time or recurring [Prices](https://stripe.com/docs/api/prices). The parameter is required for `payment` and `subscription` mode.  For `payment` mode, there is a maximum of 100 line items, however it is recommended to consolidate line items if there are more than a few dozen.  For `subscription` mode, there is a maximum of 20 line items with recurring Prices and 20 line items with one-time Prices. Line items with one-time Prices will be on the initial invoice only.
  ///
  /// * [String] locale:
  ///   The IETF language tag of the locale Checkout is displayed in. If blank or `auto`, the browser's locale is used.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] mode:
  ///   The mode of the Checkout Session. Pass `subscription` if the Checkout Session includes at least one recurring item.
  ///
  /// * [List<OptionalItemParams>] optionalItems:
  ///   A list of optional items the customer can add to their order at checkout. Use this parameter to pass one-time or recurring [Prices](https://stripe.com/docs/api/prices).  There is a maximum of 10 optional items allowed on a Checkout Session, and the existing limits on the number of line items allowed on a Checkout Session apply to the combined number of line items and optional items.  For `payment` mode, there is a maximum of 100 combined line items and optional items, however it is recommended to consolidate items if there are more than a few dozen.  For `subscription` mode, there is a maximum of 20 line items and optional items with recurring Prices and 20 line items and optional items with one-time Prices.
  ///
  /// * [String] originContext:
  ///   Where the user is coming from. This informs the optimizations that are applied to the session. For example, a session originating from a mobile app may behave more like a native app, depending on the platform. This parameter is currently not allowed if `ui_mode` is `custom`.
  ///
  /// * [PaymentIntentDataParams] paymentIntentData:
  ///
  /// * [String] paymentMethodCollection:
  ///   Specify whether Checkout should collect a payment method. When set to `if_required`, Checkout will not collect a payment method when the total due for the session is 0. This may occur if the Checkout Session includes a free trial or a discount.  Can only be set in `subscription` mode. Defaults to `always`.  If you'd like information on how to collect a payment method outside of Checkout, read the guide on configuring [subscriptions with a free trial](https://stripe.com/docs/payments/checkout/free-trials).
  ///
  /// * [String] paymentMethodConfiguration:
  ///   The ID of the payment method configuration to use with this Checkout session.
  ///
  /// * [PaymentMethodDataParam] paymentMethodData:
  ///
  /// * [PaymentMethodOptionsParam] paymentMethodOptions:
  ///
  /// * [List<String>] paymentMethodTypes:
  ///   A list of the types of payment methods (e.g., `card`) this Checkout Session can accept.  You can omit this attribute to manage your payment methods from the [Stripe Dashboard](https://dashboard.stripe.com/settings/payment_methods). See [Dynamic Payment Methods](https://stripe.com/docs/payments/payment-methods/integration-options#using-dynamic-payment-methods) for more details.  Read more about the supported payment methods and their requirements in our [payment method details guide](/docs/payments/checkout/payment-methods).  If multiple payment methods are passed, Checkout will dynamically reorder them to prioritize the most relevant payment methods based on the customer's location and other characteristics.
  ///
  /// * [PermissionsParam] permissions:
  ///
  /// * [PhoneNumberCollectionParams] phoneNumberCollection:
  ///
  /// * [String] redirectOnCompletion:
  ///   This parameter applies to `ui_mode: embedded`. Learn more about the [redirect behavior](https://stripe.com/docs/payments/checkout/custom-success-page?payment-ui=embedded-form) of embedded sessions. Defaults to `always`.
  ///
  /// * [String] returnUrl:
  ///   The URL to redirect your customer back to after they authenticate or cancel their payment on the payment method's app or site. This parameter is required if `ui_mode` is `embedded` or `custom` and redirect-based payment methods are enabled on the session.
  ///
  /// * [SavedPaymentMethodOptionsParam] savedPaymentMethodOptions:
  ///
  /// * [SetupIntentDataParam] setupIntentData:
  ///
  /// * [ShippingAddressCollectionParams] shippingAddressCollection:
  ///
  /// * [List<ShippingOptionParams>] shippingOptions:
  ///   The shipping rate options to apply to this Session. Up to a maximum of 5.
  ///
  /// * [String] submitType:
  ///   Describes the type of transaction being performed by Checkout in order to customize relevant text on the page, such as the submit button.  `submit_type` can only be specified on Checkout Sessions in `payment` or `subscription` mode. If blank or `auto`, `pay` is used.
  ///
  /// * [SubscriptionDataParams] subscriptionData:
  ///
  /// * [String] successUrl:
  ///   The URL to which Stripe should send customers when payment or setup is complete. This parameter is not allowed if ui_mode is `embedded` or `custom`. If you'd like to use information from the successful Checkout Session on your page, read the guide on [customizing your success page](https://stripe.com/docs/payments/checkout/custom-success-page).
  ///
  /// * [TaxIdCollectionParams] taxIdCollection:
  ///
  /// * [String] uiMode:
  ///   The UI mode of the Session. Defaults to `hosted`.
  ///
  /// * [WalletOptionsParam] walletOptions:
  Future<Response> postCheckoutSessionsWithHttpInfo({ AdaptivePricingParams? adaptivePricing, AfterExpirationParams? afterExpiration, bool? allowPromotionCodes, AutomaticTaxParams? automaticTax, String? billingAddressCollection, String? cancelUrl, String? clientReferenceId, ConsentCollectionParams? consentCollection, String? currency, List<CustomFieldParam>? customFields, CustomTextParam? customText, String? customer, String? customerCreation, String? customerEmail, CustomerUpdateParams? customerUpdate, List<DiscountParams>? discounts, List<String>? expand, int? expiresAt, InvoiceCreationParams? invoiceCreation, List<LineItemParams>? lineItems, String? locale, Map<String, String>? metadata, String? mode, List<OptionalItemParams>? optionalItems, String? originContext, PaymentIntentDataParams? paymentIntentData, String? paymentMethodCollection, String? paymentMethodConfiguration, PaymentMethodDataParam? paymentMethodData, PaymentMethodOptionsParam? paymentMethodOptions, List<String>? paymentMethodTypes, PermissionsParam? permissions, PhoneNumberCollectionParams? phoneNumberCollection, String? redirectOnCompletion, String? returnUrl, SavedPaymentMethodOptionsParam? savedPaymentMethodOptions, SetupIntentDataParam? setupIntentData, ShippingAddressCollectionParams? shippingAddressCollection, List<ShippingOptionParams>? shippingOptions, String? submitType, SubscriptionDataParams? subscriptionData, String? successUrl, TaxIdCollectionParams? taxIdCollection, String? uiMode, WalletOptionsParam? walletOptions, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/checkout/sessions';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (adaptivePricing != null) {
      formParams[r'adaptive_pricing'] = parameterToString(adaptivePricing);
    }
    if (afterExpiration != null) {
      formParams[r'after_expiration'] = parameterToString(afterExpiration);
    }
    if (allowPromotionCodes != null) {
      formParams[r'allow_promotion_codes'] = parameterToString(allowPromotionCodes);
    }
    if (automaticTax != null) {
      formParams[r'automatic_tax'] = parameterToString(automaticTax);
    }
    if (billingAddressCollection != null) {
      formParams[r'billing_address_collection'] = parameterToString(billingAddressCollection);
    }
    if (cancelUrl != null) {
      formParams[r'cancel_url'] = parameterToString(cancelUrl);
    }
    if (clientReferenceId != null) {
      formParams[r'client_reference_id'] = parameterToString(clientReferenceId);
    }
    if (consentCollection != null) {
      formParams[r'consent_collection'] = parameterToString(consentCollection);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (customFields != null) {
      formParams[r'custom_fields'] = parameterToString(customFields);
    }
    if (customText != null) {
      formParams[r'custom_text'] = parameterToString(customText);
    }
    if (customer != null) {
      formParams[r'customer'] = parameterToString(customer);
    }
    if (customerCreation != null) {
      formParams[r'customer_creation'] = parameterToString(customerCreation);
    }
    if (customerEmail != null) {
      formParams[r'customer_email'] = parameterToString(customerEmail);
    }
    if (customerUpdate != null) {
      formParams[r'customer_update'] = parameterToString(customerUpdate);
    }
    if (discounts != null) {
      formParams[r'discounts'] = parameterToString(discounts);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (expiresAt != null) {
      formParams[r'expires_at'] = parameterToString(expiresAt);
    }
    if (invoiceCreation != null) {
      formParams[r'invoice_creation'] = parameterToString(invoiceCreation);
    }
    if (lineItems != null) {
      formParams[r'line_items'] = parameterToString(lineItems);
    }
    if (locale != null) {
      formParams[r'locale'] = parameterToString(locale);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (mode != null) {
      formParams[r'mode'] = parameterToString(mode);
    }
    if (optionalItems != null) {
      formParams[r'optional_items'] = parameterToString(optionalItems);
    }
    if (originContext != null) {
      formParams[r'origin_context'] = parameterToString(originContext);
    }
    if (paymentIntentData != null) {
      formParams[r'payment_intent_data'] = parameterToString(paymentIntentData);
    }
    if (paymentMethodCollection != null) {
      formParams[r'payment_method_collection'] = parameterToString(paymentMethodCollection);
    }
    if (paymentMethodConfiguration != null) {
      formParams[r'payment_method_configuration'] = parameterToString(paymentMethodConfiguration);
    }
    if (paymentMethodData != null) {
      formParams[r'payment_method_data'] = parameterToString(paymentMethodData);
    }
    if (paymentMethodOptions != null) {
      formParams[r'payment_method_options'] = parameterToString(paymentMethodOptions);
    }
    if (paymentMethodTypes != null) {
      formParams[r'payment_method_types'] = parameterToString(paymentMethodTypes);
    }
    if (permissions != null) {
      formParams[r'permissions'] = parameterToString(permissions);
    }
    if (phoneNumberCollection != null) {
      formParams[r'phone_number_collection'] = parameterToString(phoneNumberCollection);
    }
    if (redirectOnCompletion != null) {
      formParams[r'redirect_on_completion'] = parameterToString(redirectOnCompletion);
    }
    if (returnUrl != null) {
      formParams[r'return_url'] = parameterToString(returnUrl);
    }
    if (savedPaymentMethodOptions != null) {
      formParams[r'saved_payment_method_options'] = parameterToString(savedPaymentMethodOptions);
    }
    if (setupIntentData != null) {
      formParams[r'setup_intent_data'] = parameterToString(setupIntentData);
    }
    if (shippingAddressCollection != null) {
      formParams[r'shipping_address_collection'] = parameterToString(shippingAddressCollection);
    }
    if (shippingOptions != null) {
      formParams[r'shipping_options'] = parameterToString(shippingOptions);
    }
    if (submitType != null) {
      formParams[r'submit_type'] = parameterToString(submitType);
    }
    if (subscriptionData != null) {
      formParams[r'subscription_data'] = parameterToString(subscriptionData);
    }
    if (successUrl != null) {
      formParams[r'success_url'] = parameterToString(successUrl);
    }
    if (taxIdCollection != null) {
      formParams[r'tax_id_collection'] = parameterToString(taxIdCollection);
    }
    if (uiMode != null) {
      formParams[r'ui_mode'] = parameterToString(uiMode);
    }
    if (walletOptions != null) {
      formParams[r'wallet_options'] = parameterToString(walletOptions);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a Checkout Session
  ///
  /// <p>Creates a Checkout Session object.</p>
  ///
  /// Parameters:
  ///
  /// * [AdaptivePricingParams] adaptivePricing:
  ///
  /// * [AfterExpirationParams] afterExpiration:
  ///
  /// * [bool] allowPromotionCodes:
  ///   Enables user redeemable promotion codes.
  ///
  /// * [AutomaticTaxParams] automaticTax:
  ///
  /// * [String] billingAddressCollection:
  ///   Specify whether Checkout should collect the customer's billing address. Defaults to `auto`.
  ///
  /// * [String] cancelUrl:
  ///   If set, Checkout displays a back button and customers will be directed to this URL if they decide to cancel payment and return to your website. This parameter is not allowed if ui_mode is `embedded` or `custom`.
  ///
  /// * [String] clientReferenceId:
  ///   A unique string to reference the Checkout Session. This can be a customer ID, a cart ID, or similar, and can be used to reconcile the session with your internal systems.
  ///
  /// * [ConsentCollectionParams] consentCollection:
  ///
  /// * [String] currency:
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). Required in `setup` mode when `payment_method_types` is not set.
  ///
  /// * [List<CustomFieldParam>] customFields:
  ///   Collect additional information from your customer using custom fields. Up to 3 fields are supported.
  ///
  /// * [CustomTextParam] customText:
  ///
  /// * [String] customer:
  ///   ID of an existing Customer, if one exists. In `payment` mode, the customer’s most recently saved card payment method will be used to prefill the email, name, card details, and billing address on the Checkout page. In `subscription` mode, the customer’s [default payment method](https://stripe.com/docs/api/customers/update#update_customer-invoice_settings-default_payment_method) will be used if it’s a card, otherwise the most recently saved card will be used. A valid billing address, billing name and billing email are required on the payment method for Checkout to prefill the customer's card details.  If the Customer already has a valid [email](https://stripe.com/docs/api/customers/object#customer_object-email) set, the email will be prefilled and not editable in Checkout. If the Customer does not have a valid `email`, Checkout will set the email entered during the session on the Customer.  If blank for Checkout Sessions in `subscription` mode or with `customer_creation` set as `always` in `payment` mode, Checkout will create a new Customer object based on information provided during the payment flow.  You can set [`payment_intent_data.setup_future_usage`](https://stripe.com/docs/api/checkout/sessions/create#create_checkout_session-payment_intent_data-setup_future_usage) to have Checkout automatically attach the payment method to the Customer you pass in for future reuse.
  ///
  /// * [String] customerCreation:
  ///   Configure whether a Checkout Session creates a [Customer](https://stripe.com/docs/api/customers) during Session confirmation.  When a Customer is not created, you can still retrieve email, address, and other customer data entered in Checkout with [customer_details](https://stripe.com/docs/api/checkout/sessions/object#checkout_session_object-customer_details).  Sessions that don't create Customers instead are grouped by [guest customers](https://stripe.com/docs/payments/checkout/guest-customers) in the Dashboard. Promotion codes limited to first time customers will return invalid for these Sessions.  Can only be set in `payment` and `setup` mode.
  ///
  /// * [String] customerEmail:
  ///   If provided, this value will be used when the Customer object is created. If not provided, customers will be asked to enter their email address. Use this parameter to prefill customer data if you already have an email on file. To access information about the customer once a session is complete, use the `customer` field.
  ///
  /// * [CustomerUpdateParams] customerUpdate:
  ///
  /// * [List<DiscountParams>] discounts:
  ///   The coupon or promotion code to apply to this Session. Currently, only up to one may be specified.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] expiresAt:
  ///   The Epoch time in seconds at which the Checkout Session will expire. It can be anywhere from 30 minutes to 24 hours after Checkout Session creation. By default, this value is 24 hours from creation.
  ///
  /// * [InvoiceCreationParams] invoiceCreation:
  ///
  /// * [List<LineItemParams>] lineItems:
  ///   A list of items the customer is purchasing. Use this parameter to pass one-time or recurring [Prices](https://stripe.com/docs/api/prices). The parameter is required for `payment` and `subscription` mode.  For `payment` mode, there is a maximum of 100 line items, however it is recommended to consolidate line items if there are more than a few dozen.  For `subscription` mode, there is a maximum of 20 line items with recurring Prices and 20 line items with one-time Prices. Line items with one-time Prices will be on the initial invoice only.
  ///
  /// * [String] locale:
  ///   The IETF language tag of the locale Checkout is displayed in. If blank or `auto`, the browser's locale is used.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] mode:
  ///   The mode of the Checkout Session. Pass `subscription` if the Checkout Session includes at least one recurring item.
  ///
  /// * [List<OptionalItemParams>] optionalItems:
  ///   A list of optional items the customer can add to their order at checkout. Use this parameter to pass one-time or recurring [Prices](https://stripe.com/docs/api/prices).  There is a maximum of 10 optional items allowed on a Checkout Session, and the existing limits on the number of line items allowed on a Checkout Session apply to the combined number of line items and optional items.  For `payment` mode, there is a maximum of 100 combined line items and optional items, however it is recommended to consolidate items if there are more than a few dozen.  For `subscription` mode, there is a maximum of 20 line items and optional items with recurring Prices and 20 line items and optional items with one-time Prices.
  ///
  /// * [String] originContext:
  ///   Where the user is coming from. This informs the optimizations that are applied to the session. For example, a session originating from a mobile app may behave more like a native app, depending on the platform. This parameter is currently not allowed if `ui_mode` is `custom`.
  ///
  /// * [PaymentIntentDataParams] paymentIntentData:
  ///
  /// * [String] paymentMethodCollection:
  ///   Specify whether Checkout should collect a payment method. When set to `if_required`, Checkout will not collect a payment method when the total due for the session is 0. This may occur if the Checkout Session includes a free trial or a discount.  Can only be set in `subscription` mode. Defaults to `always`.  If you'd like information on how to collect a payment method outside of Checkout, read the guide on configuring [subscriptions with a free trial](https://stripe.com/docs/payments/checkout/free-trials).
  ///
  /// * [String] paymentMethodConfiguration:
  ///   The ID of the payment method configuration to use with this Checkout session.
  ///
  /// * [PaymentMethodDataParam] paymentMethodData:
  ///
  /// * [PaymentMethodOptionsParam] paymentMethodOptions:
  ///
  /// * [List<String>] paymentMethodTypes:
  ///   A list of the types of payment methods (e.g., `card`) this Checkout Session can accept.  You can omit this attribute to manage your payment methods from the [Stripe Dashboard](https://dashboard.stripe.com/settings/payment_methods). See [Dynamic Payment Methods](https://stripe.com/docs/payments/payment-methods/integration-options#using-dynamic-payment-methods) for more details.  Read more about the supported payment methods and their requirements in our [payment method details guide](/docs/payments/checkout/payment-methods).  If multiple payment methods are passed, Checkout will dynamically reorder them to prioritize the most relevant payment methods based on the customer's location and other characteristics.
  ///
  /// * [PermissionsParam] permissions:
  ///
  /// * [PhoneNumberCollectionParams] phoneNumberCollection:
  ///
  /// * [String] redirectOnCompletion:
  ///   This parameter applies to `ui_mode: embedded`. Learn more about the [redirect behavior](https://stripe.com/docs/payments/checkout/custom-success-page?payment-ui=embedded-form) of embedded sessions. Defaults to `always`.
  ///
  /// * [String] returnUrl:
  ///   The URL to redirect your customer back to after they authenticate or cancel their payment on the payment method's app or site. This parameter is required if `ui_mode` is `embedded` or `custom` and redirect-based payment methods are enabled on the session.
  ///
  /// * [SavedPaymentMethodOptionsParam] savedPaymentMethodOptions:
  ///
  /// * [SetupIntentDataParam] setupIntentData:
  ///
  /// * [ShippingAddressCollectionParams] shippingAddressCollection:
  ///
  /// * [List<ShippingOptionParams>] shippingOptions:
  ///   The shipping rate options to apply to this Session. Up to a maximum of 5.
  ///
  /// * [String] submitType:
  ///   Describes the type of transaction being performed by Checkout in order to customize relevant text on the page, such as the submit button.  `submit_type` can only be specified on Checkout Sessions in `payment` or `subscription` mode. If blank or `auto`, `pay` is used.
  ///
  /// * [SubscriptionDataParams] subscriptionData:
  ///
  /// * [String] successUrl:
  ///   The URL to which Stripe should send customers when payment or setup is complete. This parameter is not allowed if ui_mode is `embedded` or `custom`. If you'd like to use information from the successful Checkout Session on your page, read the guide on [customizing your success page](https://stripe.com/docs/payments/checkout/custom-success-page).
  ///
  /// * [TaxIdCollectionParams] taxIdCollection:
  ///
  /// * [String] uiMode:
  ///   The UI mode of the Session. Defaults to `hosted`.
  ///
  /// * [WalletOptionsParam] walletOptions:
  Future<CheckoutSession?> postCheckoutSessions({ AdaptivePricingParams? adaptivePricing, AfterExpirationParams? afterExpiration, bool? allowPromotionCodes, AutomaticTaxParams? automaticTax, String? billingAddressCollection, String? cancelUrl, String? clientReferenceId, ConsentCollectionParams? consentCollection, String? currency, List<CustomFieldParam>? customFields, CustomTextParam? customText, String? customer, String? customerCreation, String? customerEmail, CustomerUpdateParams? customerUpdate, List<DiscountParams>? discounts, List<String>? expand, int? expiresAt, InvoiceCreationParams? invoiceCreation, List<LineItemParams>? lineItems, String? locale, Map<String, String>? metadata, String? mode, List<OptionalItemParams>? optionalItems, String? originContext, PaymentIntentDataParams? paymentIntentData, String? paymentMethodCollection, String? paymentMethodConfiguration, PaymentMethodDataParam? paymentMethodData, PaymentMethodOptionsParam? paymentMethodOptions, List<String>? paymentMethodTypes, PermissionsParam? permissions, PhoneNumberCollectionParams? phoneNumberCollection, String? redirectOnCompletion, String? returnUrl, SavedPaymentMethodOptionsParam? savedPaymentMethodOptions, SetupIntentDataParam? setupIntentData, ShippingAddressCollectionParams? shippingAddressCollection, List<ShippingOptionParams>? shippingOptions, String? submitType, SubscriptionDataParams? subscriptionData, String? successUrl, TaxIdCollectionParams? taxIdCollection, String? uiMode, WalletOptionsParam? walletOptions, }) async {
    final response = await postCheckoutSessionsWithHttpInfo( adaptivePricing: adaptivePricing, afterExpiration: afterExpiration, allowPromotionCodes: allowPromotionCodes, automaticTax: automaticTax, billingAddressCollection: billingAddressCollection, cancelUrl: cancelUrl, clientReferenceId: clientReferenceId, consentCollection: consentCollection, currency: currency, customFields: customFields, customText: customText, customer: customer, customerCreation: customerCreation, customerEmail: customerEmail, customerUpdate: customerUpdate, discounts: discounts, expand: expand, expiresAt: expiresAt, invoiceCreation: invoiceCreation, lineItems: lineItems, locale: locale, metadata: metadata, mode: mode, optionalItems: optionalItems, originContext: originContext, paymentIntentData: paymentIntentData, paymentMethodCollection: paymentMethodCollection, paymentMethodConfiguration: paymentMethodConfiguration, paymentMethodData: paymentMethodData, paymentMethodOptions: paymentMethodOptions, paymentMethodTypes: paymentMethodTypes, permissions: permissions, phoneNumberCollection: phoneNumberCollection, redirectOnCompletion: redirectOnCompletion, returnUrl: returnUrl, savedPaymentMethodOptions: savedPaymentMethodOptions, setupIntentData: setupIntentData, shippingAddressCollection: shippingAddressCollection, shippingOptions: shippingOptions, submitType: submitType, subscriptionData: subscriptionData, successUrl: successUrl, taxIdCollection: taxIdCollection, uiMode: uiMode, walletOptions: walletOptions, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'CheckoutSession',) as CheckoutSession;
    
    }
    return null;
  }

  /// Update a Checkout Session
  ///
  /// <p>Updates a Checkout Session object.</p>  <p>Related guide: <a href=\"/payments/checkout/dynamic-updates\">Dynamically update Checkout</a></p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] session (required):
  ///
  /// * [CollectedInformationParams] collectedInformation:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [PostCheckoutSessionsSessionRequestShippingOptions] shippingOptions:
  Future<Response> postCheckoutSessionsSessionWithHttpInfo(String session, { CollectedInformationParams? collectedInformation, List<String>? expand, PostAccountsRequestMetadata? metadata, PostCheckoutSessionsSessionRequestShippingOptions? shippingOptions, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/checkout/sessions/{session}'
      .replaceAll('{session}', session);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (collectedInformation != null) {
      formParams[r'collected_information'] = parameterToString(collectedInformation);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (shippingOptions != null) {
      formParams[r'shipping_options'] = parameterToString(shippingOptions);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a Checkout Session
  ///
  /// <p>Updates a Checkout Session object.</p>  <p>Related guide: <a href=\"/payments/checkout/dynamic-updates\">Dynamically update Checkout</a></p>
  ///
  /// Parameters:
  ///
  /// * [String] session (required):
  ///
  /// * [CollectedInformationParams] collectedInformation:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [PostCheckoutSessionsSessionRequestShippingOptions] shippingOptions:
  Future<CheckoutSession?> postCheckoutSessionsSession(String session, { CollectedInformationParams? collectedInformation, List<String>? expand, PostAccountsRequestMetadata? metadata, PostCheckoutSessionsSessionRequestShippingOptions? shippingOptions, }) async {
    final response = await postCheckoutSessionsSessionWithHttpInfo(session,  collectedInformation: collectedInformation, expand: expand, metadata: metadata, shippingOptions: shippingOptions, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'CheckoutSession',) as CheckoutSession;
    
    }
    return null;
  }

  /// Expire a Checkout Session
  ///
  /// <p>A Checkout Session can be expired when it is in one of these statuses: <code>open</code> </p>  <p>After it expires, a customer can’t complete a Checkout Session and customers loading the Checkout Session see a message saying the Checkout Session is expired.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] session (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postCheckoutSessionsSessionExpireWithHttpInfo(String session, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/checkout/sessions/{session}/expire'
      .replaceAll('{session}', session);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Expire a Checkout Session
  ///
  /// <p>A Checkout Session can be expired when it is in one of these statuses: <code>open</code> </p>  <p>After it expires, a customer can’t complete a Checkout Session and customers loading the Checkout Session see a message saying the Checkout Session is expired.</p>
  ///
  /// Parameters:
  ///
  /// * [String] session (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<CheckoutSession?> postCheckoutSessionsSessionExpire(String session, { List<String>? expand, }) async {
    final response = await postCheckoutSessionsSessionExpireWithHttpInfo(session,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'CheckoutSession',) as CheckoutSession;
    
    }
    return null;
  }

  /// Create an order
  ///
  /// <p>Creates a Climate order object for a given Climate product. The order will be processed immediately after creation and payment will be deducted your Stripe balance.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] product (required):
  ///   Unique identifier of the Climate product.
  ///
  /// * [int] amount:
  ///   Requested amount of carbon removal units. Either this or `metric_tons` must be specified.
  ///
  /// * [BeneficiaryParams] beneficiary:
  ///
  /// * [String] currency:
  ///   Request currency for the order as a three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a supported [settlement currency for your account](https://stripe.com/docs/currencies). If omitted, the account's default currency will be used.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [double] metricTons:
  ///   Requested number of tons for the order. Either this or `amount` must be specified.
  Future<Response> postClimateOrdersWithHttpInfo(String product, { int? amount, BeneficiaryParams? beneficiary, String? currency, List<String>? expand, Map<String, String>? metadata, double? metricTons, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/climate/orders';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (beneficiary != null) {
      formParams[r'beneficiary'] = parameterToString(beneficiary);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (metricTons != null) {
      formParams[r'metric_tons'] = parameterToString(metricTons);
    }
    if (product != null) {
      formParams[r'product'] = parameterToString(product);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create an order
  ///
  /// <p>Creates a Climate order object for a given Climate product. The order will be processed immediately after creation and payment will be deducted your Stripe balance.</p>
  ///
  /// Parameters:
  ///
  /// * [String] product (required):
  ///   Unique identifier of the Climate product.
  ///
  /// * [int] amount:
  ///   Requested amount of carbon removal units. Either this or `metric_tons` must be specified.
  ///
  /// * [BeneficiaryParams] beneficiary:
  ///
  /// * [String] currency:
  ///   Request currency for the order as a three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a supported [settlement currency for your account](https://stripe.com/docs/currencies). If omitted, the account's default currency will be used.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [double] metricTons:
  ///   Requested number of tons for the order. Either this or `amount` must be specified.
  Future<ClimateOrder?> postClimateOrders(String product, { int? amount, BeneficiaryParams? beneficiary, String? currency, List<String>? expand, Map<String, String>? metadata, double? metricTons, }) async {
    final response = await postClimateOrdersWithHttpInfo(product,  amount: amount, beneficiary: beneficiary, currency: currency, expand: expand, metadata: metadata, metricTons: metricTons, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ClimateOrder',) as ClimateOrder;
    
    }
    return null;
  }

  /// Update an order
  ///
  /// <p>Updates the specified order by setting the values of the parameters passed.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] order (required):
  ///   Unique identifier of the order.
  ///
  /// * [PostClimateOrdersOrderRequestBeneficiary] beneficiary:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  Future<Response> postClimateOrdersOrderWithHttpInfo(String order, { PostClimateOrdersOrderRequestBeneficiary? beneficiary, List<String>? expand, Map<String, String>? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/climate/orders/{order}'
      .replaceAll('{order}', order);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (beneficiary != null) {
      formParams[r'beneficiary'] = parameterToString(beneficiary);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update an order
  ///
  /// <p>Updates the specified order by setting the values of the parameters passed.</p>
  ///
  /// Parameters:
  ///
  /// * [String] order (required):
  ///   Unique identifier of the order.
  ///
  /// * [PostClimateOrdersOrderRequestBeneficiary] beneficiary:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  Future<ClimateOrder?> postClimateOrdersOrder(String order, { PostClimateOrdersOrderRequestBeneficiary? beneficiary, List<String>? expand, Map<String, String>? metadata, }) async {
    final response = await postClimateOrdersOrderWithHttpInfo(order,  beneficiary: beneficiary, expand: expand, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ClimateOrder',) as ClimateOrder;
    
    }
    return null;
  }

  /// Cancel an order
  ///
  /// <p>Cancels a Climate order. You can cancel an order within 24 hours of creation. Stripe refunds the reservation <code>amount_subtotal</code>, but not the <code>amount_fees</code> for user-triggered cancellations. Frontier might cancel reservations if suppliers fail to deliver. If Frontier cancels the reservation, Stripe provides 90 days advance notice and refunds the <code>amount_total</code>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] order (required):
  ///   Unique identifier of the order.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postClimateOrdersOrderCancelWithHttpInfo(String order, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/climate/orders/{order}/cancel'
      .replaceAll('{order}', order);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Cancel an order
  ///
  /// <p>Cancels a Climate order. You can cancel an order within 24 hours of creation. Stripe refunds the reservation <code>amount_subtotal</code>, but not the <code>amount_fees</code> for user-triggered cancellations. Frontier might cancel reservations if suppliers fail to deliver. If Frontier cancels the reservation, Stripe provides 90 days advance notice and refunds the <code>amount_total</code>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] order (required):
  ///   Unique identifier of the order.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<ClimateOrder?> postClimateOrdersOrderCancel(String order, { List<String>? expand, }) async {
    final response = await postClimateOrdersOrderCancelWithHttpInfo(order,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ClimateOrder',) as ClimateOrder;
    
    }
    return null;
  }

  /// Create a coupon
  ///
  /// <p>You can create coupons easily via the <a href=\"https://dashboard.stripe.com/coupons\">coupon management</a> page of the Stripe dashboard. Coupon creation is also accessible via the API if you need to create coupons on the fly.</p>  <p>A coupon has either a <code>percent_off</code> or an <code>amount_off</code> and <code>currency</code>. If you set an <code>amount_off</code>, that amount will be subtracted from any invoice’s subtotal. For example, an invoice with a subtotal of <currency>100</currency> will have a final total of <currency>0</currency> if a coupon with an <code>amount_off</code> of <amount>200</amount> is applied to it and an invoice with a subtotal of <currency>300</currency> will have a final total of <currency>100</currency> if a coupon with an <code>amount_off</code> of <amount>200</amount> is applied to it.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [int] amountOff:
  ///   A positive integer representing the amount to subtract from an invoice total (required if `percent_off` is not passed).
  ///
  /// * [AppliesToParams] appliesTo:
  ///
  /// * [String] currency:
  ///   Three-letter [ISO code for the currency](https://stripe.com/docs/currencies) of the `amount_off` parameter (required if `amount_off` is passed).
  ///
  /// * [Map<String, CurrencyOption2>] currencyOptions:
  ///   Coupons defined in each available currency option (only supported if `amount_off` is passed). Each key must be a three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html) and a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] duration:
  ///   Specifies how long the discount will be in effect if used on a subscription. Defaults to `once`.
  ///
  /// * [int] durationInMonths:
  ///   Required only if `duration` is `repeating`, in which case it must be a positive integer that specifies the number of months the discount will be in effect.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] id:
  ///   Unique string of your choice that will be used to identify this coupon when applying it to a customer. If you don't want to specify a particular code, you can leave the ID blank and we'll generate a random code for you.
  ///
  /// * [int] maxRedemptions:
  ///   A positive integer specifying the number of times the coupon can be redeemed before it's no longer valid. For example, you might have a 50% off coupon that the first 20 readers of your blog can use.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] name:
  ///   Name of the coupon displayed to customers on, for instance invoices, or receipts. By default the `id` is shown if `name` is not set.
  ///
  /// * [num] percentOff:
  ///   A positive float larger than 0, and smaller or equal to 100, that represents the discount the coupon will apply (required if `amount_off` is not passed).
  ///
  /// * [int] redeemBy:
  ///   Unix timestamp specifying the last time at which the coupon can be redeemed. After the redeem_by date, the coupon can no longer be applied to new customers.
  Future<Response> postCouponsWithHttpInfo({ int? amountOff, AppliesToParams? appliesTo, String? currency, Map<String, CurrencyOption2>? currencyOptions, String? duration, int? durationInMonths, List<String>? expand, String? id, int? maxRedemptions, PostAccountsRequestMetadata? metadata, String? name, num? percentOff, int? redeemBy, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/coupons';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amountOff != null) {
      formParams[r'amount_off'] = parameterToString(amountOff);
    }
    if (appliesTo != null) {
      formParams[r'applies_to'] = parameterToString(appliesTo);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (currencyOptions != null) {
      formParams[r'currency_options'] = parameterToString(currencyOptions);
    }
    if (duration != null) {
      formParams[r'duration'] = parameterToString(duration);
    }
    if (durationInMonths != null) {
      formParams[r'duration_in_months'] = parameterToString(durationInMonths);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (id != null) {
      formParams[r'id'] = parameterToString(id);
    }
    if (maxRedemptions != null) {
      formParams[r'max_redemptions'] = parameterToString(maxRedemptions);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (name != null) {
      formParams[r'name'] = parameterToString(name);
    }
    if (percentOff != null) {
      formParams[r'percent_off'] = parameterToString(percentOff);
    }
    if (redeemBy != null) {
      formParams[r'redeem_by'] = parameterToString(redeemBy);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a coupon
  ///
  /// <p>You can create coupons easily via the <a href=\"https://dashboard.stripe.com/coupons\">coupon management</a> page of the Stripe dashboard. Coupon creation is also accessible via the API if you need to create coupons on the fly.</p>  <p>A coupon has either a <code>percent_off</code> or an <code>amount_off</code> and <code>currency</code>. If you set an <code>amount_off</code>, that amount will be subtracted from any invoice’s subtotal. For example, an invoice with a subtotal of <currency>100</currency> will have a final total of <currency>0</currency> if a coupon with an <code>amount_off</code> of <amount>200</amount> is applied to it and an invoice with a subtotal of <currency>300</currency> will have a final total of <currency>100</currency> if a coupon with an <code>amount_off</code> of <amount>200</amount> is applied to it.</p>
  ///
  /// Parameters:
  ///
  /// * [int] amountOff:
  ///   A positive integer representing the amount to subtract from an invoice total (required if `percent_off` is not passed).
  ///
  /// * [AppliesToParams] appliesTo:
  ///
  /// * [String] currency:
  ///   Three-letter [ISO code for the currency](https://stripe.com/docs/currencies) of the `amount_off` parameter (required if `amount_off` is passed).
  ///
  /// * [Map<String, CurrencyOption2>] currencyOptions:
  ///   Coupons defined in each available currency option (only supported if `amount_off` is passed). Each key must be a three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html) and a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] duration:
  ///   Specifies how long the discount will be in effect if used on a subscription. Defaults to `once`.
  ///
  /// * [int] durationInMonths:
  ///   Required only if `duration` is `repeating`, in which case it must be a positive integer that specifies the number of months the discount will be in effect.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] id:
  ///   Unique string of your choice that will be used to identify this coupon when applying it to a customer. If you don't want to specify a particular code, you can leave the ID blank and we'll generate a random code for you.
  ///
  /// * [int] maxRedemptions:
  ///   A positive integer specifying the number of times the coupon can be redeemed before it's no longer valid. For example, you might have a 50% off coupon that the first 20 readers of your blog can use.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] name:
  ///   Name of the coupon displayed to customers on, for instance invoices, or receipts. By default the `id` is shown if `name` is not set.
  ///
  /// * [num] percentOff:
  ///   A positive float larger than 0, and smaller or equal to 100, that represents the discount the coupon will apply (required if `amount_off` is not passed).
  ///
  /// * [int] redeemBy:
  ///   Unix timestamp specifying the last time at which the coupon can be redeemed. After the redeem_by date, the coupon can no longer be applied to new customers.
  Future<Coupon?> postCoupons({ int? amountOff, AppliesToParams? appliesTo, String? currency, Map<String, CurrencyOption2>? currencyOptions, String? duration, int? durationInMonths, List<String>? expand, String? id, int? maxRedemptions, PostAccountsRequestMetadata? metadata, String? name, num? percentOff, int? redeemBy, }) async {
    final response = await postCouponsWithHttpInfo( amountOff: amountOff, appliesTo: appliesTo, currency: currency, currencyOptions: currencyOptions, duration: duration, durationInMonths: durationInMonths, expand: expand, id: id, maxRedemptions: maxRedemptions, metadata: metadata, name: name, percentOff: percentOff, redeemBy: redeemBy, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Coupon',) as Coupon;
    
    }
    return null;
  }

  /// Update a coupon
  ///
  /// <p>Updates the metadata of a coupon. Other coupon details (currency, duration, amount_off) are, by design, not editable.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] coupon (required):
  ///
  /// * [Map<String, CurrencyOption2>] currencyOptions:
  ///   Coupons defined in each available currency option (only supported if the coupon is amount-based). Each key must be a three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html) and a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] name:
  ///   Name of the coupon displayed to customers on, for instance invoices, or receipts. By default the `id` is shown if `name` is not set.
  Future<Response> postCouponsCouponWithHttpInfo(String coupon, { Map<String, CurrencyOption2>? currencyOptions, List<String>? expand, PostAccountsRequestMetadata? metadata, String? name, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/coupons/{coupon}'
      .replaceAll('{coupon}', coupon);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (currencyOptions != null) {
      formParams[r'currency_options'] = parameterToString(currencyOptions);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (name != null) {
      formParams[r'name'] = parameterToString(name);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a coupon
  ///
  /// <p>Updates the metadata of a coupon. Other coupon details (currency, duration, amount_off) are, by design, not editable.</p>
  ///
  /// Parameters:
  ///
  /// * [String] coupon (required):
  ///
  /// * [Map<String, CurrencyOption2>] currencyOptions:
  ///   Coupons defined in each available currency option (only supported if the coupon is amount-based). Each key must be a three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html) and a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] name:
  ///   Name of the coupon displayed to customers on, for instance invoices, or receipts. By default the `id` is shown if `name` is not set.
  Future<Coupon?> postCouponsCoupon(String coupon, { Map<String, CurrencyOption2>? currencyOptions, List<String>? expand, PostAccountsRequestMetadata? metadata, String? name, }) async {
    final response = await postCouponsCouponWithHttpInfo(coupon,  currencyOptions: currencyOptions, expand: expand, metadata: metadata, name: name, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Coupon',) as Coupon;
    
    }
    return null;
  }

  /// Create a credit note
  ///
  /// <p>Issue a credit note to adjust the amount of a finalized invoice. A credit note will first reduce the invoice’s <code>amount_remaining</code> (and <code>amount_due</code>), but not below zero. This amount is indicated by the credit note’s <code>pre_payment_amount</code>. The excess amount is indicated by <code>post_payment_amount</code>, and it can result in any combination of the following:</p>  <ul> <li>Refunds: create a new refund (using <code>refund_amount</code>) or link existing refunds (using <code>refunds</code>).</li> <li>Customer balance credit: credit the customer’s balance (using <code>credit_amount</code>) which will be automatically applied to their next invoice when it’s finalized.</li> <li>Outside of Stripe credit: record the amount that is or will be credited outside of Stripe (using <code>out_of_band_amount</code>).</li> </ul>  <p>The sum of refunds, customer balance credits, and outside of Stripe credits must equal the <code>post_payment_amount</code>.</p>  <p>You may issue multiple credit notes for an invoice. Each credit note may increment the invoice’s <code>pre_payment_credit_notes_amount</code>, <code>post_payment_credit_notes_amount</code>, or both, depending on the invoice’s <code>amount_remaining</code> at the time of credit note creation.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  ///   ID of the invoice.
  ///
  /// * [int] amount:
  ///   The integer amount in cents (or local equivalent) representing the total amount of the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided.
  ///
  /// * [int] creditAmount:
  ///   The integer amount in cents (or local equivalent) representing the amount to credit the customer's balance, which will be automatically applied to their next invoice.
  ///
  /// * [int] effectiveAt:
  ///   The date when this credit note is in effect. Same as `created` unless overwritten. When defined, this value replaces the system-generated 'Date of issue' printed on the credit note PDF.
  ///
  /// * [String] emailType:
  ///   Type of email to send to the customer, one of `credit_note` or `none` and the default is `credit_note`.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [List<CreditNoteLineItemParams>] lines:
  ///   Line items that make up the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided.
  ///
  /// * [String] memo:
  ///   The credit note's memo appears on the credit note PDF.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [int] outOfBandAmount:
  ///   The integer amount in cents (or local equivalent) representing the amount that is credited outside of Stripe.
  ///
  /// * [String] reason:
  ///   Reason for issuing this credit note, one of `duplicate`, `fraudulent`, `order_change`, or `product_unsatisfactory`
  ///
  /// * [int] refundAmount:
  ///   The integer amount in cents (or local equivalent) representing the amount to refund. If set, a refund will be created for the charge associated with the invoice.
  ///
  /// * [List<CreditNoteRefundParams>] refunds:
  ///   Refunds to link to this credit note.
  ///
  /// * [CreditNoteShippingCost] shippingCost:
  Future<Response> postCreditNotesWithHttpInfo(String invoice, { int? amount, int? creditAmount, int? effectiveAt, String? emailType, List<String>? expand, List<CreditNoteLineItemParams>? lines, String? memo, Map<String, String>? metadata, int? outOfBandAmount, String? reason, int? refundAmount, List<CreditNoteRefundParams>? refunds, CreditNoteShippingCost? shippingCost, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/credit_notes';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (creditAmount != null) {
      formParams[r'credit_amount'] = parameterToString(creditAmount);
    }
    if (effectiveAt != null) {
      formParams[r'effective_at'] = parameterToString(effectiveAt);
    }
    if (emailType != null) {
      formParams[r'email_type'] = parameterToString(emailType);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (invoice != null) {
      formParams[r'invoice'] = parameterToString(invoice);
    }
    if (lines != null) {
      formParams[r'lines'] = parameterToString(lines);
    }
    if (memo != null) {
      formParams[r'memo'] = parameterToString(memo);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (outOfBandAmount != null) {
      formParams[r'out_of_band_amount'] = parameterToString(outOfBandAmount);
    }
    if (reason != null) {
      formParams[r'reason'] = parameterToString(reason);
    }
    if (refundAmount != null) {
      formParams[r'refund_amount'] = parameterToString(refundAmount);
    }
    if (refunds != null) {
      formParams[r'refunds'] = parameterToString(refunds);
    }
    if (shippingCost != null) {
      formParams[r'shipping_cost'] = parameterToString(shippingCost);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a credit note
  ///
  /// <p>Issue a credit note to adjust the amount of a finalized invoice. A credit note will first reduce the invoice’s <code>amount_remaining</code> (and <code>amount_due</code>), but not below zero. This amount is indicated by the credit note’s <code>pre_payment_amount</code>. The excess amount is indicated by <code>post_payment_amount</code>, and it can result in any combination of the following:</p>  <ul> <li>Refunds: create a new refund (using <code>refund_amount</code>) or link existing refunds (using <code>refunds</code>).</li> <li>Customer balance credit: credit the customer’s balance (using <code>credit_amount</code>) which will be automatically applied to their next invoice when it’s finalized.</li> <li>Outside of Stripe credit: record the amount that is or will be credited outside of Stripe (using <code>out_of_band_amount</code>).</li> </ul>  <p>The sum of refunds, customer balance credits, and outside of Stripe credits must equal the <code>post_payment_amount</code>.</p>  <p>You may issue multiple credit notes for an invoice. Each credit note may increment the invoice’s <code>pre_payment_credit_notes_amount</code>, <code>post_payment_credit_notes_amount</code>, or both, depending on the invoice’s <code>amount_remaining</code> at the time of credit note creation.</p>
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  ///   ID of the invoice.
  ///
  /// * [int] amount:
  ///   The integer amount in cents (or local equivalent) representing the total amount of the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided.
  ///
  /// * [int] creditAmount:
  ///   The integer amount in cents (or local equivalent) representing the amount to credit the customer's balance, which will be automatically applied to their next invoice.
  ///
  /// * [int] effectiveAt:
  ///   The date when this credit note is in effect. Same as `created` unless overwritten. When defined, this value replaces the system-generated 'Date of issue' printed on the credit note PDF.
  ///
  /// * [String] emailType:
  ///   Type of email to send to the customer, one of `credit_note` or `none` and the default is `credit_note`.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [List<CreditNoteLineItemParams>] lines:
  ///   Line items that make up the credit note. One of `amount`, `lines`, or `shipping_cost` must be provided.
  ///
  /// * [String] memo:
  ///   The credit note's memo appears on the credit note PDF.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [int] outOfBandAmount:
  ///   The integer amount in cents (or local equivalent) representing the amount that is credited outside of Stripe.
  ///
  /// * [String] reason:
  ///   Reason for issuing this credit note, one of `duplicate`, `fraudulent`, `order_change`, or `product_unsatisfactory`
  ///
  /// * [int] refundAmount:
  ///   The integer amount in cents (or local equivalent) representing the amount to refund. If set, a refund will be created for the charge associated with the invoice.
  ///
  /// * [List<CreditNoteRefundParams>] refunds:
  ///   Refunds to link to this credit note.
  ///
  /// * [CreditNoteShippingCost] shippingCost:
  Future<CreditNote?> postCreditNotes(String invoice, { int? amount, int? creditAmount, int? effectiveAt, String? emailType, List<String>? expand, List<CreditNoteLineItemParams>? lines, String? memo, Map<String, String>? metadata, int? outOfBandAmount, String? reason, int? refundAmount, List<CreditNoteRefundParams>? refunds, CreditNoteShippingCost? shippingCost, }) async {
    final response = await postCreditNotesWithHttpInfo(invoice,  amount: amount, creditAmount: creditAmount, effectiveAt: effectiveAt, emailType: emailType, expand: expand, lines: lines, memo: memo, metadata: metadata, outOfBandAmount: outOfBandAmount, reason: reason, refundAmount: refundAmount, refunds: refunds, shippingCost: shippingCost, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'CreditNote',) as CreditNote;
    
    }
    return null;
  }

  /// Update a credit note
  ///
  /// <p>Updates an existing credit note.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] memo:
  ///   Credit note memo.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  Future<Response> postCreditNotesIdWithHttpInfo(String id, { List<String>? expand, String? memo, Map<String, String>? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/credit_notes/{id}'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (memo != null) {
      formParams[r'memo'] = parameterToString(memo);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a credit note
  ///
  /// <p>Updates an existing credit note.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] memo:
  ///   Credit note memo.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  Future<CreditNote?> postCreditNotesId(String id, { List<String>? expand, String? memo, Map<String, String>? metadata, }) async {
    final response = await postCreditNotesIdWithHttpInfo(id,  expand: expand, memo: memo, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'CreditNote',) as CreditNote;
    
    }
    return null;
  }

  /// Void a credit note
  ///
  /// <p>Marks a credit note as void. Learn more about <a href=\"/docs/billing/invoices/credit-notes#voiding\">voiding credit notes</a>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postCreditNotesIdVoidWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/credit_notes/{id}/void'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Void a credit note
  ///
  /// <p>Marks a credit note as void. Learn more about <a href=\"/docs/billing/invoices/credit-notes#voiding\">voiding credit notes</a>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<CreditNote?> postCreditNotesIdVoid(String id, { List<String>? expand, }) async {
    final response = await postCreditNotesIdVoidWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'CreditNote',) as CreditNote;
    
    }
    return null;
  }

  /// Create a Customer Session
  ///
  /// <p>Creates a Customer Session object that includes a single-use client secret that you can use on your front-end to grant client-side API access for certain customer resources.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [Components] components (required):
  ///
  /// * [String] customer (required):
  ///   The ID of an existing customer for which to create the Customer Session.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postCustomerSessionsWithHttpInfo(Components components, String customer, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customer_sessions';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (components != null) {
      formParams[r'components'] = parameterToString(components);
    }
    if (customer != null) {
      formParams[r'customer'] = parameterToString(customer);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a Customer Session
  ///
  /// <p>Creates a Customer Session object that includes a single-use client secret that you can use on your front-end to grant client-side API access for certain customer resources.</p>
  ///
  /// Parameters:
  ///
  /// * [Components] components (required):
  ///
  /// * [String] customer (required):
  ///   The ID of an existing customer for which to create the Customer Session.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<CustomerSession?> postCustomerSessions(Components components, String customer, { List<String>? expand, }) async {
    final response = await postCustomerSessionsWithHttpInfo(components, customer,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'CustomerSession',) as CustomerSession;
    
    }
    return null;
  }

  /// Create a customer
  ///
  /// <p>Creates a new customer object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [PostCustomersRequestAddress] address:
  ///
  /// * [int] balance:
  ///   An integer amount in cents (or local equivalent) that represents the customer's current balance, which affect the customer's future invoices. A negative amount represents a credit that decreases the amount due on an invoice; a positive amount increases the amount due on an invoice.
  ///
  /// * [CashBalanceParam] cashBalance:
  ///
  /// * [String] description:
  ///   An arbitrary string that you can attach to a customer object. It is displayed alongside the customer in the dashboard.
  ///
  /// * [String] email:
  ///   Customer's email address. It's displayed alongside the customer in your dashboard and can be useful for searching and tracking. This may be up to *512 characters*.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] invoicePrefix:
  ///   The prefix for the customer used to generate unique invoice numbers. Must be 3–12 uppercase letters or numbers.
  ///
  /// * [CustomerParam] invoiceSettings:
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] name:
  ///   The customer's full name or business name.
  ///
  /// * [int] nextInvoiceSequence:
  ///   The sequence to be used on the customer's next invoice. Defaults to 1.
  ///
  /// * [String] paymentMethod:
  ///
  /// * [String] phone:
  ///   The customer's phone number.
  ///
  /// * [List<String>] preferredLocales:
  ///   Customer's preferred languages, ordered by preference.
  ///
  /// * [PostCustomersRequestShipping] shipping:
  ///
  /// * [String] source_:
  ///
  /// * [SharedTaxCreateParam] tax:
  ///
  /// * [String] taxExempt:
  ///   The customer's tax exemption. One of `none`, `exempt`, or `reverse`.
  ///
  /// * [List<DataParams>] taxIdData:
  ///   The customer's tax IDs.
  ///
  /// * [String] testClock:
  ///   ID of the test clock to attach to the customer.
  Future<Response> postCustomersWithHttpInfo({ PostCustomersRequestAddress? address, int? balance, CashBalanceParam? cashBalance, String? description, String? email, List<String>? expand, String? invoicePrefix, CustomerParam? invoiceSettings, PostAccountsRequestMetadata? metadata, String? name, int? nextInvoiceSequence, String? paymentMethod, String? phone, List<String>? preferredLocales, PostCustomersRequestShipping? shipping, String? source_, SharedTaxCreateParam? tax, String? taxExempt, List<DataParams>? taxIdData, String? testClock, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (address != null) {
      formParams[r'address'] = parameterToString(address);
    }
    if (balance != null) {
      formParams[r'balance'] = parameterToString(balance);
    }
    if (cashBalance != null) {
      formParams[r'cash_balance'] = parameterToString(cashBalance);
    }
    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (email != null) {
      formParams[r'email'] = parameterToString(email);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (invoicePrefix != null) {
      formParams[r'invoice_prefix'] = parameterToString(invoicePrefix);
    }
    if (invoiceSettings != null) {
      formParams[r'invoice_settings'] = parameterToString(invoiceSettings);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (name != null) {
      formParams[r'name'] = parameterToString(name);
    }
    if (nextInvoiceSequence != null) {
      formParams[r'next_invoice_sequence'] = parameterToString(nextInvoiceSequence);
    }
    if (paymentMethod != null) {
      formParams[r'payment_method'] = parameterToString(paymentMethod);
    }
    if (phone != null) {
      formParams[r'phone'] = parameterToString(phone);
    }
    if (preferredLocales != null) {
      formParams[r'preferred_locales'] = parameterToString(preferredLocales);
    }
    if (shipping != null) {
      formParams[r'shipping'] = parameterToString(shipping);
    }
    if (source_ != null) {
      formParams[r'source'] = parameterToString(source_);
    }
    if (tax != null) {
      formParams[r'tax'] = parameterToString(tax);
    }
    if (taxExempt != null) {
      formParams[r'tax_exempt'] = parameterToString(taxExempt);
    }
    if (taxIdData != null) {
      formParams[r'tax_id_data'] = parameterToString(taxIdData);
    }
    if (testClock != null) {
      formParams[r'test_clock'] = parameterToString(testClock);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a customer
  ///
  /// <p>Creates a new customer object.</p>
  ///
  /// Parameters:
  ///
  /// * [PostCustomersRequestAddress] address:
  ///
  /// * [int] balance:
  ///   An integer amount in cents (or local equivalent) that represents the customer's current balance, which affect the customer's future invoices. A negative amount represents a credit that decreases the amount due on an invoice; a positive amount increases the amount due on an invoice.
  ///
  /// * [CashBalanceParam] cashBalance:
  ///
  /// * [String] description:
  ///   An arbitrary string that you can attach to a customer object. It is displayed alongside the customer in the dashboard.
  ///
  /// * [String] email:
  ///   Customer's email address. It's displayed alongside the customer in your dashboard and can be useful for searching and tracking. This may be up to *512 characters*.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] invoicePrefix:
  ///   The prefix for the customer used to generate unique invoice numbers. Must be 3–12 uppercase letters or numbers.
  ///
  /// * [CustomerParam] invoiceSettings:
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] name:
  ///   The customer's full name or business name.
  ///
  /// * [int] nextInvoiceSequence:
  ///   The sequence to be used on the customer's next invoice. Defaults to 1.
  ///
  /// * [String] paymentMethod:
  ///
  /// * [String] phone:
  ///   The customer's phone number.
  ///
  /// * [List<String>] preferredLocales:
  ///   Customer's preferred languages, ordered by preference.
  ///
  /// * [PostCustomersRequestShipping] shipping:
  ///
  /// * [String] source_:
  ///
  /// * [SharedTaxCreateParam] tax:
  ///
  /// * [String] taxExempt:
  ///   The customer's tax exemption. One of `none`, `exempt`, or `reverse`.
  ///
  /// * [List<DataParams>] taxIdData:
  ///   The customer's tax IDs.
  ///
  /// * [String] testClock:
  ///   ID of the test clock to attach to the customer.
  Future<Customer?> postCustomers({ PostCustomersRequestAddress? address, int? balance, CashBalanceParam? cashBalance, String? description, String? email, List<String>? expand, String? invoicePrefix, CustomerParam? invoiceSettings, PostAccountsRequestMetadata? metadata, String? name, int? nextInvoiceSequence, String? paymentMethod, String? phone, List<String>? preferredLocales, PostCustomersRequestShipping? shipping, String? source_, SharedTaxCreateParam? tax, String? taxExempt, List<DataParams>? taxIdData, String? testClock, }) async {
    final response = await postCustomersWithHttpInfo( address: address, balance: balance, cashBalance: cashBalance, description: description, email: email, expand: expand, invoicePrefix: invoicePrefix, invoiceSettings: invoiceSettings, metadata: metadata, name: name, nextInvoiceSequence: nextInvoiceSequence, paymentMethod: paymentMethod, phone: phone, preferredLocales: preferredLocales, shipping: shipping, source_: source_, tax: tax, taxExempt: taxExempt, taxIdData: taxIdData, testClock: testClock, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Customer',) as Customer;
    
    }
    return null;
  }

  /// Update a customer
  ///
  /// <p>Updates the specified customer by setting the values of the parameters passed. Any parameters not provided will be left unchanged. For example, if you pass the <strong>source</strong> parameter, that becomes the customer’s active source (e.g., a card) to be used for all charges in the future. When you update a customer to a new valid card source by passing the <strong>source</strong> parameter: for each of the customer’s current subscriptions, if the subscription bills automatically and is in the <code>past_due</code> state, then the latest open invoice for the subscription with automatic collection enabled will be retried. This retry will not count as an automatic retry, and will not affect the next regularly scheduled payment for the invoice. Changing the <strong>default_source</strong> for a customer will not trigger this behavior.</p>  <p>This request accepts mostly the same arguments as the customer creation call.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [PostCustomersRequestAddress] address:
  ///
  /// * [int] balance:
  ///   An integer amount in cents (or local equivalent) that represents the customer's current balance, which affect the customer's future invoices. A negative amount represents a credit that decreases the amount due on an invoice; a positive amount increases the amount due on an invoice.
  ///
  /// * [PostCustomersCustomerRequestBankAccount] bankAccount:
  ///
  /// * [PostChargesRequestCard] card:
  ///
  /// * [CashBalanceParam] cashBalance:
  ///
  /// * [String] defaultAlipayAccount:
  ///   ID of Alipay account to make the customer's new default for invoice payments.
  ///
  /// * [String] defaultBankAccount:
  ///   ID of bank account to make the customer's new default for invoice payments.
  ///
  /// * [String] defaultCard:
  ///   ID of card to make the customer's new default for invoice payments.
  ///
  /// * [String] defaultSource:
  ///   If you are using payment methods created via the PaymentMethods API, see the [invoice_settings.default_payment_method](https://stripe.com/docs/api/customers/update#update_customer-invoice_settings-default_payment_method) parameter.  Provide the ID of a payment source already attached to this customer to make it this customer's default payment source.  If you want to add a new payment source and make it the default, see the [source](https://stripe.com/docs/api/customers/update#update_customer-source) property.
  ///
  /// * [String] description:
  ///   An arbitrary string that you can attach to a customer object. It is displayed alongside the customer in the dashboard.
  ///
  /// * [String] email:
  ///   Customer's email address. It's displayed alongside the customer in your dashboard and can be useful for searching and tracking. This may be up to *512 characters*.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] invoicePrefix:
  ///   The prefix for the customer used to generate unique invoice numbers. Must be 3–12 uppercase letters or numbers.
  ///
  /// * [CustomerParam] invoiceSettings:
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] name:
  ///   The customer's full name or business name.
  ///
  /// * [int] nextInvoiceSequence:
  ///   The sequence to be used on the customer's next invoice. Defaults to 1.
  ///
  /// * [String] phone:
  ///   The customer's phone number.
  ///
  /// * [List<String>] preferredLocales:
  ///   Customer's preferred languages, ordered by preference.
  ///
  /// * [PostCustomersRequestShipping] shipping:
  ///
  /// * [String] source_:
  ///
  /// * [SharedTaxUpdateParam] tax:
  ///
  /// * [String] taxExempt:
  ///   The customer's tax exemption. One of `none`, `exempt`, or `reverse`.
  Future<Response> postCustomersCustomerWithHttpInfo(String customer, { PostCustomersRequestAddress? address, int? balance, PostCustomersCustomerRequestBankAccount? bankAccount, PostChargesRequestCard? card, CashBalanceParam? cashBalance, String? defaultAlipayAccount, String? defaultBankAccount, String? defaultCard, String? defaultSource, String? description, String? email, List<String>? expand, String? invoicePrefix, CustomerParam? invoiceSettings, PostAccountsRequestMetadata? metadata, String? name, int? nextInvoiceSequence, String? phone, List<String>? preferredLocales, PostCustomersRequestShipping? shipping, String? source_, SharedTaxUpdateParam? tax, String? taxExempt, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}'
      .replaceAll('{customer}', customer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (address != null) {
      formParams[r'address'] = parameterToString(address);
    }
    if (balance != null) {
      formParams[r'balance'] = parameterToString(balance);
    }
    if (bankAccount != null) {
      formParams[r'bank_account'] = parameterToString(bankAccount);
    }
    if (card != null) {
      formParams[r'card'] = parameterToString(card);
    }
    if (cashBalance != null) {
      formParams[r'cash_balance'] = parameterToString(cashBalance);
    }
    if (defaultAlipayAccount != null) {
      formParams[r'default_alipay_account'] = parameterToString(defaultAlipayAccount);
    }
    if (defaultBankAccount != null) {
      formParams[r'default_bank_account'] = parameterToString(defaultBankAccount);
    }
    if (defaultCard != null) {
      formParams[r'default_card'] = parameterToString(defaultCard);
    }
    if (defaultSource != null) {
      formParams[r'default_source'] = parameterToString(defaultSource);
    }
    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (email != null) {
      formParams[r'email'] = parameterToString(email);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (invoicePrefix != null) {
      formParams[r'invoice_prefix'] = parameterToString(invoicePrefix);
    }
    if (invoiceSettings != null) {
      formParams[r'invoice_settings'] = parameterToString(invoiceSettings);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (name != null) {
      formParams[r'name'] = parameterToString(name);
    }
    if (nextInvoiceSequence != null) {
      formParams[r'next_invoice_sequence'] = parameterToString(nextInvoiceSequence);
    }
    if (phone != null) {
      formParams[r'phone'] = parameterToString(phone);
    }
    if (preferredLocales != null) {
      formParams[r'preferred_locales'] = parameterToString(preferredLocales);
    }
    if (shipping != null) {
      formParams[r'shipping'] = parameterToString(shipping);
    }
    if (source_ != null) {
      formParams[r'source'] = parameterToString(source_);
    }
    if (tax != null) {
      formParams[r'tax'] = parameterToString(tax);
    }
    if (taxExempt != null) {
      formParams[r'tax_exempt'] = parameterToString(taxExempt);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a customer
  ///
  /// <p>Updates the specified customer by setting the values of the parameters passed. Any parameters not provided will be left unchanged. For example, if you pass the <strong>source</strong> parameter, that becomes the customer’s active source (e.g., a card) to be used for all charges in the future. When you update a customer to a new valid card source by passing the <strong>source</strong> parameter: for each of the customer’s current subscriptions, if the subscription bills automatically and is in the <code>past_due</code> state, then the latest open invoice for the subscription with automatic collection enabled will be retried. This retry will not count as an automatic retry, and will not affect the next regularly scheduled payment for the invoice. Changing the <strong>default_source</strong> for a customer will not trigger this behavior.</p>  <p>This request accepts mostly the same arguments as the customer creation call.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [PostCustomersRequestAddress] address:
  ///
  /// * [int] balance:
  ///   An integer amount in cents (or local equivalent) that represents the customer's current balance, which affect the customer's future invoices. A negative amount represents a credit that decreases the amount due on an invoice; a positive amount increases the amount due on an invoice.
  ///
  /// * [PostCustomersCustomerRequestBankAccount] bankAccount:
  ///
  /// * [PostChargesRequestCard] card:
  ///
  /// * [CashBalanceParam] cashBalance:
  ///
  /// * [String] defaultAlipayAccount:
  ///   ID of Alipay account to make the customer's new default for invoice payments.
  ///
  /// * [String] defaultBankAccount:
  ///   ID of bank account to make the customer's new default for invoice payments.
  ///
  /// * [String] defaultCard:
  ///   ID of card to make the customer's new default for invoice payments.
  ///
  /// * [String] defaultSource:
  ///   If you are using payment methods created via the PaymentMethods API, see the [invoice_settings.default_payment_method](https://stripe.com/docs/api/customers/update#update_customer-invoice_settings-default_payment_method) parameter.  Provide the ID of a payment source already attached to this customer to make it this customer's default payment source.  If you want to add a new payment source and make it the default, see the [source](https://stripe.com/docs/api/customers/update#update_customer-source) property.
  ///
  /// * [String] description:
  ///   An arbitrary string that you can attach to a customer object. It is displayed alongside the customer in the dashboard.
  ///
  /// * [String] email:
  ///   Customer's email address. It's displayed alongside the customer in your dashboard and can be useful for searching and tracking. This may be up to *512 characters*.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] invoicePrefix:
  ///   The prefix for the customer used to generate unique invoice numbers. Must be 3–12 uppercase letters or numbers.
  ///
  /// * [CustomerParam] invoiceSettings:
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] name:
  ///   The customer's full name or business name.
  ///
  /// * [int] nextInvoiceSequence:
  ///   The sequence to be used on the customer's next invoice. Defaults to 1.
  ///
  /// * [String] phone:
  ///   The customer's phone number.
  ///
  /// * [List<String>] preferredLocales:
  ///   Customer's preferred languages, ordered by preference.
  ///
  /// * [PostCustomersRequestShipping] shipping:
  ///
  /// * [String] source_:
  ///
  /// * [SharedTaxUpdateParam] tax:
  ///
  /// * [String] taxExempt:
  ///   The customer's tax exemption. One of `none`, `exempt`, or `reverse`.
  Future<Customer?> postCustomersCustomer(String customer, { PostCustomersRequestAddress? address, int? balance, PostCustomersCustomerRequestBankAccount? bankAccount, PostChargesRequestCard? card, CashBalanceParam? cashBalance, String? defaultAlipayAccount, String? defaultBankAccount, String? defaultCard, String? defaultSource, String? description, String? email, List<String>? expand, String? invoicePrefix, CustomerParam? invoiceSettings, PostAccountsRequestMetadata? metadata, String? name, int? nextInvoiceSequence, String? phone, List<String>? preferredLocales, PostCustomersRequestShipping? shipping, String? source_, SharedTaxUpdateParam? tax, String? taxExempt, }) async {
    final response = await postCustomersCustomerWithHttpInfo(customer,  address: address, balance: balance, bankAccount: bankAccount, card: card, cashBalance: cashBalance, defaultAlipayAccount: defaultAlipayAccount, defaultBankAccount: defaultBankAccount, defaultCard: defaultCard, defaultSource: defaultSource, description: description, email: email, expand: expand, invoicePrefix: invoicePrefix, invoiceSettings: invoiceSettings, metadata: metadata, name: name, nextInvoiceSequence: nextInvoiceSequence, phone: phone, preferredLocales: preferredLocales, shipping: shipping, source_: source_, tax: tax, taxExempt: taxExempt, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Customer',) as Customer;
    
    }
    return null;
  }

  /// Create a customer balance transaction
  ///
  /// <p>Creates an immutable transaction that updates the customer’s credit <a href=\"/docs/billing/customer/balance\">balance</a>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [int] amount (required):
  ///   The integer amount in **cents (or local equivalent)** to apply to the customer's credit balance.
  ///
  /// * [String] currency (required):
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). Specifies the [`invoice_credit_balance`](https://stripe.com/docs/api/customers/object#customer_object-invoice_credit_balance) that this transaction will apply to. If the customer's `currency` is not set, it will be updated to this value.
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<Response> postCustomersCustomerBalanceTransactionsWithHttpInfo(String customer, int amount, String currency, { String? description, List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/balance_transactions'
      .replaceAll('{customer}', customer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a customer balance transaction
  ///
  /// <p>Creates an immutable transaction that updates the customer’s credit <a href=\"/docs/billing/customer/balance\">balance</a>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [int] amount (required):
  ///   The integer amount in **cents (or local equivalent)** to apply to the customer's credit balance.
  ///
  /// * [String] currency (required):
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies). Specifies the [`invoice_credit_balance`](https://stripe.com/docs/api/customers/object#customer_object-invoice_credit_balance) that this transaction will apply to. If the customer's `currency` is not set, it will be updated to this value.
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<CustomerBalanceTransaction?> postCustomersCustomerBalanceTransactions(String customer, int amount, String currency, { String? description, List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    final response = await postCustomersCustomerBalanceTransactionsWithHttpInfo(customer, amount, currency,  description: description, expand: expand, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'CustomerBalanceTransaction',) as CustomerBalanceTransaction;
    
    }
    return null;
  }

  /// Update a customer credit balance transaction
  ///
  /// <p>Most credit balance transaction fields are immutable, but you may update its <code>description</code> and <code>metadata</code>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] transaction (required):
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<Response> postCustomersCustomerBalanceTransactionsTransactionWithHttpInfo(String customer, String transaction, { String? description, List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/balance_transactions/{transaction}'
      .replaceAll('{customer}', customer)
      .replaceAll('{transaction}', transaction);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a customer credit balance transaction
  ///
  /// <p>Most credit balance transaction fields are immutable, but you may update its <code>description</code> and <code>metadata</code>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] transaction (required):
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<CustomerBalanceTransaction?> postCustomersCustomerBalanceTransactionsTransaction(String customer, String transaction, { String? description, List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    final response = await postCustomersCustomerBalanceTransactionsTransactionWithHttpInfo(customer, transaction,  description: description, expand: expand, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'CustomerBalanceTransaction',) as CustomerBalanceTransaction;
    
    }
    return null;
  }

  /// Create a card
  ///
  /// <p>When you create a new credit card, you must specify a customer or recipient on which to create it.</p>  <p>If the card’s owner has no default card, then the new card will become the default. However, if the owner already has a default, then it will not change. To change the default, you should <a href=\"/docs/api#update_customer\">update the customer</a> to have a new <code>default_source</code>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] alipayAccount:
  ///   A token returned by [Stripe.js](https://stripe.com/docs/js) representing the user’s Alipay account details.
  ///
  /// * [PostCustomersCustomerRequestBankAccount] bankAccount:
  ///
  /// * [PostChargesRequestCard] card:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] source_:
  ///   Please refer to full [documentation](https://stripe.com/docs/api) instead.
  Future<Response> postCustomersCustomerBankAccountsWithHttpInfo(String customer, { String? alipayAccount, PostCustomersCustomerRequestBankAccount? bankAccount, PostChargesRequestCard? card, List<String>? expand, Map<String, String>? metadata, String? source_, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/bank_accounts'
      .replaceAll('{customer}', customer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (alipayAccount != null) {
      formParams[r'alipay_account'] = parameterToString(alipayAccount);
    }
    if (bankAccount != null) {
      formParams[r'bank_account'] = parameterToString(bankAccount);
    }
    if (card != null) {
      formParams[r'card'] = parameterToString(card);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (source_ != null) {
      formParams[r'source'] = parameterToString(source_);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a card
  ///
  /// <p>When you create a new credit card, you must specify a customer or recipient on which to create it.</p>  <p>If the card’s owner has no default card, then the new card will become the default. However, if the owner already has a default, then it will not change. To change the default, you should <a href=\"/docs/api#update_customer\">update the customer</a> to have a new <code>default_source</code>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] alipayAccount:
  ///   A token returned by [Stripe.js](https://stripe.com/docs/js) representing the user’s Alipay account details.
  ///
  /// * [PostCustomersCustomerRequestBankAccount] bankAccount:
  ///
  /// * [PostChargesRequestCard] card:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] source_:
  ///   Please refer to full [documentation](https://stripe.com/docs/api) instead.
  Future<PaymentSource?> postCustomersCustomerBankAccounts(String customer, { String? alipayAccount, PostCustomersCustomerRequestBankAccount? bankAccount, PostChargesRequestCard? card, List<String>? expand, Map<String, String>? metadata, String? source_, }) async {
    final response = await postCustomersCustomerBankAccountsWithHttpInfo(customer,  alipayAccount: alipayAccount, bankAccount: bankAccount, card: card, expand: expand, metadata: metadata, source_: source_, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentSource',) as PaymentSource;
    
    }
    return null;
  }

  /// <p>Update a specified source for a given customer.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] id (required):
  ///
  /// * [String] accountHolderName:
  ///   The name of the person or business that owns the bank account.
  ///
  /// * [String] accountHolderType:
  ///   The type of entity that holds the account. This can be either `individual` or `company`.
  ///
  /// * [String] addressCity:
  ///   City/District/Suburb/Town/Village.
  ///
  /// * [String] addressCountry:
  ///   Billing address country, if provided when creating card.
  ///
  /// * [String] addressLine1:
  ///   Address line 1 (Street address/PO Box/Company name).
  ///
  /// * [String] addressLine2:
  ///   Address line 2 (Apartment/Suite/Unit/Building).
  ///
  /// * [String] addressState:
  ///   State/County/Province/Region.
  ///
  /// * [String] addressZip:
  ///   ZIP or postal code.
  ///
  /// * [String] expMonth:
  ///   Two digit number representing the card’s expiration month.
  ///
  /// * [String] expYear:
  ///   Four digit number representing the card’s expiration year.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] name:
  ///   Cardholder name.
  ///
  /// * [Owner] owner:
  Future<Response> postCustomersCustomerBankAccountsIdWithHttpInfo(String customer, String id, { String? accountHolderName, String? accountHolderType, String? addressCity, String? addressCountry, String? addressLine1, String? addressLine2, String? addressState, String? addressZip, String? expMonth, String? expYear, List<String>? expand, PostAccountsRequestMetadata? metadata, String? name, Owner? owner, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/bank_accounts/{id}'
      .replaceAll('{customer}', customer)
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (accountHolderName != null) {
      formParams[r'account_holder_name'] = parameterToString(accountHolderName);
    }
    if (accountHolderType != null) {
      formParams[r'account_holder_type'] = parameterToString(accountHolderType);
    }
    if (addressCity != null) {
      formParams[r'address_city'] = parameterToString(addressCity);
    }
    if (addressCountry != null) {
      formParams[r'address_country'] = parameterToString(addressCountry);
    }
    if (addressLine1 != null) {
      formParams[r'address_line1'] = parameterToString(addressLine1);
    }
    if (addressLine2 != null) {
      formParams[r'address_line2'] = parameterToString(addressLine2);
    }
    if (addressState != null) {
      formParams[r'address_state'] = parameterToString(addressState);
    }
    if (addressZip != null) {
      formParams[r'address_zip'] = parameterToString(addressZip);
    }
    if (expMonth != null) {
      formParams[r'exp_month'] = parameterToString(expMonth);
    }
    if (expYear != null) {
      formParams[r'exp_year'] = parameterToString(expYear);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (name != null) {
      formParams[r'name'] = parameterToString(name);
    }
    if (owner != null) {
      formParams[r'owner'] = parameterToString(owner);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// <p>Update a specified source for a given customer.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] id (required):
  ///
  /// * [String] accountHolderName:
  ///   The name of the person or business that owns the bank account.
  ///
  /// * [String] accountHolderType:
  ///   The type of entity that holds the account. This can be either `individual` or `company`.
  ///
  /// * [String] addressCity:
  ///   City/District/Suburb/Town/Village.
  ///
  /// * [String] addressCountry:
  ///   Billing address country, if provided when creating card.
  ///
  /// * [String] addressLine1:
  ///   Address line 1 (Street address/PO Box/Company name).
  ///
  /// * [String] addressLine2:
  ///   Address line 2 (Apartment/Suite/Unit/Building).
  ///
  /// * [String] addressState:
  ///   State/County/Province/Region.
  ///
  /// * [String] addressZip:
  ///   ZIP or postal code.
  ///
  /// * [String] expMonth:
  ///   Two digit number representing the card’s expiration month.
  ///
  /// * [String] expYear:
  ///   Four digit number representing the card’s expiration year.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] name:
  ///   Cardholder name.
  ///
  /// * [Owner] owner:
  Future<PostCustomersCustomerBankAccountsId200Response?> postCustomersCustomerBankAccountsId(String customer, String id, { String? accountHolderName, String? accountHolderType, String? addressCity, String? addressCountry, String? addressLine1, String? addressLine2, String? addressState, String? addressZip, String? expMonth, String? expYear, List<String>? expand, PostAccountsRequestMetadata? metadata, String? name, Owner? owner, }) async {
    final response = await postCustomersCustomerBankAccountsIdWithHttpInfo(customer, id,  accountHolderName: accountHolderName, accountHolderType: accountHolderType, addressCity: addressCity, addressCountry: addressCountry, addressLine1: addressLine1, addressLine2: addressLine2, addressState: addressState, addressZip: addressZip, expMonth: expMonth, expYear: expYear, expand: expand, metadata: metadata, name: name, owner: owner, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PostCustomersCustomerBankAccountsId200Response',) as PostCustomersCustomerBankAccountsId200Response;
    
    }
    return null;
  }

  /// Verify a bank account
  ///
  /// <p>Verify a specified bank account for a given customer.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] id (required):
  ///
  /// * [List<int>] amounts:
  ///   Two positive integers, in *cents*, equal to the values of the microdeposits sent to the bank account.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postCustomersCustomerBankAccountsIdVerifyWithHttpInfo(String customer, String id, { List<int>? amounts, List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/bank_accounts/{id}/verify'
      .replaceAll('{customer}', customer)
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amounts != null) {
      formParams[r'amounts'] = parameterToString(amounts);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Verify a bank account
  ///
  /// <p>Verify a specified bank account for a given customer.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] id (required):
  ///
  /// * [List<int>] amounts:
  ///   Two positive integers, in *cents*, equal to the values of the microdeposits sent to the bank account.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<BankAccount?> postCustomersCustomerBankAccountsIdVerify(String customer, String id, { List<int>? amounts, List<String>? expand, }) async {
    final response = await postCustomersCustomerBankAccountsIdVerifyWithHttpInfo(customer, id,  amounts: amounts, expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BankAccount',) as BankAccount;
    
    }
    return null;
  }

  /// Create a card
  ///
  /// <p>When you create a new credit card, you must specify a customer or recipient on which to create it.</p>  <p>If the card’s owner has no default card, then the new card will become the default. However, if the owner already has a default, then it will not change. To change the default, you should <a href=\"/docs/api#update_customer\">update the customer</a> to have a new <code>default_source</code>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] alipayAccount:
  ///   A token returned by [Stripe.js](https://stripe.com/docs/js) representing the user’s Alipay account details.
  ///
  /// * [PostCustomersCustomerRequestBankAccount] bankAccount:
  ///
  /// * [PostChargesRequestCard] card:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] source_:
  ///   Please refer to full [documentation](https://stripe.com/docs/api) instead.
  Future<Response> postCustomersCustomerCardsWithHttpInfo(String customer, { String? alipayAccount, PostCustomersCustomerRequestBankAccount? bankAccount, PostChargesRequestCard? card, List<String>? expand, Map<String, String>? metadata, String? source_, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/cards'
      .replaceAll('{customer}', customer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (alipayAccount != null) {
      formParams[r'alipay_account'] = parameterToString(alipayAccount);
    }
    if (bankAccount != null) {
      formParams[r'bank_account'] = parameterToString(bankAccount);
    }
    if (card != null) {
      formParams[r'card'] = parameterToString(card);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (source_ != null) {
      formParams[r'source'] = parameterToString(source_);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a card
  ///
  /// <p>When you create a new credit card, you must specify a customer or recipient on which to create it.</p>  <p>If the card’s owner has no default card, then the new card will become the default. However, if the owner already has a default, then it will not change. To change the default, you should <a href=\"/docs/api#update_customer\">update the customer</a> to have a new <code>default_source</code>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] alipayAccount:
  ///   A token returned by [Stripe.js](https://stripe.com/docs/js) representing the user’s Alipay account details.
  ///
  /// * [PostCustomersCustomerRequestBankAccount] bankAccount:
  ///
  /// * [PostChargesRequestCard] card:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] source_:
  ///   Please refer to full [documentation](https://stripe.com/docs/api) instead.
  Future<PaymentSource?> postCustomersCustomerCards(String customer, { String? alipayAccount, PostCustomersCustomerRequestBankAccount? bankAccount, PostChargesRequestCard? card, List<String>? expand, Map<String, String>? metadata, String? source_, }) async {
    final response = await postCustomersCustomerCardsWithHttpInfo(customer,  alipayAccount: alipayAccount, bankAccount: bankAccount, card: card, expand: expand, metadata: metadata, source_: source_, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentSource',) as PaymentSource;
    
    }
    return null;
  }

  /// <p>Update a specified source for a given customer.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] id (required):
  ///
  /// * [String] accountHolderName:
  ///   The name of the person or business that owns the bank account.
  ///
  /// * [String] accountHolderType:
  ///   The type of entity that holds the account. This can be either `individual` or `company`.
  ///
  /// * [String] addressCity:
  ///   City/District/Suburb/Town/Village.
  ///
  /// * [String] addressCountry:
  ///   Billing address country, if provided when creating card.
  ///
  /// * [String] addressLine1:
  ///   Address line 1 (Street address/PO Box/Company name).
  ///
  /// * [String] addressLine2:
  ///   Address line 2 (Apartment/Suite/Unit/Building).
  ///
  /// * [String] addressState:
  ///   State/County/Province/Region.
  ///
  /// * [String] addressZip:
  ///   ZIP or postal code.
  ///
  /// * [String] expMonth:
  ///   Two digit number representing the card’s expiration month.
  ///
  /// * [String] expYear:
  ///   Four digit number representing the card’s expiration year.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] name:
  ///   Cardholder name.
  ///
  /// * [Owner] owner:
  Future<Response> postCustomersCustomerCardsIdWithHttpInfo(String customer, String id, { String? accountHolderName, String? accountHolderType, String? addressCity, String? addressCountry, String? addressLine1, String? addressLine2, String? addressState, String? addressZip, String? expMonth, String? expYear, List<String>? expand, PostAccountsRequestMetadata? metadata, String? name, Owner? owner, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/cards/{id}'
      .replaceAll('{customer}', customer)
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (accountHolderName != null) {
      formParams[r'account_holder_name'] = parameterToString(accountHolderName);
    }
    if (accountHolderType != null) {
      formParams[r'account_holder_type'] = parameterToString(accountHolderType);
    }
    if (addressCity != null) {
      formParams[r'address_city'] = parameterToString(addressCity);
    }
    if (addressCountry != null) {
      formParams[r'address_country'] = parameterToString(addressCountry);
    }
    if (addressLine1 != null) {
      formParams[r'address_line1'] = parameterToString(addressLine1);
    }
    if (addressLine2 != null) {
      formParams[r'address_line2'] = parameterToString(addressLine2);
    }
    if (addressState != null) {
      formParams[r'address_state'] = parameterToString(addressState);
    }
    if (addressZip != null) {
      formParams[r'address_zip'] = parameterToString(addressZip);
    }
    if (expMonth != null) {
      formParams[r'exp_month'] = parameterToString(expMonth);
    }
    if (expYear != null) {
      formParams[r'exp_year'] = parameterToString(expYear);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (name != null) {
      formParams[r'name'] = parameterToString(name);
    }
    if (owner != null) {
      formParams[r'owner'] = parameterToString(owner);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// <p>Update a specified source for a given customer.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] id (required):
  ///
  /// * [String] accountHolderName:
  ///   The name of the person or business that owns the bank account.
  ///
  /// * [String] accountHolderType:
  ///   The type of entity that holds the account. This can be either `individual` or `company`.
  ///
  /// * [String] addressCity:
  ///   City/District/Suburb/Town/Village.
  ///
  /// * [String] addressCountry:
  ///   Billing address country, if provided when creating card.
  ///
  /// * [String] addressLine1:
  ///   Address line 1 (Street address/PO Box/Company name).
  ///
  /// * [String] addressLine2:
  ///   Address line 2 (Apartment/Suite/Unit/Building).
  ///
  /// * [String] addressState:
  ///   State/County/Province/Region.
  ///
  /// * [String] addressZip:
  ///   ZIP or postal code.
  ///
  /// * [String] expMonth:
  ///   Two digit number representing the card’s expiration month.
  ///
  /// * [String] expYear:
  ///   Four digit number representing the card’s expiration year.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] name:
  ///   Cardholder name.
  ///
  /// * [Owner] owner:
  Future<PostCustomersCustomerBankAccountsId200Response?> postCustomersCustomerCardsId(String customer, String id, { String? accountHolderName, String? accountHolderType, String? addressCity, String? addressCountry, String? addressLine1, String? addressLine2, String? addressState, String? addressZip, String? expMonth, String? expYear, List<String>? expand, PostAccountsRequestMetadata? metadata, String? name, Owner? owner, }) async {
    final response = await postCustomersCustomerCardsIdWithHttpInfo(customer, id,  accountHolderName: accountHolderName, accountHolderType: accountHolderType, addressCity: addressCity, addressCountry: addressCountry, addressLine1: addressLine1, addressLine2: addressLine2, addressState: addressState, addressZip: addressZip, expMonth: expMonth, expYear: expYear, expand: expand, metadata: metadata, name: name, owner: owner, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PostCustomersCustomerBankAccountsId200Response',) as PostCustomersCustomerBankAccountsId200Response;
    
    }
    return null;
  }

  /// Update a cash balance's settings
  ///
  /// <p>Changes the settings on a customer’s cash balance.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [BalanceSettingsParam1] settings:
  Future<Response> postCustomersCustomerCashBalanceWithHttpInfo(String customer, { List<String>? expand, BalanceSettingsParam1? settings, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/cash_balance'
      .replaceAll('{customer}', customer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (settings != null) {
      formParams[r'settings'] = parameterToString(settings);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a cash balance's settings
  ///
  /// <p>Changes the settings on a customer’s cash balance.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [BalanceSettingsParam1] settings:
  Future<CashBalance?> postCustomersCustomerCashBalance(String customer, { List<String>? expand, BalanceSettingsParam1? settings, }) async {
    final response = await postCustomersCustomerCashBalanceWithHttpInfo(customer,  expand: expand, settings: settings, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'CashBalance',) as CashBalance;
    
    }
    return null;
  }

  /// Create or retrieve funding instructions for a customer cash balance
  ///
  /// <p>Retrieve funding instructions for a customer cash balance. If funding instructions do not yet exist for the customer, new funding instructions will be created. If funding instructions have already been created for a given customer, the same funding instructions will be retrieved. In other words, we will return the same funding instructions each time.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [BankTransferParams] bankTransfer (required):
  ///
  /// * [String] currency (required):
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] fundingType (required):
  ///   The `funding_type` to get the instructions for.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postCustomersCustomerFundingInstructionsWithHttpInfo(String customer, BankTransferParams bankTransfer, String currency, String fundingType, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/funding_instructions'
      .replaceAll('{customer}', customer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (bankTransfer != null) {
      formParams[r'bank_transfer'] = parameterToString(bankTransfer);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (fundingType != null) {
      formParams[r'funding_type'] = parameterToString(fundingType);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create or retrieve funding instructions for a customer cash balance
  ///
  /// <p>Retrieve funding instructions for a customer cash balance. If funding instructions do not yet exist for the customer, new funding instructions will be created. If funding instructions have already been created for a given customer, the same funding instructions will be retrieved. In other words, we will return the same funding instructions each time.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [BankTransferParams] bankTransfer (required):
  ///
  /// * [String] currency (required):
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] fundingType (required):
  ///   The `funding_type` to get the instructions for.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<FundingInstructions?> postCustomersCustomerFundingInstructions(String customer, BankTransferParams bankTransfer, String currency, String fundingType, { List<String>? expand, }) async {
    final response = await postCustomersCustomerFundingInstructionsWithHttpInfo(customer, bankTransfer, currency, fundingType,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'FundingInstructions',) as FundingInstructions;
    
    }
    return null;
  }

  /// Create a card
  ///
  /// <p>When you create a new credit card, you must specify a customer or recipient on which to create it.</p>  <p>If the card’s owner has no default card, then the new card will become the default. However, if the owner already has a default, then it will not change. To change the default, you should <a href=\"/docs/api#update_customer\">update the customer</a> to have a new <code>default_source</code>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] alipayAccount:
  ///   A token returned by [Stripe.js](https://stripe.com/docs/js) representing the user’s Alipay account details.
  ///
  /// * [PostCustomersCustomerRequestBankAccount] bankAccount:
  ///
  /// * [PostChargesRequestCard] card:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] source_:
  ///   Please refer to full [documentation](https://stripe.com/docs/api) instead.
  Future<Response> postCustomersCustomerSourcesWithHttpInfo(String customer, { String? alipayAccount, PostCustomersCustomerRequestBankAccount? bankAccount, PostChargesRequestCard? card, List<String>? expand, Map<String, String>? metadata, String? source_, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/sources'
      .replaceAll('{customer}', customer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (alipayAccount != null) {
      formParams[r'alipay_account'] = parameterToString(alipayAccount);
    }
    if (bankAccount != null) {
      formParams[r'bank_account'] = parameterToString(bankAccount);
    }
    if (card != null) {
      formParams[r'card'] = parameterToString(card);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (source_ != null) {
      formParams[r'source'] = parameterToString(source_);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a card
  ///
  /// <p>When you create a new credit card, you must specify a customer or recipient on which to create it.</p>  <p>If the card’s owner has no default card, then the new card will become the default. However, if the owner already has a default, then it will not change. To change the default, you should <a href=\"/docs/api#update_customer\">update the customer</a> to have a new <code>default_source</code>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] alipayAccount:
  ///   A token returned by [Stripe.js](https://stripe.com/docs/js) representing the user’s Alipay account details.
  ///
  /// * [PostCustomersCustomerRequestBankAccount] bankAccount:
  ///
  /// * [PostChargesRequestCard] card:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] source_:
  ///   Please refer to full [documentation](https://stripe.com/docs/api) instead.
  Future<PaymentSource?> postCustomersCustomerSources(String customer, { String? alipayAccount, PostCustomersCustomerRequestBankAccount? bankAccount, PostChargesRequestCard? card, List<String>? expand, Map<String, String>? metadata, String? source_, }) async {
    final response = await postCustomersCustomerSourcesWithHttpInfo(customer,  alipayAccount: alipayAccount, bankAccount: bankAccount, card: card, expand: expand, metadata: metadata, source_: source_, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentSource',) as PaymentSource;
    
    }
    return null;
  }

  /// <p>Update a specified source for a given customer.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] id (required):
  ///
  /// * [String] accountHolderName:
  ///   The name of the person or business that owns the bank account.
  ///
  /// * [String] accountHolderType:
  ///   The type of entity that holds the account. This can be either `individual` or `company`.
  ///
  /// * [String] addressCity:
  ///   City/District/Suburb/Town/Village.
  ///
  /// * [String] addressCountry:
  ///   Billing address country, if provided when creating card.
  ///
  /// * [String] addressLine1:
  ///   Address line 1 (Street address/PO Box/Company name).
  ///
  /// * [String] addressLine2:
  ///   Address line 2 (Apartment/Suite/Unit/Building).
  ///
  /// * [String] addressState:
  ///   State/County/Province/Region.
  ///
  /// * [String] addressZip:
  ///   ZIP or postal code.
  ///
  /// * [String] expMonth:
  ///   Two digit number representing the card’s expiration month.
  ///
  /// * [String] expYear:
  ///   Four digit number representing the card’s expiration year.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] name:
  ///   Cardholder name.
  ///
  /// * [Owner] owner:
  Future<Response> postCustomersCustomerSourcesIdWithHttpInfo(String customer, String id, { String? accountHolderName, String? accountHolderType, String? addressCity, String? addressCountry, String? addressLine1, String? addressLine2, String? addressState, String? addressZip, String? expMonth, String? expYear, List<String>? expand, PostAccountsRequestMetadata? metadata, String? name, Owner? owner, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/sources/{id}'
      .replaceAll('{customer}', customer)
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (accountHolderName != null) {
      formParams[r'account_holder_name'] = parameterToString(accountHolderName);
    }
    if (accountHolderType != null) {
      formParams[r'account_holder_type'] = parameterToString(accountHolderType);
    }
    if (addressCity != null) {
      formParams[r'address_city'] = parameterToString(addressCity);
    }
    if (addressCountry != null) {
      formParams[r'address_country'] = parameterToString(addressCountry);
    }
    if (addressLine1 != null) {
      formParams[r'address_line1'] = parameterToString(addressLine1);
    }
    if (addressLine2 != null) {
      formParams[r'address_line2'] = parameterToString(addressLine2);
    }
    if (addressState != null) {
      formParams[r'address_state'] = parameterToString(addressState);
    }
    if (addressZip != null) {
      formParams[r'address_zip'] = parameterToString(addressZip);
    }
    if (expMonth != null) {
      formParams[r'exp_month'] = parameterToString(expMonth);
    }
    if (expYear != null) {
      formParams[r'exp_year'] = parameterToString(expYear);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (name != null) {
      formParams[r'name'] = parameterToString(name);
    }
    if (owner != null) {
      formParams[r'owner'] = parameterToString(owner);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// <p>Update a specified source for a given customer.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] id (required):
  ///
  /// * [String] accountHolderName:
  ///   The name of the person or business that owns the bank account.
  ///
  /// * [String] accountHolderType:
  ///   The type of entity that holds the account. This can be either `individual` or `company`.
  ///
  /// * [String] addressCity:
  ///   City/District/Suburb/Town/Village.
  ///
  /// * [String] addressCountry:
  ///   Billing address country, if provided when creating card.
  ///
  /// * [String] addressLine1:
  ///   Address line 1 (Street address/PO Box/Company name).
  ///
  /// * [String] addressLine2:
  ///   Address line 2 (Apartment/Suite/Unit/Building).
  ///
  /// * [String] addressState:
  ///   State/County/Province/Region.
  ///
  /// * [String] addressZip:
  ///   ZIP or postal code.
  ///
  /// * [String] expMonth:
  ///   Two digit number representing the card’s expiration month.
  ///
  /// * [String] expYear:
  ///   Four digit number representing the card’s expiration year.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] name:
  ///   Cardholder name.
  ///
  /// * [Owner] owner:
  Future<PostCustomersCustomerBankAccountsId200Response?> postCustomersCustomerSourcesId(String customer, String id, { String? accountHolderName, String? accountHolderType, String? addressCity, String? addressCountry, String? addressLine1, String? addressLine2, String? addressState, String? addressZip, String? expMonth, String? expYear, List<String>? expand, PostAccountsRequestMetadata? metadata, String? name, Owner? owner, }) async {
    final response = await postCustomersCustomerSourcesIdWithHttpInfo(customer, id,  accountHolderName: accountHolderName, accountHolderType: accountHolderType, addressCity: addressCity, addressCountry: addressCountry, addressLine1: addressLine1, addressLine2: addressLine2, addressState: addressState, addressZip: addressZip, expMonth: expMonth, expYear: expYear, expand: expand, metadata: metadata, name: name, owner: owner, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PostCustomersCustomerBankAccountsId200Response',) as PostCustomersCustomerBankAccountsId200Response;
    
    }
    return null;
  }

  /// Verify a bank account
  ///
  /// <p>Verify a specified bank account for a given customer.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] id (required):
  ///
  /// * [List<int>] amounts:
  ///   Two positive integers, in *cents*, equal to the values of the microdeposits sent to the bank account.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postCustomersCustomerSourcesIdVerifyWithHttpInfo(String customer, String id, { List<int>? amounts, List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/sources/{id}/verify'
      .replaceAll('{customer}', customer)
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amounts != null) {
      formParams[r'amounts'] = parameterToString(amounts);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Verify a bank account
  ///
  /// <p>Verify a specified bank account for a given customer.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] id (required):
  ///
  /// * [List<int>] amounts:
  ///   Two positive integers, in *cents*, equal to the values of the microdeposits sent to the bank account.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<BankAccount?> postCustomersCustomerSourcesIdVerify(String customer, String id, { List<int>? amounts, List<String>? expand, }) async {
    final response = await postCustomersCustomerSourcesIdVerifyWithHttpInfo(customer, id,  amounts: amounts, expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'BankAccount',) as BankAccount;
    
    }
    return null;
  }

  /// Create a subscription
  ///
  /// <p>Creates a new subscription on an existing customer.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [List<AddInvoiceItemEntry>] addInvoiceItems:
  ///   A list of prices and quantities that will generate invoice items appended to the next invoice for this subscription. You may pass up to 20 items.
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestApplicationFeePercent] applicationFeePercent:
  ///
  /// * [AutomaticTaxConfig] automaticTax:
  ///
  /// * [int] backdateStartDate:
  ///   A past timestamp to backdate the subscription's start date to. If set, the first invoice will contain line items for the timespan between the start date and the current time. Can be combined with trials and the billing cycle anchor.
  ///
  /// * [int] billingCycleAnchor:
  ///   A future timestamp in UTC format to anchor the subscription's [billing cycle](https://stripe.com/docs/subscriptions/billing-cycle). The anchor is the reference point that aligns future billing cycle dates. It sets the day of week for `week` intervals, the day of month for `month` and `year` intervals, and the month of year for `year` intervals.
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestBillingThresholds] billingThresholds:
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestCancelAt] cancelAt:
  ///
  /// * [bool] cancelAtPeriodEnd:
  ///   Indicate whether this subscription should cancel at the end of the current period (`current_period_end`). Defaults to `false`.
  ///
  /// * [String] collectionMethod:
  ///   Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay this subscription at the end of the cycle using the default source attached to the customer. When sending an invoice, Stripe will email your customer an invoice with payment instructions and mark the subscription as `active`. Defaults to `charge_automatically`.
  ///
  /// * [String] currency:
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [int] daysUntilDue:
  ///   Number of days a customer has to pay invoices generated by this subscription. Valid only for subscriptions where `collection_method` is set to `send_invoice`.
  ///
  /// * [String] defaultPaymentMethod:
  ///   ID of the default payment method for the subscription. It must belong to the customer associated with the subscription. This takes precedence over `default_source`. If neither are set, invoices will use the customer's [invoice_settings.default_payment_method](https://stripe.com/docs/api/customers/object#customer_object-invoice_settings-default_payment_method) or [default_source](https://stripe.com/docs/api/customers/object#customer_object-default_source).
  ///
  /// * [String] defaultSource:
  ///   ID of the default payment source for the subscription. It must belong to the customer associated with the subscription and be in a chargeable state. If `default_payment_method` is also set, `default_payment_method` will take precedence. If neither are set, invoices will use the customer's [invoice_settings.default_payment_method](https://stripe.com/docs/api/customers/object#customer_object-invoice_settings-default_payment_method) or [default_source](https://stripe.com/docs/api/customers/object#customer_object-default_source).
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestDefaultTaxRates] defaultTaxRates:
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestDiscounts] discounts:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [InvoiceSettingsParam] invoiceSettings:
  ///
  /// * [List<SubscriptionItemCreateParams>] items:
  ///   A list of up to 20 subscription items, each with an attached price.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [bool] offSession:
  ///   Indicates if a customer is on or off-session while an invoice payment is attempted. Defaults to `false` (on-session).
  ///
  /// * [String] paymentBehavior:
  ///   Only applies to subscriptions with `collection_method=charge_automatically`.  Use `allow_incomplete` to create Subscriptions with `status=incomplete` if the first invoice can't be paid. Creating Subscriptions with this status allows you to manage scenarios where additional customer actions are needed to pay a subscription's invoice. For example, SCA regulation may require 3DS authentication to complete payment. See the [SCA Migration Guide](https://stripe.com/docs/billing/migration/strong-customer-authentication) for Billing to learn more. This is the default behavior.  Use `default_incomplete` to create Subscriptions with `status=incomplete` when the first invoice requires payment, otherwise start as active. Subscriptions transition to `status=active` when successfully confirming the PaymentIntent on the first invoice. This allows simpler management of scenarios where additional customer actions are needed to pay a subscription’s invoice, such as failed payments, [SCA regulation](https://stripe.com/docs/billing/migration/strong-customer-authentication), or collecting a mandate for a bank debit payment method. If the PaymentIntent is not confirmed within 23 hours Subscriptions transition to `status=incomplete_expired`, which is a terminal state.  Use `error_if_incomplete` if you want Stripe to return an HTTP 402 status code if a subscription's first invoice can't be paid. For example, if a payment method requires 3DS authentication due to SCA regulation and further customer action is needed, this parameter doesn't create a Subscription and returns an error instead. This was the default behavior for API versions prior to 2019-03-14. See the [changelog](https://stripe.com/docs/upgrades#2019-03-14) to learn more.  `pending_if_incomplete` is only used with updates and cannot be passed when creating a Subscription.  Subscriptions with `collection_method=send_invoice` are automatically activated regardless of the first Invoice status.
  ///
  /// * [PaymentSettings] paymentSettings:
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestPendingInvoiceItemInterval] pendingInvoiceItemInterval:
  ///
  /// * [String] prorationBehavior:
  ///   Determines how to handle [prorations](https://stripe.com/docs/billing/subscriptions/prorations) resulting from the `billing_cycle_anchor`. If no value is passed, the default is `create_prorations`.
  ///
  /// * [TransferDataSpecs3] transferData:
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestTrialEnd] trialEnd:
  ///
  /// * [bool] trialFromPlan:
  ///   Indicates if a plan's `trial_period_days` should be applied to the subscription. Setting `trial_end` per subscription is preferred, and this defaults to `false`. Setting this flag to `true` together with `trial_end` is not allowed. See [Using trial periods on subscriptions](https://stripe.com/docs/billing/subscriptions/trials) to learn more.
  ///
  /// * [int] trialPeriodDays:
  ///   Integer representing the number of trial period days before the customer is charged for the first time. This will always overwrite any trials that might apply via a subscribed plan. See [Using trial periods on subscriptions](https://stripe.com/docs/billing/subscriptions/trials) to learn more.
  ///
  /// * [TrialSettingsConfig1] trialSettings:
  Future<Response> postCustomersCustomerSubscriptionsWithHttpInfo(String customer, { List<AddInvoiceItemEntry>? addInvoiceItems, PostCustomersCustomerSubscriptionsRequestApplicationFeePercent? applicationFeePercent, AutomaticTaxConfig? automaticTax, int? backdateStartDate, int? billingCycleAnchor, PostCustomersCustomerSubscriptionsRequestBillingThresholds? billingThresholds, PostCustomersCustomerSubscriptionsRequestCancelAt? cancelAt, bool? cancelAtPeriodEnd, String? collectionMethod, String? currency, int? daysUntilDue, String? defaultPaymentMethod, String? defaultSource, PostCustomersCustomerSubscriptionsRequestDefaultTaxRates? defaultTaxRates, PostCustomersCustomerSubscriptionsRequestDiscounts? discounts, List<String>? expand, InvoiceSettingsParam? invoiceSettings, List<SubscriptionItemCreateParams>? items, PostAccountsRequestMetadata? metadata, bool? offSession, String? paymentBehavior, PaymentSettings? paymentSettings, PostCustomersCustomerSubscriptionsRequestPendingInvoiceItemInterval? pendingInvoiceItemInterval, String? prorationBehavior, TransferDataSpecs3? transferData, PostCustomersCustomerSubscriptionsRequestTrialEnd? trialEnd, bool? trialFromPlan, int? trialPeriodDays, TrialSettingsConfig1? trialSettings, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/subscriptions'
      .replaceAll('{customer}', customer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (addInvoiceItems != null) {
      formParams[r'add_invoice_items'] = parameterToString(addInvoiceItems);
    }
    if (applicationFeePercent != null) {
      formParams[r'application_fee_percent'] = parameterToString(applicationFeePercent);
    }
    if (automaticTax != null) {
      formParams[r'automatic_tax'] = parameterToString(automaticTax);
    }
    if (backdateStartDate != null) {
      formParams[r'backdate_start_date'] = parameterToString(backdateStartDate);
    }
    if (billingCycleAnchor != null) {
      formParams[r'billing_cycle_anchor'] = parameterToString(billingCycleAnchor);
    }
    if (billingThresholds != null) {
      formParams[r'billing_thresholds'] = parameterToString(billingThresholds);
    }
    if (cancelAt != null) {
      formParams[r'cancel_at'] = parameterToString(cancelAt);
    }
    if (cancelAtPeriodEnd != null) {
      formParams[r'cancel_at_period_end'] = parameterToString(cancelAtPeriodEnd);
    }
    if (collectionMethod != null) {
      formParams[r'collection_method'] = parameterToString(collectionMethod);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (daysUntilDue != null) {
      formParams[r'days_until_due'] = parameterToString(daysUntilDue);
    }
    if (defaultPaymentMethod != null) {
      formParams[r'default_payment_method'] = parameterToString(defaultPaymentMethod);
    }
    if (defaultSource != null) {
      formParams[r'default_source'] = parameterToString(defaultSource);
    }
    if (defaultTaxRates != null) {
      formParams[r'default_tax_rates'] = parameterToString(defaultTaxRates);
    }
    if (discounts != null) {
      formParams[r'discounts'] = parameterToString(discounts);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (invoiceSettings != null) {
      formParams[r'invoice_settings'] = parameterToString(invoiceSettings);
    }
    if (items != null) {
      formParams[r'items'] = parameterToString(items);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (offSession != null) {
      formParams[r'off_session'] = parameterToString(offSession);
    }
    if (paymentBehavior != null) {
      formParams[r'payment_behavior'] = parameterToString(paymentBehavior);
    }
    if (paymentSettings != null) {
      formParams[r'payment_settings'] = parameterToString(paymentSettings);
    }
    if (pendingInvoiceItemInterval != null) {
      formParams[r'pending_invoice_item_interval'] = parameterToString(pendingInvoiceItemInterval);
    }
    if (prorationBehavior != null) {
      formParams[r'proration_behavior'] = parameterToString(prorationBehavior);
    }
    if (transferData != null) {
      formParams[r'transfer_data'] = parameterToString(transferData);
    }
    if (trialEnd != null) {
      formParams[r'trial_end'] = parameterToString(trialEnd);
    }
    if (trialFromPlan != null) {
      formParams[r'trial_from_plan'] = parameterToString(trialFromPlan);
    }
    if (trialPeriodDays != null) {
      formParams[r'trial_period_days'] = parameterToString(trialPeriodDays);
    }
    if (trialSettings != null) {
      formParams[r'trial_settings'] = parameterToString(trialSettings);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a subscription
  ///
  /// <p>Creates a new subscription on an existing customer.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [List<AddInvoiceItemEntry>] addInvoiceItems:
  ///   A list of prices and quantities that will generate invoice items appended to the next invoice for this subscription. You may pass up to 20 items.
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestApplicationFeePercent] applicationFeePercent:
  ///
  /// * [AutomaticTaxConfig] automaticTax:
  ///
  /// * [int] backdateStartDate:
  ///   A past timestamp to backdate the subscription's start date to. If set, the first invoice will contain line items for the timespan between the start date and the current time. Can be combined with trials and the billing cycle anchor.
  ///
  /// * [int] billingCycleAnchor:
  ///   A future timestamp in UTC format to anchor the subscription's [billing cycle](https://stripe.com/docs/subscriptions/billing-cycle). The anchor is the reference point that aligns future billing cycle dates. It sets the day of week for `week` intervals, the day of month for `month` and `year` intervals, and the month of year for `year` intervals.
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestBillingThresholds] billingThresholds:
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestCancelAt] cancelAt:
  ///
  /// * [bool] cancelAtPeriodEnd:
  ///   Indicate whether this subscription should cancel at the end of the current period (`current_period_end`). Defaults to `false`.
  ///
  /// * [String] collectionMethod:
  ///   Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay this subscription at the end of the cycle using the default source attached to the customer. When sending an invoice, Stripe will email your customer an invoice with payment instructions and mark the subscription as `active`. Defaults to `charge_automatically`.
  ///
  /// * [String] currency:
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [int] daysUntilDue:
  ///   Number of days a customer has to pay invoices generated by this subscription. Valid only for subscriptions where `collection_method` is set to `send_invoice`.
  ///
  /// * [String] defaultPaymentMethod:
  ///   ID of the default payment method for the subscription. It must belong to the customer associated with the subscription. This takes precedence over `default_source`. If neither are set, invoices will use the customer's [invoice_settings.default_payment_method](https://stripe.com/docs/api/customers/object#customer_object-invoice_settings-default_payment_method) or [default_source](https://stripe.com/docs/api/customers/object#customer_object-default_source).
  ///
  /// * [String] defaultSource:
  ///   ID of the default payment source for the subscription. It must belong to the customer associated with the subscription and be in a chargeable state. If `default_payment_method` is also set, `default_payment_method` will take precedence. If neither are set, invoices will use the customer's [invoice_settings.default_payment_method](https://stripe.com/docs/api/customers/object#customer_object-invoice_settings-default_payment_method) or [default_source](https://stripe.com/docs/api/customers/object#customer_object-default_source).
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestDefaultTaxRates] defaultTaxRates:
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestDiscounts] discounts:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [InvoiceSettingsParam] invoiceSettings:
  ///
  /// * [List<SubscriptionItemCreateParams>] items:
  ///   A list of up to 20 subscription items, each with an attached price.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [bool] offSession:
  ///   Indicates if a customer is on or off-session while an invoice payment is attempted. Defaults to `false` (on-session).
  ///
  /// * [String] paymentBehavior:
  ///   Only applies to subscriptions with `collection_method=charge_automatically`.  Use `allow_incomplete` to create Subscriptions with `status=incomplete` if the first invoice can't be paid. Creating Subscriptions with this status allows you to manage scenarios where additional customer actions are needed to pay a subscription's invoice. For example, SCA regulation may require 3DS authentication to complete payment. See the [SCA Migration Guide](https://stripe.com/docs/billing/migration/strong-customer-authentication) for Billing to learn more. This is the default behavior.  Use `default_incomplete` to create Subscriptions with `status=incomplete` when the first invoice requires payment, otherwise start as active. Subscriptions transition to `status=active` when successfully confirming the PaymentIntent on the first invoice. This allows simpler management of scenarios where additional customer actions are needed to pay a subscription’s invoice, such as failed payments, [SCA regulation](https://stripe.com/docs/billing/migration/strong-customer-authentication), or collecting a mandate for a bank debit payment method. If the PaymentIntent is not confirmed within 23 hours Subscriptions transition to `status=incomplete_expired`, which is a terminal state.  Use `error_if_incomplete` if you want Stripe to return an HTTP 402 status code if a subscription's first invoice can't be paid. For example, if a payment method requires 3DS authentication due to SCA regulation and further customer action is needed, this parameter doesn't create a Subscription and returns an error instead. This was the default behavior for API versions prior to 2019-03-14. See the [changelog](https://stripe.com/docs/upgrades#2019-03-14) to learn more.  `pending_if_incomplete` is only used with updates and cannot be passed when creating a Subscription.  Subscriptions with `collection_method=send_invoice` are automatically activated regardless of the first Invoice status.
  ///
  /// * [PaymentSettings] paymentSettings:
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestPendingInvoiceItemInterval] pendingInvoiceItemInterval:
  ///
  /// * [String] prorationBehavior:
  ///   Determines how to handle [prorations](https://stripe.com/docs/billing/subscriptions/prorations) resulting from the `billing_cycle_anchor`. If no value is passed, the default is `create_prorations`.
  ///
  /// * [TransferDataSpecs3] transferData:
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestTrialEnd] trialEnd:
  ///
  /// * [bool] trialFromPlan:
  ///   Indicates if a plan's `trial_period_days` should be applied to the subscription. Setting `trial_end` per subscription is preferred, and this defaults to `false`. Setting this flag to `true` together with `trial_end` is not allowed. See [Using trial periods on subscriptions](https://stripe.com/docs/billing/subscriptions/trials) to learn more.
  ///
  /// * [int] trialPeriodDays:
  ///   Integer representing the number of trial period days before the customer is charged for the first time. This will always overwrite any trials that might apply via a subscribed plan. See [Using trial periods on subscriptions](https://stripe.com/docs/billing/subscriptions/trials) to learn more.
  ///
  /// * [TrialSettingsConfig1] trialSettings:
  Future<Subscription?> postCustomersCustomerSubscriptions(String customer, { List<AddInvoiceItemEntry>? addInvoiceItems, PostCustomersCustomerSubscriptionsRequestApplicationFeePercent? applicationFeePercent, AutomaticTaxConfig? automaticTax, int? backdateStartDate, int? billingCycleAnchor, PostCustomersCustomerSubscriptionsRequestBillingThresholds? billingThresholds, PostCustomersCustomerSubscriptionsRequestCancelAt? cancelAt, bool? cancelAtPeriodEnd, String? collectionMethod, String? currency, int? daysUntilDue, String? defaultPaymentMethod, String? defaultSource, PostCustomersCustomerSubscriptionsRequestDefaultTaxRates? defaultTaxRates, PostCustomersCustomerSubscriptionsRequestDiscounts? discounts, List<String>? expand, InvoiceSettingsParam? invoiceSettings, List<SubscriptionItemCreateParams>? items, PostAccountsRequestMetadata? metadata, bool? offSession, String? paymentBehavior, PaymentSettings? paymentSettings, PostCustomersCustomerSubscriptionsRequestPendingInvoiceItemInterval? pendingInvoiceItemInterval, String? prorationBehavior, TransferDataSpecs3? transferData, PostCustomersCustomerSubscriptionsRequestTrialEnd? trialEnd, bool? trialFromPlan, int? trialPeriodDays, TrialSettingsConfig1? trialSettings, }) async {
    final response = await postCustomersCustomerSubscriptionsWithHttpInfo(customer,  addInvoiceItems: addInvoiceItems, applicationFeePercent: applicationFeePercent, automaticTax: automaticTax, backdateStartDate: backdateStartDate, billingCycleAnchor: billingCycleAnchor, billingThresholds: billingThresholds, cancelAt: cancelAt, cancelAtPeriodEnd: cancelAtPeriodEnd, collectionMethod: collectionMethod, currency: currency, daysUntilDue: daysUntilDue, defaultPaymentMethod: defaultPaymentMethod, defaultSource: defaultSource, defaultTaxRates: defaultTaxRates, discounts: discounts, expand: expand, invoiceSettings: invoiceSettings, items: items, metadata: metadata, offSession: offSession, paymentBehavior: paymentBehavior, paymentSettings: paymentSettings, pendingInvoiceItemInterval: pendingInvoiceItemInterval, prorationBehavior: prorationBehavior, transferData: transferData, trialEnd: trialEnd, trialFromPlan: trialFromPlan, trialPeriodDays: trialPeriodDays, trialSettings: trialSettings, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Subscription',) as Subscription;
    
    }
    return null;
  }

  /// Update a subscription on a customer
  ///
  /// <p>Updates an existing subscription on a customer to match the specified parameters. When changing plans or quantities, we will optionally prorate the price we charge next month to make up for any price changes. To preview how the proration will be calculated, use the <a href=\"#upcoming_invoice\">upcoming invoice</a> endpoint.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] subscriptionExposedId (required):
  ///
  /// * [List<AddInvoiceItemEntry>] addInvoiceItems:
  ///   A list of prices and quantities that will generate invoice items appended to the next invoice for this subscription. You may pass up to 20 items.
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestApplicationFeePercent] applicationFeePercent:
  ///
  /// * [AutomaticTaxConfig1] automaticTax:
  ///
  /// * [String] billingCycleAnchor:
  ///   Either `now` or `unchanged`. Setting the value to `now` resets the subscription's billing cycle anchor to the current time. For more information, see the billing cycle [documentation](https://stripe.com/docs/billing/subscriptions/billing-cycle).
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestBillingThresholds] billingThresholds:
  ///
  /// * [PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestCancelAt] cancelAt:
  ///
  /// * [bool] cancelAtPeriodEnd:
  ///   Indicate whether this subscription should cancel at the end of the current period (`current_period_end`). Defaults to `false`.
  ///
  /// * [CancellationDetailsParam] cancellationDetails:
  ///
  /// * [String] collectionMethod:
  ///   Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay this subscription at the end of the cycle using the default source attached to the customer. When sending an invoice, Stripe will email your customer an invoice with payment instructions and mark the subscription as `active`. Defaults to `charge_automatically`.
  ///
  /// * [int] daysUntilDue:
  ///   Number of days a customer has to pay invoices generated by this subscription. Valid only for subscriptions where `collection_method` is set to `send_invoice`.
  ///
  /// * [String] defaultPaymentMethod:
  ///   ID of the default payment method for the subscription. It must belong to the customer associated with the subscription. This takes precedence over `default_source`. If neither are set, invoices will use the customer's [invoice_settings.default_payment_method](https://stripe.com/docs/api/customers/object#customer_object-invoice_settings-default_payment_method) or [default_source](https://stripe.com/docs/api/customers/object#customer_object-default_source).
  ///
  /// * [PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestDefaultSource] defaultSource:
  ///
  /// * [PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestDefaultTaxRates] defaultTaxRates:
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestDiscounts] discounts:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [InvoiceSettingsParam] invoiceSettings:
  ///
  /// * [List<SubscriptionItemUpdateParams>] items:
  ///   A list of up to 20 subscription items, each with an attached price.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [bool] offSession:
  ///   Indicates if a customer is on or off-session while an invoice payment is attempted. Defaults to `false` (on-session).
  ///
  /// * [PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestPauseCollection] pauseCollection:
  ///
  /// * [String] paymentBehavior:
  ///   Use `allow_incomplete` to transition the subscription to `status=past_due` if a payment is required but cannot be paid. This allows you to manage scenarios where additional user actions are needed to pay a subscription's invoice. For example, SCA regulation may require 3DS authentication to complete payment. See the [SCA Migration Guide](https://stripe.com/docs/billing/migration/strong-customer-authentication) for Billing to learn more. This is the default behavior.  Use `default_incomplete` to transition the subscription to `status=past_due` when payment is required and await explicit confirmation of the invoice's payment intent. This allows simpler management of scenarios where additional user actions are needed to pay a subscription’s invoice. Such as failed payments, [SCA regulation](https://stripe.com/docs/billing/migration/strong-customer-authentication), or collecting a mandate for a bank debit payment method.  Use `pending_if_incomplete` to update the subscription using [pending updates](https://stripe.com/docs/billing/subscriptions/pending-updates). When you use `pending_if_incomplete` you can only pass the parameters [supported by pending updates](https://stripe.com/docs/billing/pending-updates-reference#supported-attributes).  Use `error_if_incomplete` if you want Stripe to return an HTTP 402 status code if a subscription's invoice cannot be paid. For example, if a payment method requires 3DS authentication due to SCA regulation and further user action is needed, this parameter does not update the subscription and returns an error instead. This was the default behavior for API versions prior to 2019-03-14. See the [changelog](https://docs.stripe.com/changelog/2019-03-14) to learn more.
  ///
  /// * [PaymentSettings] paymentSettings:
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestPendingInvoiceItemInterval] pendingInvoiceItemInterval:
  ///
  /// * [String] prorationBehavior:
  ///   Determines how to handle [prorations](https://stripe.com/docs/billing/subscriptions/prorations) when the billing cycle changes (e.g., when switching plans, resetting `billing_cycle_anchor=now`, or starting a trial), or if an item's `quantity` changes. The default value is `create_prorations`.
  ///
  /// * [int] prorationDate:
  ///   If set, prorations will be calculated as though the subscription was updated at the given time. This can be used to apply exactly the same prorations that were previewed with the [create preview](https://stripe.com/docs/api/invoices/create_preview) endpoint. `proration_date` can also be used to implement custom proration logic, such as prorating by day instead of by second, by providing the time that you wish to use for proration calculations.
  ///
  /// * [PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestTransferData] transferData:
  ///
  /// * [PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestTrialEnd] trialEnd:
  ///
  /// * [bool] trialFromPlan:
  ///   Indicates if a plan's `trial_period_days` should be applied to the subscription. Setting `trial_end` per subscription is preferred, and this defaults to `false`. Setting this flag to `true` together with `trial_end` is not allowed. See [Using trial periods on subscriptions](https://stripe.com/docs/billing/subscriptions/trials) to learn more.
  ///
  /// * [TrialSettingsConfig1] trialSettings:
  Future<Response> postCustomersCustomerSubscriptionsSubscriptionExposedIdWithHttpInfo(String customer, String subscriptionExposedId, { List<AddInvoiceItemEntry>? addInvoiceItems, PostCustomersCustomerSubscriptionsRequestApplicationFeePercent? applicationFeePercent, AutomaticTaxConfig1? automaticTax, String? billingCycleAnchor, PostCustomersCustomerSubscriptionsRequestBillingThresholds? billingThresholds, PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestCancelAt? cancelAt, bool? cancelAtPeriodEnd, CancellationDetailsParam? cancellationDetails, String? collectionMethod, int? daysUntilDue, String? defaultPaymentMethod, PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestDefaultSource? defaultSource, PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestDefaultTaxRates? defaultTaxRates, PostCustomersCustomerSubscriptionsRequestDiscounts? discounts, List<String>? expand, InvoiceSettingsParam? invoiceSettings, List<SubscriptionItemUpdateParams>? items, PostAccountsRequestMetadata? metadata, bool? offSession, PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestPauseCollection? pauseCollection, String? paymentBehavior, PaymentSettings? paymentSettings, PostCustomersCustomerSubscriptionsRequestPendingInvoiceItemInterval? pendingInvoiceItemInterval, String? prorationBehavior, int? prorationDate, PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestTransferData? transferData, PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestTrialEnd? trialEnd, bool? trialFromPlan, TrialSettingsConfig1? trialSettings, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/subscriptions/{subscription_exposed_id}'
      .replaceAll('{customer}', customer)
      .replaceAll('{subscription_exposed_id}', subscriptionExposedId);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (addInvoiceItems != null) {
      formParams[r'add_invoice_items'] = parameterToString(addInvoiceItems);
    }
    if (applicationFeePercent != null) {
      formParams[r'application_fee_percent'] = parameterToString(applicationFeePercent);
    }
    if (automaticTax != null) {
      formParams[r'automatic_tax'] = parameterToString(automaticTax);
    }
    if (billingCycleAnchor != null) {
      formParams[r'billing_cycle_anchor'] = parameterToString(billingCycleAnchor);
    }
    if (billingThresholds != null) {
      formParams[r'billing_thresholds'] = parameterToString(billingThresholds);
    }
    if (cancelAt != null) {
      formParams[r'cancel_at'] = parameterToString(cancelAt);
    }
    if (cancelAtPeriodEnd != null) {
      formParams[r'cancel_at_period_end'] = parameterToString(cancelAtPeriodEnd);
    }
    if (cancellationDetails != null) {
      formParams[r'cancellation_details'] = parameterToString(cancellationDetails);
    }
    if (collectionMethod != null) {
      formParams[r'collection_method'] = parameterToString(collectionMethod);
    }
    if (daysUntilDue != null) {
      formParams[r'days_until_due'] = parameterToString(daysUntilDue);
    }
    if (defaultPaymentMethod != null) {
      formParams[r'default_payment_method'] = parameterToString(defaultPaymentMethod);
    }
    if (defaultSource != null) {
      formParams[r'default_source'] = parameterToString(defaultSource);
    }
    if (defaultTaxRates != null) {
      formParams[r'default_tax_rates'] = parameterToString(defaultTaxRates);
    }
    if (discounts != null) {
      formParams[r'discounts'] = parameterToString(discounts);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (invoiceSettings != null) {
      formParams[r'invoice_settings'] = parameterToString(invoiceSettings);
    }
    if (items != null) {
      formParams[r'items'] = parameterToString(items);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (offSession != null) {
      formParams[r'off_session'] = parameterToString(offSession);
    }
    if (pauseCollection != null) {
      formParams[r'pause_collection'] = parameterToString(pauseCollection);
    }
    if (paymentBehavior != null) {
      formParams[r'payment_behavior'] = parameterToString(paymentBehavior);
    }
    if (paymentSettings != null) {
      formParams[r'payment_settings'] = parameterToString(paymentSettings);
    }
    if (pendingInvoiceItemInterval != null) {
      formParams[r'pending_invoice_item_interval'] = parameterToString(pendingInvoiceItemInterval);
    }
    if (prorationBehavior != null) {
      formParams[r'proration_behavior'] = parameterToString(prorationBehavior);
    }
    if (prorationDate != null) {
      formParams[r'proration_date'] = parameterToString(prorationDate);
    }
    if (transferData != null) {
      formParams[r'transfer_data'] = parameterToString(transferData);
    }
    if (trialEnd != null) {
      formParams[r'trial_end'] = parameterToString(trialEnd);
    }
    if (trialFromPlan != null) {
      formParams[r'trial_from_plan'] = parameterToString(trialFromPlan);
    }
    if (trialSettings != null) {
      formParams[r'trial_settings'] = parameterToString(trialSettings);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a subscription on a customer
  ///
  /// <p>Updates an existing subscription on a customer to match the specified parameters. When changing plans or quantities, we will optionally prorate the price we charge next month to make up for any price changes. To preview how the proration will be calculated, use the <a href=\"#upcoming_invoice\">upcoming invoice</a> endpoint.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] subscriptionExposedId (required):
  ///
  /// * [List<AddInvoiceItemEntry>] addInvoiceItems:
  ///   A list of prices and quantities that will generate invoice items appended to the next invoice for this subscription. You may pass up to 20 items.
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestApplicationFeePercent] applicationFeePercent:
  ///
  /// * [AutomaticTaxConfig1] automaticTax:
  ///
  /// * [String] billingCycleAnchor:
  ///   Either `now` or `unchanged`. Setting the value to `now` resets the subscription's billing cycle anchor to the current time. For more information, see the billing cycle [documentation](https://stripe.com/docs/billing/subscriptions/billing-cycle).
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestBillingThresholds] billingThresholds:
  ///
  /// * [PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestCancelAt] cancelAt:
  ///
  /// * [bool] cancelAtPeriodEnd:
  ///   Indicate whether this subscription should cancel at the end of the current period (`current_period_end`). Defaults to `false`.
  ///
  /// * [CancellationDetailsParam] cancellationDetails:
  ///
  /// * [String] collectionMethod:
  ///   Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay this subscription at the end of the cycle using the default source attached to the customer. When sending an invoice, Stripe will email your customer an invoice with payment instructions and mark the subscription as `active`. Defaults to `charge_automatically`.
  ///
  /// * [int] daysUntilDue:
  ///   Number of days a customer has to pay invoices generated by this subscription. Valid only for subscriptions where `collection_method` is set to `send_invoice`.
  ///
  /// * [String] defaultPaymentMethod:
  ///   ID of the default payment method for the subscription. It must belong to the customer associated with the subscription. This takes precedence over `default_source`. If neither are set, invoices will use the customer's [invoice_settings.default_payment_method](https://stripe.com/docs/api/customers/object#customer_object-invoice_settings-default_payment_method) or [default_source](https://stripe.com/docs/api/customers/object#customer_object-default_source).
  ///
  /// * [PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestDefaultSource] defaultSource:
  ///
  /// * [PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestDefaultTaxRates] defaultTaxRates:
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestDiscounts] discounts:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [InvoiceSettingsParam] invoiceSettings:
  ///
  /// * [List<SubscriptionItemUpdateParams>] items:
  ///   A list of up to 20 subscription items, each with an attached price.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [bool] offSession:
  ///   Indicates if a customer is on or off-session while an invoice payment is attempted. Defaults to `false` (on-session).
  ///
  /// * [PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestPauseCollection] pauseCollection:
  ///
  /// * [String] paymentBehavior:
  ///   Use `allow_incomplete` to transition the subscription to `status=past_due` if a payment is required but cannot be paid. This allows you to manage scenarios where additional user actions are needed to pay a subscription's invoice. For example, SCA regulation may require 3DS authentication to complete payment. See the [SCA Migration Guide](https://stripe.com/docs/billing/migration/strong-customer-authentication) for Billing to learn more. This is the default behavior.  Use `default_incomplete` to transition the subscription to `status=past_due` when payment is required and await explicit confirmation of the invoice's payment intent. This allows simpler management of scenarios where additional user actions are needed to pay a subscription’s invoice. Such as failed payments, [SCA regulation](https://stripe.com/docs/billing/migration/strong-customer-authentication), or collecting a mandate for a bank debit payment method.  Use `pending_if_incomplete` to update the subscription using [pending updates](https://stripe.com/docs/billing/subscriptions/pending-updates). When you use `pending_if_incomplete` you can only pass the parameters [supported by pending updates](https://stripe.com/docs/billing/pending-updates-reference#supported-attributes).  Use `error_if_incomplete` if you want Stripe to return an HTTP 402 status code if a subscription's invoice cannot be paid. For example, if a payment method requires 3DS authentication due to SCA regulation and further user action is needed, this parameter does not update the subscription and returns an error instead. This was the default behavior for API versions prior to 2019-03-14. See the [changelog](https://docs.stripe.com/changelog/2019-03-14) to learn more.
  ///
  /// * [PaymentSettings] paymentSettings:
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestPendingInvoiceItemInterval] pendingInvoiceItemInterval:
  ///
  /// * [String] prorationBehavior:
  ///   Determines how to handle [prorations](https://stripe.com/docs/billing/subscriptions/prorations) when the billing cycle changes (e.g., when switching plans, resetting `billing_cycle_anchor=now`, or starting a trial), or if an item's `quantity` changes. The default value is `create_prorations`.
  ///
  /// * [int] prorationDate:
  ///   If set, prorations will be calculated as though the subscription was updated at the given time. This can be used to apply exactly the same prorations that were previewed with the [create preview](https://stripe.com/docs/api/invoices/create_preview) endpoint. `proration_date` can also be used to implement custom proration logic, such as prorating by day instead of by second, by providing the time that you wish to use for proration calculations.
  ///
  /// * [PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestTransferData] transferData:
  ///
  /// * [PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestTrialEnd] trialEnd:
  ///
  /// * [bool] trialFromPlan:
  ///   Indicates if a plan's `trial_period_days` should be applied to the subscription. Setting `trial_end` per subscription is preferred, and this defaults to `false`. Setting this flag to `true` together with `trial_end` is not allowed. See [Using trial periods on subscriptions](https://stripe.com/docs/billing/subscriptions/trials) to learn more.
  ///
  /// * [TrialSettingsConfig1] trialSettings:
  Future<Subscription?> postCustomersCustomerSubscriptionsSubscriptionExposedId(String customer, String subscriptionExposedId, { List<AddInvoiceItemEntry>? addInvoiceItems, PostCustomersCustomerSubscriptionsRequestApplicationFeePercent? applicationFeePercent, AutomaticTaxConfig1? automaticTax, String? billingCycleAnchor, PostCustomersCustomerSubscriptionsRequestBillingThresholds? billingThresholds, PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestCancelAt? cancelAt, bool? cancelAtPeriodEnd, CancellationDetailsParam? cancellationDetails, String? collectionMethod, int? daysUntilDue, String? defaultPaymentMethod, PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestDefaultSource? defaultSource, PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestDefaultTaxRates? defaultTaxRates, PostCustomersCustomerSubscriptionsRequestDiscounts? discounts, List<String>? expand, InvoiceSettingsParam? invoiceSettings, List<SubscriptionItemUpdateParams>? items, PostAccountsRequestMetadata? metadata, bool? offSession, PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestPauseCollection? pauseCollection, String? paymentBehavior, PaymentSettings? paymentSettings, PostCustomersCustomerSubscriptionsRequestPendingInvoiceItemInterval? pendingInvoiceItemInterval, String? prorationBehavior, int? prorationDate, PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestTransferData? transferData, PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestTrialEnd? trialEnd, bool? trialFromPlan, TrialSettingsConfig1? trialSettings, }) async {
    final response = await postCustomersCustomerSubscriptionsSubscriptionExposedIdWithHttpInfo(customer, subscriptionExposedId,  addInvoiceItems: addInvoiceItems, applicationFeePercent: applicationFeePercent, automaticTax: automaticTax, billingCycleAnchor: billingCycleAnchor, billingThresholds: billingThresholds, cancelAt: cancelAt, cancelAtPeriodEnd: cancelAtPeriodEnd, cancellationDetails: cancellationDetails, collectionMethod: collectionMethod, daysUntilDue: daysUntilDue, defaultPaymentMethod: defaultPaymentMethod, defaultSource: defaultSource, defaultTaxRates: defaultTaxRates, discounts: discounts, expand: expand, invoiceSettings: invoiceSettings, items: items, metadata: metadata, offSession: offSession, pauseCollection: pauseCollection, paymentBehavior: paymentBehavior, paymentSettings: paymentSettings, pendingInvoiceItemInterval: pendingInvoiceItemInterval, prorationBehavior: prorationBehavior, prorationDate: prorationDate, transferData: transferData, trialEnd: trialEnd, trialFromPlan: trialFromPlan, trialSettings: trialSettings, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Subscription',) as Subscription;
    
    }
    return null;
  }

  /// Create a Customer tax ID
  ///
  /// <p>Creates a new <code>tax_id</code> object for a customer.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] type (required):
  ///   Type of the tax ID, one of `ad_nrt`, `ae_trn`, `al_tin`, `am_tin`, `ao_tin`, `ar_cuit`, `au_abn`, `au_arn`, `aw_tin`, `az_tin`, `ba_tin`, `bb_tin`, `bd_bin`, `bf_ifu`, `bg_uic`, `bh_vat`, `bj_ifu`, `bo_tin`, `br_cnpj`, `br_cpf`, `bs_tin`, `by_tin`, `ca_bn`, `ca_gst_hst`, `ca_pst_bc`, `ca_pst_mb`, `ca_pst_sk`, `ca_qst`, `cd_nif`, `ch_uid`, `ch_vat`, `cl_tin`, `cm_niu`, `cn_tin`, `co_nit`, `cr_tin`, `cv_nif`, `de_stn`, `do_rcn`, `ec_ruc`, `eg_tin`, `es_cif`, `et_tin`, `eu_oss_vat`, `eu_vat`, `gb_vat`, `ge_vat`, `gn_nif`, `hk_br`, `hr_oib`, `hu_tin`, `id_npwp`, `il_vat`, `in_gst`, `is_vat`, `jp_cn`, `jp_rn`, `jp_trn`, `ke_pin`, `kg_tin`, `kh_tin`, `kr_brn`, `kz_bin`, `la_tin`, `li_uid`, `li_vat`, `ma_vat`, `md_vat`, `me_pib`, `mk_vat`, `mr_nif`, `mx_rfc`, `my_frp`, `my_itn`, `my_sst`, `ng_tin`, `no_vat`, `no_voec`, `np_pan`, `nz_gst`, `om_vat`, `pe_ruc`, `ph_tin`, `ro_tin`, `rs_pib`, `ru_inn`, `ru_kpp`, `sa_vat`, `sg_gst`, `sg_uen`, `si_tin`, `sn_ninea`, `sr_fin`, `sv_nit`, `th_vat`, `tj_tin`, `tr_tin`, `tw_vat`, `tz_vat`, `ua_vat`, `ug_tin`, `us_ein`, `uy_ruc`, `uz_tin`, `uz_vat`, `ve_rif`, `vn_tin`, `za_vat`, `zm_tin`, or `zw_tin`
  ///
  /// * [String] value (required):
  ///   Value of the tax ID.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postCustomersCustomerTaxIdsWithHttpInfo(String customer, String type, String value, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/customers/{customer}/tax_ids'
      .replaceAll('{customer}', customer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (type != null) {
      formParams[r'type'] = parameterToString(type);
    }
    if (value != null) {
      formParams[r'value'] = parameterToString(value);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a Customer tax ID
  ///
  /// <p>Creates a new <code>tax_id</code> object for a customer.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [String] type (required):
  ///   Type of the tax ID, one of `ad_nrt`, `ae_trn`, `al_tin`, `am_tin`, `ao_tin`, `ar_cuit`, `au_abn`, `au_arn`, `aw_tin`, `az_tin`, `ba_tin`, `bb_tin`, `bd_bin`, `bf_ifu`, `bg_uic`, `bh_vat`, `bj_ifu`, `bo_tin`, `br_cnpj`, `br_cpf`, `bs_tin`, `by_tin`, `ca_bn`, `ca_gst_hst`, `ca_pst_bc`, `ca_pst_mb`, `ca_pst_sk`, `ca_qst`, `cd_nif`, `ch_uid`, `ch_vat`, `cl_tin`, `cm_niu`, `cn_tin`, `co_nit`, `cr_tin`, `cv_nif`, `de_stn`, `do_rcn`, `ec_ruc`, `eg_tin`, `es_cif`, `et_tin`, `eu_oss_vat`, `eu_vat`, `gb_vat`, `ge_vat`, `gn_nif`, `hk_br`, `hr_oib`, `hu_tin`, `id_npwp`, `il_vat`, `in_gst`, `is_vat`, `jp_cn`, `jp_rn`, `jp_trn`, `ke_pin`, `kg_tin`, `kh_tin`, `kr_brn`, `kz_bin`, `la_tin`, `li_uid`, `li_vat`, `ma_vat`, `md_vat`, `me_pib`, `mk_vat`, `mr_nif`, `mx_rfc`, `my_frp`, `my_itn`, `my_sst`, `ng_tin`, `no_vat`, `no_voec`, `np_pan`, `nz_gst`, `om_vat`, `pe_ruc`, `ph_tin`, `ro_tin`, `rs_pib`, `ru_inn`, `ru_kpp`, `sa_vat`, `sg_gst`, `sg_uen`, `si_tin`, `sn_ninea`, `sr_fin`, `sv_nit`, `th_vat`, `tj_tin`, `tr_tin`, `tw_vat`, `tz_vat`, `ua_vat`, `ug_tin`, `us_ein`, `uy_ruc`, `uz_tin`, `uz_vat`, `ve_rif`, `vn_tin`, `za_vat`, `zm_tin`, or `zw_tin`
  ///
  /// * [String] value (required):
  ///   Value of the tax ID.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TaxId?> postCustomersCustomerTaxIds(String customer, String type, String value, { List<String>? expand, }) async {
    final response = await postCustomersCustomerTaxIdsWithHttpInfo(customer, type, value,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TaxId',) as TaxId;
    
    }
    return null;
  }

  /// Update a dispute
  ///
  /// <p>When you get a dispute, contacting your customer is always the best first step. If that doesn’t work, you can submit evidence to help us resolve the dispute in your favor. You can do this in your <a href=\"https://dashboard.stripe.com/disputes\">dashboard</a>, but if you prefer, you can use the API to submit evidence programmatically.</p>  <p>Depending on your dispute type, different evidence fields will give you a better chance of winning your dispute. To figure out which evidence fields to provide, see our <a href=\"/docs/disputes/categories\">guide to dispute types</a>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] dispute (required):
  ///
  /// * [DisputeEvidenceParams] evidence:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [bool] submit:
  ///   Whether to immediately submit evidence to the bank. If `false`, evidence is staged on the dispute. Staged evidence is visible in the API and Dashboard, and can be submitted to the bank by making another request with this attribute set to `true` (the default).
  Future<Response> postDisputesDisputeWithHttpInfo(String dispute, { DisputeEvidenceParams? evidence, List<String>? expand, PostAccountsRequestMetadata? metadata, bool? submit, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/disputes/{dispute}'
      .replaceAll('{dispute}', dispute);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (evidence != null) {
      formParams[r'evidence'] = parameterToString(evidence);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (submit != null) {
      formParams[r'submit'] = parameterToString(submit);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a dispute
  ///
  /// <p>When you get a dispute, contacting your customer is always the best first step. If that doesn’t work, you can submit evidence to help us resolve the dispute in your favor. You can do this in your <a href=\"https://dashboard.stripe.com/disputes\">dashboard</a>, but if you prefer, you can use the API to submit evidence programmatically.</p>  <p>Depending on your dispute type, different evidence fields will give you a better chance of winning your dispute. To figure out which evidence fields to provide, see our <a href=\"/docs/disputes/categories\">guide to dispute types</a>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] dispute (required):
  ///
  /// * [DisputeEvidenceParams] evidence:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [bool] submit:
  ///   Whether to immediately submit evidence to the bank. If `false`, evidence is staged on the dispute. Staged evidence is visible in the API and Dashboard, and can be submitted to the bank by making another request with this attribute set to `true` (the default).
  Future<Dispute?> postDisputesDispute(String dispute, { DisputeEvidenceParams? evidence, List<String>? expand, PostAccountsRequestMetadata? metadata, bool? submit, }) async {
    final response = await postDisputesDisputeWithHttpInfo(dispute,  evidence: evidence, expand: expand, metadata: metadata, submit: submit, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Dispute',) as Dispute;
    
    }
    return null;
  }

  /// Close a dispute
  ///
  /// <p>Closing the dispute for a charge indicates that you do not have any evidence to submit and are essentially dismissing the dispute, acknowledging it as lost.</p>  <p>The status of the dispute will change from <code>needs_response</code> to <code>lost</code>. <em>Closing a dispute is irreversible</em>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] dispute (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postDisputesDisputeCloseWithHttpInfo(String dispute, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/disputes/{dispute}/close'
      .replaceAll('{dispute}', dispute);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Close a dispute
  ///
  /// <p>Closing the dispute for a charge indicates that you do not have any evidence to submit and are essentially dismissing the dispute, acknowledging it as lost.</p>  <p>The status of the dispute will change from <code>needs_response</code> to <code>lost</code>. <em>Closing a dispute is irreversible</em>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] dispute (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Dispute?> postDisputesDisputeClose(String dispute, { List<String>? expand, }) async {
    final response = await postDisputesDisputeCloseWithHttpInfo(dispute,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Dispute',) as Dispute;
    
    }
    return null;
  }

  /// Create a feature
  ///
  /// <p>Creates a feature</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] lookupKey (required):
  ///   A unique key you provide as your own system identifier. This may be up to 80 characters.
  ///
  /// * [String] name (required):
  ///   The feature's name, for your own purpose, not meant to be displayable to the customer.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of key-value pairs that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
  Future<Response> postEntitlementsFeaturesWithHttpInfo(String lookupKey, String name, { List<String>? expand, Map<String, String>? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/entitlements/features';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (lookupKey != null) {
      formParams[r'lookup_key'] = parameterToString(lookupKey);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (name != null) {
      formParams[r'name'] = parameterToString(name);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a feature
  ///
  /// <p>Creates a feature</p>
  ///
  /// Parameters:
  ///
  /// * [String] lookupKey (required):
  ///   A unique key you provide as your own system identifier. This may be up to 80 characters.
  ///
  /// * [String] name (required):
  ///   The feature's name, for your own purpose, not meant to be displayable to the customer.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of key-value pairs that you can attach to an object. This can be useful for storing additional information about the object in a structured format.
  Future<EntitlementsFeature?> postEntitlementsFeatures(String lookupKey, String name, { List<String>? expand, Map<String, String>? metadata, }) async {
    final response = await postEntitlementsFeaturesWithHttpInfo(lookupKey, name,  expand: expand, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'EntitlementsFeature',) as EntitlementsFeature;
    
    }
    return null;
  }

  /// Updates a feature
  ///
  /// <p>Update a feature’s metadata or permanently deactivate it.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [bool] active:
  ///   Inactive features cannot be attached to new products and will not be returned from the features list endpoint.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostEntitlementsFeaturesIdRequestMetadata] metadata:
  ///
  /// * [String] name:
  ///   The feature's name, for your own purpose, not meant to be displayable to the customer.
  Future<Response> postEntitlementsFeaturesIdWithHttpInfo(String id, { bool? active, List<String>? expand, PostEntitlementsFeaturesIdRequestMetadata? metadata, String? name, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/entitlements/features/{id}'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (active != null) {
      formParams[r'active'] = parameterToString(active);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (name != null) {
      formParams[r'name'] = parameterToString(name);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Updates a feature
  ///
  /// <p>Update a feature’s metadata or permanently deactivate it.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [bool] active:
  ///   Inactive features cannot be attached to new products and will not be returned from the features list endpoint.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostEntitlementsFeaturesIdRequestMetadata] metadata:
  ///
  /// * [String] name:
  ///   The feature's name, for your own purpose, not meant to be displayable to the customer.
  Future<EntitlementsFeature?> postEntitlementsFeaturesId(String id, { bool? active, List<String>? expand, PostEntitlementsFeaturesIdRequestMetadata? metadata, String? name, }) async {
    final response = await postEntitlementsFeaturesIdWithHttpInfo(id,  active: active, expand: expand, metadata: metadata, name: name, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'EntitlementsFeature',) as EntitlementsFeature;
    
    }
    return null;
  }

  /// Create an ephemeral key
  ///
  /// <p>Creates a short-lived API key for a given resource.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer:
  ///   The ID of the Customer you'd like to modify using the resulting ephemeral key.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] issuingCard:
  ///   The ID of the Issuing Card you'd like to access using the resulting ephemeral key.
  ///
  /// * [String] nonce:
  ///   A single-use token, created by Stripe.js, used for creating ephemeral keys for Issuing Cards without exchanging sensitive information.
  ///
  /// * [String] verificationSession:
  ///   The ID of the Identity VerificationSession you'd like to access using the resulting ephemeral key
  Future<Response> postEphemeralKeysWithHttpInfo({ String? customer, List<String>? expand, String? issuingCard, String? nonce, String? verificationSession, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/ephemeral_keys';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (customer != null) {
      formParams[r'customer'] = parameterToString(customer);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (issuingCard != null) {
      formParams[r'issuing_card'] = parameterToString(issuingCard);
    }
    if (nonce != null) {
      formParams[r'nonce'] = parameterToString(nonce);
    }
    if (verificationSession != null) {
      formParams[r'verification_session'] = parameterToString(verificationSession);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create an ephemeral key
  ///
  /// <p>Creates a short-lived API key for a given resource.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer:
  ///   The ID of the Customer you'd like to modify using the resulting ephemeral key.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] issuingCard:
  ///   The ID of the Issuing Card you'd like to access using the resulting ephemeral key.
  ///
  /// * [String] nonce:
  ///   A single-use token, created by Stripe.js, used for creating ephemeral keys for Issuing Cards without exchanging sensitive information.
  ///
  /// * [String] verificationSession:
  ///   The ID of the Identity VerificationSession you'd like to access using the resulting ephemeral key
  Future<EphemeralKey?> postEphemeralKeys({ String? customer, List<String>? expand, String? issuingCard, String? nonce, String? verificationSession, }) async {
    final response = await postEphemeralKeysWithHttpInfo( customer: customer, expand: expand, issuingCard: issuingCard, nonce: nonce, verificationSession: verificationSession, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'EphemeralKey',) as EphemeralKey;
    
    }
    return null;
  }

  /// <p>Updates the metadata, account holder name, account holder type of a bank account belonging to a connected account and optionally sets it as the default for its currency. Other bank account details are not editable by design.</p>  <p>You can only update bank accounts when <a href=\"/api/accounts/object#account_object-controller-requirement_collection\">account.controller.requirement_collection</a> is <code>application</code>, which includes <a href=\"/connect/custom-accounts\">Custom accounts</a>.</p>  <p>You can re-enable a disabled bank account by performing an update call without providing any arguments or changes.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [String] accountHolderName:
  ///   The name of the person or business that owns the bank account.
  ///
  /// * [String] accountHolderType:
  ///   The type of entity that holds the account. This can be either `individual` or `company`.
  ///
  /// * [String] accountType:
  ///   The bank account type. This can only be `checking` or `savings` in most countries. In Japan, this can only be `futsu` or `toza`.
  ///
  /// * [String] addressCity:
  ///   City/District/Suburb/Town/Village.
  ///
  /// * [String] addressCountry:
  ///   Billing address country, if provided when creating card.
  ///
  /// * [String] addressLine1:
  ///   Address line 1 (Street address/PO Box/Company name).
  ///
  /// * [String] addressLine2:
  ///   Address line 2 (Apartment/Suite/Unit/Building).
  ///
  /// * [String] addressState:
  ///   State/County/Province/Region.
  ///
  /// * [String] addressZip:
  ///   ZIP or postal code.
  ///
  /// * [bool] defaultForCurrency:
  ///   When set to true, this becomes the default external account for its currency.
  ///
  /// * [ExternalAccountDocumentsParam1] documents:
  ///
  /// * [String] expMonth:
  ///   Two digit number representing the card’s expiration month.
  ///
  /// * [String] expYear:
  ///   Four digit number representing the card’s expiration year.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] name:
  ///   Cardholder name.
  Future<Response> postExternalAccountsIdWithHttpInfo(String id, { String? accountHolderName, String? accountHolderType, String? accountType, String? addressCity, String? addressCountry, String? addressLine1, String? addressLine2, String? addressState, String? addressZip, bool? defaultForCurrency, ExternalAccountDocumentsParam1? documents, String? expMonth, String? expYear, List<String>? expand, PostAccountsRequestMetadata? metadata, String? name, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/external_accounts/{id}'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (accountHolderName != null) {
      formParams[r'account_holder_name'] = parameterToString(accountHolderName);
    }
    if (accountHolderType != null) {
      formParams[r'account_holder_type'] = parameterToString(accountHolderType);
    }
    if (accountType != null) {
      formParams[r'account_type'] = parameterToString(accountType);
    }
    if (addressCity != null) {
      formParams[r'address_city'] = parameterToString(addressCity);
    }
    if (addressCountry != null) {
      formParams[r'address_country'] = parameterToString(addressCountry);
    }
    if (addressLine1 != null) {
      formParams[r'address_line1'] = parameterToString(addressLine1);
    }
    if (addressLine2 != null) {
      formParams[r'address_line2'] = parameterToString(addressLine2);
    }
    if (addressState != null) {
      formParams[r'address_state'] = parameterToString(addressState);
    }
    if (addressZip != null) {
      formParams[r'address_zip'] = parameterToString(addressZip);
    }
    if (defaultForCurrency != null) {
      formParams[r'default_for_currency'] = parameterToString(defaultForCurrency);
    }
    if (documents != null) {
      formParams[r'documents'] = parameterToString(documents);
    }
    if (expMonth != null) {
      formParams[r'exp_month'] = parameterToString(expMonth);
    }
    if (expYear != null) {
      formParams[r'exp_year'] = parameterToString(expYear);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (name != null) {
      formParams[r'name'] = parameterToString(name);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// <p>Updates the metadata, account holder name, account holder type of a bank account belonging to a connected account and optionally sets it as the default for its currency. Other bank account details are not editable by design.</p>  <p>You can only update bank accounts when <a href=\"/api/accounts/object#account_object-controller-requirement_collection\">account.controller.requirement_collection</a> is <code>application</code>, which includes <a href=\"/connect/custom-accounts\">Custom accounts</a>.</p>  <p>You can re-enable a disabled bank account by performing an update call without providing any arguments or changes.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [String] accountHolderName:
  ///   The name of the person or business that owns the bank account.
  ///
  /// * [String] accountHolderType:
  ///   The type of entity that holds the account. This can be either `individual` or `company`.
  ///
  /// * [String] accountType:
  ///   The bank account type. This can only be `checking` or `savings` in most countries. In Japan, this can only be `futsu` or `toza`.
  ///
  /// * [String] addressCity:
  ///   City/District/Suburb/Town/Village.
  ///
  /// * [String] addressCountry:
  ///   Billing address country, if provided when creating card.
  ///
  /// * [String] addressLine1:
  ///   Address line 1 (Street address/PO Box/Company name).
  ///
  /// * [String] addressLine2:
  ///   Address line 2 (Apartment/Suite/Unit/Building).
  ///
  /// * [String] addressState:
  ///   State/County/Province/Region.
  ///
  /// * [String] addressZip:
  ///   ZIP or postal code.
  ///
  /// * [bool] defaultForCurrency:
  ///   When set to true, this becomes the default external account for its currency.
  ///
  /// * [ExternalAccountDocumentsParam1] documents:
  ///
  /// * [String] expMonth:
  ///   Two digit number representing the card’s expiration month.
  ///
  /// * [String] expYear:
  ///   Four digit number representing the card’s expiration year.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] name:
  ///   Cardholder name.
  Future<ExternalAccount?> postExternalAccountsId(String id, { String? accountHolderName, String? accountHolderType, String? accountType, String? addressCity, String? addressCountry, String? addressLine1, String? addressLine2, String? addressState, String? addressZip, bool? defaultForCurrency, ExternalAccountDocumentsParam1? documents, String? expMonth, String? expYear, List<String>? expand, PostAccountsRequestMetadata? metadata, String? name, }) async {
    final response = await postExternalAccountsIdWithHttpInfo(id,  accountHolderName: accountHolderName, accountHolderType: accountHolderType, accountType: accountType, addressCity: addressCity, addressCountry: addressCountry, addressLine1: addressLine1, addressLine2: addressLine2, addressState: addressState, addressZip: addressZip, defaultForCurrency: defaultForCurrency, documents: documents, expMonth: expMonth, expYear: expYear, expand: expand, metadata: metadata, name: name, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ExternalAccount',) as ExternalAccount;
    
    }
    return null;
  }

  /// Create a file link
  ///
  /// <p>Creates a new file link object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] file (required):
  ///   The ID of the file. The file's `purpose` must be one of the following: `business_icon`, `business_logo`, `customer_signature`, `dispute_evidence`, `finance_report_run`, `financial_account_statement`, `identity_document_downloadable`, `issuing_regulatory_reporting`, `pci_document`, `selfie`, `sigma_scheduled_query`, `tax_document_user_upload`, or `terminal_reader_splashscreen`.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] expiresAt:
  ///   The link isn't usable after this future timestamp.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<Response> postFileLinksWithHttpInfo(String file, { List<String>? expand, int? expiresAt, PostAccountsRequestMetadata? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/file_links';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (expiresAt != null) {
      formParams[r'expires_at'] = parameterToString(expiresAt);
    }
    if (file != null) {
      formParams[r'file'] = parameterToString(file);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a file link
  ///
  /// <p>Creates a new file link object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] file (required):
  ///   The ID of the file. The file's `purpose` must be one of the following: `business_icon`, `business_logo`, `customer_signature`, `dispute_evidence`, `finance_report_run`, `financial_account_statement`, `identity_document_downloadable`, `issuing_regulatory_reporting`, `pci_document`, `selfie`, `sigma_scheduled_query`, `tax_document_user_upload`, or `terminal_reader_splashscreen`.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] expiresAt:
  ///   The link isn't usable after this future timestamp.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<FileLink?> postFileLinks(String file, { List<String>? expand, int? expiresAt, PostAccountsRequestMetadata? metadata, }) async {
    final response = await postFileLinksWithHttpInfo(file,  expand: expand, expiresAt: expiresAt, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'FileLink',) as FileLink;
    
    }
    return null;
  }

  /// Update a file link
  ///
  /// <p>Updates an existing file link object. Expired links can no longer be updated.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] link (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostFileLinksLinkRequestExpiresAt] expiresAt:
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<Response> postFileLinksLinkWithHttpInfo(String link, { List<String>? expand, PostFileLinksLinkRequestExpiresAt? expiresAt, PostAccountsRequestMetadata? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/file_links/{link}'
      .replaceAll('{link}', link);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (expiresAt != null) {
      formParams[r'expires_at'] = parameterToString(expiresAt);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a file link
  ///
  /// <p>Updates an existing file link object. Expired links can no longer be updated.</p>
  ///
  /// Parameters:
  ///
  /// * [String] link (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostFileLinksLinkRequestExpiresAt] expiresAt:
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<FileLink?> postFileLinksLink(String link, { List<String>? expand, PostFileLinksLinkRequestExpiresAt? expiresAt, PostAccountsRequestMetadata? metadata, }) async {
    final response = await postFileLinksLinkWithHttpInfo(link,  expand: expand, expiresAt: expiresAt, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'FileLink',) as FileLink;
    
    }
    return null;
  }

  /// Create a file
  ///
  /// <p>To upload a file to Stripe, you need to send a request of type <code>multipart/form-data</code>. Include the file you want to upload in the request, and the parameters for creating a file.</p>  <p>All of Stripe’s officially supported Client libraries support sending <code>multipart/form-data</code>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [MultipartFile] file (required):
  ///   A file to upload. Make sure that the specifications follow RFC 2388, which defines file transfers for the `multipart/form-data` protocol.
  ///
  /// * [String] purpose (required):
  ///   The [purpose](https://stripe.com/docs/file-upload#uploading-a-file) of the uploaded file.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [FileLinkCreationParams] fileLinkData:
  Future<Response> postFilesWithHttpInfo(MultipartFile file, String purpose, { List<String>? expand, FileLinkCreationParams? fileLinkData, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/files';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['multipart/form-data'];

    bool hasFields = false;
    final mp = MultipartRequest('POST', Uri.parse(path));
    if (expand != null) {
      hasFields = true;
      mp.fields[r'expand'] = parameterToString(expand);
    }
    if (file != null) {
      hasFields = true;
      mp.fields[r'file'] = file.field;
      mp.files.add(file);
    }
    if (fileLinkData != null) {
      hasFields = true;
      mp.fields[r'file_link_data'] = parameterToString(fileLinkData);
    }
    if (purpose != null) {
      hasFields = true;
      mp.fields[r'purpose'] = parameterToString(purpose);
    }
    if (hasFields) {
      postBody = mp;
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a file
  ///
  /// <p>To upload a file to Stripe, you need to send a request of type <code>multipart/form-data</code>. Include the file you want to upload in the request, and the parameters for creating a file.</p>  <p>All of Stripe’s officially supported Client libraries support sending <code>multipart/form-data</code>.</p>
  ///
  /// Parameters:
  ///
  /// * [MultipartFile] file (required):
  ///   A file to upload. Make sure that the specifications follow RFC 2388, which defines file transfers for the `multipart/form-data` protocol.
  ///
  /// * [String] purpose (required):
  ///   The [purpose](https://stripe.com/docs/file-upload#uploading-a-file) of the uploaded file.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [FileLinkCreationParams] fileLinkData:
  Future<MultipartFile?> postFiles(MultipartFile file, String purpose, { List<String>? expand, FileLinkCreationParams? fileLinkData, }) async {
    final response = await postFilesWithHttpInfo(file, purpose,  expand: expand, fileLinkData: fileLinkData, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'MultipartFile',) as MultipartFile;
    
    }
    return null;
  }

  /// Disconnect an Account
  ///
  /// <p>Disables your access to a Financial Connections <code>Account</code>. You will no longer be able to access data associated with the account (e.g. balances, transactions).</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postFinancialConnectionsAccountsAccountDisconnectWithHttpInfo(String account, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/financial_connections/accounts/{account}/disconnect'
      .replaceAll('{account}', account);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Disconnect an Account
  ///
  /// <p>Disables your access to a Financial Connections <code>Account</code>. You will no longer be able to access data associated with the account (e.g. balances, transactions).</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<FinancialConnectionsAccount?> postFinancialConnectionsAccountsAccountDisconnect(String account, { List<String>? expand, }) async {
    final response = await postFinancialConnectionsAccountsAccountDisconnectWithHttpInfo(account,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'FinancialConnectionsAccount',) as FinancialConnectionsAccount;
    
    }
    return null;
  }

  /// Refresh Account data
  ///
  /// <p>Refreshes the data associated with a Financial Connections <code>Account</code>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [List<String>] features (required):
  ///   The list of account features that you would like to refresh.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postFinancialConnectionsAccountsAccountRefreshWithHttpInfo(String account, List<String> features, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/financial_connections/accounts/{account}/refresh'
      .replaceAll('{account}', account);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (features != null) {
      formParams[r'features'] = parameterToString(features);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Refresh Account data
  ///
  /// <p>Refreshes the data associated with a Financial Connections <code>Account</code>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [List<String>] features (required):
  ///   The list of account features that you would like to refresh.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<FinancialConnectionsAccount?> postFinancialConnectionsAccountsAccountRefresh(String account, List<String> features, { List<String>? expand, }) async {
    final response = await postFinancialConnectionsAccountsAccountRefreshWithHttpInfo(account, features,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'FinancialConnectionsAccount',) as FinancialConnectionsAccount;
    
    }
    return null;
  }

  /// Subscribe to data refreshes for an Account
  ///
  /// <p>Subscribes to periodic refreshes of data associated with a Financial Connections <code>Account</code>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [List<String>] features (required):
  ///   The list of account features to which you would like to subscribe.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postFinancialConnectionsAccountsAccountSubscribeWithHttpInfo(String account, List<String> features, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/financial_connections/accounts/{account}/subscribe'
      .replaceAll('{account}', account);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (features != null) {
      formParams[r'features'] = parameterToString(features);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Subscribe to data refreshes for an Account
  ///
  /// <p>Subscribes to periodic refreshes of data associated with a Financial Connections <code>Account</code>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [List<String>] features (required):
  ///   The list of account features to which you would like to subscribe.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<FinancialConnectionsAccount?> postFinancialConnectionsAccountsAccountSubscribe(String account, List<String> features, { List<String>? expand, }) async {
    final response = await postFinancialConnectionsAccountsAccountSubscribeWithHttpInfo(account, features,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'FinancialConnectionsAccount',) as FinancialConnectionsAccount;
    
    }
    return null;
  }

  /// Unsubscribe from data refreshes for an Account
  ///
  /// <p>Unsubscribes from periodic refreshes of data associated with a Financial Connections <code>Account</code>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [List<String>] features (required):
  ///   The list of account features from which you would like to unsubscribe.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postFinancialConnectionsAccountsAccountUnsubscribeWithHttpInfo(String account, List<String> features, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/financial_connections/accounts/{account}/unsubscribe'
      .replaceAll('{account}', account);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (features != null) {
      formParams[r'features'] = parameterToString(features);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Unsubscribe from data refreshes for an Account
  ///
  /// <p>Unsubscribes from periodic refreshes of data associated with a Financial Connections <code>Account</code>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [List<String>] features (required):
  ///   The list of account features from which you would like to unsubscribe.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<FinancialConnectionsAccount?> postFinancialConnectionsAccountsAccountUnsubscribe(String account, List<String> features, { List<String>? expand, }) async {
    final response = await postFinancialConnectionsAccountsAccountUnsubscribeWithHttpInfo(account, features,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'FinancialConnectionsAccount',) as FinancialConnectionsAccount;
    
    }
    return null;
  }

  /// Create a Session
  ///
  /// <p>To launch the Financial Connections authorization flow, create a <code>Session</code>. The session’s <code>client_secret</code> can be used to launch the flow using Stripe.js.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [AccountholderParams1] accountHolder (required):
  ///
  /// * [List<String>] permissions (required):
  ///   List of data features that you would like to request access to.  Possible values are `balances`, `transactions`, `ownership`, and `payment_method`.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [FiltersParams] filters:
  ///
  /// * [List<String>] prefetch:
  ///   List of data features that you would like to retrieve upon account creation.
  ///
  /// * [String] returnUrl:
  ///   For webview integrations only. Upon completing OAuth login in the native browser, the user will be redirected to this URL to return to your app.
  Future<Response> postFinancialConnectionsSessionsWithHttpInfo(AccountholderParams1 accountHolder, List<String> permissions, { List<String>? expand, FiltersParams? filters, List<String>? prefetch, String? returnUrl, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/financial_connections/sessions';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (accountHolder != null) {
      formParams[r'account_holder'] = parameterToString(accountHolder);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (filters != null) {
      formParams[r'filters'] = parameterToString(filters);
    }
    if (permissions != null) {
      formParams[r'permissions'] = parameterToString(permissions);
    }
    if (prefetch != null) {
      formParams[r'prefetch'] = parameterToString(prefetch);
    }
    if (returnUrl != null) {
      formParams[r'return_url'] = parameterToString(returnUrl);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a Session
  ///
  /// <p>To launch the Financial Connections authorization flow, create a <code>Session</code>. The session’s <code>client_secret</code> can be used to launch the flow using Stripe.js.</p>
  ///
  /// Parameters:
  ///
  /// * [AccountholderParams1] accountHolder (required):
  ///
  /// * [List<String>] permissions (required):
  ///   List of data features that you would like to request access to.  Possible values are `balances`, `transactions`, `ownership`, and `payment_method`.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [FiltersParams] filters:
  ///
  /// * [List<String>] prefetch:
  ///   List of data features that you would like to retrieve upon account creation.
  ///
  /// * [String] returnUrl:
  ///   For webview integrations only. Upon completing OAuth login in the native browser, the user will be redirected to this URL to return to your app.
  Future<FinancialConnectionsSession?> postFinancialConnectionsSessions(AccountholderParams1 accountHolder, List<String> permissions, { List<String>? expand, FiltersParams? filters, List<String>? prefetch, String? returnUrl, }) async {
    final response = await postFinancialConnectionsSessionsWithHttpInfo(accountHolder, permissions,  expand: expand, filters: filters, prefetch: prefetch, returnUrl: returnUrl, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'FinancialConnectionsSession',) as FinancialConnectionsSession;
    
    }
    return null;
  }

  /// Create a ForwardingRequest
  ///
  /// <p>Creates a ForwardingRequest object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] paymentMethod (required):
  ///   The PaymentMethod to insert into the forwarded request. Forwarding previously consumed PaymentMethods is allowed.
  ///
  /// * [List<String>] replacements (required):
  ///   The field kinds to be replaced in the forwarded request.
  ///
  /// * [String] url (required):
  ///   The destination URL for the forwarded request. Must be supported by the config.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [RequestParam] request:
  Future<Response> postForwardingRequestsWithHttpInfo(String paymentMethod, List<String> replacements, String url, { List<String>? expand, Map<String, String>? metadata, RequestParam? request, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/forwarding/requests';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (paymentMethod != null) {
      formParams[r'payment_method'] = parameterToString(paymentMethod);
    }
    if (replacements != null) {
      formParams[r'replacements'] = parameterToString(replacements);
    }
    if (request != null) {
      formParams[r'request'] = parameterToString(request);
    }
    if (url != null) {
      formParams[r'url'] = parameterToString(url);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a ForwardingRequest
  ///
  /// <p>Creates a ForwardingRequest object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] paymentMethod (required):
  ///   The PaymentMethod to insert into the forwarded request. Forwarding previously consumed PaymentMethods is allowed.
  ///
  /// * [List<String>] replacements (required):
  ///   The field kinds to be replaced in the forwarded request.
  ///
  /// * [String] url (required):
  ///   The destination URL for the forwarded request. Must be supported by the config.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [RequestParam] request:
  Future<ForwardingRequest?> postForwardingRequests(String paymentMethod, List<String> replacements, String url, { List<String>? expand, Map<String, String>? metadata, RequestParam? request, }) async {
    final response = await postForwardingRequestsWithHttpInfo(paymentMethod, replacements, url,  expand: expand, metadata: metadata, request: request, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ForwardingRequest',) as ForwardingRequest;
    
    }
    return null;
  }

  /// Create a VerificationSession
  ///
  /// <p>Creates a VerificationSession object.</p>  <p>After the VerificationSession is created, display a verification modal using the session <code>client_secret</code> or send your users to the session’s <code>url</code>.</p>  <p>If your API key is in test mode, verification checks won’t actually process, though everything else will occur as if in live mode.</p>  <p>Related guide: <a href=\"/docs/identity/verify-identity-documents\">Verify your users’ identity documents</a></p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] clientReferenceId:
  ///   A string to reference this user. This can be a customer ID, a session ID, or similar, and can be used to reconcile this verification with your internal systems.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [SessionOptionsParam] options:
  ///
  /// * [ProvidedDetailsParam] providedDetails:
  ///
  /// * [String] relatedCustomer:
  ///   Customer ID
  ///
  /// * [RelatedPersonParam] relatedPerson:
  ///
  /// * [String] returnUrl:
  ///   The URL that the user will be redirected to upon completing the verification flow.
  ///
  /// * [String] type:
  ///   The type of [verification check](https://stripe.com/docs/identity/verification-checks) to be performed. You must provide a `type` if not passing `verification_flow`.
  ///
  /// * [String] verificationFlow:
  ///   The ID of a verification flow from the Dashboard. See https://docs.stripe.com/identity/verification-flows.
  Future<Response> postIdentityVerificationSessionsWithHttpInfo({ String? clientReferenceId, List<String>? expand, Map<String, String>? metadata, SessionOptionsParam? options, ProvidedDetailsParam? providedDetails, String? relatedCustomer, RelatedPersonParam? relatedPerson, String? returnUrl, String? type, String? verificationFlow, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/identity/verification_sessions';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (clientReferenceId != null) {
      formParams[r'client_reference_id'] = parameterToString(clientReferenceId);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (options != null) {
      formParams[r'options'] = parameterToString(options);
    }
    if (providedDetails != null) {
      formParams[r'provided_details'] = parameterToString(providedDetails);
    }
    if (relatedCustomer != null) {
      formParams[r'related_customer'] = parameterToString(relatedCustomer);
    }
    if (relatedPerson != null) {
      formParams[r'related_person'] = parameterToString(relatedPerson);
    }
    if (returnUrl != null) {
      formParams[r'return_url'] = parameterToString(returnUrl);
    }
    if (type != null) {
      formParams[r'type'] = parameterToString(type);
    }
    if (verificationFlow != null) {
      formParams[r'verification_flow'] = parameterToString(verificationFlow);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a VerificationSession
  ///
  /// <p>Creates a VerificationSession object.</p>  <p>After the VerificationSession is created, display a verification modal using the session <code>client_secret</code> or send your users to the session’s <code>url</code>.</p>  <p>If your API key is in test mode, verification checks won’t actually process, though everything else will occur as if in live mode.</p>  <p>Related guide: <a href=\"/docs/identity/verify-identity-documents\">Verify your users’ identity documents</a></p>
  ///
  /// Parameters:
  ///
  /// * [String] clientReferenceId:
  ///   A string to reference this user. This can be a customer ID, a session ID, or similar, and can be used to reconcile this verification with your internal systems.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [SessionOptionsParam] options:
  ///
  /// * [ProvidedDetailsParam] providedDetails:
  ///
  /// * [String] relatedCustomer:
  ///   Customer ID
  ///
  /// * [RelatedPersonParam] relatedPerson:
  ///
  /// * [String] returnUrl:
  ///   The URL that the user will be redirected to upon completing the verification flow.
  ///
  /// * [String] type:
  ///   The type of [verification check](https://stripe.com/docs/identity/verification-checks) to be performed. You must provide a `type` if not passing `verification_flow`.
  ///
  /// * [String] verificationFlow:
  ///   The ID of a verification flow from the Dashboard. See https://docs.stripe.com/identity/verification-flows.
  Future<IdentityVerificationSession?> postIdentityVerificationSessions({ String? clientReferenceId, List<String>? expand, Map<String, String>? metadata, SessionOptionsParam? options, ProvidedDetailsParam? providedDetails, String? relatedCustomer, RelatedPersonParam? relatedPerson, String? returnUrl, String? type, String? verificationFlow, }) async {
    final response = await postIdentityVerificationSessionsWithHttpInfo( clientReferenceId: clientReferenceId, expand: expand, metadata: metadata, options: options, providedDetails: providedDetails, relatedCustomer: relatedCustomer, relatedPerson: relatedPerson, returnUrl: returnUrl, type: type, verificationFlow: verificationFlow, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IdentityVerificationSession',) as IdentityVerificationSession;
    
    }
    return null;
  }

  /// Update a VerificationSession
  ///
  /// <p>Updates a VerificationSession object.</p>  <p>When the session status is <code>requires_input</code>, you can use this method to update the verification check and options.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] session (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [SessionOptionsParam] options:
  ///
  /// * [ProvidedDetailsParam] providedDetails:
  ///
  /// * [String] type:
  ///   The type of [verification check](https://stripe.com/docs/identity/verification-checks) to be performed.
  Future<Response> postIdentityVerificationSessionsSessionWithHttpInfo(String session, { List<String>? expand, Map<String, String>? metadata, SessionOptionsParam? options, ProvidedDetailsParam? providedDetails, String? type, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/identity/verification_sessions/{session}'
      .replaceAll('{session}', session);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (options != null) {
      formParams[r'options'] = parameterToString(options);
    }
    if (providedDetails != null) {
      formParams[r'provided_details'] = parameterToString(providedDetails);
    }
    if (type != null) {
      formParams[r'type'] = parameterToString(type);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a VerificationSession
  ///
  /// <p>Updates a VerificationSession object.</p>  <p>When the session status is <code>requires_input</code>, you can use this method to update the verification check and options.</p>
  ///
  /// Parameters:
  ///
  /// * [String] session (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [SessionOptionsParam] options:
  ///
  /// * [ProvidedDetailsParam] providedDetails:
  ///
  /// * [String] type:
  ///   The type of [verification check](https://stripe.com/docs/identity/verification-checks) to be performed.
  Future<IdentityVerificationSession?> postIdentityVerificationSessionsSession(String session, { List<String>? expand, Map<String, String>? metadata, SessionOptionsParam? options, ProvidedDetailsParam? providedDetails, String? type, }) async {
    final response = await postIdentityVerificationSessionsSessionWithHttpInfo(session,  expand: expand, metadata: metadata, options: options, providedDetails: providedDetails, type: type, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IdentityVerificationSession',) as IdentityVerificationSession;
    
    }
    return null;
  }

  /// Cancel a VerificationSession
  ///
  /// <p>A VerificationSession object can be canceled when it is in <code>requires_input</code> <a href=\"/docs/identity/how-sessions-work\">status</a>.</p>  <p>Once canceled, future submission attempts are disabled. This cannot be undone. <a href=\"/docs/identity/verification-sessions#cancel\">Learn more</a>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] session (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postIdentityVerificationSessionsSessionCancelWithHttpInfo(String session, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/identity/verification_sessions/{session}/cancel'
      .replaceAll('{session}', session);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Cancel a VerificationSession
  ///
  /// <p>A VerificationSession object can be canceled when it is in <code>requires_input</code> <a href=\"/docs/identity/how-sessions-work\">status</a>.</p>  <p>Once canceled, future submission attempts are disabled. This cannot be undone. <a href=\"/docs/identity/verification-sessions#cancel\">Learn more</a>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] session (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<IdentityVerificationSession?> postIdentityVerificationSessionsSessionCancel(String session, { List<String>? expand, }) async {
    final response = await postIdentityVerificationSessionsSessionCancelWithHttpInfo(session,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IdentityVerificationSession',) as IdentityVerificationSession;
    
    }
    return null;
  }

  /// Redact a VerificationSession
  ///
  /// <p>Redact a VerificationSession to remove all collected information from Stripe. This will redact the VerificationSession and all objects related to it, including VerificationReports, Events, request logs, etc.</p>  <p>A VerificationSession object can be redacted when it is in <code>requires_input</code> or <code>verified</code> <a href=\"/docs/identity/how-sessions-work\">status</a>. Redacting a VerificationSession in <code>requires_action</code> state will automatically cancel it.</p>  <p>The redaction process may take up to four days. When the redaction process is in progress, the VerificationSession’s <code>redaction.status</code> field will be set to <code>processing</code>; when the process is finished, it will change to <code>redacted</code> and an <code>identity.verification_session.redacted</code> event will be emitted.</p>  <p>Redaction is irreversible. Redacted objects are still accessible in the Stripe API, but all the fields that contain personal data will be replaced by the string <code>[redacted]</code> or a similar placeholder. The <code>metadata</code> field will also be erased. Redacted objects cannot be updated or used for any purpose.</p>  <p><a href=\"/docs/identity/verification-sessions#redact\">Learn more</a>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] session (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postIdentityVerificationSessionsSessionRedactWithHttpInfo(String session, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/identity/verification_sessions/{session}/redact'
      .replaceAll('{session}', session);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Redact a VerificationSession
  ///
  /// <p>Redact a VerificationSession to remove all collected information from Stripe. This will redact the VerificationSession and all objects related to it, including VerificationReports, Events, request logs, etc.</p>  <p>A VerificationSession object can be redacted when it is in <code>requires_input</code> or <code>verified</code> <a href=\"/docs/identity/how-sessions-work\">status</a>. Redacting a VerificationSession in <code>requires_action</code> state will automatically cancel it.</p>  <p>The redaction process may take up to four days. When the redaction process is in progress, the VerificationSession’s <code>redaction.status</code> field will be set to <code>processing</code>; when the process is finished, it will change to <code>redacted</code> and an <code>identity.verification_session.redacted</code> event will be emitted.</p>  <p>Redaction is irreversible. Redacted objects are still accessible in the Stripe API, but all the fields that contain personal data will be replaced by the string <code>[redacted]</code> or a similar placeholder. The <code>metadata</code> field will also be erased. Redacted objects cannot be updated or used for any purpose.</p>  <p><a href=\"/docs/identity/verification-sessions#redact\">Learn more</a>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] session (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<IdentityVerificationSession?> postIdentityVerificationSessionsSessionRedact(String session, { List<String>? expand, }) async {
    final response = await postIdentityVerificationSessionsSessionRedactWithHttpInfo(session,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IdentityVerificationSession',) as IdentityVerificationSession;
    
    }
    return null;
  }

  /// Archive an invoice rendering template
  ///
  /// <p>Updates the status of an invoice rendering template to ‘archived’ so no new Stripe objects (customers, invoices, etc.) can reference it. The template can also no longer be updated. However, if the template is already set on a Stripe object, it will continue to be applied on invoices generated by it.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] template (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postInvoiceRenderingTemplatesTemplateArchiveWithHttpInfo(String template, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/invoice_rendering_templates/{template}/archive'
      .replaceAll('{template}', template);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Archive an invoice rendering template
  ///
  /// <p>Updates the status of an invoice rendering template to ‘archived’ so no new Stripe objects (customers, invoices, etc.) can reference it. The template can also no longer be updated. However, if the template is already set on a Stripe object, it will continue to be applied on invoices generated by it.</p>
  ///
  /// Parameters:
  ///
  /// * [String] template (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<InvoiceRenderingTemplate?> postInvoiceRenderingTemplatesTemplateArchive(String template, { List<String>? expand, }) async {
    final response = await postInvoiceRenderingTemplatesTemplateArchiveWithHttpInfo(template,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'InvoiceRenderingTemplate',) as InvoiceRenderingTemplate;
    
    }
    return null;
  }

  /// Unarchive an invoice rendering template
  ///
  /// <p>Unarchive an invoice rendering template so it can be used on new Stripe objects again.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] template (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postInvoiceRenderingTemplatesTemplateUnarchiveWithHttpInfo(String template, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/invoice_rendering_templates/{template}/unarchive'
      .replaceAll('{template}', template);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Unarchive an invoice rendering template
  ///
  /// <p>Unarchive an invoice rendering template so it can be used on new Stripe objects again.</p>
  ///
  /// Parameters:
  ///
  /// * [String] template (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<InvoiceRenderingTemplate?> postInvoiceRenderingTemplatesTemplateUnarchive(String template, { List<String>? expand, }) async {
    final response = await postInvoiceRenderingTemplatesTemplateUnarchiveWithHttpInfo(template,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'InvoiceRenderingTemplate',) as InvoiceRenderingTemplate;
    
    }
    return null;
  }

  /// Create an invoice item
  ///
  /// <p>Creates an item to be added to a draft invoice (up to 250 items per invoice). If no invoice is specified, the item will be on the next invoice created for the customer specified.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///   The ID of the customer who will be billed when this invoice item is billed.
  ///
  /// * [int] amount:
  ///   The integer amount in cents (or local equivalent) of the charge to be applied to the upcoming invoice. Passing in a negative `amount` will reduce the `amount_due` on the invoice.
  ///
  /// * [String] currency:
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] description:
  ///   An arbitrary string which you can attach to the invoice item. The description is displayed in the invoice for easy tracking.
  ///
  /// * [bool] discountable:
  ///   Controls whether discounts apply to this invoice item. Defaults to false for prorations or negative invoice items, and true for all other invoice items.
  ///
  /// * [PostInvoiceitemsRequestDiscounts] discounts:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] invoice:
  ///   The ID of an existing invoice to add this invoice item to. For subscription invoices, when left blank, the invoice item will be added to the next upcoming scheduled invoice. For standalone invoices, the invoice item won't be automatically added unless you pass `pending_invoice_item_behavior: 'include'` when creating the invoice. This is useful when adding invoice items in response to an invoice.created webhook. You can only add invoice items to draft invoices and there is a maximum of 250 items per invoice.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [Period] period:
  ///
  /// * [OneTimePriceData] priceData:
  ///
  /// * [PricingParam] pricing:
  ///
  /// * [int] quantity:
  ///   Non-negative integer. The quantity of units for the invoice item.
  ///
  /// * [String] subscription:
  ///   The ID of a subscription to add this invoice item to. When left blank, the invoice item is added to the next upcoming scheduled invoice. When set, scheduled invoices for subscriptions other than the specified subscription will ignore the invoice item. Use this when you want to express that an invoice item has been accrued within the context of a particular subscription.
  ///
  /// * [String] taxBehavior:
  ///   Only required if a [default tax behavior](https://stripe.com/docs/tax/products-prices-tax-categories-tax-behavior#setting-a-default-tax-behavior-(recommended)) was not provided in the Stripe Tax settings. Specifies whether the price is considered inclusive of taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or `unspecified`. Once specified as either `inclusive` or `exclusive`, it cannot be changed.
  ///
  /// * [PostInvoiceitemsRequestTaxCode] taxCode:
  ///
  /// * [List<String>] taxRates:
  ///   The tax rates which apply to the invoice item. When set, the `default_tax_rates` on the invoice do not apply to this invoice item.
  ///
  /// * [double] unitAmountDecimal:
  ///   The decimal unit amount in cents (or local equivalent) of the charge to be applied to the upcoming invoice. This `unit_amount_decimal` will be multiplied by the quantity to get the full amount. Passing in a negative `unit_amount_decimal` will reduce the `amount_due` on the invoice. Accepts at most 12 decimal places.
  Future<Response> postInvoiceitemsWithHttpInfo(String customer, { int? amount, String? currency, String? description, bool? discountable, PostInvoiceitemsRequestDiscounts? discounts, List<String>? expand, String? invoice, PostAccountsRequestMetadata? metadata, Period? period, OneTimePriceData? priceData, PricingParam? pricing, int? quantity, String? subscription, String? taxBehavior, PostInvoiceitemsRequestTaxCode? taxCode, List<String>? taxRates, double? unitAmountDecimal, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/invoiceitems';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (customer != null) {
      formParams[r'customer'] = parameterToString(customer);
    }
    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (discountable != null) {
      formParams[r'discountable'] = parameterToString(discountable);
    }
    if (discounts != null) {
      formParams[r'discounts'] = parameterToString(discounts);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (invoice != null) {
      formParams[r'invoice'] = parameterToString(invoice);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (period != null) {
      formParams[r'period'] = parameterToString(period);
    }
    if (priceData != null) {
      formParams[r'price_data'] = parameterToString(priceData);
    }
    if (pricing != null) {
      formParams[r'pricing'] = parameterToString(pricing);
    }
    if (quantity != null) {
      formParams[r'quantity'] = parameterToString(quantity);
    }
    if (subscription != null) {
      formParams[r'subscription'] = parameterToString(subscription);
    }
    if (taxBehavior != null) {
      formParams[r'tax_behavior'] = parameterToString(taxBehavior);
    }
    if (taxCode != null) {
      formParams[r'tax_code'] = parameterToString(taxCode);
    }
    if (taxRates != null) {
      formParams[r'tax_rates'] = parameterToString(taxRates);
    }
    if (unitAmountDecimal != null) {
      formParams[r'unit_amount_decimal'] = parameterToString(unitAmountDecimal);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create an invoice item
  ///
  /// <p>Creates an item to be added to a draft invoice (up to 250 items per invoice). If no invoice is specified, the item will be on the next invoice created for the customer specified.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///   The ID of the customer who will be billed when this invoice item is billed.
  ///
  /// * [int] amount:
  ///   The integer amount in cents (or local equivalent) of the charge to be applied to the upcoming invoice. Passing in a negative `amount` will reduce the `amount_due` on the invoice.
  ///
  /// * [String] currency:
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] description:
  ///   An arbitrary string which you can attach to the invoice item. The description is displayed in the invoice for easy tracking.
  ///
  /// * [bool] discountable:
  ///   Controls whether discounts apply to this invoice item. Defaults to false for prorations or negative invoice items, and true for all other invoice items.
  ///
  /// * [PostInvoiceitemsRequestDiscounts] discounts:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] invoice:
  ///   The ID of an existing invoice to add this invoice item to. For subscription invoices, when left blank, the invoice item will be added to the next upcoming scheduled invoice. For standalone invoices, the invoice item won't be automatically added unless you pass `pending_invoice_item_behavior: 'include'` when creating the invoice. This is useful when adding invoice items in response to an invoice.created webhook. You can only add invoice items to draft invoices and there is a maximum of 250 items per invoice.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [Period] period:
  ///
  /// * [OneTimePriceData] priceData:
  ///
  /// * [PricingParam] pricing:
  ///
  /// * [int] quantity:
  ///   Non-negative integer. The quantity of units for the invoice item.
  ///
  /// * [String] subscription:
  ///   The ID of a subscription to add this invoice item to. When left blank, the invoice item is added to the next upcoming scheduled invoice. When set, scheduled invoices for subscriptions other than the specified subscription will ignore the invoice item. Use this when you want to express that an invoice item has been accrued within the context of a particular subscription.
  ///
  /// * [String] taxBehavior:
  ///   Only required if a [default tax behavior](https://stripe.com/docs/tax/products-prices-tax-categories-tax-behavior#setting-a-default-tax-behavior-(recommended)) was not provided in the Stripe Tax settings. Specifies whether the price is considered inclusive of taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or `unspecified`. Once specified as either `inclusive` or `exclusive`, it cannot be changed.
  ///
  /// * [PostInvoiceitemsRequestTaxCode] taxCode:
  ///
  /// * [List<String>] taxRates:
  ///   The tax rates which apply to the invoice item. When set, the `default_tax_rates` on the invoice do not apply to this invoice item.
  ///
  /// * [double] unitAmountDecimal:
  ///   The decimal unit amount in cents (or local equivalent) of the charge to be applied to the upcoming invoice. This `unit_amount_decimal` will be multiplied by the quantity to get the full amount. Passing in a negative `unit_amount_decimal` will reduce the `amount_due` on the invoice. Accepts at most 12 decimal places.
  Future<Invoiceitem?> postInvoiceitems(String customer, { int? amount, String? currency, String? description, bool? discountable, PostInvoiceitemsRequestDiscounts? discounts, List<String>? expand, String? invoice, PostAccountsRequestMetadata? metadata, Period? period, OneTimePriceData? priceData, PricingParam? pricing, int? quantity, String? subscription, String? taxBehavior, PostInvoiceitemsRequestTaxCode? taxCode, List<String>? taxRates, double? unitAmountDecimal, }) async {
    final response = await postInvoiceitemsWithHttpInfo(customer,  amount: amount, currency: currency, description: description, discountable: discountable, discounts: discounts, expand: expand, invoice: invoice, metadata: metadata, period: period, priceData: priceData, pricing: pricing, quantity: quantity, subscription: subscription, taxBehavior: taxBehavior, taxCode: taxCode, taxRates: taxRates, unitAmountDecimal: unitAmountDecimal, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Invoiceitem',) as Invoiceitem;
    
    }
    return null;
  }

  /// Update an invoice item
  ///
  /// <p>Updates the amount or description of an invoice item on an upcoming invoice. Updating an invoice item is only possible before the invoice it’s attached to is closed.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] invoiceitem (required):
  ///
  /// * [int] amount:
  ///   The integer amount in cents (or local equivalent) of the charge to be applied to the upcoming invoice. If you want to apply a credit to the customer's account, pass a negative amount.
  ///
  /// * [String] description:
  ///   An arbitrary string which you can attach to the invoice item. The description is displayed in the invoice for easy tracking.
  ///
  /// * [bool] discountable:
  ///   Controls whether discounts apply to this invoice item. Defaults to false for prorations or negative invoice items, and true for all other invoice items. Cannot be set to true for prorations.
  ///
  /// * [PostInvoiceitemsInvoiceitemRequestDiscounts] discounts:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [Period] period:
  ///
  /// * [OneTimePriceData] priceData:
  ///
  /// * [PricingParam] pricing:
  ///
  /// * [int] quantity:
  ///   Non-negative integer. The quantity of units for the invoice item.
  ///
  /// * [String] taxBehavior:
  ///   Only required if a [default tax behavior](https://stripe.com/docs/tax/products-prices-tax-categories-tax-behavior#setting-a-default-tax-behavior-(recommended)) was not provided in the Stripe Tax settings. Specifies whether the price is considered inclusive of taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or `unspecified`. Once specified as either `inclusive` or `exclusive`, it cannot be changed.
  ///
  /// * [PostInvoiceitemsRequestTaxCode] taxCode:
  ///
  /// * [PostInvoiceitemsInvoiceitemRequestTaxRates] taxRates:
  ///
  /// * [double] unitAmountDecimal:
  ///   The decimal unit amount in cents (or local equivalent) of the charge to be applied to the upcoming invoice. This `unit_amount_decimal` will be multiplied by the quantity to get the full amount. Passing in a negative `unit_amount_decimal` will reduce the `amount_due` on the invoice. Accepts at most 12 decimal places.
  Future<Response> postInvoiceitemsInvoiceitemWithHttpInfo(String invoiceitem, { int? amount, String? description, bool? discountable, PostInvoiceitemsInvoiceitemRequestDiscounts? discounts, List<String>? expand, PostAccountsRequestMetadata? metadata, Period? period, OneTimePriceData? priceData, PricingParam? pricing, int? quantity, String? taxBehavior, PostInvoiceitemsRequestTaxCode? taxCode, PostInvoiceitemsInvoiceitemRequestTaxRates? taxRates, double? unitAmountDecimal, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/invoiceitems/{invoiceitem}'
      .replaceAll('{invoiceitem}', invoiceitem);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (discountable != null) {
      formParams[r'discountable'] = parameterToString(discountable);
    }
    if (discounts != null) {
      formParams[r'discounts'] = parameterToString(discounts);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (period != null) {
      formParams[r'period'] = parameterToString(period);
    }
    if (priceData != null) {
      formParams[r'price_data'] = parameterToString(priceData);
    }
    if (pricing != null) {
      formParams[r'pricing'] = parameterToString(pricing);
    }
    if (quantity != null) {
      formParams[r'quantity'] = parameterToString(quantity);
    }
    if (taxBehavior != null) {
      formParams[r'tax_behavior'] = parameterToString(taxBehavior);
    }
    if (taxCode != null) {
      formParams[r'tax_code'] = parameterToString(taxCode);
    }
    if (taxRates != null) {
      formParams[r'tax_rates'] = parameterToString(taxRates);
    }
    if (unitAmountDecimal != null) {
      formParams[r'unit_amount_decimal'] = parameterToString(unitAmountDecimal);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update an invoice item
  ///
  /// <p>Updates the amount or description of an invoice item on an upcoming invoice. Updating an invoice item is only possible before the invoice it’s attached to is closed.</p>
  ///
  /// Parameters:
  ///
  /// * [String] invoiceitem (required):
  ///
  /// * [int] amount:
  ///   The integer amount in cents (or local equivalent) of the charge to be applied to the upcoming invoice. If you want to apply a credit to the customer's account, pass a negative amount.
  ///
  /// * [String] description:
  ///   An arbitrary string which you can attach to the invoice item. The description is displayed in the invoice for easy tracking.
  ///
  /// * [bool] discountable:
  ///   Controls whether discounts apply to this invoice item. Defaults to false for prorations or negative invoice items, and true for all other invoice items. Cannot be set to true for prorations.
  ///
  /// * [PostInvoiceitemsInvoiceitemRequestDiscounts] discounts:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [Period] period:
  ///
  /// * [OneTimePriceData] priceData:
  ///
  /// * [PricingParam] pricing:
  ///
  /// * [int] quantity:
  ///   Non-negative integer. The quantity of units for the invoice item.
  ///
  /// * [String] taxBehavior:
  ///   Only required if a [default tax behavior](https://stripe.com/docs/tax/products-prices-tax-categories-tax-behavior#setting-a-default-tax-behavior-(recommended)) was not provided in the Stripe Tax settings. Specifies whether the price is considered inclusive of taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or `unspecified`. Once specified as either `inclusive` or `exclusive`, it cannot be changed.
  ///
  /// * [PostInvoiceitemsRequestTaxCode] taxCode:
  ///
  /// * [PostInvoiceitemsInvoiceitemRequestTaxRates] taxRates:
  ///
  /// * [double] unitAmountDecimal:
  ///   The decimal unit amount in cents (or local equivalent) of the charge to be applied to the upcoming invoice. This `unit_amount_decimal` will be multiplied by the quantity to get the full amount. Passing in a negative `unit_amount_decimal` will reduce the `amount_due` on the invoice. Accepts at most 12 decimal places.
  Future<Invoiceitem?> postInvoiceitemsInvoiceitem(String invoiceitem, { int? amount, String? description, bool? discountable, PostInvoiceitemsInvoiceitemRequestDiscounts? discounts, List<String>? expand, PostAccountsRequestMetadata? metadata, Period? period, OneTimePriceData? priceData, PricingParam? pricing, int? quantity, String? taxBehavior, PostInvoiceitemsRequestTaxCode? taxCode, PostInvoiceitemsInvoiceitemRequestTaxRates? taxRates, double? unitAmountDecimal, }) async {
    final response = await postInvoiceitemsInvoiceitemWithHttpInfo(invoiceitem,  amount: amount, description: description, discountable: discountable, discounts: discounts, expand: expand, metadata: metadata, period: period, priceData: priceData, pricing: pricing, quantity: quantity, taxBehavior: taxBehavior, taxCode: taxCode, taxRates: taxRates, unitAmountDecimal: unitAmountDecimal, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Invoiceitem',) as Invoiceitem;
    
    }
    return null;
  }

  /// Create an invoice
  ///
  /// <p>This endpoint creates a draft invoice for a given customer. The invoice remains a draft until you <a href=\"#finalize_invoice\">finalize</a> the invoice, which allows you to <a href=\"#pay_invoice\">pay</a> or <a href=\"#send_invoice\">send</a> the invoice to your customers.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [PostInvoicesRequestAccountTaxIds] accountTaxIds:
  ///
  /// * [int] applicationFeeAmount:
  ///   A fee in cents (or local equivalent) that will be applied to the invoice and transferred to the application owner's Stripe account. The request must be made with an OAuth key or the Stripe-Account header in order to take an application fee. For more information, see the application fees [documentation](https://stripe.com/docs/billing/invoices/connect#collecting-fees).
  ///
  /// * [bool] autoAdvance:
  ///   Controls whether Stripe performs [automatic collection](https://stripe.com/docs/invoicing/integration/automatic-advancement-collection) of the invoice. If `false`, the invoice's state doesn't automatically advance without an explicit action. Defaults to false.
  ///
  /// * [AutomaticTaxParam] automaticTax:
  ///
  /// * [int] automaticallyFinalizesAt:
  ///   The time when this invoice should be scheduled to finalize (up to 5 years in the future). The invoice is finalized at this time if it's still in draft state.
  ///
  /// * [String] collectionMethod:
  ///   Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay this invoice using the default source attached to the customer. When sending an invoice, Stripe will email this invoice to the customer with payment instructions. Defaults to `charge_automatically`.
  ///
  /// * [String] currency:
  ///   The currency to create this invoice in. Defaults to that of `customer` if not specified.
  ///
  /// * [PostInvoicesRequestCustomFields] customFields:
  ///
  /// * [String] customer:
  ///   The ID of the customer who will be billed.
  ///
  /// * [int] daysUntilDue:
  ///   The number of days from when the invoice is created until it is due. Valid only for invoices where `collection_method=send_invoice`.
  ///
  /// * [String] defaultPaymentMethod:
  ///   ID of the default payment method for the invoice. It must belong to the customer associated with the invoice. If not set, defaults to the subscription's default payment method, if any, or to the default payment method in the customer's invoice settings.
  ///
  /// * [String] defaultSource:
  ///   ID of the default payment source for the invoice. It must belong to the customer associated with the invoice and be in a chargeable state. If not set, defaults to the subscription's default source, if any, or to the customer's default source.
  ///
  /// * [List<String>] defaultTaxRates:
  ///   The tax rates that will apply to any line item that does not have `tax_rates` set.
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users. Referenced as 'memo' in the Dashboard.
  ///
  /// * [PostInvoicesRequestDiscounts] discounts:
  ///
  /// * [int] dueDate:
  ///   The date on which payment for this invoice is due. Valid only for invoices where `collection_method=send_invoice`.
  ///
  /// * [int] effectiveAt:
  ///   The date when this invoice is in effect. Same as `finalized_at` unless overwritten. When defined, this value replaces the system-generated 'Date of issue' printed on the invoice PDF and receipt.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] footer:
  ///   Footer to be displayed on the invoice.
  ///
  /// * [FromInvoice] fromInvoice:
  ///
  /// * [Param1] issuer:
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] number:
  ///   Set the number for this invoice. If no number is present then a number will be assigned automatically when the invoice is finalized. In many markets, regulations require invoices to be unique, sequential and / or gapless. You are responsible for ensuring this is true across all your different invoicing systems in the event that you edit the invoice number using our API. If you use only Stripe for your invoices and do not change invoice numbers, Stripe handles this aspect of compliance for you automatically.
  ///
  /// * [String] onBehalfOf:
  ///   The account (if any) for which the funds of the invoice payment are intended. If set, the invoice will be presented with the branding and support information of the specified account. See the [Invoices with Connect](https://stripe.com/docs/billing/invoices/connect) documentation for details.
  ///
  /// * [PaymentSettings1] paymentSettings:
  ///
  /// * [String] pendingInvoiceItemsBehavior:
  ///   How to handle pending invoice items on invoice creation. Defaults to `exclude` if the parameter is omitted.
  ///
  /// * [RenderingParam] rendering:
  ///
  /// * [ShippingCost] shippingCost:
  ///
  /// * [RecipientShippingWithOptionalFieldsAddress] shippingDetails:
  ///
  /// * [String] statementDescriptor:
  ///   Extra information about a charge for the customer's credit card statement. It must contain at least one letter. If not specified and this invoice is part of a subscription, the default `statement_descriptor` will be set to the first subscription item's product's `statement_descriptor`.
  ///
  /// * [String] subscription:
  ///   The ID of the subscription to invoice, if any. If set, the created invoice will only include pending invoice items for that subscription. The subscription's billing cycle and regular subscription events won't be affected.
  ///
  /// * [TransferDataSpecs4] transferData:
  Future<Response> postInvoicesWithHttpInfo({ PostInvoicesRequestAccountTaxIds? accountTaxIds, int? applicationFeeAmount, bool? autoAdvance, AutomaticTaxParam? automaticTax, int? automaticallyFinalizesAt, String? collectionMethod, String? currency, PostInvoicesRequestCustomFields? customFields, String? customer, int? daysUntilDue, String? defaultPaymentMethod, String? defaultSource, List<String>? defaultTaxRates, String? description, PostInvoicesRequestDiscounts? discounts, int? dueDate, int? effectiveAt, List<String>? expand, String? footer, FromInvoice? fromInvoice, Param1? issuer, PostAccountsRequestMetadata? metadata, String? number, String? onBehalfOf, PaymentSettings1? paymentSettings, String? pendingInvoiceItemsBehavior, RenderingParam? rendering, ShippingCost? shippingCost, RecipientShippingWithOptionalFieldsAddress? shippingDetails, String? statementDescriptor, String? subscription, TransferDataSpecs4? transferData, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/invoices';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (accountTaxIds != null) {
      formParams[r'account_tax_ids'] = parameterToString(accountTaxIds);
    }
    if (applicationFeeAmount != null) {
      formParams[r'application_fee_amount'] = parameterToString(applicationFeeAmount);
    }
    if (autoAdvance != null) {
      formParams[r'auto_advance'] = parameterToString(autoAdvance);
    }
    if (automaticTax != null) {
      formParams[r'automatic_tax'] = parameterToString(automaticTax);
    }
    if (automaticallyFinalizesAt != null) {
      formParams[r'automatically_finalizes_at'] = parameterToString(automaticallyFinalizesAt);
    }
    if (collectionMethod != null) {
      formParams[r'collection_method'] = parameterToString(collectionMethod);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (customFields != null) {
      formParams[r'custom_fields'] = parameterToString(customFields);
    }
    if (customer != null) {
      formParams[r'customer'] = parameterToString(customer);
    }
    if (daysUntilDue != null) {
      formParams[r'days_until_due'] = parameterToString(daysUntilDue);
    }
    if (defaultPaymentMethod != null) {
      formParams[r'default_payment_method'] = parameterToString(defaultPaymentMethod);
    }
    if (defaultSource != null) {
      formParams[r'default_source'] = parameterToString(defaultSource);
    }
    if (defaultTaxRates != null) {
      formParams[r'default_tax_rates'] = parameterToString(defaultTaxRates);
    }
    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (discounts != null) {
      formParams[r'discounts'] = parameterToString(discounts);
    }
    if (dueDate != null) {
      formParams[r'due_date'] = parameterToString(dueDate);
    }
    if (effectiveAt != null) {
      formParams[r'effective_at'] = parameterToString(effectiveAt);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (footer != null) {
      formParams[r'footer'] = parameterToString(footer);
    }
    if (fromInvoice != null) {
      formParams[r'from_invoice'] = parameterToString(fromInvoice);
    }
    if (issuer != null) {
      formParams[r'issuer'] = parameterToString(issuer);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (number != null) {
      formParams[r'number'] = parameterToString(number);
    }
    if (onBehalfOf != null) {
      formParams[r'on_behalf_of'] = parameterToString(onBehalfOf);
    }
    if (paymentSettings != null) {
      formParams[r'payment_settings'] = parameterToString(paymentSettings);
    }
    if (pendingInvoiceItemsBehavior != null) {
      formParams[r'pending_invoice_items_behavior'] = parameterToString(pendingInvoiceItemsBehavior);
    }
    if (rendering != null) {
      formParams[r'rendering'] = parameterToString(rendering);
    }
    if (shippingCost != null) {
      formParams[r'shipping_cost'] = parameterToString(shippingCost);
    }
    if (shippingDetails != null) {
      formParams[r'shipping_details'] = parameterToString(shippingDetails);
    }
    if (statementDescriptor != null) {
      formParams[r'statement_descriptor'] = parameterToString(statementDescriptor);
    }
    if (subscription != null) {
      formParams[r'subscription'] = parameterToString(subscription);
    }
    if (transferData != null) {
      formParams[r'transfer_data'] = parameterToString(transferData);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create an invoice
  ///
  /// <p>This endpoint creates a draft invoice for a given customer. The invoice remains a draft until you <a href=\"#finalize_invoice\">finalize</a> the invoice, which allows you to <a href=\"#pay_invoice\">pay</a> or <a href=\"#send_invoice\">send</a> the invoice to your customers.</p>
  ///
  /// Parameters:
  ///
  /// * [PostInvoicesRequestAccountTaxIds] accountTaxIds:
  ///
  /// * [int] applicationFeeAmount:
  ///   A fee in cents (or local equivalent) that will be applied to the invoice and transferred to the application owner's Stripe account. The request must be made with an OAuth key or the Stripe-Account header in order to take an application fee. For more information, see the application fees [documentation](https://stripe.com/docs/billing/invoices/connect#collecting-fees).
  ///
  /// * [bool] autoAdvance:
  ///   Controls whether Stripe performs [automatic collection](https://stripe.com/docs/invoicing/integration/automatic-advancement-collection) of the invoice. If `false`, the invoice's state doesn't automatically advance without an explicit action. Defaults to false.
  ///
  /// * [AutomaticTaxParam] automaticTax:
  ///
  /// * [int] automaticallyFinalizesAt:
  ///   The time when this invoice should be scheduled to finalize (up to 5 years in the future). The invoice is finalized at this time if it's still in draft state.
  ///
  /// * [String] collectionMethod:
  ///   Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay this invoice using the default source attached to the customer. When sending an invoice, Stripe will email this invoice to the customer with payment instructions. Defaults to `charge_automatically`.
  ///
  /// * [String] currency:
  ///   The currency to create this invoice in. Defaults to that of `customer` if not specified.
  ///
  /// * [PostInvoicesRequestCustomFields] customFields:
  ///
  /// * [String] customer:
  ///   The ID of the customer who will be billed.
  ///
  /// * [int] daysUntilDue:
  ///   The number of days from when the invoice is created until it is due. Valid only for invoices where `collection_method=send_invoice`.
  ///
  /// * [String] defaultPaymentMethod:
  ///   ID of the default payment method for the invoice. It must belong to the customer associated with the invoice. If not set, defaults to the subscription's default payment method, if any, or to the default payment method in the customer's invoice settings.
  ///
  /// * [String] defaultSource:
  ///   ID of the default payment source for the invoice. It must belong to the customer associated with the invoice and be in a chargeable state. If not set, defaults to the subscription's default source, if any, or to the customer's default source.
  ///
  /// * [List<String>] defaultTaxRates:
  ///   The tax rates that will apply to any line item that does not have `tax_rates` set.
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users. Referenced as 'memo' in the Dashboard.
  ///
  /// * [PostInvoicesRequestDiscounts] discounts:
  ///
  /// * [int] dueDate:
  ///   The date on which payment for this invoice is due. Valid only for invoices where `collection_method=send_invoice`.
  ///
  /// * [int] effectiveAt:
  ///   The date when this invoice is in effect. Same as `finalized_at` unless overwritten. When defined, this value replaces the system-generated 'Date of issue' printed on the invoice PDF and receipt.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] footer:
  ///   Footer to be displayed on the invoice.
  ///
  /// * [FromInvoice] fromInvoice:
  ///
  /// * [Param1] issuer:
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] number:
  ///   Set the number for this invoice. If no number is present then a number will be assigned automatically when the invoice is finalized. In many markets, regulations require invoices to be unique, sequential and / or gapless. You are responsible for ensuring this is true across all your different invoicing systems in the event that you edit the invoice number using our API. If you use only Stripe for your invoices and do not change invoice numbers, Stripe handles this aspect of compliance for you automatically.
  ///
  /// * [String] onBehalfOf:
  ///   The account (if any) for which the funds of the invoice payment are intended. If set, the invoice will be presented with the branding and support information of the specified account. See the [Invoices with Connect](https://stripe.com/docs/billing/invoices/connect) documentation for details.
  ///
  /// * [PaymentSettings1] paymentSettings:
  ///
  /// * [String] pendingInvoiceItemsBehavior:
  ///   How to handle pending invoice items on invoice creation. Defaults to `exclude` if the parameter is omitted.
  ///
  /// * [RenderingParam] rendering:
  ///
  /// * [ShippingCost] shippingCost:
  ///
  /// * [RecipientShippingWithOptionalFieldsAddress] shippingDetails:
  ///
  /// * [String] statementDescriptor:
  ///   Extra information about a charge for the customer's credit card statement. It must contain at least one letter. If not specified and this invoice is part of a subscription, the default `statement_descriptor` will be set to the first subscription item's product's `statement_descriptor`.
  ///
  /// * [String] subscription:
  ///   The ID of the subscription to invoice, if any. If set, the created invoice will only include pending invoice items for that subscription. The subscription's billing cycle and regular subscription events won't be affected.
  ///
  /// * [TransferDataSpecs4] transferData:
  Future<Invoice?> postInvoices({ PostInvoicesRequestAccountTaxIds? accountTaxIds, int? applicationFeeAmount, bool? autoAdvance, AutomaticTaxParam? automaticTax, int? automaticallyFinalizesAt, String? collectionMethod, String? currency, PostInvoicesRequestCustomFields? customFields, String? customer, int? daysUntilDue, String? defaultPaymentMethod, String? defaultSource, List<String>? defaultTaxRates, String? description, PostInvoicesRequestDiscounts? discounts, int? dueDate, int? effectiveAt, List<String>? expand, String? footer, FromInvoice? fromInvoice, Param1? issuer, PostAccountsRequestMetadata? metadata, String? number, String? onBehalfOf, PaymentSettings1? paymentSettings, String? pendingInvoiceItemsBehavior, RenderingParam? rendering, ShippingCost? shippingCost, RecipientShippingWithOptionalFieldsAddress? shippingDetails, String? statementDescriptor, String? subscription, TransferDataSpecs4? transferData, }) async {
    final response = await postInvoicesWithHttpInfo( accountTaxIds: accountTaxIds, applicationFeeAmount: applicationFeeAmount, autoAdvance: autoAdvance, automaticTax: automaticTax, automaticallyFinalizesAt: automaticallyFinalizesAt, collectionMethod: collectionMethod, currency: currency, customFields: customFields, customer: customer, daysUntilDue: daysUntilDue, defaultPaymentMethod: defaultPaymentMethod, defaultSource: defaultSource, defaultTaxRates: defaultTaxRates, description: description, discounts: discounts, dueDate: dueDate, effectiveAt: effectiveAt, expand: expand, footer: footer, fromInvoice: fromInvoice, issuer: issuer, metadata: metadata, number: number, onBehalfOf: onBehalfOf, paymentSettings: paymentSettings, pendingInvoiceItemsBehavior: pendingInvoiceItemsBehavior, rendering: rendering, shippingCost: shippingCost, shippingDetails: shippingDetails, statementDescriptor: statementDescriptor, subscription: subscription, transferData: transferData, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Invoice',) as Invoice;
    
    }
    return null;
  }

  /// Create a preview invoice
  ///
  /// <p>At any time, you can preview the upcoming invoice for a subscription or subscription schedule. This will show you all the charges that are pending, including subscription renewal charges, invoice item charges, etc. It will also show you any discounts that are applicable to the invoice.</p>  <p>You can also preview the effects of creating or updating a subscription or subscription schedule, including a preview of any prorations that will take place. To ensure that the actual proration is calculated exactly the same as the previewed proration, you should pass the <code>subscription_details.proration_date</code> parameter when doing the actual subscription update.</p>  <p>The recommended way to get only the prorations being previewed on the invoice is to consider line items where <code>parent.subscription_item_details.proration</code> is <code>true</code>.</p>  <p>Note that when you are viewing an upcoming invoice, you are simply viewing a preview – the invoice has not yet been created. As such, the upcoming invoice will not show up in invoice listing calls, and you cannot use the API to pay or edit the invoice. If you want to change the amount that your customer will be billed, you can add, remove, or update pending invoice items, or update the customer’s discount.</p>  <p>Note: Currency conversion calculations use the latest exchange rates. Exchange rates may vary between the time of the preview and the time of the actual invoice creation. <a href=\"https://docs.stripe.com/currencies/conversions\">Learn more</a></p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [AutomaticTaxParam1] automaticTax:
  ///
  /// * [String] currency:
  ///   The currency to preview this invoice in. Defaults to that of `customer` if not specified.
  ///
  /// * [String] customer:
  ///   The identifier of the customer whose upcoming invoice you'd like to retrieve. If `automatic_tax` is enabled then one of `customer`, `customer_details`, `subscription`, or `schedule` must be set.
  ///
  /// * [CustomerDetailsParam] customerDetails:
  ///
  /// * [PostInvoicesCreatePreviewRequestDiscounts] discounts:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [List<InvoiceItemPreviewParams>] invoiceItems:
  ///   List of invoice items to add or update in the upcoming invoice preview (up to 250).
  ///
  /// * [Param1] issuer:
  ///
  /// * [PostInvoicesCreatePreviewRequestOnBehalfOf] onBehalfOf:
  ///
  /// * [String] previewMode:
  ///   Customizes the types of values to include when calculating the invoice. Defaults to `next` if unspecified.
  ///
  /// * [String] schedule:
  ///   The identifier of the schedule whose upcoming invoice you'd like to retrieve. Cannot be used with subscription or subscription fields.
  ///
  /// * [ScheduleDetailsParams] scheduleDetails:
  ///
  /// * [String] subscription:
  ///   The identifier of the subscription for which you'd like to retrieve the upcoming invoice. If not provided, but a `subscription_details.items` is provided, you will preview creating a subscription with those items. If neither `subscription` nor `subscription_details.items` is provided, you will retrieve the next upcoming invoice from among the customer's subscriptions.
  ///
  /// * [SubscriptionDetailsParams] subscriptionDetails:
  Future<Response> postInvoicesCreatePreviewWithHttpInfo({ AutomaticTaxParam1? automaticTax, String? currency, String? customer, CustomerDetailsParam? customerDetails, PostInvoicesCreatePreviewRequestDiscounts? discounts, List<String>? expand, List<InvoiceItemPreviewParams>? invoiceItems, Param1? issuer, PostInvoicesCreatePreviewRequestOnBehalfOf? onBehalfOf, String? previewMode, String? schedule, ScheduleDetailsParams? scheduleDetails, String? subscription, SubscriptionDetailsParams? subscriptionDetails, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/invoices/create_preview';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (automaticTax != null) {
      formParams[r'automatic_tax'] = parameterToString(automaticTax);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (customer != null) {
      formParams[r'customer'] = parameterToString(customer);
    }
    if (customerDetails != null) {
      formParams[r'customer_details'] = parameterToString(customerDetails);
    }
    if (discounts != null) {
      formParams[r'discounts'] = parameterToString(discounts);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (invoiceItems != null) {
      formParams[r'invoice_items'] = parameterToString(invoiceItems);
    }
    if (issuer != null) {
      formParams[r'issuer'] = parameterToString(issuer);
    }
    if (onBehalfOf != null) {
      formParams[r'on_behalf_of'] = parameterToString(onBehalfOf);
    }
    if (previewMode != null) {
      formParams[r'preview_mode'] = parameterToString(previewMode);
    }
    if (schedule != null) {
      formParams[r'schedule'] = parameterToString(schedule);
    }
    if (scheduleDetails != null) {
      formParams[r'schedule_details'] = parameterToString(scheduleDetails);
    }
    if (subscription != null) {
      formParams[r'subscription'] = parameterToString(subscription);
    }
    if (subscriptionDetails != null) {
      formParams[r'subscription_details'] = parameterToString(subscriptionDetails);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a preview invoice
  ///
  /// <p>At any time, you can preview the upcoming invoice for a subscription or subscription schedule. This will show you all the charges that are pending, including subscription renewal charges, invoice item charges, etc. It will also show you any discounts that are applicable to the invoice.</p>  <p>You can also preview the effects of creating or updating a subscription or subscription schedule, including a preview of any prorations that will take place. To ensure that the actual proration is calculated exactly the same as the previewed proration, you should pass the <code>subscription_details.proration_date</code> parameter when doing the actual subscription update.</p>  <p>The recommended way to get only the prorations being previewed on the invoice is to consider line items where <code>parent.subscription_item_details.proration</code> is <code>true</code>.</p>  <p>Note that when you are viewing an upcoming invoice, you are simply viewing a preview – the invoice has not yet been created. As such, the upcoming invoice will not show up in invoice listing calls, and you cannot use the API to pay or edit the invoice. If you want to change the amount that your customer will be billed, you can add, remove, or update pending invoice items, or update the customer’s discount.</p>  <p>Note: Currency conversion calculations use the latest exchange rates. Exchange rates may vary between the time of the preview and the time of the actual invoice creation. <a href=\"https://docs.stripe.com/currencies/conversions\">Learn more</a></p>
  ///
  /// Parameters:
  ///
  /// * [AutomaticTaxParam1] automaticTax:
  ///
  /// * [String] currency:
  ///   The currency to preview this invoice in. Defaults to that of `customer` if not specified.
  ///
  /// * [String] customer:
  ///   The identifier of the customer whose upcoming invoice you'd like to retrieve. If `automatic_tax` is enabled then one of `customer`, `customer_details`, `subscription`, or `schedule` must be set.
  ///
  /// * [CustomerDetailsParam] customerDetails:
  ///
  /// * [PostInvoicesCreatePreviewRequestDiscounts] discounts:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [List<InvoiceItemPreviewParams>] invoiceItems:
  ///   List of invoice items to add or update in the upcoming invoice preview (up to 250).
  ///
  /// * [Param1] issuer:
  ///
  /// * [PostInvoicesCreatePreviewRequestOnBehalfOf] onBehalfOf:
  ///
  /// * [String] previewMode:
  ///   Customizes the types of values to include when calculating the invoice. Defaults to `next` if unspecified.
  ///
  /// * [String] schedule:
  ///   The identifier of the schedule whose upcoming invoice you'd like to retrieve. Cannot be used with subscription or subscription fields.
  ///
  /// * [ScheduleDetailsParams] scheduleDetails:
  ///
  /// * [String] subscription:
  ///   The identifier of the subscription for which you'd like to retrieve the upcoming invoice. If not provided, but a `subscription_details.items` is provided, you will preview creating a subscription with those items. If neither `subscription` nor `subscription_details.items` is provided, you will retrieve the next upcoming invoice from among the customer's subscriptions.
  ///
  /// * [SubscriptionDetailsParams] subscriptionDetails:
  Future<Invoice?> postInvoicesCreatePreview({ AutomaticTaxParam1? automaticTax, String? currency, String? customer, CustomerDetailsParam? customerDetails, PostInvoicesCreatePreviewRequestDiscounts? discounts, List<String>? expand, List<InvoiceItemPreviewParams>? invoiceItems, Param1? issuer, PostInvoicesCreatePreviewRequestOnBehalfOf? onBehalfOf, String? previewMode, String? schedule, ScheduleDetailsParams? scheduleDetails, String? subscription, SubscriptionDetailsParams? subscriptionDetails, }) async {
    final response = await postInvoicesCreatePreviewWithHttpInfo( automaticTax: automaticTax, currency: currency, customer: customer, customerDetails: customerDetails, discounts: discounts, expand: expand, invoiceItems: invoiceItems, issuer: issuer, onBehalfOf: onBehalfOf, previewMode: previewMode, schedule: schedule, scheduleDetails: scheduleDetails, subscription: subscription, subscriptionDetails: subscriptionDetails, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Invoice',) as Invoice;
    
    }
    return null;
  }

  /// Update an invoice
  ///
  /// <p>Draft invoices are fully editable. Once an invoice is <a href=\"/docs/billing/invoices/workflow#finalized\">finalized</a>, monetary values, as well as <code>collection_method</code>, become uneditable.</p>  <p>If you would like to stop the Stripe Billing engine from automatically finalizing, reattempting payments on, sending reminders for, or <a href=\"/docs/billing/invoices/reconciliation\">automatically reconciling</a> invoices, pass <code>auto_advance=false</code>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  ///
  /// * [PostInvoicesRequestAccountTaxIds] accountTaxIds:
  ///
  /// * [int] applicationFeeAmount:
  ///   A fee in cents (or local equivalent) that will be applied to the invoice and transferred to the application owner's Stripe account. The request must be made with an OAuth key or the Stripe-Account header in order to take an application fee. For more information, see the application fees [documentation](https://stripe.com/docs/billing/invoices/connect#collecting-fees).
  ///
  /// * [bool] autoAdvance:
  ///   Controls whether Stripe performs [automatic collection](https://stripe.com/docs/invoicing/integration/automatic-advancement-collection) of the invoice.
  ///
  /// * [AutomaticTaxParam] automaticTax:
  ///
  /// * [int] automaticallyFinalizesAt:
  ///   The time when this invoice should be scheduled to finalize (up to 5 years in the future). The invoice is finalized at this time if it's still in draft state. To turn off automatic finalization, set `auto_advance` to false.
  ///
  /// * [String] collectionMethod:
  ///   Either `charge_automatically` or `send_invoice`. This field can be updated only on `draft` invoices.
  ///
  /// * [PostInvoicesInvoiceRequestCustomFields] customFields:
  ///
  /// * [int] daysUntilDue:
  ///   The number of days from which the invoice is created until it is due. Only valid for invoices where `collection_method=send_invoice`. This field can only be updated on `draft` invoices.
  ///
  /// * [String] defaultPaymentMethod:
  ///   ID of the default payment method for the invoice. It must belong to the customer associated with the invoice. If not set, defaults to the subscription's default payment method, if any, or to the default payment method in the customer's invoice settings.
  ///
  /// * [PostInvoicesInvoiceRequestDefaultSource] defaultSource:
  ///
  /// * [PostInvoicesInvoiceRequestDefaultTaxRates] defaultTaxRates:
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users. Referenced as 'memo' in the Dashboard.
  ///
  /// * [PostInvoicesInvoiceRequestDiscounts] discounts:
  ///
  /// * [int] dueDate:
  ///   The date on which payment for this invoice is due. Only valid for invoices where `collection_method=send_invoice`. This field can only be updated on `draft` invoices.
  ///
  /// * [PostInvoicesInvoiceRequestEffectiveAt] effectiveAt:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] footer:
  ///   Footer to be displayed on the invoice.
  ///
  /// * [Param1] issuer:
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [PostInvoicesInvoiceRequestNumber] number:
  ///
  /// * [PostInvoicesCreatePreviewRequestOnBehalfOf] onBehalfOf:
  ///
  /// * [PaymentSettings1] paymentSettings:
  ///
  /// * [RenderingParam] rendering:
  ///
  /// * [PostInvoicesInvoiceRequestShippingCost] shippingCost:
  ///
  /// * [PostInvoicesInvoiceRequestShippingDetails] shippingDetails:
  ///
  /// * [String] statementDescriptor:
  ///   Extra information about a charge for the customer's credit card statement. It must contain at least one letter. If not specified and this invoice is part of a subscription, the default `statement_descriptor` will be set to the first subscription item's product's `statement_descriptor`.
  ///
  /// * [PostInvoicesInvoiceRequestTransferData] transferData:
  Future<Response> postInvoicesInvoiceWithHttpInfo(String invoice, { PostInvoicesRequestAccountTaxIds? accountTaxIds, int? applicationFeeAmount, bool? autoAdvance, AutomaticTaxParam? automaticTax, int? automaticallyFinalizesAt, String? collectionMethod, PostInvoicesInvoiceRequestCustomFields? customFields, int? daysUntilDue, String? defaultPaymentMethod, PostInvoicesInvoiceRequestDefaultSource? defaultSource, PostInvoicesInvoiceRequestDefaultTaxRates? defaultTaxRates, String? description, PostInvoicesInvoiceRequestDiscounts? discounts, int? dueDate, PostInvoicesInvoiceRequestEffectiveAt? effectiveAt, List<String>? expand, String? footer, Param1? issuer, PostAccountsRequestMetadata? metadata, PostInvoicesInvoiceRequestNumber? number, PostInvoicesCreatePreviewRequestOnBehalfOf? onBehalfOf, PaymentSettings1? paymentSettings, RenderingParam? rendering, PostInvoicesInvoiceRequestShippingCost? shippingCost, PostInvoicesInvoiceRequestShippingDetails? shippingDetails, String? statementDescriptor, PostInvoicesInvoiceRequestTransferData? transferData, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/invoices/{invoice}'
      .replaceAll('{invoice}', invoice);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (accountTaxIds != null) {
      formParams[r'account_tax_ids'] = parameterToString(accountTaxIds);
    }
    if (applicationFeeAmount != null) {
      formParams[r'application_fee_amount'] = parameterToString(applicationFeeAmount);
    }
    if (autoAdvance != null) {
      formParams[r'auto_advance'] = parameterToString(autoAdvance);
    }
    if (automaticTax != null) {
      formParams[r'automatic_tax'] = parameterToString(automaticTax);
    }
    if (automaticallyFinalizesAt != null) {
      formParams[r'automatically_finalizes_at'] = parameterToString(automaticallyFinalizesAt);
    }
    if (collectionMethod != null) {
      formParams[r'collection_method'] = parameterToString(collectionMethod);
    }
    if (customFields != null) {
      formParams[r'custom_fields'] = parameterToString(customFields);
    }
    if (daysUntilDue != null) {
      formParams[r'days_until_due'] = parameterToString(daysUntilDue);
    }
    if (defaultPaymentMethod != null) {
      formParams[r'default_payment_method'] = parameterToString(defaultPaymentMethod);
    }
    if (defaultSource != null) {
      formParams[r'default_source'] = parameterToString(defaultSource);
    }
    if (defaultTaxRates != null) {
      formParams[r'default_tax_rates'] = parameterToString(defaultTaxRates);
    }
    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (discounts != null) {
      formParams[r'discounts'] = parameterToString(discounts);
    }
    if (dueDate != null) {
      formParams[r'due_date'] = parameterToString(dueDate);
    }
    if (effectiveAt != null) {
      formParams[r'effective_at'] = parameterToString(effectiveAt);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (footer != null) {
      formParams[r'footer'] = parameterToString(footer);
    }
    if (issuer != null) {
      formParams[r'issuer'] = parameterToString(issuer);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (number != null) {
      formParams[r'number'] = parameterToString(number);
    }
    if (onBehalfOf != null) {
      formParams[r'on_behalf_of'] = parameterToString(onBehalfOf);
    }
    if (paymentSettings != null) {
      formParams[r'payment_settings'] = parameterToString(paymentSettings);
    }
    if (rendering != null) {
      formParams[r'rendering'] = parameterToString(rendering);
    }
    if (shippingCost != null) {
      formParams[r'shipping_cost'] = parameterToString(shippingCost);
    }
    if (shippingDetails != null) {
      formParams[r'shipping_details'] = parameterToString(shippingDetails);
    }
    if (statementDescriptor != null) {
      formParams[r'statement_descriptor'] = parameterToString(statementDescriptor);
    }
    if (transferData != null) {
      formParams[r'transfer_data'] = parameterToString(transferData);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update an invoice
  ///
  /// <p>Draft invoices are fully editable. Once an invoice is <a href=\"/docs/billing/invoices/workflow#finalized\">finalized</a>, monetary values, as well as <code>collection_method</code>, become uneditable.</p>  <p>If you would like to stop the Stripe Billing engine from automatically finalizing, reattempting payments on, sending reminders for, or <a href=\"/docs/billing/invoices/reconciliation\">automatically reconciling</a> invoices, pass <code>auto_advance=false</code>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  ///
  /// * [PostInvoicesRequestAccountTaxIds] accountTaxIds:
  ///
  /// * [int] applicationFeeAmount:
  ///   A fee in cents (or local equivalent) that will be applied to the invoice and transferred to the application owner's Stripe account. The request must be made with an OAuth key or the Stripe-Account header in order to take an application fee. For more information, see the application fees [documentation](https://stripe.com/docs/billing/invoices/connect#collecting-fees).
  ///
  /// * [bool] autoAdvance:
  ///   Controls whether Stripe performs [automatic collection](https://stripe.com/docs/invoicing/integration/automatic-advancement-collection) of the invoice.
  ///
  /// * [AutomaticTaxParam] automaticTax:
  ///
  /// * [int] automaticallyFinalizesAt:
  ///   The time when this invoice should be scheduled to finalize (up to 5 years in the future). The invoice is finalized at this time if it's still in draft state. To turn off automatic finalization, set `auto_advance` to false.
  ///
  /// * [String] collectionMethod:
  ///   Either `charge_automatically` or `send_invoice`. This field can be updated only on `draft` invoices.
  ///
  /// * [PostInvoicesInvoiceRequestCustomFields] customFields:
  ///
  /// * [int] daysUntilDue:
  ///   The number of days from which the invoice is created until it is due. Only valid for invoices where `collection_method=send_invoice`. This field can only be updated on `draft` invoices.
  ///
  /// * [String] defaultPaymentMethod:
  ///   ID of the default payment method for the invoice. It must belong to the customer associated with the invoice. If not set, defaults to the subscription's default payment method, if any, or to the default payment method in the customer's invoice settings.
  ///
  /// * [PostInvoicesInvoiceRequestDefaultSource] defaultSource:
  ///
  /// * [PostInvoicesInvoiceRequestDefaultTaxRates] defaultTaxRates:
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users. Referenced as 'memo' in the Dashboard.
  ///
  /// * [PostInvoicesInvoiceRequestDiscounts] discounts:
  ///
  /// * [int] dueDate:
  ///   The date on which payment for this invoice is due. Only valid for invoices where `collection_method=send_invoice`. This field can only be updated on `draft` invoices.
  ///
  /// * [PostInvoicesInvoiceRequestEffectiveAt] effectiveAt:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] footer:
  ///   Footer to be displayed on the invoice.
  ///
  /// * [Param1] issuer:
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [PostInvoicesInvoiceRequestNumber] number:
  ///
  /// * [PostInvoicesCreatePreviewRequestOnBehalfOf] onBehalfOf:
  ///
  /// * [PaymentSettings1] paymentSettings:
  ///
  /// * [RenderingParam] rendering:
  ///
  /// * [PostInvoicesInvoiceRequestShippingCost] shippingCost:
  ///
  /// * [PostInvoicesInvoiceRequestShippingDetails] shippingDetails:
  ///
  /// * [String] statementDescriptor:
  ///   Extra information about a charge for the customer's credit card statement. It must contain at least one letter. If not specified and this invoice is part of a subscription, the default `statement_descriptor` will be set to the first subscription item's product's `statement_descriptor`.
  ///
  /// * [PostInvoicesInvoiceRequestTransferData] transferData:
  Future<Invoice?> postInvoicesInvoice(String invoice, { PostInvoicesRequestAccountTaxIds? accountTaxIds, int? applicationFeeAmount, bool? autoAdvance, AutomaticTaxParam? automaticTax, int? automaticallyFinalizesAt, String? collectionMethod, PostInvoicesInvoiceRequestCustomFields? customFields, int? daysUntilDue, String? defaultPaymentMethod, PostInvoicesInvoiceRequestDefaultSource? defaultSource, PostInvoicesInvoiceRequestDefaultTaxRates? defaultTaxRates, String? description, PostInvoicesInvoiceRequestDiscounts? discounts, int? dueDate, PostInvoicesInvoiceRequestEffectiveAt? effectiveAt, List<String>? expand, String? footer, Param1? issuer, PostAccountsRequestMetadata? metadata, PostInvoicesInvoiceRequestNumber? number, PostInvoicesCreatePreviewRequestOnBehalfOf? onBehalfOf, PaymentSettings1? paymentSettings, RenderingParam? rendering, PostInvoicesInvoiceRequestShippingCost? shippingCost, PostInvoicesInvoiceRequestShippingDetails? shippingDetails, String? statementDescriptor, PostInvoicesInvoiceRequestTransferData? transferData, }) async {
    final response = await postInvoicesInvoiceWithHttpInfo(invoice,  accountTaxIds: accountTaxIds, applicationFeeAmount: applicationFeeAmount, autoAdvance: autoAdvance, automaticTax: automaticTax, automaticallyFinalizesAt: automaticallyFinalizesAt, collectionMethod: collectionMethod, customFields: customFields, daysUntilDue: daysUntilDue, defaultPaymentMethod: defaultPaymentMethod, defaultSource: defaultSource, defaultTaxRates: defaultTaxRates, description: description, discounts: discounts, dueDate: dueDate, effectiveAt: effectiveAt, expand: expand, footer: footer, issuer: issuer, metadata: metadata, number: number, onBehalfOf: onBehalfOf, paymentSettings: paymentSettings, rendering: rendering, shippingCost: shippingCost, shippingDetails: shippingDetails, statementDescriptor: statementDescriptor, transferData: transferData, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Invoice',) as Invoice;
    
    }
    return null;
  }

  /// Bulk add invoice line items
  ///
  /// <p>Adds multiple line items to an invoice. This is only possible when an invoice is still a draft.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  ///
  /// * [List<LinesDataParam>] lines (required):
  ///   The line items to add.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] invoiceMetadata:
  Future<Response> postInvoicesInvoiceAddLinesWithHttpInfo(String invoice, List<LinesDataParam> lines, { List<String>? expand, PostAccountsRequestMetadata? invoiceMetadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/invoices/{invoice}/add_lines'
      .replaceAll('{invoice}', invoice);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (invoiceMetadata != null) {
      formParams[r'invoice_metadata'] = parameterToString(invoiceMetadata);
    }
    if (lines != null) {
      formParams[r'lines'] = parameterToString(lines);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Bulk add invoice line items
  ///
  /// <p>Adds multiple line items to an invoice. This is only possible when an invoice is still a draft.</p>
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  ///
  /// * [List<LinesDataParam>] lines (required):
  ///   The line items to add.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] invoiceMetadata:
  Future<Invoice?> postInvoicesInvoiceAddLines(String invoice, List<LinesDataParam> lines, { List<String>? expand, PostAccountsRequestMetadata? invoiceMetadata, }) async {
    final response = await postInvoicesInvoiceAddLinesWithHttpInfo(invoice, lines,  expand: expand, invoiceMetadata: invoiceMetadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Invoice',) as Invoice;
    
    }
    return null;
  }

  /// Attach a payment to an Invoice
  ///
  /// <p>Attaches a PaymentIntent or an Out of Band Payment to the invoice, adding it to the list of <code>payments</code>.</p>  <p>For the PaymentIntent, when the PaymentIntent’s status changes to <code>succeeded</code>, the payment is credited to the invoice, increasing its <code>amount_paid</code>. When the invoice is fully paid, the invoice’s status becomes <code>paid</code>.</p>  <p>If the PaymentIntent’s status is already <code>succeeded</code> when it’s attached, it’s credited to the invoice immediately.</p>  <p>See: <a href=\"/docs/invoicing/partial-payments\">Partial payments</a> to learn more.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] paymentIntent:
  ///   The ID of the PaymentIntent to attach to the invoice.
  Future<Response> postInvoicesInvoiceAttachPaymentWithHttpInfo(String invoice, { List<String>? expand, String? paymentIntent, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/invoices/{invoice}/attach_payment'
      .replaceAll('{invoice}', invoice);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (paymentIntent != null) {
      formParams[r'payment_intent'] = parameterToString(paymentIntent);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Attach a payment to an Invoice
  ///
  /// <p>Attaches a PaymentIntent or an Out of Band Payment to the invoice, adding it to the list of <code>payments</code>.</p>  <p>For the PaymentIntent, when the PaymentIntent’s status changes to <code>succeeded</code>, the payment is credited to the invoice, increasing its <code>amount_paid</code>. When the invoice is fully paid, the invoice’s status becomes <code>paid</code>.</p>  <p>If the PaymentIntent’s status is already <code>succeeded</code> when it’s attached, it’s credited to the invoice immediately.</p>  <p>See: <a href=\"/docs/invoicing/partial-payments\">Partial payments</a> to learn more.</p>
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] paymentIntent:
  ///   The ID of the PaymentIntent to attach to the invoice.
  Future<Invoice?> postInvoicesInvoiceAttachPayment(String invoice, { List<String>? expand, String? paymentIntent, }) async {
    final response = await postInvoicesInvoiceAttachPaymentWithHttpInfo(invoice,  expand: expand, paymentIntent: paymentIntent, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Invoice',) as Invoice;
    
    }
    return null;
  }

  /// Finalize an invoice
  ///
  /// <p>Stripe automatically finalizes drafts before sending and attempting payment on invoices. However, if you’d like to finalize a draft invoice manually, you can do so using this method.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  ///
  /// * [bool] autoAdvance:
  ///   Controls whether Stripe performs [automatic collection](https://stripe.com/docs/invoicing/integration/automatic-advancement-collection) of the invoice. If `false`, the invoice's state doesn't automatically advance without an explicit action.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postInvoicesInvoiceFinalizeWithHttpInfo(String invoice, { bool? autoAdvance, List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/invoices/{invoice}/finalize'
      .replaceAll('{invoice}', invoice);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (autoAdvance != null) {
      formParams[r'auto_advance'] = parameterToString(autoAdvance);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Finalize an invoice
  ///
  /// <p>Stripe automatically finalizes drafts before sending and attempting payment on invoices. However, if you’d like to finalize a draft invoice manually, you can do so using this method.</p>
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  ///
  /// * [bool] autoAdvance:
  ///   Controls whether Stripe performs [automatic collection](https://stripe.com/docs/invoicing/integration/automatic-advancement-collection) of the invoice. If `false`, the invoice's state doesn't automatically advance without an explicit action.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Invoice?> postInvoicesInvoiceFinalize(String invoice, { bool? autoAdvance, List<String>? expand, }) async {
    final response = await postInvoicesInvoiceFinalizeWithHttpInfo(invoice,  autoAdvance: autoAdvance, expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Invoice',) as Invoice;
    
    }
    return null;
  }

  /// Update an invoice's line item
  ///
  /// <p>Updates an invoice’s line item. Some fields, such as <code>tax_amounts</code>, only live on the invoice line item, so they can only be updated through this endpoint. Other fields, such as <code>amount</code>, live on both the invoice item and the invoice line item, so updates on this endpoint will propagate to the invoice item as well. Updating an invoice’s line item is only possible before the invoice is finalized.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  ///   Invoice ID of line item
  ///
  /// * [String] lineItemId (required):
  ///   Invoice line item ID
  ///
  /// * [int] amount:
  ///   The integer amount in cents (or local equivalent) of the charge to be applied to the upcoming invoice. If you want to apply a credit to the customer's account, pass a negative amount.
  ///
  /// * [String] description:
  ///   An arbitrary string which you can attach to the invoice item. The description is displayed in the invoice for easy tracking.
  ///
  /// * [bool] discountable:
  ///   Controls whether discounts apply to this line item. Defaults to false for prorations or negative line items, and true for all other line items. Cannot be set to true for prorations.
  ///
  /// * [PostInvoicesInvoiceLinesLineItemIdRequestDiscounts] discounts:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostInvoicesInvoiceLinesLineItemIdRequestMetadata] metadata:
  ///
  /// * [Period] period:
  ///
  /// * [OneTimePriceDataWithProductData1] priceData:
  ///
  /// * [PricingParam] pricing:
  ///
  /// * [int] quantity:
  ///   Non-negative integer. The quantity of units for the line item.
  ///
  /// * [PostInvoicesInvoiceLinesLineItemIdRequestTaxAmounts] taxAmounts:
  ///
  /// * [PostInvoicesInvoiceLinesLineItemIdRequestTaxRates] taxRates:
  Future<Response> postInvoicesInvoiceLinesLineItemIdWithHttpInfo(String invoice, String lineItemId, { int? amount, String? description, bool? discountable, PostInvoicesInvoiceLinesLineItemIdRequestDiscounts? discounts, List<String>? expand, PostInvoicesInvoiceLinesLineItemIdRequestMetadata? metadata, Period? period, OneTimePriceDataWithProductData1? priceData, PricingParam? pricing, int? quantity, PostInvoicesInvoiceLinesLineItemIdRequestTaxAmounts? taxAmounts, PostInvoicesInvoiceLinesLineItemIdRequestTaxRates? taxRates, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/invoices/{invoice}/lines/{line_item_id}'
      .replaceAll('{invoice}', invoice)
      .replaceAll('{line_item_id}', lineItemId);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (discountable != null) {
      formParams[r'discountable'] = parameterToString(discountable);
    }
    if (discounts != null) {
      formParams[r'discounts'] = parameterToString(discounts);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (period != null) {
      formParams[r'period'] = parameterToString(period);
    }
    if (priceData != null) {
      formParams[r'price_data'] = parameterToString(priceData);
    }
    if (pricing != null) {
      formParams[r'pricing'] = parameterToString(pricing);
    }
    if (quantity != null) {
      formParams[r'quantity'] = parameterToString(quantity);
    }
    if (taxAmounts != null) {
      formParams[r'tax_amounts'] = parameterToString(taxAmounts);
    }
    if (taxRates != null) {
      formParams[r'tax_rates'] = parameterToString(taxRates);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update an invoice's line item
  ///
  /// <p>Updates an invoice’s line item. Some fields, such as <code>tax_amounts</code>, only live on the invoice line item, so they can only be updated through this endpoint. Other fields, such as <code>amount</code>, live on both the invoice item and the invoice line item, so updates on this endpoint will propagate to the invoice item as well. Updating an invoice’s line item is only possible before the invoice is finalized.</p>
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  ///   Invoice ID of line item
  ///
  /// * [String] lineItemId (required):
  ///   Invoice line item ID
  ///
  /// * [int] amount:
  ///   The integer amount in cents (or local equivalent) of the charge to be applied to the upcoming invoice. If you want to apply a credit to the customer's account, pass a negative amount.
  ///
  /// * [String] description:
  ///   An arbitrary string which you can attach to the invoice item. The description is displayed in the invoice for easy tracking.
  ///
  /// * [bool] discountable:
  ///   Controls whether discounts apply to this line item. Defaults to false for prorations or negative line items, and true for all other line items. Cannot be set to true for prorations.
  ///
  /// * [PostInvoicesInvoiceLinesLineItemIdRequestDiscounts] discounts:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostInvoicesInvoiceLinesLineItemIdRequestMetadata] metadata:
  ///
  /// * [Period] period:
  ///
  /// * [OneTimePriceDataWithProductData1] priceData:
  ///
  /// * [PricingParam] pricing:
  ///
  /// * [int] quantity:
  ///   Non-negative integer. The quantity of units for the line item.
  ///
  /// * [PostInvoicesInvoiceLinesLineItemIdRequestTaxAmounts] taxAmounts:
  ///
  /// * [PostInvoicesInvoiceLinesLineItemIdRequestTaxRates] taxRates:
  Future<LineItem?> postInvoicesInvoiceLinesLineItemId(String invoice, String lineItemId, { int? amount, String? description, bool? discountable, PostInvoicesInvoiceLinesLineItemIdRequestDiscounts? discounts, List<String>? expand, PostInvoicesInvoiceLinesLineItemIdRequestMetadata? metadata, Period? period, OneTimePriceDataWithProductData1? priceData, PricingParam? pricing, int? quantity, PostInvoicesInvoiceLinesLineItemIdRequestTaxAmounts? taxAmounts, PostInvoicesInvoiceLinesLineItemIdRequestTaxRates? taxRates, }) async {
    final response = await postInvoicesInvoiceLinesLineItemIdWithHttpInfo(invoice, lineItemId,  amount: amount, description: description, discountable: discountable, discounts: discounts, expand: expand, metadata: metadata, period: period, priceData: priceData, pricing: pricing, quantity: quantity, taxAmounts: taxAmounts, taxRates: taxRates, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'LineItem',) as LineItem;
    
    }
    return null;
  }

  /// Mark an invoice as uncollectible
  ///
  /// <p>Marking an invoice as uncollectible is useful for keeping track of bad debts that can be written off for accounting purposes.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postInvoicesInvoiceMarkUncollectibleWithHttpInfo(String invoice, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/invoices/{invoice}/mark_uncollectible'
      .replaceAll('{invoice}', invoice);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Mark an invoice as uncollectible
  ///
  /// <p>Marking an invoice as uncollectible is useful for keeping track of bad debts that can be written off for accounting purposes.</p>
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Invoice?> postInvoicesInvoiceMarkUncollectible(String invoice, { List<String>? expand, }) async {
    final response = await postInvoicesInvoiceMarkUncollectibleWithHttpInfo(invoice,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Invoice',) as Invoice;
    
    }
    return null;
  }

  /// Pay an invoice
  ///
  /// <p>Stripe automatically creates and then attempts to collect payment on invoices for customers on subscriptions according to your <a href=\"https://dashboard.stripe.com/account/billing/automatic\">subscriptions settings</a>. However, if you’d like to attempt payment on an invoice out of the normal collection schedule or for some other reason, you can do so.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [bool] forgive:
  ///   In cases where the source used to pay the invoice has insufficient funds, passing `forgive=true` controls whether a charge should be attempted for the full amount available on the source, up to the amount to fully pay the invoice. This effectively forgives the difference between the amount available on the source and the amount due.   Passing `forgive=false` will fail the charge if the source hasn't been pre-funded with the right amount. An example for this case is with ACH Credit Transfers and wires: if the amount wired is less than the amount due by a small amount, you might want to forgive the difference. Defaults to `false`.
  ///
  /// * [PostInvoicesInvoicePayRequestMandate] mandate:
  ///
  /// * [bool] offSession:
  ///   Indicates if a customer is on or off-session while an invoice payment is attempted. Defaults to `true` (off-session).
  ///
  /// * [bool] paidOutOfBand:
  ///   Boolean representing whether an invoice is paid outside of Stripe. This will result in no charge being made. Defaults to `false`.
  ///
  /// * [String] paymentMethod:
  ///   A PaymentMethod to be charged. The PaymentMethod must be the ID of a PaymentMethod belonging to the customer associated with the invoice being paid.
  ///
  /// * [String] source_:
  ///   A payment source to be charged. The source must be the ID of a source belonging to the customer associated with the invoice being paid.
  Future<Response> postInvoicesInvoicePayWithHttpInfo(String invoice, { List<String>? expand, bool? forgive, PostInvoicesInvoicePayRequestMandate? mandate, bool? offSession, bool? paidOutOfBand, String? paymentMethod, String? source_, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/invoices/{invoice}/pay'
      .replaceAll('{invoice}', invoice);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (forgive != null) {
      formParams[r'forgive'] = parameterToString(forgive);
    }
    if (mandate != null) {
      formParams[r'mandate'] = parameterToString(mandate);
    }
    if (offSession != null) {
      formParams[r'off_session'] = parameterToString(offSession);
    }
    if (paidOutOfBand != null) {
      formParams[r'paid_out_of_band'] = parameterToString(paidOutOfBand);
    }
    if (paymentMethod != null) {
      formParams[r'payment_method'] = parameterToString(paymentMethod);
    }
    if (source_ != null) {
      formParams[r'source'] = parameterToString(source_);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Pay an invoice
  ///
  /// <p>Stripe automatically creates and then attempts to collect payment on invoices for customers on subscriptions according to your <a href=\"https://dashboard.stripe.com/account/billing/automatic\">subscriptions settings</a>. However, if you’d like to attempt payment on an invoice out of the normal collection schedule or for some other reason, you can do so.</p>
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [bool] forgive:
  ///   In cases where the source used to pay the invoice has insufficient funds, passing `forgive=true` controls whether a charge should be attempted for the full amount available on the source, up to the amount to fully pay the invoice. This effectively forgives the difference between the amount available on the source and the amount due.   Passing `forgive=false` will fail the charge if the source hasn't been pre-funded with the right amount. An example for this case is with ACH Credit Transfers and wires: if the amount wired is less than the amount due by a small amount, you might want to forgive the difference. Defaults to `false`.
  ///
  /// * [PostInvoicesInvoicePayRequestMandate] mandate:
  ///
  /// * [bool] offSession:
  ///   Indicates if a customer is on or off-session while an invoice payment is attempted. Defaults to `true` (off-session).
  ///
  /// * [bool] paidOutOfBand:
  ///   Boolean representing whether an invoice is paid outside of Stripe. This will result in no charge being made. Defaults to `false`.
  ///
  /// * [String] paymentMethod:
  ///   A PaymentMethod to be charged. The PaymentMethod must be the ID of a PaymentMethod belonging to the customer associated with the invoice being paid.
  ///
  /// * [String] source_:
  ///   A payment source to be charged. The source must be the ID of a source belonging to the customer associated with the invoice being paid.
  Future<Invoice?> postInvoicesInvoicePay(String invoice, { List<String>? expand, bool? forgive, PostInvoicesInvoicePayRequestMandate? mandate, bool? offSession, bool? paidOutOfBand, String? paymentMethod, String? source_, }) async {
    final response = await postInvoicesInvoicePayWithHttpInfo(invoice,  expand: expand, forgive: forgive, mandate: mandate, offSession: offSession, paidOutOfBand: paidOutOfBand, paymentMethod: paymentMethod, source_: source_, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Invoice',) as Invoice;
    
    }
    return null;
  }

  /// Bulk remove invoice line items
  ///
  /// <p>Removes multiple line items from an invoice. This is only possible when an invoice is still a draft.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  ///
  /// * [List<LinesDataParam1>] lines (required):
  ///   The line items to remove.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] invoiceMetadata:
  Future<Response> postInvoicesInvoiceRemoveLinesWithHttpInfo(String invoice, List<LinesDataParam1> lines, { List<String>? expand, PostAccountsRequestMetadata? invoiceMetadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/invoices/{invoice}/remove_lines'
      .replaceAll('{invoice}', invoice);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (invoiceMetadata != null) {
      formParams[r'invoice_metadata'] = parameterToString(invoiceMetadata);
    }
    if (lines != null) {
      formParams[r'lines'] = parameterToString(lines);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Bulk remove invoice line items
  ///
  /// <p>Removes multiple line items from an invoice. This is only possible when an invoice is still a draft.</p>
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  ///
  /// * [List<LinesDataParam1>] lines (required):
  ///   The line items to remove.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] invoiceMetadata:
  Future<Invoice?> postInvoicesInvoiceRemoveLines(String invoice, List<LinesDataParam1> lines, { List<String>? expand, PostAccountsRequestMetadata? invoiceMetadata, }) async {
    final response = await postInvoicesInvoiceRemoveLinesWithHttpInfo(invoice, lines,  expand: expand, invoiceMetadata: invoiceMetadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Invoice',) as Invoice;
    
    }
    return null;
  }

  /// Send an invoice for manual payment
  ///
  /// <p>Stripe will automatically send invoices to customers according to your <a href=\"https://dashboard.stripe.com/account/billing/automatic\">subscriptions settings</a>. However, if you’d like to manually send an invoice to your customer out of the normal schedule, you can do so. When sending invoices that have already been paid, there will be no reference to the payment in the email.</p>  <p>Requests made in test-mode result in no emails being sent, despite sending an <code>invoice.sent</code> event.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postInvoicesInvoiceSendWithHttpInfo(String invoice, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/invoices/{invoice}/send'
      .replaceAll('{invoice}', invoice);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Send an invoice for manual payment
  ///
  /// <p>Stripe will automatically send invoices to customers according to your <a href=\"https://dashboard.stripe.com/account/billing/automatic\">subscriptions settings</a>. However, if you’d like to manually send an invoice to your customer out of the normal schedule, you can do so. When sending invoices that have already been paid, there will be no reference to the payment in the email.</p>  <p>Requests made in test-mode result in no emails being sent, despite sending an <code>invoice.sent</code> event.</p>
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Invoice?> postInvoicesInvoiceSend(String invoice, { List<String>? expand, }) async {
    final response = await postInvoicesInvoiceSendWithHttpInfo(invoice,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Invoice',) as Invoice;
    
    }
    return null;
  }

  /// Bulk update invoice line items
  ///
  /// <p>Updates multiple line items on an invoice. This is only possible when an invoice is still a draft.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  ///
  /// * [List<LinesDataParam2>] lines (required):
  ///   The line items to update.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostInvoicesInvoiceLinesLineItemIdRequestMetadata] invoiceMetadata:
  Future<Response> postInvoicesInvoiceUpdateLinesWithHttpInfo(String invoice, List<LinesDataParam2> lines, { List<String>? expand, PostInvoicesInvoiceLinesLineItemIdRequestMetadata? invoiceMetadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/invoices/{invoice}/update_lines'
      .replaceAll('{invoice}', invoice);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (invoiceMetadata != null) {
      formParams[r'invoice_metadata'] = parameterToString(invoiceMetadata);
    }
    if (lines != null) {
      formParams[r'lines'] = parameterToString(lines);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Bulk update invoice line items
  ///
  /// <p>Updates multiple line items on an invoice. This is only possible when an invoice is still a draft.</p>
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  ///
  /// * [List<LinesDataParam2>] lines (required):
  ///   The line items to update.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostInvoicesInvoiceLinesLineItemIdRequestMetadata] invoiceMetadata:
  Future<Invoice?> postInvoicesInvoiceUpdateLines(String invoice, List<LinesDataParam2> lines, { List<String>? expand, PostInvoicesInvoiceLinesLineItemIdRequestMetadata? invoiceMetadata, }) async {
    final response = await postInvoicesInvoiceUpdateLinesWithHttpInfo(invoice, lines,  expand: expand, invoiceMetadata: invoiceMetadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Invoice',) as Invoice;
    
    }
    return null;
  }

  /// Void an invoice
  ///
  /// <p>Mark a finalized invoice as void. This cannot be undone. Voiding an invoice is similar to <a href=\"#delete_invoice\">deletion</a>, however it only applies to finalized invoices and maintains a papertrail where the invoice can still be found.</p>  <p>Consult with local regulations to determine whether and how an invoice might be amended, canceled, or voided in the jurisdiction you’re doing business in. You might need to <a href=\"#create_invoice\">issue another invoice</a> or <a href=\"#create_credit_note\">credit note</a> instead. Stripe recommends that you consult with your legal counsel for advice specific to your business.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postInvoicesInvoiceVoidWithHttpInfo(String invoice, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/invoices/{invoice}/void'
      .replaceAll('{invoice}', invoice);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Void an invoice
  ///
  /// <p>Mark a finalized invoice as void. This cannot be undone. Voiding an invoice is similar to <a href=\"#delete_invoice\">deletion</a>, however it only applies to finalized invoices and maintains a papertrail where the invoice can still be found.</p>  <p>Consult with local regulations to determine whether and how an invoice might be amended, canceled, or voided in the jurisdiction you’re doing business in. You might need to <a href=\"#create_invoice\">issue another invoice</a> or <a href=\"#create_credit_note\">credit note</a> instead. Stripe recommends that you consult with your legal counsel for advice specific to your business.</p>
  ///
  /// Parameters:
  ///
  /// * [String] invoice (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Invoice?> postInvoicesInvoiceVoid(String invoice, { List<String>? expand, }) async {
    final response = await postInvoicesInvoiceVoidWithHttpInfo(invoice,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Invoice',) as Invoice;
    
    }
    return null;
  }

  /// Update an authorization
  ///
  /// <p>Updates the specified Issuing <code>Authorization</code> object by setting the values of the parameters passed. Any parameters not provided will be left unchanged.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] authorization (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<Response> postIssuingAuthorizationsAuthorizationWithHttpInfo(String authorization, { List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/issuing/authorizations/{authorization}'
      .replaceAll('{authorization}', authorization);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update an authorization
  ///
  /// <p>Updates the specified Issuing <code>Authorization</code> object by setting the values of the parameters passed. Any parameters not provided will be left unchanged.</p>
  ///
  /// Parameters:
  ///
  /// * [String] authorization (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<IssuingAuthorization?> postIssuingAuthorizationsAuthorization(String authorization, { List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    final response = await postIssuingAuthorizationsAuthorizationWithHttpInfo(authorization,  expand: expand, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingAuthorization',) as IssuingAuthorization;
    
    }
    return null;
  }

  /// Approve an authorization
  ///
  /// <p>[Deprecated] Approves a pending Issuing <code>Authorization</code> object. This request should be made within the timeout window of the <a href=\"/docs/issuing/controls/real-time-authorizations\">real-time authorization</a> flow.  This method is deprecated. Instead, <a href=\"/docs/issuing/controls/real-time-authorizations#authorization-handling\">respond directly to the webhook request to approve an authorization</a>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] authorization (required):
  ///
  /// * [int] amount:
  ///   If the authorization's `pending_request.is_amount_controllable` property is `true`, you may provide this value to control how much to hold for the authorization. Must be positive (use [`decline`](https://stripe.com/docs/api/issuing/authorizations/decline) to decline an authorization request).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<Response> postIssuingAuthorizationsAuthorizationApproveWithHttpInfo(String authorization, { int? amount, List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/issuing/authorizations/{authorization}/approve'
      .replaceAll('{authorization}', authorization);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Approve an authorization
  ///
  /// <p>[Deprecated] Approves a pending Issuing <code>Authorization</code> object. This request should be made within the timeout window of the <a href=\"/docs/issuing/controls/real-time-authorizations\">real-time authorization</a> flow.  This method is deprecated. Instead, <a href=\"/docs/issuing/controls/real-time-authorizations#authorization-handling\">respond directly to the webhook request to approve an authorization</a>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] authorization (required):
  ///
  /// * [int] amount:
  ///   If the authorization's `pending_request.is_amount_controllable` property is `true`, you may provide this value to control how much to hold for the authorization. Must be positive (use [`decline`](https://stripe.com/docs/api/issuing/authorizations/decline) to decline an authorization request).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<IssuingAuthorization?> postIssuingAuthorizationsAuthorizationApprove(String authorization, { int? amount, List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    final response = await postIssuingAuthorizationsAuthorizationApproveWithHttpInfo(authorization,  amount: amount, expand: expand, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingAuthorization',) as IssuingAuthorization;
    
    }
    return null;
  }

  /// Decline an authorization
  ///
  /// <p>[Deprecated] Declines a pending Issuing <code>Authorization</code> object. This request should be made within the timeout window of the <a href=\"/docs/issuing/controls/real-time-authorizations\">real time authorization</a> flow. This method is deprecated. Instead, <a href=\"/docs/issuing/controls/real-time-authorizations#authorization-handling\">respond directly to the webhook request to decline an authorization</a>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] authorization (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<Response> postIssuingAuthorizationsAuthorizationDeclineWithHttpInfo(String authorization, { List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/issuing/authorizations/{authorization}/decline'
      .replaceAll('{authorization}', authorization);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Decline an authorization
  ///
  /// <p>[Deprecated] Declines a pending Issuing <code>Authorization</code> object. This request should be made within the timeout window of the <a href=\"/docs/issuing/controls/real-time-authorizations\">real time authorization</a> flow. This method is deprecated. Instead, <a href=\"/docs/issuing/controls/real-time-authorizations#authorization-handling\">respond directly to the webhook request to decline an authorization</a>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] authorization (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<IssuingAuthorization?> postIssuingAuthorizationsAuthorizationDecline(String authorization, { List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    final response = await postIssuingAuthorizationsAuthorizationDeclineWithHttpInfo(authorization,  expand: expand, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingAuthorization',) as IssuingAuthorization;
    
    }
    return null;
  }

  /// Create a cardholder
  ///
  /// <p>Creates a new Issuing <code>Cardholder</code> object that can be issued cards.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [BillingSpecs] billing (required):
  ///
  /// * [String] name (required):
  ///   The cardholder's name. This will be printed on cards issued to them. The maximum length of this field is 24 characters. This field cannot contain any special characters or numbers.
  ///
  /// * [CompanyParam] company:
  ///
  /// * [String] email:
  ///   The cardholder's email address.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [IndividualParam] individual:
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] phoneNumber:
  ///   The cardholder's phone number. This will be transformed to [E.164](https://en.wikipedia.org/wiki/E.164) if it is not provided in that format already. This is required for all cardholders who will be creating EU cards. See the [3D Secure documentation](https://stripe.com/docs/issuing/3d-secure#when-is-3d-secure-applied) for more details.
  ///
  /// * [List<String>] preferredLocales:
  ///   The cardholder’s preferred locales (languages), ordered by preference. Locales can be `de`, `en`, `es`, `fr`, or `it`.  This changes the language of the [3D Secure flow](https://stripe.com/docs/issuing/3d-secure) and one-time password messages sent to the cardholder.
  ///
  /// * [AuthorizationControlsParamV2] spendingControls:
  ///
  /// * [String] status:
  ///   Specifies whether to permit authorizations on this cardholder's cards. Defaults to `active`.
  ///
  /// * [String] type:
  ///   One of `individual` or `company`. See [Choose a cardholder type](https://stripe.com/docs/issuing/other/choose-cardholder) for more details.
  Future<Response> postIssuingCardholdersWithHttpInfo(BillingSpecs billing, String name, { CompanyParam? company, String? email, List<String>? expand, IndividualParam? individual, Map<String, String>? metadata, String? phoneNumber, List<String>? preferredLocales, AuthorizationControlsParamV2? spendingControls, String? status, String? type, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/issuing/cardholders';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (billing != null) {
      formParams[r'billing'] = parameterToString(billing);
    }
    if (company != null) {
      formParams[r'company'] = parameterToString(company);
    }
    if (email != null) {
      formParams[r'email'] = parameterToString(email);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (individual != null) {
      formParams[r'individual'] = parameterToString(individual);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (name != null) {
      formParams[r'name'] = parameterToString(name);
    }
    if (phoneNumber != null) {
      formParams[r'phone_number'] = parameterToString(phoneNumber);
    }
    if (preferredLocales != null) {
      formParams[r'preferred_locales'] = parameterToString(preferredLocales);
    }
    if (spendingControls != null) {
      formParams[r'spending_controls'] = parameterToString(spendingControls);
    }
    if (status != null) {
      formParams[r'status'] = parameterToString(status);
    }
    if (type != null) {
      formParams[r'type'] = parameterToString(type);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a cardholder
  ///
  /// <p>Creates a new Issuing <code>Cardholder</code> object that can be issued cards.</p>
  ///
  /// Parameters:
  ///
  /// * [BillingSpecs] billing (required):
  ///
  /// * [String] name (required):
  ///   The cardholder's name. This will be printed on cards issued to them. The maximum length of this field is 24 characters. This field cannot contain any special characters or numbers.
  ///
  /// * [CompanyParam] company:
  ///
  /// * [String] email:
  ///   The cardholder's email address.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [IndividualParam] individual:
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] phoneNumber:
  ///   The cardholder's phone number. This will be transformed to [E.164](https://en.wikipedia.org/wiki/E.164) if it is not provided in that format already. This is required for all cardholders who will be creating EU cards. See the [3D Secure documentation](https://stripe.com/docs/issuing/3d-secure#when-is-3d-secure-applied) for more details.
  ///
  /// * [List<String>] preferredLocales:
  ///   The cardholder’s preferred locales (languages), ordered by preference. Locales can be `de`, `en`, `es`, `fr`, or `it`.  This changes the language of the [3D Secure flow](https://stripe.com/docs/issuing/3d-secure) and one-time password messages sent to the cardholder.
  ///
  /// * [AuthorizationControlsParamV2] spendingControls:
  ///
  /// * [String] status:
  ///   Specifies whether to permit authorizations on this cardholder's cards. Defaults to `active`.
  ///
  /// * [String] type:
  ///   One of `individual` or `company`. See [Choose a cardholder type](https://stripe.com/docs/issuing/other/choose-cardholder) for more details.
  Future<IssuingCardholder?> postIssuingCardholders(BillingSpecs billing, String name, { CompanyParam? company, String? email, List<String>? expand, IndividualParam? individual, Map<String, String>? metadata, String? phoneNumber, List<String>? preferredLocales, AuthorizationControlsParamV2? spendingControls, String? status, String? type, }) async {
    final response = await postIssuingCardholdersWithHttpInfo(billing, name,  company: company, email: email, expand: expand, individual: individual, metadata: metadata, phoneNumber: phoneNumber, preferredLocales: preferredLocales, spendingControls: spendingControls, status: status, type: type, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingCardholder',) as IssuingCardholder;
    
    }
    return null;
  }

  /// Update a cardholder
  ///
  /// <p>Updates the specified Issuing <code>Cardholder</code> object by setting the values of the parameters passed. Any parameters not provided will be left unchanged.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] cardholder (required):
  ///
  /// * [BillingSpecs] billing:
  ///
  /// * [CompanyParam] company:
  ///
  /// * [String] email:
  ///   The cardholder's email address.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [IndividualParam] individual:
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] phoneNumber:
  ///   The cardholder's phone number. This is required for all cardholders who will be creating EU cards. See the [3D Secure documentation](https://stripe.com/docs/issuing/3d-secure) for more details.
  ///
  /// * [List<String>] preferredLocales:
  ///   The cardholder’s preferred locales (languages), ordered by preference. Locales can be `de`, `en`, `es`, `fr`, or `it`.  This changes the language of the [3D Secure flow](https://stripe.com/docs/issuing/3d-secure) and one-time password messages sent to the cardholder.
  ///
  /// * [AuthorizationControlsParamV2] spendingControls:
  ///
  /// * [String] status:
  ///   Specifies whether to permit authorizations on this cardholder's cards.
  Future<Response> postIssuingCardholdersCardholderWithHttpInfo(String cardholder, { BillingSpecs? billing, CompanyParam? company, String? email, List<String>? expand, IndividualParam? individual, Map<String, String>? metadata, String? phoneNumber, List<String>? preferredLocales, AuthorizationControlsParamV2? spendingControls, String? status, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/issuing/cardholders/{cardholder}'
      .replaceAll('{cardholder}', cardholder);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (billing != null) {
      formParams[r'billing'] = parameterToString(billing);
    }
    if (company != null) {
      formParams[r'company'] = parameterToString(company);
    }
    if (email != null) {
      formParams[r'email'] = parameterToString(email);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (individual != null) {
      formParams[r'individual'] = parameterToString(individual);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (phoneNumber != null) {
      formParams[r'phone_number'] = parameterToString(phoneNumber);
    }
    if (preferredLocales != null) {
      formParams[r'preferred_locales'] = parameterToString(preferredLocales);
    }
    if (spendingControls != null) {
      formParams[r'spending_controls'] = parameterToString(spendingControls);
    }
    if (status != null) {
      formParams[r'status'] = parameterToString(status);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a cardholder
  ///
  /// <p>Updates the specified Issuing <code>Cardholder</code> object by setting the values of the parameters passed. Any parameters not provided will be left unchanged.</p>
  ///
  /// Parameters:
  ///
  /// * [String] cardholder (required):
  ///
  /// * [BillingSpecs] billing:
  ///
  /// * [CompanyParam] company:
  ///
  /// * [String] email:
  ///   The cardholder's email address.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [IndividualParam] individual:
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] phoneNumber:
  ///   The cardholder's phone number. This is required for all cardholders who will be creating EU cards. See the [3D Secure documentation](https://stripe.com/docs/issuing/3d-secure) for more details.
  ///
  /// * [List<String>] preferredLocales:
  ///   The cardholder’s preferred locales (languages), ordered by preference. Locales can be `de`, `en`, `es`, `fr`, or `it`.  This changes the language of the [3D Secure flow](https://stripe.com/docs/issuing/3d-secure) and one-time password messages sent to the cardholder.
  ///
  /// * [AuthorizationControlsParamV2] spendingControls:
  ///
  /// * [String] status:
  ///   Specifies whether to permit authorizations on this cardholder's cards.
  Future<IssuingCardholder?> postIssuingCardholdersCardholder(String cardholder, { BillingSpecs? billing, CompanyParam? company, String? email, List<String>? expand, IndividualParam? individual, Map<String, String>? metadata, String? phoneNumber, List<String>? preferredLocales, AuthorizationControlsParamV2? spendingControls, String? status, }) async {
    final response = await postIssuingCardholdersCardholderWithHttpInfo(cardholder,  billing: billing, company: company, email: email, expand: expand, individual: individual, metadata: metadata, phoneNumber: phoneNumber, preferredLocales: preferredLocales, spendingControls: spendingControls, status: status, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingCardholder',) as IssuingCardholder;
    
    }
    return null;
  }

  /// Create a card
  ///
  /// <p>Creates an Issuing <code>Card</code> object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] currency (required):
  ///   The currency for the card.
  ///
  /// * [String] type (required):
  ///   The type of card to issue. Possible values are `physical` or `virtual`.
  ///
  /// * [String] cardholder:
  ///   The [Cardholder](https://stripe.com/docs/api#issuing_cardholder_object) object with which the card will be associated.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] financialAccount:
  ///   The new financial account ID the card will be associated with. This field allows a card to be reassigned to a different financial account.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] personalizationDesign:
  ///   The personalization design object belonging to this card.
  ///
  /// * [EncryptedPinParam] pin:
  ///
  /// * [String] replacementFor:
  ///   The card this is meant to be a replacement for (if any).
  ///
  /// * [String] replacementReason:
  ///   If `replacement_for` is specified, this should indicate why that card is being replaced.
  ///
  /// * [PostIssuingCardsRequestSecondLine] secondLine:
  ///
  /// * [ShippingSpecs] shipping:
  ///
  /// * [AuthorizationControlsParam] spendingControls:
  ///
  /// * [String] status:
  ///   Whether authorizations can be approved on this card. May be blocked from activating cards depending on past-due Cardholder requirements. Defaults to `inactive`.
  Future<Response> postIssuingCardsWithHttpInfo(String currency, String type, { String? cardholder, List<String>? expand, String? financialAccount, Map<String, String>? metadata, String? personalizationDesign, EncryptedPinParam? pin, String? replacementFor, String? replacementReason, PostIssuingCardsRequestSecondLine? secondLine, ShippingSpecs? shipping, AuthorizationControlsParam? spendingControls, String? status, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/issuing/cards';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (cardholder != null) {
      formParams[r'cardholder'] = parameterToString(cardholder);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (financialAccount != null) {
      formParams[r'financial_account'] = parameterToString(financialAccount);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (personalizationDesign != null) {
      formParams[r'personalization_design'] = parameterToString(personalizationDesign);
    }
    if (pin != null) {
      formParams[r'pin'] = parameterToString(pin);
    }
    if (replacementFor != null) {
      formParams[r'replacement_for'] = parameterToString(replacementFor);
    }
    if (replacementReason != null) {
      formParams[r'replacement_reason'] = parameterToString(replacementReason);
    }
    if (secondLine != null) {
      formParams[r'second_line'] = parameterToString(secondLine);
    }
    if (shipping != null) {
      formParams[r'shipping'] = parameterToString(shipping);
    }
    if (spendingControls != null) {
      formParams[r'spending_controls'] = parameterToString(spendingControls);
    }
    if (status != null) {
      formParams[r'status'] = parameterToString(status);
    }
    if (type != null) {
      formParams[r'type'] = parameterToString(type);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a card
  ///
  /// <p>Creates an Issuing <code>Card</code> object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] currency (required):
  ///   The currency for the card.
  ///
  /// * [String] type (required):
  ///   The type of card to issue. Possible values are `physical` or `virtual`.
  ///
  /// * [String] cardholder:
  ///   The [Cardholder](https://stripe.com/docs/api#issuing_cardholder_object) object with which the card will be associated.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] financialAccount:
  ///   The new financial account ID the card will be associated with. This field allows a card to be reassigned to a different financial account.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] personalizationDesign:
  ///   The personalization design object belonging to this card.
  ///
  /// * [EncryptedPinParam] pin:
  ///
  /// * [String] replacementFor:
  ///   The card this is meant to be a replacement for (if any).
  ///
  /// * [String] replacementReason:
  ///   If `replacement_for` is specified, this should indicate why that card is being replaced.
  ///
  /// * [PostIssuingCardsRequestSecondLine] secondLine:
  ///
  /// * [ShippingSpecs] shipping:
  ///
  /// * [AuthorizationControlsParam] spendingControls:
  ///
  /// * [String] status:
  ///   Whether authorizations can be approved on this card. May be blocked from activating cards depending on past-due Cardholder requirements. Defaults to `inactive`.
  Future<IssuingCard?> postIssuingCards(String currency, String type, { String? cardholder, List<String>? expand, String? financialAccount, Map<String, String>? metadata, String? personalizationDesign, EncryptedPinParam? pin, String? replacementFor, String? replacementReason, PostIssuingCardsRequestSecondLine? secondLine, ShippingSpecs? shipping, AuthorizationControlsParam? spendingControls, String? status, }) async {
    final response = await postIssuingCardsWithHttpInfo(currency, type,  cardholder: cardholder, expand: expand, financialAccount: financialAccount, metadata: metadata, personalizationDesign: personalizationDesign, pin: pin, replacementFor: replacementFor, replacementReason: replacementReason, secondLine: secondLine, shipping: shipping, spendingControls: spendingControls, status: status, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingCard',) as IssuingCard;
    
    }
    return null;
  }

  /// Update a card
  ///
  /// <p>Updates the specified Issuing <code>Card</code> object by setting the values of the parameters passed. Any parameters not provided will be left unchanged.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] card (required):
  ///
  /// * [String] cancellationReason:
  ///   Reason why the `status` of this card is `canceled`.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] personalizationDesign:
  ///
  /// * [EncryptedPinParam1] pin:
  ///
  /// * [ShippingSpecs1] shipping:
  ///
  /// * [AuthorizationControlsParam] spendingControls:
  ///
  /// * [String] status:
  ///   Dictates whether authorizations can be approved on this card. May be blocked from activating cards depending on past-due Cardholder requirements. Defaults to `inactive`. If this card is being canceled because it was lost or stolen, this information should be provided as `cancellation_reason`.
  Future<Response> postIssuingCardsCardWithHttpInfo(String card, { String? cancellationReason, List<String>? expand, PostAccountsRequestMetadata? metadata, String? personalizationDesign, EncryptedPinParam1? pin, ShippingSpecs1? shipping, AuthorizationControlsParam? spendingControls, String? status, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/issuing/cards/{card}'
      .replaceAll('{card}', card);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (cancellationReason != null) {
      formParams[r'cancellation_reason'] = parameterToString(cancellationReason);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (personalizationDesign != null) {
      formParams[r'personalization_design'] = parameterToString(personalizationDesign);
    }
    if (pin != null) {
      formParams[r'pin'] = parameterToString(pin);
    }
    if (shipping != null) {
      formParams[r'shipping'] = parameterToString(shipping);
    }
    if (spendingControls != null) {
      formParams[r'spending_controls'] = parameterToString(spendingControls);
    }
    if (status != null) {
      formParams[r'status'] = parameterToString(status);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a card
  ///
  /// <p>Updates the specified Issuing <code>Card</code> object by setting the values of the parameters passed. Any parameters not provided will be left unchanged.</p>
  ///
  /// Parameters:
  ///
  /// * [String] card (required):
  ///
  /// * [String] cancellationReason:
  ///   Reason why the `status` of this card is `canceled`.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] personalizationDesign:
  ///
  /// * [EncryptedPinParam1] pin:
  ///
  /// * [ShippingSpecs1] shipping:
  ///
  /// * [AuthorizationControlsParam] spendingControls:
  ///
  /// * [String] status:
  ///   Dictates whether authorizations can be approved on this card. May be blocked from activating cards depending on past-due Cardholder requirements. Defaults to `inactive`. If this card is being canceled because it was lost or stolen, this information should be provided as `cancellation_reason`.
  Future<IssuingCard?> postIssuingCardsCard(String card, { String? cancellationReason, List<String>? expand, PostAccountsRequestMetadata? metadata, String? personalizationDesign, EncryptedPinParam1? pin, ShippingSpecs1? shipping, AuthorizationControlsParam? spendingControls, String? status, }) async {
    final response = await postIssuingCardsCardWithHttpInfo(card,  cancellationReason: cancellationReason, expand: expand, metadata: metadata, personalizationDesign: personalizationDesign, pin: pin, shipping: shipping, spendingControls: spendingControls, status: status, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingCard',) as IssuingCard;
    
    }
    return null;
  }

  /// Create a dispute
  ///
  /// <p>Creates an Issuing <code>Dispute</code> object. Individual pieces of evidence within the <code>evidence</code> object are optional at this point. Stripe only validates that required evidence is present during submission. Refer to <a href=\"/docs/issuing/purchases/disputes#dispute-reasons-and-evidence\">Dispute reasons and evidence</a> for more details about evidence requirements.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [int] amount:
  ///   The dispute amount in the card's currency and in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal). If not set, defaults to the full transaction amount.
  ///
  /// * [EvidenceParam] evidence:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] transaction:
  ///   The ID of the issuing transaction to create a dispute for. For transaction on Treasury FinancialAccounts, use `treasury.received_debit`.
  ///
  /// * [TreasuryParam] treasury:
  Future<Response> postIssuingDisputesWithHttpInfo({ int? amount, EvidenceParam? evidence, List<String>? expand, Map<String, String>? metadata, String? transaction, TreasuryParam? treasury, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/issuing/disputes';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (evidence != null) {
      formParams[r'evidence'] = parameterToString(evidence);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (transaction != null) {
      formParams[r'transaction'] = parameterToString(transaction);
    }
    if (treasury != null) {
      formParams[r'treasury'] = parameterToString(treasury);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a dispute
  ///
  /// <p>Creates an Issuing <code>Dispute</code> object. Individual pieces of evidence within the <code>evidence</code> object are optional at this point. Stripe only validates that required evidence is present during submission. Refer to <a href=\"/docs/issuing/purchases/disputes#dispute-reasons-and-evidence\">Dispute reasons and evidence</a> for more details about evidence requirements.</p>
  ///
  /// Parameters:
  ///
  /// * [int] amount:
  ///   The dispute amount in the card's currency and in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal). If not set, defaults to the full transaction amount.
  ///
  /// * [EvidenceParam] evidence:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] transaction:
  ///   The ID of the issuing transaction to create a dispute for. For transaction on Treasury FinancialAccounts, use `treasury.received_debit`.
  ///
  /// * [TreasuryParam] treasury:
  Future<IssuingDispute?> postIssuingDisputes({ int? amount, EvidenceParam? evidence, List<String>? expand, Map<String, String>? metadata, String? transaction, TreasuryParam? treasury, }) async {
    final response = await postIssuingDisputesWithHttpInfo( amount: amount, evidence: evidence, expand: expand, metadata: metadata, transaction: transaction, treasury: treasury, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingDispute',) as IssuingDispute;
    
    }
    return null;
  }

  /// Update a dispute
  ///
  /// <p>Updates the specified Issuing <code>Dispute</code> object by setting the values of the parameters passed. Any parameters not provided will be left unchanged. Properties on the <code>evidence</code> object can be unset by passing in an empty string.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] dispute (required):
  ///
  /// * [int] amount:
  ///   The dispute amount in the card's currency and in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal).
  ///
  /// * [EvidenceParam] evidence:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<Response> postIssuingDisputesDisputeWithHttpInfo(String dispute, { int? amount, EvidenceParam? evidence, List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/issuing/disputes/{dispute}'
      .replaceAll('{dispute}', dispute);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (evidence != null) {
      formParams[r'evidence'] = parameterToString(evidence);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a dispute
  ///
  /// <p>Updates the specified Issuing <code>Dispute</code> object by setting the values of the parameters passed. Any parameters not provided will be left unchanged. Properties on the <code>evidence</code> object can be unset by passing in an empty string.</p>
  ///
  /// Parameters:
  ///
  /// * [String] dispute (required):
  ///
  /// * [int] amount:
  ///   The dispute amount in the card's currency and in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal).
  ///
  /// * [EvidenceParam] evidence:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<IssuingDispute?> postIssuingDisputesDispute(String dispute, { int? amount, EvidenceParam? evidence, List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    final response = await postIssuingDisputesDisputeWithHttpInfo(dispute,  amount: amount, evidence: evidence, expand: expand, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingDispute',) as IssuingDispute;
    
    }
    return null;
  }

  /// Submit a dispute
  ///
  /// <p>Submits an Issuing <code>Dispute</code> to the card network. Stripe validates that all evidence fields required for the dispute’s reason are present. For more details, see <a href=\"/docs/issuing/purchases/disputes#dispute-reasons-and-evidence\">Dispute reasons and evidence</a>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] dispute (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<Response> postIssuingDisputesDisputeSubmitWithHttpInfo(String dispute, { List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/issuing/disputes/{dispute}/submit'
      .replaceAll('{dispute}', dispute);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Submit a dispute
  ///
  /// <p>Submits an Issuing <code>Dispute</code> to the card network. Stripe validates that all evidence fields required for the dispute’s reason are present. For more details, see <a href=\"/docs/issuing/purchases/disputes#dispute-reasons-and-evidence\">Dispute reasons and evidence</a>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] dispute (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<IssuingDispute?> postIssuingDisputesDisputeSubmit(String dispute, { List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    final response = await postIssuingDisputesDisputeSubmitWithHttpInfo(dispute,  expand: expand, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingDispute',) as IssuingDispute;
    
    }
    return null;
  }

  /// Create a personalization design
  ///
  /// <p>Creates a personalization design object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] physicalBundle (required):
  ///   The physical bundle object belonging to this personalization design.
  ///
  /// * [String] cardLogo:
  ///   The file for the card logo, for use with physical bundles that support card logos. Must have a `purpose` value of `issuing_logo`.
  ///
  /// * [CarrierTextParam] carrierText:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] lookupKey:
  ///   A lookup key used to retrieve personalization designs dynamically from a static string. This may be up to 200 characters.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] name:
  ///   Friendly display name.
  ///
  /// * [PreferencesParam] preferences:
  ///
  /// * [bool] transferLookupKey:
  ///   If set to true, will atomically remove the lookup key from the existing personalization design, and assign it to this personalization design.
  Future<Response> postIssuingPersonalizationDesignsWithHttpInfo(String physicalBundle, { String? cardLogo, CarrierTextParam? carrierText, List<String>? expand, String? lookupKey, Map<String, String>? metadata, String? name, PreferencesParam? preferences, bool? transferLookupKey, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/issuing/personalization_designs';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (cardLogo != null) {
      formParams[r'card_logo'] = parameterToString(cardLogo);
    }
    if (carrierText != null) {
      formParams[r'carrier_text'] = parameterToString(carrierText);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (lookupKey != null) {
      formParams[r'lookup_key'] = parameterToString(lookupKey);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (name != null) {
      formParams[r'name'] = parameterToString(name);
    }
    if (physicalBundle != null) {
      formParams[r'physical_bundle'] = parameterToString(physicalBundle);
    }
    if (preferences != null) {
      formParams[r'preferences'] = parameterToString(preferences);
    }
    if (transferLookupKey != null) {
      formParams[r'transfer_lookup_key'] = parameterToString(transferLookupKey);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a personalization design
  ///
  /// <p>Creates a personalization design object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] physicalBundle (required):
  ///   The physical bundle object belonging to this personalization design.
  ///
  /// * [String] cardLogo:
  ///   The file for the card logo, for use with physical bundles that support card logos. Must have a `purpose` value of `issuing_logo`.
  ///
  /// * [CarrierTextParam] carrierText:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] lookupKey:
  ///   A lookup key used to retrieve personalization designs dynamically from a static string. This may be up to 200 characters.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] name:
  ///   Friendly display name.
  ///
  /// * [PreferencesParam] preferences:
  ///
  /// * [bool] transferLookupKey:
  ///   If set to true, will atomically remove the lookup key from the existing personalization design, and assign it to this personalization design.
  Future<IssuingPersonalizationDesign?> postIssuingPersonalizationDesigns(String physicalBundle, { String? cardLogo, CarrierTextParam? carrierText, List<String>? expand, String? lookupKey, Map<String, String>? metadata, String? name, PreferencesParam? preferences, bool? transferLookupKey, }) async {
    final response = await postIssuingPersonalizationDesignsWithHttpInfo(physicalBundle,  cardLogo: cardLogo, carrierText: carrierText, expand: expand, lookupKey: lookupKey, metadata: metadata, name: name, preferences: preferences, transferLookupKey: transferLookupKey, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingPersonalizationDesign',) as IssuingPersonalizationDesign;
    
    }
    return null;
  }

  /// Update a personalization design
  ///
  /// <p>Updates a card personalization object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] personalizationDesign (required):
  ///
  /// * [PostIssuingPersonalizationDesignsPersonalizationDesignRequestCardLogo] cardLogo:
  ///
  /// * [PostIssuingPersonalizationDesignsPersonalizationDesignRequestCarrierText] carrierText:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostIssuingPersonalizationDesignsPersonalizationDesignRequestLookupKey] lookupKey:
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [PostIssuingPersonalizationDesignsPersonalizationDesignRequestName] name:
  ///
  /// * [String] physicalBundle:
  ///   The physical bundle object belonging to this personalization design.
  ///
  /// * [PreferencesParam] preferences:
  ///
  /// * [bool] transferLookupKey:
  ///   If set to true, will atomically remove the lookup key from the existing personalization design, and assign it to this personalization design.
  Future<Response> postIssuingPersonalizationDesignsPersonalizationDesignWithHttpInfo(String personalizationDesign, { PostIssuingPersonalizationDesignsPersonalizationDesignRequestCardLogo? cardLogo, PostIssuingPersonalizationDesignsPersonalizationDesignRequestCarrierText? carrierText, List<String>? expand, PostIssuingPersonalizationDesignsPersonalizationDesignRequestLookupKey? lookupKey, Map<String, String>? metadata, PostIssuingPersonalizationDesignsPersonalizationDesignRequestName? name, String? physicalBundle, PreferencesParam? preferences, bool? transferLookupKey, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/issuing/personalization_designs/{personalization_design}'
      .replaceAll('{personalization_design}', personalizationDesign);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (cardLogo != null) {
      formParams[r'card_logo'] = parameterToString(cardLogo);
    }
    if (carrierText != null) {
      formParams[r'carrier_text'] = parameterToString(carrierText);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (lookupKey != null) {
      formParams[r'lookup_key'] = parameterToString(lookupKey);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (name != null) {
      formParams[r'name'] = parameterToString(name);
    }
    if (physicalBundle != null) {
      formParams[r'physical_bundle'] = parameterToString(physicalBundle);
    }
    if (preferences != null) {
      formParams[r'preferences'] = parameterToString(preferences);
    }
    if (transferLookupKey != null) {
      formParams[r'transfer_lookup_key'] = parameterToString(transferLookupKey);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a personalization design
  ///
  /// <p>Updates a card personalization object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] personalizationDesign (required):
  ///
  /// * [PostIssuingPersonalizationDesignsPersonalizationDesignRequestCardLogo] cardLogo:
  ///
  /// * [PostIssuingPersonalizationDesignsPersonalizationDesignRequestCarrierText] carrierText:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostIssuingPersonalizationDesignsPersonalizationDesignRequestLookupKey] lookupKey:
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [PostIssuingPersonalizationDesignsPersonalizationDesignRequestName] name:
  ///
  /// * [String] physicalBundle:
  ///   The physical bundle object belonging to this personalization design.
  ///
  /// * [PreferencesParam] preferences:
  ///
  /// * [bool] transferLookupKey:
  ///   If set to true, will atomically remove the lookup key from the existing personalization design, and assign it to this personalization design.
  Future<IssuingPersonalizationDesign?> postIssuingPersonalizationDesignsPersonalizationDesign(String personalizationDesign, { PostIssuingPersonalizationDesignsPersonalizationDesignRequestCardLogo? cardLogo, PostIssuingPersonalizationDesignsPersonalizationDesignRequestCarrierText? carrierText, List<String>? expand, PostIssuingPersonalizationDesignsPersonalizationDesignRequestLookupKey? lookupKey, Map<String, String>? metadata, PostIssuingPersonalizationDesignsPersonalizationDesignRequestName? name, String? physicalBundle, PreferencesParam? preferences, bool? transferLookupKey, }) async {
    final response = await postIssuingPersonalizationDesignsPersonalizationDesignWithHttpInfo(personalizationDesign,  cardLogo: cardLogo, carrierText: carrierText, expand: expand, lookupKey: lookupKey, metadata: metadata, name: name, physicalBundle: physicalBundle, preferences: preferences, transferLookupKey: transferLookupKey, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingPersonalizationDesign',) as IssuingPersonalizationDesign;
    
    }
    return null;
  }

  /// Update a settlement
  ///
  /// <p>Updates the specified Issuing <code>Settlement</code> object by setting the values of the parameters passed. Any parameters not provided will be left unchanged.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] settlement (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  Future<Response> postIssuingSettlementsSettlementWithHttpInfo(String settlement, { List<String>? expand, Map<String, String>? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/issuing/settlements/{settlement}'
      .replaceAll('{settlement}', settlement);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a settlement
  ///
  /// <p>Updates the specified Issuing <code>Settlement</code> object by setting the values of the parameters passed. Any parameters not provided will be left unchanged.</p>
  ///
  /// Parameters:
  ///
  /// * [String] settlement (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  Future<IssuingSettlement?> postIssuingSettlementsSettlement(String settlement, { List<String>? expand, Map<String, String>? metadata, }) async {
    final response = await postIssuingSettlementsSettlementWithHttpInfo(settlement,  expand: expand, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingSettlement',) as IssuingSettlement;
    
    }
    return null;
  }

  /// Update a token status
  ///
  /// <p>Attempts to update the specified Issuing <code>Token</code> object to the status specified.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] token (required):
  ///
  /// * [String] status (required):
  ///   Specifies which status the token should be updated to.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postIssuingTokensTokenWithHttpInfo(String token, String status, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/issuing/tokens/{token}'
      .replaceAll('{token}', token);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (status != null) {
      formParams[r'status'] = parameterToString(status);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a token status
  ///
  /// <p>Attempts to update the specified Issuing <code>Token</code> object to the status specified.</p>
  ///
  /// Parameters:
  ///
  /// * [String] token (required):
  ///
  /// * [String] status (required):
  ///   Specifies which status the token should be updated to.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<IssuingToken?> postIssuingTokensToken(String token, String status, { List<String>? expand, }) async {
    final response = await postIssuingTokensTokenWithHttpInfo(token, status,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingToken',) as IssuingToken;
    
    }
    return null;
  }

  /// Update a transaction
  ///
  /// <p>Updates the specified Issuing <code>Transaction</code> object by setting the values of the parameters passed. Any parameters not provided will be left unchanged.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] transaction (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<Response> postIssuingTransactionsTransactionWithHttpInfo(String transaction, { List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/issuing/transactions/{transaction}'
      .replaceAll('{transaction}', transaction);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a transaction
  ///
  /// <p>Updates the specified Issuing <code>Transaction</code> object by setting the values of the parameters passed. Any parameters not provided will be left unchanged.</p>
  ///
  /// Parameters:
  ///
  /// * [String] transaction (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<IssuingTransaction?> postIssuingTransactionsTransaction(String transaction, { List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    final response = await postIssuingTransactionsTransactionWithHttpInfo(transaction,  expand: expand, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingTransaction',) as IssuingTransaction;
    
    }
    return null;
  }

  /// Create a Session
  ///
  /// <p>To launch the Financial Connections authorization flow, create a <code>Session</code>. The session’s <code>client_secret</code> can be used to launch the flow using Stripe.js.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [AccountholderParams1] accountHolder (required):
  ///
  /// * [List<String>] permissions (required):
  ///   List of data features that you would like to request access to.  Possible values are `balances`, `transactions`, `ownership`, and `payment_method`.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [FiltersParams] filters:
  ///
  /// * [List<String>] prefetch:
  ///   List of data features that you would like to retrieve upon account creation.
  ///
  /// * [String] returnUrl:
  ///   For webview integrations only. Upon completing OAuth login in the native browser, the user will be redirected to this URL to return to your app.
  Future<Response> postLinkAccountSessionsWithHttpInfo(AccountholderParams1 accountHolder, List<String> permissions, { List<String>? expand, FiltersParams? filters, List<String>? prefetch, String? returnUrl, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/link_account_sessions';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (accountHolder != null) {
      formParams[r'account_holder'] = parameterToString(accountHolder);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (filters != null) {
      formParams[r'filters'] = parameterToString(filters);
    }
    if (permissions != null) {
      formParams[r'permissions'] = parameterToString(permissions);
    }
    if (prefetch != null) {
      formParams[r'prefetch'] = parameterToString(prefetch);
    }
    if (returnUrl != null) {
      formParams[r'return_url'] = parameterToString(returnUrl);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a Session
  ///
  /// <p>To launch the Financial Connections authorization flow, create a <code>Session</code>. The session’s <code>client_secret</code> can be used to launch the flow using Stripe.js.</p>
  ///
  /// Parameters:
  ///
  /// * [AccountholderParams1] accountHolder (required):
  ///
  /// * [List<String>] permissions (required):
  ///   List of data features that you would like to request access to.  Possible values are `balances`, `transactions`, `ownership`, and `payment_method`.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [FiltersParams] filters:
  ///
  /// * [List<String>] prefetch:
  ///   List of data features that you would like to retrieve upon account creation.
  ///
  /// * [String] returnUrl:
  ///   For webview integrations only. Upon completing OAuth login in the native browser, the user will be redirected to this URL to return to your app.
  Future<FinancialConnectionsSession?> postLinkAccountSessions(AccountholderParams1 accountHolder, List<String> permissions, { List<String>? expand, FiltersParams? filters, List<String>? prefetch, String? returnUrl, }) async {
    final response = await postLinkAccountSessionsWithHttpInfo(accountHolder, permissions,  expand: expand, filters: filters, prefetch: prefetch, returnUrl: returnUrl, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'FinancialConnectionsSession',) as FinancialConnectionsSession;
    
    }
    return null;
  }

  /// Disconnect an Account
  ///
  /// <p>Disables your access to a Financial Connections <code>Account</code>. You will no longer be able to access data associated with the account (e.g. balances, transactions).</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postLinkedAccountsAccountDisconnectWithHttpInfo(String account, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/linked_accounts/{account}/disconnect'
      .replaceAll('{account}', account);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Disconnect an Account
  ///
  /// <p>Disables your access to a Financial Connections <code>Account</code>. You will no longer be able to access data associated with the account (e.g. balances, transactions).</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<FinancialConnectionsAccount?> postLinkedAccountsAccountDisconnect(String account, { List<String>? expand, }) async {
    final response = await postLinkedAccountsAccountDisconnectWithHttpInfo(account,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'FinancialConnectionsAccount',) as FinancialConnectionsAccount;
    
    }
    return null;
  }

  /// Refresh Account data
  ///
  /// <p>Refreshes the data associated with a Financial Connections <code>Account</code>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [List<String>] features (required):
  ///   The list of account features that you would like to refresh.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postLinkedAccountsAccountRefreshWithHttpInfo(String account, List<String> features, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/linked_accounts/{account}/refresh'
      .replaceAll('{account}', account);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (features != null) {
      formParams[r'features'] = parameterToString(features);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Refresh Account data
  ///
  /// <p>Refreshes the data associated with a Financial Connections <code>Account</code>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] account (required):
  ///
  /// * [List<String>] features (required):
  ///   The list of account features that you would like to refresh.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<FinancialConnectionsAccount?> postLinkedAccountsAccountRefresh(String account, List<String> features, { List<String>? expand, }) async {
    final response = await postLinkedAccountsAccountRefreshWithHttpInfo(account, features,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'FinancialConnectionsAccount',) as FinancialConnectionsAccount;
    
    }
    return null;
  }

  /// Create a PaymentIntent
  ///
  /// <p>Creates a PaymentIntent object.</p>  <p>After the PaymentIntent is created, attach a payment method and <a href=\"/docs/api/payment_intents/confirm\">confirm</a> to continue the payment. Learn more about <a href=\"/docs/payments/payment-intents\">the available payment flows with the Payment Intents API</a>.</p>  <p>When you use <code>confirm=true</code> during creation, it’s equivalent to creating and confirming the PaymentIntent in the same call. You can use any parameters available in the <a href=\"/docs/api/payment_intents/confirm\">confirm API</a> when you supply <code>confirm=true</code>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [int] amount (required):
  ///   Amount intended to be collected by this PaymentIntent. A positive integer representing how much to charge in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal) (e.g., 100 cents to charge $1.00 or 100 to charge ¥100, a zero-decimal currency). The minimum amount is $0.50 US or [equivalent in charge currency](https://stripe.com/docs/currencies#minimum-and-maximum-charge-amounts). The amount value supports up to eight digits (e.g., a value of 99999999 for a USD charge of $999,999.99).
  ///
  /// * [String] currency (required):
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [int] applicationFeeAmount:
  ///   The amount of the application fee (if any) that will be requested to be applied to the payment and transferred to the application owner's Stripe account. The amount of the application fee collected will be capped at the total amount captured. For more information, see the PaymentIntents [use case for connected accounts](https://stripe.com/docs/payments/connected-accounts).
  ///
  /// * [AutomaticPaymentMethodsParam] automaticPaymentMethods:
  ///
  /// * [String] captureMethod:
  ///   Controls when the funds will be captured from the customer's account.
  ///
  /// * [bool] confirm:
  ///   Set to `true` to attempt to [confirm this PaymentIntent](https://stripe.com/docs/api/payment_intents/confirm) immediately. This parameter defaults to `false`. When creating and confirming a PaymentIntent at the same time, you can also provide the parameters available in the [Confirm API](https://stripe.com/docs/api/payment_intents/confirm).
  ///
  /// * [String] confirmationMethod:
  ///   Describes whether we can confirm this PaymentIntent automatically, or if it requires customer action to confirm the payment.
  ///
  /// * [String] confirmationToken:
  ///   ID of the ConfirmationToken used to confirm this PaymentIntent.  If the provided ConfirmationToken contains properties that are also being provided in this request, such as `payment_method`, then the values in this request will take precedence.
  ///
  /// * [String] customer:
  ///   ID of the Customer this PaymentIntent belongs to, if one exists.  Payment methods attached to other Customers cannot be used with this PaymentIntent.  If [setup_future_usage](https://stripe.com/docs/api#payment_intent_object-setup_future_usage) is set and this PaymentIntent's payment method is not `card_present`, then the payment method attaches to the Customer after the PaymentIntent has been confirmed and any required actions from the user are complete. If the payment method is `card_present` and isn't a digital wallet, then a [generated_card](https://docs.stripe.com/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card is created and attached to the Customer instead.
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [bool] errorOnRequiresAction:
  ///   Set to `true` to fail the payment attempt if the PaymentIntent transitions into `requires_action`. Use this parameter for simpler integrations that don't handle customer actions, such as [saving cards without authentication](https://stripe.com/docs/payments/save-card-without-authentication). This parameter can only be used with [`confirm=true`](https://stripe.com/docs/api/payment_intents/create#create_payment_intent-confirm).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] mandate:
  ///   ID of the mandate that's used for this payment. This parameter can only be used with [`confirm=true`](https://stripe.com/docs/api/payment_intents/create#create_payment_intent-confirm).
  ///
  /// * [PostPaymentIntentsRequestMandateData] mandateData:
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [PostPaymentIntentsRequestOffSession] offSession:
  ///
  /// * [String] onBehalfOf:
  ///   The Stripe account ID that these funds are intended for. Learn more about the [use case for connected accounts](https://stripe.com/docs/payments/connected-accounts).
  ///
  /// * [String] paymentMethod:
  ///   ID of the payment method (a PaymentMethod, Card, or [compatible Source](https://stripe.com/docs/payments/payment-methods/transitioning#compatibility) object) to attach to this PaymentIntent.  If you omit this parameter with `confirm=true`, `customer.default_source` attaches as this PaymentIntent's payment instrument to improve migration for users of the Charges API. We recommend that you explicitly provide the `payment_method` moving forward. If the payment method is attached to a Customer, you must also provide the ID of that Customer as the [customer](https://stripe.com/docs/api#create_payment_intent-customer) parameter of this PaymentIntent.
  ///
  /// * [String] paymentMethodConfiguration:
  ///   The ID of the [payment method configuration](https://stripe.com/docs/api/payment_method_configurations) to use with this PaymentIntent.
  ///
  /// * [PaymentMethodDataParams] paymentMethodData:
  ///
  /// * [PaymentMethodOptionsParam19] paymentMethodOptions:
  ///
  /// * [List<String>] paymentMethodTypes:
  ///   The list of payment method types (for example, a card) that this PaymentIntent can use. If you don't provide this, Stripe will dynamically show relevant payment methods from your [payment method settings](https://dashboard.stripe.com/settings/payment_methods). A list of valid payment method types can be found [here](https://docs.stripe.com/api/payment_methods/object#payment_method_object-type).
  ///
  /// * [RadarOptionsWithHiddenOptions2] radarOptions:
  ///
  /// * [String] receiptEmail:
  ///   Email address to send the receipt to. If you specify `receipt_email` for a payment in live mode, you send a receipt regardless of your [email settings](https://dashboard.stripe.com/account/emails).
  ///
  /// * [String] returnUrl:
  ///   The URL to redirect your customer back to after they authenticate or cancel their payment on the payment method's app or site. If you'd prefer to redirect to a mobile application, you can alternatively supply an application URI scheme. This parameter can only be used with [`confirm=true`](https://stripe.com/docs/api/payment_intents/create#create_payment_intent-confirm).
  ///
  /// * [String] setupFutureUsage:
  ///   Indicates that you intend to make future payments with this PaymentIntent's payment method.  If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.  If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.  When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
  ///
  /// * [OptionalFieldsShipping1] shipping:
  ///
  /// * [String] statementDescriptor:
  ///   Text that appears on the customer's statement as the statement descriptor for a non-card charge. This value overrides the account's default statement descriptor. For information about requirements, including the 22-character limit, see [the Statement Descriptor docs](https://docs.stripe.com/get-started/account/statement-descriptors).  Setting this value for a card charge returns an error. For card charges, set the [statement_descriptor_suffix](https://docs.stripe.com/get-started/account/statement-descriptors#dynamic) instead.
  ///
  /// * [String] statementDescriptorSuffix:
  ///   Provides information about a card charge. Concatenated to the account's [statement descriptor prefix](https://docs.stripe.com/get-started/account/statement-descriptors#static) to form the complete statement descriptor that appears on the customer's statement.
  ///
  /// * [TransferDataCreationParams] transferData:
  ///
  /// * [String] transferGroup:
  ///   A string that identifies the resulting payment as part of a group. Learn more about the [use case for connected accounts](https://stripe.com/docs/connect/separate-charges-and-transfers).
  ///
  /// * [bool] useStripeSdk:
  ///   Set to `true` when confirming server-side and using Stripe.js, iOS, or Android client-side SDKs to handle the next actions.
  Future<Response> postPaymentIntentsWithHttpInfo(int amount, String currency, { int? applicationFeeAmount, AutomaticPaymentMethodsParam? automaticPaymentMethods, String? captureMethod, bool? confirm, String? confirmationMethod, String? confirmationToken, String? customer, String? description, bool? errorOnRequiresAction, List<String>? expand, String? mandate, PostPaymentIntentsRequestMandateData? mandateData, Map<String, String>? metadata, PostPaymentIntentsRequestOffSession? offSession, String? onBehalfOf, String? paymentMethod, String? paymentMethodConfiguration, PaymentMethodDataParams? paymentMethodData, PaymentMethodOptionsParam19? paymentMethodOptions, List<String>? paymentMethodTypes, RadarOptionsWithHiddenOptions2? radarOptions, String? receiptEmail, String? returnUrl, String? setupFutureUsage, OptionalFieldsShipping1? shipping, String? statementDescriptor, String? statementDescriptorSuffix, TransferDataCreationParams? transferData, String? transferGroup, bool? useStripeSdk, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payment_intents';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (applicationFeeAmount != null) {
      formParams[r'application_fee_amount'] = parameterToString(applicationFeeAmount);
    }
    if (automaticPaymentMethods != null) {
      formParams[r'automatic_payment_methods'] = parameterToString(automaticPaymentMethods);
    }
    if (captureMethod != null) {
      formParams[r'capture_method'] = parameterToString(captureMethod);
    }
    if (confirm != null) {
      formParams[r'confirm'] = parameterToString(confirm);
    }
    if (confirmationMethod != null) {
      formParams[r'confirmation_method'] = parameterToString(confirmationMethod);
    }
    if (confirmationToken != null) {
      formParams[r'confirmation_token'] = parameterToString(confirmationToken);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (customer != null) {
      formParams[r'customer'] = parameterToString(customer);
    }
    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (errorOnRequiresAction != null) {
      formParams[r'error_on_requires_action'] = parameterToString(errorOnRequiresAction);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (mandate != null) {
      formParams[r'mandate'] = parameterToString(mandate);
    }
    if (mandateData != null) {
      formParams[r'mandate_data'] = parameterToString(mandateData);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (offSession != null) {
      formParams[r'off_session'] = parameterToString(offSession);
    }
    if (onBehalfOf != null) {
      formParams[r'on_behalf_of'] = parameterToString(onBehalfOf);
    }
    if (paymentMethod != null) {
      formParams[r'payment_method'] = parameterToString(paymentMethod);
    }
    if (paymentMethodConfiguration != null) {
      formParams[r'payment_method_configuration'] = parameterToString(paymentMethodConfiguration);
    }
    if (paymentMethodData != null) {
      formParams[r'payment_method_data'] = parameterToString(paymentMethodData);
    }
    if (paymentMethodOptions != null) {
      formParams[r'payment_method_options'] = parameterToString(paymentMethodOptions);
    }
    if (paymentMethodTypes != null) {
      formParams[r'payment_method_types'] = parameterToString(paymentMethodTypes);
    }
    if (radarOptions != null) {
      formParams[r'radar_options'] = parameterToString(radarOptions);
    }
    if (receiptEmail != null) {
      formParams[r'receipt_email'] = parameterToString(receiptEmail);
    }
    if (returnUrl != null) {
      formParams[r'return_url'] = parameterToString(returnUrl);
    }
    if (setupFutureUsage != null) {
      formParams[r'setup_future_usage'] = parameterToString(setupFutureUsage);
    }
    if (shipping != null) {
      formParams[r'shipping'] = parameterToString(shipping);
    }
    if (statementDescriptor != null) {
      formParams[r'statement_descriptor'] = parameterToString(statementDescriptor);
    }
    if (statementDescriptorSuffix != null) {
      formParams[r'statement_descriptor_suffix'] = parameterToString(statementDescriptorSuffix);
    }
    if (transferData != null) {
      formParams[r'transfer_data'] = parameterToString(transferData);
    }
    if (transferGroup != null) {
      formParams[r'transfer_group'] = parameterToString(transferGroup);
    }
    if (useStripeSdk != null) {
      formParams[r'use_stripe_sdk'] = parameterToString(useStripeSdk);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a PaymentIntent
  ///
  /// <p>Creates a PaymentIntent object.</p>  <p>After the PaymentIntent is created, attach a payment method and <a href=\"/docs/api/payment_intents/confirm\">confirm</a> to continue the payment. Learn more about <a href=\"/docs/payments/payment-intents\">the available payment flows with the Payment Intents API</a>.</p>  <p>When you use <code>confirm=true</code> during creation, it’s equivalent to creating and confirming the PaymentIntent in the same call. You can use any parameters available in the <a href=\"/docs/api/payment_intents/confirm\">confirm API</a> when you supply <code>confirm=true</code>.</p>
  ///
  /// Parameters:
  ///
  /// * [int] amount (required):
  ///   Amount intended to be collected by this PaymentIntent. A positive integer representing how much to charge in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal) (e.g., 100 cents to charge $1.00 or 100 to charge ¥100, a zero-decimal currency). The minimum amount is $0.50 US or [equivalent in charge currency](https://stripe.com/docs/currencies#minimum-and-maximum-charge-amounts). The amount value supports up to eight digits (e.g., a value of 99999999 for a USD charge of $999,999.99).
  ///
  /// * [String] currency (required):
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [int] applicationFeeAmount:
  ///   The amount of the application fee (if any) that will be requested to be applied to the payment and transferred to the application owner's Stripe account. The amount of the application fee collected will be capped at the total amount captured. For more information, see the PaymentIntents [use case for connected accounts](https://stripe.com/docs/payments/connected-accounts).
  ///
  /// * [AutomaticPaymentMethodsParam] automaticPaymentMethods:
  ///
  /// * [String] captureMethod:
  ///   Controls when the funds will be captured from the customer's account.
  ///
  /// * [bool] confirm:
  ///   Set to `true` to attempt to [confirm this PaymentIntent](https://stripe.com/docs/api/payment_intents/confirm) immediately. This parameter defaults to `false`. When creating and confirming a PaymentIntent at the same time, you can also provide the parameters available in the [Confirm API](https://stripe.com/docs/api/payment_intents/confirm).
  ///
  /// * [String] confirmationMethod:
  ///   Describes whether we can confirm this PaymentIntent automatically, or if it requires customer action to confirm the payment.
  ///
  /// * [String] confirmationToken:
  ///   ID of the ConfirmationToken used to confirm this PaymentIntent.  If the provided ConfirmationToken contains properties that are also being provided in this request, such as `payment_method`, then the values in this request will take precedence.
  ///
  /// * [String] customer:
  ///   ID of the Customer this PaymentIntent belongs to, if one exists.  Payment methods attached to other Customers cannot be used with this PaymentIntent.  If [setup_future_usage](https://stripe.com/docs/api#payment_intent_object-setup_future_usage) is set and this PaymentIntent's payment method is not `card_present`, then the payment method attaches to the Customer after the PaymentIntent has been confirmed and any required actions from the user are complete. If the payment method is `card_present` and isn't a digital wallet, then a [generated_card](https://docs.stripe.com/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card is created and attached to the Customer instead.
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [bool] errorOnRequiresAction:
  ///   Set to `true` to fail the payment attempt if the PaymentIntent transitions into `requires_action`. Use this parameter for simpler integrations that don't handle customer actions, such as [saving cards without authentication](https://stripe.com/docs/payments/save-card-without-authentication). This parameter can only be used with [`confirm=true`](https://stripe.com/docs/api/payment_intents/create#create_payment_intent-confirm).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] mandate:
  ///   ID of the mandate that's used for this payment. This parameter can only be used with [`confirm=true`](https://stripe.com/docs/api/payment_intents/create#create_payment_intent-confirm).
  ///
  /// * [PostPaymentIntentsRequestMandateData] mandateData:
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [PostPaymentIntentsRequestOffSession] offSession:
  ///
  /// * [String] onBehalfOf:
  ///   The Stripe account ID that these funds are intended for. Learn more about the [use case for connected accounts](https://stripe.com/docs/payments/connected-accounts).
  ///
  /// * [String] paymentMethod:
  ///   ID of the payment method (a PaymentMethod, Card, or [compatible Source](https://stripe.com/docs/payments/payment-methods/transitioning#compatibility) object) to attach to this PaymentIntent.  If you omit this parameter with `confirm=true`, `customer.default_source` attaches as this PaymentIntent's payment instrument to improve migration for users of the Charges API. We recommend that you explicitly provide the `payment_method` moving forward. If the payment method is attached to a Customer, you must also provide the ID of that Customer as the [customer](https://stripe.com/docs/api#create_payment_intent-customer) parameter of this PaymentIntent.
  ///
  /// * [String] paymentMethodConfiguration:
  ///   The ID of the [payment method configuration](https://stripe.com/docs/api/payment_method_configurations) to use with this PaymentIntent.
  ///
  /// * [PaymentMethodDataParams] paymentMethodData:
  ///
  /// * [PaymentMethodOptionsParam19] paymentMethodOptions:
  ///
  /// * [List<String>] paymentMethodTypes:
  ///   The list of payment method types (for example, a card) that this PaymentIntent can use. If you don't provide this, Stripe will dynamically show relevant payment methods from your [payment method settings](https://dashboard.stripe.com/settings/payment_methods). A list of valid payment method types can be found [here](https://docs.stripe.com/api/payment_methods/object#payment_method_object-type).
  ///
  /// * [RadarOptionsWithHiddenOptions2] radarOptions:
  ///
  /// * [String] receiptEmail:
  ///   Email address to send the receipt to. If you specify `receipt_email` for a payment in live mode, you send a receipt regardless of your [email settings](https://dashboard.stripe.com/account/emails).
  ///
  /// * [String] returnUrl:
  ///   The URL to redirect your customer back to after they authenticate or cancel their payment on the payment method's app or site. If you'd prefer to redirect to a mobile application, you can alternatively supply an application URI scheme. This parameter can only be used with [`confirm=true`](https://stripe.com/docs/api/payment_intents/create#create_payment_intent-confirm).
  ///
  /// * [String] setupFutureUsage:
  ///   Indicates that you intend to make future payments with this PaymentIntent's payment method.  If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.  If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.  When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).
  ///
  /// * [OptionalFieldsShipping1] shipping:
  ///
  /// * [String] statementDescriptor:
  ///   Text that appears on the customer's statement as the statement descriptor for a non-card charge. This value overrides the account's default statement descriptor. For information about requirements, including the 22-character limit, see [the Statement Descriptor docs](https://docs.stripe.com/get-started/account/statement-descriptors).  Setting this value for a card charge returns an error. For card charges, set the [statement_descriptor_suffix](https://docs.stripe.com/get-started/account/statement-descriptors#dynamic) instead.
  ///
  /// * [String] statementDescriptorSuffix:
  ///   Provides information about a card charge. Concatenated to the account's [statement descriptor prefix](https://docs.stripe.com/get-started/account/statement-descriptors#static) to form the complete statement descriptor that appears on the customer's statement.
  ///
  /// * [TransferDataCreationParams] transferData:
  ///
  /// * [String] transferGroup:
  ///   A string that identifies the resulting payment as part of a group. Learn more about the [use case for connected accounts](https://stripe.com/docs/connect/separate-charges-and-transfers).
  ///
  /// * [bool] useStripeSdk:
  ///   Set to `true` when confirming server-side and using Stripe.js, iOS, or Android client-side SDKs to handle the next actions.
  Future<PaymentIntent?> postPaymentIntents(int amount, String currency, { int? applicationFeeAmount, AutomaticPaymentMethodsParam? automaticPaymentMethods, String? captureMethod, bool? confirm, String? confirmationMethod, String? confirmationToken, String? customer, String? description, bool? errorOnRequiresAction, List<String>? expand, String? mandate, PostPaymentIntentsRequestMandateData? mandateData, Map<String, String>? metadata, PostPaymentIntentsRequestOffSession? offSession, String? onBehalfOf, String? paymentMethod, String? paymentMethodConfiguration, PaymentMethodDataParams? paymentMethodData, PaymentMethodOptionsParam19? paymentMethodOptions, List<String>? paymentMethodTypes, RadarOptionsWithHiddenOptions2? radarOptions, String? receiptEmail, String? returnUrl, String? setupFutureUsage, OptionalFieldsShipping1? shipping, String? statementDescriptor, String? statementDescriptorSuffix, TransferDataCreationParams? transferData, String? transferGroup, bool? useStripeSdk, }) async {
    final response = await postPaymentIntentsWithHttpInfo(amount, currency,  applicationFeeAmount: applicationFeeAmount, automaticPaymentMethods: automaticPaymentMethods, captureMethod: captureMethod, confirm: confirm, confirmationMethod: confirmationMethod, confirmationToken: confirmationToken, customer: customer, description: description, errorOnRequiresAction: errorOnRequiresAction, expand: expand, mandate: mandate, mandateData: mandateData, metadata: metadata, offSession: offSession, onBehalfOf: onBehalfOf, paymentMethod: paymentMethod, paymentMethodConfiguration: paymentMethodConfiguration, paymentMethodData: paymentMethodData, paymentMethodOptions: paymentMethodOptions, paymentMethodTypes: paymentMethodTypes, radarOptions: radarOptions, receiptEmail: receiptEmail, returnUrl: returnUrl, setupFutureUsage: setupFutureUsage, shipping: shipping, statementDescriptor: statementDescriptor, statementDescriptorSuffix: statementDescriptorSuffix, transferData: transferData, transferGroup: transferGroup, useStripeSdk: useStripeSdk, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentIntent',) as PaymentIntent;
    
    }
    return null;
  }

  /// Update a PaymentIntent
  ///
  /// <p>Updates properties on a PaymentIntent object without confirming.</p>  <p>Depending on which properties you update, you might need to confirm the PaymentIntent again. For example, updating the <code>payment_method</code> always requires you to confirm the PaymentIntent again. If you prefer to update and confirm at the same time, we recommend updating properties through the <a href=\"/docs/api/payment_intents/confirm\">confirm API</a> instead.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] intent (required):
  ///
  /// * [int] amount:
  ///   Amount intended to be collected by this PaymentIntent. A positive integer representing how much to charge in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal) (e.g., 100 cents to charge $1.00 or 100 to charge ¥100, a zero-decimal currency). The minimum amount is $0.50 US or [equivalent in charge currency](https://stripe.com/docs/currencies#minimum-and-maximum-charge-amounts). The amount value supports up to eight digits (e.g., a value of 99999999 for a USD charge of $999,999.99).
  ///
  /// * [PostPaymentIntentsIntentRequestApplicationFeeAmount] applicationFeeAmount:
  ///
  /// * [String] captureMethod:
  ///   Controls when the funds will be captured from the customer's account.
  ///
  /// * [String] currency:
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] customer:
  ///   ID of the Customer this PaymentIntent belongs to, if one exists.  Payment methods attached to other Customers cannot be used with this PaymentIntent.  If [setup_future_usage](https://stripe.com/docs/api#payment_intent_object-setup_future_usage) is set and this PaymentIntent's payment method is not `card_present`, then the payment method attaches to the Customer after the PaymentIntent has been confirmed and any required actions from the user are complete. If the payment method is `card_present` and isn't a digital wallet, then a [generated_card](https://docs.stripe.com/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card is created and attached to the Customer instead.
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] paymentMethod:
  ///   ID of the payment method (a PaymentMethod, Card, or [compatible Source](https://stripe.com/docs/payments/payment-methods/transitioning#compatibility) object) to attach to this PaymentIntent. To unset this field to null, pass in an empty string.
  ///
  /// * [String] paymentMethodConfiguration:
  ///   The ID of the [payment method configuration](https://stripe.com/docs/api/payment_method_configurations) to use with this PaymentIntent.
  ///
  /// * [PaymentMethodDataParams] paymentMethodData:
  ///
  /// * [PaymentMethodOptionsParam35] paymentMethodOptions:
  ///
  /// * [List<String>] paymentMethodTypes:
  ///   The list of payment method types (for example, card) that this PaymentIntent can use. Use `automatic_payment_methods` to manage payment methods from the [Stripe Dashboard](https://dashboard.stripe.com/settings/payment_methods). A list of valid payment method types can be found [here](https://docs.stripe.com/api/payment_methods/object#payment_method_object-type).
  ///
  /// * [PostPaymentIntentsIntentRequestReceiptEmail] receiptEmail:
  ///
  /// * [String] setupFutureUsage:
  ///   Indicates that you intend to make future payments with this PaymentIntent's payment method.  If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.  If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.  When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).  If you've already set `setup_future_usage` and you're performing a request using a publishable key, you can only update the value from `on_session` to `off_session`.
  ///
  /// * [PostPaymentIntentsIntentRequestShipping] shipping:
  ///
  /// * [String] statementDescriptor:
  ///   Text that appears on the customer's statement as the statement descriptor for a non-card charge. This value overrides the account's default statement descriptor. For information about requirements, including the 22-character limit, see [the Statement Descriptor docs](https://docs.stripe.com/get-started/account/statement-descriptors).  Setting this value for a card charge returns an error. For card charges, set the [statement_descriptor_suffix](https://docs.stripe.com/get-started/account/statement-descriptors#dynamic) instead.
  ///
  /// * [String] statementDescriptorSuffix:
  ///   Provides information about a card charge. Concatenated to the account's [statement descriptor prefix](https://docs.stripe.com/get-started/account/statement-descriptors#static) to form the complete statement descriptor that appears on the customer's statement.
  ///
  /// * [TransferDataUpdateParams] transferData:
  ///
  /// * [String] transferGroup:
  ///   A string that identifies the resulting payment as part of a group. You can only provide `transfer_group` if it hasn't been set. Learn more about the [use case for connected accounts](https://stripe.com/docs/payments/connected-accounts).
  Future<Response> postPaymentIntentsIntentWithHttpInfo(String intent, { int? amount, PostPaymentIntentsIntentRequestApplicationFeeAmount? applicationFeeAmount, String? captureMethod, String? currency, String? customer, String? description, List<String>? expand, PostAccountsRequestMetadata? metadata, String? paymentMethod, String? paymentMethodConfiguration, PaymentMethodDataParams? paymentMethodData, PaymentMethodOptionsParam35? paymentMethodOptions, List<String>? paymentMethodTypes, PostPaymentIntentsIntentRequestReceiptEmail? receiptEmail, String? setupFutureUsage, PostPaymentIntentsIntentRequestShipping? shipping, String? statementDescriptor, String? statementDescriptorSuffix, TransferDataUpdateParams? transferData, String? transferGroup, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payment_intents/{intent}'
      .replaceAll('{intent}', intent);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (applicationFeeAmount != null) {
      formParams[r'application_fee_amount'] = parameterToString(applicationFeeAmount);
    }
    if (captureMethod != null) {
      formParams[r'capture_method'] = parameterToString(captureMethod);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (customer != null) {
      formParams[r'customer'] = parameterToString(customer);
    }
    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (paymentMethod != null) {
      formParams[r'payment_method'] = parameterToString(paymentMethod);
    }
    if (paymentMethodConfiguration != null) {
      formParams[r'payment_method_configuration'] = parameterToString(paymentMethodConfiguration);
    }
    if (paymentMethodData != null) {
      formParams[r'payment_method_data'] = parameterToString(paymentMethodData);
    }
    if (paymentMethodOptions != null) {
      formParams[r'payment_method_options'] = parameterToString(paymentMethodOptions);
    }
    if (paymentMethodTypes != null) {
      formParams[r'payment_method_types'] = parameterToString(paymentMethodTypes);
    }
    if (receiptEmail != null) {
      formParams[r'receipt_email'] = parameterToString(receiptEmail);
    }
    if (setupFutureUsage != null) {
      formParams[r'setup_future_usage'] = parameterToString(setupFutureUsage);
    }
    if (shipping != null) {
      formParams[r'shipping'] = parameterToString(shipping);
    }
    if (statementDescriptor != null) {
      formParams[r'statement_descriptor'] = parameterToString(statementDescriptor);
    }
    if (statementDescriptorSuffix != null) {
      formParams[r'statement_descriptor_suffix'] = parameterToString(statementDescriptorSuffix);
    }
    if (transferData != null) {
      formParams[r'transfer_data'] = parameterToString(transferData);
    }
    if (transferGroup != null) {
      formParams[r'transfer_group'] = parameterToString(transferGroup);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a PaymentIntent
  ///
  /// <p>Updates properties on a PaymentIntent object without confirming.</p>  <p>Depending on which properties you update, you might need to confirm the PaymentIntent again. For example, updating the <code>payment_method</code> always requires you to confirm the PaymentIntent again. If you prefer to update and confirm at the same time, we recommend updating properties through the <a href=\"/docs/api/payment_intents/confirm\">confirm API</a> instead.</p>
  ///
  /// Parameters:
  ///
  /// * [String] intent (required):
  ///
  /// * [int] amount:
  ///   Amount intended to be collected by this PaymentIntent. A positive integer representing how much to charge in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal) (e.g., 100 cents to charge $1.00 or 100 to charge ¥100, a zero-decimal currency). The minimum amount is $0.50 US or [equivalent in charge currency](https://stripe.com/docs/currencies#minimum-and-maximum-charge-amounts). The amount value supports up to eight digits (e.g., a value of 99999999 for a USD charge of $999,999.99).
  ///
  /// * [PostPaymentIntentsIntentRequestApplicationFeeAmount] applicationFeeAmount:
  ///
  /// * [String] captureMethod:
  ///   Controls when the funds will be captured from the customer's account.
  ///
  /// * [String] currency:
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] customer:
  ///   ID of the Customer this PaymentIntent belongs to, if one exists.  Payment methods attached to other Customers cannot be used with this PaymentIntent.  If [setup_future_usage](https://stripe.com/docs/api#payment_intent_object-setup_future_usage) is set and this PaymentIntent's payment method is not `card_present`, then the payment method attaches to the Customer after the PaymentIntent has been confirmed and any required actions from the user are complete. If the payment method is `card_present` and isn't a digital wallet, then a [generated_card](https://docs.stripe.com/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card is created and attached to the Customer instead.
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] paymentMethod:
  ///   ID of the payment method (a PaymentMethod, Card, or [compatible Source](https://stripe.com/docs/payments/payment-methods/transitioning#compatibility) object) to attach to this PaymentIntent. To unset this field to null, pass in an empty string.
  ///
  /// * [String] paymentMethodConfiguration:
  ///   The ID of the [payment method configuration](https://stripe.com/docs/api/payment_method_configurations) to use with this PaymentIntent.
  ///
  /// * [PaymentMethodDataParams] paymentMethodData:
  ///
  /// * [PaymentMethodOptionsParam35] paymentMethodOptions:
  ///
  /// * [List<String>] paymentMethodTypes:
  ///   The list of payment method types (for example, card) that this PaymentIntent can use. Use `automatic_payment_methods` to manage payment methods from the [Stripe Dashboard](https://dashboard.stripe.com/settings/payment_methods). A list of valid payment method types can be found [here](https://docs.stripe.com/api/payment_methods/object#payment_method_object-type).
  ///
  /// * [PostPaymentIntentsIntentRequestReceiptEmail] receiptEmail:
  ///
  /// * [String] setupFutureUsage:
  ///   Indicates that you intend to make future payments with this PaymentIntent's payment method.  If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.  If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.  When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).  If you've already set `setup_future_usage` and you're performing a request using a publishable key, you can only update the value from `on_session` to `off_session`.
  ///
  /// * [PostPaymentIntentsIntentRequestShipping] shipping:
  ///
  /// * [String] statementDescriptor:
  ///   Text that appears on the customer's statement as the statement descriptor for a non-card charge. This value overrides the account's default statement descriptor. For information about requirements, including the 22-character limit, see [the Statement Descriptor docs](https://docs.stripe.com/get-started/account/statement-descriptors).  Setting this value for a card charge returns an error. For card charges, set the [statement_descriptor_suffix](https://docs.stripe.com/get-started/account/statement-descriptors#dynamic) instead.
  ///
  /// * [String] statementDescriptorSuffix:
  ///   Provides information about a card charge. Concatenated to the account's [statement descriptor prefix](https://docs.stripe.com/get-started/account/statement-descriptors#static) to form the complete statement descriptor that appears on the customer's statement.
  ///
  /// * [TransferDataUpdateParams] transferData:
  ///
  /// * [String] transferGroup:
  ///   A string that identifies the resulting payment as part of a group. You can only provide `transfer_group` if it hasn't been set. Learn more about the [use case for connected accounts](https://stripe.com/docs/payments/connected-accounts).
  Future<PaymentIntent?> postPaymentIntentsIntent(String intent, { int? amount, PostPaymentIntentsIntentRequestApplicationFeeAmount? applicationFeeAmount, String? captureMethod, String? currency, String? customer, String? description, List<String>? expand, PostAccountsRequestMetadata? metadata, String? paymentMethod, String? paymentMethodConfiguration, PaymentMethodDataParams? paymentMethodData, PaymentMethodOptionsParam35? paymentMethodOptions, List<String>? paymentMethodTypes, PostPaymentIntentsIntentRequestReceiptEmail? receiptEmail, String? setupFutureUsage, PostPaymentIntentsIntentRequestShipping? shipping, String? statementDescriptor, String? statementDescriptorSuffix, TransferDataUpdateParams? transferData, String? transferGroup, }) async {
    final response = await postPaymentIntentsIntentWithHttpInfo(intent,  amount: amount, applicationFeeAmount: applicationFeeAmount, captureMethod: captureMethod, currency: currency, customer: customer, description: description, expand: expand, metadata: metadata, paymentMethod: paymentMethod, paymentMethodConfiguration: paymentMethodConfiguration, paymentMethodData: paymentMethodData, paymentMethodOptions: paymentMethodOptions, paymentMethodTypes: paymentMethodTypes, receiptEmail: receiptEmail, setupFutureUsage: setupFutureUsage, shipping: shipping, statementDescriptor: statementDescriptor, statementDescriptorSuffix: statementDescriptorSuffix, transferData: transferData, transferGroup: transferGroup, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentIntent',) as PaymentIntent;
    
    }
    return null;
  }

  /// Reconcile a customer_balance PaymentIntent
  ///
  /// <p>Manually reconcile the remaining amount for a <code>customer_balance</code> PaymentIntent.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] intent (required):
  ///
  /// * [int] amount:
  ///   Amount that you intend to apply to this PaymentIntent from the customer’s cash balance. If the PaymentIntent was created by an Invoice, the full amount of the PaymentIntent is applied regardless of this parameter.  A positive integer representing how much to charge in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal) (for example, 100 cents to charge 1 USD or 100 to charge 100 JPY, a zero-decimal currency). The maximum amount is the amount of the PaymentIntent.  When you omit the amount, it defaults to the remaining amount requested on the PaymentIntent.
  ///
  /// * [String] currency:
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postPaymentIntentsIntentApplyCustomerBalanceWithHttpInfo(String intent, { int? amount, String? currency, List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payment_intents/{intent}/apply_customer_balance'
      .replaceAll('{intent}', intent);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Reconcile a customer_balance PaymentIntent
  ///
  /// <p>Manually reconcile the remaining amount for a <code>customer_balance</code> PaymentIntent.</p>
  ///
  /// Parameters:
  ///
  /// * [String] intent (required):
  ///
  /// * [int] amount:
  ///   Amount that you intend to apply to this PaymentIntent from the customer’s cash balance. If the PaymentIntent was created by an Invoice, the full amount of the PaymentIntent is applied regardless of this parameter.  A positive integer representing how much to charge in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal) (for example, 100 cents to charge 1 USD or 100 to charge 100 JPY, a zero-decimal currency). The maximum amount is the amount of the PaymentIntent.  When you omit the amount, it defaults to the remaining amount requested on the PaymentIntent.
  ///
  /// * [String] currency:
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<PaymentIntent?> postPaymentIntentsIntentApplyCustomerBalance(String intent, { int? amount, String? currency, List<String>? expand, }) async {
    final response = await postPaymentIntentsIntentApplyCustomerBalanceWithHttpInfo(intent,  amount: amount, currency: currency, expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentIntent',) as PaymentIntent;
    
    }
    return null;
  }

  /// Cancel a PaymentIntent
  ///
  /// <p>You can cancel a PaymentIntent object when it’s in one of these statuses: <code>requires_payment_method</code>, <code>requires_capture</code>, <code>requires_confirmation</code>, <code>requires_action</code> or, <a href=\"/docs/payments/intents\">in rare cases</a>, <code>processing</code>. </p>  <p>After it’s canceled, no additional charges are made by the PaymentIntent and any operations on the PaymentIntent fail with an error. For PaymentIntents with a <code>status</code> of <code>requires_capture</code>, the remaining <code>amount_capturable</code> is automatically refunded. </p>  <p>You can’t cancel the PaymentIntent for a Checkout Session. <a href=\"/docs/api/checkout/sessions/expire\">Expire the Checkout Session</a> instead.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] intent (required):
  ///
  /// * [String] cancellationReason:
  ///   Reason for canceling this PaymentIntent. Possible values are: `duplicate`, `fraudulent`, `requested_by_customer`, or `abandoned`
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postPaymentIntentsIntentCancelWithHttpInfo(String intent, { String? cancellationReason, List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payment_intents/{intent}/cancel'
      .replaceAll('{intent}', intent);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (cancellationReason != null) {
      formParams[r'cancellation_reason'] = parameterToString(cancellationReason);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Cancel a PaymentIntent
  ///
  /// <p>You can cancel a PaymentIntent object when it’s in one of these statuses: <code>requires_payment_method</code>, <code>requires_capture</code>, <code>requires_confirmation</code>, <code>requires_action</code> or, <a href=\"/docs/payments/intents\">in rare cases</a>, <code>processing</code>. </p>  <p>After it’s canceled, no additional charges are made by the PaymentIntent and any operations on the PaymentIntent fail with an error. For PaymentIntents with a <code>status</code> of <code>requires_capture</code>, the remaining <code>amount_capturable</code> is automatically refunded. </p>  <p>You can’t cancel the PaymentIntent for a Checkout Session. <a href=\"/docs/api/checkout/sessions/expire\">Expire the Checkout Session</a> instead.</p>
  ///
  /// Parameters:
  ///
  /// * [String] intent (required):
  ///
  /// * [String] cancellationReason:
  ///   Reason for canceling this PaymentIntent. Possible values are: `duplicate`, `fraudulent`, `requested_by_customer`, or `abandoned`
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<PaymentIntent?> postPaymentIntentsIntentCancel(String intent, { String? cancellationReason, List<String>? expand, }) async {
    final response = await postPaymentIntentsIntentCancelWithHttpInfo(intent,  cancellationReason: cancellationReason, expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentIntent',) as PaymentIntent;
    
    }
    return null;
  }

  /// Capture a PaymentIntent
  ///
  /// <p>Capture the funds of an existing uncaptured PaymentIntent when its status is <code>requires_capture</code>.</p>  <p>Uncaptured PaymentIntents are cancelled a set number of days (7 by default) after their creation.</p>  <p>Learn more about <a href=\"/docs/payments/capture-later\">separate authorization and capture</a>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] intent (required):
  ///
  /// * [int] amountToCapture:
  ///   The amount to capture from the PaymentIntent, which must be less than or equal to the original amount. Defaults to the full `amount_capturable` if it's not provided.
  ///
  /// * [int] applicationFeeAmount:
  ///   The amount of the application fee (if any) that will be requested to be applied to the payment and transferred to the application owner's Stripe account. The amount of the application fee collected will be capped at the total amount captured. For more information, see the PaymentIntents [use case for connected accounts](https://stripe.com/docs/payments/connected-accounts).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [bool] finalCapture:
  ///   Defaults to `true`. When capturing a PaymentIntent, setting `final_capture` to `false` notifies Stripe to not release the remaining uncaptured funds to make sure that they're captured in future requests. You can only use this setting when [multicapture](https://stripe.com/docs/payments/multicapture) is available for PaymentIntents.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] statementDescriptor:
  ///   Text that appears on the customer's statement as the statement descriptor for a non-card charge. This value overrides the account's default statement descriptor. For information about requirements, including the 22-character limit, see [the Statement Descriptor docs](https://docs.stripe.com/get-started/account/statement-descriptors).  Setting this value for a card charge returns an error. For card charges, set the [statement_descriptor_suffix](https://docs.stripe.com/get-started/account/statement-descriptors#dynamic) instead.
  ///
  /// * [String] statementDescriptorSuffix:
  ///   Provides information about a card charge. Concatenated to the account's [statement descriptor prefix](https://docs.stripe.com/get-started/account/statement-descriptors#static) to form the complete statement descriptor that appears on the customer's statement.
  ///
  /// * [TransferDataCaptureParams] transferData:
  Future<Response> postPaymentIntentsIntentCaptureWithHttpInfo(String intent, { int? amountToCapture, int? applicationFeeAmount, List<String>? expand, bool? finalCapture, PostAccountsRequestMetadata? metadata, String? statementDescriptor, String? statementDescriptorSuffix, TransferDataCaptureParams? transferData, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payment_intents/{intent}/capture'
      .replaceAll('{intent}', intent);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amountToCapture != null) {
      formParams[r'amount_to_capture'] = parameterToString(amountToCapture);
    }
    if (applicationFeeAmount != null) {
      formParams[r'application_fee_amount'] = parameterToString(applicationFeeAmount);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (finalCapture != null) {
      formParams[r'final_capture'] = parameterToString(finalCapture);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (statementDescriptor != null) {
      formParams[r'statement_descriptor'] = parameterToString(statementDescriptor);
    }
    if (statementDescriptorSuffix != null) {
      formParams[r'statement_descriptor_suffix'] = parameterToString(statementDescriptorSuffix);
    }
    if (transferData != null) {
      formParams[r'transfer_data'] = parameterToString(transferData);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Capture a PaymentIntent
  ///
  /// <p>Capture the funds of an existing uncaptured PaymentIntent when its status is <code>requires_capture</code>.</p>  <p>Uncaptured PaymentIntents are cancelled a set number of days (7 by default) after their creation.</p>  <p>Learn more about <a href=\"/docs/payments/capture-later\">separate authorization and capture</a>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] intent (required):
  ///
  /// * [int] amountToCapture:
  ///   The amount to capture from the PaymentIntent, which must be less than or equal to the original amount. Defaults to the full `amount_capturable` if it's not provided.
  ///
  /// * [int] applicationFeeAmount:
  ///   The amount of the application fee (if any) that will be requested to be applied to the payment and transferred to the application owner's Stripe account. The amount of the application fee collected will be capped at the total amount captured. For more information, see the PaymentIntents [use case for connected accounts](https://stripe.com/docs/payments/connected-accounts).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [bool] finalCapture:
  ///   Defaults to `true`. When capturing a PaymentIntent, setting `final_capture` to `false` notifies Stripe to not release the remaining uncaptured funds to make sure that they're captured in future requests. You can only use this setting when [multicapture](https://stripe.com/docs/payments/multicapture) is available for PaymentIntents.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] statementDescriptor:
  ///   Text that appears on the customer's statement as the statement descriptor for a non-card charge. This value overrides the account's default statement descriptor. For information about requirements, including the 22-character limit, see [the Statement Descriptor docs](https://docs.stripe.com/get-started/account/statement-descriptors).  Setting this value for a card charge returns an error. For card charges, set the [statement_descriptor_suffix](https://docs.stripe.com/get-started/account/statement-descriptors#dynamic) instead.
  ///
  /// * [String] statementDescriptorSuffix:
  ///   Provides information about a card charge. Concatenated to the account's [statement descriptor prefix](https://docs.stripe.com/get-started/account/statement-descriptors#static) to form the complete statement descriptor that appears on the customer's statement.
  ///
  /// * [TransferDataCaptureParams] transferData:
  Future<PaymentIntent?> postPaymentIntentsIntentCapture(String intent, { int? amountToCapture, int? applicationFeeAmount, List<String>? expand, bool? finalCapture, PostAccountsRequestMetadata? metadata, String? statementDescriptor, String? statementDescriptorSuffix, TransferDataCaptureParams? transferData, }) async {
    final response = await postPaymentIntentsIntentCaptureWithHttpInfo(intent,  amountToCapture: amountToCapture, applicationFeeAmount: applicationFeeAmount, expand: expand, finalCapture: finalCapture, metadata: metadata, statementDescriptor: statementDescriptor, statementDescriptorSuffix: statementDescriptorSuffix, transferData: transferData, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentIntent',) as PaymentIntent;
    
    }
    return null;
  }

  /// Confirm a PaymentIntent
  ///
  /// <p>Confirm that your customer intends to pay with current or provided payment method. Upon confirmation, the PaymentIntent will attempt to initiate a payment.</p>  <p>If the selected payment method requires additional authentication steps, the PaymentIntent will transition to the <code>requires_action</code> status and suggest additional actions via <code>next_action</code>. If payment fails, the PaymentIntent transitions to the <code>requires_payment_method</code> status or the <code>canceled</code> status if the confirmation limit is reached. If payment succeeds, the PaymentIntent will transition to the <code>succeeded</code> status (or <code>requires_capture</code>, if <code>capture_method</code> is set to <code>manual</code>).</p>  <p>If the <code>confirmation_method</code> is <code>automatic</code>, payment may be attempted using our <a href=\"/docs/stripe-js/reference#stripe-handle-card-payment\">client SDKs</a> and the PaymentIntent’s <a href=\"#payment_intent_object-client_secret\">client_secret</a>. After <code>next_action</code>s are handled by the client, no additional confirmation is required to complete the payment.</p>  <p>If the <code>confirmation_method</code> is <code>manual</code>, all payment attempts must be initiated using a secret key.</p>  <p>If any actions are required for the payment, the PaymentIntent will return to the <code>requires_confirmation</code> state after those actions are completed. Your server needs to then explicitly re-confirm the PaymentIntent to initiate the next payment attempt.</p>  <p>There is a variable upper limit on how many times a PaymentIntent can be confirmed. After this limit is reached, any further calls to this endpoint will transition the PaymentIntent to the <code>canceled</code> state.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] intent (required):
  ///
  /// * [String] captureMethod:
  ///   Controls when the funds will be captured from the customer's account.
  ///
  /// * [String] clientSecret:
  ///   The client secret of the PaymentIntent.
  ///
  /// * [String] confirmationToken:
  ///   ID of the ConfirmationToken used to confirm this PaymentIntent.  If the provided ConfirmationToken contains properties that are also being provided in this request, such as `payment_method`, then the values in this request will take precedence.
  ///
  /// * [bool] errorOnRequiresAction:
  ///   Set to `true` to fail the payment attempt if the PaymentIntent transitions into `requires_action`. This parameter is intended for simpler integrations that do not handle customer actions, like [saving cards without authentication](https://stripe.com/docs/payments/save-card-without-authentication).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] mandate:
  ///   ID of the mandate that's used for this payment.
  ///
  /// * [PostPaymentIntentsIntentConfirmRequestMandateData] mandateData:
  ///
  /// * [PostPaymentIntentsIntentConfirmRequestOffSession] offSession:
  ///
  /// * [String] paymentMethod:
  ///   ID of the payment method (a PaymentMethod, Card, or [compatible Source](https://stripe.com/docs/payments/payment-methods/transitioning#compatibility) object) to attach to this PaymentIntent. If the payment method is attached to a Customer, it must match the [customer](https://stripe.com/docs/api#create_payment_intent-customer) that is set on this PaymentIntent.
  ///
  /// * [PaymentMethodDataParams] paymentMethodData:
  ///
  /// * [PaymentMethodOptionsParam19] paymentMethodOptions:
  ///
  /// * [List<String>] paymentMethodTypes:
  ///   The list of payment method types (for example, a card) that this PaymentIntent can use. Use `automatic_payment_methods` to manage payment methods from the [Stripe Dashboard](https://dashboard.stripe.com/settings/payment_methods). A list of valid payment method types can be found [here](https://docs.stripe.com/api/payment_methods/object#payment_method_object-type).
  ///
  /// * [RadarOptionsWithHiddenOptions2] radarOptions:
  ///
  /// * [PostPaymentIntentsIntentRequestReceiptEmail] receiptEmail:
  ///
  /// * [String] returnUrl:
  ///   The URL to redirect your customer back to after they authenticate or cancel their payment on the payment method's app or site. If you'd prefer to redirect to a mobile application, you can alternatively supply an application URI scheme. This parameter is only used for cards and other redirect-based payment methods.
  ///
  /// * [String] setupFutureUsage:
  ///   Indicates that you intend to make future payments with this PaymentIntent's payment method.  If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.  If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.  When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).  If you've already set `setup_future_usage` and you're performing a request using a publishable key, you can only update the value from `on_session` to `off_session`.
  ///
  /// * [PostPaymentIntentsIntentRequestShipping] shipping:
  ///
  /// * [bool] useStripeSdk:
  ///   Set to `true` when confirming server-side and using Stripe.js, iOS, or Android client-side SDKs to handle the next actions.
  Future<Response> postPaymentIntentsIntentConfirmWithHttpInfo(String intent, { String? captureMethod, String? clientSecret, String? confirmationToken, bool? errorOnRequiresAction, List<String>? expand, String? mandate, PostPaymentIntentsIntentConfirmRequestMandateData? mandateData, PostPaymentIntentsIntentConfirmRequestOffSession? offSession, String? paymentMethod, PaymentMethodDataParams? paymentMethodData, PaymentMethodOptionsParam19? paymentMethodOptions, List<String>? paymentMethodTypes, RadarOptionsWithHiddenOptions2? radarOptions, PostPaymentIntentsIntentRequestReceiptEmail? receiptEmail, String? returnUrl, String? setupFutureUsage, PostPaymentIntentsIntentRequestShipping? shipping, bool? useStripeSdk, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payment_intents/{intent}/confirm'
      .replaceAll('{intent}', intent);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (captureMethod != null) {
      formParams[r'capture_method'] = parameterToString(captureMethod);
    }
    if (clientSecret != null) {
      formParams[r'client_secret'] = parameterToString(clientSecret);
    }
    if (confirmationToken != null) {
      formParams[r'confirmation_token'] = parameterToString(confirmationToken);
    }
    if (errorOnRequiresAction != null) {
      formParams[r'error_on_requires_action'] = parameterToString(errorOnRequiresAction);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (mandate != null) {
      formParams[r'mandate'] = parameterToString(mandate);
    }
    if (mandateData != null) {
      formParams[r'mandate_data'] = parameterToString(mandateData);
    }
    if (offSession != null) {
      formParams[r'off_session'] = parameterToString(offSession);
    }
    if (paymentMethod != null) {
      formParams[r'payment_method'] = parameterToString(paymentMethod);
    }
    if (paymentMethodData != null) {
      formParams[r'payment_method_data'] = parameterToString(paymentMethodData);
    }
    if (paymentMethodOptions != null) {
      formParams[r'payment_method_options'] = parameterToString(paymentMethodOptions);
    }
    if (paymentMethodTypes != null) {
      formParams[r'payment_method_types'] = parameterToString(paymentMethodTypes);
    }
    if (radarOptions != null) {
      formParams[r'radar_options'] = parameterToString(radarOptions);
    }
    if (receiptEmail != null) {
      formParams[r'receipt_email'] = parameterToString(receiptEmail);
    }
    if (returnUrl != null) {
      formParams[r'return_url'] = parameterToString(returnUrl);
    }
    if (setupFutureUsage != null) {
      formParams[r'setup_future_usage'] = parameterToString(setupFutureUsage);
    }
    if (shipping != null) {
      formParams[r'shipping'] = parameterToString(shipping);
    }
    if (useStripeSdk != null) {
      formParams[r'use_stripe_sdk'] = parameterToString(useStripeSdk);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Confirm a PaymentIntent
  ///
  /// <p>Confirm that your customer intends to pay with current or provided payment method. Upon confirmation, the PaymentIntent will attempt to initiate a payment.</p>  <p>If the selected payment method requires additional authentication steps, the PaymentIntent will transition to the <code>requires_action</code> status and suggest additional actions via <code>next_action</code>. If payment fails, the PaymentIntent transitions to the <code>requires_payment_method</code> status or the <code>canceled</code> status if the confirmation limit is reached. If payment succeeds, the PaymentIntent will transition to the <code>succeeded</code> status (or <code>requires_capture</code>, if <code>capture_method</code> is set to <code>manual</code>).</p>  <p>If the <code>confirmation_method</code> is <code>automatic</code>, payment may be attempted using our <a href=\"/docs/stripe-js/reference#stripe-handle-card-payment\">client SDKs</a> and the PaymentIntent’s <a href=\"#payment_intent_object-client_secret\">client_secret</a>. After <code>next_action</code>s are handled by the client, no additional confirmation is required to complete the payment.</p>  <p>If the <code>confirmation_method</code> is <code>manual</code>, all payment attempts must be initiated using a secret key.</p>  <p>If any actions are required for the payment, the PaymentIntent will return to the <code>requires_confirmation</code> state after those actions are completed. Your server needs to then explicitly re-confirm the PaymentIntent to initiate the next payment attempt.</p>  <p>There is a variable upper limit on how many times a PaymentIntent can be confirmed. After this limit is reached, any further calls to this endpoint will transition the PaymentIntent to the <code>canceled</code> state.</p>
  ///
  /// Parameters:
  ///
  /// * [String] intent (required):
  ///
  /// * [String] captureMethod:
  ///   Controls when the funds will be captured from the customer's account.
  ///
  /// * [String] clientSecret:
  ///   The client secret of the PaymentIntent.
  ///
  /// * [String] confirmationToken:
  ///   ID of the ConfirmationToken used to confirm this PaymentIntent.  If the provided ConfirmationToken contains properties that are also being provided in this request, such as `payment_method`, then the values in this request will take precedence.
  ///
  /// * [bool] errorOnRequiresAction:
  ///   Set to `true` to fail the payment attempt if the PaymentIntent transitions into `requires_action`. This parameter is intended for simpler integrations that do not handle customer actions, like [saving cards without authentication](https://stripe.com/docs/payments/save-card-without-authentication).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] mandate:
  ///   ID of the mandate that's used for this payment.
  ///
  /// * [PostPaymentIntentsIntentConfirmRequestMandateData] mandateData:
  ///
  /// * [PostPaymentIntentsIntentConfirmRequestOffSession] offSession:
  ///
  /// * [String] paymentMethod:
  ///   ID of the payment method (a PaymentMethod, Card, or [compatible Source](https://stripe.com/docs/payments/payment-methods/transitioning#compatibility) object) to attach to this PaymentIntent. If the payment method is attached to a Customer, it must match the [customer](https://stripe.com/docs/api#create_payment_intent-customer) that is set on this PaymentIntent.
  ///
  /// * [PaymentMethodDataParams] paymentMethodData:
  ///
  /// * [PaymentMethodOptionsParam19] paymentMethodOptions:
  ///
  /// * [List<String>] paymentMethodTypes:
  ///   The list of payment method types (for example, a card) that this PaymentIntent can use. Use `automatic_payment_methods` to manage payment methods from the [Stripe Dashboard](https://dashboard.stripe.com/settings/payment_methods). A list of valid payment method types can be found [here](https://docs.stripe.com/api/payment_methods/object#payment_method_object-type).
  ///
  /// * [RadarOptionsWithHiddenOptions2] radarOptions:
  ///
  /// * [PostPaymentIntentsIntentRequestReceiptEmail] receiptEmail:
  ///
  /// * [String] returnUrl:
  ///   The URL to redirect your customer back to after they authenticate or cancel their payment on the payment method's app or site. If you'd prefer to redirect to a mobile application, you can alternatively supply an application URI scheme. This parameter is only used for cards and other redirect-based payment methods.
  ///
  /// * [String] setupFutureUsage:
  ///   Indicates that you intend to make future payments with this PaymentIntent's payment method.  If you provide a Customer with the PaymentIntent, you can use this parameter to [attach the payment method](/payments/save-during-payment) to the Customer after the PaymentIntent is confirmed and the customer completes any required actions. If you don't provide a Customer, you can still [attach](/api/payment_methods/attach) the payment method to a Customer after the transaction completes.  If the payment method is `card_present` and isn't a digital wallet, Stripe creates and attaches a [generated_card](/api/charges/object#charge_object-payment_method_details-card_present-generated_card) payment method representing the card to the Customer instead.  When processing card payments, Stripe uses `setup_future_usage` to help you comply with regional legislation and network rules, such as [SCA](/strong-customer-authentication).  If you've already set `setup_future_usage` and you're performing a request using a publishable key, you can only update the value from `on_session` to `off_session`.
  ///
  /// * [PostPaymentIntentsIntentRequestShipping] shipping:
  ///
  /// * [bool] useStripeSdk:
  ///   Set to `true` when confirming server-side and using Stripe.js, iOS, or Android client-side SDKs to handle the next actions.
  Future<PaymentIntent?> postPaymentIntentsIntentConfirm(String intent, { String? captureMethod, String? clientSecret, String? confirmationToken, bool? errorOnRequiresAction, List<String>? expand, String? mandate, PostPaymentIntentsIntentConfirmRequestMandateData? mandateData, PostPaymentIntentsIntentConfirmRequestOffSession? offSession, String? paymentMethod, PaymentMethodDataParams? paymentMethodData, PaymentMethodOptionsParam19? paymentMethodOptions, List<String>? paymentMethodTypes, RadarOptionsWithHiddenOptions2? radarOptions, PostPaymentIntentsIntentRequestReceiptEmail? receiptEmail, String? returnUrl, String? setupFutureUsage, PostPaymentIntentsIntentRequestShipping? shipping, bool? useStripeSdk, }) async {
    final response = await postPaymentIntentsIntentConfirmWithHttpInfo(intent,  captureMethod: captureMethod, clientSecret: clientSecret, confirmationToken: confirmationToken, errorOnRequiresAction: errorOnRequiresAction, expand: expand, mandate: mandate, mandateData: mandateData, offSession: offSession, paymentMethod: paymentMethod, paymentMethodData: paymentMethodData, paymentMethodOptions: paymentMethodOptions, paymentMethodTypes: paymentMethodTypes, radarOptions: radarOptions, receiptEmail: receiptEmail, returnUrl: returnUrl, setupFutureUsage: setupFutureUsage, shipping: shipping, useStripeSdk: useStripeSdk, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentIntent',) as PaymentIntent;
    
    }
    return null;
  }

  /// Increment an authorization
  ///
  /// <p>Perform an incremental authorization on an eligible <a href=\"/docs/api/payment_intents/object\">PaymentIntent</a>. To be eligible, the PaymentIntent’s status must be <code>requires_capture</code> and <a href=\"/docs/api/charges/object#charge_object-payment_method_details-card_present-incremental_authorization_supported\">incremental_authorization_supported</a> must be <code>true</code>.</p>  <p>Incremental authorizations attempt to increase the authorized amount on your customer’s card to the new, higher <code>amount</code> provided. Similar to the initial authorization, incremental authorizations can be declined. A single PaymentIntent can call this endpoint multiple times to further increase the authorized amount.</p>  <p>If the incremental authorization succeeds, the PaymentIntent object returns with the updated <a href=\"/docs/api/payment_intents/object#payment_intent_object-amount\">amount</a>. If the incremental authorization fails, a <a href=\"/docs/error-codes#card-declined\">card_declined</a> error returns, and no other fields on the PaymentIntent or Charge update. The PaymentIntent object remains capturable for the previously authorized amount.</p>  <p>Each PaymentIntent can have a maximum of 10 incremental authorization attempts, including declines. After it’s captured, a PaymentIntent can no longer be incremented.</p>  <p>Learn more about <a href=\"/docs/terminal/features/incremental-authorizations\">incremental authorizations</a>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] intent (required):
  ///
  /// * [int] amount (required):
  ///   The updated total amount that you intend to collect from the cardholder. This amount must be greater than the currently authorized amount.
  ///
  /// * [int] applicationFeeAmount:
  ///   The amount of the application fee (if any) that will be requested to be applied to the payment and transferred to the application owner's Stripe account. The amount of the application fee collected will be capped at the total amount captured. For more information, see the PaymentIntents [use case for connected accounts](https://stripe.com/docs/payments/connected-accounts).
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] statementDescriptor:
  ///   Text that appears on the customer's statement as the statement descriptor for a non-card or card charge. This value overrides the account's default statement descriptor. For information about requirements, including the 22-character limit, see [the Statement Descriptor docs](https://docs.stripe.com/get-started/account/statement-descriptors).
  ///
  /// * [TransferDataUpdateParams1] transferData:
  Future<Response> postPaymentIntentsIntentIncrementAuthorizationWithHttpInfo(String intent, int amount, { int? applicationFeeAmount, String? description, List<String>? expand, Map<String, String>? metadata, String? statementDescriptor, TransferDataUpdateParams1? transferData, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payment_intents/{intent}/increment_authorization'
      .replaceAll('{intent}', intent);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (applicationFeeAmount != null) {
      formParams[r'application_fee_amount'] = parameterToString(applicationFeeAmount);
    }
    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (statementDescriptor != null) {
      formParams[r'statement_descriptor'] = parameterToString(statementDescriptor);
    }
    if (transferData != null) {
      formParams[r'transfer_data'] = parameterToString(transferData);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Increment an authorization
  ///
  /// <p>Perform an incremental authorization on an eligible <a href=\"/docs/api/payment_intents/object\">PaymentIntent</a>. To be eligible, the PaymentIntent’s status must be <code>requires_capture</code> and <a href=\"/docs/api/charges/object#charge_object-payment_method_details-card_present-incremental_authorization_supported\">incremental_authorization_supported</a> must be <code>true</code>.</p>  <p>Incremental authorizations attempt to increase the authorized amount on your customer’s card to the new, higher <code>amount</code> provided. Similar to the initial authorization, incremental authorizations can be declined. A single PaymentIntent can call this endpoint multiple times to further increase the authorized amount.</p>  <p>If the incremental authorization succeeds, the PaymentIntent object returns with the updated <a href=\"/docs/api/payment_intents/object#payment_intent_object-amount\">amount</a>. If the incremental authorization fails, a <a href=\"/docs/error-codes#card-declined\">card_declined</a> error returns, and no other fields on the PaymentIntent or Charge update. The PaymentIntent object remains capturable for the previously authorized amount.</p>  <p>Each PaymentIntent can have a maximum of 10 incremental authorization attempts, including declines. After it’s captured, a PaymentIntent can no longer be incremented.</p>  <p>Learn more about <a href=\"/docs/terminal/features/incremental-authorizations\">incremental authorizations</a>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] intent (required):
  ///
  /// * [int] amount (required):
  ///   The updated total amount that you intend to collect from the cardholder. This amount must be greater than the currently authorized amount.
  ///
  /// * [int] applicationFeeAmount:
  ///   The amount of the application fee (if any) that will be requested to be applied to the payment and transferred to the application owner's Stripe account. The amount of the application fee collected will be capped at the total amount captured. For more information, see the PaymentIntents [use case for connected accounts](https://stripe.com/docs/payments/connected-accounts).
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] statementDescriptor:
  ///   Text that appears on the customer's statement as the statement descriptor for a non-card or card charge. This value overrides the account's default statement descriptor. For information about requirements, including the 22-character limit, see [the Statement Descriptor docs](https://docs.stripe.com/get-started/account/statement-descriptors).
  ///
  /// * [TransferDataUpdateParams1] transferData:
  Future<PaymentIntent?> postPaymentIntentsIntentIncrementAuthorization(String intent, int amount, { int? applicationFeeAmount, String? description, List<String>? expand, Map<String, String>? metadata, String? statementDescriptor, TransferDataUpdateParams1? transferData, }) async {
    final response = await postPaymentIntentsIntentIncrementAuthorizationWithHttpInfo(intent, amount,  applicationFeeAmount: applicationFeeAmount, description: description, expand: expand, metadata: metadata, statementDescriptor: statementDescriptor, transferData: transferData, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentIntent',) as PaymentIntent;
    
    }
    return null;
  }

  /// Verify microdeposits on a PaymentIntent
  ///
  /// <p>Verifies microdeposits on a PaymentIntent object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] intent (required):
  ///
  /// * [List<int>] amounts:
  ///   Two positive integers, in *cents*, equal to the values of the microdeposits sent to the bank account.
  ///
  /// * [String] clientSecret:
  ///   The client secret of the PaymentIntent.
  ///
  /// * [String] descriptorCode:
  ///   A six-character code starting with SM present in the microdeposit sent to the bank account.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postPaymentIntentsIntentVerifyMicrodepositsWithHttpInfo(String intent, { List<int>? amounts, String? clientSecret, String? descriptorCode, List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payment_intents/{intent}/verify_microdeposits'
      .replaceAll('{intent}', intent);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amounts != null) {
      formParams[r'amounts'] = parameterToString(amounts);
    }
    if (clientSecret != null) {
      formParams[r'client_secret'] = parameterToString(clientSecret);
    }
    if (descriptorCode != null) {
      formParams[r'descriptor_code'] = parameterToString(descriptorCode);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Verify microdeposits on a PaymentIntent
  ///
  /// <p>Verifies microdeposits on a PaymentIntent object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] intent (required):
  ///
  /// * [List<int>] amounts:
  ///   Two positive integers, in *cents*, equal to the values of the microdeposits sent to the bank account.
  ///
  /// * [String] clientSecret:
  ///   The client secret of the PaymentIntent.
  ///
  /// * [String] descriptorCode:
  ///   A six-character code starting with SM present in the microdeposit sent to the bank account.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<PaymentIntent?> postPaymentIntentsIntentVerifyMicrodeposits(String intent, { List<int>? amounts, String? clientSecret, String? descriptorCode, List<String>? expand, }) async {
    final response = await postPaymentIntentsIntentVerifyMicrodepositsWithHttpInfo(intent,  amounts: amounts, clientSecret: clientSecret, descriptorCode: descriptorCode, expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentIntent',) as PaymentIntent;
    
    }
    return null;
  }

  /// Create a payment link
  ///
  /// <p>Creates a payment link.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [List<LineItemsCreateParams>] lineItems (required):
  ///   The line items representing what is being sold. Each line item represents an item being sold. Up to 20 line items are supported.
  ///
  /// * [AfterCompletionParams] afterCompletion:
  ///
  /// * [bool] allowPromotionCodes:
  ///   Enables user redeemable promotion codes.
  ///
  /// * [int] applicationFeeAmount:
  ///   The amount of the application fee (if any) that will be requested to be applied to the payment and transferred to the application owner's Stripe account. Can only be applied when there are no line items with recurring prices.
  ///
  /// * [num] applicationFeePercent:
  ///   A non-negative decimal between 0 and 100, with at most two decimal places. This represents the percentage of the subscription invoice total that will be transferred to the application owner's Stripe account. There must be at least 1 line item with a recurring price to use this field.
  ///
  /// * [AutomaticTaxParams1] automaticTax:
  ///
  /// * [String] billingAddressCollection:
  ///   Configuration for collecting the customer's billing address. Defaults to `auto`.
  ///
  /// * [ConsentCollectionParams1] consentCollection:
  ///
  /// * [String] currency:
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies) and supported by each line item's price.
  ///
  /// * [List<CustomFieldParam>] customFields:
  ///   Collect additional information from your customer using custom fields. Up to 3 fields are supported.
  ///
  /// * [CustomTextParam] customText:
  ///
  /// * [String] customerCreation:
  ///   Configures whether [checkout sessions](https://stripe.com/docs/api/checkout/sessions) created by this payment link create a [Customer](https://stripe.com/docs/api/customers).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] inactiveMessage:
  ///   The custom message to be displayed to a customer when a payment link is no longer active.
  ///
  /// * [InvoiceCreationCreateParams] invoiceCreation:
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. Metadata associated with this Payment Link will automatically be copied to [checkout sessions](https://stripe.com/docs/api/checkout/sessions) created by this payment link.
  ///
  /// * [String] onBehalfOf:
  ///   The account on behalf of which to charge.
  ///
  /// * [List<OptionalItemParams>] optionalItems:
  ///   A list of optional items the customer can add to their order at checkout. Use this parameter to pass one-time or recurring [Prices](https://stripe.com/docs/api/prices). There is a maximum of 10 optional items allowed on a payment link, and the existing limits on the number of line items allowed on a payment link apply to the combined number of line items and optional items. There is a maximum of 20 combined line items and optional items.
  ///
  /// * [PaymentIntentDataParams1] paymentIntentData:
  ///
  /// * [String] paymentMethodCollection:
  ///   Specify whether Checkout should collect a payment method. When set to `if_required`, Checkout will not collect a payment method when the total due for the session is 0.This may occur if the Checkout Session includes a free trial or a discount.  Can only be set in `subscription` mode. Defaults to `always`.  If you'd like information on how to collect a payment method outside of Checkout, read the guide on [configuring subscriptions with a free trial](https://stripe.com/docs/payments/checkout/free-trials).
  ///
  /// * [List<String>] paymentMethodTypes:
  ///   The list of payment method types that customers can use. If no value is passed, Stripe will dynamically show relevant payment methods from your [payment method settings](https://dashboard.stripe.com/settings/payment_methods) (20+ payment methods [supported](https://stripe.com/docs/payments/payment-methods/integration-options#payment-method-product-support)).
  ///
  /// * [PhoneNumberCollectionParams1] phoneNumberCollection:
  ///
  /// * [RestrictionsParams] restrictions:
  ///
  /// * [ShippingAddressCollectionParams1] shippingAddressCollection:
  ///
  /// * [List<ShippingOptionParams1>] shippingOptions:
  ///   The shipping rate options to apply to [checkout sessions](https://stripe.com/docs/api/checkout/sessions) created by this payment link.
  ///
  /// * [String] submitType:
  ///   Describes the type of transaction being performed in order to customize relevant text on the page, such as the submit button. Changing this value will also affect the hostname in the [url](https://stripe.com/docs/api/payment_links/payment_links/object#url) property (example: `donate.stripe.com`).
  ///
  /// * [SubscriptionDataParams1] subscriptionData:
  ///
  /// * [TaxIdCollectionParams] taxIdCollection:
  ///
  /// * [TransferDataParams1] transferData:
  Future<Response> postPaymentLinksWithHttpInfo(List<LineItemsCreateParams> lineItems, { AfterCompletionParams? afterCompletion, bool? allowPromotionCodes, int? applicationFeeAmount, num? applicationFeePercent, AutomaticTaxParams1? automaticTax, String? billingAddressCollection, ConsentCollectionParams1? consentCollection, String? currency, List<CustomFieldParam>? customFields, CustomTextParam? customText, String? customerCreation, List<String>? expand, String? inactiveMessage, InvoiceCreationCreateParams? invoiceCreation, Map<String, String>? metadata, String? onBehalfOf, List<OptionalItemParams>? optionalItems, PaymentIntentDataParams1? paymentIntentData, String? paymentMethodCollection, List<String>? paymentMethodTypes, PhoneNumberCollectionParams1? phoneNumberCollection, RestrictionsParams? restrictions, ShippingAddressCollectionParams1? shippingAddressCollection, List<ShippingOptionParams1>? shippingOptions, String? submitType, SubscriptionDataParams1? subscriptionData, TaxIdCollectionParams? taxIdCollection, TransferDataParams1? transferData, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payment_links';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (afterCompletion != null) {
      formParams[r'after_completion'] = parameterToString(afterCompletion);
    }
    if (allowPromotionCodes != null) {
      formParams[r'allow_promotion_codes'] = parameterToString(allowPromotionCodes);
    }
    if (applicationFeeAmount != null) {
      formParams[r'application_fee_amount'] = parameterToString(applicationFeeAmount);
    }
    if (applicationFeePercent != null) {
      formParams[r'application_fee_percent'] = parameterToString(applicationFeePercent);
    }
    if (automaticTax != null) {
      formParams[r'automatic_tax'] = parameterToString(automaticTax);
    }
    if (billingAddressCollection != null) {
      formParams[r'billing_address_collection'] = parameterToString(billingAddressCollection);
    }
    if (consentCollection != null) {
      formParams[r'consent_collection'] = parameterToString(consentCollection);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (customFields != null) {
      formParams[r'custom_fields'] = parameterToString(customFields);
    }
    if (customText != null) {
      formParams[r'custom_text'] = parameterToString(customText);
    }
    if (customerCreation != null) {
      formParams[r'customer_creation'] = parameterToString(customerCreation);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (inactiveMessage != null) {
      formParams[r'inactive_message'] = parameterToString(inactiveMessage);
    }
    if (invoiceCreation != null) {
      formParams[r'invoice_creation'] = parameterToString(invoiceCreation);
    }
    if (lineItems != null) {
      formParams[r'line_items'] = parameterToString(lineItems);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (onBehalfOf != null) {
      formParams[r'on_behalf_of'] = parameterToString(onBehalfOf);
    }
    if (optionalItems != null) {
      formParams[r'optional_items'] = parameterToString(optionalItems);
    }
    if (paymentIntentData != null) {
      formParams[r'payment_intent_data'] = parameterToString(paymentIntentData);
    }
    if (paymentMethodCollection != null) {
      formParams[r'payment_method_collection'] = parameterToString(paymentMethodCollection);
    }
    if (paymentMethodTypes != null) {
      formParams[r'payment_method_types'] = parameterToString(paymentMethodTypes);
    }
    if (phoneNumberCollection != null) {
      formParams[r'phone_number_collection'] = parameterToString(phoneNumberCollection);
    }
    if (restrictions != null) {
      formParams[r'restrictions'] = parameterToString(restrictions);
    }
    if (shippingAddressCollection != null) {
      formParams[r'shipping_address_collection'] = parameterToString(shippingAddressCollection);
    }
    if (shippingOptions != null) {
      formParams[r'shipping_options'] = parameterToString(shippingOptions);
    }
    if (submitType != null) {
      formParams[r'submit_type'] = parameterToString(submitType);
    }
    if (subscriptionData != null) {
      formParams[r'subscription_data'] = parameterToString(subscriptionData);
    }
    if (taxIdCollection != null) {
      formParams[r'tax_id_collection'] = parameterToString(taxIdCollection);
    }
    if (transferData != null) {
      formParams[r'transfer_data'] = parameterToString(transferData);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a payment link
  ///
  /// <p>Creates a payment link.</p>
  ///
  /// Parameters:
  ///
  /// * [List<LineItemsCreateParams>] lineItems (required):
  ///   The line items representing what is being sold. Each line item represents an item being sold. Up to 20 line items are supported.
  ///
  /// * [AfterCompletionParams] afterCompletion:
  ///
  /// * [bool] allowPromotionCodes:
  ///   Enables user redeemable promotion codes.
  ///
  /// * [int] applicationFeeAmount:
  ///   The amount of the application fee (if any) that will be requested to be applied to the payment and transferred to the application owner's Stripe account. Can only be applied when there are no line items with recurring prices.
  ///
  /// * [num] applicationFeePercent:
  ///   A non-negative decimal between 0 and 100, with at most two decimal places. This represents the percentage of the subscription invoice total that will be transferred to the application owner's Stripe account. There must be at least 1 line item with a recurring price to use this field.
  ///
  /// * [AutomaticTaxParams1] automaticTax:
  ///
  /// * [String] billingAddressCollection:
  ///   Configuration for collecting the customer's billing address. Defaults to `auto`.
  ///
  /// * [ConsentCollectionParams1] consentCollection:
  ///
  /// * [String] currency:
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies) and supported by each line item's price.
  ///
  /// * [List<CustomFieldParam>] customFields:
  ///   Collect additional information from your customer using custom fields. Up to 3 fields are supported.
  ///
  /// * [CustomTextParam] customText:
  ///
  /// * [String] customerCreation:
  ///   Configures whether [checkout sessions](https://stripe.com/docs/api/checkout/sessions) created by this payment link create a [Customer](https://stripe.com/docs/api/customers).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] inactiveMessage:
  ///   The custom message to be displayed to a customer when a payment link is no longer active.
  ///
  /// * [InvoiceCreationCreateParams] invoiceCreation:
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. Metadata associated with this Payment Link will automatically be copied to [checkout sessions](https://stripe.com/docs/api/checkout/sessions) created by this payment link.
  ///
  /// * [String] onBehalfOf:
  ///   The account on behalf of which to charge.
  ///
  /// * [List<OptionalItemParams>] optionalItems:
  ///   A list of optional items the customer can add to their order at checkout. Use this parameter to pass one-time or recurring [Prices](https://stripe.com/docs/api/prices). There is a maximum of 10 optional items allowed on a payment link, and the existing limits on the number of line items allowed on a payment link apply to the combined number of line items and optional items. There is a maximum of 20 combined line items and optional items.
  ///
  /// * [PaymentIntentDataParams1] paymentIntentData:
  ///
  /// * [String] paymentMethodCollection:
  ///   Specify whether Checkout should collect a payment method. When set to `if_required`, Checkout will not collect a payment method when the total due for the session is 0.This may occur if the Checkout Session includes a free trial or a discount.  Can only be set in `subscription` mode. Defaults to `always`.  If you'd like information on how to collect a payment method outside of Checkout, read the guide on [configuring subscriptions with a free trial](https://stripe.com/docs/payments/checkout/free-trials).
  ///
  /// * [List<String>] paymentMethodTypes:
  ///   The list of payment method types that customers can use. If no value is passed, Stripe will dynamically show relevant payment methods from your [payment method settings](https://dashboard.stripe.com/settings/payment_methods) (20+ payment methods [supported](https://stripe.com/docs/payments/payment-methods/integration-options#payment-method-product-support)).
  ///
  /// * [PhoneNumberCollectionParams1] phoneNumberCollection:
  ///
  /// * [RestrictionsParams] restrictions:
  ///
  /// * [ShippingAddressCollectionParams1] shippingAddressCollection:
  ///
  /// * [List<ShippingOptionParams1>] shippingOptions:
  ///   The shipping rate options to apply to [checkout sessions](https://stripe.com/docs/api/checkout/sessions) created by this payment link.
  ///
  /// * [String] submitType:
  ///   Describes the type of transaction being performed in order to customize relevant text on the page, such as the submit button. Changing this value will also affect the hostname in the [url](https://stripe.com/docs/api/payment_links/payment_links/object#url) property (example: `donate.stripe.com`).
  ///
  /// * [SubscriptionDataParams1] subscriptionData:
  ///
  /// * [TaxIdCollectionParams] taxIdCollection:
  ///
  /// * [TransferDataParams1] transferData:
  Future<PaymentLink?> postPaymentLinks(List<LineItemsCreateParams> lineItems, { AfterCompletionParams? afterCompletion, bool? allowPromotionCodes, int? applicationFeeAmount, num? applicationFeePercent, AutomaticTaxParams1? automaticTax, String? billingAddressCollection, ConsentCollectionParams1? consentCollection, String? currency, List<CustomFieldParam>? customFields, CustomTextParam? customText, String? customerCreation, List<String>? expand, String? inactiveMessage, InvoiceCreationCreateParams? invoiceCreation, Map<String, String>? metadata, String? onBehalfOf, List<OptionalItemParams>? optionalItems, PaymentIntentDataParams1? paymentIntentData, String? paymentMethodCollection, List<String>? paymentMethodTypes, PhoneNumberCollectionParams1? phoneNumberCollection, RestrictionsParams? restrictions, ShippingAddressCollectionParams1? shippingAddressCollection, List<ShippingOptionParams1>? shippingOptions, String? submitType, SubscriptionDataParams1? subscriptionData, TaxIdCollectionParams? taxIdCollection, TransferDataParams1? transferData, }) async {
    final response = await postPaymentLinksWithHttpInfo(lineItems,  afterCompletion: afterCompletion, allowPromotionCodes: allowPromotionCodes, applicationFeeAmount: applicationFeeAmount, applicationFeePercent: applicationFeePercent, automaticTax: automaticTax, billingAddressCollection: billingAddressCollection, consentCollection: consentCollection, currency: currency, customFields: customFields, customText: customText, customerCreation: customerCreation, expand: expand, inactiveMessage: inactiveMessage, invoiceCreation: invoiceCreation, metadata: metadata, onBehalfOf: onBehalfOf, optionalItems: optionalItems, paymentIntentData: paymentIntentData, paymentMethodCollection: paymentMethodCollection, paymentMethodTypes: paymentMethodTypes, phoneNumberCollection: phoneNumberCollection, restrictions: restrictions, shippingAddressCollection: shippingAddressCollection, shippingOptions: shippingOptions, submitType: submitType, subscriptionData: subscriptionData, taxIdCollection: taxIdCollection, transferData: transferData, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentLink',) as PaymentLink;
    
    }
    return null;
  }

  /// Update a payment link
  ///
  /// <p>Updates a payment link.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] paymentLink (required):
  ///
  /// * [bool] active:
  ///   Whether the payment link's `url` is active. If `false`, customers visiting the URL will be shown a page saying that the link has been deactivated.
  ///
  /// * [AfterCompletionParams] afterCompletion:
  ///
  /// * [bool] allowPromotionCodes:
  ///   Enables user redeemable promotion codes.
  ///
  /// * [AutomaticTaxParams1] automaticTax:
  ///
  /// * [String] billingAddressCollection:
  ///   Configuration for collecting the customer's billing address. Defaults to `auto`.
  ///
  /// * [PostPaymentLinksPaymentLinkRequestCustomFields] customFields:
  ///
  /// * [CustomTextParam] customText:
  ///
  /// * [String] customerCreation:
  ///   Configures whether [checkout sessions](https://stripe.com/docs/api/checkout/sessions) created by this payment link create a [Customer](https://stripe.com/docs/api/customers).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostPaymentLinksPaymentLinkRequestInactiveMessage] inactiveMessage:
  ///
  /// * [InvoiceCreationUpdateParams] invoiceCreation:
  ///
  /// * [List<LineItemsUpdateParams>] lineItems:
  ///   The line items representing what is being sold. Each line item represents an item being sold. Up to 20 line items are supported.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. Metadata associated with this Payment Link will automatically be copied to [checkout sessions](https://stripe.com/docs/api/checkout/sessions) created by this payment link.
  ///
  /// * [PaymentIntentDataUpdateParams] paymentIntentData:
  ///
  /// * [String] paymentMethodCollection:
  ///   Specify whether Checkout should collect a payment method. When set to `if_required`, Checkout will not collect a payment method when the total due for the session is 0.This may occur if the Checkout Session includes a free trial or a discount.  Can only be set in `subscription` mode. Defaults to `always`.  If you'd like information on how to collect a payment method outside of Checkout, read the guide on [configuring subscriptions with a free trial](https://stripe.com/docs/payments/checkout/free-trials).
  ///
  /// * [PostPaymentLinksPaymentLinkRequestPaymentMethodTypes] paymentMethodTypes:
  ///
  /// * [PhoneNumberCollectionParams1] phoneNumberCollection:
  ///
  /// * [PostPaymentLinksPaymentLinkRequestRestrictions] restrictions:
  ///
  /// * [PostPaymentLinksPaymentLinkRequestShippingAddressCollection] shippingAddressCollection:
  ///
  /// * [String] submitType:
  ///   Describes the type of transaction being performed in order to customize relevant text on the page, such as the submit button. Changing this value will also affect the hostname in the [url](https://stripe.com/docs/api/payment_links/payment_links/object#url) property (example: `donate.stripe.com`).
  ///
  /// * [SubscriptionDataUpdateParams] subscriptionData:
  ///
  /// * [TaxIdCollectionParams] taxIdCollection:
  Future<Response> postPaymentLinksPaymentLinkWithHttpInfo(String paymentLink, { bool? active, AfterCompletionParams? afterCompletion, bool? allowPromotionCodes, AutomaticTaxParams1? automaticTax, String? billingAddressCollection, PostPaymentLinksPaymentLinkRequestCustomFields? customFields, CustomTextParam? customText, String? customerCreation, List<String>? expand, PostPaymentLinksPaymentLinkRequestInactiveMessage? inactiveMessage, InvoiceCreationUpdateParams? invoiceCreation, List<LineItemsUpdateParams>? lineItems, Map<String, String>? metadata, PaymentIntentDataUpdateParams? paymentIntentData, String? paymentMethodCollection, PostPaymentLinksPaymentLinkRequestPaymentMethodTypes? paymentMethodTypes, PhoneNumberCollectionParams1? phoneNumberCollection, PostPaymentLinksPaymentLinkRequestRestrictions? restrictions, PostPaymentLinksPaymentLinkRequestShippingAddressCollection? shippingAddressCollection, String? submitType, SubscriptionDataUpdateParams? subscriptionData, TaxIdCollectionParams? taxIdCollection, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payment_links/{payment_link}'
      .replaceAll('{payment_link}', paymentLink);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (active != null) {
      formParams[r'active'] = parameterToString(active);
    }
    if (afterCompletion != null) {
      formParams[r'after_completion'] = parameterToString(afterCompletion);
    }
    if (allowPromotionCodes != null) {
      formParams[r'allow_promotion_codes'] = parameterToString(allowPromotionCodes);
    }
    if (automaticTax != null) {
      formParams[r'automatic_tax'] = parameterToString(automaticTax);
    }
    if (billingAddressCollection != null) {
      formParams[r'billing_address_collection'] = parameterToString(billingAddressCollection);
    }
    if (customFields != null) {
      formParams[r'custom_fields'] = parameterToString(customFields);
    }
    if (customText != null) {
      formParams[r'custom_text'] = parameterToString(customText);
    }
    if (customerCreation != null) {
      formParams[r'customer_creation'] = parameterToString(customerCreation);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (inactiveMessage != null) {
      formParams[r'inactive_message'] = parameterToString(inactiveMessage);
    }
    if (invoiceCreation != null) {
      formParams[r'invoice_creation'] = parameterToString(invoiceCreation);
    }
    if (lineItems != null) {
      formParams[r'line_items'] = parameterToString(lineItems);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (paymentIntentData != null) {
      formParams[r'payment_intent_data'] = parameterToString(paymentIntentData);
    }
    if (paymentMethodCollection != null) {
      formParams[r'payment_method_collection'] = parameterToString(paymentMethodCollection);
    }
    if (paymentMethodTypes != null) {
      formParams[r'payment_method_types'] = parameterToString(paymentMethodTypes);
    }
    if (phoneNumberCollection != null) {
      formParams[r'phone_number_collection'] = parameterToString(phoneNumberCollection);
    }
    if (restrictions != null) {
      formParams[r'restrictions'] = parameterToString(restrictions);
    }
    if (shippingAddressCollection != null) {
      formParams[r'shipping_address_collection'] = parameterToString(shippingAddressCollection);
    }
    if (submitType != null) {
      formParams[r'submit_type'] = parameterToString(submitType);
    }
    if (subscriptionData != null) {
      formParams[r'subscription_data'] = parameterToString(subscriptionData);
    }
    if (taxIdCollection != null) {
      formParams[r'tax_id_collection'] = parameterToString(taxIdCollection);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a payment link
  ///
  /// <p>Updates a payment link.</p>
  ///
  /// Parameters:
  ///
  /// * [String] paymentLink (required):
  ///
  /// * [bool] active:
  ///   Whether the payment link's `url` is active. If `false`, customers visiting the URL will be shown a page saying that the link has been deactivated.
  ///
  /// * [AfterCompletionParams] afterCompletion:
  ///
  /// * [bool] allowPromotionCodes:
  ///   Enables user redeemable promotion codes.
  ///
  /// * [AutomaticTaxParams1] automaticTax:
  ///
  /// * [String] billingAddressCollection:
  ///   Configuration for collecting the customer's billing address. Defaults to `auto`.
  ///
  /// * [PostPaymentLinksPaymentLinkRequestCustomFields] customFields:
  ///
  /// * [CustomTextParam] customText:
  ///
  /// * [String] customerCreation:
  ///   Configures whether [checkout sessions](https://stripe.com/docs/api/checkout/sessions) created by this payment link create a [Customer](https://stripe.com/docs/api/customers).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostPaymentLinksPaymentLinkRequestInactiveMessage] inactiveMessage:
  ///
  /// * [InvoiceCreationUpdateParams] invoiceCreation:
  ///
  /// * [List<LineItemsUpdateParams>] lineItems:
  ///   The line items representing what is being sold. Each line item represents an item being sold. Up to 20 line items are supported.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`. Metadata associated with this Payment Link will automatically be copied to [checkout sessions](https://stripe.com/docs/api/checkout/sessions) created by this payment link.
  ///
  /// * [PaymentIntentDataUpdateParams] paymentIntentData:
  ///
  /// * [String] paymentMethodCollection:
  ///   Specify whether Checkout should collect a payment method. When set to `if_required`, Checkout will not collect a payment method when the total due for the session is 0.This may occur if the Checkout Session includes a free trial or a discount.  Can only be set in `subscription` mode. Defaults to `always`.  If you'd like information on how to collect a payment method outside of Checkout, read the guide on [configuring subscriptions with a free trial](https://stripe.com/docs/payments/checkout/free-trials).
  ///
  /// * [PostPaymentLinksPaymentLinkRequestPaymentMethodTypes] paymentMethodTypes:
  ///
  /// * [PhoneNumberCollectionParams1] phoneNumberCollection:
  ///
  /// * [PostPaymentLinksPaymentLinkRequestRestrictions] restrictions:
  ///
  /// * [PostPaymentLinksPaymentLinkRequestShippingAddressCollection] shippingAddressCollection:
  ///
  /// * [String] submitType:
  ///   Describes the type of transaction being performed in order to customize relevant text on the page, such as the submit button. Changing this value will also affect the hostname in the [url](https://stripe.com/docs/api/payment_links/payment_links/object#url) property (example: `donate.stripe.com`).
  ///
  /// * [SubscriptionDataUpdateParams] subscriptionData:
  ///
  /// * [TaxIdCollectionParams] taxIdCollection:
  Future<PaymentLink?> postPaymentLinksPaymentLink(String paymentLink, { bool? active, AfterCompletionParams? afterCompletion, bool? allowPromotionCodes, AutomaticTaxParams1? automaticTax, String? billingAddressCollection, PostPaymentLinksPaymentLinkRequestCustomFields? customFields, CustomTextParam? customText, String? customerCreation, List<String>? expand, PostPaymentLinksPaymentLinkRequestInactiveMessage? inactiveMessage, InvoiceCreationUpdateParams? invoiceCreation, List<LineItemsUpdateParams>? lineItems, Map<String, String>? metadata, PaymentIntentDataUpdateParams? paymentIntentData, String? paymentMethodCollection, PostPaymentLinksPaymentLinkRequestPaymentMethodTypes? paymentMethodTypes, PhoneNumberCollectionParams1? phoneNumberCollection, PostPaymentLinksPaymentLinkRequestRestrictions? restrictions, PostPaymentLinksPaymentLinkRequestShippingAddressCollection? shippingAddressCollection, String? submitType, SubscriptionDataUpdateParams? subscriptionData, TaxIdCollectionParams? taxIdCollection, }) async {
    final response = await postPaymentLinksPaymentLinkWithHttpInfo(paymentLink,  active: active, afterCompletion: afterCompletion, allowPromotionCodes: allowPromotionCodes, automaticTax: automaticTax, billingAddressCollection: billingAddressCollection, customFields: customFields, customText: customText, customerCreation: customerCreation, expand: expand, inactiveMessage: inactiveMessage, invoiceCreation: invoiceCreation, lineItems: lineItems, metadata: metadata, paymentIntentData: paymentIntentData, paymentMethodCollection: paymentMethodCollection, paymentMethodTypes: paymentMethodTypes, phoneNumberCollection: phoneNumberCollection, restrictions: restrictions, shippingAddressCollection: shippingAddressCollection, submitType: submitType, subscriptionData: subscriptionData, taxIdCollection: taxIdCollection, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentLink',) as PaymentLink;
    
    }
    return null;
  }

  /// Create a payment method configuration
  ///
  /// <p>Creates a payment method configuration</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [PaymentMethodParam2] acssDebit:
  ///
  /// * [PaymentMethodParam3] affirm:
  ///
  /// * [PaymentMethodParam4] afterpayClearpay:
  ///
  /// * [PaymentMethodParam5] alipay:
  ///
  /// * [PaymentMethodParam6] alma:
  ///
  /// * [PaymentMethodParam7] amazonPay:
  ///
  /// * [PaymentMethodParam8] applePay:
  ///
  /// * [PaymentMethodParam9] applePayLater:
  ///
  /// * [PaymentMethodParam10] auBecsDebit:
  ///
  /// * [PaymentMethodParam11] bacsDebit:
  ///
  /// * [PaymentMethodParam12] bancontact:
  ///
  /// * [PaymentMethodParam13] billie:
  ///
  /// * [PaymentMethodParam14] blik:
  ///
  /// * [PaymentMethodParam15] boleto:
  ///
  /// * [PaymentMethodParam16] card:
  ///
  /// * [PaymentMethodParam17] cartesBancaires:
  ///
  /// * [PaymentMethodParam18] cashapp:
  ///
  /// * [PaymentMethodParam19] customerBalance:
  ///
  /// * [PaymentMethodParam20] eps:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PaymentMethodParam21] fpx:
  ///
  /// * [PaymentMethodParam22] giropay:
  ///
  /// * [PaymentMethodParam23] googlePay:
  ///
  /// * [PaymentMethodParam24] grabpay:
  ///
  /// * [PaymentMethodParam25] ideal:
  ///
  /// * [PaymentMethodParam26] jcb:
  ///
  /// * [PaymentMethodParam27] kakaoPay:
  ///
  /// * [PaymentMethodParam28] klarna:
  ///
  /// * [PaymentMethodParam29] konbini:
  ///
  /// * [PaymentMethodParam30] krCard:
  ///
  /// * [PaymentMethodParam31] link:
  ///
  /// * [PaymentMethodParam32] mobilepay:
  ///
  /// * [PaymentMethodParam33] multibanco:
  ///
  /// * [String] name:
  ///   Configuration name.
  ///
  /// * [PaymentMethodParam34] naverPay:
  ///
  /// * [PaymentMethodParam35] nzBankAccount:
  ///
  /// * [PaymentMethodParam36] oxxo:
  ///
  /// * [PaymentMethodParam37] p24:
  ///
  /// * [String] parent:
  ///   Configuration's parent configuration. Specify to create a child configuration.
  ///
  /// * [PaymentMethodParam38] payByBank:
  ///
  /// * [PaymentMethodParam39] payco:
  ///
  /// * [PaymentMethodParam40] paynow:
  ///
  /// * [PaymentMethodParam41] paypal:
  ///
  /// * [PaymentMethodParam42] pix:
  ///
  /// * [PaymentMethodParam43] promptpay:
  ///
  /// * [PaymentMethodParam44] revolutPay:
  ///
  /// * [PaymentMethodParam45] samsungPay:
  ///
  /// * [PaymentMethodParam46] satispay:
  ///
  /// * [PaymentMethodParam47] sepaDebit:
  ///
  /// * [PaymentMethodParam48] sofort:
  ///
  /// * [PaymentMethodParam49] swish:
  ///
  /// * [PaymentMethodParam50] twint:
  ///
  /// * [PaymentMethodParam51] usBankAccount:
  ///
  /// * [PaymentMethodParam52] wechatPay:
  ///
  /// * [PaymentMethodParam53] zip:
  Future<Response> postPaymentMethodConfigurationsWithHttpInfo({ PaymentMethodParam2? acssDebit, PaymentMethodParam3? affirm, PaymentMethodParam4? afterpayClearpay, PaymentMethodParam5? alipay, PaymentMethodParam6? alma, PaymentMethodParam7? amazonPay, PaymentMethodParam8? applePay, PaymentMethodParam9? applePayLater, PaymentMethodParam10? auBecsDebit, PaymentMethodParam11? bacsDebit, PaymentMethodParam12? bancontact, PaymentMethodParam13? billie, PaymentMethodParam14? blik, PaymentMethodParam15? boleto, PaymentMethodParam16? card, PaymentMethodParam17? cartesBancaires, PaymentMethodParam18? cashapp, PaymentMethodParam19? customerBalance, PaymentMethodParam20? eps, List<String>? expand, PaymentMethodParam21? fpx, PaymentMethodParam22? giropay, PaymentMethodParam23? googlePay, PaymentMethodParam24? grabpay, PaymentMethodParam25? ideal, PaymentMethodParam26? jcb, PaymentMethodParam27? kakaoPay, PaymentMethodParam28? klarna, PaymentMethodParam29? konbini, PaymentMethodParam30? krCard, PaymentMethodParam31? link, PaymentMethodParam32? mobilepay, PaymentMethodParam33? multibanco, String? name, PaymentMethodParam34? naverPay, PaymentMethodParam35? nzBankAccount, PaymentMethodParam36? oxxo, PaymentMethodParam37? p24, String? parent, PaymentMethodParam38? payByBank, PaymentMethodParam39? payco, PaymentMethodParam40? paynow, PaymentMethodParam41? paypal, PaymentMethodParam42? pix, PaymentMethodParam43? promptpay, PaymentMethodParam44? revolutPay, PaymentMethodParam45? samsungPay, PaymentMethodParam46? satispay, PaymentMethodParam47? sepaDebit, PaymentMethodParam48? sofort, PaymentMethodParam49? swish, PaymentMethodParam50? twint, PaymentMethodParam51? usBankAccount, PaymentMethodParam52? wechatPay, PaymentMethodParam53? zip, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payment_method_configurations';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (acssDebit != null) {
      formParams[r'acss_debit'] = parameterToString(acssDebit);
    }
    if (affirm != null) {
      formParams[r'affirm'] = parameterToString(affirm);
    }
    if (afterpayClearpay != null) {
      formParams[r'afterpay_clearpay'] = parameterToString(afterpayClearpay);
    }
    if (alipay != null) {
      formParams[r'alipay'] = parameterToString(alipay);
    }
    if (alma != null) {
      formParams[r'alma'] = parameterToString(alma);
    }
    if (amazonPay != null) {
      formParams[r'amazon_pay'] = parameterToString(amazonPay);
    }
    if (applePay != null) {
      formParams[r'apple_pay'] = parameterToString(applePay);
    }
    if (applePayLater != null) {
      formParams[r'apple_pay_later'] = parameterToString(applePayLater);
    }
    if (auBecsDebit != null) {
      formParams[r'au_becs_debit'] = parameterToString(auBecsDebit);
    }
    if (bacsDebit != null) {
      formParams[r'bacs_debit'] = parameterToString(bacsDebit);
    }
    if (bancontact != null) {
      formParams[r'bancontact'] = parameterToString(bancontact);
    }
    if (billie != null) {
      formParams[r'billie'] = parameterToString(billie);
    }
    if (blik != null) {
      formParams[r'blik'] = parameterToString(blik);
    }
    if (boleto != null) {
      formParams[r'boleto'] = parameterToString(boleto);
    }
    if (card != null) {
      formParams[r'card'] = parameterToString(card);
    }
    if (cartesBancaires != null) {
      formParams[r'cartes_bancaires'] = parameterToString(cartesBancaires);
    }
    if (cashapp != null) {
      formParams[r'cashapp'] = parameterToString(cashapp);
    }
    if (customerBalance != null) {
      formParams[r'customer_balance'] = parameterToString(customerBalance);
    }
    if (eps != null) {
      formParams[r'eps'] = parameterToString(eps);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (fpx != null) {
      formParams[r'fpx'] = parameterToString(fpx);
    }
    if (giropay != null) {
      formParams[r'giropay'] = parameterToString(giropay);
    }
    if (googlePay != null) {
      formParams[r'google_pay'] = parameterToString(googlePay);
    }
    if (grabpay != null) {
      formParams[r'grabpay'] = parameterToString(grabpay);
    }
    if (ideal != null) {
      formParams[r'ideal'] = parameterToString(ideal);
    }
    if (jcb != null) {
      formParams[r'jcb'] = parameterToString(jcb);
    }
    if (kakaoPay != null) {
      formParams[r'kakao_pay'] = parameterToString(kakaoPay);
    }
    if (klarna != null) {
      formParams[r'klarna'] = parameterToString(klarna);
    }
    if (konbini != null) {
      formParams[r'konbini'] = parameterToString(konbini);
    }
    if (krCard != null) {
      formParams[r'kr_card'] = parameterToString(krCard);
    }
    if (link != null) {
      formParams[r'link'] = parameterToString(link);
    }
    if (mobilepay != null) {
      formParams[r'mobilepay'] = parameterToString(mobilepay);
    }
    if (multibanco != null) {
      formParams[r'multibanco'] = parameterToString(multibanco);
    }
    if (name != null) {
      formParams[r'name'] = parameterToString(name);
    }
    if (naverPay != null) {
      formParams[r'naver_pay'] = parameterToString(naverPay);
    }
    if (nzBankAccount != null) {
      formParams[r'nz_bank_account'] = parameterToString(nzBankAccount);
    }
    if (oxxo != null) {
      formParams[r'oxxo'] = parameterToString(oxxo);
    }
    if (p24 != null) {
      formParams[r'p24'] = parameterToString(p24);
    }
    if (parent != null) {
      formParams[r'parent'] = parameterToString(parent);
    }
    if (payByBank != null) {
      formParams[r'pay_by_bank'] = parameterToString(payByBank);
    }
    if (payco != null) {
      formParams[r'payco'] = parameterToString(payco);
    }
    if (paynow != null) {
      formParams[r'paynow'] = parameterToString(paynow);
    }
    if (paypal != null) {
      formParams[r'paypal'] = parameterToString(paypal);
    }
    if (pix != null) {
      formParams[r'pix'] = parameterToString(pix);
    }
    if (promptpay != null) {
      formParams[r'promptpay'] = parameterToString(promptpay);
    }
    if (revolutPay != null) {
      formParams[r'revolut_pay'] = parameterToString(revolutPay);
    }
    if (samsungPay != null) {
      formParams[r'samsung_pay'] = parameterToString(samsungPay);
    }
    if (satispay != null) {
      formParams[r'satispay'] = parameterToString(satispay);
    }
    if (sepaDebit != null) {
      formParams[r'sepa_debit'] = parameterToString(sepaDebit);
    }
    if (sofort != null) {
      formParams[r'sofort'] = parameterToString(sofort);
    }
    if (swish != null) {
      formParams[r'swish'] = parameterToString(swish);
    }
    if (twint != null) {
      formParams[r'twint'] = parameterToString(twint);
    }
    if (usBankAccount != null) {
      formParams[r'us_bank_account'] = parameterToString(usBankAccount);
    }
    if (wechatPay != null) {
      formParams[r'wechat_pay'] = parameterToString(wechatPay);
    }
    if (zip != null) {
      formParams[r'zip'] = parameterToString(zip);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a payment method configuration
  ///
  /// <p>Creates a payment method configuration</p>
  ///
  /// Parameters:
  ///
  /// * [PaymentMethodParam2] acssDebit:
  ///
  /// * [PaymentMethodParam3] affirm:
  ///
  /// * [PaymentMethodParam4] afterpayClearpay:
  ///
  /// * [PaymentMethodParam5] alipay:
  ///
  /// * [PaymentMethodParam6] alma:
  ///
  /// * [PaymentMethodParam7] amazonPay:
  ///
  /// * [PaymentMethodParam8] applePay:
  ///
  /// * [PaymentMethodParam9] applePayLater:
  ///
  /// * [PaymentMethodParam10] auBecsDebit:
  ///
  /// * [PaymentMethodParam11] bacsDebit:
  ///
  /// * [PaymentMethodParam12] bancontact:
  ///
  /// * [PaymentMethodParam13] billie:
  ///
  /// * [PaymentMethodParam14] blik:
  ///
  /// * [PaymentMethodParam15] boleto:
  ///
  /// * [PaymentMethodParam16] card:
  ///
  /// * [PaymentMethodParam17] cartesBancaires:
  ///
  /// * [PaymentMethodParam18] cashapp:
  ///
  /// * [PaymentMethodParam19] customerBalance:
  ///
  /// * [PaymentMethodParam20] eps:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PaymentMethodParam21] fpx:
  ///
  /// * [PaymentMethodParam22] giropay:
  ///
  /// * [PaymentMethodParam23] googlePay:
  ///
  /// * [PaymentMethodParam24] grabpay:
  ///
  /// * [PaymentMethodParam25] ideal:
  ///
  /// * [PaymentMethodParam26] jcb:
  ///
  /// * [PaymentMethodParam27] kakaoPay:
  ///
  /// * [PaymentMethodParam28] klarna:
  ///
  /// * [PaymentMethodParam29] konbini:
  ///
  /// * [PaymentMethodParam30] krCard:
  ///
  /// * [PaymentMethodParam31] link:
  ///
  /// * [PaymentMethodParam32] mobilepay:
  ///
  /// * [PaymentMethodParam33] multibanco:
  ///
  /// * [String] name:
  ///   Configuration name.
  ///
  /// * [PaymentMethodParam34] naverPay:
  ///
  /// * [PaymentMethodParam35] nzBankAccount:
  ///
  /// * [PaymentMethodParam36] oxxo:
  ///
  /// * [PaymentMethodParam37] p24:
  ///
  /// * [String] parent:
  ///   Configuration's parent configuration. Specify to create a child configuration.
  ///
  /// * [PaymentMethodParam38] payByBank:
  ///
  /// * [PaymentMethodParam39] payco:
  ///
  /// * [PaymentMethodParam40] paynow:
  ///
  /// * [PaymentMethodParam41] paypal:
  ///
  /// * [PaymentMethodParam42] pix:
  ///
  /// * [PaymentMethodParam43] promptpay:
  ///
  /// * [PaymentMethodParam44] revolutPay:
  ///
  /// * [PaymentMethodParam45] samsungPay:
  ///
  /// * [PaymentMethodParam46] satispay:
  ///
  /// * [PaymentMethodParam47] sepaDebit:
  ///
  /// * [PaymentMethodParam48] sofort:
  ///
  /// * [PaymentMethodParam49] swish:
  ///
  /// * [PaymentMethodParam50] twint:
  ///
  /// * [PaymentMethodParam51] usBankAccount:
  ///
  /// * [PaymentMethodParam52] wechatPay:
  ///
  /// * [PaymentMethodParam53] zip:
  Future<PaymentMethodConfiguration?> postPaymentMethodConfigurations({ PaymentMethodParam2? acssDebit, PaymentMethodParam3? affirm, PaymentMethodParam4? afterpayClearpay, PaymentMethodParam5? alipay, PaymentMethodParam6? alma, PaymentMethodParam7? amazonPay, PaymentMethodParam8? applePay, PaymentMethodParam9? applePayLater, PaymentMethodParam10? auBecsDebit, PaymentMethodParam11? bacsDebit, PaymentMethodParam12? bancontact, PaymentMethodParam13? billie, PaymentMethodParam14? blik, PaymentMethodParam15? boleto, PaymentMethodParam16? card, PaymentMethodParam17? cartesBancaires, PaymentMethodParam18? cashapp, PaymentMethodParam19? customerBalance, PaymentMethodParam20? eps, List<String>? expand, PaymentMethodParam21? fpx, PaymentMethodParam22? giropay, PaymentMethodParam23? googlePay, PaymentMethodParam24? grabpay, PaymentMethodParam25? ideal, PaymentMethodParam26? jcb, PaymentMethodParam27? kakaoPay, PaymentMethodParam28? klarna, PaymentMethodParam29? konbini, PaymentMethodParam30? krCard, PaymentMethodParam31? link, PaymentMethodParam32? mobilepay, PaymentMethodParam33? multibanco, String? name, PaymentMethodParam34? naverPay, PaymentMethodParam35? nzBankAccount, PaymentMethodParam36? oxxo, PaymentMethodParam37? p24, String? parent, PaymentMethodParam38? payByBank, PaymentMethodParam39? payco, PaymentMethodParam40? paynow, PaymentMethodParam41? paypal, PaymentMethodParam42? pix, PaymentMethodParam43? promptpay, PaymentMethodParam44? revolutPay, PaymentMethodParam45? samsungPay, PaymentMethodParam46? satispay, PaymentMethodParam47? sepaDebit, PaymentMethodParam48? sofort, PaymentMethodParam49? swish, PaymentMethodParam50? twint, PaymentMethodParam51? usBankAccount, PaymentMethodParam52? wechatPay, PaymentMethodParam53? zip, }) async {
    final response = await postPaymentMethodConfigurationsWithHttpInfo( acssDebit: acssDebit, affirm: affirm, afterpayClearpay: afterpayClearpay, alipay: alipay, alma: alma, amazonPay: amazonPay, applePay: applePay, applePayLater: applePayLater, auBecsDebit: auBecsDebit, bacsDebit: bacsDebit, bancontact: bancontact, billie: billie, blik: blik, boleto: boleto, card: card, cartesBancaires: cartesBancaires, cashapp: cashapp, customerBalance: customerBalance, eps: eps, expand: expand, fpx: fpx, giropay: giropay, googlePay: googlePay, grabpay: grabpay, ideal: ideal, jcb: jcb, kakaoPay: kakaoPay, klarna: klarna, konbini: konbini, krCard: krCard, link: link, mobilepay: mobilepay, multibanco: multibanco, name: name, naverPay: naverPay, nzBankAccount: nzBankAccount, oxxo: oxxo, p24: p24, parent: parent, payByBank: payByBank, payco: payco, paynow: paynow, paypal: paypal, pix: pix, promptpay: promptpay, revolutPay: revolutPay, samsungPay: samsungPay, satispay: satispay, sepaDebit: sepaDebit, sofort: sofort, swish: swish, twint: twint, usBankAccount: usBankAccount, wechatPay: wechatPay, zip: zip, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentMethodConfiguration',) as PaymentMethodConfiguration;
    
    }
    return null;
  }

  /// Update payment method configuration
  ///
  /// <p>Update payment method configuration</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] configuration (required):
  ///
  /// * [PaymentMethodParam2] acssDebit:
  ///
  /// * [bool] active:
  ///   Whether the configuration can be used for new payments.
  ///
  /// * [PaymentMethodParam3] affirm:
  ///
  /// * [PaymentMethodParam4] afterpayClearpay:
  ///
  /// * [PaymentMethodParam5] alipay:
  ///
  /// * [PaymentMethodParam6] alma:
  ///
  /// * [PaymentMethodParam7] amazonPay:
  ///
  /// * [PaymentMethodParam8] applePay:
  ///
  /// * [PaymentMethodParam9] applePayLater:
  ///
  /// * [PaymentMethodParam10] auBecsDebit:
  ///
  /// * [PaymentMethodParam11] bacsDebit:
  ///
  /// * [PaymentMethodParam12] bancontact:
  ///
  /// * [PaymentMethodParam13] billie:
  ///
  /// * [PaymentMethodParam14] blik:
  ///
  /// * [PaymentMethodParam15] boleto:
  ///
  /// * [PaymentMethodParam16] card:
  ///
  /// * [PaymentMethodParam17] cartesBancaires:
  ///
  /// * [PaymentMethodParam18] cashapp:
  ///
  /// * [PaymentMethodParam19] customerBalance:
  ///
  /// * [PaymentMethodParam20] eps:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PaymentMethodParam21] fpx:
  ///
  /// * [PaymentMethodParam22] giropay:
  ///
  /// * [PaymentMethodParam23] googlePay:
  ///
  /// * [PaymentMethodParam24] grabpay:
  ///
  /// * [PaymentMethodParam25] ideal:
  ///
  /// * [PaymentMethodParam26] jcb:
  ///
  /// * [PaymentMethodParam27] kakaoPay:
  ///
  /// * [PaymentMethodParam28] klarna:
  ///
  /// * [PaymentMethodParam29] konbini:
  ///
  /// * [PaymentMethodParam30] krCard:
  ///
  /// * [PaymentMethodParam31] link:
  ///
  /// * [PaymentMethodParam32] mobilepay:
  ///
  /// * [PaymentMethodParam33] multibanco:
  ///
  /// * [String] name:
  ///   Configuration name.
  ///
  /// * [PaymentMethodParam34] naverPay:
  ///
  /// * [PaymentMethodParam35] nzBankAccount:
  ///
  /// * [PaymentMethodParam36] oxxo:
  ///
  /// * [PaymentMethodParam37] p24:
  ///
  /// * [PaymentMethodParam38] payByBank:
  ///
  /// * [PaymentMethodParam39] payco:
  ///
  /// * [PaymentMethodParam40] paynow:
  ///
  /// * [PaymentMethodParam41] paypal:
  ///
  /// * [PaymentMethodParam42] pix:
  ///
  /// * [PaymentMethodParam43] promptpay:
  ///
  /// * [PaymentMethodParam44] revolutPay:
  ///
  /// * [PaymentMethodParam45] samsungPay:
  ///
  /// * [PaymentMethodParam46] satispay:
  ///
  /// * [PaymentMethodParam47] sepaDebit:
  ///
  /// * [PaymentMethodParam48] sofort:
  ///
  /// * [PaymentMethodParam49] swish:
  ///
  /// * [PaymentMethodParam50] twint:
  ///
  /// * [PaymentMethodParam51] usBankAccount:
  ///
  /// * [PaymentMethodParam52] wechatPay:
  ///
  /// * [PaymentMethodParam53] zip:
  Future<Response> postPaymentMethodConfigurationsConfigurationWithHttpInfo(String configuration, { PaymentMethodParam2? acssDebit, bool? active, PaymentMethodParam3? affirm, PaymentMethodParam4? afterpayClearpay, PaymentMethodParam5? alipay, PaymentMethodParam6? alma, PaymentMethodParam7? amazonPay, PaymentMethodParam8? applePay, PaymentMethodParam9? applePayLater, PaymentMethodParam10? auBecsDebit, PaymentMethodParam11? bacsDebit, PaymentMethodParam12? bancontact, PaymentMethodParam13? billie, PaymentMethodParam14? blik, PaymentMethodParam15? boleto, PaymentMethodParam16? card, PaymentMethodParam17? cartesBancaires, PaymentMethodParam18? cashapp, PaymentMethodParam19? customerBalance, PaymentMethodParam20? eps, List<String>? expand, PaymentMethodParam21? fpx, PaymentMethodParam22? giropay, PaymentMethodParam23? googlePay, PaymentMethodParam24? grabpay, PaymentMethodParam25? ideal, PaymentMethodParam26? jcb, PaymentMethodParam27? kakaoPay, PaymentMethodParam28? klarna, PaymentMethodParam29? konbini, PaymentMethodParam30? krCard, PaymentMethodParam31? link, PaymentMethodParam32? mobilepay, PaymentMethodParam33? multibanco, String? name, PaymentMethodParam34? naverPay, PaymentMethodParam35? nzBankAccount, PaymentMethodParam36? oxxo, PaymentMethodParam37? p24, PaymentMethodParam38? payByBank, PaymentMethodParam39? payco, PaymentMethodParam40? paynow, PaymentMethodParam41? paypal, PaymentMethodParam42? pix, PaymentMethodParam43? promptpay, PaymentMethodParam44? revolutPay, PaymentMethodParam45? samsungPay, PaymentMethodParam46? satispay, PaymentMethodParam47? sepaDebit, PaymentMethodParam48? sofort, PaymentMethodParam49? swish, PaymentMethodParam50? twint, PaymentMethodParam51? usBankAccount, PaymentMethodParam52? wechatPay, PaymentMethodParam53? zip, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payment_method_configurations/{configuration}'
      .replaceAll('{configuration}', configuration);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (acssDebit != null) {
      formParams[r'acss_debit'] = parameterToString(acssDebit);
    }
    if (active != null) {
      formParams[r'active'] = parameterToString(active);
    }
    if (affirm != null) {
      formParams[r'affirm'] = parameterToString(affirm);
    }
    if (afterpayClearpay != null) {
      formParams[r'afterpay_clearpay'] = parameterToString(afterpayClearpay);
    }
    if (alipay != null) {
      formParams[r'alipay'] = parameterToString(alipay);
    }
    if (alma != null) {
      formParams[r'alma'] = parameterToString(alma);
    }
    if (amazonPay != null) {
      formParams[r'amazon_pay'] = parameterToString(amazonPay);
    }
    if (applePay != null) {
      formParams[r'apple_pay'] = parameterToString(applePay);
    }
    if (applePayLater != null) {
      formParams[r'apple_pay_later'] = parameterToString(applePayLater);
    }
    if (auBecsDebit != null) {
      formParams[r'au_becs_debit'] = parameterToString(auBecsDebit);
    }
    if (bacsDebit != null) {
      formParams[r'bacs_debit'] = parameterToString(bacsDebit);
    }
    if (bancontact != null) {
      formParams[r'bancontact'] = parameterToString(bancontact);
    }
    if (billie != null) {
      formParams[r'billie'] = parameterToString(billie);
    }
    if (blik != null) {
      formParams[r'blik'] = parameterToString(blik);
    }
    if (boleto != null) {
      formParams[r'boleto'] = parameterToString(boleto);
    }
    if (card != null) {
      formParams[r'card'] = parameterToString(card);
    }
    if (cartesBancaires != null) {
      formParams[r'cartes_bancaires'] = parameterToString(cartesBancaires);
    }
    if (cashapp != null) {
      formParams[r'cashapp'] = parameterToString(cashapp);
    }
    if (customerBalance != null) {
      formParams[r'customer_balance'] = parameterToString(customerBalance);
    }
    if (eps != null) {
      formParams[r'eps'] = parameterToString(eps);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (fpx != null) {
      formParams[r'fpx'] = parameterToString(fpx);
    }
    if (giropay != null) {
      formParams[r'giropay'] = parameterToString(giropay);
    }
    if (googlePay != null) {
      formParams[r'google_pay'] = parameterToString(googlePay);
    }
    if (grabpay != null) {
      formParams[r'grabpay'] = parameterToString(grabpay);
    }
    if (ideal != null) {
      formParams[r'ideal'] = parameterToString(ideal);
    }
    if (jcb != null) {
      formParams[r'jcb'] = parameterToString(jcb);
    }
    if (kakaoPay != null) {
      formParams[r'kakao_pay'] = parameterToString(kakaoPay);
    }
    if (klarna != null) {
      formParams[r'klarna'] = parameterToString(klarna);
    }
    if (konbini != null) {
      formParams[r'konbini'] = parameterToString(konbini);
    }
    if (krCard != null) {
      formParams[r'kr_card'] = parameterToString(krCard);
    }
    if (link != null) {
      formParams[r'link'] = parameterToString(link);
    }
    if (mobilepay != null) {
      formParams[r'mobilepay'] = parameterToString(mobilepay);
    }
    if (multibanco != null) {
      formParams[r'multibanco'] = parameterToString(multibanco);
    }
    if (name != null) {
      formParams[r'name'] = parameterToString(name);
    }
    if (naverPay != null) {
      formParams[r'naver_pay'] = parameterToString(naverPay);
    }
    if (nzBankAccount != null) {
      formParams[r'nz_bank_account'] = parameterToString(nzBankAccount);
    }
    if (oxxo != null) {
      formParams[r'oxxo'] = parameterToString(oxxo);
    }
    if (p24 != null) {
      formParams[r'p24'] = parameterToString(p24);
    }
    if (payByBank != null) {
      formParams[r'pay_by_bank'] = parameterToString(payByBank);
    }
    if (payco != null) {
      formParams[r'payco'] = parameterToString(payco);
    }
    if (paynow != null) {
      formParams[r'paynow'] = parameterToString(paynow);
    }
    if (paypal != null) {
      formParams[r'paypal'] = parameterToString(paypal);
    }
    if (pix != null) {
      formParams[r'pix'] = parameterToString(pix);
    }
    if (promptpay != null) {
      formParams[r'promptpay'] = parameterToString(promptpay);
    }
    if (revolutPay != null) {
      formParams[r'revolut_pay'] = parameterToString(revolutPay);
    }
    if (samsungPay != null) {
      formParams[r'samsung_pay'] = parameterToString(samsungPay);
    }
    if (satispay != null) {
      formParams[r'satispay'] = parameterToString(satispay);
    }
    if (sepaDebit != null) {
      formParams[r'sepa_debit'] = parameterToString(sepaDebit);
    }
    if (sofort != null) {
      formParams[r'sofort'] = parameterToString(sofort);
    }
    if (swish != null) {
      formParams[r'swish'] = parameterToString(swish);
    }
    if (twint != null) {
      formParams[r'twint'] = parameterToString(twint);
    }
    if (usBankAccount != null) {
      formParams[r'us_bank_account'] = parameterToString(usBankAccount);
    }
    if (wechatPay != null) {
      formParams[r'wechat_pay'] = parameterToString(wechatPay);
    }
    if (zip != null) {
      formParams[r'zip'] = parameterToString(zip);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update payment method configuration
  ///
  /// <p>Update payment method configuration</p>
  ///
  /// Parameters:
  ///
  /// * [String] configuration (required):
  ///
  /// * [PaymentMethodParam2] acssDebit:
  ///
  /// * [bool] active:
  ///   Whether the configuration can be used for new payments.
  ///
  /// * [PaymentMethodParam3] affirm:
  ///
  /// * [PaymentMethodParam4] afterpayClearpay:
  ///
  /// * [PaymentMethodParam5] alipay:
  ///
  /// * [PaymentMethodParam6] alma:
  ///
  /// * [PaymentMethodParam7] amazonPay:
  ///
  /// * [PaymentMethodParam8] applePay:
  ///
  /// * [PaymentMethodParam9] applePayLater:
  ///
  /// * [PaymentMethodParam10] auBecsDebit:
  ///
  /// * [PaymentMethodParam11] bacsDebit:
  ///
  /// * [PaymentMethodParam12] bancontact:
  ///
  /// * [PaymentMethodParam13] billie:
  ///
  /// * [PaymentMethodParam14] blik:
  ///
  /// * [PaymentMethodParam15] boleto:
  ///
  /// * [PaymentMethodParam16] card:
  ///
  /// * [PaymentMethodParam17] cartesBancaires:
  ///
  /// * [PaymentMethodParam18] cashapp:
  ///
  /// * [PaymentMethodParam19] customerBalance:
  ///
  /// * [PaymentMethodParam20] eps:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PaymentMethodParam21] fpx:
  ///
  /// * [PaymentMethodParam22] giropay:
  ///
  /// * [PaymentMethodParam23] googlePay:
  ///
  /// * [PaymentMethodParam24] grabpay:
  ///
  /// * [PaymentMethodParam25] ideal:
  ///
  /// * [PaymentMethodParam26] jcb:
  ///
  /// * [PaymentMethodParam27] kakaoPay:
  ///
  /// * [PaymentMethodParam28] klarna:
  ///
  /// * [PaymentMethodParam29] konbini:
  ///
  /// * [PaymentMethodParam30] krCard:
  ///
  /// * [PaymentMethodParam31] link:
  ///
  /// * [PaymentMethodParam32] mobilepay:
  ///
  /// * [PaymentMethodParam33] multibanco:
  ///
  /// * [String] name:
  ///   Configuration name.
  ///
  /// * [PaymentMethodParam34] naverPay:
  ///
  /// * [PaymentMethodParam35] nzBankAccount:
  ///
  /// * [PaymentMethodParam36] oxxo:
  ///
  /// * [PaymentMethodParam37] p24:
  ///
  /// * [PaymentMethodParam38] payByBank:
  ///
  /// * [PaymentMethodParam39] payco:
  ///
  /// * [PaymentMethodParam40] paynow:
  ///
  /// * [PaymentMethodParam41] paypal:
  ///
  /// * [PaymentMethodParam42] pix:
  ///
  /// * [PaymentMethodParam43] promptpay:
  ///
  /// * [PaymentMethodParam44] revolutPay:
  ///
  /// * [PaymentMethodParam45] samsungPay:
  ///
  /// * [PaymentMethodParam46] satispay:
  ///
  /// * [PaymentMethodParam47] sepaDebit:
  ///
  /// * [PaymentMethodParam48] sofort:
  ///
  /// * [PaymentMethodParam49] swish:
  ///
  /// * [PaymentMethodParam50] twint:
  ///
  /// * [PaymentMethodParam51] usBankAccount:
  ///
  /// * [PaymentMethodParam52] wechatPay:
  ///
  /// * [PaymentMethodParam53] zip:
  Future<PaymentMethodConfiguration?> postPaymentMethodConfigurationsConfiguration(String configuration, { PaymentMethodParam2? acssDebit, bool? active, PaymentMethodParam3? affirm, PaymentMethodParam4? afterpayClearpay, PaymentMethodParam5? alipay, PaymentMethodParam6? alma, PaymentMethodParam7? amazonPay, PaymentMethodParam8? applePay, PaymentMethodParam9? applePayLater, PaymentMethodParam10? auBecsDebit, PaymentMethodParam11? bacsDebit, PaymentMethodParam12? bancontact, PaymentMethodParam13? billie, PaymentMethodParam14? blik, PaymentMethodParam15? boleto, PaymentMethodParam16? card, PaymentMethodParam17? cartesBancaires, PaymentMethodParam18? cashapp, PaymentMethodParam19? customerBalance, PaymentMethodParam20? eps, List<String>? expand, PaymentMethodParam21? fpx, PaymentMethodParam22? giropay, PaymentMethodParam23? googlePay, PaymentMethodParam24? grabpay, PaymentMethodParam25? ideal, PaymentMethodParam26? jcb, PaymentMethodParam27? kakaoPay, PaymentMethodParam28? klarna, PaymentMethodParam29? konbini, PaymentMethodParam30? krCard, PaymentMethodParam31? link, PaymentMethodParam32? mobilepay, PaymentMethodParam33? multibanco, String? name, PaymentMethodParam34? naverPay, PaymentMethodParam35? nzBankAccount, PaymentMethodParam36? oxxo, PaymentMethodParam37? p24, PaymentMethodParam38? payByBank, PaymentMethodParam39? payco, PaymentMethodParam40? paynow, PaymentMethodParam41? paypal, PaymentMethodParam42? pix, PaymentMethodParam43? promptpay, PaymentMethodParam44? revolutPay, PaymentMethodParam45? samsungPay, PaymentMethodParam46? satispay, PaymentMethodParam47? sepaDebit, PaymentMethodParam48? sofort, PaymentMethodParam49? swish, PaymentMethodParam50? twint, PaymentMethodParam51? usBankAccount, PaymentMethodParam52? wechatPay, PaymentMethodParam53? zip, }) async {
    final response = await postPaymentMethodConfigurationsConfigurationWithHttpInfo(configuration,  acssDebit: acssDebit, active: active, affirm: affirm, afterpayClearpay: afterpayClearpay, alipay: alipay, alma: alma, amazonPay: amazonPay, applePay: applePay, applePayLater: applePayLater, auBecsDebit: auBecsDebit, bacsDebit: bacsDebit, bancontact: bancontact, billie: billie, blik: blik, boleto: boleto, card: card, cartesBancaires: cartesBancaires, cashapp: cashapp, customerBalance: customerBalance, eps: eps, expand: expand, fpx: fpx, giropay: giropay, googlePay: googlePay, grabpay: grabpay, ideal: ideal, jcb: jcb, kakaoPay: kakaoPay, klarna: klarna, konbini: konbini, krCard: krCard, link: link, mobilepay: mobilepay, multibanco: multibanco, name: name, naverPay: naverPay, nzBankAccount: nzBankAccount, oxxo: oxxo, p24: p24, payByBank: payByBank, payco: payco, paynow: paynow, paypal: paypal, pix: pix, promptpay: promptpay, revolutPay: revolutPay, samsungPay: samsungPay, satispay: satispay, sepaDebit: sepaDebit, sofort: sofort, swish: swish, twint: twint, usBankAccount: usBankAccount, wechatPay: wechatPay, zip: zip, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentMethodConfiguration',) as PaymentMethodConfiguration;
    
    }
    return null;
  }

  /// Create a payment method domain
  ///
  /// <p>Creates a payment method domain.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] domainName (required):
  ///   The domain name that this payment method domain object represents.
  ///
  /// * [bool] enabled:
  ///   Whether this payment method domain is enabled. If the domain is not enabled, payment methods that require a payment method domain will not appear in Elements or Embedded Checkout.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postPaymentMethodDomainsWithHttpInfo(String domainName, { bool? enabled, List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payment_method_domains';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (domainName != null) {
      formParams[r'domain_name'] = parameterToString(domainName);
    }
    if (enabled != null) {
      formParams[r'enabled'] = parameterToString(enabled);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a payment method domain
  ///
  /// <p>Creates a payment method domain.</p>
  ///
  /// Parameters:
  ///
  /// * [String] domainName (required):
  ///   The domain name that this payment method domain object represents.
  ///
  /// * [bool] enabled:
  ///   Whether this payment method domain is enabled. If the domain is not enabled, payment methods that require a payment method domain will not appear in Elements or Embedded Checkout.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<PaymentMethodDomain?> postPaymentMethodDomains(String domainName, { bool? enabled, List<String>? expand, }) async {
    final response = await postPaymentMethodDomainsWithHttpInfo(domainName,  enabled: enabled, expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentMethodDomain',) as PaymentMethodDomain;
    
    }
    return null;
  }

  /// Update a payment method domain
  ///
  /// <p>Updates an existing payment method domain.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] paymentMethodDomain (required):
  ///
  /// * [bool] enabled:
  ///   Whether this payment method domain is enabled. If the domain is not enabled, payment methods that require a payment method domain will not appear in Elements or Embedded Checkout.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postPaymentMethodDomainsPaymentMethodDomainWithHttpInfo(String paymentMethodDomain, { bool? enabled, List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payment_method_domains/{payment_method_domain}'
      .replaceAll('{payment_method_domain}', paymentMethodDomain);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (enabled != null) {
      formParams[r'enabled'] = parameterToString(enabled);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a payment method domain
  ///
  /// <p>Updates an existing payment method domain.</p>
  ///
  /// Parameters:
  ///
  /// * [String] paymentMethodDomain (required):
  ///
  /// * [bool] enabled:
  ///   Whether this payment method domain is enabled. If the domain is not enabled, payment methods that require a payment method domain will not appear in Elements or Embedded Checkout.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<PaymentMethodDomain?> postPaymentMethodDomainsPaymentMethodDomain(String paymentMethodDomain, { bool? enabled, List<String>? expand, }) async {
    final response = await postPaymentMethodDomainsPaymentMethodDomainWithHttpInfo(paymentMethodDomain,  enabled: enabled, expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentMethodDomain',) as PaymentMethodDomain;
    
    }
    return null;
  }

  /// Validate an existing payment method domain
  ///
  /// <p>Some payment methods might require additional steps to register a domain. If the requirements weren’t satisfied when the domain was created, the payment method will be inactive on the domain. The payment method doesn’t appear in Elements or Embedded Checkout for this domain until it is active.</p>  <p>To activate a payment method on an existing payment method domain, complete the required registration steps specific to the payment method, and then validate the payment method domain with this endpoint.</p>  <p>Related guides: <a href=\"/docs/payments/payment-methods/pmd-registration\">Payment method domains</a>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] paymentMethodDomain (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postPaymentMethodDomainsPaymentMethodDomainValidateWithHttpInfo(String paymentMethodDomain, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payment_method_domains/{payment_method_domain}/validate'
      .replaceAll('{payment_method_domain}', paymentMethodDomain);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Validate an existing payment method domain
  ///
  /// <p>Some payment methods might require additional steps to register a domain. If the requirements weren’t satisfied when the domain was created, the payment method will be inactive on the domain. The payment method doesn’t appear in Elements or Embedded Checkout for this domain until it is active.</p>  <p>To activate a payment method on an existing payment method domain, complete the required registration steps specific to the payment method, and then validate the payment method domain with this endpoint.</p>  <p>Related guides: <a href=\"/docs/payments/payment-methods/pmd-registration\">Payment method domains</a>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] paymentMethodDomain (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<PaymentMethodDomain?> postPaymentMethodDomainsPaymentMethodDomainValidate(String paymentMethodDomain, { List<String>? expand, }) async {
    final response = await postPaymentMethodDomainsPaymentMethodDomainValidateWithHttpInfo(paymentMethodDomain,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentMethodDomain',) as PaymentMethodDomain;
    
    }
    return null;
  }

  /// Shares a PaymentMethod
  ///
  /// <p>Creates a PaymentMethod object. Read the <a href=\"/docs/stripe-js/reference#stripe-create-payment-method\">Stripe.js reference</a> to learn how to create PaymentMethods via Stripe.js.</p>  <p>Instead of creating a PaymentMethod directly, we recommend using the <a href=\"/docs/payments/accept-a-payment\">PaymentIntents</a> API to accept a payment immediately or the <a href=\"/docs/payments/save-and-reuse\">SetupIntent</a> API to collect payment method details ahead of a future payment.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [PaymentMethodParam54] acssDebit:
  ///
  /// * [Object] affirm:
  ///   If this is an `affirm` PaymentMethod, this hash contains details about the Affirm payment method.
  ///
  /// * [Object] afterpayClearpay:
  ///   If this is an `AfterpayClearpay` PaymentMethod, this hash contains details about the AfterpayClearpay payment method.
  ///
  /// * [Object] alipay:
  ///   If this is an `Alipay` PaymentMethod, this hash contains details about the Alipay payment method.
  ///
  /// * [String] allowRedisplay:
  ///   This field indicates whether this payment method can be shown again to its customer in a checkout flow. Stripe products such as Checkout and Elements use this field to determine whether a payment method can be shown as a saved payment method in a checkout flow. The field defaults to `unspecified`.
  ///
  /// * [Object] alma:
  ///   If this is a Alma PaymentMethod, this hash contains details about the Alma payment method.
  ///
  /// * [Object] amazonPay:
  ///   If this is a AmazonPay PaymentMethod, this hash contains details about the AmazonPay payment method.
  ///
  /// * [Param14] auBecsDebit:
  ///
  /// * [Param15] bacsDebit:
  ///
  /// * [Object] bancontact:
  ///   If this is a `bancontact` PaymentMethod, this hash contains details about the Bancontact payment method.
  ///
  /// * [Object] billie:
  ///   If this is a `billie` PaymentMethod, this hash contains details about the Billie payment method.
  ///
  /// * [BillingDetailsInnerParams1] billingDetails:
  ///
  /// * [Object] blik:
  ///   If this is a `blik` PaymentMethod, this hash contains details about the BLIK payment method.
  ///
  /// * [Param16] boleto:
  ///
  /// * [PostPaymentMethodsRequestCard] card:
  ///
  /// * [Object] cashapp:
  ///   If this is a `cashapp` PaymentMethod, this hash contains details about the Cash App Pay payment method.
  ///
  /// * [Object] crypto:
  ///   If this is a Crypto PaymentMethod, this hash contains details about the Crypto payment method.
  ///
  /// * [String] customer:
  ///   The `Customer` to whom the original PaymentMethod is attached.
  ///
  /// * [Object] customerBalance:
  ///   If this is a `customer_balance` PaymentMethod, this hash contains details about the CustomerBalance payment method.
  ///
  /// * [Param17] eps:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Param18] fpx:
  ///
  /// * [Object] giropay:
  ///   If this is a `giropay` PaymentMethod, this hash contains details about the Giropay payment method.
  ///
  /// * [Object] grabpay:
  ///   If this is a `grabpay` PaymentMethod, this hash contains details about the GrabPay payment method.
  ///
  /// * [Param19] ideal:
  ///
  /// * [Object] interacPresent:
  ///   If this is an `interac_present` PaymentMethod, this hash contains details about the Interac Present payment method.
  ///
  /// * [Object] kakaoPay:
  ///   If this is a `kakao_pay` PaymentMethod, this hash contains details about the Kakao Pay payment method.
  ///
  /// * [Param20] klarna:
  ///
  /// * [Object] konbini:
  ///   If this is a `konbini` PaymentMethod, this hash contains details about the Konbini payment method.
  ///
  /// * [Object] krCard:
  ///   If this is a `kr_card` PaymentMethod, this hash contains details about the Korean Card payment method.
  ///
  /// * [Object] link:
  ///   If this is an `Link` PaymentMethod, this hash contains details about the Link payment method.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [Object] mobilepay:
  ///   If this is a `mobilepay` PaymentMethod, this hash contains details about the MobilePay payment method.
  ///
  /// * [Object] multibanco:
  ///   If this is a `multibanco` PaymentMethod, this hash contains details about the Multibanco payment method.
  ///
  /// * [Param21] naverPay:
  ///
  /// * [Param22] nzBankAccount:
  ///
  /// * [Object] oxxo:
  ///   If this is an `oxxo` PaymentMethod, this hash contains details about the OXXO payment method.
  ///
  /// * [Param23] p24:
  ///
  /// * [Object] payByBank:
  ///   If this is a `pay_by_bank` PaymentMethod, this hash contains details about the PayByBank payment method.
  ///
  /// * [Object] payco:
  ///   If this is a `payco` PaymentMethod, this hash contains details about the PAYCO payment method.
  ///
  /// * [String] paymentMethod:
  ///   The PaymentMethod to share.
  ///
  /// * [Object] paynow:
  ///   If this is a `paynow` PaymentMethod, this hash contains details about the PayNow payment method.
  ///
  /// * [Object] paypal:
  ///   If this is a `paypal` PaymentMethod, this hash contains details about the PayPal payment method.
  ///
  /// * [Object] pix:
  ///   If this is a `pix` PaymentMethod, this hash contains details about the Pix payment method.
  ///
  /// * [Object] promptpay:
  ///   If this is a `promptpay` PaymentMethod, this hash contains details about the PromptPay payment method.
  ///
  /// * [RadarOptionsWithHiddenOptions] radarOptions:
  ///
  /// * [Object] revolutPay:
  ///   If this is a `revolut_pay` PaymentMethod, this hash contains details about the Revolut Pay payment method.
  ///
  /// * [Object] samsungPay:
  ///   If this is a `samsung_pay` PaymentMethod, this hash contains details about the SamsungPay payment method.
  ///
  /// * [Object] satispay:
  ///   If this is a `satispay` PaymentMethod, this hash contains details about the Satispay payment method.
  ///
  /// * [Param24] sepaDebit:
  ///
  /// * [Param25] sofort:
  ///
  /// * [Object] swish:
  ///   If this is a `swish` PaymentMethod, this hash contains details about the Swish payment method.
  ///
  /// * [Object] twint:
  ///   If this is a TWINT PaymentMethod, this hash contains details about the TWINT payment method.
  ///
  /// * [String] type:
  ///   The type of the PaymentMethod. An additional hash is included on the PaymentMethod with a name matching this value. It contains additional information specific to the PaymentMethod type.
  ///
  /// * [PaymentMethodParam55] usBankAccount:
  ///
  /// * [Object] wechatPay:
  ///   If this is an `wechat_pay` PaymentMethod, this hash contains details about the wechat_pay payment method.
  ///
  /// * [Object] zip:
  ///   If this is a `zip` PaymentMethod, this hash contains details about the Zip payment method.
  Future<Response> postPaymentMethodsWithHttpInfo({ PaymentMethodParam54? acssDebit, Object? affirm, Object? afterpayClearpay, Object? alipay, String? allowRedisplay, Object? alma, Object? amazonPay, Param14? auBecsDebit, Param15? bacsDebit, Object? bancontact, Object? billie, BillingDetailsInnerParams1? billingDetails, Object? blik, Param16? boleto, PostPaymentMethodsRequestCard? card, Object? cashapp, Object? crypto, String? customer, Object? customerBalance, Param17? eps, List<String>? expand, Param18? fpx, Object? giropay, Object? grabpay, Param19? ideal, Object? interacPresent, Object? kakaoPay, Param20? klarna, Object? konbini, Object? krCard, Object? link, Map<String, String>? metadata, Object? mobilepay, Object? multibanco, Param21? naverPay, Param22? nzBankAccount, Object? oxxo, Param23? p24, Object? payByBank, Object? payco, String? paymentMethod, Object? paynow, Object? paypal, Object? pix, Object? promptpay, RadarOptionsWithHiddenOptions? radarOptions, Object? revolutPay, Object? samsungPay, Object? satispay, Param24? sepaDebit, Param25? sofort, Object? swish, Object? twint, String? type, PaymentMethodParam55? usBankAccount, Object? wechatPay, Object? zip, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payment_methods';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (acssDebit != null) {
      formParams[r'acss_debit'] = parameterToString(acssDebit);
    }
    if (affirm != null) {
      formParams[r'affirm'] = parameterToString(affirm);
    }
    if (afterpayClearpay != null) {
      formParams[r'afterpay_clearpay'] = parameterToString(afterpayClearpay);
    }
    if (alipay != null) {
      formParams[r'alipay'] = parameterToString(alipay);
    }
    if (allowRedisplay != null) {
      formParams[r'allow_redisplay'] = parameterToString(allowRedisplay);
    }
    if (alma != null) {
      formParams[r'alma'] = parameterToString(alma);
    }
    if (amazonPay != null) {
      formParams[r'amazon_pay'] = parameterToString(amazonPay);
    }
    if (auBecsDebit != null) {
      formParams[r'au_becs_debit'] = parameterToString(auBecsDebit);
    }
    if (bacsDebit != null) {
      formParams[r'bacs_debit'] = parameterToString(bacsDebit);
    }
    if (bancontact != null) {
      formParams[r'bancontact'] = parameterToString(bancontact);
    }
    if (billie != null) {
      formParams[r'billie'] = parameterToString(billie);
    }
    if (billingDetails != null) {
      formParams[r'billing_details'] = parameterToString(billingDetails);
    }
    if (blik != null) {
      formParams[r'blik'] = parameterToString(blik);
    }
    if (boleto != null) {
      formParams[r'boleto'] = parameterToString(boleto);
    }
    if (card != null) {
      formParams[r'card'] = parameterToString(card);
    }
    if (cashapp != null) {
      formParams[r'cashapp'] = parameterToString(cashapp);
    }
    if (crypto != null) {
      formParams[r'crypto'] = parameterToString(crypto);
    }
    if (customer != null) {
      formParams[r'customer'] = parameterToString(customer);
    }
    if (customerBalance != null) {
      formParams[r'customer_balance'] = parameterToString(customerBalance);
    }
    if (eps != null) {
      formParams[r'eps'] = parameterToString(eps);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (fpx != null) {
      formParams[r'fpx'] = parameterToString(fpx);
    }
    if (giropay != null) {
      formParams[r'giropay'] = parameterToString(giropay);
    }
    if (grabpay != null) {
      formParams[r'grabpay'] = parameterToString(grabpay);
    }
    if (ideal != null) {
      formParams[r'ideal'] = parameterToString(ideal);
    }
    if (interacPresent != null) {
      formParams[r'interac_present'] = parameterToString(interacPresent);
    }
    if (kakaoPay != null) {
      formParams[r'kakao_pay'] = parameterToString(kakaoPay);
    }
    if (klarna != null) {
      formParams[r'klarna'] = parameterToString(klarna);
    }
    if (konbini != null) {
      formParams[r'konbini'] = parameterToString(konbini);
    }
    if (krCard != null) {
      formParams[r'kr_card'] = parameterToString(krCard);
    }
    if (link != null) {
      formParams[r'link'] = parameterToString(link);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (mobilepay != null) {
      formParams[r'mobilepay'] = parameterToString(mobilepay);
    }
    if (multibanco != null) {
      formParams[r'multibanco'] = parameterToString(multibanco);
    }
    if (naverPay != null) {
      formParams[r'naver_pay'] = parameterToString(naverPay);
    }
    if (nzBankAccount != null) {
      formParams[r'nz_bank_account'] = parameterToString(nzBankAccount);
    }
    if (oxxo != null) {
      formParams[r'oxxo'] = parameterToString(oxxo);
    }
    if (p24 != null) {
      formParams[r'p24'] = parameterToString(p24);
    }
    if (payByBank != null) {
      formParams[r'pay_by_bank'] = parameterToString(payByBank);
    }
    if (payco != null) {
      formParams[r'payco'] = parameterToString(payco);
    }
    if (paymentMethod != null) {
      formParams[r'payment_method'] = parameterToString(paymentMethod);
    }
    if (paynow != null) {
      formParams[r'paynow'] = parameterToString(paynow);
    }
    if (paypal != null) {
      formParams[r'paypal'] = parameterToString(paypal);
    }
    if (pix != null) {
      formParams[r'pix'] = parameterToString(pix);
    }
    if (promptpay != null) {
      formParams[r'promptpay'] = parameterToString(promptpay);
    }
    if (radarOptions != null) {
      formParams[r'radar_options'] = parameterToString(radarOptions);
    }
    if (revolutPay != null) {
      formParams[r'revolut_pay'] = parameterToString(revolutPay);
    }
    if (samsungPay != null) {
      formParams[r'samsung_pay'] = parameterToString(samsungPay);
    }
    if (satispay != null) {
      formParams[r'satispay'] = parameterToString(satispay);
    }
    if (sepaDebit != null) {
      formParams[r'sepa_debit'] = parameterToString(sepaDebit);
    }
    if (sofort != null) {
      formParams[r'sofort'] = parameterToString(sofort);
    }
    if (swish != null) {
      formParams[r'swish'] = parameterToString(swish);
    }
    if (twint != null) {
      formParams[r'twint'] = parameterToString(twint);
    }
    if (type != null) {
      formParams[r'type'] = parameterToString(type);
    }
    if (usBankAccount != null) {
      formParams[r'us_bank_account'] = parameterToString(usBankAccount);
    }
    if (wechatPay != null) {
      formParams[r'wechat_pay'] = parameterToString(wechatPay);
    }
    if (zip != null) {
      formParams[r'zip'] = parameterToString(zip);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Shares a PaymentMethod
  ///
  /// <p>Creates a PaymentMethod object. Read the <a href=\"/docs/stripe-js/reference#stripe-create-payment-method\">Stripe.js reference</a> to learn how to create PaymentMethods via Stripe.js.</p>  <p>Instead of creating a PaymentMethod directly, we recommend using the <a href=\"/docs/payments/accept-a-payment\">PaymentIntents</a> API to accept a payment immediately or the <a href=\"/docs/payments/save-and-reuse\">SetupIntent</a> API to collect payment method details ahead of a future payment.</p>
  ///
  /// Parameters:
  ///
  /// * [PaymentMethodParam54] acssDebit:
  ///
  /// * [Object] affirm:
  ///   If this is an `affirm` PaymentMethod, this hash contains details about the Affirm payment method.
  ///
  /// * [Object] afterpayClearpay:
  ///   If this is an `AfterpayClearpay` PaymentMethod, this hash contains details about the AfterpayClearpay payment method.
  ///
  /// * [Object] alipay:
  ///   If this is an `Alipay` PaymentMethod, this hash contains details about the Alipay payment method.
  ///
  /// * [String] allowRedisplay:
  ///   This field indicates whether this payment method can be shown again to its customer in a checkout flow. Stripe products such as Checkout and Elements use this field to determine whether a payment method can be shown as a saved payment method in a checkout flow. The field defaults to `unspecified`.
  ///
  /// * [Object] alma:
  ///   If this is a Alma PaymentMethod, this hash contains details about the Alma payment method.
  ///
  /// * [Object] amazonPay:
  ///   If this is a AmazonPay PaymentMethod, this hash contains details about the AmazonPay payment method.
  ///
  /// * [Param14] auBecsDebit:
  ///
  /// * [Param15] bacsDebit:
  ///
  /// * [Object] bancontact:
  ///   If this is a `bancontact` PaymentMethod, this hash contains details about the Bancontact payment method.
  ///
  /// * [Object] billie:
  ///   If this is a `billie` PaymentMethod, this hash contains details about the Billie payment method.
  ///
  /// * [BillingDetailsInnerParams1] billingDetails:
  ///
  /// * [Object] blik:
  ///   If this is a `blik` PaymentMethod, this hash contains details about the BLIK payment method.
  ///
  /// * [Param16] boleto:
  ///
  /// * [PostPaymentMethodsRequestCard] card:
  ///
  /// * [Object] cashapp:
  ///   If this is a `cashapp` PaymentMethod, this hash contains details about the Cash App Pay payment method.
  ///
  /// * [Object] crypto:
  ///   If this is a Crypto PaymentMethod, this hash contains details about the Crypto payment method.
  ///
  /// * [String] customer:
  ///   The `Customer` to whom the original PaymentMethod is attached.
  ///
  /// * [Object] customerBalance:
  ///   If this is a `customer_balance` PaymentMethod, this hash contains details about the CustomerBalance payment method.
  ///
  /// * [Param17] eps:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Param18] fpx:
  ///
  /// * [Object] giropay:
  ///   If this is a `giropay` PaymentMethod, this hash contains details about the Giropay payment method.
  ///
  /// * [Object] grabpay:
  ///   If this is a `grabpay` PaymentMethod, this hash contains details about the GrabPay payment method.
  ///
  /// * [Param19] ideal:
  ///
  /// * [Object] interacPresent:
  ///   If this is an `interac_present` PaymentMethod, this hash contains details about the Interac Present payment method.
  ///
  /// * [Object] kakaoPay:
  ///   If this is a `kakao_pay` PaymentMethod, this hash contains details about the Kakao Pay payment method.
  ///
  /// * [Param20] klarna:
  ///
  /// * [Object] konbini:
  ///   If this is a `konbini` PaymentMethod, this hash contains details about the Konbini payment method.
  ///
  /// * [Object] krCard:
  ///   If this is a `kr_card` PaymentMethod, this hash contains details about the Korean Card payment method.
  ///
  /// * [Object] link:
  ///   If this is an `Link` PaymentMethod, this hash contains details about the Link payment method.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [Object] mobilepay:
  ///   If this is a `mobilepay` PaymentMethod, this hash contains details about the MobilePay payment method.
  ///
  /// * [Object] multibanco:
  ///   If this is a `multibanco` PaymentMethod, this hash contains details about the Multibanco payment method.
  ///
  /// * [Param21] naverPay:
  ///
  /// * [Param22] nzBankAccount:
  ///
  /// * [Object] oxxo:
  ///   If this is an `oxxo` PaymentMethod, this hash contains details about the OXXO payment method.
  ///
  /// * [Param23] p24:
  ///
  /// * [Object] payByBank:
  ///   If this is a `pay_by_bank` PaymentMethod, this hash contains details about the PayByBank payment method.
  ///
  /// * [Object] payco:
  ///   If this is a `payco` PaymentMethod, this hash contains details about the PAYCO payment method.
  ///
  /// * [String] paymentMethod:
  ///   The PaymentMethod to share.
  ///
  /// * [Object] paynow:
  ///   If this is a `paynow` PaymentMethod, this hash contains details about the PayNow payment method.
  ///
  /// * [Object] paypal:
  ///   If this is a `paypal` PaymentMethod, this hash contains details about the PayPal payment method.
  ///
  /// * [Object] pix:
  ///   If this is a `pix` PaymentMethod, this hash contains details about the Pix payment method.
  ///
  /// * [Object] promptpay:
  ///   If this is a `promptpay` PaymentMethod, this hash contains details about the PromptPay payment method.
  ///
  /// * [RadarOptionsWithHiddenOptions] radarOptions:
  ///
  /// * [Object] revolutPay:
  ///   If this is a `revolut_pay` PaymentMethod, this hash contains details about the Revolut Pay payment method.
  ///
  /// * [Object] samsungPay:
  ///   If this is a `samsung_pay` PaymentMethod, this hash contains details about the SamsungPay payment method.
  ///
  /// * [Object] satispay:
  ///   If this is a `satispay` PaymentMethod, this hash contains details about the Satispay payment method.
  ///
  /// * [Param24] sepaDebit:
  ///
  /// * [Param25] sofort:
  ///
  /// * [Object] swish:
  ///   If this is a `swish` PaymentMethod, this hash contains details about the Swish payment method.
  ///
  /// * [Object] twint:
  ///   If this is a TWINT PaymentMethod, this hash contains details about the TWINT payment method.
  ///
  /// * [String] type:
  ///   The type of the PaymentMethod. An additional hash is included on the PaymentMethod with a name matching this value. It contains additional information specific to the PaymentMethod type.
  ///
  /// * [PaymentMethodParam55] usBankAccount:
  ///
  /// * [Object] wechatPay:
  ///   If this is an `wechat_pay` PaymentMethod, this hash contains details about the wechat_pay payment method.
  ///
  /// * [Object] zip:
  ///   If this is a `zip` PaymentMethod, this hash contains details about the Zip payment method.
  Future<PaymentMethod?> postPaymentMethods({ PaymentMethodParam54? acssDebit, Object? affirm, Object? afterpayClearpay, Object? alipay, String? allowRedisplay, Object? alma, Object? amazonPay, Param14? auBecsDebit, Param15? bacsDebit, Object? bancontact, Object? billie, BillingDetailsInnerParams1? billingDetails, Object? blik, Param16? boleto, PostPaymentMethodsRequestCard? card, Object? cashapp, Object? crypto, String? customer, Object? customerBalance, Param17? eps, List<String>? expand, Param18? fpx, Object? giropay, Object? grabpay, Param19? ideal, Object? interacPresent, Object? kakaoPay, Param20? klarna, Object? konbini, Object? krCard, Object? link, Map<String, String>? metadata, Object? mobilepay, Object? multibanco, Param21? naverPay, Param22? nzBankAccount, Object? oxxo, Param23? p24, Object? payByBank, Object? payco, String? paymentMethod, Object? paynow, Object? paypal, Object? pix, Object? promptpay, RadarOptionsWithHiddenOptions? radarOptions, Object? revolutPay, Object? samsungPay, Object? satispay, Param24? sepaDebit, Param25? sofort, Object? swish, Object? twint, String? type, PaymentMethodParam55? usBankAccount, Object? wechatPay, Object? zip, }) async {
    final response = await postPaymentMethodsWithHttpInfo( acssDebit: acssDebit, affirm: affirm, afterpayClearpay: afterpayClearpay, alipay: alipay, allowRedisplay: allowRedisplay, alma: alma, amazonPay: amazonPay, auBecsDebit: auBecsDebit, bacsDebit: bacsDebit, bancontact: bancontact, billie: billie, billingDetails: billingDetails, blik: blik, boleto: boleto, card: card, cashapp: cashapp, crypto: crypto, customer: customer, customerBalance: customerBalance, eps: eps, expand: expand, fpx: fpx, giropay: giropay, grabpay: grabpay, ideal: ideal, interacPresent: interacPresent, kakaoPay: kakaoPay, klarna: klarna, konbini: konbini, krCard: krCard, link: link, metadata: metadata, mobilepay: mobilepay, multibanco: multibanco, naverPay: naverPay, nzBankAccount: nzBankAccount, oxxo: oxxo, p24: p24, payByBank: payByBank, payco: payco, paymentMethod: paymentMethod, paynow: paynow, paypal: paypal, pix: pix, promptpay: promptpay, radarOptions: radarOptions, revolutPay: revolutPay, samsungPay: samsungPay, satispay: satispay, sepaDebit: sepaDebit, sofort: sofort, swish: swish, twint: twint, type: type, usBankAccount: usBankAccount, wechatPay: wechatPay, zip: zip, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentMethod',) as PaymentMethod;
    
    }
    return null;
  }

  /// Update a PaymentMethod
  ///
  /// <p>Updates a PaymentMethod object. A PaymentMethod must be attached to a customer to be updated.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] paymentMethod (required):
  ///
  /// * [String] allowRedisplay:
  ///   This field indicates whether this payment method can be shown again to its customer in a checkout flow. Stripe products such as Checkout and Elements use this field to determine whether a payment method can be shown as a saved payment method in a checkout flow. The field defaults to `unspecified`.
  ///
  /// * [BillingDetailsInnerParams1] billingDetails:
  ///
  /// * [UpdateApiParam] card:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Object] link:
  ///   If this is an `Link` PaymentMethod, this hash contains details about the Link payment method.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [Object] payByBank:
  ///   If this is a `pay_by_bank` PaymentMethod, this hash contains details about the PayByBank payment method.
  ///
  /// * [UpdateParam] usBankAccount:
  Future<Response> postPaymentMethodsPaymentMethodWithHttpInfo(String paymentMethod, { String? allowRedisplay, BillingDetailsInnerParams1? billingDetails, UpdateApiParam? card, List<String>? expand, Object? link, PostAccountsRequestMetadata? metadata, Object? payByBank, UpdateParam? usBankAccount, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payment_methods/{payment_method}'
      .replaceAll('{payment_method}', paymentMethod);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (allowRedisplay != null) {
      formParams[r'allow_redisplay'] = parameterToString(allowRedisplay);
    }
    if (billingDetails != null) {
      formParams[r'billing_details'] = parameterToString(billingDetails);
    }
    if (card != null) {
      formParams[r'card'] = parameterToString(card);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (link != null) {
      formParams[r'link'] = parameterToString(link);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (payByBank != null) {
      formParams[r'pay_by_bank'] = parameterToString(payByBank);
    }
    if (usBankAccount != null) {
      formParams[r'us_bank_account'] = parameterToString(usBankAccount);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a PaymentMethod
  ///
  /// <p>Updates a PaymentMethod object. A PaymentMethod must be attached to a customer to be updated.</p>
  ///
  /// Parameters:
  ///
  /// * [String] paymentMethod (required):
  ///
  /// * [String] allowRedisplay:
  ///   This field indicates whether this payment method can be shown again to its customer in a checkout flow. Stripe products such as Checkout and Elements use this field to determine whether a payment method can be shown as a saved payment method in a checkout flow. The field defaults to `unspecified`.
  ///
  /// * [BillingDetailsInnerParams1] billingDetails:
  ///
  /// * [UpdateApiParam] card:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Object] link:
  ///   If this is an `Link` PaymentMethod, this hash contains details about the Link payment method.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [Object] payByBank:
  ///   If this is a `pay_by_bank` PaymentMethod, this hash contains details about the PayByBank payment method.
  ///
  /// * [UpdateParam] usBankAccount:
  Future<PaymentMethod?> postPaymentMethodsPaymentMethod(String paymentMethod, { String? allowRedisplay, BillingDetailsInnerParams1? billingDetails, UpdateApiParam? card, List<String>? expand, Object? link, PostAccountsRequestMetadata? metadata, Object? payByBank, UpdateParam? usBankAccount, }) async {
    final response = await postPaymentMethodsPaymentMethodWithHttpInfo(paymentMethod,  allowRedisplay: allowRedisplay, billingDetails: billingDetails, card: card, expand: expand, link: link, metadata: metadata, payByBank: payByBank, usBankAccount: usBankAccount, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentMethod',) as PaymentMethod;
    
    }
    return null;
  }

  /// Attach a PaymentMethod to a Customer
  ///
  /// <p>Attaches a PaymentMethod object to a Customer.</p>  <p>To attach a new PaymentMethod to a customer for future payments, we recommend you use a <a href=\"/docs/api/setup_intents\">SetupIntent</a> or a PaymentIntent with <a href=\"/docs/api/payment_intents/create#create_payment_intent-setup_future_usage\">setup_future_usage</a>. These approaches will perform any necessary steps to set up the PaymentMethod for future payments. Using the <code>/v1/payment_methods/:id/attach</code> endpoint without first using a SetupIntent or PaymentIntent with <code>setup_future_usage</code> does not optimize the PaymentMethod for future use, which makes later declines and payment friction more likely. See <a href=\"/docs/payments/payment-intents#future-usage\">Optimizing cards for future payments</a> for more information about setting up future payments.</p>  <p>To use this PaymentMethod as the default for invoice or subscription payments, set <a href=\"/docs/api/customers/update#update_customer-invoice_settings-default_payment_method\"><code>invoice_settings.default_payment_method</code></a>, on the Customer to the PaymentMethod’s ID.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] paymentMethod (required):
  ///
  /// * [String] customer (required):
  ///   The ID of the customer to which to attach the PaymentMethod.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postPaymentMethodsPaymentMethodAttachWithHttpInfo(String paymentMethod, String customer, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payment_methods/{payment_method}/attach'
      .replaceAll('{payment_method}', paymentMethod);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (customer != null) {
      formParams[r'customer'] = parameterToString(customer);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Attach a PaymentMethod to a Customer
  ///
  /// <p>Attaches a PaymentMethod object to a Customer.</p>  <p>To attach a new PaymentMethod to a customer for future payments, we recommend you use a <a href=\"/docs/api/setup_intents\">SetupIntent</a> or a PaymentIntent with <a href=\"/docs/api/payment_intents/create#create_payment_intent-setup_future_usage\">setup_future_usage</a>. These approaches will perform any necessary steps to set up the PaymentMethod for future payments. Using the <code>/v1/payment_methods/:id/attach</code> endpoint without first using a SetupIntent or PaymentIntent with <code>setup_future_usage</code> does not optimize the PaymentMethod for future use, which makes later declines and payment friction more likely. See <a href=\"/docs/payments/payment-intents#future-usage\">Optimizing cards for future payments</a> for more information about setting up future payments.</p>  <p>To use this PaymentMethod as the default for invoice or subscription payments, set <a href=\"/docs/api/customers/update#update_customer-invoice_settings-default_payment_method\"><code>invoice_settings.default_payment_method</code></a>, on the Customer to the PaymentMethod’s ID.</p>
  ///
  /// Parameters:
  ///
  /// * [String] paymentMethod (required):
  ///
  /// * [String] customer (required):
  ///   The ID of the customer to which to attach the PaymentMethod.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<PaymentMethod?> postPaymentMethodsPaymentMethodAttach(String paymentMethod, String customer, { List<String>? expand, }) async {
    final response = await postPaymentMethodsPaymentMethodAttachWithHttpInfo(paymentMethod, customer,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentMethod',) as PaymentMethod;
    
    }
    return null;
  }

  /// Detach a PaymentMethod from a Customer
  ///
  /// <p>Detaches a PaymentMethod object from a Customer. After a PaymentMethod is detached, it can no longer be used for a payment or re-attached to a Customer.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] paymentMethod (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postPaymentMethodsPaymentMethodDetachWithHttpInfo(String paymentMethod, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payment_methods/{payment_method}/detach'
      .replaceAll('{payment_method}', paymentMethod);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Detach a PaymentMethod from a Customer
  ///
  /// <p>Detaches a PaymentMethod object from a Customer. After a PaymentMethod is detached, it can no longer be used for a payment or re-attached to a Customer.</p>
  ///
  /// Parameters:
  ///
  /// * [String] paymentMethod (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<PaymentMethod?> postPaymentMethodsPaymentMethodDetach(String paymentMethod, { List<String>? expand, }) async {
    final response = await postPaymentMethodsPaymentMethodDetachWithHttpInfo(paymentMethod,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PaymentMethod',) as PaymentMethod;
    
    }
    return null;
  }

  /// Create a payout
  ///
  /// <p>To send funds to your own bank account, create a new payout object. Your <a href=\"#balance\">Stripe balance</a> must cover the payout amount. If it doesn’t, you receive an “Insufficient Funds” error.</p>  <p>If your API key is in test mode, money won’t actually be sent, though every other action occurs as if you’re in live mode.</p>  <p>If you create a manual payout on a Stripe account that uses multiple payment source types, you need to specify the source type balance that the payout draws from. The <a href=\"#balance_object\">balance object</a> details available and pending amounts by source type.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [int] amount (required):
  ///   A positive integer in cents representing how much to payout.
  ///
  /// * [String] currency (required):
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [String] destination:
  ///   The ID of a bank account or a card to send the payout to. If you don't provide a destination, we use the default external account for the specified currency.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] method:
  ///   The method used to send this payout, which is `standard` or `instant`. We support `instant` for payouts to debit cards and bank accounts in certain countries. Learn more about [bank support for Instant Payouts](https://stripe.com/docs/payouts/instant-payouts-banks).
  ///
  /// * [String] sourceType:
  ///   The balance type of your Stripe balance to draw this payout from. Balances for different payment sources are kept separately. You can find the amounts with the Balances API. One of `bank_account`, `card`, or `fpx`.
  ///
  /// * [String] statementDescriptor:
  ///   A string that displays on the recipient's bank or card statement (up to 22 characters). A `statement_descriptor` that's longer than 22 characters return an error. Most banks truncate this information and display it inconsistently. Some banks might not display it at all.
  Future<Response> postPayoutsWithHttpInfo(int amount, String currency, { String? description, String? destination, List<String>? expand, Map<String, String>? metadata, String? method, String? sourceType, String? statementDescriptor, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payouts';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (destination != null) {
      formParams[r'destination'] = parameterToString(destination);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (method != null) {
      formParams[r'method'] = parameterToString(method);
    }
    if (sourceType != null) {
      formParams[r'source_type'] = parameterToString(sourceType);
    }
    if (statementDescriptor != null) {
      formParams[r'statement_descriptor'] = parameterToString(statementDescriptor);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a payout
  ///
  /// <p>To send funds to your own bank account, create a new payout object. Your <a href=\"#balance\">Stripe balance</a> must cover the payout amount. If it doesn’t, you receive an “Insufficient Funds” error.</p>  <p>If your API key is in test mode, money won’t actually be sent, though every other action occurs as if you’re in live mode.</p>  <p>If you create a manual payout on a Stripe account that uses multiple payment source types, you need to specify the source type balance that the payout draws from. The <a href=\"#balance_object\">balance object</a> details available and pending amounts by source type.</p>
  ///
  /// Parameters:
  ///
  /// * [int] amount (required):
  ///   A positive integer in cents representing how much to payout.
  ///
  /// * [String] currency (required):
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [String] destination:
  ///   The ID of a bank account or a card to send the payout to. If you don't provide a destination, we use the default external account for the specified currency.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] method:
  ///   The method used to send this payout, which is `standard` or `instant`. We support `instant` for payouts to debit cards and bank accounts in certain countries. Learn more about [bank support for Instant Payouts](https://stripe.com/docs/payouts/instant-payouts-banks).
  ///
  /// * [String] sourceType:
  ///   The balance type of your Stripe balance to draw this payout from. Balances for different payment sources are kept separately. You can find the amounts with the Balances API. One of `bank_account`, `card`, or `fpx`.
  ///
  /// * [String] statementDescriptor:
  ///   A string that displays on the recipient's bank or card statement (up to 22 characters). A `statement_descriptor` that's longer than 22 characters return an error. Most banks truncate this information and display it inconsistently. Some banks might not display it at all.
  Future<Payout?> postPayouts(int amount, String currency, { String? description, String? destination, List<String>? expand, Map<String, String>? metadata, String? method, String? sourceType, String? statementDescriptor, }) async {
    final response = await postPayoutsWithHttpInfo(amount, currency,  description: description, destination: destination, expand: expand, metadata: metadata, method: method, sourceType: sourceType, statementDescriptor: statementDescriptor, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Payout',) as Payout;
    
    }
    return null;
  }

  /// Update a payout
  ///
  /// <p>Updates the specified payout by setting the values of the parameters you pass. We don’t change parameters that you don’t provide. This request only accepts the metadata as arguments.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] payout (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<Response> postPayoutsPayoutWithHttpInfo(String payout, { List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payouts/{payout}'
      .replaceAll('{payout}', payout);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a payout
  ///
  /// <p>Updates the specified payout by setting the values of the parameters you pass. We don’t change parameters that you don’t provide. This request only accepts the metadata as arguments.</p>
  ///
  /// Parameters:
  ///
  /// * [String] payout (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<Payout?> postPayoutsPayout(String payout, { List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    final response = await postPayoutsPayoutWithHttpInfo(payout,  expand: expand, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Payout',) as Payout;
    
    }
    return null;
  }

  /// Cancel a payout
  ///
  /// <p>You can cancel a previously created payout if its status is <code>pending</code>. Stripe refunds the funds to your available balance. You can’t cancel automatic Stripe payouts.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] payout (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postPayoutsPayoutCancelWithHttpInfo(String payout, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payouts/{payout}/cancel'
      .replaceAll('{payout}', payout);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Cancel a payout
  ///
  /// <p>You can cancel a previously created payout if its status is <code>pending</code>. Stripe refunds the funds to your available balance. You can’t cancel automatic Stripe payouts.</p>
  ///
  /// Parameters:
  ///
  /// * [String] payout (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Payout?> postPayoutsPayoutCancel(String payout, { List<String>? expand, }) async {
    final response = await postPayoutsPayoutCancelWithHttpInfo(payout,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Payout',) as Payout;
    
    }
    return null;
  }

  /// Reverse a payout
  ///
  /// <p>Reverses a payout by debiting the destination bank account. At this time, you can only reverse payouts for connected accounts to US bank accounts. If the payout is manual and in the <code>pending</code> status, use <code>/v1/payouts/:id/cancel</code> instead.</p>  <p>By requesting a reversal through <code>/v1/payouts/:id/reverse</code>, you confirm that the authorized signatory of the selected bank account authorizes the debit on the bank account and that no other authorization is required.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] payout (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  Future<Response> postPayoutsPayoutReverseWithHttpInfo(String payout, { List<String>? expand, Map<String, String>? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/payouts/{payout}/reverse'
      .replaceAll('{payout}', payout);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Reverse a payout
  ///
  /// <p>Reverses a payout by debiting the destination bank account. At this time, you can only reverse payouts for connected accounts to US bank accounts. If the payout is manual and in the <code>pending</code> status, use <code>/v1/payouts/:id/cancel</code> instead.</p>  <p>By requesting a reversal through <code>/v1/payouts/:id/reverse</code>, you confirm that the authorized signatory of the selected bank account authorizes the debit on the bank account and that no other authorization is required.</p>
  ///
  /// Parameters:
  ///
  /// * [String] payout (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  Future<Payout?> postPayoutsPayoutReverse(String payout, { List<String>? expand, Map<String, String>? metadata, }) async {
    final response = await postPayoutsPayoutReverseWithHttpInfo(payout,  expand: expand, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Payout',) as Payout;
    
    }
    return null;
  }

  /// Create a plan
  ///
  /// <p>You can now model subscriptions more flexibly using the <a href=\"#prices\">Prices API</a>. It replaces the Plans API and is backwards compatible to simplify your migration.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] currency (required):
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] interval (required):
  ///   Specifies billing frequency. Either `day`, `week`, `month` or `year`.
  ///
  /// * [bool] active:
  ///   Whether the plan is currently available for new subscriptions. Defaults to `true`.
  ///
  /// * [int] amount:
  ///   A positive integer in cents (or local equivalent) (or 0 for a free plan) representing how much to charge on a recurring basis.
  ///
  /// * [double] amountDecimal:
  ///   Same as `amount`, but accepts a decimal value with at most 12 decimal places. Only one of `amount` and `amount_decimal` can be set.
  ///
  /// * [String] billingScheme:
  ///   Describes how to compute the price per period. Either `per_unit` or `tiered`. `per_unit` indicates that the fixed amount (specified in `amount`) will be charged per unit in `quantity` (for plans with `usage_type=licensed`), or per unit of total usage (for plans with `usage_type=metered`). `tiered` indicates that the unit pricing will be computed using a tiering strategy as defined using the `tiers` and `tiers_mode` attributes.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] id:
  ///   An identifier randomly generated by Stripe. Used to identify this plan when subscribing a customer. You can optionally override this ID, but the ID must be unique across all plans in your Stripe account. You can, however, use the same plan ID in both live and test modes.
  ///
  /// * [int] intervalCount:
  ///   The number of intervals between subscription billings. For example, `interval=month` and `interval_count=3` bills every 3 months. Maximum of three years interval allowed (3 years, 36 months, or 156 weeks).
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] meter:
  ///   The meter tracking the usage of a metered price
  ///
  /// * [String] nickname:
  ///   A brief description of the plan, hidden from customers.
  ///
  /// * [PostPlansRequestProduct] product:
  ///
  /// * [List<Tier>] tiers:
  ///   Each element represents a pricing tier. This parameter requires `billing_scheme` to be set to `tiered`. See also the documentation for `billing_scheme`.
  ///
  /// * [String] tiersMode:
  ///   Defines if the tiering price should be `graduated` or `volume` based. In `volume`-based tiering, the maximum quantity within a period determines the per unit price, in `graduated` tiering pricing can successively change as the quantity grows.
  ///
  /// * [TransformUsageParam] transformUsage:
  ///
  /// * [int] trialPeriodDays:
  ///   Default number of trial days when subscribing a customer to this plan using [`trial_from_plan=true`](https://stripe.com/docs/api#create_subscription-trial_from_plan).
  ///
  /// * [String] usageType:
  ///   Configures how the quantity per period should be determined. Can be either `metered` or `licensed`. `licensed` automatically bills the `quantity` set when adding it to a subscription. `metered` aggregates the total usage based on usage records. Defaults to `licensed`.
  Future<Response> postPlansWithHttpInfo(String currency, String interval, { bool? active, int? amount, double? amountDecimal, String? billingScheme, List<String>? expand, String? id, int? intervalCount, PostAccountsRequestMetadata? metadata, String? meter, String? nickname, PostPlansRequestProduct? product, List<Tier>? tiers, String? tiersMode, TransformUsageParam? transformUsage, int? trialPeriodDays, String? usageType, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/plans';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (active != null) {
      formParams[r'active'] = parameterToString(active);
    }
    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (amountDecimal != null) {
      formParams[r'amount_decimal'] = parameterToString(amountDecimal);
    }
    if (billingScheme != null) {
      formParams[r'billing_scheme'] = parameterToString(billingScheme);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (id != null) {
      formParams[r'id'] = parameterToString(id);
    }
    if (interval != null) {
      formParams[r'interval'] = parameterToString(interval);
    }
    if (intervalCount != null) {
      formParams[r'interval_count'] = parameterToString(intervalCount);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (meter != null) {
      formParams[r'meter'] = parameterToString(meter);
    }
    if (nickname != null) {
      formParams[r'nickname'] = parameterToString(nickname);
    }
    if (product != null) {
      formParams[r'product'] = parameterToString(product);
    }
    if (tiers != null) {
      formParams[r'tiers'] = parameterToString(tiers);
    }
    if (tiersMode != null) {
      formParams[r'tiers_mode'] = parameterToString(tiersMode);
    }
    if (transformUsage != null) {
      formParams[r'transform_usage'] = parameterToString(transformUsage);
    }
    if (trialPeriodDays != null) {
      formParams[r'trial_period_days'] = parameterToString(trialPeriodDays);
    }
    if (usageType != null) {
      formParams[r'usage_type'] = parameterToString(usageType);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a plan
  ///
  /// <p>You can now model subscriptions more flexibly using the <a href=\"#prices\">Prices API</a>. It replaces the Plans API and is backwards compatible to simplify your migration.</p>
  ///
  /// Parameters:
  ///
  /// * [String] currency (required):
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] interval (required):
  ///   Specifies billing frequency. Either `day`, `week`, `month` or `year`.
  ///
  /// * [bool] active:
  ///   Whether the plan is currently available for new subscriptions. Defaults to `true`.
  ///
  /// * [int] amount:
  ///   A positive integer in cents (or local equivalent) (or 0 for a free plan) representing how much to charge on a recurring basis.
  ///
  /// * [double] amountDecimal:
  ///   Same as `amount`, but accepts a decimal value with at most 12 decimal places. Only one of `amount` and `amount_decimal` can be set.
  ///
  /// * [String] billingScheme:
  ///   Describes how to compute the price per period. Either `per_unit` or `tiered`. `per_unit` indicates that the fixed amount (specified in `amount`) will be charged per unit in `quantity` (for plans with `usage_type=licensed`), or per unit of total usage (for plans with `usage_type=metered`). `tiered` indicates that the unit pricing will be computed using a tiering strategy as defined using the `tiers` and `tiers_mode` attributes.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] id:
  ///   An identifier randomly generated by Stripe. Used to identify this plan when subscribing a customer. You can optionally override this ID, but the ID must be unique across all plans in your Stripe account. You can, however, use the same plan ID in both live and test modes.
  ///
  /// * [int] intervalCount:
  ///   The number of intervals between subscription billings. For example, `interval=month` and `interval_count=3` bills every 3 months. Maximum of three years interval allowed (3 years, 36 months, or 156 weeks).
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] meter:
  ///   The meter tracking the usage of a metered price
  ///
  /// * [String] nickname:
  ///   A brief description of the plan, hidden from customers.
  ///
  /// * [PostPlansRequestProduct] product:
  ///
  /// * [List<Tier>] tiers:
  ///   Each element represents a pricing tier. This parameter requires `billing_scheme` to be set to `tiered`. See also the documentation for `billing_scheme`.
  ///
  /// * [String] tiersMode:
  ///   Defines if the tiering price should be `graduated` or `volume` based. In `volume`-based tiering, the maximum quantity within a period determines the per unit price, in `graduated` tiering pricing can successively change as the quantity grows.
  ///
  /// * [TransformUsageParam] transformUsage:
  ///
  /// * [int] trialPeriodDays:
  ///   Default number of trial days when subscribing a customer to this plan using [`trial_from_plan=true`](https://stripe.com/docs/api#create_subscription-trial_from_plan).
  ///
  /// * [String] usageType:
  ///   Configures how the quantity per period should be determined. Can be either `metered` or `licensed`. `licensed` automatically bills the `quantity` set when adding it to a subscription. `metered` aggregates the total usage based on usage records. Defaults to `licensed`.
  Future<Plan?> postPlans(String currency, String interval, { bool? active, int? amount, double? amountDecimal, String? billingScheme, List<String>? expand, String? id, int? intervalCount, PostAccountsRequestMetadata? metadata, String? meter, String? nickname, PostPlansRequestProduct? product, List<Tier>? tiers, String? tiersMode, TransformUsageParam? transformUsage, int? trialPeriodDays, String? usageType, }) async {
    final response = await postPlansWithHttpInfo(currency, interval,  active: active, amount: amount, amountDecimal: amountDecimal, billingScheme: billingScheme, expand: expand, id: id, intervalCount: intervalCount, metadata: metadata, meter: meter, nickname: nickname, product: product, tiers: tiers, tiersMode: tiersMode, transformUsage: transformUsage, trialPeriodDays: trialPeriodDays, usageType: usageType, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Plan',) as Plan;
    
    }
    return null;
  }

  /// Update a plan
  ///
  /// <p>Updates the specified plan by setting the values of the parameters passed. Any parameters not provided are left unchanged. By design, you cannot change a plan’s ID, amount, currency, or billing cycle.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] plan (required):
  ///
  /// * [bool] active:
  ///   Whether the plan is currently available for new subscriptions.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] nickname:
  ///   A brief description of the plan, hidden from customers.
  ///
  /// * [String] product:
  ///   The product the plan belongs to. This cannot be changed once it has been used in a subscription or subscription schedule.
  ///
  /// * [int] trialPeriodDays:
  ///   Default number of trial days when subscribing a customer to this plan using [`trial_from_plan=true`](https://stripe.com/docs/api#create_subscription-trial_from_plan).
  Future<Response> postPlansPlanWithHttpInfo(String plan, { bool? active, List<String>? expand, PostAccountsRequestMetadata? metadata, String? nickname, String? product, int? trialPeriodDays, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/plans/{plan}'
      .replaceAll('{plan}', plan);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (active != null) {
      formParams[r'active'] = parameterToString(active);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (nickname != null) {
      formParams[r'nickname'] = parameterToString(nickname);
    }
    if (product != null) {
      formParams[r'product'] = parameterToString(product);
    }
    if (trialPeriodDays != null) {
      formParams[r'trial_period_days'] = parameterToString(trialPeriodDays);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a plan
  ///
  /// <p>Updates the specified plan by setting the values of the parameters passed. Any parameters not provided are left unchanged. By design, you cannot change a plan’s ID, amount, currency, or billing cycle.</p>
  ///
  /// Parameters:
  ///
  /// * [String] plan (required):
  ///
  /// * [bool] active:
  ///   Whether the plan is currently available for new subscriptions.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] nickname:
  ///   A brief description of the plan, hidden from customers.
  ///
  /// * [String] product:
  ///   The product the plan belongs to. This cannot be changed once it has been used in a subscription or subscription schedule.
  ///
  /// * [int] trialPeriodDays:
  ///   Default number of trial days when subscribing a customer to this plan using [`trial_from_plan=true`](https://stripe.com/docs/api#create_subscription-trial_from_plan).
  Future<Plan?> postPlansPlan(String plan, { bool? active, List<String>? expand, PostAccountsRequestMetadata? metadata, String? nickname, String? product, int? trialPeriodDays, }) async {
    final response = await postPlansPlanWithHttpInfo(plan,  active: active, expand: expand, metadata: metadata, nickname: nickname, product: product, trialPeriodDays: trialPeriodDays, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Plan',) as Plan;
    
    }
    return null;
  }

  /// Create a price
  ///
  /// <p>Creates a new <a href=\"https://docs.stripe.com/api/prices\">Price</a> for an existing <a href=\"https://docs.stripe.com/api/products\">Product</a>. The Price can be recurring or one-time.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] currency (required):
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [bool] active:
  ///   Whether the price can be used for new purchases. Defaults to `true`.
  ///
  /// * [String] billingScheme:
  ///   Describes how to compute the price per period. Either `per_unit` or `tiered`. `per_unit` indicates that the fixed amount (specified in `unit_amount` or `unit_amount_decimal`) will be charged per unit in `quantity` (for prices with `usage_type=licensed`), or per unit of total usage (for prices with `usage_type=metered`). `tiered` indicates that the unit pricing will be computed using a tiering strategy as defined using the `tiers` and `tiers_mode` attributes.
  ///
  /// * [Map<String, CurrencyOption3>] currencyOptions:
  ///   Prices defined in each available currency option. Each key must be a three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html) and a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [CustomUnitAmount2] customUnitAmount:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] lookupKey:
  ///   A lookup key used to retrieve prices dynamically from a static string. This may be up to 200 characters.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] nickname:
  ///   A brief description of the price, hidden from customers.
  ///
  /// * [String] product:
  ///   The ID of the [Product](https://docs.stripe.com/api/products) that this [Price](https://docs.stripe.com/api/prices) will belong to.
  ///
  /// * [InlineProductParams1] productData:
  ///
  /// * [Recurring1] recurring:
  ///
  /// * [String] taxBehavior:
  ///   Only required if a [default tax behavior](https://stripe.com/docs/tax/products-prices-tax-categories-tax-behavior#setting-a-default-tax-behavior-(recommended)) was not provided in the Stripe Tax settings. Specifies whether the price is considered inclusive of taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or `unspecified`. Once specified as either `inclusive` or `exclusive`, it cannot be changed.
  ///
  /// * [List<Tier>] tiers:
  ///   Each element represents a pricing tier. This parameter requires `billing_scheme` to be set to `tiered`. See also the documentation for `billing_scheme`.
  ///
  /// * [String] tiersMode:
  ///   Defines if the tiering price should be `graduated` or `volume` based. In `volume`-based tiering, the maximum quantity within a period determines the per unit price, in `graduated` tiering pricing can successively change as the quantity grows.
  ///
  /// * [bool] transferLookupKey:
  ///   If set to true, will atomically remove the lookup key from the existing price, and assign it to this price.
  ///
  /// * [TransformUsageParam] transformQuantity:
  ///
  /// * [int] unitAmount:
  ///   A positive integer in cents (or local equivalent) (or 0 for a free price) representing how much to charge. One of `unit_amount`, `unit_amount_decimal`, or `custom_unit_amount` is required, unless `billing_scheme=tiered`.
  ///
  /// * [double] unitAmountDecimal:
  ///   Same as `unit_amount`, but accepts a decimal value in cents (or local equivalent) with at most 12 decimal places. Only one of `unit_amount` and `unit_amount_decimal` can be set.
  Future<Response> postPricesWithHttpInfo(String currency, { bool? active, String? billingScheme, Map<String, CurrencyOption3>? currencyOptions, CustomUnitAmount2? customUnitAmount, List<String>? expand, String? lookupKey, Map<String, String>? metadata, String? nickname, String? product, InlineProductParams1? productData, Recurring1? recurring, String? taxBehavior, List<Tier>? tiers, String? tiersMode, bool? transferLookupKey, TransformUsageParam? transformQuantity, int? unitAmount, double? unitAmountDecimal, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/prices';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (active != null) {
      formParams[r'active'] = parameterToString(active);
    }
    if (billingScheme != null) {
      formParams[r'billing_scheme'] = parameterToString(billingScheme);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (currencyOptions != null) {
      formParams[r'currency_options'] = parameterToString(currencyOptions);
    }
    if (customUnitAmount != null) {
      formParams[r'custom_unit_amount'] = parameterToString(customUnitAmount);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (lookupKey != null) {
      formParams[r'lookup_key'] = parameterToString(lookupKey);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (nickname != null) {
      formParams[r'nickname'] = parameterToString(nickname);
    }
    if (product != null) {
      formParams[r'product'] = parameterToString(product);
    }
    if (productData != null) {
      formParams[r'product_data'] = parameterToString(productData);
    }
    if (recurring != null) {
      formParams[r'recurring'] = parameterToString(recurring);
    }
    if (taxBehavior != null) {
      formParams[r'tax_behavior'] = parameterToString(taxBehavior);
    }
    if (tiers != null) {
      formParams[r'tiers'] = parameterToString(tiers);
    }
    if (tiersMode != null) {
      formParams[r'tiers_mode'] = parameterToString(tiersMode);
    }
    if (transferLookupKey != null) {
      formParams[r'transfer_lookup_key'] = parameterToString(transferLookupKey);
    }
    if (transformQuantity != null) {
      formParams[r'transform_quantity'] = parameterToString(transformQuantity);
    }
    if (unitAmount != null) {
      formParams[r'unit_amount'] = parameterToString(unitAmount);
    }
    if (unitAmountDecimal != null) {
      formParams[r'unit_amount_decimal'] = parameterToString(unitAmountDecimal);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a price
  ///
  /// <p>Creates a new <a href=\"https://docs.stripe.com/api/prices\">Price</a> for an existing <a href=\"https://docs.stripe.com/api/products\">Product</a>. The Price can be recurring or one-time.</p>
  ///
  /// Parameters:
  ///
  /// * [String] currency (required):
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [bool] active:
  ///   Whether the price can be used for new purchases. Defaults to `true`.
  ///
  /// * [String] billingScheme:
  ///   Describes how to compute the price per period. Either `per_unit` or `tiered`. `per_unit` indicates that the fixed amount (specified in `unit_amount` or `unit_amount_decimal`) will be charged per unit in `quantity` (for prices with `usage_type=licensed`), or per unit of total usage (for prices with `usage_type=metered`). `tiered` indicates that the unit pricing will be computed using a tiering strategy as defined using the `tiers` and `tiers_mode` attributes.
  ///
  /// * [Map<String, CurrencyOption3>] currencyOptions:
  ///   Prices defined in each available currency option. Each key must be a three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html) and a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [CustomUnitAmount2] customUnitAmount:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] lookupKey:
  ///   A lookup key used to retrieve prices dynamically from a static string. This may be up to 200 characters.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] nickname:
  ///   A brief description of the price, hidden from customers.
  ///
  /// * [String] product:
  ///   The ID of the [Product](https://docs.stripe.com/api/products) that this [Price](https://docs.stripe.com/api/prices) will belong to.
  ///
  /// * [InlineProductParams1] productData:
  ///
  /// * [Recurring1] recurring:
  ///
  /// * [String] taxBehavior:
  ///   Only required if a [default tax behavior](https://stripe.com/docs/tax/products-prices-tax-categories-tax-behavior#setting-a-default-tax-behavior-(recommended)) was not provided in the Stripe Tax settings. Specifies whether the price is considered inclusive of taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or `unspecified`. Once specified as either `inclusive` or `exclusive`, it cannot be changed.
  ///
  /// * [List<Tier>] tiers:
  ///   Each element represents a pricing tier. This parameter requires `billing_scheme` to be set to `tiered`. See also the documentation for `billing_scheme`.
  ///
  /// * [String] tiersMode:
  ///   Defines if the tiering price should be `graduated` or `volume` based. In `volume`-based tiering, the maximum quantity within a period determines the per unit price, in `graduated` tiering pricing can successively change as the quantity grows.
  ///
  /// * [bool] transferLookupKey:
  ///   If set to true, will atomically remove the lookup key from the existing price, and assign it to this price.
  ///
  /// * [TransformUsageParam] transformQuantity:
  ///
  /// * [int] unitAmount:
  ///   A positive integer in cents (or local equivalent) (or 0 for a free price) representing how much to charge. One of `unit_amount`, `unit_amount_decimal`, or `custom_unit_amount` is required, unless `billing_scheme=tiered`.
  ///
  /// * [double] unitAmountDecimal:
  ///   Same as `unit_amount`, but accepts a decimal value in cents (or local equivalent) with at most 12 decimal places. Only one of `unit_amount` and `unit_amount_decimal` can be set.
  Future<Price?> postPrices(String currency, { bool? active, String? billingScheme, Map<String, CurrencyOption3>? currencyOptions, CustomUnitAmount2? customUnitAmount, List<String>? expand, String? lookupKey, Map<String, String>? metadata, String? nickname, String? product, InlineProductParams1? productData, Recurring1? recurring, String? taxBehavior, List<Tier>? tiers, String? tiersMode, bool? transferLookupKey, TransformUsageParam? transformQuantity, int? unitAmount, double? unitAmountDecimal, }) async {
    final response = await postPricesWithHttpInfo(currency,  active: active, billingScheme: billingScheme, currencyOptions: currencyOptions, customUnitAmount: customUnitAmount, expand: expand, lookupKey: lookupKey, metadata: metadata, nickname: nickname, product: product, productData: productData, recurring: recurring, taxBehavior: taxBehavior, tiers: tiers, tiersMode: tiersMode, transferLookupKey: transferLookupKey, transformQuantity: transformQuantity, unitAmount: unitAmount, unitAmountDecimal: unitAmountDecimal, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Price',) as Price;
    
    }
    return null;
  }

  /// Update a price
  ///
  /// <p>Updates the specified price by setting the values of the parameters passed. Any parameters not provided are left unchanged.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] price (required):
  ///
  /// * [bool] active:
  ///   Whether the price can be used for new purchases. Defaults to `true`.
  ///
  /// * [PostPricesPriceRequestCurrencyOptions] currencyOptions:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] lookupKey:
  ///   A lookup key used to retrieve prices dynamically from a static string. This may be up to 200 characters.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] nickname:
  ///   A brief description of the price, hidden from customers.
  ///
  /// * [String] taxBehavior:
  ///   Only required if a [default tax behavior](https://stripe.com/docs/tax/products-prices-tax-categories-tax-behavior#setting-a-default-tax-behavior-(recommended)) was not provided in the Stripe Tax settings. Specifies whether the price is considered inclusive of taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or `unspecified`. Once specified as either `inclusive` or `exclusive`, it cannot be changed.
  ///
  /// * [bool] transferLookupKey:
  ///   If set to true, will atomically remove the lookup key from the existing price, and assign it to this price.
  Future<Response> postPricesPriceWithHttpInfo(String price, { bool? active, PostPricesPriceRequestCurrencyOptions? currencyOptions, List<String>? expand, String? lookupKey, PostAccountsRequestMetadata? metadata, String? nickname, String? taxBehavior, bool? transferLookupKey, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/prices/{price}'
      .replaceAll('{price}', price);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (active != null) {
      formParams[r'active'] = parameterToString(active);
    }
    if (currencyOptions != null) {
      formParams[r'currency_options'] = parameterToString(currencyOptions);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (lookupKey != null) {
      formParams[r'lookup_key'] = parameterToString(lookupKey);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (nickname != null) {
      formParams[r'nickname'] = parameterToString(nickname);
    }
    if (taxBehavior != null) {
      formParams[r'tax_behavior'] = parameterToString(taxBehavior);
    }
    if (transferLookupKey != null) {
      formParams[r'transfer_lookup_key'] = parameterToString(transferLookupKey);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a price
  ///
  /// <p>Updates the specified price by setting the values of the parameters passed. Any parameters not provided are left unchanged.</p>
  ///
  /// Parameters:
  ///
  /// * [String] price (required):
  ///
  /// * [bool] active:
  ///   Whether the price can be used for new purchases. Defaults to `true`.
  ///
  /// * [PostPricesPriceRequestCurrencyOptions] currencyOptions:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] lookupKey:
  ///   A lookup key used to retrieve prices dynamically from a static string. This may be up to 200 characters.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] nickname:
  ///   A brief description of the price, hidden from customers.
  ///
  /// * [String] taxBehavior:
  ///   Only required if a [default tax behavior](https://stripe.com/docs/tax/products-prices-tax-categories-tax-behavior#setting-a-default-tax-behavior-(recommended)) was not provided in the Stripe Tax settings. Specifies whether the price is considered inclusive of taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or `unspecified`. Once specified as either `inclusive` or `exclusive`, it cannot be changed.
  ///
  /// * [bool] transferLookupKey:
  ///   If set to true, will atomically remove the lookup key from the existing price, and assign it to this price.
  Future<Price?> postPricesPrice(String price, { bool? active, PostPricesPriceRequestCurrencyOptions? currencyOptions, List<String>? expand, String? lookupKey, PostAccountsRequestMetadata? metadata, String? nickname, String? taxBehavior, bool? transferLookupKey, }) async {
    final response = await postPricesPriceWithHttpInfo(price,  active: active, currencyOptions: currencyOptions, expand: expand, lookupKey: lookupKey, metadata: metadata, nickname: nickname, taxBehavior: taxBehavior, transferLookupKey: transferLookupKey, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Price',) as Price;
    
    }
    return null;
  }

  /// Create a product
  ///
  /// <p>Creates a new product object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] name (required):
  ///   The product's name, meant to be displayable to the customer.
  ///
  /// * [bool] active:
  ///   Whether the product is currently available for purchase. Defaults to `true`.
  ///
  /// * [PriceDataWithoutProductWithMetadata] defaultPriceData:
  ///
  /// * [String] description:
  ///   The product's description, meant to be displayable to the customer. Use this field to optionally store a long form explanation of the product being sold for your own rendering purposes.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] id:
  ///   An identifier will be randomly generated by Stripe. You can optionally override this ID, but the ID must be unique across all products in your Stripe account.
  ///
  /// * [List<String>] images:
  ///   A list of up to 8 URLs of images for this product, meant to be displayable to the customer.
  ///
  /// * [List<Features>] marketingFeatures:
  ///   A list of up to 15 marketing features for this product. These are displayed in [pricing tables](https://stripe.com/docs/payments/checkout/pricing-table).
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [PackageDimensionsSpecs] packageDimensions:
  ///
  /// * [bool] shippable:
  ///   Whether this product is shipped (i.e., physical goods).
  ///
  /// * [String] statementDescriptor:
  ///   An arbitrary string to be displayed on your customer's credit card or bank statement. While most banks display this information consistently, some may display it incorrectly or not at all.  This may be up to 22 characters. The statement description may not include `<`, `>`, `\\\\`, `\\\"`, `'` characters, and will appear on your customer's statement in capital letters. Non-ASCII characters are automatically stripped.  It must contain at least one letter. Only used for subscription payments.
  ///
  /// * [String] taxCode:
  ///   A [tax code](https://stripe.com/docs/tax/tax-categories) ID.
  ///
  /// * [String] unitLabel:
  ///   A label that represents units of this product. When set, this will be included in customers' receipts, invoices, Checkout, and the customer portal.
  ///
  /// * [String] url:
  ///   A URL of a publicly-accessible webpage for this product.
  Future<Response> postProductsWithHttpInfo(String name, { bool? active, PriceDataWithoutProductWithMetadata? defaultPriceData, String? description, List<String>? expand, String? id, List<String>? images, List<Features>? marketingFeatures, Map<String, String>? metadata, PackageDimensionsSpecs? packageDimensions, bool? shippable, String? statementDescriptor, String? taxCode, String? unitLabel, String? url, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/products';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (active != null) {
      formParams[r'active'] = parameterToString(active);
    }
    if (defaultPriceData != null) {
      formParams[r'default_price_data'] = parameterToString(defaultPriceData);
    }
    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (id != null) {
      formParams[r'id'] = parameterToString(id);
    }
    if (images != null) {
      formParams[r'images'] = parameterToString(images);
    }
    if (marketingFeatures != null) {
      formParams[r'marketing_features'] = parameterToString(marketingFeatures);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (name != null) {
      formParams[r'name'] = parameterToString(name);
    }
    if (packageDimensions != null) {
      formParams[r'package_dimensions'] = parameterToString(packageDimensions);
    }
    if (shippable != null) {
      formParams[r'shippable'] = parameterToString(shippable);
    }
    if (statementDescriptor != null) {
      formParams[r'statement_descriptor'] = parameterToString(statementDescriptor);
    }
    if (taxCode != null) {
      formParams[r'tax_code'] = parameterToString(taxCode);
    }
    if (unitLabel != null) {
      formParams[r'unit_label'] = parameterToString(unitLabel);
    }
    if (url != null) {
      formParams[r'url'] = parameterToString(url);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a product
  ///
  /// <p>Creates a new product object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] name (required):
  ///   The product's name, meant to be displayable to the customer.
  ///
  /// * [bool] active:
  ///   Whether the product is currently available for purchase. Defaults to `true`.
  ///
  /// * [PriceDataWithoutProductWithMetadata] defaultPriceData:
  ///
  /// * [String] description:
  ///   The product's description, meant to be displayable to the customer. Use this field to optionally store a long form explanation of the product being sold for your own rendering purposes.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] id:
  ///   An identifier will be randomly generated by Stripe. You can optionally override this ID, but the ID must be unique across all products in your Stripe account.
  ///
  /// * [List<String>] images:
  ///   A list of up to 8 URLs of images for this product, meant to be displayable to the customer.
  ///
  /// * [List<Features>] marketingFeatures:
  ///   A list of up to 15 marketing features for this product. These are displayed in [pricing tables](https://stripe.com/docs/payments/checkout/pricing-table).
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [PackageDimensionsSpecs] packageDimensions:
  ///
  /// * [bool] shippable:
  ///   Whether this product is shipped (i.e., physical goods).
  ///
  /// * [String] statementDescriptor:
  ///   An arbitrary string to be displayed on your customer's credit card or bank statement. While most banks display this information consistently, some may display it incorrectly or not at all.  This may be up to 22 characters. The statement description may not include `<`, `>`, `\\\\`, `\\\"`, `'` characters, and will appear on your customer's statement in capital letters. Non-ASCII characters are automatically stripped.  It must contain at least one letter. Only used for subscription payments.
  ///
  /// * [String] taxCode:
  ///   A [tax code](https://stripe.com/docs/tax/tax-categories) ID.
  ///
  /// * [String] unitLabel:
  ///   A label that represents units of this product. When set, this will be included in customers' receipts, invoices, Checkout, and the customer portal.
  ///
  /// * [String] url:
  ///   A URL of a publicly-accessible webpage for this product.
  Future<Product?> postProducts(String name, { bool? active, PriceDataWithoutProductWithMetadata? defaultPriceData, String? description, List<String>? expand, String? id, List<String>? images, List<Features>? marketingFeatures, Map<String, String>? metadata, PackageDimensionsSpecs? packageDimensions, bool? shippable, String? statementDescriptor, String? taxCode, String? unitLabel, String? url, }) async {
    final response = await postProductsWithHttpInfo(name,  active: active, defaultPriceData: defaultPriceData, description: description, expand: expand, id: id, images: images, marketingFeatures: marketingFeatures, metadata: metadata, packageDimensions: packageDimensions, shippable: shippable, statementDescriptor: statementDescriptor, taxCode: taxCode, unitLabel: unitLabel, url: url, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Product',) as Product;
    
    }
    return null;
  }

  /// Update a product
  ///
  /// <p>Updates the specific product by setting the values of the parameters passed. Any parameters not provided will be left unchanged.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [bool] active:
  ///   Whether the product is available for purchase.
  ///
  /// * [String] defaultPrice:
  ///   The ID of the [Price](https://stripe.com/docs/api/prices) object that is the default price for this product.
  ///
  /// * [PostProductsIdRequestDescription] description:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostProductsIdRequestImages] images:
  ///
  /// * [PostProductsIdRequestMarketingFeatures] marketingFeatures:
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] name:
  ///   The product's name, meant to be displayable to the customer.
  ///
  /// * [PostProductsIdRequestPackageDimensions] packageDimensions:
  ///
  /// * [bool] shippable:
  ///   Whether this product is shipped (i.e., physical goods).
  ///
  /// * [String] statementDescriptor:
  ///   An arbitrary string to be displayed on your customer's credit card or bank statement. While most banks display this information consistently, some may display it incorrectly or not at all.  This may be up to 22 characters. The statement description may not include `<`, `>`, `\\\\`, `\\\"`, `'` characters, and will appear on your customer's statement in capital letters. Non-ASCII characters are automatically stripped.  It must contain at least one letter. May only be set if `type=service`. Only used for subscription payments.
  ///
  /// * [PostInvoiceitemsRequestTaxCode] taxCode:
  ///
  /// * [PostProductsIdRequestUnitLabel] unitLabel:
  ///
  /// * [PostProductsIdRequestUrl] url:
  Future<Response> postProductsIdWithHttpInfo(String id, { bool? active, String? defaultPrice, PostProductsIdRequestDescription? description, List<String>? expand, PostProductsIdRequestImages? images, PostProductsIdRequestMarketingFeatures? marketingFeatures, PostAccountsRequestMetadata? metadata, String? name, PostProductsIdRequestPackageDimensions? packageDimensions, bool? shippable, String? statementDescriptor, PostInvoiceitemsRequestTaxCode? taxCode, PostProductsIdRequestUnitLabel? unitLabel, PostProductsIdRequestUrl? url, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/products/{id}'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (active != null) {
      formParams[r'active'] = parameterToString(active);
    }
    if (defaultPrice != null) {
      formParams[r'default_price'] = parameterToString(defaultPrice);
    }
    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (images != null) {
      formParams[r'images'] = parameterToString(images);
    }
    if (marketingFeatures != null) {
      formParams[r'marketing_features'] = parameterToString(marketingFeatures);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (name != null) {
      formParams[r'name'] = parameterToString(name);
    }
    if (packageDimensions != null) {
      formParams[r'package_dimensions'] = parameterToString(packageDimensions);
    }
    if (shippable != null) {
      formParams[r'shippable'] = parameterToString(shippable);
    }
    if (statementDescriptor != null) {
      formParams[r'statement_descriptor'] = parameterToString(statementDescriptor);
    }
    if (taxCode != null) {
      formParams[r'tax_code'] = parameterToString(taxCode);
    }
    if (unitLabel != null) {
      formParams[r'unit_label'] = parameterToString(unitLabel);
    }
    if (url != null) {
      formParams[r'url'] = parameterToString(url);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a product
  ///
  /// <p>Updates the specific product by setting the values of the parameters passed. Any parameters not provided will be left unchanged.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [bool] active:
  ///   Whether the product is available for purchase.
  ///
  /// * [String] defaultPrice:
  ///   The ID of the [Price](https://stripe.com/docs/api/prices) object that is the default price for this product.
  ///
  /// * [PostProductsIdRequestDescription] description:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostProductsIdRequestImages] images:
  ///
  /// * [PostProductsIdRequestMarketingFeatures] marketingFeatures:
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] name:
  ///   The product's name, meant to be displayable to the customer.
  ///
  /// * [PostProductsIdRequestPackageDimensions] packageDimensions:
  ///
  /// * [bool] shippable:
  ///   Whether this product is shipped (i.e., physical goods).
  ///
  /// * [String] statementDescriptor:
  ///   An arbitrary string to be displayed on your customer's credit card or bank statement. While most banks display this information consistently, some may display it incorrectly or not at all.  This may be up to 22 characters. The statement description may not include `<`, `>`, `\\\\`, `\\\"`, `'` characters, and will appear on your customer's statement in capital letters. Non-ASCII characters are automatically stripped.  It must contain at least one letter. May only be set if `type=service`. Only used for subscription payments.
  ///
  /// * [PostInvoiceitemsRequestTaxCode] taxCode:
  ///
  /// * [PostProductsIdRequestUnitLabel] unitLabel:
  ///
  /// * [PostProductsIdRequestUrl] url:
  Future<Product?> postProductsId(String id, { bool? active, String? defaultPrice, PostProductsIdRequestDescription? description, List<String>? expand, PostProductsIdRequestImages? images, PostProductsIdRequestMarketingFeatures? marketingFeatures, PostAccountsRequestMetadata? metadata, String? name, PostProductsIdRequestPackageDimensions? packageDimensions, bool? shippable, String? statementDescriptor, PostInvoiceitemsRequestTaxCode? taxCode, PostProductsIdRequestUnitLabel? unitLabel, PostProductsIdRequestUrl? url, }) async {
    final response = await postProductsIdWithHttpInfo(id,  active: active, defaultPrice: defaultPrice, description: description, expand: expand, images: images, marketingFeatures: marketingFeatures, metadata: metadata, name: name, packageDimensions: packageDimensions, shippable: shippable, statementDescriptor: statementDescriptor, taxCode: taxCode, unitLabel: unitLabel, url: url, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Product',) as Product;
    
    }
    return null;
  }

  /// Attach a feature to a product
  ///
  /// <p>Creates a product_feature, which represents a feature attachment to a product</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] product (required):
  ///
  /// * [String] entitlementFeature (required):
  ///   The ID of the [Feature](https://stripe.com/docs/api/entitlements/feature) object attached to this product.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postProductsProductFeaturesWithHttpInfo(String product, String entitlementFeature, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/products/{product}/features'
      .replaceAll('{product}', product);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (entitlementFeature != null) {
      formParams[r'entitlement_feature'] = parameterToString(entitlementFeature);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Attach a feature to a product
  ///
  /// <p>Creates a product_feature, which represents a feature attachment to a product</p>
  ///
  /// Parameters:
  ///
  /// * [String] product (required):
  ///
  /// * [String] entitlementFeature (required):
  ///   The ID of the [Feature](https://stripe.com/docs/api/entitlements/feature) object attached to this product.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<ProductFeature?> postProductsProductFeatures(String product, String entitlementFeature, { List<String>? expand, }) async {
    final response = await postProductsProductFeaturesWithHttpInfo(product, entitlementFeature,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ProductFeature',) as ProductFeature;
    
    }
    return null;
  }

  /// Create a promotion code
  ///
  /// <p>A promotion code points to a coupon. You can optionally restrict the code to a specific customer, redemption limit, and expiration date.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] coupon (required):
  ///   The coupon for this promotion code.
  ///
  /// * [bool] active:
  ///   Whether the promotion code is currently active.
  ///
  /// * [String] code:
  ///   The customer-facing code. Regardless of case, this code must be unique across all active promotion codes for a specific customer. Valid characters are lower case letters (a-z), upper case letters (A-Z), and digits (0-9).  If left blank, we will generate one automatically.
  ///
  /// * [String] customer:
  ///   The customer that this promotion code can be used by. If not set, the promotion code can be used by all customers.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] expiresAt:
  ///   The timestamp at which this promotion code will expire. If the coupon has specified a `redeems_by`, then this value cannot be after the coupon's `redeems_by`.
  ///
  /// * [int] maxRedemptions:
  ///   A positive integer specifying the number of times the promotion code can be redeemed. If the coupon has specified a `max_redemptions`, then this value cannot be greater than the coupon's `max_redemptions`.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [RestrictionsParams2] restrictions:
  Future<Response> postPromotionCodesWithHttpInfo(String coupon, { bool? active, String? code, String? customer, List<String>? expand, int? expiresAt, int? maxRedemptions, Map<String, String>? metadata, RestrictionsParams2? restrictions, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/promotion_codes';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (active != null) {
      formParams[r'active'] = parameterToString(active);
    }
    if (code != null) {
      formParams[r'code'] = parameterToString(code);
    }
    if (coupon != null) {
      formParams[r'coupon'] = parameterToString(coupon);
    }
    if (customer != null) {
      formParams[r'customer'] = parameterToString(customer);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (expiresAt != null) {
      formParams[r'expires_at'] = parameterToString(expiresAt);
    }
    if (maxRedemptions != null) {
      formParams[r'max_redemptions'] = parameterToString(maxRedemptions);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (restrictions != null) {
      formParams[r'restrictions'] = parameterToString(restrictions);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a promotion code
  ///
  /// <p>A promotion code points to a coupon. You can optionally restrict the code to a specific customer, redemption limit, and expiration date.</p>
  ///
  /// Parameters:
  ///
  /// * [String] coupon (required):
  ///   The coupon for this promotion code.
  ///
  /// * [bool] active:
  ///   Whether the promotion code is currently active.
  ///
  /// * [String] code:
  ///   The customer-facing code. Regardless of case, this code must be unique across all active promotion codes for a specific customer. Valid characters are lower case letters (a-z), upper case letters (A-Z), and digits (0-9).  If left blank, we will generate one automatically.
  ///
  /// * [String] customer:
  ///   The customer that this promotion code can be used by. If not set, the promotion code can be used by all customers.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] expiresAt:
  ///   The timestamp at which this promotion code will expire. If the coupon has specified a `redeems_by`, then this value cannot be after the coupon's `redeems_by`.
  ///
  /// * [int] maxRedemptions:
  ///   A positive integer specifying the number of times the promotion code can be redeemed. If the coupon has specified a `max_redemptions`, then this value cannot be greater than the coupon's `max_redemptions`.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [RestrictionsParams2] restrictions:
  Future<PromotionCode?> postPromotionCodes(String coupon, { bool? active, String? code, String? customer, List<String>? expand, int? expiresAt, int? maxRedemptions, Map<String, String>? metadata, RestrictionsParams2? restrictions, }) async {
    final response = await postPromotionCodesWithHttpInfo(coupon,  active: active, code: code, customer: customer, expand: expand, expiresAt: expiresAt, maxRedemptions: maxRedemptions, metadata: metadata, restrictions: restrictions, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PromotionCode',) as PromotionCode;
    
    }
    return null;
  }

  /// Update a promotion code
  ///
  /// <p>Updates the specified promotion code by setting the values of the parameters passed. Most fields are, by design, not editable.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] promotionCode (required):
  ///
  /// * [bool] active:
  ///   Whether the promotion code is currently active. A promotion code can only be reactivated when the coupon is still valid and the promotion code is otherwise redeemable.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [RestrictionsParams3] restrictions:
  Future<Response> postPromotionCodesPromotionCodeWithHttpInfo(String promotionCode, { bool? active, List<String>? expand, PostAccountsRequestMetadata? metadata, RestrictionsParams3? restrictions, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/promotion_codes/{promotion_code}'
      .replaceAll('{promotion_code}', promotionCode);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (active != null) {
      formParams[r'active'] = parameterToString(active);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (restrictions != null) {
      formParams[r'restrictions'] = parameterToString(restrictions);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a promotion code
  ///
  /// <p>Updates the specified promotion code by setting the values of the parameters passed. Most fields are, by design, not editable.</p>
  ///
  /// Parameters:
  ///
  /// * [String] promotionCode (required):
  ///
  /// * [bool] active:
  ///   Whether the promotion code is currently active. A promotion code can only be reactivated when the coupon is still valid and the promotion code is otherwise redeemable.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [RestrictionsParams3] restrictions:
  Future<PromotionCode?> postPromotionCodesPromotionCode(String promotionCode, { bool? active, List<String>? expand, PostAccountsRequestMetadata? metadata, RestrictionsParams3? restrictions, }) async {
    final response = await postPromotionCodesPromotionCodeWithHttpInfo(promotionCode,  active: active, expand: expand, metadata: metadata, restrictions: restrictions, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'PromotionCode',) as PromotionCode;
    
    }
    return null;
  }

  /// Create a quote
  ///
  /// <p>A quote models prices and services for a customer. Default options for <code>header</code>, <code>description</code>, <code>footer</code>, and <code>expires_at</code> can be set in the dashboard via the <a href=\"https://dashboard.stripe.com/settings/billing/quote\">quote template</a>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [PostQuotesRequestApplicationFeeAmount] applicationFeeAmount:
  ///
  /// * [PostQuotesRequestApplicationFeePercent] applicationFeePercent:
  ///
  /// * [AutomaticTaxParam2] automaticTax:
  ///
  /// * [String] collectionMethod:
  ///   Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay invoices at the end of the subscription cycle or at invoice finalization using the default payment method attached to the subscription or customer. When sending an invoice, Stripe will email your customer an invoice with payment instructions and mark the subscription as `active`. Defaults to `charge_automatically`.
  ///
  /// * [String] customer:
  ///   The customer for which this quote belongs to. A customer is required before finalizing the quote. Once specified, it cannot be changed.
  ///
  /// * [PostQuotesRequestDefaultTaxRates] defaultTaxRates:
  ///
  /// * [PostQuotesRequestDescription] description:
  ///
  /// * [PostQuotesRequestDiscounts] discounts:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] expiresAt:
  ///   A future timestamp on which the quote will be canceled if in `open` or `draft` status. Measured in seconds since the Unix epoch. If no value is passed, the default expiration date configured in your [quote template settings](https://dashboard.stripe.com/settings/billing/quote) will be used.
  ///
  /// * [PostQuotesRequestFooter] footer:
  ///
  /// * [FromQuoteParams] fromQuote:
  ///
  /// * [PostQuotesRequestHeader] header:
  ///
  /// * [QuoteParam] invoiceSettings:
  ///
  /// * [List<LineItemCreateParams>] lineItems:
  ///   A list of line items the customer is being quoted for. Each line item includes information about the product, the quantity, and the resulting cost.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [PostQuotesRequestOnBehalfOf] onBehalfOf:
  ///
  /// * [SubscriptionDataCreateParams] subscriptionData:
  ///
  /// * [String] testClock:
  ///   ID of the test clock to attach to the quote.
  ///
  /// * [PostQuotesRequestTransferData] transferData:
  Future<Response> postQuotesWithHttpInfo({ PostQuotesRequestApplicationFeeAmount? applicationFeeAmount, PostQuotesRequestApplicationFeePercent? applicationFeePercent, AutomaticTaxParam2? automaticTax, String? collectionMethod, String? customer, PostQuotesRequestDefaultTaxRates? defaultTaxRates, PostQuotesRequestDescription? description, PostQuotesRequestDiscounts? discounts, List<String>? expand, int? expiresAt, PostQuotesRequestFooter? footer, FromQuoteParams? fromQuote, PostQuotesRequestHeader? header, QuoteParam? invoiceSettings, List<LineItemCreateParams>? lineItems, Map<String, String>? metadata, PostQuotesRequestOnBehalfOf? onBehalfOf, SubscriptionDataCreateParams? subscriptionData, String? testClock, PostQuotesRequestTransferData? transferData, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/quotes';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (applicationFeeAmount != null) {
      formParams[r'application_fee_amount'] = parameterToString(applicationFeeAmount);
    }
    if (applicationFeePercent != null) {
      formParams[r'application_fee_percent'] = parameterToString(applicationFeePercent);
    }
    if (automaticTax != null) {
      formParams[r'automatic_tax'] = parameterToString(automaticTax);
    }
    if (collectionMethod != null) {
      formParams[r'collection_method'] = parameterToString(collectionMethod);
    }
    if (customer != null) {
      formParams[r'customer'] = parameterToString(customer);
    }
    if (defaultTaxRates != null) {
      formParams[r'default_tax_rates'] = parameterToString(defaultTaxRates);
    }
    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (discounts != null) {
      formParams[r'discounts'] = parameterToString(discounts);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (expiresAt != null) {
      formParams[r'expires_at'] = parameterToString(expiresAt);
    }
    if (footer != null) {
      formParams[r'footer'] = parameterToString(footer);
    }
    if (fromQuote != null) {
      formParams[r'from_quote'] = parameterToString(fromQuote);
    }
    if (header != null) {
      formParams[r'header'] = parameterToString(header);
    }
    if (invoiceSettings != null) {
      formParams[r'invoice_settings'] = parameterToString(invoiceSettings);
    }
    if (lineItems != null) {
      formParams[r'line_items'] = parameterToString(lineItems);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (onBehalfOf != null) {
      formParams[r'on_behalf_of'] = parameterToString(onBehalfOf);
    }
    if (subscriptionData != null) {
      formParams[r'subscription_data'] = parameterToString(subscriptionData);
    }
    if (testClock != null) {
      formParams[r'test_clock'] = parameterToString(testClock);
    }
    if (transferData != null) {
      formParams[r'transfer_data'] = parameterToString(transferData);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a quote
  ///
  /// <p>A quote models prices and services for a customer. Default options for <code>header</code>, <code>description</code>, <code>footer</code>, and <code>expires_at</code> can be set in the dashboard via the <a href=\"https://dashboard.stripe.com/settings/billing/quote\">quote template</a>.</p>
  ///
  /// Parameters:
  ///
  /// * [PostQuotesRequestApplicationFeeAmount] applicationFeeAmount:
  ///
  /// * [PostQuotesRequestApplicationFeePercent] applicationFeePercent:
  ///
  /// * [AutomaticTaxParam2] automaticTax:
  ///
  /// * [String] collectionMethod:
  ///   Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay invoices at the end of the subscription cycle or at invoice finalization using the default payment method attached to the subscription or customer. When sending an invoice, Stripe will email your customer an invoice with payment instructions and mark the subscription as `active`. Defaults to `charge_automatically`.
  ///
  /// * [String] customer:
  ///   The customer for which this quote belongs to. A customer is required before finalizing the quote. Once specified, it cannot be changed.
  ///
  /// * [PostQuotesRequestDefaultTaxRates] defaultTaxRates:
  ///
  /// * [PostQuotesRequestDescription] description:
  ///
  /// * [PostQuotesRequestDiscounts] discounts:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] expiresAt:
  ///   A future timestamp on which the quote will be canceled if in `open` or `draft` status. Measured in seconds since the Unix epoch. If no value is passed, the default expiration date configured in your [quote template settings](https://dashboard.stripe.com/settings/billing/quote) will be used.
  ///
  /// * [PostQuotesRequestFooter] footer:
  ///
  /// * [FromQuoteParams] fromQuote:
  ///
  /// * [PostQuotesRequestHeader] header:
  ///
  /// * [QuoteParam] invoiceSettings:
  ///
  /// * [List<LineItemCreateParams>] lineItems:
  ///   A list of line items the customer is being quoted for. Each line item includes information about the product, the quantity, and the resulting cost.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [PostQuotesRequestOnBehalfOf] onBehalfOf:
  ///
  /// * [SubscriptionDataCreateParams] subscriptionData:
  ///
  /// * [String] testClock:
  ///   ID of the test clock to attach to the quote.
  ///
  /// * [PostQuotesRequestTransferData] transferData:
  Future<Quote?> postQuotes({ PostQuotesRequestApplicationFeeAmount? applicationFeeAmount, PostQuotesRequestApplicationFeePercent? applicationFeePercent, AutomaticTaxParam2? automaticTax, String? collectionMethod, String? customer, PostQuotesRequestDefaultTaxRates? defaultTaxRates, PostQuotesRequestDescription? description, PostQuotesRequestDiscounts? discounts, List<String>? expand, int? expiresAt, PostQuotesRequestFooter? footer, FromQuoteParams? fromQuote, PostQuotesRequestHeader? header, QuoteParam? invoiceSettings, List<LineItemCreateParams>? lineItems, Map<String, String>? metadata, PostQuotesRequestOnBehalfOf? onBehalfOf, SubscriptionDataCreateParams? subscriptionData, String? testClock, PostQuotesRequestTransferData? transferData, }) async {
    final response = await postQuotesWithHttpInfo( applicationFeeAmount: applicationFeeAmount, applicationFeePercent: applicationFeePercent, automaticTax: automaticTax, collectionMethod: collectionMethod, customer: customer, defaultTaxRates: defaultTaxRates, description: description, discounts: discounts, expand: expand, expiresAt: expiresAt, footer: footer, fromQuote: fromQuote, header: header, invoiceSettings: invoiceSettings, lineItems: lineItems, metadata: metadata, onBehalfOf: onBehalfOf, subscriptionData: subscriptionData, testClock: testClock, transferData: transferData, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Quote',) as Quote;
    
    }
    return null;
  }

  /// Update a quote
  ///
  /// <p>A quote models prices and services for a customer.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] quote (required):
  ///
  /// * [PostQuotesRequestApplicationFeeAmount] applicationFeeAmount:
  ///
  /// * [PostQuotesRequestApplicationFeePercent] applicationFeePercent:
  ///
  /// * [AutomaticTaxParam2] automaticTax:
  ///
  /// * [String] collectionMethod:
  ///   Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay invoices at the end of the subscription cycle or at invoice finalization using the default payment method attached to the subscription or customer. When sending an invoice, Stripe will email your customer an invoice with payment instructions and mark the subscription as `active`. Defaults to `charge_automatically`.
  ///
  /// * [String] customer:
  ///   The customer for which this quote belongs to. A customer is required before finalizing the quote. Once specified, it cannot be changed.
  ///
  /// * [PostQuotesRequestDefaultTaxRates] defaultTaxRates:
  ///
  /// * [PostQuotesQuoteRequestDescription] description:
  ///
  /// * [PostQuotesRequestDiscounts] discounts:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] expiresAt:
  ///   A future timestamp on which the quote will be canceled if in `open` or `draft` status. Measured in seconds since the Unix epoch.
  ///
  /// * [PostQuotesQuoteRequestFooter] footer:
  ///
  /// * [PostQuotesQuoteRequestHeader] header:
  ///
  /// * [QuoteParam] invoiceSettings:
  ///
  /// * [List<LineItemUpdateParams>] lineItems:
  ///   A list of line items the customer is being quoted for. Each line item includes information about the product, the quantity, and the resulting cost.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [PostQuotesRequestOnBehalfOf] onBehalfOf:
  ///
  /// * [SubscriptionDataUpdateParams1] subscriptionData:
  ///
  /// * [PostQuotesRequestTransferData] transferData:
  Future<Response> postQuotesQuoteWithHttpInfo(String quote, { PostQuotesRequestApplicationFeeAmount? applicationFeeAmount, PostQuotesRequestApplicationFeePercent? applicationFeePercent, AutomaticTaxParam2? automaticTax, String? collectionMethod, String? customer, PostQuotesRequestDefaultTaxRates? defaultTaxRates, PostQuotesQuoteRequestDescription? description, PostQuotesRequestDiscounts? discounts, List<String>? expand, int? expiresAt, PostQuotesQuoteRequestFooter? footer, PostQuotesQuoteRequestHeader? header, QuoteParam? invoiceSettings, List<LineItemUpdateParams>? lineItems, Map<String, String>? metadata, PostQuotesRequestOnBehalfOf? onBehalfOf, SubscriptionDataUpdateParams1? subscriptionData, PostQuotesRequestTransferData? transferData, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/quotes/{quote}'
      .replaceAll('{quote}', quote);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (applicationFeeAmount != null) {
      formParams[r'application_fee_amount'] = parameterToString(applicationFeeAmount);
    }
    if (applicationFeePercent != null) {
      formParams[r'application_fee_percent'] = parameterToString(applicationFeePercent);
    }
    if (automaticTax != null) {
      formParams[r'automatic_tax'] = parameterToString(automaticTax);
    }
    if (collectionMethod != null) {
      formParams[r'collection_method'] = parameterToString(collectionMethod);
    }
    if (customer != null) {
      formParams[r'customer'] = parameterToString(customer);
    }
    if (defaultTaxRates != null) {
      formParams[r'default_tax_rates'] = parameterToString(defaultTaxRates);
    }
    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (discounts != null) {
      formParams[r'discounts'] = parameterToString(discounts);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (expiresAt != null) {
      formParams[r'expires_at'] = parameterToString(expiresAt);
    }
    if (footer != null) {
      formParams[r'footer'] = parameterToString(footer);
    }
    if (header != null) {
      formParams[r'header'] = parameterToString(header);
    }
    if (invoiceSettings != null) {
      formParams[r'invoice_settings'] = parameterToString(invoiceSettings);
    }
    if (lineItems != null) {
      formParams[r'line_items'] = parameterToString(lineItems);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (onBehalfOf != null) {
      formParams[r'on_behalf_of'] = parameterToString(onBehalfOf);
    }
    if (subscriptionData != null) {
      formParams[r'subscription_data'] = parameterToString(subscriptionData);
    }
    if (transferData != null) {
      formParams[r'transfer_data'] = parameterToString(transferData);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a quote
  ///
  /// <p>A quote models prices and services for a customer.</p>
  ///
  /// Parameters:
  ///
  /// * [String] quote (required):
  ///
  /// * [PostQuotesRequestApplicationFeeAmount] applicationFeeAmount:
  ///
  /// * [PostQuotesRequestApplicationFeePercent] applicationFeePercent:
  ///
  /// * [AutomaticTaxParam2] automaticTax:
  ///
  /// * [String] collectionMethod:
  ///   Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay invoices at the end of the subscription cycle or at invoice finalization using the default payment method attached to the subscription or customer. When sending an invoice, Stripe will email your customer an invoice with payment instructions and mark the subscription as `active`. Defaults to `charge_automatically`.
  ///
  /// * [String] customer:
  ///   The customer for which this quote belongs to. A customer is required before finalizing the quote. Once specified, it cannot be changed.
  ///
  /// * [PostQuotesRequestDefaultTaxRates] defaultTaxRates:
  ///
  /// * [PostQuotesQuoteRequestDescription] description:
  ///
  /// * [PostQuotesRequestDiscounts] discounts:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] expiresAt:
  ///   A future timestamp on which the quote will be canceled if in `open` or `draft` status. Measured in seconds since the Unix epoch.
  ///
  /// * [PostQuotesQuoteRequestFooter] footer:
  ///
  /// * [PostQuotesQuoteRequestHeader] header:
  ///
  /// * [QuoteParam] invoiceSettings:
  ///
  /// * [List<LineItemUpdateParams>] lineItems:
  ///   A list of line items the customer is being quoted for. Each line item includes information about the product, the quantity, and the resulting cost.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [PostQuotesRequestOnBehalfOf] onBehalfOf:
  ///
  /// * [SubscriptionDataUpdateParams1] subscriptionData:
  ///
  /// * [PostQuotesRequestTransferData] transferData:
  Future<Quote?> postQuotesQuote(String quote, { PostQuotesRequestApplicationFeeAmount? applicationFeeAmount, PostQuotesRequestApplicationFeePercent? applicationFeePercent, AutomaticTaxParam2? automaticTax, String? collectionMethod, String? customer, PostQuotesRequestDefaultTaxRates? defaultTaxRates, PostQuotesQuoteRequestDescription? description, PostQuotesRequestDiscounts? discounts, List<String>? expand, int? expiresAt, PostQuotesQuoteRequestFooter? footer, PostQuotesQuoteRequestHeader? header, QuoteParam? invoiceSettings, List<LineItemUpdateParams>? lineItems, Map<String, String>? metadata, PostQuotesRequestOnBehalfOf? onBehalfOf, SubscriptionDataUpdateParams1? subscriptionData, PostQuotesRequestTransferData? transferData, }) async {
    final response = await postQuotesQuoteWithHttpInfo(quote,  applicationFeeAmount: applicationFeeAmount, applicationFeePercent: applicationFeePercent, automaticTax: automaticTax, collectionMethod: collectionMethod, customer: customer, defaultTaxRates: defaultTaxRates, description: description, discounts: discounts, expand: expand, expiresAt: expiresAt, footer: footer, header: header, invoiceSettings: invoiceSettings, lineItems: lineItems, metadata: metadata, onBehalfOf: onBehalfOf, subscriptionData: subscriptionData, transferData: transferData, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Quote',) as Quote;
    
    }
    return null;
  }

  /// Accept a quote
  ///
  /// <p>Accepts the specified quote.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] quote (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postQuotesQuoteAcceptWithHttpInfo(String quote, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/quotes/{quote}/accept'
      .replaceAll('{quote}', quote);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Accept a quote
  ///
  /// <p>Accepts the specified quote.</p>
  ///
  /// Parameters:
  ///
  /// * [String] quote (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Quote?> postQuotesQuoteAccept(String quote, { List<String>? expand, }) async {
    final response = await postQuotesQuoteAcceptWithHttpInfo(quote,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Quote',) as Quote;
    
    }
    return null;
  }

  /// Cancel a quote
  ///
  /// <p>Cancels the quote.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] quote (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postQuotesQuoteCancelWithHttpInfo(String quote, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/quotes/{quote}/cancel'
      .replaceAll('{quote}', quote);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Cancel a quote
  ///
  /// <p>Cancels the quote.</p>
  ///
  /// Parameters:
  ///
  /// * [String] quote (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Quote?> postQuotesQuoteCancel(String quote, { List<String>? expand, }) async {
    final response = await postQuotesQuoteCancelWithHttpInfo(quote,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Quote',) as Quote;
    
    }
    return null;
  }

  /// Finalize a quote
  ///
  /// <p>Finalizes the quote.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] quote (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] expiresAt:
  ///   A future timestamp on which the quote will be canceled if in `open` or `draft` status. Measured in seconds since the Unix epoch.
  Future<Response> postQuotesQuoteFinalizeWithHttpInfo(String quote, { List<String>? expand, int? expiresAt, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/quotes/{quote}/finalize'
      .replaceAll('{quote}', quote);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (expiresAt != null) {
      formParams[r'expires_at'] = parameterToString(expiresAt);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Finalize a quote
  ///
  /// <p>Finalizes the quote.</p>
  ///
  /// Parameters:
  ///
  /// * [String] quote (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] expiresAt:
  ///   A future timestamp on which the quote will be canceled if in `open` or `draft` status. Measured in seconds since the Unix epoch.
  Future<Quote?> postQuotesQuoteFinalize(String quote, { List<String>? expand, int? expiresAt, }) async {
    final response = await postQuotesQuoteFinalizeWithHttpInfo(quote,  expand: expand, expiresAt: expiresAt, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Quote',) as Quote;
    
    }
    return null;
  }

  /// Create a value list item
  ///
  /// <p>Creates a new <code>ValueListItem</code> object, which is added to the specified parent value list.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] value (required):
  ///   The value of the item (whose type must match the type of the parent value list).
  ///
  /// * [String] valueList (required):
  ///   The identifier of the value list which the created item will be added to.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postRadarValueListItemsWithHttpInfo(String value, String valueList, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/radar/value_list_items';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (value != null) {
      formParams[r'value'] = parameterToString(value);
    }
    if (valueList != null) {
      formParams[r'value_list'] = parameterToString(valueList);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a value list item
  ///
  /// <p>Creates a new <code>ValueListItem</code> object, which is added to the specified parent value list.</p>
  ///
  /// Parameters:
  ///
  /// * [String] value (required):
  ///   The value of the item (whose type must match the type of the parent value list).
  ///
  /// * [String] valueList (required):
  ///   The identifier of the value list which the created item will be added to.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<RadarValueListItem?> postRadarValueListItems(String value, String valueList, { List<String>? expand, }) async {
    final response = await postRadarValueListItemsWithHttpInfo(value, valueList,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'RadarValueListItem',) as RadarValueListItem;
    
    }
    return null;
  }

  /// Create a value list
  ///
  /// <p>Creates a new <code>ValueList</code> object, which can then be referenced in rules.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] alias (required):
  ///   The name of the value list for use in rules.
  ///
  /// * [String] name (required):
  ///   The human-readable name of the value list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] itemType:
  ///   Type of the items in the value list. One of `card_fingerprint`, `card_bin`, `email`, `ip_address`, `country`, `string`, `case_sensitive_string`, `customer_id`, `sepa_debit_fingerprint`, or `us_bank_account_fingerprint`. Use `string` if the item type is unknown or mixed.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  Future<Response> postRadarValueListsWithHttpInfo(String alias, String name, { List<String>? expand, String? itemType, Map<String, String>? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/radar/value_lists';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (alias != null) {
      formParams[r'alias'] = parameterToString(alias);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (itemType != null) {
      formParams[r'item_type'] = parameterToString(itemType);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (name != null) {
      formParams[r'name'] = parameterToString(name);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a value list
  ///
  /// <p>Creates a new <code>ValueList</code> object, which can then be referenced in rules.</p>
  ///
  /// Parameters:
  ///
  /// * [String] alias (required):
  ///   The name of the value list for use in rules.
  ///
  /// * [String] name (required):
  ///   The human-readable name of the value list.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] itemType:
  ///   Type of the items in the value list. One of `card_fingerprint`, `card_bin`, `email`, `ip_address`, `country`, `string`, `case_sensitive_string`, `customer_id`, `sepa_debit_fingerprint`, or `us_bank_account_fingerprint`. Use `string` if the item type is unknown or mixed.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  Future<RadarValueList?> postRadarValueLists(String alias, String name, { List<String>? expand, String? itemType, Map<String, String>? metadata, }) async {
    final response = await postRadarValueListsWithHttpInfo(alias, name,  expand: expand, itemType: itemType, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'RadarValueList',) as RadarValueList;
    
    }
    return null;
  }

  /// Update a value list
  ///
  /// <p>Updates a <code>ValueList</code> object by setting the values of the parameters passed. Any parameters not provided will be left unchanged. Note that <code>item_type</code> is immutable.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] valueList (required):
  ///
  /// * [String] alias:
  ///   The name of the value list for use in rules.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] name:
  ///   The human-readable name of the value list.
  Future<Response> postRadarValueListsValueListWithHttpInfo(String valueList, { String? alias, List<String>? expand, Map<String, String>? metadata, String? name, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/radar/value_lists/{value_list}'
      .replaceAll('{value_list}', valueList);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (alias != null) {
      formParams[r'alias'] = parameterToString(alias);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (name != null) {
      formParams[r'name'] = parameterToString(name);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a value list
  ///
  /// <p>Updates a <code>ValueList</code> object by setting the values of the parameters passed. Any parameters not provided will be left unchanged. Note that <code>item_type</code> is immutable.</p>
  ///
  /// Parameters:
  ///
  /// * [String] valueList (required):
  ///
  /// * [String] alias:
  ///   The name of the value list for use in rules.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] name:
  ///   The human-readable name of the value list.
  Future<RadarValueList?> postRadarValueListsValueList(String valueList, { String? alias, List<String>? expand, Map<String, String>? metadata, String? name, }) async {
    final response = await postRadarValueListsValueListWithHttpInfo(valueList,  alias: alias, expand: expand, metadata: metadata, name: name, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'RadarValueList',) as RadarValueList;
    
    }
    return null;
  }

  /// Create customer balance refund
  ///
  /// <p>When you create a new refund, you must specify a Charge or a PaymentIntent object on which to create it.</p>  <p>Creating a new refund will refund a charge that has previously been created but not yet refunded. Funds will be refunded to the credit or debit card that was originally charged.</p>  <p>You can optionally refund only part of a charge. You can do so multiple times, until the entire charge has been refunded.</p>  <p>Once entirely refunded, a charge can’t be refunded again. This method will raise an error when called on an already-refunded charge, or when trying to refund more money than is left on a charge.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [int] amount:
  ///
  /// * [String] charge:
  ///   The identifier of the charge to refund.
  ///
  /// * [String] currency:
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] customer:
  ///   Customer whose customer balance to refund from.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] instructionsEmail:
  ///   For payment methods without native refund support (e.g., Konbini, PromptPay), use this email from the customer to receive refund instructions.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] origin:
  ///   Origin of the refund
  ///
  /// * [String] paymentIntent:
  ///   The identifier of the PaymentIntent to refund.
  ///
  /// * [String] reason:
  ///   String indicating the reason for the refund. If set, possible values are `duplicate`, `fraudulent`, and `requested_by_customer`. If you believe the charge to be fraudulent, specifying `fraudulent` as the reason will add the associated card and email to your [block lists](https://stripe.com/docs/radar/lists), and will also help us improve our fraud detection algorithms.
  ///
  /// * [bool] refundApplicationFee:
  ///   Boolean indicating whether the application fee should be refunded when refunding this charge. If a full charge refund is given, the full application fee will be refunded. Otherwise, the application fee will be refunded in an amount proportional to the amount of the charge refunded. An application fee can be refunded only by the application that created the charge.
  ///
  /// * [bool] reverseTransfer:
  ///   Boolean indicating whether the transfer should be reversed when refunding this charge. The transfer will be reversed proportionally to the amount being refunded (either the entire or partial amount).<br><br>A transfer can be reversed only by the application that created the charge.
  Future<Response> postRefundsWithHttpInfo({ int? amount, String? charge, String? currency, String? customer, List<String>? expand, String? instructionsEmail, PostAccountsRequestMetadata? metadata, String? origin, String? paymentIntent, String? reason, bool? refundApplicationFee, bool? reverseTransfer, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/refunds';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (charge != null) {
      formParams[r'charge'] = parameterToString(charge);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (customer != null) {
      formParams[r'customer'] = parameterToString(customer);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (instructionsEmail != null) {
      formParams[r'instructions_email'] = parameterToString(instructionsEmail);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (origin != null) {
      formParams[r'origin'] = parameterToString(origin);
    }
    if (paymentIntent != null) {
      formParams[r'payment_intent'] = parameterToString(paymentIntent);
    }
    if (reason != null) {
      formParams[r'reason'] = parameterToString(reason);
    }
    if (refundApplicationFee != null) {
      formParams[r'refund_application_fee'] = parameterToString(refundApplicationFee);
    }
    if (reverseTransfer != null) {
      formParams[r'reverse_transfer'] = parameterToString(reverseTransfer);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create customer balance refund
  ///
  /// <p>When you create a new refund, you must specify a Charge or a PaymentIntent object on which to create it.</p>  <p>Creating a new refund will refund a charge that has previously been created but not yet refunded. Funds will be refunded to the credit or debit card that was originally charged.</p>  <p>You can optionally refund only part of a charge. You can do so multiple times, until the entire charge has been refunded.</p>  <p>Once entirely refunded, a charge can’t be refunded again. This method will raise an error when called on an already-refunded charge, or when trying to refund more money than is left on a charge.</p>
  ///
  /// Parameters:
  ///
  /// * [int] amount:
  ///
  /// * [String] charge:
  ///   The identifier of the charge to refund.
  ///
  /// * [String] currency:
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] customer:
  ///   Customer whose customer balance to refund from.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] instructionsEmail:
  ///   For payment methods without native refund support (e.g., Konbini, PromptPay), use this email from the customer to receive refund instructions.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] origin:
  ///   Origin of the refund
  ///
  /// * [String] paymentIntent:
  ///   The identifier of the PaymentIntent to refund.
  ///
  /// * [String] reason:
  ///   String indicating the reason for the refund. If set, possible values are `duplicate`, `fraudulent`, and `requested_by_customer`. If you believe the charge to be fraudulent, specifying `fraudulent` as the reason will add the associated card and email to your [block lists](https://stripe.com/docs/radar/lists), and will also help us improve our fraud detection algorithms.
  ///
  /// * [bool] refundApplicationFee:
  ///   Boolean indicating whether the application fee should be refunded when refunding this charge. If a full charge refund is given, the full application fee will be refunded. Otherwise, the application fee will be refunded in an amount proportional to the amount of the charge refunded. An application fee can be refunded only by the application that created the charge.
  ///
  /// * [bool] reverseTransfer:
  ///   Boolean indicating whether the transfer should be reversed when refunding this charge. The transfer will be reversed proportionally to the amount being refunded (either the entire or partial amount).<br><br>A transfer can be reversed only by the application that created the charge.
  Future<Refund?> postRefunds({ int? amount, String? charge, String? currency, String? customer, List<String>? expand, String? instructionsEmail, PostAccountsRequestMetadata? metadata, String? origin, String? paymentIntent, String? reason, bool? refundApplicationFee, bool? reverseTransfer, }) async {
    final response = await postRefundsWithHttpInfo( amount: amount, charge: charge, currency: currency, customer: customer, expand: expand, instructionsEmail: instructionsEmail, metadata: metadata, origin: origin, paymentIntent: paymentIntent, reason: reason, refundApplicationFee: refundApplicationFee, reverseTransfer: reverseTransfer, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Refund',) as Refund;
    
    }
    return null;
  }

  /// Update a refund
  ///
  /// <p>Updates the refund that you specify by setting the values of the passed parameters. Any parameters that you don’t provide remain unchanged.</p>  <p>This request only accepts <code>metadata</code> as an argument.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] refund (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<Response> postRefundsRefundWithHttpInfo(String refund, { List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/refunds/{refund}'
      .replaceAll('{refund}', refund);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a refund
  ///
  /// <p>Updates the refund that you specify by setting the values of the passed parameters. Any parameters that you don’t provide remain unchanged.</p>  <p>This request only accepts <code>metadata</code> as an argument.</p>
  ///
  /// Parameters:
  ///
  /// * [String] refund (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<Refund?> postRefundsRefund(String refund, { List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    final response = await postRefundsRefundWithHttpInfo(refund,  expand: expand, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Refund',) as Refund;
    
    }
    return null;
  }

  /// Cancel a refund
  ///
  /// <p>Cancels a refund with a status of <code>requires_action</code>.</p>  <p>You can’t cancel refunds in other states. Only refunds for payment methods that require customer action can enter the <code>requires_action</code> state.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] refund (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postRefundsRefundCancelWithHttpInfo(String refund, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/refunds/{refund}/cancel'
      .replaceAll('{refund}', refund);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Cancel a refund
  ///
  /// <p>Cancels a refund with a status of <code>requires_action</code>.</p>  <p>You can’t cancel refunds in other states. Only refunds for payment methods that require customer action can enter the <code>requires_action</code> state.</p>
  ///
  /// Parameters:
  ///
  /// * [String] refund (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Refund?> postRefundsRefundCancel(String refund, { List<String>? expand, }) async {
    final response = await postRefundsRefundCancelWithHttpInfo(refund,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Refund',) as Refund;
    
    }
    return null;
  }

  /// Create a Report Run
  ///
  /// <p>Creates a new object and begin running the report. (Certain report types require a <a href=\"https://stripe.com/docs/keys#test-live-modes\">live-mode API key</a>.)</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] reportType (required):
  ///   The ID of the [report type](https://stripe.com/docs/reporting/statements/api#report-types) to run, such as `\\\"balance.summary.1\\\"`.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [RunParameterSpecs] parameters:
  Future<Response> postReportingReportRunsWithHttpInfo(String reportType, { List<String>? expand, RunParameterSpecs? parameters, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/reporting/report_runs';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (parameters != null) {
      formParams[r'parameters'] = parameterToString(parameters);
    }
    if (reportType != null) {
      formParams[r'report_type'] = parameterToString(reportType);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a Report Run
  ///
  /// <p>Creates a new object and begin running the report. (Certain report types require a <a href=\"https://stripe.com/docs/keys#test-live-modes\">live-mode API key</a>.)</p>
  ///
  /// Parameters:
  ///
  /// * [String] reportType (required):
  ///   The ID of the [report type](https://stripe.com/docs/reporting/statements/api#report-types) to run, such as `\\\"balance.summary.1\\\"`.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [RunParameterSpecs] parameters:
  Future<ReportingReportRun?> postReportingReportRuns(String reportType, { List<String>? expand, RunParameterSpecs? parameters, }) async {
    final response = await postReportingReportRunsWithHttpInfo(reportType,  expand: expand, parameters: parameters, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ReportingReportRun',) as ReportingReportRun;
    
    }
    return null;
  }

  /// Approve a review
  ///
  /// <p>Approves a <code>Review</code> object, closing it and removing it from the list of reviews.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] review (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postReviewsReviewApproveWithHttpInfo(String review, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/reviews/{review}/approve'
      .replaceAll('{review}', review);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Approve a review
  ///
  /// <p>Approves a <code>Review</code> object, closing it and removing it from the list of reviews.</p>
  ///
  /// Parameters:
  ///
  /// * [String] review (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Review?> postReviewsReviewApprove(String review, { List<String>? expand, }) async {
    final response = await postReviewsReviewApproveWithHttpInfo(review,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Review',) as Review;
    
    }
    return null;
  }

  /// Create a SetupIntent
  ///
  /// <p>Creates a SetupIntent object.</p>  <p>After you create the SetupIntent, attach a payment method and <a href=\"/docs/api/setup_intents/confirm\">confirm</a> it to collect any required permissions to charge the payment method later.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [bool] attachToSelf:
  ///   If present, the SetupIntent's payment method will be attached to the in-context Stripe Account.  It can only be used for this Stripe Account’s own money movement flows like InboundTransfer and OutboundTransfers. It cannot be set to true when setting up a PaymentMethod for a Customer, and defaults to false when attaching a PaymentMethod to a Customer.
  ///
  /// * [AutomaticPaymentMethodsParam1] automaticPaymentMethods:
  ///
  /// * [bool] confirm:
  ///   Set to `true` to attempt to confirm this SetupIntent immediately. This parameter defaults to `false`. If a card is the attached payment method, you can provide a `return_url` in case further authentication is necessary.
  ///
  /// * [String] confirmationToken:
  ///   ID of the ConfirmationToken used to confirm this SetupIntent.  If the provided ConfirmationToken contains properties that are also being provided in this request, such as `payment_method`, then the values in this request will take precedence.
  ///
  /// * [String] customer:
  ///   ID of the Customer this SetupIntent belongs to, if one exists.  If present, the SetupIntent's payment method will be attached to the Customer on successful setup. Payment methods attached to other Customers cannot be used with this SetupIntent.
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [List<String>] flowDirections:
  ///   Indicates the directions of money movement for which this payment method is intended to be used.  Include `inbound` if you intend to use the payment method as the origin to pull funds from. Include `outbound` if you intend to use the payment method as the destination to send funds to. You can include both if you intend to use the payment method for both purposes.
  ///
  /// * [PostSetupIntentsRequestMandateData] mandateData:
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] onBehalfOf:
  ///   The Stripe account ID created for this SetupIntent.
  ///
  /// * [String] paymentMethod:
  ///   ID of the payment method (a PaymentMethod, Card, or saved Source object) to attach to this SetupIntent.
  ///
  /// * [String] paymentMethodConfiguration:
  ///   The ID of the [payment method configuration](https://stripe.com/docs/api/payment_method_configurations) to use with this SetupIntent.
  ///
  /// * [PaymentMethodDataParams1] paymentMethodData:
  ///
  /// * [PaymentMethodOptionsParam36] paymentMethodOptions:
  ///
  /// * [List<String>] paymentMethodTypes:
  ///   The list of payment method types (for example, card) that this SetupIntent can use. If you don't provide this, Stripe will dynamically show relevant payment methods from your [payment method settings](https://dashboard.stripe.com/settings/payment_methods). A list of valid payment method types can be found [here](https://docs.stripe.com/api/payment_methods/object#payment_method_object-type).
  ///
  /// * [String] returnUrl:
  ///   The URL to redirect your customer back to after they authenticate or cancel their payment on the payment method's app or site. To redirect to a mobile application, you can alternatively supply an application URI scheme. This parameter can only be used with [`confirm=true`](https://stripe.com/docs/api/setup_intents/create#create_setup_intent-confirm).
  ///
  /// * [SetupIntentSingleUseParams] singleUse:
  ///
  /// * [String] usage:
  ///   Indicates how the payment method is intended to be used in the future. If not provided, this value defaults to `off_session`.
  ///
  /// * [bool] useStripeSdk:
  ///   Set to `true` when confirming server-side and using Stripe.js, iOS, or Android client-side SDKs to handle the next actions.
  Future<Response> postSetupIntentsWithHttpInfo({ bool? attachToSelf, AutomaticPaymentMethodsParam1? automaticPaymentMethods, bool? confirm, String? confirmationToken, String? customer, String? description, List<String>? expand, List<String>? flowDirections, PostSetupIntentsRequestMandateData? mandateData, Map<String, String>? metadata, String? onBehalfOf, String? paymentMethod, String? paymentMethodConfiguration, PaymentMethodDataParams1? paymentMethodData, PaymentMethodOptionsParam36? paymentMethodOptions, List<String>? paymentMethodTypes, String? returnUrl, SetupIntentSingleUseParams? singleUse, String? usage, bool? useStripeSdk, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/setup_intents';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (attachToSelf != null) {
      formParams[r'attach_to_self'] = parameterToString(attachToSelf);
    }
    if (automaticPaymentMethods != null) {
      formParams[r'automatic_payment_methods'] = parameterToString(automaticPaymentMethods);
    }
    if (confirm != null) {
      formParams[r'confirm'] = parameterToString(confirm);
    }
    if (confirmationToken != null) {
      formParams[r'confirmation_token'] = parameterToString(confirmationToken);
    }
    if (customer != null) {
      formParams[r'customer'] = parameterToString(customer);
    }
    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (flowDirections != null) {
      formParams[r'flow_directions'] = parameterToString(flowDirections);
    }
    if (mandateData != null) {
      formParams[r'mandate_data'] = parameterToString(mandateData);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (onBehalfOf != null) {
      formParams[r'on_behalf_of'] = parameterToString(onBehalfOf);
    }
    if (paymentMethod != null) {
      formParams[r'payment_method'] = parameterToString(paymentMethod);
    }
    if (paymentMethodConfiguration != null) {
      formParams[r'payment_method_configuration'] = parameterToString(paymentMethodConfiguration);
    }
    if (paymentMethodData != null) {
      formParams[r'payment_method_data'] = parameterToString(paymentMethodData);
    }
    if (paymentMethodOptions != null) {
      formParams[r'payment_method_options'] = parameterToString(paymentMethodOptions);
    }
    if (paymentMethodTypes != null) {
      formParams[r'payment_method_types'] = parameterToString(paymentMethodTypes);
    }
    if (returnUrl != null) {
      formParams[r'return_url'] = parameterToString(returnUrl);
    }
    if (singleUse != null) {
      formParams[r'single_use'] = parameterToString(singleUse);
    }
    if (usage != null) {
      formParams[r'usage'] = parameterToString(usage);
    }
    if (useStripeSdk != null) {
      formParams[r'use_stripe_sdk'] = parameterToString(useStripeSdk);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a SetupIntent
  ///
  /// <p>Creates a SetupIntent object.</p>  <p>After you create the SetupIntent, attach a payment method and <a href=\"/docs/api/setup_intents/confirm\">confirm</a> it to collect any required permissions to charge the payment method later.</p>
  ///
  /// Parameters:
  ///
  /// * [bool] attachToSelf:
  ///   If present, the SetupIntent's payment method will be attached to the in-context Stripe Account.  It can only be used for this Stripe Account’s own money movement flows like InboundTransfer and OutboundTransfers. It cannot be set to true when setting up a PaymentMethod for a Customer, and defaults to false when attaching a PaymentMethod to a Customer.
  ///
  /// * [AutomaticPaymentMethodsParam1] automaticPaymentMethods:
  ///
  /// * [bool] confirm:
  ///   Set to `true` to attempt to confirm this SetupIntent immediately. This parameter defaults to `false`. If a card is the attached payment method, you can provide a `return_url` in case further authentication is necessary.
  ///
  /// * [String] confirmationToken:
  ///   ID of the ConfirmationToken used to confirm this SetupIntent.  If the provided ConfirmationToken contains properties that are also being provided in this request, such as `payment_method`, then the values in this request will take precedence.
  ///
  /// * [String] customer:
  ///   ID of the Customer this SetupIntent belongs to, if one exists.  If present, the SetupIntent's payment method will be attached to the Customer on successful setup. Payment methods attached to other Customers cannot be used with this SetupIntent.
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [List<String>] flowDirections:
  ///   Indicates the directions of money movement for which this payment method is intended to be used.  Include `inbound` if you intend to use the payment method as the origin to pull funds from. Include `outbound` if you intend to use the payment method as the destination to send funds to. You can include both if you intend to use the payment method for both purposes.
  ///
  /// * [PostSetupIntentsRequestMandateData] mandateData:
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] onBehalfOf:
  ///   The Stripe account ID created for this SetupIntent.
  ///
  /// * [String] paymentMethod:
  ///   ID of the payment method (a PaymentMethod, Card, or saved Source object) to attach to this SetupIntent.
  ///
  /// * [String] paymentMethodConfiguration:
  ///   The ID of the [payment method configuration](https://stripe.com/docs/api/payment_method_configurations) to use with this SetupIntent.
  ///
  /// * [PaymentMethodDataParams1] paymentMethodData:
  ///
  /// * [PaymentMethodOptionsParam36] paymentMethodOptions:
  ///
  /// * [List<String>] paymentMethodTypes:
  ///   The list of payment method types (for example, card) that this SetupIntent can use. If you don't provide this, Stripe will dynamically show relevant payment methods from your [payment method settings](https://dashboard.stripe.com/settings/payment_methods). A list of valid payment method types can be found [here](https://docs.stripe.com/api/payment_methods/object#payment_method_object-type).
  ///
  /// * [String] returnUrl:
  ///   The URL to redirect your customer back to after they authenticate or cancel their payment on the payment method's app or site. To redirect to a mobile application, you can alternatively supply an application URI scheme. This parameter can only be used with [`confirm=true`](https://stripe.com/docs/api/setup_intents/create#create_setup_intent-confirm).
  ///
  /// * [SetupIntentSingleUseParams] singleUse:
  ///
  /// * [String] usage:
  ///   Indicates how the payment method is intended to be used in the future. If not provided, this value defaults to `off_session`.
  ///
  /// * [bool] useStripeSdk:
  ///   Set to `true` when confirming server-side and using Stripe.js, iOS, or Android client-side SDKs to handle the next actions.
  Future<SetupIntent?> postSetupIntents({ bool? attachToSelf, AutomaticPaymentMethodsParam1? automaticPaymentMethods, bool? confirm, String? confirmationToken, String? customer, String? description, List<String>? expand, List<String>? flowDirections, PostSetupIntentsRequestMandateData? mandateData, Map<String, String>? metadata, String? onBehalfOf, String? paymentMethod, String? paymentMethodConfiguration, PaymentMethodDataParams1? paymentMethodData, PaymentMethodOptionsParam36? paymentMethodOptions, List<String>? paymentMethodTypes, String? returnUrl, SetupIntentSingleUseParams? singleUse, String? usage, bool? useStripeSdk, }) async {
    final response = await postSetupIntentsWithHttpInfo( attachToSelf: attachToSelf, automaticPaymentMethods: automaticPaymentMethods, confirm: confirm, confirmationToken: confirmationToken, customer: customer, description: description, expand: expand, flowDirections: flowDirections, mandateData: mandateData, metadata: metadata, onBehalfOf: onBehalfOf, paymentMethod: paymentMethod, paymentMethodConfiguration: paymentMethodConfiguration, paymentMethodData: paymentMethodData, paymentMethodOptions: paymentMethodOptions, paymentMethodTypes: paymentMethodTypes, returnUrl: returnUrl, singleUse: singleUse, usage: usage, useStripeSdk: useStripeSdk, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'SetupIntent',) as SetupIntent;
    
    }
    return null;
  }

  /// Update a SetupIntent
  ///
  /// <p>Updates a SetupIntent object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] intent (required):
  ///
  /// * [bool] attachToSelf:
  ///   If present, the SetupIntent's payment method will be attached to the in-context Stripe Account.  It can only be used for this Stripe Account’s own money movement flows like InboundTransfer and OutboundTransfers. It cannot be set to true when setting up a PaymentMethod for a Customer, and defaults to false when attaching a PaymentMethod to a Customer.
  ///
  /// * [String] customer:
  ///   ID of the Customer this SetupIntent belongs to, if one exists.  If present, the SetupIntent's payment method will be attached to the Customer on successful setup. Payment methods attached to other Customers cannot be used with this SetupIntent.
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [List<String>] flowDirections:
  ///   Indicates the directions of money movement for which this payment method is intended to be used.  Include `inbound` if you intend to use the payment method as the origin to pull funds from. Include `outbound` if you intend to use the payment method as the destination to send funds to. You can include both if you intend to use the payment method for both purposes.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] paymentMethod:
  ///   ID of the payment method (a PaymentMethod, Card, or saved Source object) to attach to this SetupIntent. To unset this field to null, pass in an empty string.
  ///
  /// * [String] paymentMethodConfiguration:
  ///   The ID of the [payment method configuration](https://stripe.com/docs/api/payment_method_configurations) to use with this SetupIntent.
  ///
  /// * [PaymentMethodDataParams1] paymentMethodData:
  ///
  /// * [PaymentMethodOptionsParam36] paymentMethodOptions:
  ///
  /// * [List<String>] paymentMethodTypes:
  ///   The list of payment method types (for example, card) that this SetupIntent can set up. If you don't provide this, Stripe will dynamically show relevant payment methods from your [payment method settings](https://dashboard.stripe.com/settings/payment_methods). A list of valid payment method types can be found [here](https://docs.stripe.com/api/payment_methods/object#payment_method_object-type).
  Future<Response> postSetupIntentsIntentWithHttpInfo(String intent, { bool? attachToSelf, String? customer, String? description, List<String>? expand, List<String>? flowDirections, PostAccountsRequestMetadata? metadata, String? paymentMethod, String? paymentMethodConfiguration, PaymentMethodDataParams1? paymentMethodData, PaymentMethodOptionsParam36? paymentMethodOptions, List<String>? paymentMethodTypes, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/setup_intents/{intent}'
      .replaceAll('{intent}', intent);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (attachToSelf != null) {
      formParams[r'attach_to_self'] = parameterToString(attachToSelf);
    }
    if (customer != null) {
      formParams[r'customer'] = parameterToString(customer);
    }
    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (flowDirections != null) {
      formParams[r'flow_directions'] = parameterToString(flowDirections);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (paymentMethod != null) {
      formParams[r'payment_method'] = parameterToString(paymentMethod);
    }
    if (paymentMethodConfiguration != null) {
      formParams[r'payment_method_configuration'] = parameterToString(paymentMethodConfiguration);
    }
    if (paymentMethodData != null) {
      formParams[r'payment_method_data'] = parameterToString(paymentMethodData);
    }
    if (paymentMethodOptions != null) {
      formParams[r'payment_method_options'] = parameterToString(paymentMethodOptions);
    }
    if (paymentMethodTypes != null) {
      formParams[r'payment_method_types'] = parameterToString(paymentMethodTypes);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a SetupIntent
  ///
  /// <p>Updates a SetupIntent object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] intent (required):
  ///
  /// * [bool] attachToSelf:
  ///   If present, the SetupIntent's payment method will be attached to the in-context Stripe Account.  It can only be used for this Stripe Account’s own money movement flows like InboundTransfer and OutboundTransfers. It cannot be set to true when setting up a PaymentMethod for a Customer, and defaults to false when attaching a PaymentMethod to a Customer.
  ///
  /// * [String] customer:
  ///   ID of the Customer this SetupIntent belongs to, if one exists.  If present, the SetupIntent's payment method will be attached to the Customer on successful setup. Payment methods attached to other Customers cannot be used with this SetupIntent.
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [List<String>] flowDirections:
  ///   Indicates the directions of money movement for which this payment method is intended to be used.  Include `inbound` if you intend to use the payment method as the origin to pull funds from. Include `outbound` if you intend to use the payment method as the destination to send funds to. You can include both if you intend to use the payment method for both purposes.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] paymentMethod:
  ///   ID of the payment method (a PaymentMethod, Card, or saved Source object) to attach to this SetupIntent. To unset this field to null, pass in an empty string.
  ///
  /// * [String] paymentMethodConfiguration:
  ///   The ID of the [payment method configuration](https://stripe.com/docs/api/payment_method_configurations) to use with this SetupIntent.
  ///
  /// * [PaymentMethodDataParams1] paymentMethodData:
  ///
  /// * [PaymentMethodOptionsParam36] paymentMethodOptions:
  ///
  /// * [List<String>] paymentMethodTypes:
  ///   The list of payment method types (for example, card) that this SetupIntent can set up. If you don't provide this, Stripe will dynamically show relevant payment methods from your [payment method settings](https://dashboard.stripe.com/settings/payment_methods). A list of valid payment method types can be found [here](https://docs.stripe.com/api/payment_methods/object#payment_method_object-type).
  Future<SetupIntent?> postSetupIntentsIntent(String intent, { bool? attachToSelf, String? customer, String? description, List<String>? expand, List<String>? flowDirections, PostAccountsRequestMetadata? metadata, String? paymentMethod, String? paymentMethodConfiguration, PaymentMethodDataParams1? paymentMethodData, PaymentMethodOptionsParam36? paymentMethodOptions, List<String>? paymentMethodTypes, }) async {
    final response = await postSetupIntentsIntentWithHttpInfo(intent,  attachToSelf: attachToSelf, customer: customer, description: description, expand: expand, flowDirections: flowDirections, metadata: metadata, paymentMethod: paymentMethod, paymentMethodConfiguration: paymentMethodConfiguration, paymentMethodData: paymentMethodData, paymentMethodOptions: paymentMethodOptions, paymentMethodTypes: paymentMethodTypes, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'SetupIntent',) as SetupIntent;
    
    }
    return null;
  }

  /// Cancel a SetupIntent
  ///
  /// <p>You can cancel a SetupIntent object when it’s in one of these statuses: <code>requires_payment_method</code>, <code>requires_confirmation</code>, or <code>requires_action</code>. </p>  <p>After you cancel it, setup is abandoned and any operations on the SetupIntent fail with an error. You can’t cancel the SetupIntent for a Checkout Session. <a href=\"/docs/api/checkout/sessions/expire\">Expire the Checkout Session</a> instead.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] intent (required):
  ///
  /// * [String] cancellationReason:
  ///   Reason for canceling this SetupIntent. Possible values are: `abandoned`, `requested_by_customer`, or `duplicate`
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postSetupIntentsIntentCancelWithHttpInfo(String intent, { String? cancellationReason, List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/setup_intents/{intent}/cancel'
      .replaceAll('{intent}', intent);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (cancellationReason != null) {
      formParams[r'cancellation_reason'] = parameterToString(cancellationReason);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Cancel a SetupIntent
  ///
  /// <p>You can cancel a SetupIntent object when it’s in one of these statuses: <code>requires_payment_method</code>, <code>requires_confirmation</code>, or <code>requires_action</code>. </p>  <p>After you cancel it, setup is abandoned and any operations on the SetupIntent fail with an error. You can’t cancel the SetupIntent for a Checkout Session. <a href=\"/docs/api/checkout/sessions/expire\">Expire the Checkout Session</a> instead.</p>
  ///
  /// Parameters:
  ///
  /// * [String] intent (required):
  ///
  /// * [String] cancellationReason:
  ///   Reason for canceling this SetupIntent. Possible values are: `abandoned`, `requested_by_customer`, or `duplicate`
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<SetupIntent?> postSetupIntentsIntentCancel(String intent, { String? cancellationReason, List<String>? expand, }) async {
    final response = await postSetupIntentsIntentCancelWithHttpInfo(intent,  cancellationReason: cancellationReason, expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'SetupIntent',) as SetupIntent;
    
    }
    return null;
  }

  /// Confirm a SetupIntent
  ///
  /// <p>Confirm that your customer intends to set up the current or provided payment method. For example, you would confirm a SetupIntent when a customer hits the “Save” button on a payment method management page on your website.</p>  <p>If the selected payment method does not require any additional steps from the customer, the SetupIntent will transition to the <code>succeeded</code> status.</p>  <p>Otherwise, it will transition to the <code>requires_action</code> status and suggest additional actions via <code>next_action</code>. If setup fails, the SetupIntent will transition to the <code>requires_payment_method</code> status or the <code>canceled</code> status if the confirmation limit is reached.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] intent (required):
  ///
  /// * [String] clientSecret:
  ///   The client secret of the SetupIntent.
  ///
  /// * [String] confirmationToken:
  ///   ID of the ConfirmationToken used to confirm this SetupIntent.  If the provided ConfirmationToken contains properties that are also being provided in this request, such as `payment_method`, then the values in this request will take precedence.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostPaymentIntentsIntentConfirmRequestMandateData] mandateData:
  ///
  /// * [String] paymentMethod:
  ///   ID of the payment method (a PaymentMethod, Card, or saved Source object) to attach to this SetupIntent.
  ///
  /// * [PaymentMethodDataParams1] paymentMethodData:
  ///
  /// * [PaymentMethodOptionsParam36] paymentMethodOptions:
  ///
  /// * [String] returnUrl:
  ///   The URL to redirect your customer back to after they authenticate on the payment method's app or site. If you'd prefer to redirect to a mobile application, you can alternatively supply an application URI scheme. This parameter is only used for cards and other redirect-based payment methods.
  ///
  /// * [bool] useStripeSdk:
  ///   Set to `true` when confirming server-side and using Stripe.js, iOS, or Android client-side SDKs to handle the next actions.
  Future<Response> postSetupIntentsIntentConfirmWithHttpInfo(String intent, { String? clientSecret, String? confirmationToken, List<String>? expand, PostPaymentIntentsIntentConfirmRequestMandateData? mandateData, String? paymentMethod, PaymentMethodDataParams1? paymentMethodData, PaymentMethodOptionsParam36? paymentMethodOptions, String? returnUrl, bool? useStripeSdk, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/setup_intents/{intent}/confirm'
      .replaceAll('{intent}', intent);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (clientSecret != null) {
      formParams[r'client_secret'] = parameterToString(clientSecret);
    }
    if (confirmationToken != null) {
      formParams[r'confirmation_token'] = parameterToString(confirmationToken);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (mandateData != null) {
      formParams[r'mandate_data'] = parameterToString(mandateData);
    }
    if (paymentMethod != null) {
      formParams[r'payment_method'] = parameterToString(paymentMethod);
    }
    if (paymentMethodData != null) {
      formParams[r'payment_method_data'] = parameterToString(paymentMethodData);
    }
    if (paymentMethodOptions != null) {
      formParams[r'payment_method_options'] = parameterToString(paymentMethodOptions);
    }
    if (returnUrl != null) {
      formParams[r'return_url'] = parameterToString(returnUrl);
    }
    if (useStripeSdk != null) {
      formParams[r'use_stripe_sdk'] = parameterToString(useStripeSdk);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Confirm a SetupIntent
  ///
  /// <p>Confirm that your customer intends to set up the current or provided payment method. For example, you would confirm a SetupIntent when a customer hits the “Save” button on a payment method management page on your website.</p>  <p>If the selected payment method does not require any additional steps from the customer, the SetupIntent will transition to the <code>succeeded</code> status.</p>  <p>Otherwise, it will transition to the <code>requires_action</code> status and suggest additional actions via <code>next_action</code>. If setup fails, the SetupIntent will transition to the <code>requires_payment_method</code> status or the <code>canceled</code> status if the confirmation limit is reached.</p>
  ///
  /// Parameters:
  ///
  /// * [String] intent (required):
  ///
  /// * [String] clientSecret:
  ///   The client secret of the SetupIntent.
  ///
  /// * [String] confirmationToken:
  ///   ID of the ConfirmationToken used to confirm this SetupIntent.  If the provided ConfirmationToken contains properties that are also being provided in this request, such as `payment_method`, then the values in this request will take precedence.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostPaymentIntentsIntentConfirmRequestMandateData] mandateData:
  ///
  /// * [String] paymentMethod:
  ///   ID of the payment method (a PaymentMethod, Card, or saved Source object) to attach to this SetupIntent.
  ///
  /// * [PaymentMethodDataParams1] paymentMethodData:
  ///
  /// * [PaymentMethodOptionsParam36] paymentMethodOptions:
  ///
  /// * [String] returnUrl:
  ///   The URL to redirect your customer back to after they authenticate on the payment method's app or site. If you'd prefer to redirect to a mobile application, you can alternatively supply an application URI scheme. This parameter is only used for cards and other redirect-based payment methods.
  ///
  /// * [bool] useStripeSdk:
  ///   Set to `true` when confirming server-side and using Stripe.js, iOS, or Android client-side SDKs to handle the next actions.
  Future<SetupIntent?> postSetupIntentsIntentConfirm(String intent, { String? clientSecret, String? confirmationToken, List<String>? expand, PostPaymentIntentsIntentConfirmRequestMandateData? mandateData, String? paymentMethod, PaymentMethodDataParams1? paymentMethodData, PaymentMethodOptionsParam36? paymentMethodOptions, String? returnUrl, bool? useStripeSdk, }) async {
    final response = await postSetupIntentsIntentConfirmWithHttpInfo(intent,  clientSecret: clientSecret, confirmationToken: confirmationToken, expand: expand, mandateData: mandateData, paymentMethod: paymentMethod, paymentMethodData: paymentMethodData, paymentMethodOptions: paymentMethodOptions, returnUrl: returnUrl, useStripeSdk: useStripeSdk, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'SetupIntent',) as SetupIntent;
    
    }
    return null;
  }

  /// Verify microdeposits on a SetupIntent
  ///
  /// <p>Verifies microdeposits on a SetupIntent object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] intent (required):
  ///
  /// * [List<int>] amounts:
  ///   Two positive integers, in *cents*, equal to the values of the microdeposits sent to the bank account.
  ///
  /// * [String] clientSecret:
  ///   The client secret of the SetupIntent.
  ///
  /// * [String] descriptorCode:
  ///   A six-character code starting with SM present in the microdeposit sent to the bank account.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postSetupIntentsIntentVerifyMicrodepositsWithHttpInfo(String intent, { List<int>? amounts, String? clientSecret, String? descriptorCode, List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/setup_intents/{intent}/verify_microdeposits'
      .replaceAll('{intent}', intent);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amounts != null) {
      formParams[r'amounts'] = parameterToString(amounts);
    }
    if (clientSecret != null) {
      formParams[r'client_secret'] = parameterToString(clientSecret);
    }
    if (descriptorCode != null) {
      formParams[r'descriptor_code'] = parameterToString(descriptorCode);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Verify microdeposits on a SetupIntent
  ///
  /// <p>Verifies microdeposits on a SetupIntent object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] intent (required):
  ///
  /// * [List<int>] amounts:
  ///   Two positive integers, in *cents*, equal to the values of the microdeposits sent to the bank account.
  ///
  /// * [String] clientSecret:
  ///   The client secret of the SetupIntent.
  ///
  /// * [String] descriptorCode:
  ///   A six-character code starting with SM present in the microdeposit sent to the bank account.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<SetupIntent?> postSetupIntentsIntentVerifyMicrodeposits(String intent, { List<int>? amounts, String? clientSecret, String? descriptorCode, List<String>? expand, }) async {
    final response = await postSetupIntentsIntentVerifyMicrodepositsWithHttpInfo(intent,  amounts: amounts, clientSecret: clientSecret, descriptorCode: descriptorCode, expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'SetupIntent',) as SetupIntent;
    
    }
    return null;
  }

  /// Create a shipping rate
  ///
  /// <p>Creates a new shipping rate object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] displayName (required):
  ///   The name of the shipping rate, meant to be displayable to the customer. This will appear on CheckoutSessions.
  ///
  /// * [DeliveryEstimate1] deliveryEstimate:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [FixedAmount1] fixedAmount:
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] taxBehavior:
  ///   Specifies whether the rate is considered inclusive of taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or `unspecified`.
  ///
  /// * [String] taxCode:
  ///   A [tax code](https://stripe.com/docs/tax/tax-categories) ID. The Shipping tax code is `txcd_92010001`.
  ///
  /// * [String] type:
  ///   The type of calculation to use on the shipping rate.
  Future<Response> postShippingRatesWithHttpInfo(String displayName, { DeliveryEstimate1? deliveryEstimate, List<String>? expand, FixedAmount1? fixedAmount, Map<String, String>? metadata, String? taxBehavior, String? taxCode, String? type, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/shipping_rates';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (deliveryEstimate != null) {
      formParams[r'delivery_estimate'] = parameterToString(deliveryEstimate);
    }
    if (displayName != null) {
      formParams[r'display_name'] = parameterToString(displayName);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (fixedAmount != null) {
      formParams[r'fixed_amount'] = parameterToString(fixedAmount);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (taxBehavior != null) {
      formParams[r'tax_behavior'] = parameterToString(taxBehavior);
    }
    if (taxCode != null) {
      formParams[r'tax_code'] = parameterToString(taxCode);
    }
    if (type != null) {
      formParams[r'type'] = parameterToString(type);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a shipping rate
  ///
  /// <p>Creates a new shipping rate object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] displayName (required):
  ///   The name of the shipping rate, meant to be displayable to the customer. This will appear on CheckoutSessions.
  ///
  /// * [DeliveryEstimate1] deliveryEstimate:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [FixedAmount1] fixedAmount:
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] taxBehavior:
  ///   Specifies whether the rate is considered inclusive of taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or `unspecified`.
  ///
  /// * [String] taxCode:
  ///   A [tax code](https://stripe.com/docs/tax/tax-categories) ID. The Shipping tax code is `txcd_92010001`.
  ///
  /// * [String] type:
  ///   The type of calculation to use on the shipping rate.
  Future<ShippingRate?> postShippingRates(String displayName, { DeliveryEstimate1? deliveryEstimate, List<String>? expand, FixedAmount1? fixedAmount, Map<String, String>? metadata, String? taxBehavior, String? taxCode, String? type, }) async {
    final response = await postShippingRatesWithHttpInfo(displayName,  deliveryEstimate: deliveryEstimate, expand: expand, fixedAmount: fixedAmount, metadata: metadata, taxBehavior: taxBehavior, taxCode: taxCode, type: type, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ShippingRate',) as ShippingRate;
    
    }
    return null;
  }

  /// Update a shipping rate
  ///
  /// <p>Updates an existing shipping rate object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] shippingRateToken (required):
  ///
  /// * [bool] active:
  ///   Whether the shipping rate can be used for new purchases. Defaults to `true`.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [FixedAmountUpdate] fixedAmount:
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] taxBehavior:
  ///   Specifies whether the rate is considered inclusive of taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or `unspecified`.
  Future<Response> postShippingRatesShippingRateTokenWithHttpInfo(String shippingRateToken, { bool? active, List<String>? expand, FixedAmountUpdate? fixedAmount, PostAccountsRequestMetadata? metadata, String? taxBehavior, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/shipping_rates/{shipping_rate_token}'
      .replaceAll('{shipping_rate_token}', shippingRateToken);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (active != null) {
      formParams[r'active'] = parameterToString(active);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (fixedAmount != null) {
      formParams[r'fixed_amount'] = parameterToString(fixedAmount);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (taxBehavior != null) {
      formParams[r'tax_behavior'] = parameterToString(taxBehavior);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a shipping rate
  ///
  /// <p>Updates an existing shipping rate object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] shippingRateToken (required):
  ///
  /// * [bool] active:
  ///   Whether the shipping rate can be used for new purchases. Defaults to `true`.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [FixedAmountUpdate] fixedAmount:
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] taxBehavior:
  ///   Specifies whether the rate is considered inclusive of taxes or exclusive of taxes. One of `inclusive`, `exclusive`, or `unspecified`.
  Future<ShippingRate?> postShippingRatesShippingRateToken(String shippingRateToken, { bool? active, List<String>? expand, FixedAmountUpdate? fixedAmount, PostAccountsRequestMetadata? metadata, String? taxBehavior, }) async {
    final response = await postShippingRatesShippingRateTokenWithHttpInfo(shippingRateToken,  active: active, expand: expand, fixedAmount: fixedAmount, metadata: metadata, taxBehavior: taxBehavior, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ShippingRate',) as ShippingRate;
    
    }
    return null;
  }

  /// Update an existing Sigma Query
  ///
  /// <p>Update an existing Sigma query that previously exists</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///   The `id` of the saved query to update. This should be a valid `id` that was previously created.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] name:
  ///   The name of the query to update.
  ///
  /// * [String] sql:
  ///   The sql statement to update the specified query statement with. This should be a valid Trino SQL statement that can be run in Sigma.
  Future<Response> postSigmaSavedQueriesIdWithHttpInfo(String id, { List<String>? expand, String? name, String? sql, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/sigma/saved_queries/{id}'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (name != null) {
      formParams[r'name'] = parameterToString(name);
    }
    if (sql != null) {
      formParams[r'sql'] = parameterToString(sql);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update an existing Sigma Query
  ///
  /// <p>Update an existing Sigma query that previously exists</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///   The `id` of the saved query to update. This should be a valid `id` that was previously created.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] name:
  ///   The name of the query to update.
  ///
  /// * [String] sql:
  ///   The sql statement to update the specified query statement with. This should be a valid Trino SQL statement that can be run in Sigma.
  Future<SigmaSigmaApiQuery?> postSigmaSavedQueriesId(String id, { List<String>? expand, String? name, String? sql, }) async {
    final response = await postSigmaSavedQueriesIdWithHttpInfo(id,  expand: expand, name: name, sql: sql, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'SigmaSigmaApiQuery',) as SigmaSigmaApiQuery;
    
    }
    return null;
  }

  /// Shares a source
  ///
  /// <p>Creates a new source object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [int] amount:
  ///   Amount associated with the source. This is the amount for which the source will be chargeable once ready. Required for `single_use` sources. Not supported for `receiver` type sources, where charge amount may not be specified until funds land.
  ///
  /// * [String] currency:
  ///   Three-letter [ISO code for the currency](https://stripe.com/docs/currencies) associated with the source. This is the currency for which the source will be chargeable once ready.
  ///
  /// * [String] customer:
  ///   The `Customer` to whom the original source is attached to. Must be set when the original source is not a `Source` (e.g., `Card`).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] flow:
  ///   The authentication `flow` of the source to create. `flow` is one of `redirect`, `receiver`, `code_verification`, `none`. It is generally inferred unless a type supports multiple flows.
  ///
  /// * [MandateParams] mandate:
  ///
  /// * [Map<String, String>] metadata:
  ///
  /// * [String] originalSource:
  ///   The source to share.
  ///
  /// * [Owner1] owner:
  ///
  /// * [ReceiverParams] receiver:
  ///
  /// * [RedirectParams] redirect:
  ///
  /// * [ShallowOrderSpecs] sourceOrder:
  ///
  /// * [String] statementDescriptor:
  ///   An arbitrary string to be displayed on your customer's statement. As an example, if your website is `RunClub` and the item you're charging for is a race ticket, you may want to specify a `statement_descriptor` of `RunClub 5K race ticket.` While many payment types will display this information, some may not display it at all.
  ///
  /// * [String] token:
  ///   An optional token used to create the source. When passed, token properties will override source parameters.
  ///
  /// * [String] type:
  ///   The `type` of the source to create. Required unless `customer` and `original_source` are specified (see the [Cloning card Sources](https://stripe.com/docs/sources/connect#cloning-card-sources) guide)
  ///
  /// * [String] usage:
  Future<Response> postSourcesWithHttpInfo({ int? amount, String? currency, String? customer, List<String>? expand, String? flow, MandateParams? mandate, Map<String, String>? metadata, String? originalSource, Owner1? owner, ReceiverParams? receiver, RedirectParams? redirect, ShallowOrderSpecs? sourceOrder, String? statementDescriptor, String? token, String? type, String? usage, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/sources';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (customer != null) {
      formParams[r'customer'] = parameterToString(customer);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (flow != null) {
      formParams[r'flow'] = parameterToString(flow);
    }
    if (mandate != null) {
      formParams[r'mandate'] = parameterToString(mandate);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (originalSource != null) {
      formParams[r'original_source'] = parameterToString(originalSource);
    }
    if (owner != null) {
      formParams[r'owner'] = parameterToString(owner);
    }
    if (receiver != null) {
      formParams[r'receiver'] = parameterToString(receiver);
    }
    if (redirect != null) {
      formParams[r'redirect'] = parameterToString(redirect);
    }
    if (sourceOrder != null) {
      formParams[r'source_order'] = parameterToString(sourceOrder);
    }
    if (statementDescriptor != null) {
      formParams[r'statement_descriptor'] = parameterToString(statementDescriptor);
    }
    if (token != null) {
      formParams[r'token'] = parameterToString(token);
    }
    if (type != null) {
      formParams[r'type'] = parameterToString(type);
    }
    if (usage != null) {
      formParams[r'usage'] = parameterToString(usage);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Shares a source
  ///
  /// <p>Creates a new source object.</p>
  ///
  /// Parameters:
  ///
  /// * [int] amount:
  ///   Amount associated with the source. This is the amount for which the source will be chargeable once ready. Required for `single_use` sources. Not supported for `receiver` type sources, where charge amount may not be specified until funds land.
  ///
  /// * [String] currency:
  ///   Three-letter [ISO code for the currency](https://stripe.com/docs/currencies) associated with the source. This is the currency for which the source will be chargeable once ready.
  ///
  /// * [String] customer:
  ///   The `Customer` to whom the original source is attached to. Must be set when the original source is not a `Source` (e.g., `Card`).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] flow:
  ///   The authentication `flow` of the source to create. `flow` is one of `redirect`, `receiver`, `code_verification`, `none`. It is generally inferred unless a type supports multiple flows.
  ///
  /// * [MandateParams] mandate:
  ///
  /// * [Map<String, String>] metadata:
  ///
  /// * [String] originalSource:
  ///   The source to share.
  ///
  /// * [Owner1] owner:
  ///
  /// * [ReceiverParams] receiver:
  ///
  /// * [RedirectParams] redirect:
  ///
  /// * [ShallowOrderSpecs] sourceOrder:
  ///
  /// * [String] statementDescriptor:
  ///   An arbitrary string to be displayed on your customer's statement. As an example, if your website is `RunClub` and the item you're charging for is a race ticket, you may want to specify a `statement_descriptor` of `RunClub 5K race ticket.` While many payment types will display this information, some may not display it at all.
  ///
  /// * [String] token:
  ///   An optional token used to create the source. When passed, token properties will override source parameters.
  ///
  /// * [String] type:
  ///   The `type` of the source to create. Required unless `customer` and `original_source` are specified (see the [Cloning card Sources](https://stripe.com/docs/sources/connect#cloning-card-sources) guide)
  ///
  /// * [String] usage:
  Future<ModelSource?> postSources({ int? amount, String? currency, String? customer, List<String>? expand, String? flow, MandateParams? mandate, Map<String, String>? metadata, String? originalSource, Owner1? owner, ReceiverParams? receiver, RedirectParams? redirect, ShallowOrderSpecs? sourceOrder, String? statementDescriptor, String? token, String? type, String? usage, }) async {
    final response = await postSourcesWithHttpInfo( amount: amount, currency: currency, customer: customer, expand: expand, flow: flow, mandate: mandate, metadata: metadata, originalSource: originalSource, owner: owner, receiver: receiver, redirect: redirect, sourceOrder: sourceOrder, statementDescriptor: statementDescriptor, token: token, type: type, usage: usage, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ModelSource',) as ModelSource;
    
    }
    return null;
  }

  /// Update a source
  ///
  /// <p>Updates the specified source by setting the values of the parameters passed. Any parameters not provided will be left unchanged.</p>  <p>This request accepts the <code>metadata</code> and <code>owner</code> as arguments. It is also possible to update type specific information for selected payment methods. Please refer to our <a href=\"/docs/sources\">payment method guides</a> for more detail.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] source_ (required):
  ///
  /// * [int] amount:
  ///   Amount associated with the source.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [MandateParams] mandate:
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [Owner1] owner:
  ///
  /// * [OrderParams] sourceOrder:
  Future<Response> postSourcesSourceWithHttpInfo(String source_, { int? amount, List<String>? expand, MandateParams? mandate, PostAccountsRequestMetadata? metadata, Owner1? owner, OrderParams? sourceOrder, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/sources/{source}'
      .replaceAll('{source}', source_);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (mandate != null) {
      formParams[r'mandate'] = parameterToString(mandate);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (owner != null) {
      formParams[r'owner'] = parameterToString(owner);
    }
    if (sourceOrder != null) {
      formParams[r'source_order'] = parameterToString(sourceOrder);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a source
  ///
  /// <p>Updates the specified source by setting the values of the parameters passed. Any parameters not provided will be left unchanged.</p>  <p>This request accepts the <code>metadata</code> and <code>owner</code> as arguments. It is also possible to update type specific information for selected payment methods. Please refer to our <a href=\"/docs/sources\">payment method guides</a> for more detail.</p>
  ///
  /// Parameters:
  ///
  /// * [String] source_ (required):
  ///
  /// * [int] amount:
  ///   Amount associated with the source.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [MandateParams] mandate:
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [Owner1] owner:
  ///
  /// * [OrderParams] sourceOrder:
  Future<ModelSource?> postSourcesSource(String source_, { int? amount, List<String>? expand, MandateParams? mandate, PostAccountsRequestMetadata? metadata, Owner1? owner, OrderParams? sourceOrder, }) async {
    final response = await postSourcesSourceWithHttpInfo(source_,  amount: amount, expand: expand, mandate: mandate, metadata: metadata, owner: owner, sourceOrder: sourceOrder, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ModelSource',) as ModelSource;
    
    }
    return null;
  }

  /// <p>Verify a given source.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] source_ (required):
  ///
  /// * [List<String>] values (required):
  ///   The values needed to verify the source.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postSourcesSourceVerifyWithHttpInfo(String source_, List<String> values, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/sources/{source}/verify'
      .replaceAll('{source}', source_);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (values != null) {
      formParams[r'values'] = parameterToString(values);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// <p>Verify a given source.</p>
  ///
  /// Parameters:
  ///
  /// * [String] source_ (required):
  ///
  /// * [List<String>] values (required):
  ///   The values needed to verify the source.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<ModelSource?> postSourcesSourceVerify(String source_, List<String> values, { List<String>? expand, }) async {
    final response = await postSourcesSourceVerifyWithHttpInfo(source_, values,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ModelSource',) as ModelSource;
    
    }
    return null;
  }

  /// Create a subscription item
  ///
  /// <p>Adds a new item to an existing subscription. No existing items will be changed or replaced.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] subscription (required):
  ///   The identifier of the subscription to modify.
  ///
  /// * [PostSubscriptionItemsRequestBillingThresholds] billingThresholds:
  ///
  /// * [PostSubscriptionItemsRequestDiscounts] discounts:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] paymentBehavior:
  ///   Use `allow_incomplete` to transition the subscription to `status=past_due` if a payment is required but cannot be paid. This allows you to manage scenarios where additional user actions are needed to pay a subscription's invoice. For example, SCA regulation may require 3DS authentication to complete payment. See the [SCA Migration Guide](https://stripe.com/docs/billing/migration/strong-customer-authentication) for Billing to learn more. This is the default behavior.  Use `default_incomplete` to transition the subscription to `status=past_due` when payment is required and await explicit confirmation of the invoice's payment intent. This allows simpler management of scenarios where additional user actions are needed to pay a subscription’s invoice. Such as failed payments, [SCA regulation](https://stripe.com/docs/billing/migration/strong-customer-authentication), or collecting a mandate for a bank debit payment method.  Use `pending_if_incomplete` to update the subscription using [pending updates](https://stripe.com/docs/billing/subscriptions/pending-updates). When you use `pending_if_incomplete` you can only pass the parameters [supported by pending updates](https://stripe.com/docs/billing/pending-updates-reference#supported-attributes).  Use `error_if_incomplete` if you want Stripe to return an HTTP 402 status code if a subscription's invoice cannot be paid. For example, if a payment method requires 3DS authentication due to SCA regulation and further user action is needed, this parameter does not update the subscription and returns an error instead. This was the default behavior for API versions prior to 2019-03-14. See the [changelog](https://docs.stripe.com/changelog/2019-03-14) to learn more.
  ///
  /// * [String] price:
  ///   The ID of the price object.
  ///
  /// * [RecurringPriceData1] priceData:
  ///
  /// * [String] prorationBehavior:
  ///   Determines how to handle [prorations](https://stripe.com/docs/billing/subscriptions/prorations) when the billing cycle changes (e.g., when switching plans, resetting `billing_cycle_anchor=now`, or starting a trial), or if an item's `quantity` changes. The default value is `create_prorations`.
  ///
  /// * [int] prorationDate:
  ///   If set, the proration will be calculated as though the subscription was updated at the given time. This can be used to apply the same proration that was previewed with the [upcoming invoice](https://stripe.com/docs/api#retrieve_customer_invoice) endpoint.
  ///
  /// * [int] quantity:
  ///   The quantity you'd like to apply to the subscription item you're creating.
  ///
  /// * [PostSubscriptionItemsRequestTaxRates] taxRates:
  Future<Response> postSubscriptionItemsWithHttpInfo(String subscription, { PostSubscriptionItemsRequestBillingThresholds? billingThresholds, PostSubscriptionItemsRequestDiscounts? discounts, List<String>? expand, Map<String, String>? metadata, String? paymentBehavior, String? price, RecurringPriceData1? priceData, String? prorationBehavior, int? prorationDate, int? quantity, PostSubscriptionItemsRequestTaxRates? taxRates, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/subscription_items';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (billingThresholds != null) {
      formParams[r'billing_thresholds'] = parameterToString(billingThresholds);
    }
    if (discounts != null) {
      formParams[r'discounts'] = parameterToString(discounts);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (paymentBehavior != null) {
      formParams[r'payment_behavior'] = parameterToString(paymentBehavior);
    }
    if (price != null) {
      formParams[r'price'] = parameterToString(price);
    }
    if (priceData != null) {
      formParams[r'price_data'] = parameterToString(priceData);
    }
    if (prorationBehavior != null) {
      formParams[r'proration_behavior'] = parameterToString(prorationBehavior);
    }
    if (prorationDate != null) {
      formParams[r'proration_date'] = parameterToString(prorationDate);
    }
    if (quantity != null) {
      formParams[r'quantity'] = parameterToString(quantity);
    }
    if (subscription != null) {
      formParams[r'subscription'] = parameterToString(subscription);
    }
    if (taxRates != null) {
      formParams[r'tax_rates'] = parameterToString(taxRates);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a subscription item
  ///
  /// <p>Adds a new item to an existing subscription. No existing items will be changed or replaced.</p>
  ///
  /// Parameters:
  ///
  /// * [String] subscription (required):
  ///   The identifier of the subscription to modify.
  ///
  /// * [PostSubscriptionItemsRequestBillingThresholds] billingThresholds:
  ///
  /// * [PostSubscriptionItemsRequestDiscounts] discounts:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] paymentBehavior:
  ///   Use `allow_incomplete` to transition the subscription to `status=past_due` if a payment is required but cannot be paid. This allows you to manage scenarios where additional user actions are needed to pay a subscription's invoice. For example, SCA regulation may require 3DS authentication to complete payment. See the [SCA Migration Guide](https://stripe.com/docs/billing/migration/strong-customer-authentication) for Billing to learn more. This is the default behavior.  Use `default_incomplete` to transition the subscription to `status=past_due` when payment is required and await explicit confirmation of the invoice's payment intent. This allows simpler management of scenarios where additional user actions are needed to pay a subscription’s invoice. Such as failed payments, [SCA regulation](https://stripe.com/docs/billing/migration/strong-customer-authentication), or collecting a mandate for a bank debit payment method.  Use `pending_if_incomplete` to update the subscription using [pending updates](https://stripe.com/docs/billing/subscriptions/pending-updates). When you use `pending_if_incomplete` you can only pass the parameters [supported by pending updates](https://stripe.com/docs/billing/pending-updates-reference#supported-attributes).  Use `error_if_incomplete` if you want Stripe to return an HTTP 402 status code if a subscription's invoice cannot be paid. For example, if a payment method requires 3DS authentication due to SCA regulation and further user action is needed, this parameter does not update the subscription and returns an error instead. This was the default behavior for API versions prior to 2019-03-14. See the [changelog](https://docs.stripe.com/changelog/2019-03-14) to learn more.
  ///
  /// * [String] price:
  ///   The ID of the price object.
  ///
  /// * [RecurringPriceData1] priceData:
  ///
  /// * [String] prorationBehavior:
  ///   Determines how to handle [prorations](https://stripe.com/docs/billing/subscriptions/prorations) when the billing cycle changes (e.g., when switching plans, resetting `billing_cycle_anchor=now`, or starting a trial), or if an item's `quantity` changes. The default value is `create_prorations`.
  ///
  /// * [int] prorationDate:
  ///   If set, the proration will be calculated as though the subscription was updated at the given time. This can be used to apply the same proration that was previewed with the [upcoming invoice](https://stripe.com/docs/api#retrieve_customer_invoice) endpoint.
  ///
  /// * [int] quantity:
  ///   The quantity you'd like to apply to the subscription item you're creating.
  ///
  /// * [PostSubscriptionItemsRequestTaxRates] taxRates:
  Future<SubscriptionItem?> postSubscriptionItems(String subscription, { PostSubscriptionItemsRequestBillingThresholds? billingThresholds, PostSubscriptionItemsRequestDiscounts? discounts, List<String>? expand, Map<String, String>? metadata, String? paymentBehavior, String? price, RecurringPriceData1? priceData, String? prorationBehavior, int? prorationDate, int? quantity, PostSubscriptionItemsRequestTaxRates? taxRates, }) async {
    final response = await postSubscriptionItemsWithHttpInfo(subscription,  billingThresholds: billingThresholds, discounts: discounts, expand: expand, metadata: metadata, paymentBehavior: paymentBehavior, price: price, priceData: priceData, prorationBehavior: prorationBehavior, prorationDate: prorationDate, quantity: quantity, taxRates: taxRates, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'SubscriptionItem',) as SubscriptionItem;
    
    }
    return null;
  }

  /// Update a subscription item
  ///
  /// <p>Updates the plan or quantity of an item on a current subscription.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] item (required):
  ///
  /// * [PostSubscriptionItemsRequestBillingThresholds] billingThresholds:
  ///
  /// * [PostSubscriptionItemsRequestDiscounts] discounts:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [bool] offSession:
  ///   Indicates if a customer is on or off-session while an invoice payment is attempted. Defaults to `false` (on-session).
  ///
  /// * [String] paymentBehavior:
  ///   Use `allow_incomplete` to transition the subscription to `status=past_due` if a payment is required but cannot be paid. This allows you to manage scenarios where additional user actions are needed to pay a subscription's invoice. For example, SCA regulation may require 3DS authentication to complete payment. See the [SCA Migration Guide](https://stripe.com/docs/billing/migration/strong-customer-authentication) for Billing to learn more. This is the default behavior.  Use `default_incomplete` to transition the subscription to `status=past_due` when payment is required and await explicit confirmation of the invoice's payment intent. This allows simpler management of scenarios where additional user actions are needed to pay a subscription’s invoice. Such as failed payments, [SCA regulation](https://stripe.com/docs/billing/migration/strong-customer-authentication), or collecting a mandate for a bank debit payment method.  Use `pending_if_incomplete` to update the subscription using [pending updates](https://stripe.com/docs/billing/subscriptions/pending-updates). When you use `pending_if_incomplete` you can only pass the parameters [supported by pending updates](https://stripe.com/docs/billing/pending-updates-reference#supported-attributes).  Use `error_if_incomplete` if you want Stripe to return an HTTP 402 status code if a subscription's invoice cannot be paid. For example, if a payment method requires 3DS authentication due to SCA regulation and further user action is needed, this parameter does not update the subscription and returns an error instead. This was the default behavior for API versions prior to 2019-03-14. See the [changelog](https://docs.stripe.com/changelog/2019-03-14) to learn more.
  ///
  /// * [String] price:
  ///   The ID of the price object. One of `price` or `price_data` is required. When changing a subscription item's price, `quantity` is set to 1 unless a `quantity` parameter is provided.
  ///
  /// * [RecurringPriceData2] priceData:
  ///
  /// * [String] prorationBehavior:
  ///   Determines how to handle [prorations](https://stripe.com/docs/billing/subscriptions/prorations) when the billing cycle changes (e.g., when switching plans, resetting `billing_cycle_anchor=now`, or starting a trial), or if an item's `quantity` changes. The default value is `create_prorations`.
  ///
  /// * [int] prorationDate:
  ///   If set, the proration will be calculated as though the subscription was updated at the given time. This can be used to apply the same proration that was previewed with the [upcoming invoice](https://stripe.com/docs/api#retrieve_customer_invoice) endpoint.
  ///
  /// * [int] quantity:
  ///   The quantity you'd like to apply to the subscription item you're creating.
  ///
  /// * [PostSubscriptionItemsRequestTaxRates] taxRates:
  Future<Response> postSubscriptionItemsItemWithHttpInfo(String item, { PostSubscriptionItemsRequestBillingThresholds? billingThresholds, PostSubscriptionItemsRequestDiscounts? discounts, List<String>? expand, PostAccountsRequestMetadata? metadata, bool? offSession, String? paymentBehavior, String? price, RecurringPriceData2? priceData, String? prorationBehavior, int? prorationDate, int? quantity, PostSubscriptionItemsRequestTaxRates? taxRates, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/subscription_items/{item}'
      .replaceAll('{item}', item);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (billingThresholds != null) {
      formParams[r'billing_thresholds'] = parameterToString(billingThresholds);
    }
    if (discounts != null) {
      formParams[r'discounts'] = parameterToString(discounts);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (offSession != null) {
      formParams[r'off_session'] = parameterToString(offSession);
    }
    if (paymentBehavior != null) {
      formParams[r'payment_behavior'] = parameterToString(paymentBehavior);
    }
    if (price != null) {
      formParams[r'price'] = parameterToString(price);
    }
    if (priceData != null) {
      formParams[r'price_data'] = parameterToString(priceData);
    }
    if (prorationBehavior != null) {
      formParams[r'proration_behavior'] = parameterToString(prorationBehavior);
    }
    if (prorationDate != null) {
      formParams[r'proration_date'] = parameterToString(prorationDate);
    }
    if (quantity != null) {
      formParams[r'quantity'] = parameterToString(quantity);
    }
    if (taxRates != null) {
      formParams[r'tax_rates'] = parameterToString(taxRates);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a subscription item
  ///
  /// <p>Updates the plan or quantity of an item on a current subscription.</p>
  ///
  /// Parameters:
  ///
  /// * [String] item (required):
  ///
  /// * [PostSubscriptionItemsRequestBillingThresholds] billingThresholds:
  ///
  /// * [PostSubscriptionItemsRequestDiscounts] discounts:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [bool] offSession:
  ///   Indicates if a customer is on or off-session while an invoice payment is attempted. Defaults to `false` (on-session).
  ///
  /// * [String] paymentBehavior:
  ///   Use `allow_incomplete` to transition the subscription to `status=past_due` if a payment is required but cannot be paid. This allows you to manage scenarios where additional user actions are needed to pay a subscription's invoice. For example, SCA regulation may require 3DS authentication to complete payment. See the [SCA Migration Guide](https://stripe.com/docs/billing/migration/strong-customer-authentication) for Billing to learn more. This is the default behavior.  Use `default_incomplete` to transition the subscription to `status=past_due` when payment is required and await explicit confirmation of the invoice's payment intent. This allows simpler management of scenarios where additional user actions are needed to pay a subscription’s invoice. Such as failed payments, [SCA regulation](https://stripe.com/docs/billing/migration/strong-customer-authentication), or collecting a mandate for a bank debit payment method.  Use `pending_if_incomplete` to update the subscription using [pending updates](https://stripe.com/docs/billing/subscriptions/pending-updates). When you use `pending_if_incomplete` you can only pass the parameters [supported by pending updates](https://stripe.com/docs/billing/pending-updates-reference#supported-attributes).  Use `error_if_incomplete` if you want Stripe to return an HTTP 402 status code if a subscription's invoice cannot be paid. For example, if a payment method requires 3DS authentication due to SCA regulation and further user action is needed, this parameter does not update the subscription and returns an error instead. This was the default behavior for API versions prior to 2019-03-14. See the [changelog](https://docs.stripe.com/changelog/2019-03-14) to learn more.
  ///
  /// * [String] price:
  ///   The ID of the price object. One of `price` or `price_data` is required. When changing a subscription item's price, `quantity` is set to 1 unless a `quantity` parameter is provided.
  ///
  /// * [RecurringPriceData2] priceData:
  ///
  /// * [String] prorationBehavior:
  ///   Determines how to handle [prorations](https://stripe.com/docs/billing/subscriptions/prorations) when the billing cycle changes (e.g., when switching plans, resetting `billing_cycle_anchor=now`, or starting a trial), or if an item's `quantity` changes. The default value is `create_prorations`.
  ///
  /// * [int] prorationDate:
  ///   If set, the proration will be calculated as though the subscription was updated at the given time. This can be used to apply the same proration that was previewed with the [upcoming invoice](https://stripe.com/docs/api#retrieve_customer_invoice) endpoint.
  ///
  /// * [int] quantity:
  ///   The quantity you'd like to apply to the subscription item you're creating.
  ///
  /// * [PostSubscriptionItemsRequestTaxRates] taxRates:
  Future<SubscriptionItem?> postSubscriptionItemsItem(String item, { PostSubscriptionItemsRequestBillingThresholds? billingThresholds, PostSubscriptionItemsRequestDiscounts? discounts, List<String>? expand, PostAccountsRequestMetadata? metadata, bool? offSession, String? paymentBehavior, String? price, RecurringPriceData2? priceData, String? prorationBehavior, int? prorationDate, int? quantity, PostSubscriptionItemsRequestTaxRates? taxRates, }) async {
    final response = await postSubscriptionItemsItemWithHttpInfo(item,  billingThresholds: billingThresholds, discounts: discounts, expand: expand, metadata: metadata, offSession: offSession, paymentBehavior: paymentBehavior, price: price, priceData: priceData, prorationBehavior: prorationBehavior, prorationDate: prorationDate, quantity: quantity, taxRates: taxRates, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'SubscriptionItem',) as SubscriptionItem;
    
    }
    return null;
  }

  /// Create a schedule
  ///
  /// <p>Creates a new subscription schedule object. Each customer can have up to 500 active or scheduled subscriptions.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [BillingMode1] billingMode:
  ///
  /// * [String] customer:
  ///   The identifier of the customer to create the subscription schedule for.
  ///
  /// * [DefaultSettingsParams] defaultSettings:
  ///
  /// * [String] endBehavior:
  ///   Behavior of the subscription schedule and underlying subscription when it ends. Possible values are `release` or `cancel` with the default being `release`. `release` will end the subscription schedule and keep the underlying subscription running. `cancel` will end the subscription schedule and cancel the underlying subscription.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] fromSubscription:
  ///   Migrate an existing subscription to be managed by a subscription schedule. If this parameter is set, a subscription schedule will be created using the subscription's item(s), set to auto-renew using the subscription's interval. When using this parameter, other parameters (such as phase values) cannot be set. To create a subscription schedule with other modifications, we recommend making two separate API calls.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [List<PhaseConfigurationParams1>] phases:
  ///   List representing phases of the subscription schedule. Each phase can be customized to have different durations, plans, and coupons. If there are multiple phases, the `end_date` of one phase will always equal the `start_date` of the next phase.
  ///
  /// * [PostSubscriptionSchedulesRequestStartDate] startDate:
  Future<Response> postSubscriptionSchedulesWithHttpInfo({ BillingMode1? billingMode, String? customer, DefaultSettingsParams? defaultSettings, String? endBehavior, List<String>? expand, String? fromSubscription, PostAccountsRequestMetadata? metadata, List<PhaseConfigurationParams1>? phases, PostSubscriptionSchedulesRequestStartDate? startDate, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/subscription_schedules';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (billingMode != null) {
      formParams[r'billing_mode'] = parameterToString(billingMode);
    }
    if (customer != null) {
      formParams[r'customer'] = parameterToString(customer);
    }
    if (defaultSettings != null) {
      formParams[r'default_settings'] = parameterToString(defaultSettings);
    }
    if (endBehavior != null) {
      formParams[r'end_behavior'] = parameterToString(endBehavior);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (fromSubscription != null) {
      formParams[r'from_subscription'] = parameterToString(fromSubscription);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (phases != null) {
      formParams[r'phases'] = parameterToString(phases);
    }
    if (startDate != null) {
      formParams[r'start_date'] = parameterToString(startDate);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a schedule
  ///
  /// <p>Creates a new subscription schedule object. Each customer can have up to 500 active or scheduled subscriptions.</p>
  ///
  /// Parameters:
  ///
  /// * [BillingMode1] billingMode:
  ///
  /// * [String] customer:
  ///   The identifier of the customer to create the subscription schedule for.
  ///
  /// * [DefaultSettingsParams] defaultSettings:
  ///
  /// * [String] endBehavior:
  ///   Behavior of the subscription schedule and underlying subscription when it ends. Possible values are `release` or `cancel` with the default being `release`. `release` will end the subscription schedule and keep the underlying subscription running. `cancel` will end the subscription schedule and cancel the underlying subscription.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] fromSubscription:
  ///   Migrate an existing subscription to be managed by a subscription schedule. If this parameter is set, a subscription schedule will be created using the subscription's item(s), set to auto-renew using the subscription's interval. When using this parameter, other parameters (such as phase values) cannot be set. To create a subscription schedule with other modifications, we recommend making two separate API calls.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [List<PhaseConfigurationParams1>] phases:
  ///   List representing phases of the subscription schedule. Each phase can be customized to have different durations, plans, and coupons. If there are multiple phases, the `end_date` of one phase will always equal the `start_date` of the next phase.
  ///
  /// * [PostSubscriptionSchedulesRequestStartDate] startDate:
  Future<SubscriptionSchedule?> postSubscriptionSchedules({ BillingMode1? billingMode, String? customer, DefaultSettingsParams? defaultSettings, String? endBehavior, List<String>? expand, String? fromSubscription, PostAccountsRequestMetadata? metadata, List<PhaseConfigurationParams1>? phases, PostSubscriptionSchedulesRequestStartDate? startDate, }) async {
    final response = await postSubscriptionSchedulesWithHttpInfo( billingMode: billingMode, customer: customer, defaultSettings: defaultSettings, endBehavior: endBehavior, expand: expand, fromSubscription: fromSubscription, metadata: metadata, phases: phases, startDate: startDate, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'SubscriptionSchedule',) as SubscriptionSchedule;
    
    }
    return null;
  }

  /// Update a schedule
  ///
  /// <p>Updates an existing subscription schedule.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] schedule (required):
  ///
  /// * [DefaultSettingsParams] defaultSettings:
  ///
  /// * [String] endBehavior:
  ///   Behavior of the subscription schedule and underlying subscription when it ends. Possible values are `release` or `cancel` with the default being `release`. `release` will end the subscription schedule and keep the underlying subscription running. `cancel` will end the subscription schedule and cancel the underlying subscription.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [List<PhaseConfigurationParams>] phases:
  ///   List representing phases of the subscription schedule. Each phase can be customized to have different durations, plans, and coupons. If there are multiple phases, the `end_date` of one phase will always equal the `start_date` of the next phase. Note that past phases can be omitted.
  ///
  /// * [String] prorationBehavior:
  ///   If the update changes the billing configuration (item price, quantity, etc.) of the current phase, indicates how prorations from this change should be handled. The default value is `create_prorations`.
  Future<Response> postSubscriptionSchedulesScheduleWithHttpInfo(String schedule, { DefaultSettingsParams? defaultSettings, String? endBehavior, List<String>? expand, PostAccountsRequestMetadata? metadata, List<PhaseConfigurationParams>? phases, String? prorationBehavior, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/subscription_schedules/{schedule}'
      .replaceAll('{schedule}', schedule);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (defaultSettings != null) {
      formParams[r'default_settings'] = parameterToString(defaultSettings);
    }
    if (endBehavior != null) {
      formParams[r'end_behavior'] = parameterToString(endBehavior);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (phases != null) {
      formParams[r'phases'] = parameterToString(phases);
    }
    if (prorationBehavior != null) {
      formParams[r'proration_behavior'] = parameterToString(prorationBehavior);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a schedule
  ///
  /// <p>Updates an existing subscription schedule.</p>
  ///
  /// Parameters:
  ///
  /// * [String] schedule (required):
  ///
  /// * [DefaultSettingsParams] defaultSettings:
  ///
  /// * [String] endBehavior:
  ///   Behavior of the subscription schedule and underlying subscription when it ends. Possible values are `release` or `cancel` with the default being `release`. `release` will end the subscription schedule and keep the underlying subscription running. `cancel` will end the subscription schedule and cancel the underlying subscription.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [List<PhaseConfigurationParams>] phases:
  ///   List representing phases of the subscription schedule. Each phase can be customized to have different durations, plans, and coupons. If there are multiple phases, the `end_date` of one phase will always equal the `start_date` of the next phase. Note that past phases can be omitted.
  ///
  /// * [String] prorationBehavior:
  ///   If the update changes the billing configuration (item price, quantity, etc.) of the current phase, indicates how prorations from this change should be handled. The default value is `create_prorations`.
  Future<SubscriptionSchedule?> postSubscriptionSchedulesSchedule(String schedule, { DefaultSettingsParams? defaultSettings, String? endBehavior, List<String>? expand, PostAccountsRequestMetadata? metadata, List<PhaseConfigurationParams>? phases, String? prorationBehavior, }) async {
    final response = await postSubscriptionSchedulesScheduleWithHttpInfo(schedule,  defaultSettings: defaultSettings, endBehavior: endBehavior, expand: expand, metadata: metadata, phases: phases, prorationBehavior: prorationBehavior, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'SubscriptionSchedule',) as SubscriptionSchedule;
    
    }
    return null;
  }

  /// Cancel a schedule
  ///
  /// <p>Cancels a subscription schedule and its associated subscription immediately (if the subscription schedule has an active subscription). A subscription schedule can only be canceled if its status is <code>not_started</code> or <code>active</code>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] schedule (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [bool] invoiceNow:
  ///   If the subscription schedule is `active`, indicates if a final invoice will be generated that contains any un-invoiced metered usage and new/pending proration invoice items. Defaults to `true`.
  ///
  /// * [bool] prorate:
  ///   If the subscription schedule is `active`, indicates if the cancellation should be prorated. Defaults to `true`.
  Future<Response> postSubscriptionSchedulesScheduleCancelWithHttpInfo(String schedule, { List<String>? expand, bool? invoiceNow, bool? prorate, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/subscription_schedules/{schedule}/cancel'
      .replaceAll('{schedule}', schedule);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (invoiceNow != null) {
      formParams[r'invoice_now'] = parameterToString(invoiceNow);
    }
    if (prorate != null) {
      formParams[r'prorate'] = parameterToString(prorate);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Cancel a schedule
  ///
  /// <p>Cancels a subscription schedule and its associated subscription immediately (if the subscription schedule has an active subscription). A subscription schedule can only be canceled if its status is <code>not_started</code> or <code>active</code>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] schedule (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [bool] invoiceNow:
  ///   If the subscription schedule is `active`, indicates if a final invoice will be generated that contains any un-invoiced metered usage and new/pending proration invoice items. Defaults to `true`.
  ///
  /// * [bool] prorate:
  ///   If the subscription schedule is `active`, indicates if the cancellation should be prorated. Defaults to `true`.
  Future<SubscriptionSchedule?> postSubscriptionSchedulesScheduleCancel(String schedule, { List<String>? expand, bool? invoiceNow, bool? prorate, }) async {
    final response = await postSubscriptionSchedulesScheduleCancelWithHttpInfo(schedule,  expand: expand, invoiceNow: invoiceNow, prorate: prorate, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'SubscriptionSchedule',) as SubscriptionSchedule;
    
    }
    return null;
  }

  /// Release a schedule
  ///
  /// <p>Releases the subscription schedule immediately, which will stop scheduling of its phases, but leave any existing subscription in place. A schedule can only be released if its status is <code>not_started</code> or <code>active</code>. If the subscription schedule is currently associated with a subscription, releasing it will remove its <code>subscription</code> property and set the subscription’s ID to the <code>released_subscription</code> property.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] schedule (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [bool] preserveCancelDate:
  ///   Keep any cancellation on the subscription that the schedule has set
  Future<Response> postSubscriptionSchedulesScheduleReleaseWithHttpInfo(String schedule, { List<String>? expand, bool? preserveCancelDate, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/subscription_schedules/{schedule}/release'
      .replaceAll('{schedule}', schedule);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (preserveCancelDate != null) {
      formParams[r'preserve_cancel_date'] = parameterToString(preserveCancelDate);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Release a schedule
  ///
  /// <p>Releases the subscription schedule immediately, which will stop scheduling of its phases, but leave any existing subscription in place. A schedule can only be released if its status is <code>not_started</code> or <code>active</code>. If the subscription schedule is currently associated with a subscription, releasing it will remove its <code>subscription</code> property and set the subscription’s ID to the <code>released_subscription</code> property.</p>
  ///
  /// Parameters:
  ///
  /// * [String] schedule (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [bool] preserveCancelDate:
  ///   Keep any cancellation on the subscription that the schedule has set
  Future<SubscriptionSchedule?> postSubscriptionSchedulesScheduleRelease(String schedule, { List<String>? expand, bool? preserveCancelDate, }) async {
    final response = await postSubscriptionSchedulesScheduleReleaseWithHttpInfo(schedule,  expand: expand, preserveCancelDate: preserveCancelDate, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'SubscriptionSchedule',) as SubscriptionSchedule;
    
    }
    return null;
  }

  /// Create a subscription
  ///
  /// <p>Creates a new subscription on an existing customer. Each customer can have up to 500 active or scheduled subscriptions.</p>  <p>When you create a subscription with <code>collection_method=charge_automatically</code>, the first invoice is finalized as part of the request. The <code>payment_behavior</code> parameter determines the exact behavior of the initial payment.</p>  <p>To start subscriptions where the first invoice always begins in a <code>draft</code> status, use <a href=\"/docs/billing/subscriptions/subscription-schedules#managing\">subscription schedules</a> instead. Schedules provide the flexibility to model more complex billing configurations that change over time.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///   The identifier of the customer to subscribe.
  ///
  /// * [List<AddInvoiceItemEntry>] addInvoiceItems:
  ///   A list of prices and quantities that will generate invoice items appended to the next invoice for this subscription. You may pass up to 20 items.
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestApplicationFeePercent] applicationFeePercent:
  ///
  /// * [AutomaticTaxConfig] automaticTax:
  ///
  /// * [int] backdateStartDate:
  ///   A past timestamp to backdate the subscription's start date to. If set, the first invoice will contain line items for the timespan between the start date and the current time. Can be combined with trials and the billing cycle anchor.
  ///
  /// * [int] billingCycleAnchor:
  ///   A future timestamp in UTC format to anchor the subscription's [billing cycle](https://stripe.com/docs/subscriptions/billing-cycle). The anchor is the reference point that aligns future billing cycle dates. It sets the day of week for `week` intervals, the day of month for `month` and `year` intervals, and the month of year for `year` intervals.
  ///
  /// * [BillingCycleAnchorConfigParam] billingCycleAnchorConfig:
  ///
  /// * [BillingMode1] billingMode:
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestBillingThresholds] billingThresholds:
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestCancelAt] cancelAt:
  ///
  /// * [bool] cancelAtPeriodEnd:
  ///   Indicate whether this subscription should cancel at the end of the current period (`current_period_end`). Defaults to `false`.
  ///
  /// * [String] collectionMethod:
  ///   Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay this subscription at the end of the cycle using the default source attached to the customer. When sending an invoice, Stripe will email your customer an invoice with payment instructions and mark the subscription as `active`. Defaults to `charge_automatically`.
  ///
  /// * [String] currency:
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [int] daysUntilDue:
  ///   Number of days a customer has to pay invoices generated by this subscription. Valid only for subscriptions where `collection_method` is set to `send_invoice`.
  ///
  /// * [String] defaultPaymentMethod:
  ///   ID of the default payment method for the subscription. It must belong to the customer associated with the subscription. This takes precedence over `default_source`. If neither are set, invoices will use the customer's [invoice_settings.default_payment_method](https://stripe.com/docs/api/customers/object#customer_object-invoice_settings-default_payment_method) or [default_source](https://stripe.com/docs/api/customers/object#customer_object-default_source).
  ///
  /// * [String] defaultSource:
  ///   ID of the default payment source for the subscription. It must belong to the customer associated with the subscription and be in a chargeable state. If `default_payment_method` is also set, `default_payment_method` will take precedence. If neither are set, invoices will use the customer's [invoice_settings.default_payment_method](https://stripe.com/docs/api/customers/object#customer_object-invoice_settings-default_payment_method) or [default_source](https://stripe.com/docs/api/customers/object#customer_object-default_source).
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestDefaultTaxRates] defaultTaxRates:
  ///
  /// * [String] description:
  ///   The subscription's description, meant to be displayable to the customer. Use this field to optionally store an explanation of the subscription for rendering in Stripe surfaces and certain local payment methods UIs.
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestDiscounts] discounts:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [InvoiceSettingsParam] invoiceSettings:
  ///
  /// * [List<SubscriptionItemCreateParams>] items:
  ///   A list of up to 20 subscription items, each with an attached price.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [bool] offSession:
  ///   Indicates if a customer is on or off-session while an invoice payment is attempted. Defaults to `false` (on-session).
  ///
  /// * [PostSubscriptionsRequestOnBehalfOf] onBehalfOf:
  ///
  /// * [String] paymentBehavior:
  ///   Only applies to subscriptions with `collection_method=charge_automatically`.  Use `allow_incomplete` to create Subscriptions with `status=incomplete` if the first invoice can't be paid. Creating Subscriptions with this status allows you to manage scenarios where additional customer actions are needed to pay a subscription's invoice. For example, SCA regulation may require 3DS authentication to complete payment. See the [SCA Migration Guide](https://stripe.com/docs/billing/migration/strong-customer-authentication) for Billing to learn more. This is the default behavior.  Use `default_incomplete` to create Subscriptions with `status=incomplete` when the first invoice requires payment, otherwise start as active. Subscriptions transition to `status=active` when successfully confirming the PaymentIntent on the first invoice. This allows simpler management of scenarios where additional customer actions are needed to pay a subscription’s invoice, such as failed payments, [SCA regulation](https://stripe.com/docs/billing/migration/strong-customer-authentication), or collecting a mandate for a bank debit payment method. If the PaymentIntent is not confirmed within 23 hours Subscriptions transition to `status=incomplete_expired`, which is a terminal state.  Use `error_if_incomplete` if you want Stripe to return an HTTP 402 status code if a subscription's first invoice can't be paid. For example, if a payment method requires 3DS authentication due to SCA regulation and further customer action is needed, this parameter doesn't create a Subscription and returns an error instead. This was the default behavior for API versions prior to 2019-03-14. See the [changelog](https://stripe.com/docs/upgrades#2019-03-14) to learn more.  `pending_if_incomplete` is only used with updates and cannot be passed when creating a Subscription.  Subscriptions with `collection_method=send_invoice` are automatically activated regardless of the first Invoice status.
  ///
  /// * [PaymentSettings] paymentSettings:
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestPendingInvoiceItemInterval] pendingInvoiceItemInterval:
  ///
  /// * [String] prorationBehavior:
  ///   Determines how to handle [prorations](https://stripe.com/docs/billing/subscriptions/prorations) resulting from the `billing_cycle_anchor`. If no value is passed, the default is `create_prorations`.
  ///
  /// * [TransferDataSpecs3] transferData:
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestTrialEnd] trialEnd:
  ///
  /// * [bool] trialFromPlan:
  ///   Indicates if a plan's `trial_period_days` should be applied to the subscription. Setting `trial_end` per subscription is preferred, and this defaults to `false`. Setting this flag to `true` together with `trial_end` is not allowed. See [Using trial periods on subscriptions](https://stripe.com/docs/billing/subscriptions/trials) to learn more.
  ///
  /// * [int] trialPeriodDays:
  ///   Integer representing the number of trial period days before the customer is charged for the first time. This will always overwrite any trials that might apply via a subscribed plan. See [Using trial periods on subscriptions](https://stripe.com/docs/billing/subscriptions/trials) to learn more.
  ///
  /// * [TrialSettingsConfig1] trialSettings:
  Future<Response> postSubscriptionsWithHttpInfo(String customer, { List<AddInvoiceItemEntry>? addInvoiceItems, PostCustomersCustomerSubscriptionsRequestApplicationFeePercent? applicationFeePercent, AutomaticTaxConfig? automaticTax, int? backdateStartDate, int? billingCycleAnchor, BillingCycleAnchorConfigParam? billingCycleAnchorConfig, BillingMode1? billingMode, PostCustomersCustomerSubscriptionsRequestBillingThresholds? billingThresholds, PostCustomersCustomerSubscriptionsRequestCancelAt? cancelAt, bool? cancelAtPeriodEnd, String? collectionMethod, String? currency, int? daysUntilDue, String? defaultPaymentMethod, String? defaultSource, PostCustomersCustomerSubscriptionsRequestDefaultTaxRates? defaultTaxRates, String? description, PostCustomersCustomerSubscriptionsRequestDiscounts? discounts, List<String>? expand, InvoiceSettingsParam? invoiceSettings, List<SubscriptionItemCreateParams>? items, PostAccountsRequestMetadata? metadata, bool? offSession, PostSubscriptionsRequestOnBehalfOf? onBehalfOf, String? paymentBehavior, PaymentSettings? paymentSettings, PostCustomersCustomerSubscriptionsRequestPendingInvoiceItemInterval? pendingInvoiceItemInterval, String? prorationBehavior, TransferDataSpecs3? transferData, PostCustomersCustomerSubscriptionsRequestTrialEnd? trialEnd, bool? trialFromPlan, int? trialPeriodDays, TrialSettingsConfig1? trialSettings, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/subscriptions';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (addInvoiceItems != null) {
      formParams[r'add_invoice_items'] = parameterToString(addInvoiceItems);
    }
    if (applicationFeePercent != null) {
      formParams[r'application_fee_percent'] = parameterToString(applicationFeePercent);
    }
    if (automaticTax != null) {
      formParams[r'automatic_tax'] = parameterToString(automaticTax);
    }
    if (backdateStartDate != null) {
      formParams[r'backdate_start_date'] = parameterToString(backdateStartDate);
    }
    if (billingCycleAnchor != null) {
      formParams[r'billing_cycle_anchor'] = parameterToString(billingCycleAnchor);
    }
    if (billingCycleAnchorConfig != null) {
      formParams[r'billing_cycle_anchor_config'] = parameterToString(billingCycleAnchorConfig);
    }
    if (billingMode != null) {
      formParams[r'billing_mode'] = parameterToString(billingMode);
    }
    if (billingThresholds != null) {
      formParams[r'billing_thresholds'] = parameterToString(billingThresholds);
    }
    if (cancelAt != null) {
      formParams[r'cancel_at'] = parameterToString(cancelAt);
    }
    if (cancelAtPeriodEnd != null) {
      formParams[r'cancel_at_period_end'] = parameterToString(cancelAtPeriodEnd);
    }
    if (collectionMethod != null) {
      formParams[r'collection_method'] = parameterToString(collectionMethod);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (customer != null) {
      formParams[r'customer'] = parameterToString(customer);
    }
    if (daysUntilDue != null) {
      formParams[r'days_until_due'] = parameterToString(daysUntilDue);
    }
    if (defaultPaymentMethod != null) {
      formParams[r'default_payment_method'] = parameterToString(defaultPaymentMethod);
    }
    if (defaultSource != null) {
      formParams[r'default_source'] = parameterToString(defaultSource);
    }
    if (defaultTaxRates != null) {
      formParams[r'default_tax_rates'] = parameterToString(defaultTaxRates);
    }
    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (discounts != null) {
      formParams[r'discounts'] = parameterToString(discounts);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (invoiceSettings != null) {
      formParams[r'invoice_settings'] = parameterToString(invoiceSettings);
    }
    if (items != null) {
      formParams[r'items'] = parameterToString(items);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (offSession != null) {
      formParams[r'off_session'] = parameterToString(offSession);
    }
    if (onBehalfOf != null) {
      formParams[r'on_behalf_of'] = parameterToString(onBehalfOf);
    }
    if (paymentBehavior != null) {
      formParams[r'payment_behavior'] = parameterToString(paymentBehavior);
    }
    if (paymentSettings != null) {
      formParams[r'payment_settings'] = parameterToString(paymentSettings);
    }
    if (pendingInvoiceItemInterval != null) {
      formParams[r'pending_invoice_item_interval'] = parameterToString(pendingInvoiceItemInterval);
    }
    if (prorationBehavior != null) {
      formParams[r'proration_behavior'] = parameterToString(prorationBehavior);
    }
    if (transferData != null) {
      formParams[r'transfer_data'] = parameterToString(transferData);
    }
    if (trialEnd != null) {
      formParams[r'trial_end'] = parameterToString(trialEnd);
    }
    if (trialFromPlan != null) {
      formParams[r'trial_from_plan'] = parameterToString(trialFromPlan);
    }
    if (trialPeriodDays != null) {
      formParams[r'trial_period_days'] = parameterToString(trialPeriodDays);
    }
    if (trialSettings != null) {
      formParams[r'trial_settings'] = parameterToString(trialSettings);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a subscription
  ///
  /// <p>Creates a new subscription on an existing customer. Each customer can have up to 500 active or scheduled subscriptions.</p>  <p>When you create a subscription with <code>collection_method=charge_automatically</code>, the first invoice is finalized as part of the request. The <code>payment_behavior</code> parameter determines the exact behavior of the initial payment.</p>  <p>To start subscriptions where the first invoice always begins in a <code>draft</code> status, use <a href=\"/docs/billing/subscriptions/subscription-schedules#managing\">subscription schedules</a> instead. Schedules provide the flexibility to model more complex billing configurations that change over time.</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///   The identifier of the customer to subscribe.
  ///
  /// * [List<AddInvoiceItemEntry>] addInvoiceItems:
  ///   A list of prices and quantities that will generate invoice items appended to the next invoice for this subscription. You may pass up to 20 items.
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestApplicationFeePercent] applicationFeePercent:
  ///
  /// * [AutomaticTaxConfig] automaticTax:
  ///
  /// * [int] backdateStartDate:
  ///   A past timestamp to backdate the subscription's start date to. If set, the first invoice will contain line items for the timespan between the start date and the current time. Can be combined with trials and the billing cycle anchor.
  ///
  /// * [int] billingCycleAnchor:
  ///   A future timestamp in UTC format to anchor the subscription's [billing cycle](https://stripe.com/docs/subscriptions/billing-cycle). The anchor is the reference point that aligns future billing cycle dates. It sets the day of week for `week` intervals, the day of month for `month` and `year` intervals, and the month of year for `year` intervals.
  ///
  /// * [BillingCycleAnchorConfigParam] billingCycleAnchorConfig:
  ///
  /// * [BillingMode1] billingMode:
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestBillingThresholds] billingThresholds:
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestCancelAt] cancelAt:
  ///
  /// * [bool] cancelAtPeriodEnd:
  ///   Indicate whether this subscription should cancel at the end of the current period (`current_period_end`). Defaults to `false`.
  ///
  /// * [String] collectionMethod:
  ///   Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay this subscription at the end of the cycle using the default source attached to the customer. When sending an invoice, Stripe will email your customer an invoice with payment instructions and mark the subscription as `active`. Defaults to `charge_automatically`.
  ///
  /// * [String] currency:
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [int] daysUntilDue:
  ///   Number of days a customer has to pay invoices generated by this subscription. Valid only for subscriptions where `collection_method` is set to `send_invoice`.
  ///
  /// * [String] defaultPaymentMethod:
  ///   ID of the default payment method for the subscription. It must belong to the customer associated with the subscription. This takes precedence over `default_source`. If neither are set, invoices will use the customer's [invoice_settings.default_payment_method](https://stripe.com/docs/api/customers/object#customer_object-invoice_settings-default_payment_method) or [default_source](https://stripe.com/docs/api/customers/object#customer_object-default_source).
  ///
  /// * [String] defaultSource:
  ///   ID of the default payment source for the subscription. It must belong to the customer associated with the subscription and be in a chargeable state. If `default_payment_method` is also set, `default_payment_method` will take precedence. If neither are set, invoices will use the customer's [invoice_settings.default_payment_method](https://stripe.com/docs/api/customers/object#customer_object-invoice_settings-default_payment_method) or [default_source](https://stripe.com/docs/api/customers/object#customer_object-default_source).
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestDefaultTaxRates] defaultTaxRates:
  ///
  /// * [String] description:
  ///   The subscription's description, meant to be displayable to the customer. Use this field to optionally store an explanation of the subscription for rendering in Stripe surfaces and certain local payment methods UIs.
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestDiscounts] discounts:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [InvoiceSettingsParam] invoiceSettings:
  ///
  /// * [List<SubscriptionItemCreateParams>] items:
  ///   A list of up to 20 subscription items, each with an attached price.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [bool] offSession:
  ///   Indicates if a customer is on or off-session while an invoice payment is attempted. Defaults to `false` (on-session).
  ///
  /// * [PostSubscriptionsRequestOnBehalfOf] onBehalfOf:
  ///
  /// * [String] paymentBehavior:
  ///   Only applies to subscriptions with `collection_method=charge_automatically`.  Use `allow_incomplete` to create Subscriptions with `status=incomplete` if the first invoice can't be paid. Creating Subscriptions with this status allows you to manage scenarios where additional customer actions are needed to pay a subscription's invoice. For example, SCA regulation may require 3DS authentication to complete payment. See the [SCA Migration Guide](https://stripe.com/docs/billing/migration/strong-customer-authentication) for Billing to learn more. This is the default behavior.  Use `default_incomplete` to create Subscriptions with `status=incomplete` when the first invoice requires payment, otherwise start as active. Subscriptions transition to `status=active` when successfully confirming the PaymentIntent on the first invoice. This allows simpler management of scenarios where additional customer actions are needed to pay a subscription’s invoice, such as failed payments, [SCA regulation](https://stripe.com/docs/billing/migration/strong-customer-authentication), or collecting a mandate for a bank debit payment method. If the PaymentIntent is not confirmed within 23 hours Subscriptions transition to `status=incomplete_expired`, which is a terminal state.  Use `error_if_incomplete` if you want Stripe to return an HTTP 402 status code if a subscription's first invoice can't be paid. For example, if a payment method requires 3DS authentication due to SCA regulation and further customer action is needed, this parameter doesn't create a Subscription and returns an error instead. This was the default behavior for API versions prior to 2019-03-14. See the [changelog](https://stripe.com/docs/upgrades#2019-03-14) to learn more.  `pending_if_incomplete` is only used with updates and cannot be passed when creating a Subscription.  Subscriptions with `collection_method=send_invoice` are automatically activated regardless of the first Invoice status.
  ///
  /// * [PaymentSettings] paymentSettings:
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestPendingInvoiceItemInterval] pendingInvoiceItemInterval:
  ///
  /// * [String] prorationBehavior:
  ///   Determines how to handle [prorations](https://stripe.com/docs/billing/subscriptions/prorations) resulting from the `billing_cycle_anchor`. If no value is passed, the default is `create_prorations`.
  ///
  /// * [TransferDataSpecs3] transferData:
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestTrialEnd] trialEnd:
  ///
  /// * [bool] trialFromPlan:
  ///   Indicates if a plan's `trial_period_days` should be applied to the subscription. Setting `trial_end` per subscription is preferred, and this defaults to `false`. Setting this flag to `true` together with `trial_end` is not allowed. See [Using trial periods on subscriptions](https://stripe.com/docs/billing/subscriptions/trials) to learn more.
  ///
  /// * [int] trialPeriodDays:
  ///   Integer representing the number of trial period days before the customer is charged for the first time. This will always overwrite any trials that might apply via a subscribed plan. See [Using trial periods on subscriptions](https://stripe.com/docs/billing/subscriptions/trials) to learn more.
  ///
  /// * [TrialSettingsConfig1] trialSettings:
  Future<Subscription?> postSubscriptions(String customer, { List<AddInvoiceItemEntry>? addInvoiceItems, PostCustomersCustomerSubscriptionsRequestApplicationFeePercent? applicationFeePercent, AutomaticTaxConfig? automaticTax, int? backdateStartDate, int? billingCycleAnchor, BillingCycleAnchorConfigParam? billingCycleAnchorConfig, BillingMode1? billingMode, PostCustomersCustomerSubscriptionsRequestBillingThresholds? billingThresholds, PostCustomersCustomerSubscriptionsRequestCancelAt? cancelAt, bool? cancelAtPeriodEnd, String? collectionMethod, String? currency, int? daysUntilDue, String? defaultPaymentMethod, String? defaultSource, PostCustomersCustomerSubscriptionsRequestDefaultTaxRates? defaultTaxRates, String? description, PostCustomersCustomerSubscriptionsRequestDiscounts? discounts, List<String>? expand, InvoiceSettingsParam? invoiceSettings, List<SubscriptionItemCreateParams>? items, PostAccountsRequestMetadata? metadata, bool? offSession, PostSubscriptionsRequestOnBehalfOf? onBehalfOf, String? paymentBehavior, PaymentSettings? paymentSettings, PostCustomersCustomerSubscriptionsRequestPendingInvoiceItemInterval? pendingInvoiceItemInterval, String? prorationBehavior, TransferDataSpecs3? transferData, PostCustomersCustomerSubscriptionsRequestTrialEnd? trialEnd, bool? trialFromPlan, int? trialPeriodDays, TrialSettingsConfig1? trialSettings, }) async {
    final response = await postSubscriptionsWithHttpInfo(customer,  addInvoiceItems: addInvoiceItems, applicationFeePercent: applicationFeePercent, automaticTax: automaticTax, backdateStartDate: backdateStartDate, billingCycleAnchor: billingCycleAnchor, billingCycleAnchorConfig: billingCycleAnchorConfig, billingMode: billingMode, billingThresholds: billingThresholds, cancelAt: cancelAt, cancelAtPeriodEnd: cancelAtPeriodEnd, collectionMethod: collectionMethod, currency: currency, daysUntilDue: daysUntilDue, defaultPaymentMethod: defaultPaymentMethod, defaultSource: defaultSource, defaultTaxRates: defaultTaxRates, description: description, discounts: discounts, expand: expand, invoiceSettings: invoiceSettings, items: items, metadata: metadata, offSession: offSession, onBehalfOf: onBehalfOf, paymentBehavior: paymentBehavior, paymentSettings: paymentSettings, pendingInvoiceItemInterval: pendingInvoiceItemInterval, prorationBehavior: prorationBehavior, transferData: transferData, trialEnd: trialEnd, trialFromPlan: trialFromPlan, trialPeriodDays: trialPeriodDays, trialSettings: trialSettings, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Subscription',) as Subscription;
    
    }
    return null;
  }

  /// Update a subscription
  ///
  /// <p>Updates an existing subscription to match the specified parameters. When changing prices or quantities, we optionally prorate the price we charge next month to make up for any price changes. To preview how the proration is calculated, use the <a href=\"/docs/api/invoices/create_preview\">create preview</a> endpoint.</p>  <p>By default, we prorate subscription changes. For example, if a customer signs up on May 1 for a <currency>100</currency> price, they’ll be billed <currency>100</currency> immediately. If on May 15 they switch to a <currency>200</currency> price, then on June 1 they’ll be billed <currency>250</currency> (<currency>200</currency> for a renewal of her subscription, plus a <currency>50</currency> prorating adjustment for half of the previous month’s <currency>100</currency> difference). Similarly, a downgrade generates a credit that is applied to the next invoice. We also prorate when you make quantity changes.</p>  <p>Switching prices does not normally change the billing date or generate an immediate charge unless:</p>  <ul> <li>The billing interval is changed (for example, from monthly to yearly).</li> <li>The subscription moves from free to paid.</li> <li>A trial starts or ends.</li> </ul>  <p>In these cases, we apply a credit for the unused time on the previous price, immediately charge the customer using the new price, and reset the billing date. Learn about how <a href=\"/docs/billing/subscriptions/upgrade-downgrade#immediate-payment\">Stripe immediately attempts payment for subscription changes</a>.</p>  <p>If you want to charge for an upgrade immediately, pass <code>proration_behavior</code> as <code>always_invoice</code> to create prorations, automatically invoice the customer for those proration adjustments, and attempt to collect payment. If you pass <code>create_prorations</code>, the prorations are created but not automatically invoiced. If you want to bill the customer for the prorations before the subscription’s renewal date, you need to manually <a href=\"/docs/api/invoices/create\">invoice the customer</a>.</p>  <p>If you don’t want to prorate, set the <code>proration_behavior</code> option to <code>none</code>. With this option, the customer is billed <currency>100</currency> on May 1 and <currency>200</currency> on June 1. Similarly, if you set <code>proration_behavior</code> to <code>none</code> when switching between different billing intervals (for example, from monthly to yearly), we don’t generate any credits for the old subscription’s unused time. We still reset the billing date and bill immediately for the new subscription.</p>  <p>Updating the quantity on a subscription many times in an hour may result in <a href=\"/docs/rate-limits\">rate limiting</a>. If you need to bill for a frequently changing quantity, consider integrating <a href=\"/docs/billing/subscriptions/usage-based\">usage-based billing</a> instead.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] subscriptionExposedId (required):
  ///
  /// * [List<AddInvoiceItemEntry>] addInvoiceItems:
  ///   A list of prices and quantities that will generate invoice items appended to the next invoice for this subscription. You may pass up to 20 items.
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestApplicationFeePercent] applicationFeePercent:
  ///
  /// * [AutomaticTaxConfig1] automaticTax:
  ///
  /// * [String] billingCycleAnchor:
  ///   Either `now` or `unchanged`. Setting the value to `now` resets the subscription's billing cycle anchor to the current time (in UTC). For more information, see the billing cycle [documentation](https://stripe.com/docs/billing/subscriptions/billing-cycle).
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestBillingThresholds] billingThresholds:
  ///
  /// * [PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestCancelAt] cancelAt:
  ///
  /// * [bool] cancelAtPeriodEnd:
  ///   Indicate whether this subscription should cancel at the end of the current period (`current_period_end`). Defaults to `false`.
  ///
  /// * [CancellationDetailsParam] cancellationDetails:
  ///
  /// * [String] collectionMethod:
  ///   Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay this subscription at the end of the cycle using the default source attached to the customer. When sending an invoice, Stripe will email your customer an invoice with payment instructions and mark the subscription as `active`. Defaults to `charge_automatically`.
  ///
  /// * [int] daysUntilDue:
  ///   Number of days a customer has to pay invoices generated by this subscription. Valid only for subscriptions where `collection_method` is set to `send_invoice`.
  ///
  /// * [String] defaultPaymentMethod:
  ///   ID of the default payment method for the subscription. It must belong to the customer associated with the subscription. This takes precedence over `default_source`. If neither are set, invoices will use the customer's [invoice_settings.default_payment_method](https://stripe.com/docs/api/customers/object#customer_object-invoice_settings-default_payment_method) or [default_source](https://stripe.com/docs/api/customers/object#customer_object-default_source).
  ///
  /// * [PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestDefaultSource] defaultSource:
  ///
  /// * [PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestDefaultTaxRates] defaultTaxRates:
  ///
  /// * [PostSubscriptionsSubscriptionExposedIdRequestDescription] description:
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestDiscounts] discounts:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [InvoiceSettingsParam] invoiceSettings:
  ///
  /// * [List<SubscriptionItemUpdateParams>] items:
  ///   A list of up to 20 subscription items, each with an attached price.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [bool] offSession:
  ///   Indicates if a customer is on or off-session while an invoice payment is attempted. Defaults to `false` (on-session).
  ///
  /// * [PostSubscriptionsRequestOnBehalfOf] onBehalfOf:
  ///
  /// * [PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestPauseCollection] pauseCollection:
  ///
  /// * [String] paymentBehavior:
  ///   Use `allow_incomplete` to transition the subscription to `status=past_due` if a payment is required but cannot be paid. This allows you to manage scenarios where additional user actions are needed to pay a subscription's invoice. For example, SCA regulation may require 3DS authentication to complete payment. See the [SCA Migration Guide](https://stripe.com/docs/billing/migration/strong-customer-authentication) for Billing to learn more. This is the default behavior.  Use `default_incomplete` to transition the subscription to `status=past_due` when payment is required and await explicit confirmation of the invoice's payment intent. This allows simpler management of scenarios where additional user actions are needed to pay a subscription’s invoice. Such as failed payments, [SCA regulation](https://stripe.com/docs/billing/migration/strong-customer-authentication), or collecting a mandate for a bank debit payment method.  Use `pending_if_incomplete` to update the subscription using [pending updates](https://stripe.com/docs/billing/subscriptions/pending-updates). When you use `pending_if_incomplete` you can only pass the parameters [supported by pending updates](https://stripe.com/docs/billing/pending-updates-reference#supported-attributes).  Use `error_if_incomplete` if you want Stripe to return an HTTP 402 status code if a subscription's invoice cannot be paid. For example, if a payment method requires 3DS authentication due to SCA regulation and further user action is needed, this parameter does not update the subscription and returns an error instead. This was the default behavior for API versions prior to 2019-03-14. See the [changelog](https://docs.stripe.com/changelog/2019-03-14) to learn more.
  ///
  /// * [PaymentSettings] paymentSettings:
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestPendingInvoiceItemInterval] pendingInvoiceItemInterval:
  ///
  /// * [String] prorationBehavior:
  ///   Determines how to handle [prorations](https://stripe.com/docs/billing/subscriptions/prorations) when the billing cycle changes (e.g., when switching plans, resetting `billing_cycle_anchor=now`, or starting a trial), or if an item's `quantity` changes. The default value is `create_prorations`.
  ///
  /// * [int] prorationDate:
  ///   If set, prorations will be calculated as though the subscription was updated at the given time. This can be used to apply exactly the same prorations that were previewed with the [create preview](https://stripe.com/docs/api/invoices/create_preview) endpoint. `proration_date` can also be used to implement custom proration logic, such as prorating by day instead of by second, by providing the time that you wish to use for proration calculations.
  ///
  /// * [PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestTransferData] transferData:
  ///
  /// * [PostSubscriptionsSubscriptionExposedIdRequestTrialEnd] trialEnd:
  ///
  /// * [bool] trialFromPlan:
  ///   Indicates if a plan's `trial_period_days` should be applied to the subscription. Setting `trial_end` per subscription is preferred, and this defaults to `false`. Setting this flag to `true` together with `trial_end` is not allowed. See [Using trial periods on subscriptions](https://stripe.com/docs/billing/subscriptions/trials) to learn more.
  ///
  /// * [TrialSettingsConfig1] trialSettings:
  Future<Response> postSubscriptionsSubscriptionExposedIdWithHttpInfo(String subscriptionExposedId, { List<AddInvoiceItemEntry>? addInvoiceItems, PostCustomersCustomerSubscriptionsRequestApplicationFeePercent? applicationFeePercent, AutomaticTaxConfig1? automaticTax, String? billingCycleAnchor, PostCustomersCustomerSubscriptionsRequestBillingThresholds? billingThresholds, PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestCancelAt? cancelAt, bool? cancelAtPeriodEnd, CancellationDetailsParam? cancellationDetails, String? collectionMethod, int? daysUntilDue, String? defaultPaymentMethod, PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestDefaultSource? defaultSource, PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestDefaultTaxRates? defaultTaxRates, PostSubscriptionsSubscriptionExposedIdRequestDescription? description, PostCustomersCustomerSubscriptionsRequestDiscounts? discounts, List<String>? expand, InvoiceSettingsParam? invoiceSettings, List<SubscriptionItemUpdateParams>? items, PostAccountsRequestMetadata? metadata, bool? offSession, PostSubscriptionsRequestOnBehalfOf? onBehalfOf, PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestPauseCollection? pauseCollection, String? paymentBehavior, PaymentSettings? paymentSettings, PostCustomersCustomerSubscriptionsRequestPendingInvoiceItemInterval? pendingInvoiceItemInterval, String? prorationBehavior, int? prorationDate, PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestTransferData? transferData, PostSubscriptionsSubscriptionExposedIdRequestTrialEnd? trialEnd, bool? trialFromPlan, TrialSettingsConfig1? trialSettings, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/subscriptions/{subscription_exposed_id}'
      .replaceAll('{subscription_exposed_id}', subscriptionExposedId);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (addInvoiceItems != null) {
      formParams[r'add_invoice_items'] = parameterToString(addInvoiceItems);
    }
    if (applicationFeePercent != null) {
      formParams[r'application_fee_percent'] = parameterToString(applicationFeePercent);
    }
    if (automaticTax != null) {
      formParams[r'automatic_tax'] = parameterToString(automaticTax);
    }
    if (billingCycleAnchor != null) {
      formParams[r'billing_cycle_anchor'] = parameterToString(billingCycleAnchor);
    }
    if (billingThresholds != null) {
      formParams[r'billing_thresholds'] = parameterToString(billingThresholds);
    }
    if (cancelAt != null) {
      formParams[r'cancel_at'] = parameterToString(cancelAt);
    }
    if (cancelAtPeriodEnd != null) {
      formParams[r'cancel_at_period_end'] = parameterToString(cancelAtPeriodEnd);
    }
    if (cancellationDetails != null) {
      formParams[r'cancellation_details'] = parameterToString(cancellationDetails);
    }
    if (collectionMethod != null) {
      formParams[r'collection_method'] = parameterToString(collectionMethod);
    }
    if (daysUntilDue != null) {
      formParams[r'days_until_due'] = parameterToString(daysUntilDue);
    }
    if (defaultPaymentMethod != null) {
      formParams[r'default_payment_method'] = parameterToString(defaultPaymentMethod);
    }
    if (defaultSource != null) {
      formParams[r'default_source'] = parameterToString(defaultSource);
    }
    if (defaultTaxRates != null) {
      formParams[r'default_tax_rates'] = parameterToString(defaultTaxRates);
    }
    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (discounts != null) {
      formParams[r'discounts'] = parameterToString(discounts);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (invoiceSettings != null) {
      formParams[r'invoice_settings'] = parameterToString(invoiceSettings);
    }
    if (items != null) {
      formParams[r'items'] = parameterToString(items);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (offSession != null) {
      formParams[r'off_session'] = parameterToString(offSession);
    }
    if (onBehalfOf != null) {
      formParams[r'on_behalf_of'] = parameterToString(onBehalfOf);
    }
    if (pauseCollection != null) {
      formParams[r'pause_collection'] = parameterToString(pauseCollection);
    }
    if (paymentBehavior != null) {
      formParams[r'payment_behavior'] = parameterToString(paymentBehavior);
    }
    if (paymentSettings != null) {
      formParams[r'payment_settings'] = parameterToString(paymentSettings);
    }
    if (pendingInvoiceItemInterval != null) {
      formParams[r'pending_invoice_item_interval'] = parameterToString(pendingInvoiceItemInterval);
    }
    if (prorationBehavior != null) {
      formParams[r'proration_behavior'] = parameterToString(prorationBehavior);
    }
    if (prorationDate != null) {
      formParams[r'proration_date'] = parameterToString(prorationDate);
    }
    if (transferData != null) {
      formParams[r'transfer_data'] = parameterToString(transferData);
    }
    if (trialEnd != null) {
      formParams[r'trial_end'] = parameterToString(trialEnd);
    }
    if (trialFromPlan != null) {
      formParams[r'trial_from_plan'] = parameterToString(trialFromPlan);
    }
    if (trialSettings != null) {
      formParams[r'trial_settings'] = parameterToString(trialSettings);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a subscription
  ///
  /// <p>Updates an existing subscription to match the specified parameters. When changing prices or quantities, we optionally prorate the price we charge next month to make up for any price changes. To preview how the proration is calculated, use the <a href=\"/docs/api/invoices/create_preview\">create preview</a> endpoint.</p>  <p>By default, we prorate subscription changes. For example, if a customer signs up on May 1 for a <currency>100</currency> price, they’ll be billed <currency>100</currency> immediately. If on May 15 they switch to a <currency>200</currency> price, then on June 1 they’ll be billed <currency>250</currency> (<currency>200</currency> for a renewal of her subscription, plus a <currency>50</currency> prorating adjustment for half of the previous month’s <currency>100</currency> difference). Similarly, a downgrade generates a credit that is applied to the next invoice. We also prorate when you make quantity changes.</p>  <p>Switching prices does not normally change the billing date or generate an immediate charge unless:</p>  <ul> <li>The billing interval is changed (for example, from monthly to yearly).</li> <li>The subscription moves from free to paid.</li> <li>A trial starts or ends.</li> </ul>  <p>In these cases, we apply a credit for the unused time on the previous price, immediately charge the customer using the new price, and reset the billing date. Learn about how <a href=\"/docs/billing/subscriptions/upgrade-downgrade#immediate-payment\">Stripe immediately attempts payment for subscription changes</a>.</p>  <p>If you want to charge for an upgrade immediately, pass <code>proration_behavior</code> as <code>always_invoice</code> to create prorations, automatically invoice the customer for those proration adjustments, and attempt to collect payment. If you pass <code>create_prorations</code>, the prorations are created but not automatically invoiced. If you want to bill the customer for the prorations before the subscription’s renewal date, you need to manually <a href=\"/docs/api/invoices/create\">invoice the customer</a>.</p>  <p>If you don’t want to prorate, set the <code>proration_behavior</code> option to <code>none</code>. With this option, the customer is billed <currency>100</currency> on May 1 and <currency>200</currency> on June 1. Similarly, if you set <code>proration_behavior</code> to <code>none</code> when switching between different billing intervals (for example, from monthly to yearly), we don’t generate any credits for the old subscription’s unused time. We still reset the billing date and bill immediately for the new subscription.</p>  <p>Updating the quantity on a subscription many times in an hour may result in <a href=\"/docs/rate-limits\">rate limiting</a>. If you need to bill for a frequently changing quantity, consider integrating <a href=\"/docs/billing/subscriptions/usage-based\">usage-based billing</a> instead.</p>
  ///
  /// Parameters:
  ///
  /// * [String] subscriptionExposedId (required):
  ///
  /// * [List<AddInvoiceItemEntry>] addInvoiceItems:
  ///   A list of prices and quantities that will generate invoice items appended to the next invoice for this subscription. You may pass up to 20 items.
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestApplicationFeePercent] applicationFeePercent:
  ///
  /// * [AutomaticTaxConfig1] automaticTax:
  ///
  /// * [String] billingCycleAnchor:
  ///   Either `now` or `unchanged`. Setting the value to `now` resets the subscription's billing cycle anchor to the current time (in UTC). For more information, see the billing cycle [documentation](https://stripe.com/docs/billing/subscriptions/billing-cycle).
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestBillingThresholds] billingThresholds:
  ///
  /// * [PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestCancelAt] cancelAt:
  ///
  /// * [bool] cancelAtPeriodEnd:
  ///   Indicate whether this subscription should cancel at the end of the current period (`current_period_end`). Defaults to `false`.
  ///
  /// * [CancellationDetailsParam] cancellationDetails:
  ///
  /// * [String] collectionMethod:
  ///   Either `charge_automatically`, or `send_invoice`. When charging automatically, Stripe will attempt to pay this subscription at the end of the cycle using the default source attached to the customer. When sending an invoice, Stripe will email your customer an invoice with payment instructions and mark the subscription as `active`. Defaults to `charge_automatically`.
  ///
  /// * [int] daysUntilDue:
  ///   Number of days a customer has to pay invoices generated by this subscription. Valid only for subscriptions where `collection_method` is set to `send_invoice`.
  ///
  /// * [String] defaultPaymentMethod:
  ///   ID of the default payment method for the subscription. It must belong to the customer associated with the subscription. This takes precedence over `default_source`. If neither are set, invoices will use the customer's [invoice_settings.default_payment_method](https://stripe.com/docs/api/customers/object#customer_object-invoice_settings-default_payment_method) or [default_source](https://stripe.com/docs/api/customers/object#customer_object-default_source).
  ///
  /// * [PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestDefaultSource] defaultSource:
  ///
  /// * [PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestDefaultTaxRates] defaultTaxRates:
  ///
  /// * [PostSubscriptionsSubscriptionExposedIdRequestDescription] description:
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestDiscounts] discounts:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [InvoiceSettingsParam] invoiceSettings:
  ///
  /// * [List<SubscriptionItemUpdateParams>] items:
  ///   A list of up to 20 subscription items, each with an attached price.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [bool] offSession:
  ///   Indicates if a customer is on or off-session while an invoice payment is attempted. Defaults to `false` (on-session).
  ///
  /// * [PostSubscriptionsRequestOnBehalfOf] onBehalfOf:
  ///
  /// * [PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestPauseCollection] pauseCollection:
  ///
  /// * [String] paymentBehavior:
  ///   Use `allow_incomplete` to transition the subscription to `status=past_due` if a payment is required but cannot be paid. This allows you to manage scenarios where additional user actions are needed to pay a subscription's invoice. For example, SCA regulation may require 3DS authentication to complete payment. See the [SCA Migration Guide](https://stripe.com/docs/billing/migration/strong-customer-authentication) for Billing to learn more. This is the default behavior.  Use `default_incomplete` to transition the subscription to `status=past_due` when payment is required and await explicit confirmation of the invoice's payment intent. This allows simpler management of scenarios where additional user actions are needed to pay a subscription’s invoice. Such as failed payments, [SCA regulation](https://stripe.com/docs/billing/migration/strong-customer-authentication), or collecting a mandate for a bank debit payment method.  Use `pending_if_incomplete` to update the subscription using [pending updates](https://stripe.com/docs/billing/subscriptions/pending-updates). When you use `pending_if_incomplete` you can only pass the parameters [supported by pending updates](https://stripe.com/docs/billing/pending-updates-reference#supported-attributes).  Use `error_if_incomplete` if you want Stripe to return an HTTP 402 status code if a subscription's invoice cannot be paid. For example, if a payment method requires 3DS authentication due to SCA regulation and further user action is needed, this parameter does not update the subscription and returns an error instead. This was the default behavior for API versions prior to 2019-03-14. See the [changelog](https://docs.stripe.com/changelog/2019-03-14) to learn more.
  ///
  /// * [PaymentSettings] paymentSettings:
  ///
  /// * [PostCustomersCustomerSubscriptionsRequestPendingInvoiceItemInterval] pendingInvoiceItemInterval:
  ///
  /// * [String] prorationBehavior:
  ///   Determines how to handle [prorations](https://stripe.com/docs/billing/subscriptions/prorations) when the billing cycle changes (e.g., when switching plans, resetting `billing_cycle_anchor=now`, or starting a trial), or if an item's `quantity` changes. The default value is `create_prorations`.
  ///
  /// * [int] prorationDate:
  ///   If set, prorations will be calculated as though the subscription was updated at the given time. This can be used to apply exactly the same prorations that were previewed with the [create preview](https://stripe.com/docs/api/invoices/create_preview) endpoint. `proration_date` can also be used to implement custom proration logic, such as prorating by day instead of by second, by providing the time that you wish to use for proration calculations.
  ///
  /// * [PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestTransferData] transferData:
  ///
  /// * [PostSubscriptionsSubscriptionExposedIdRequestTrialEnd] trialEnd:
  ///
  /// * [bool] trialFromPlan:
  ///   Indicates if a plan's `trial_period_days` should be applied to the subscription. Setting `trial_end` per subscription is preferred, and this defaults to `false`. Setting this flag to `true` together with `trial_end` is not allowed. See [Using trial periods on subscriptions](https://stripe.com/docs/billing/subscriptions/trials) to learn more.
  ///
  /// * [TrialSettingsConfig1] trialSettings:
  Future<Subscription?> postSubscriptionsSubscriptionExposedId(String subscriptionExposedId, { List<AddInvoiceItemEntry>? addInvoiceItems, PostCustomersCustomerSubscriptionsRequestApplicationFeePercent? applicationFeePercent, AutomaticTaxConfig1? automaticTax, String? billingCycleAnchor, PostCustomersCustomerSubscriptionsRequestBillingThresholds? billingThresholds, PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestCancelAt? cancelAt, bool? cancelAtPeriodEnd, CancellationDetailsParam? cancellationDetails, String? collectionMethod, int? daysUntilDue, String? defaultPaymentMethod, PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestDefaultSource? defaultSource, PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestDefaultTaxRates? defaultTaxRates, PostSubscriptionsSubscriptionExposedIdRequestDescription? description, PostCustomersCustomerSubscriptionsRequestDiscounts? discounts, List<String>? expand, InvoiceSettingsParam? invoiceSettings, List<SubscriptionItemUpdateParams>? items, PostAccountsRequestMetadata? metadata, bool? offSession, PostSubscriptionsRequestOnBehalfOf? onBehalfOf, PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestPauseCollection? pauseCollection, String? paymentBehavior, PaymentSettings? paymentSettings, PostCustomersCustomerSubscriptionsRequestPendingInvoiceItemInterval? pendingInvoiceItemInterval, String? prorationBehavior, int? prorationDate, PostCustomersCustomerSubscriptionsSubscriptionExposedIdRequestTransferData? transferData, PostSubscriptionsSubscriptionExposedIdRequestTrialEnd? trialEnd, bool? trialFromPlan, TrialSettingsConfig1? trialSettings, }) async {
    final response = await postSubscriptionsSubscriptionExposedIdWithHttpInfo(subscriptionExposedId,  addInvoiceItems: addInvoiceItems, applicationFeePercent: applicationFeePercent, automaticTax: automaticTax, billingCycleAnchor: billingCycleAnchor, billingThresholds: billingThresholds, cancelAt: cancelAt, cancelAtPeriodEnd: cancelAtPeriodEnd, cancellationDetails: cancellationDetails, collectionMethod: collectionMethod, daysUntilDue: daysUntilDue, defaultPaymentMethod: defaultPaymentMethod, defaultSource: defaultSource, defaultTaxRates: defaultTaxRates, description: description, discounts: discounts, expand: expand, invoiceSettings: invoiceSettings, items: items, metadata: metadata, offSession: offSession, onBehalfOf: onBehalfOf, pauseCollection: pauseCollection, paymentBehavior: paymentBehavior, paymentSettings: paymentSettings, pendingInvoiceItemInterval: pendingInvoiceItemInterval, prorationBehavior: prorationBehavior, prorationDate: prorationDate, transferData: transferData, trialEnd: trialEnd, trialFromPlan: trialFromPlan, trialSettings: trialSettings, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Subscription',) as Subscription;
    
    }
    return null;
  }

  /// Migrate a subscription
  ///
  /// <p>Upgrade the billing_mode of an existing subscription.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] subscription (required):
  ///
  /// * [BillingModeMigrate] billingMode (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postSubscriptionsSubscriptionMigrateWithHttpInfo(String subscription, BillingModeMigrate billingMode, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/subscriptions/{subscription}/migrate'
      .replaceAll('{subscription}', subscription);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (billingMode != null) {
      formParams[r'billing_mode'] = parameterToString(billingMode);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Migrate a subscription
  ///
  /// <p>Upgrade the billing_mode of an existing subscription.</p>
  ///
  /// Parameters:
  ///
  /// * [String] subscription (required):
  ///
  /// * [BillingModeMigrate] billingMode (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Subscription?> postSubscriptionsSubscriptionMigrate(String subscription, BillingModeMigrate billingMode, { List<String>? expand, }) async {
    final response = await postSubscriptionsSubscriptionMigrateWithHttpInfo(subscription, billingMode,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Subscription',) as Subscription;
    
    }
    return null;
  }

  /// Resume a subscription
  ///
  /// <p>Initiates resumption of a paused subscription, optionally resetting the billing cycle anchor and creating prorations. If a resumption invoice is generated, it must be paid or marked uncollectible before the subscription will be unpaused. If payment succeeds the subscription will become <code>active</code>, and if payment fails the subscription will be <code>past_due</code>. The resumption invoice will void automatically if not paid by the expiration date.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] subscription (required):
  ///
  /// * [String] billingCycleAnchor:
  ///   The billing cycle anchor that applies when the subscription is resumed. Either `now` or `unchanged`. The default is `now`. For more information, see the billing cycle [documentation](https://stripe.com/docs/billing/subscriptions/billing-cycle).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] prorationBehavior:
  ///   Determines how to handle [prorations](https://stripe.com/docs/billing/subscriptions/prorations) resulting from the `billing_cycle_anchor` being `unchanged`. When the `billing_cycle_anchor` is set to `now` (default value), no prorations are generated. If no value is passed, the default is `create_prorations`.
  ///
  /// * [int] prorationDate:
  ///   If set, prorations will be calculated as though the subscription was resumed at the given time. This can be used to apply exactly the same prorations that were previewed with the [create preview](https://stripe.com/docs/api/invoices/create_preview) endpoint.
  Future<Response> postSubscriptionsSubscriptionResumeWithHttpInfo(String subscription, { String? billingCycleAnchor, List<String>? expand, String? prorationBehavior, int? prorationDate, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/subscriptions/{subscription}/resume'
      .replaceAll('{subscription}', subscription);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (billingCycleAnchor != null) {
      formParams[r'billing_cycle_anchor'] = parameterToString(billingCycleAnchor);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (prorationBehavior != null) {
      formParams[r'proration_behavior'] = parameterToString(prorationBehavior);
    }
    if (prorationDate != null) {
      formParams[r'proration_date'] = parameterToString(prorationDate);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Resume a subscription
  ///
  /// <p>Initiates resumption of a paused subscription, optionally resetting the billing cycle anchor and creating prorations. If a resumption invoice is generated, it must be paid or marked uncollectible before the subscription will be unpaused. If payment succeeds the subscription will become <code>active</code>, and if payment fails the subscription will be <code>past_due</code>. The resumption invoice will void automatically if not paid by the expiration date.</p>
  ///
  /// Parameters:
  ///
  /// * [String] subscription (required):
  ///
  /// * [String] billingCycleAnchor:
  ///   The billing cycle anchor that applies when the subscription is resumed. Either `now` or `unchanged`. The default is `now`. For more information, see the billing cycle [documentation](https://stripe.com/docs/billing/subscriptions/billing-cycle).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] prorationBehavior:
  ///   Determines how to handle [prorations](https://stripe.com/docs/billing/subscriptions/prorations) resulting from the `billing_cycle_anchor` being `unchanged`. When the `billing_cycle_anchor` is set to `now` (default value), no prorations are generated. If no value is passed, the default is `create_prorations`.
  ///
  /// * [int] prorationDate:
  ///   If set, prorations will be calculated as though the subscription was resumed at the given time. This can be used to apply exactly the same prorations that were previewed with the [create preview](https://stripe.com/docs/api/invoices/create_preview) endpoint.
  Future<Subscription?> postSubscriptionsSubscriptionResume(String subscription, { String? billingCycleAnchor, List<String>? expand, String? prorationBehavior, int? prorationDate, }) async {
    final response = await postSubscriptionsSubscriptionResumeWithHttpInfo(subscription,  billingCycleAnchor: billingCycleAnchor, expand: expand, prorationBehavior: prorationBehavior, prorationDate: prorationDate, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Subscription',) as Subscription;
    
    }
    return null;
  }

  /// Create a Tax Calculation
  ///
  /// <p>Calculates tax based on the input and returns a Tax <code>Calculation</code> object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] currency (required):
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [List<CalculationLineItem>] lineItems (required):
  ///   A list of items the customer is purchasing.
  ///
  /// * [String] customer:
  ///   The ID of an existing customer to use for this calculation. If provided, the customer's address and tax IDs are copied to `customer_details`.
  ///
  /// * [CustomerDetails] customerDetails:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [ShipFromDetails] shipFromDetails:
  ///
  /// * [ShippingCost2] shippingCost:
  ///
  /// * [int] taxDate:
  ///   Timestamp of date at which the tax rules and rates in effect applies for the calculation. Measured in seconds since the Unix epoch. Can be up to 48 hours in the past, and up to 48 hours in the future.
  Future<Response> postTaxCalculationsWithHttpInfo(String currency, List<CalculationLineItem> lineItems, { String? customer, CustomerDetails? customerDetails, List<String>? expand, ShipFromDetails? shipFromDetails, ShippingCost2? shippingCost, int? taxDate, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/tax/calculations';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (customer != null) {
      formParams[r'customer'] = parameterToString(customer);
    }
    if (customerDetails != null) {
      formParams[r'customer_details'] = parameterToString(customerDetails);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (lineItems != null) {
      formParams[r'line_items'] = parameterToString(lineItems);
    }
    if (shipFromDetails != null) {
      formParams[r'ship_from_details'] = parameterToString(shipFromDetails);
    }
    if (shippingCost != null) {
      formParams[r'shipping_cost'] = parameterToString(shippingCost);
    }
    if (taxDate != null) {
      formParams[r'tax_date'] = parameterToString(taxDate);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a Tax Calculation
  ///
  /// <p>Calculates tax based on the input and returns a Tax <code>Calculation</code> object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] currency (required):
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [List<CalculationLineItem>] lineItems (required):
  ///   A list of items the customer is purchasing.
  ///
  /// * [String] customer:
  ///   The ID of an existing customer to use for this calculation. If provided, the customer's address and tax IDs are copied to `customer_details`.
  ///
  /// * [CustomerDetails] customerDetails:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [ShipFromDetails] shipFromDetails:
  ///
  /// * [ShippingCost2] shippingCost:
  ///
  /// * [int] taxDate:
  ///   Timestamp of date at which the tax rules and rates in effect applies for the calculation. Measured in seconds since the Unix epoch. Can be up to 48 hours in the past, and up to 48 hours in the future.
  Future<TaxCalculation?> postTaxCalculations(String currency, List<CalculationLineItem> lineItems, { String? customer, CustomerDetails? customerDetails, List<String>? expand, ShipFromDetails? shipFromDetails, ShippingCost2? shippingCost, int? taxDate, }) async {
    final response = await postTaxCalculationsWithHttpInfo(currency, lineItems,  customer: customer, customerDetails: customerDetails, expand: expand, shipFromDetails: shipFromDetails, shippingCost: shippingCost, taxDate: taxDate, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TaxCalculation',) as TaxCalculation;
    
    }
    return null;
  }

  /// Create a tax ID
  ///
  /// <p>Creates a new account or customer <code>tax_id</code> object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] type (required):
  ///   Type of the tax ID, one of `ad_nrt`, `ae_trn`, `al_tin`, `am_tin`, `ao_tin`, `ar_cuit`, `au_abn`, `au_arn`, `aw_tin`, `az_tin`, `ba_tin`, `bb_tin`, `bd_bin`, `bf_ifu`, `bg_uic`, `bh_vat`, `bj_ifu`, `bo_tin`, `br_cnpj`, `br_cpf`, `bs_tin`, `by_tin`, `ca_bn`, `ca_gst_hst`, `ca_pst_bc`, `ca_pst_mb`, `ca_pst_sk`, `ca_qst`, `cd_nif`, `ch_uid`, `ch_vat`, `cl_tin`, `cm_niu`, `cn_tin`, `co_nit`, `cr_tin`, `cv_nif`, `de_stn`, `do_rcn`, `ec_ruc`, `eg_tin`, `es_cif`, `et_tin`, `eu_oss_vat`, `eu_vat`, `gb_vat`, `ge_vat`, `gn_nif`, `hk_br`, `hr_oib`, `hu_tin`, `id_npwp`, `il_vat`, `in_gst`, `is_vat`, `jp_cn`, `jp_rn`, `jp_trn`, `ke_pin`, `kg_tin`, `kh_tin`, `kr_brn`, `kz_bin`, `la_tin`, `li_uid`, `li_vat`, `ma_vat`, `md_vat`, `me_pib`, `mk_vat`, `mr_nif`, `mx_rfc`, `my_frp`, `my_itn`, `my_sst`, `ng_tin`, `no_vat`, `no_voec`, `np_pan`, `nz_gst`, `om_vat`, `pe_ruc`, `ph_tin`, `ro_tin`, `rs_pib`, `ru_inn`, `ru_kpp`, `sa_vat`, `sg_gst`, `sg_uen`, `si_tin`, `sn_ninea`, `sr_fin`, `sv_nit`, `th_vat`, `tj_tin`, `tr_tin`, `tw_vat`, `tz_vat`, `ua_vat`, `ug_tin`, `us_ein`, `uy_ruc`, `uz_tin`, `uz_vat`, `ve_rif`, `vn_tin`, `za_vat`, `zm_tin`, or `zw_tin`
  ///
  /// * [String] value (required):
  ///   Value of the tax ID.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [OwnerParams1] owner:
  Future<Response> postTaxIdsWithHttpInfo(String type, String value, { List<String>? expand, OwnerParams1? owner, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/tax_ids';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (owner != null) {
      formParams[r'owner'] = parameterToString(owner);
    }
    if (type != null) {
      formParams[r'type'] = parameterToString(type);
    }
    if (value != null) {
      formParams[r'value'] = parameterToString(value);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a tax ID
  ///
  /// <p>Creates a new account or customer <code>tax_id</code> object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] type (required):
  ///   Type of the tax ID, one of `ad_nrt`, `ae_trn`, `al_tin`, `am_tin`, `ao_tin`, `ar_cuit`, `au_abn`, `au_arn`, `aw_tin`, `az_tin`, `ba_tin`, `bb_tin`, `bd_bin`, `bf_ifu`, `bg_uic`, `bh_vat`, `bj_ifu`, `bo_tin`, `br_cnpj`, `br_cpf`, `bs_tin`, `by_tin`, `ca_bn`, `ca_gst_hst`, `ca_pst_bc`, `ca_pst_mb`, `ca_pst_sk`, `ca_qst`, `cd_nif`, `ch_uid`, `ch_vat`, `cl_tin`, `cm_niu`, `cn_tin`, `co_nit`, `cr_tin`, `cv_nif`, `de_stn`, `do_rcn`, `ec_ruc`, `eg_tin`, `es_cif`, `et_tin`, `eu_oss_vat`, `eu_vat`, `gb_vat`, `ge_vat`, `gn_nif`, `hk_br`, `hr_oib`, `hu_tin`, `id_npwp`, `il_vat`, `in_gst`, `is_vat`, `jp_cn`, `jp_rn`, `jp_trn`, `ke_pin`, `kg_tin`, `kh_tin`, `kr_brn`, `kz_bin`, `la_tin`, `li_uid`, `li_vat`, `ma_vat`, `md_vat`, `me_pib`, `mk_vat`, `mr_nif`, `mx_rfc`, `my_frp`, `my_itn`, `my_sst`, `ng_tin`, `no_vat`, `no_voec`, `np_pan`, `nz_gst`, `om_vat`, `pe_ruc`, `ph_tin`, `ro_tin`, `rs_pib`, `ru_inn`, `ru_kpp`, `sa_vat`, `sg_gst`, `sg_uen`, `si_tin`, `sn_ninea`, `sr_fin`, `sv_nit`, `th_vat`, `tj_tin`, `tr_tin`, `tw_vat`, `tz_vat`, `ua_vat`, `ug_tin`, `us_ein`, `uy_ruc`, `uz_tin`, `uz_vat`, `ve_rif`, `vn_tin`, `za_vat`, `zm_tin`, or `zw_tin`
  ///
  /// * [String] value (required):
  ///   Value of the tax ID.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [OwnerParams1] owner:
  Future<TaxId?> postTaxIds(String type, String value, { List<String>? expand, OwnerParams1? owner, }) async {
    final response = await postTaxIdsWithHttpInfo(type, value,  expand: expand, owner: owner, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TaxId',) as TaxId;
    
    }
    return null;
  }

  /// Create a tax rate
  ///
  /// <p>Creates a new tax rate.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] displayName (required):
  ///   The display name of the tax rate, which will be shown to users.
  ///
  /// * [bool] inclusive (required):
  ///   This specifies if the tax rate is inclusive or exclusive.
  ///
  /// * [num] percentage (required):
  ///   This represents the tax rate percent out of 100.
  ///
  /// * [bool] active:
  ///   Flag determining whether the tax rate is active or inactive (archived). Inactive tax rates cannot be used with new applications or Checkout Sessions, but will still work for subscriptions and invoices that already have it set.
  ///
  /// * [String] country:
  ///   Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)).
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the tax rate for your internal use only. It will not be visible to your customers.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] jurisdiction:
  ///   The jurisdiction for the tax rate. You can use this label field for tax reporting purposes. It also appears on your customer’s invoice.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] state:
  ///   [ISO 3166-2 subdivision code](https://en.wikipedia.org/wiki/ISO_3166-2), without country prefix. For example, \\\"NY\\\" for New York, United States.
  ///
  /// * [String] taxType:
  ///   The high-level tax type, such as `vat` or `sales_tax`.
  Future<Response> postTaxRatesWithHttpInfo(String displayName, bool inclusive, num percentage, { bool? active, String? country, String? description, List<String>? expand, String? jurisdiction, Map<String, String>? metadata, String? state, String? taxType, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/tax_rates';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (active != null) {
      formParams[r'active'] = parameterToString(active);
    }
    if (country != null) {
      formParams[r'country'] = parameterToString(country);
    }
    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (displayName != null) {
      formParams[r'display_name'] = parameterToString(displayName);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (inclusive != null) {
      formParams[r'inclusive'] = parameterToString(inclusive);
    }
    if (jurisdiction != null) {
      formParams[r'jurisdiction'] = parameterToString(jurisdiction);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (percentage != null) {
      formParams[r'percentage'] = parameterToString(percentage);
    }
    if (state != null) {
      formParams[r'state'] = parameterToString(state);
    }
    if (taxType != null) {
      formParams[r'tax_type'] = parameterToString(taxType);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a tax rate
  ///
  /// <p>Creates a new tax rate.</p>
  ///
  /// Parameters:
  ///
  /// * [String] displayName (required):
  ///   The display name of the tax rate, which will be shown to users.
  ///
  /// * [bool] inclusive (required):
  ///   This specifies if the tax rate is inclusive or exclusive.
  ///
  /// * [num] percentage (required):
  ///   This represents the tax rate percent out of 100.
  ///
  /// * [bool] active:
  ///   Flag determining whether the tax rate is active or inactive (archived). Inactive tax rates cannot be used with new applications or Checkout Sessions, but will still work for subscriptions and invoices that already have it set.
  ///
  /// * [String] country:
  ///   Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)).
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the tax rate for your internal use only. It will not be visible to your customers.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] jurisdiction:
  ///   The jurisdiction for the tax rate. You can use this label field for tax reporting purposes. It also appears on your customer’s invoice.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] state:
  ///   [ISO 3166-2 subdivision code](https://en.wikipedia.org/wiki/ISO_3166-2), without country prefix. For example, \\\"NY\\\" for New York, United States.
  ///
  /// * [String] taxType:
  ///   The high-level tax type, such as `vat` or `sales_tax`.
  Future<TaxRate?> postTaxRates(String displayName, bool inclusive, num percentage, { bool? active, String? country, String? description, List<String>? expand, String? jurisdiction, Map<String, String>? metadata, String? state, String? taxType, }) async {
    final response = await postTaxRatesWithHttpInfo(displayName, inclusive, percentage,  active: active, country: country, description: description, expand: expand, jurisdiction: jurisdiction, metadata: metadata, state: state, taxType: taxType, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TaxRate',) as TaxRate;
    
    }
    return null;
  }

  /// Update a tax rate
  ///
  /// <p>Updates an existing tax rate.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] taxRate (required):
  ///
  /// * [bool] active:
  ///   Flag determining whether the tax rate is active or inactive (archived). Inactive tax rates cannot be used with new applications or Checkout Sessions, but will still work for subscriptions and invoices that already have it set.
  ///
  /// * [String] country:
  ///   Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)).
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the tax rate for your internal use only. It will not be visible to your customers.
  ///
  /// * [String] displayName:
  ///   The display name of the tax rate, which will be shown to users.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] jurisdiction:
  ///   The jurisdiction for the tax rate. You can use this label field for tax reporting purposes. It also appears on your customer’s invoice.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] state:
  ///   [ISO 3166-2 subdivision code](https://en.wikipedia.org/wiki/ISO_3166-2), without country prefix. For example, \\\"NY\\\" for New York, United States.
  ///
  /// * [String] taxType:
  ///   The high-level tax type, such as `vat` or `sales_tax`.
  Future<Response> postTaxRatesTaxRateWithHttpInfo(String taxRate, { bool? active, String? country, String? description, String? displayName, List<String>? expand, String? jurisdiction, PostAccountsRequestMetadata? metadata, String? state, String? taxType, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/tax_rates/{tax_rate}'
      .replaceAll('{tax_rate}', taxRate);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (active != null) {
      formParams[r'active'] = parameterToString(active);
    }
    if (country != null) {
      formParams[r'country'] = parameterToString(country);
    }
    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (displayName != null) {
      formParams[r'display_name'] = parameterToString(displayName);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (jurisdiction != null) {
      formParams[r'jurisdiction'] = parameterToString(jurisdiction);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (state != null) {
      formParams[r'state'] = parameterToString(state);
    }
    if (taxType != null) {
      formParams[r'tax_type'] = parameterToString(taxType);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a tax rate
  ///
  /// <p>Updates an existing tax rate.</p>
  ///
  /// Parameters:
  ///
  /// * [String] taxRate (required):
  ///
  /// * [bool] active:
  ///   Flag determining whether the tax rate is active or inactive (archived). Inactive tax rates cannot be used with new applications or Checkout Sessions, but will still work for subscriptions and invoices that already have it set.
  ///
  /// * [String] country:
  ///   Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)).
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the tax rate for your internal use only. It will not be visible to your customers.
  ///
  /// * [String] displayName:
  ///   The display name of the tax rate, which will be shown to users.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] jurisdiction:
  ///   The jurisdiction for the tax rate. You can use this label field for tax reporting purposes. It also appears on your customer’s invoice.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] state:
  ///   [ISO 3166-2 subdivision code](https://en.wikipedia.org/wiki/ISO_3166-2), without country prefix. For example, \\\"NY\\\" for New York, United States.
  ///
  /// * [String] taxType:
  ///   The high-level tax type, such as `vat` or `sales_tax`.
  Future<TaxRate?> postTaxRatesTaxRate(String taxRate, { bool? active, String? country, String? description, String? displayName, List<String>? expand, String? jurisdiction, PostAccountsRequestMetadata? metadata, String? state, String? taxType, }) async {
    final response = await postTaxRatesTaxRateWithHttpInfo(taxRate,  active: active, country: country, description: description, displayName: displayName, expand: expand, jurisdiction: jurisdiction, metadata: metadata, state: state, taxType: taxType, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TaxRate',) as TaxRate;
    
    }
    return null;
  }

  /// Create a registration
  ///
  /// <p>Creates a new Tax <code>Registration</code> object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [PostTaxRegistrationsRequestActiveFrom] activeFrom (required):
  ///
  /// * [String] country (required):
  ///   Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)).
  ///
  /// * [CountryOptions] countryOptions (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] expiresAt:
  ///   If set, the Tax Registration stops being active at this time. If not set, the Tax Registration will be active indefinitely. Timestamp measured in seconds since the Unix epoch.
  Future<Response> postTaxRegistrationsWithHttpInfo(PostTaxRegistrationsRequestActiveFrom activeFrom, String country, CountryOptions countryOptions, { List<String>? expand, int? expiresAt, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/tax/registrations';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (activeFrom != null) {
      formParams[r'active_from'] = parameterToString(activeFrom);
    }
    if (country != null) {
      formParams[r'country'] = parameterToString(country);
    }
    if (countryOptions != null) {
      formParams[r'country_options'] = parameterToString(countryOptions);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (expiresAt != null) {
      formParams[r'expires_at'] = parameterToString(expiresAt);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a registration
  ///
  /// <p>Creates a new Tax <code>Registration</code> object.</p>
  ///
  /// Parameters:
  ///
  /// * [PostTaxRegistrationsRequestActiveFrom] activeFrom (required):
  ///
  /// * [String] country (required):
  ///   Two-letter country code ([ISO 3166-1 alpha-2](https://en.wikipedia.org/wiki/ISO_3166-1_alpha-2)).
  ///
  /// * [CountryOptions] countryOptions (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] expiresAt:
  ///   If set, the Tax Registration stops being active at this time. If not set, the Tax Registration will be active indefinitely. Timestamp measured in seconds since the Unix epoch.
  Future<TaxRegistration?> postTaxRegistrations(PostTaxRegistrationsRequestActiveFrom activeFrom, String country, CountryOptions countryOptions, { List<String>? expand, int? expiresAt, }) async {
    final response = await postTaxRegistrationsWithHttpInfo(activeFrom, country, countryOptions,  expand: expand, expiresAt: expiresAt, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TaxRegistration',) as TaxRegistration;
    
    }
    return null;
  }

  /// Update a registration
  ///
  /// <p>Updates an existing Tax <code>Registration</code> object.</p>  <p>A registration cannot be deleted after it has been created. If you wish to end a registration you may do so by setting <code>expires_at</code>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [PostTaxRegistrationsIdRequestActiveFrom] activeFrom:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostTaxRegistrationsIdRequestExpiresAt] expiresAt:
  Future<Response> postTaxRegistrationsIdWithHttpInfo(String id, { PostTaxRegistrationsIdRequestActiveFrom? activeFrom, List<String>? expand, PostTaxRegistrationsIdRequestExpiresAt? expiresAt, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/tax/registrations/{id}'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (activeFrom != null) {
      formParams[r'active_from'] = parameterToString(activeFrom);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (expiresAt != null) {
      formParams[r'expires_at'] = parameterToString(expiresAt);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a registration
  ///
  /// <p>Updates an existing Tax <code>Registration</code> object.</p>  <p>A registration cannot be deleted after it has been created. If you wish to end a registration you may do so by setting <code>expires_at</code>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [PostTaxRegistrationsIdRequestActiveFrom] activeFrom:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostTaxRegistrationsIdRequestExpiresAt] expiresAt:
  Future<TaxRegistration?> postTaxRegistrationsId(String id, { PostTaxRegistrationsIdRequestActiveFrom? activeFrom, List<String>? expand, PostTaxRegistrationsIdRequestExpiresAt? expiresAt, }) async {
    final response = await postTaxRegistrationsIdWithHttpInfo(id,  activeFrom: activeFrom, expand: expand, expiresAt: expiresAt, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TaxRegistration',) as TaxRegistration;
    
    }
    return null;
  }

  /// Update settings
  ///
  /// <p>Updates Tax <code>Settings</code> parameters used in tax calculations. All parameters are editable but none can be removed once set.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [DefaultsParam] defaults:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [HeadOfficeParam] headOffice:
  Future<Response> postTaxSettingsWithHttpInfo({ DefaultsParam? defaults, List<String>? expand, HeadOfficeParam? headOffice, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/tax/settings';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (defaults != null) {
      formParams[r'defaults'] = parameterToString(defaults);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (headOffice != null) {
      formParams[r'head_office'] = parameterToString(headOffice);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update settings
  ///
  /// <p>Updates Tax <code>Settings</code> parameters used in tax calculations. All parameters are editable but none can be removed once set.</p>
  ///
  /// Parameters:
  ///
  /// * [DefaultsParam] defaults:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [HeadOfficeParam] headOffice:
  Future<TaxSettings?> postTaxSettings({ DefaultsParam? defaults, List<String>? expand, HeadOfficeParam? headOffice, }) async {
    final response = await postTaxSettingsWithHttpInfo( defaults: defaults, expand: expand, headOffice: headOffice, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TaxSettings',) as TaxSettings;
    
    }
    return null;
  }

  /// Create a transaction from a calculation
  ///
  /// <p>Creates a Tax Transaction from a calculation, if that calculation hasn’t expired. Calculations expire after 90 days.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] calculation (required):
  ///   Tax Calculation ID to be used as input when creating the transaction.
  ///
  /// * [String] reference (required):
  ///   A custom order or sale identifier, such as 'myOrder_123'. Must be unique across all transactions, including reversals.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [int] postedAt:
  ///   The Unix timestamp representing when the tax liability is assumed or reduced, which determines the liability posting period and handling in tax liability reports. The timestamp must fall within the `tax_date` and the current time, unless the `tax_date` is scheduled in advance. Defaults to the current time.
  Future<Response> postTaxTransactionsCreateFromCalculationWithHttpInfo(String calculation, String reference, { List<String>? expand, Map<String, String>? metadata, int? postedAt, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/tax/transactions/create_from_calculation';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (calculation != null) {
      formParams[r'calculation'] = parameterToString(calculation);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (postedAt != null) {
      formParams[r'posted_at'] = parameterToString(postedAt);
    }
    if (reference != null) {
      formParams[r'reference'] = parameterToString(reference);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a transaction from a calculation
  ///
  /// <p>Creates a Tax Transaction from a calculation, if that calculation hasn’t expired. Calculations expire after 90 days.</p>
  ///
  /// Parameters:
  ///
  /// * [String] calculation (required):
  ///   Tax Calculation ID to be used as input when creating the transaction.
  ///
  /// * [String] reference (required):
  ///   A custom order or sale identifier, such as 'myOrder_123'. Must be unique across all transactions, including reversals.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [int] postedAt:
  ///   The Unix timestamp representing when the tax liability is assumed or reduced, which determines the liability posting period and handling in tax liability reports. The timestamp must fall within the `tax_date` and the current time, unless the `tax_date` is scheduled in advance. Defaults to the current time.
  Future<TaxTransaction?> postTaxTransactionsCreateFromCalculation(String calculation, String reference, { List<String>? expand, Map<String, String>? metadata, int? postedAt, }) async {
    final response = await postTaxTransactionsCreateFromCalculationWithHttpInfo(calculation, reference,  expand: expand, metadata: metadata, postedAt: postedAt, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TaxTransaction',) as TaxTransaction;
    
    }
    return null;
  }

  /// Create a reversal transaction
  ///
  /// <p>Partially or fully reverses a previously created <code>Transaction</code>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] mode (required):
  ///   If `partial`, the provided line item or shipping cost amounts are reversed. If `full`, the original transaction is fully reversed.
  ///
  /// * [String] originalTransaction (required):
  ///   The ID of the Transaction to partially or fully reverse.
  ///
  /// * [String] reference (required):
  ///   A custom identifier for this reversal, such as `myOrder_123-refund_1`, which must be unique across all transactions. The reference helps identify this reversal transaction in exported [tax reports](https://stripe.com/docs/tax/reports).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] flatAmount:
  ///   A flat amount to reverse across the entire transaction, in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal) in negative. This value represents the total amount to refund from the transaction, including taxes.
  ///
  /// * [List<TransactionLineItemReversal>] lineItems:
  ///   The line item amounts to reverse.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [TransactionShippingCostReversal] shippingCost:
  Future<Response> postTaxTransactionsCreateReversalWithHttpInfo(String mode, String originalTransaction, String reference, { List<String>? expand, int? flatAmount, List<TransactionLineItemReversal>? lineItems, Map<String, String>? metadata, TransactionShippingCostReversal? shippingCost, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/tax/transactions/create_reversal';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (flatAmount != null) {
      formParams[r'flat_amount'] = parameterToString(flatAmount);
    }
    if (lineItems != null) {
      formParams[r'line_items'] = parameterToString(lineItems);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (mode != null) {
      formParams[r'mode'] = parameterToString(mode);
    }
    if (originalTransaction != null) {
      formParams[r'original_transaction'] = parameterToString(originalTransaction);
    }
    if (reference != null) {
      formParams[r'reference'] = parameterToString(reference);
    }
    if (shippingCost != null) {
      formParams[r'shipping_cost'] = parameterToString(shippingCost);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a reversal transaction
  ///
  /// <p>Partially or fully reverses a previously created <code>Transaction</code>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] mode (required):
  ///   If `partial`, the provided line item or shipping cost amounts are reversed. If `full`, the original transaction is fully reversed.
  ///
  /// * [String] originalTransaction (required):
  ///   The ID of the Transaction to partially or fully reverse.
  ///
  /// * [String] reference (required):
  ///   A custom identifier for this reversal, such as `myOrder_123-refund_1`, which must be unique across all transactions. The reference helps identify this reversal transaction in exported [tax reports](https://stripe.com/docs/tax/reports).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] flatAmount:
  ///   A flat amount to reverse across the entire transaction, in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal) in negative. This value represents the total amount to refund from the transaction, including taxes.
  ///
  /// * [List<TransactionLineItemReversal>] lineItems:
  ///   The line item amounts to reverse.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [TransactionShippingCostReversal] shippingCost:
  Future<TaxTransaction?> postTaxTransactionsCreateReversal(String mode, String originalTransaction, String reference, { List<String>? expand, int? flatAmount, List<TransactionLineItemReversal>? lineItems, Map<String, String>? metadata, TransactionShippingCostReversal? shippingCost, }) async {
    final response = await postTaxTransactionsCreateReversalWithHttpInfo(mode, originalTransaction, reference,  expand: expand, flatAmount: flatAmount, lineItems: lineItems, metadata: metadata, shippingCost: shippingCost, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TaxTransaction',) as TaxTransaction;
    
    }
    return null;
  }

  /// Create a Configuration
  ///
  /// <p>Creates a new <code>Configuration</code> object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [BbposWisePose] bbposWiseposE:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] name:
  ///   Name of the configuration
  ///
  /// * [PostTerminalConfigurationsRequestOffline] offline:
  ///
  /// * [RebootWindow] rebootWindow:
  ///
  /// * [StripeS700] stripeS700:
  ///
  /// * [PostTerminalConfigurationsRequestTipping] tipping:
  ///
  /// * [VerifoneP400] verifoneP400:
  ///
  /// * [PostTerminalConfigurationsRequestWifi] wifi:
  Future<Response> postTerminalConfigurationsWithHttpInfo({ BbposWisePose? bbposWiseposE, List<String>? expand, String? name, PostTerminalConfigurationsRequestOffline? offline, RebootWindow? rebootWindow, StripeS700? stripeS700, PostTerminalConfigurationsRequestTipping? tipping, VerifoneP400? verifoneP400, PostTerminalConfigurationsRequestWifi? wifi, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/terminal/configurations';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (bbposWiseposE != null) {
      formParams[r'bbpos_wisepos_e'] = parameterToString(bbposWiseposE);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (name != null) {
      formParams[r'name'] = parameterToString(name);
    }
    if (offline != null) {
      formParams[r'offline'] = parameterToString(offline);
    }
    if (rebootWindow != null) {
      formParams[r'reboot_window'] = parameterToString(rebootWindow);
    }
    if (stripeS700 != null) {
      formParams[r'stripe_s700'] = parameterToString(stripeS700);
    }
    if (tipping != null) {
      formParams[r'tipping'] = parameterToString(tipping);
    }
    if (verifoneP400 != null) {
      formParams[r'verifone_p400'] = parameterToString(verifoneP400);
    }
    if (wifi != null) {
      formParams[r'wifi'] = parameterToString(wifi);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a Configuration
  ///
  /// <p>Creates a new <code>Configuration</code> object.</p>
  ///
  /// Parameters:
  ///
  /// * [BbposWisePose] bbposWiseposE:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] name:
  ///   Name of the configuration
  ///
  /// * [PostTerminalConfigurationsRequestOffline] offline:
  ///
  /// * [RebootWindow] rebootWindow:
  ///
  /// * [StripeS700] stripeS700:
  ///
  /// * [PostTerminalConfigurationsRequestTipping] tipping:
  ///
  /// * [VerifoneP400] verifoneP400:
  ///
  /// * [PostTerminalConfigurationsRequestWifi] wifi:
  Future<TerminalConfiguration?> postTerminalConfigurations({ BbposWisePose? bbposWiseposE, List<String>? expand, String? name, PostTerminalConfigurationsRequestOffline? offline, RebootWindow? rebootWindow, StripeS700? stripeS700, PostTerminalConfigurationsRequestTipping? tipping, VerifoneP400? verifoneP400, PostTerminalConfigurationsRequestWifi? wifi, }) async {
    final response = await postTerminalConfigurationsWithHttpInfo( bbposWiseposE: bbposWiseposE, expand: expand, name: name, offline: offline, rebootWindow: rebootWindow, stripeS700: stripeS700, tipping: tipping, verifoneP400: verifoneP400, wifi: wifi, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TerminalConfiguration',) as TerminalConfiguration;
    
    }
    return null;
  }

  /// Update a Configuration
  ///
  /// <p>Updates a new <code>Configuration</code> object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] configuration (required):
  ///
  /// * [PostTerminalConfigurationsConfigurationRequestBbposWiseposE] bbposWiseposE:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] name:
  ///   Name of the configuration
  ///
  /// * [PostTerminalConfigurationsRequestOffline] offline:
  ///
  /// * [PostTerminalConfigurationsConfigurationRequestRebootWindow] rebootWindow:
  ///
  /// * [PostTerminalConfigurationsConfigurationRequestStripeS700] stripeS700:
  ///
  /// * [PostTerminalConfigurationsRequestTipping] tipping:
  ///
  /// * [PostTerminalConfigurationsConfigurationRequestVerifoneP400] verifoneP400:
  ///
  /// * [PostTerminalConfigurationsRequestWifi] wifi:
  Future<Response> postTerminalConfigurationsConfigurationWithHttpInfo(String configuration, { PostTerminalConfigurationsConfigurationRequestBbposWiseposE? bbposWiseposE, List<String>? expand, String? name, PostTerminalConfigurationsRequestOffline? offline, PostTerminalConfigurationsConfigurationRequestRebootWindow? rebootWindow, PostTerminalConfigurationsConfigurationRequestStripeS700? stripeS700, PostTerminalConfigurationsRequestTipping? tipping, PostTerminalConfigurationsConfigurationRequestVerifoneP400? verifoneP400, PostTerminalConfigurationsRequestWifi? wifi, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/terminal/configurations/{configuration}'
      .replaceAll('{configuration}', configuration);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (bbposWiseposE != null) {
      formParams[r'bbpos_wisepos_e'] = parameterToString(bbposWiseposE);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (name != null) {
      formParams[r'name'] = parameterToString(name);
    }
    if (offline != null) {
      formParams[r'offline'] = parameterToString(offline);
    }
    if (rebootWindow != null) {
      formParams[r'reboot_window'] = parameterToString(rebootWindow);
    }
    if (stripeS700 != null) {
      formParams[r'stripe_s700'] = parameterToString(stripeS700);
    }
    if (tipping != null) {
      formParams[r'tipping'] = parameterToString(tipping);
    }
    if (verifoneP400 != null) {
      formParams[r'verifone_p400'] = parameterToString(verifoneP400);
    }
    if (wifi != null) {
      formParams[r'wifi'] = parameterToString(wifi);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a Configuration
  ///
  /// <p>Updates a new <code>Configuration</code> object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] configuration (required):
  ///
  /// * [PostTerminalConfigurationsConfigurationRequestBbposWiseposE] bbposWiseposE:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] name:
  ///   Name of the configuration
  ///
  /// * [PostTerminalConfigurationsRequestOffline] offline:
  ///
  /// * [PostTerminalConfigurationsConfigurationRequestRebootWindow] rebootWindow:
  ///
  /// * [PostTerminalConfigurationsConfigurationRequestStripeS700] stripeS700:
  ///
  /// * [PostTerminalConfigurationsRequestTipping] tipping:
  ///
  /// * [PostTerminalConfigurationsConfigurationRequestVerifoneP400] verifoneP400:
  ///
  /// * [PostTerminalConfigurationsRequestWifi] wifi:
  Future<GetTerminalConfigurationsConfiguration200Response?> postTerminalConfigurationsConfiguration(String configuration, { PostTerminalConfigurationsConfigurationRequestBbposWiseposE? bbposWiseposE, List<String>? expand, String? name, PostTerminalConfigurationsRequestOffline? offline, PostTerminalConfigurationsConfigurationRequestRebootWindow? rebootWindow, PostTerminalConfigurationsConfigurationRequestStripeS700? stripeS700, PostTerminalConfigurationsRequestTipping? tipping, PostTerminalConfigurationsConfigurationRequestVerifoneP400? verifoneP400, PostTerminalConfigurationsRequestWifi? wifi, }) async {
    final response = await postTerminalConfigurationsConfigurationWithHttpInfo(configuration,  bbposWiseposE: bbposWiseposE, expand: expand, name: name, offline: offline, rebootWindow: rebootWindow, stripeS700: stripeS700, tipping: tipping, verifoneP400: verifoneP400, wifi: wifi, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'GetTerminalConfigurationsConfiguration200Response',) as GetTerminalConfigurationsConfiguration200Response;
    
    }
    return null;
  }

  /// Create a Connection Token
  ///
  /// <p>To connect to a reader the Stripe Terminal SDK needs to retrieve a short-lived connection token from Stripe, proxied through your server. On your backend, add an endpoint that creates and returns a connection token.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] location:
  ///   The id of the location that this connection token is scoped to. If specified the connection token will only be usable with readers assigned to that location, otherwise the connection token will be usable with all readers. Note that location scoping only applies to internet-connected readers. For more details, see [the docs on scoping connection tokens](https://docs.stripe.com/terminal/fleet/locations-and-zones?dashboard-or-api=api#connection-tokens).
  Future<Response> postTerminalConnectionTokensWithHttpInfo({ List<String>? expand, String? location, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/terminal/connection_tokens';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (location != null) {
      formParams[r'location'] = parameterToString(location);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a Connection Token
  ///
  /// <p>To connect to a reader the Stripe Terminal SDK needs to retrieve a short-lived connection token from Stripe, proxied through your server. On your backend, add an endpoint that creates and returns a connection token.</p>
  ///
  /// Parameters:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] location:
  ///   The id of the location that this connection token is scoped to. If specified the connection token will only be usable with readers assigned to that location, otherwise the connection token will be usable with all readers. Note that location scoping only applies to internet-connected readers. For more details, see [the docs on scoping connection tokens](https://docs.stripe.com/terminal/fleet/locations-and-zones?dashboard-or-api=api#connection-tokens).
  Future<TerminalConnectionToken?> postTerminalConnectionTokens({ List<String>? expand, String? location, }) async {
    final response = await postTerminalConnectionTokensWithHttpInfo( expand: expand, location: location, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TerminalConnectionToken',) as TerminalConnectionToken;
    
    }
    return null;
  }

  /// Create a Location
  ///
  /// <p>Creates a new <code>Location</code> object. For further details, including which address fields are required in each country, see the <a href=\"/docs/terminal/fleet/locations\">Manage locations</a> guide.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [CreateLocationAddressParam] address (required):
  ///
  /// * [String] displayName (required):
  ///   A name for the location. Maximum length is 1000 characters.
  ///
  /// * [String] configurationOverrides:
  ///   The ID of a configuration that will be used to customize all readers in this location.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<Response> postTerminalLocationsWithHttpInfo(CreateLocationAddressParam address, String displayName, { String? configurationOverrides, List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/terminal/locations';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (address != null) {
      formParams[r'address'] = parameterToString(address);
    }
    if (configurationOverrides != null) {
      formParams[r'configuration_overrides'] = parameterToString(configurationOverrides);
    }
    if (displayName != null) {
      formParams[r'display_name'] = parameterToString(displayName);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a Location
  ///
  /// <p>Creates a new <code>Location</code> object. For further details, including which address fields are required in each country, see the <a href=\"/docs/terminal/fleet/locations\">Manage locations</a> guide.</p>
  ///
  /// Parameters:
  ///
  /// * [CreateLocationAddressParam] address (required):
  ///
  /// * [String] displayName (required):
  ///   A name for the location. Maximum length is 1000 characters.
  ///
  /// * [String] configurationOverrides:
  ///   The ID of a configuration that will be used to customize all readers in this location.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<TerminalLocation?> postTerminalLocations(CreateLocationAddressParam address, String displayName, { String? configurationOverrides, List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    final response = await postTerminalLocationsWithHttpInfo(address, displayName,  configurationOverrides: configurationOverrides, expand: expand, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TerminalLocation',) as TerminalLocation;
    
    }
    return null;
  }

  /// Update a Location
  ///
  /// <p>Updates a <code>Location</code> object by setting the values of the parameters passed. Any parameters not provided will be left unchanged.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] location (required):
  ///
  /// * [OptionalFieldsAddress1] address:
  ///
  /// * [PostTerminalLocationsLocationRequestConfigurationOverrides] configurationOverrides:
  ///
  /// * [PostTerminalLocationsLocationRequestDisplayName] displayName:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<Response> postTerminalLocationsLocationWithHttpInfo(String location, { OptionalFieldsAddress1? address, PostTerminalLocationsLocationRequestConfigurationOverrides? configurationOverrides, PostTerminalLocationsLocationRequestDisplayName? displayName, List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/terminal/locations/{location}'
      .replaceAll('{location}', location);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (address != null) {
      formParams[r'address'] = parameterToString(address);
    }
    if (configurationOverrides != null) {
      formParams[r'configuration_overrides'] = parameterToString(configurationOverrides);
    }
    if (displayName != null) {
      formParams[r'display_name'] = parameterToString(displayName);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a Location
  ///
  /// <p>Updates a <code>Location</code> object by setting the values of the parameters passed. Any parameters not provided will be left unchanged.</p>
  ///
  /// Parameters:
  ///
  /// * [String] location (required):
  ///
  /// * [OptionalFieldsAddress1] address:
  ///
  /// * [PostTerminalLocationsLocationRequestConfigurationOverrides] configurationOverrides:
  ///
  /// * [PostTerminalLocationsLocationRequestDisplayName] displayName:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<GetTerminalLocationsLocation200Response?> postTerminalLocationsLocation(String location, { OptionalFieldsAddress1? address, PostTerminalLocationsLocationRequestConfigurationOverrides? configurationOverrides, PostTerminalLocationsLocationRequestDisplayName? displayName, List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    final response = await postTerminalLocationsLocationWithHttpInfo(location,  address: address, configurationOverrides: configurationOverrides, displayName: displayName, expand: expand, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'GetTerminalLocationsLocation200Response',) as GetTerminalLocationsLocation200Response;
    
    }
    return null;
  }

  /// Create a Reader
  ///
  /// <p>Creates a new <code>Reader</code> object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] registrationCode (required):
  ///   A code generated by the reader used for registering to an account.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] label:
  ///   Custom label given to the reader for easier identification. If no label is specified, the registration code will be used.
  ///
  /// * [String] location:
  ///   The location to assign the reader to.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<Response> postTerminalReadersWithHttpInfo(String registrationCode, { List<String>? expand, String? label, String? location, PostAccountsRequestMetadata? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/terminal/readers';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (label != null) {
      formParams[r'label'] = parameterToString(label);
    }
    if (location != null) {
      formParams[r'location'] = parameterToString(location);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (registrationCode != null) {
      formParams[r'registration_code'] = parameterToString(registrationCode);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a Reader
  ///
  /// <p>Creates a new <code>Reader</code> object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] registrationCode (required):
  ///   A code generated by the reader used for registering to an account.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] label:
  ///   Custom label given to the reader for easier identification. If no label is specified, the registration code will be used.
  ///
  /// * [String] location:
  ///   The location to assign the reader to.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<TerminalReader?> postTerminalReaders(String registrationCode, { List<String>? expand, String? label, String? location, PostAccountsRequestMetadata? metadata, }) async {
    final response = await postTerminalReadersWithHttpInfo(registrationCode,  expand: expand, label: label, location: location, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TerminalReader',) as TerminalReader;
    
    }
    return null;
  }

  /// Update a Reader
  ///
  /// <p>Updates a <code>Reader</code> object by setting the values of the parameters passed. Any parameters not provided will be left unchanged.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] reader (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostTerminalReadersReaderRequestLabel] label:
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<Response> postTerminalReadersReaderWithHttpInfo(String reader, { List<String>? expand, PostTerminalReadersReaderRequestLabel? label, PostAccountsRequestMetadata? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/terminal/readers/{reader}'
      .replaceAll('{reader}', reader);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (label != null) {
      formParams[r'label'] = parameterToString(label);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a Reader
  ///
  /// <p>Updates a <code>Reader</code> object by setting the values of the parameters passed. Any parameters not provided will be left unchanged.</p>
  ///
  /// Parameters:
  ///
  /// * [String] reader (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostTerminalReadersReaderRequestLabel] label:
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<GetTerminalReadersReader200Response?> postTerminalReadersReader(String reader, { List<String>? expand, PostTerminalReadersReaderRequestLabel? label, PostAccountsRequestMetadata? metadata, }) async {
    final response = await postTerminalReadersReaderWithHttpInfo(reader,  expand: expand, label: label, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'GetTerminalReadersReader200Response',) as GetTerminalReadersReader200Response;
    
    }
    return null;
  }

  /// Cancel the current reader action
  ///
  /// <p>Cancels the current reader action.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] reader (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postTerminalReadersReaderCancelActionWithHttpInfo(String reader, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/terminal/readers/{reader}/cancel_action'
      .replaceAll('{reader}', reader);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Cancel the current reader action
  ///
  /// <p>Cancels the current reader action.</p>
  ///
  /// Parameters:
  ///
  /// * [String] reader (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TerminalReader?> postTerminalReadersReaderCancelAction(String reader, { List<String>? expand, }) async {
    final response = await postTerminalReadersReaderCancelActionWithHttpInfo(reader,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TerminalReader',) as TerminalReader;
    
    }
    return null;
  }

  /// Collect inputs using a Reader
  ///
  /// <p>Initiates an input collection flow on a Reader.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] reader (required):
  ///
  /// * [List<InputParams>] inputs (required):
  ///   List of inputs to be collected using the Reader
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  Future<Response> postTerminalReadersReaderCollectInputsWithHttpInfo(String reader, List<InputParams> inputs, { List<String>? expand, Map<String, String>? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/terminal/readers/{reader}/collect_inputs'
      .replaceAll('{reader}', reader);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (inputs != null) {
      formParams[r'inputs'] = parameterToString(inputs);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Collect inputs using a Reader
  ///
  /// <p>Initiates an input collection flow on a Reader.</p>
  ///
  /// Parameters:
  ///
  /// * [String] reader (required):
  ///
  /// * [List<InputParams>] inputs (required):
  ///   List of inputs to be collected using the Reader
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  Future<TerminalReader?> postTerminalReadersReaderCollectInputs(String reader, List<InputParams> inputs, { List<String>? expand, Map<String, String>? metadata, }) async {
    final response = await postTerminalReadersReaderCollectInputsWithHttpInfo(reader, inputs,  expand: expand, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TerminalReader',) as TerminalReader;
    
    }
    return null;
  }

  /// Hand off a PaymentIntent to a Reader and collect card details
  ///
  /// <p>Initiates a payment flow on a Reader and updates the PaymentIntent with card details before manual confirmation.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] reader (required):
  ///
  /// * [String] paymentIntent (required):
  ///   PaymentIntent ID.
  ///
  /// * [CollectConfig] collectConfig:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postTerminalReadersReaderCollectPaymentMethodWithHttpInfo(String reader, String paymentIntent, { CollectConfig? collectConfig, List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/terminal/readers/{reader}/collect_payment_method'
      .replaceAll('{reader}', reader);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (collectConfig != null) {
      formParams[r'collect_config'] = parameterToString(collectConfig);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (paymentIntent != null) {
      formParams[r'payment_intent'] = parameterToString(paymentIntent);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Hand off a PaymentIntent to a Reader and collect card details
  ///
  /// <p>Initiates a payment flow on a Reader and updates the PaymentIntent with card details before manual confirmation.</p>
  ///
  /// Parameters:
  ///
  /// * [String] reader (required):
  ///
  /// * [String] paymentIntent (required):
  ///   PaymentIntent ID.
  ///
  /// * [CollectConfig] collectConfig:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TerminalReader?> postTerminalReadersReaderCollectPaymentMethod(String reader, String paymentIntent, { CollectConfig? collectConfig, List<String>? expand, }) async {
    final response = await postTerminalReadersReaderCollectPaymentMethodWithHttpInfo(reader, paymentIntent,  collectConfig: collectConfig, expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TerminalReader',) as TerminalReader;
    
    }
    return null;
  }

  /// Confirm a PaymentIntent on the Reader
  ///
  /// <p>Finalizes a payment on a Reader.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] reader (required):
  ///
  /// * [String] paymentIntent (required):
  ///   PaymentIntent ID.
  ///
  /// * [ConfirmConfig] confirmConfig:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postTerminalReadersReaderConfirmPaymentIntentWithHttpInfo(String reader, String paymentIntent, { ConfirmConfig? confirmConfig, List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/terminal/readers/{reader}/confirm_payment_intent'
      .replaceAll('{reader}', reader);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (confirmConfig != null) {
      formParams[r'confirm_config'] = parameterToString(confirmConfig);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (paymentIntent != null) {
      formParams[r'payment_intent'] = parameterToString(paymentIntent);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Confirm a PaymentIntent on the Reader
  ///
  /// <p>Finalizes a payment on a Reader.</p>
  ///
  /// Parameters:
  ///
  /// * [String] reader (required):
  ///
  /// * [String] paymentIntent (required):
  ///   PaymentIntent ID.
  ///
  /// * [ConfirmConfig] confirmConfig:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TerminalReader?> postTerminalReadersReaderConfirmPaymentIntent(String reader, String paymentIntent, { ConfirmConfig? confirmConfig, List<String>? expand, }) async {
    final response = await postTerminalReadersReaderConfirmPaymentIntentWithHttpInfo(reader, paymentIntent,  confirmConfig: confirmConfig, expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TerminalReader',) as TerminalReader;
    
    }
    return null;
  }

  /// Hand-off a PaymentIntent to a Reader
  ///
  /// <p>Initiates a payment flow on a Reader.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] reader (required):
  ///
  /// * [String] paymentIntent (required):
  ///   PaymentIntent ID
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [ProcessConfig] processConfig:
  Future<Response> postTerminalReadersReaderProcessPaymentIntentWithHttpInfo(String reader, String paymentIntent, { List<String>? expand, ProcessConfig? processConfig, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/terminal/readers/{reader}/process_payment_intent'
      .replaceAll('{reader}', reader);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (paymentIntent != null) {
      formParams[r'payment_intent'] = parameterToString(paymentIntent);
    }
    if (processConfig != null) {
      formParams[r'process_config'] = parameterToString(processConfig);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Hand-off a PaymentIntent to a Reader
  ///
  /// <p>Initiates a payment flow on a Reader.</p>
  ///
  /// Parameters:
  ///
  /// * [String] reader (required):
  ///
  /// * [String] paymentIntent (required):
  ///   PaymentIntent ID
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [ProcessConfig] processConfig:
  Future<TerminalReader?> postTerminalReadersReaderProcessPaymentIntent(String reader, String paymentIntent, { List<String>? expand, ProcessConfig? processConfig, }) async {
    final response = await postTerminalReadersReaderProcessPaymentIntentWithHttpInfo(reader, paymentIntent,  expand: expand, processConfig: processConfig, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TerminalReader',) as TerminalReader;
    
    }
    return null;
  }

  /// Hand-off a SetupIntent to a Reader
  ///
  /// <p>Initiates a setup intent flow on a Reader.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] reader (required):
  ///
  /// * [String] allowRedisplay (required):
  ///   This field indicates whether this payment method can be shown again to its customer in a checkout flow. Stripe products such as Checkout and Elements use this field to determine whether a payment method can be shown as a saved payment method in a checkout flow.
  ///
  /// * [String] setupIntent (required):
  ///   SetupIntent ID
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [ProcessSetupConfig] processConfig:
  Future<Response> postTerminalReadersReaderProcessSetupIntentWithHttpInfo(String reader, String allowRedisplay, String setupIntent, { List<String>? expand, ProcessSetupConfig? processConfig, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/terminal/readers/{reader}/process_setup_intent'
      .replaceAll('{reader}', reader);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (allowRedisplay != null) {
      formParams[r'allow_redisplay'] = parameterToString(allowRedisplay);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (processConfig != null) {
      formParams[r'process_config'] = parameterToString(processConfig);
    }
    if (setupIntent != null) {
      formParams[r'setup_intent'] = parameterToString(setupIntent);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Hand-off a SetupIntent to a Reader
  ///
  /// <p>Initiates a setup intent flow on a Reader.</p>
  ///
  /// Parameters:
  ///
  /// * [String] reader (required):
  ///
  /// * [String] allowRedisplay (required):
  ///   This field indicates whether this payment method can be shown again to its customer in a checkout flow. Stripe products such as Checkout and Elements use this field to determine whether a payment method can be shown as a saved payment method in a checkout flow.
  ///
  /// * [String] setupIntent (required):
  ///   SetupIntent ID
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [ProcessSetupConfig] processConfig:
  Future<TerminalReader?> postTerminalReadersReaderProcessSetupIntent(String reader, String allowRedisplay, String setupIntent, { List<String>? expand, ProcessSetupConfig? processConfig, }) async {
    final response = await postTerminalReadersReaderProcessSetupIntentWithHttpInfo(reader, allowRedisplay, setupIntent,  expand: expand, processConfig: processConfig, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TerminalReader',) as TerminalReader;
    
    }
    return null;
  }

  /// Refund a Charge or a PaymentIntent in-person
  ///
  /// <p>Initiates a refund on a Reader</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] reader (required):
  ///
  /// * [int] amount:
  ///   A positive integer in __cents__ representing how much of this charge to refund.
  ///
  /// * [String] charge:
  ///   ID of the Charge to refund.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] paymentIntent:
  ///   ID of the PaymentIntent to refund.
  ///
  /// * [bool] refundApplicationFee:
  ///   Boolean indicating whether the application fee should be refunded when refunding this charge. If a full charge refund is given, the full application fee will be refunded. Otherwise, the application fee will be refunded in an amount proportional to the amount of the charge refunded. An application fee can be refunded only by the application that created the charge.
  ///
  /// * [RefundPaymentConfig] refundPaymentConfig:
  ///
  /// * [bool] reverseTransfer:
  ///   Boolean indicating whether the transfer should be reversed when refunding this charge. The transfer will be reversed proportionally to the amount being refunded (either the entire or partial amount). A transfer can be reversed only by the application that created the charge.
  Future<Response> postTerminalReadersReaderRefundPaymentWithHttpInfo(String reader, { int? amount, String? charge, List<String>? expand, Map<String, String>? metadata, String? paymentIntent, bool? refundApplicationFee, RefundPaymentConfig? refundPaymentConfig, bool? reverseTransfer, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/terminal/readers/{reader}/refund_payment'
      .replaceAll('{reader}', reader);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (charge != null) {
      formParams[r'charge'] = parameterToString(charge);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (paymentIntent != null) {
      formParams[r'payment_intent'] = parameterToString(paymentIntent);
    }
    if (refundApplicationFee != null) {
      formParams[r'refund_application_fee'] = parameterToString(refundApplicationFee);
    }
    if (refundPaymentConfig != null) {
      formParams[r'refund_payment_config'] = parameterToString(refundPaymentConfig);
    }
    if (reverseTransfer != null) {
      formParams[r'reverse_transfer'] = parameterToString(reverseTransfer);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Refund a Charge or a PaymentIntent in-person
  ///
  /// <p>Initiates a refund on a Reader</p>
  ///
  /// Parameters:
  ///
  /// * [String] reader (required):
  ///
  /// * [int] amount:
  ///   A positive integer in __cents__ representing how much of this charge to refund.
  ///
  /// * [String] charge:
  ///   ID of the Charge to refund.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] paymentIntent:
  ///   ID of the PaymentIntent to refund.
  ///
  /// * [bool] refundApplicationFee:
  ///   Boolean indicating whether the application fee should be refunded when refunding this charge. If a full charge refund is given, the full application fee will be refunded. Otherwise, the application fee will be refunded in an amount proportional to the amount of the charge refunded. An application fee can be refunded only by the application that created the charge.
  ///
  /// * [RefundPaymentConfig] refundPaymentConfig:
  ///
  /// * [bool] reverseTransfer:
  ///   Boolean indicating whether the transfer should be reversed when refunding this charge. The transfer will be reversed proportionally to the amount being refunded (either the entire or partial amount). A transfer can be reversed only by the application that created the charge.
  Future<TerminalReader?> postTerminalReadersReaderRefundPayment(String reader, { int? amount, String? charge, List<String>? expand, Map<String, String>? metadata, String? paymentIntent, bool? refundApplicationFee, RefundPaymentConfig? refundPaymentConfig, bool? reverseTransfer, }) async {
    final response = await postTerminalReadersReaderRefundPaymentWithHttpInfo(reader,  amount: amount, charge: charge, expand: expand, metadata: metadata, paymentIntent: paymentIntent, refundApplicationFee: refundApplicationFee, refundPaymentConfig: refundPaymentConfig, reverseTransfer: reverseTransfer, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TerminalReader',) as TerminalReader;
    
    }
    return null;
  }

  /// Set reader display
  ///
  /// <p>Sets reader display to show cart details.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] reader (required):
  ///
  /// * [String] type (required):
  ///   Type
  ///
  /// * [Cart] cart:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postTerminalReadersReaderSetReaderDisplayWithHttpInfo(String reader, String type, { Cart? cart, List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/terminal/readers/{reader}/set_reader_display'
      .replaceAll('{reader}', reader);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (cart != null) {
      formParams[r'cart'] = parameterToString(cart);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (type != null) {
      formParams[r'type'] = parameterToString(type);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Set reader display
  ///
  /// <p>Sets reader display to show cart details.</p>
  ///
  /// Parameters:
  ///
  /// * [String] reader (required):
  ///
  /// * [String] type (required):
  ///   Type
  ///
  /// * [Cart] cart:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TerminalReader?> postTerminalReadersReaderSetReaderDisplay(String reader, String type, { Cart? cart, List<String>? expand, }) async {
    final response = await postTerminalReadersReaderSetReaderDisplayWithHttpInfo(reader, type,  cart: cart, expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TerminalReader',) as TerminalReader;
    
    }
    return null;
  }

  /// Create a test Confirmation Token
  ///
  /// <p>Creates a test mode Confirmation Token server side for your integration tests.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] paymentMethod:
  ///   ID of an existing PaymentMethod.
  ///
  /// * [PaymentMethodDataParams2] paymentMethodData:
  ///
  /// * [TestPaymentMethodOptionsParam] paymentMethodOptions:
  ///
  /// * [String] returnUrl:
  ///   Return URL used to confirm the Intent.
  ///
  /// * [String] setupFutureUsage:
  ///   Indicates that you intend to make future payments with this ConfirmationToken's payment method.  The presence of this property will [attach the payment method](https://stripe.com/docs/payments/save-during-payment) to the PaymentIntent's Customer, if present, after the PaymentIntent is confirmed and any required actions from the user are complete.
  ///
  /// * [RecipientShippingWithOptionalFieldsAddress2] shipping:
  Future<Response> postTestHelpersConfirmationTokensWithHttpInfo({ List<String>? expand, String? paymentMethod, PaymentMethodDataParams2? paymentMethodData, TestPaymentMethodOptionsParam? paymentMethodOptions, String? returnUrl, String? setupFutureUsage, RecipientShippingWithOptionalFieldsAddress2? shipping, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/confirmation_tokens';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (paymentMethod != null) {
      formParams[r'payment_method'] = parameterToString(paymentMethod);
    }
    if (paymentMethodData != null) {
      formParams[r'payment_method_data'] = parameterToString(paymentMethodData);
    }
    if (paymentMethodOptions != null) {
      formParams[r'payment_method_options'] = parameterToString(paymentMethodOptions);
    }
    if (returnUrl != null) {
      formParams[r'return_url'] = parameterToString(returnUrl);
    }
    if (setupFutureUsage != null) {
      formParams[r'setup_future_usage'] = parameterToString(setupFutureUsage);
    }
    if (shipping != null) {
      formParams[r'shipping'] = parameterToString(shipping);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a test Confirmation Token
  ///
  /// <p>Creates a test mode Confirmation Token server side for your integration tests.</p>
  ///
  /// Parameters:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] paymentMethod:
  ///   ID of an existing PaymentMethod.
  ///
  /// * [PaymentMethodDataParams2] paymentMethodData:
  ///
  /// * [TestPaymentMethodOptionsParam] paymentMethodOptions:
  ///
  /// * [String] returnUrl:
  ///   Return URL used to confirm the Intent.
  ///
  /// * [String] setupFutureUsage:
  ///   Indicates that you intend to make future payments with this ConfirmationToken's payment method.  The presence of this property will [attach the payment method](https://stripe.com/docs/payments/save-during-payment) to the PaymentIntent's Customer, if present, after the PaymentIntent is confirmed and any required actions from the user are complete.
  ///
  /// * [RecipientShippingWithOptionalFieldsAddress2] shipping:
  Future<ConfirmationToken?> postTestHelpersConfirmationTokens({ List<String>? expand, String? paymentMethod, PaymentMethodDataParams2? paymentMethodData, TestPaymentMethodOptionsParam? paymentMethodOptions, String? returnUrl, String? setupFutureUsage, RecipientShippingWithOptionalFieldsAddress2? shipping, }) async {
    final response = await postTestHelpersConfirmationTokensWithHttpInfo( expand: expand, paymentMethod: paymentMethod, paymentMethodData: paymentMethodData, paymentMethodOptions: paymentMethodOptions, returnUrl: returnUrl, setupFutureUsage: setupFutureUsage, shipping: shipping, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'ConfirmationToken',) as ConfirmationToken;
    
    }
    return null;
  }

  /// Fund a test mode cash balance
  ///
  /// <p>Create an incoming testmode bank transfer</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [int] amount (required):
  ///   Amount to be used for this test cash balance transaction. A positive integer representing how much to fund in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal) (e.g., 100 cents to fund $1.00 or 100 to fund ¥100, a zero-decimal currency).
  ///
  /// * [String] currency (required):
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] reference:
  ///   A description of the test funding. This simulates free-text references supplied by customers when making bank transfers to their cash balance. You can use this to test how Stripe's [reconciliation algorithm](https://stripe.com/docs/payments/customer-balance/reconciliation) applies to different user inputs.
  Future<Response> postTestHelpersCustomersCustomerFundCashBalanceWithHttpInfo(String customer, int amount, String currency, { List<String>? expand, String? reference, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/customers/{customer}/fund_cash_balance'
      .replaceAll('{customer}', customer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (reference != null) {
      formParams[r'reference'] = parameterToString(reference);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Fund a test mode cash balance
  ///
  /// <p>Create an incoming testmode bank transfer</p>
  ///
  /// Parameters:
  ///
  /// * [String] customer (required):
  ///
  /// * [int] amount (required):
  ///   Amount to be used for this test cash balance transaction. A positive integer representing how much to fund in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal) (e.g., 100 cents to fund $1.00 or 100 to fund ¥100, a zero-decimal currency).
  ///
  /// * [String] currency (required):
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] reference:
  ///   A description of the test funding. This simulates free-text references supplied by customers when making bank transfers to their cash balance. You can use this to test how Stripe's [reconciliation algorithm](https://stripe.com/docs/payments/customer-balance/reconciliation) applies to different user inputs.
  Future<CustomerCashBalanceTransaction?> postTestHelpersCustomersCustomerFundCashBalance(String customer, int amount, String currency, { List<String>? expand, String? reference, }) async {
    final response = await postTestHelpersCustomersCustomerFundCashBalanceWithHttpInfo(customer, amount, currency,  expand: expand, reference: reference, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'CustomerCashBalanceTransaction',) as CustomerCashBalanceTransaction;
    
    }
    return null;
  }

  /// Create a test-mode authorization
  ///
  /// <p>Create a test-mode authorization.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] card (required):
  ///   Card associated with this authorization.
  ///
  /// * [int] amount:
  ///   The total amount to attempt to authorize. This amount is in the provided currency, or defaults to the card's currency, and in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal).
  ///
  /// * [AmountDetailsSpecs] amountDetails:
  ///
  /// * [String] authorizationMethod:
  ///   How the card details were provided. Defaults to online.
  ///
  /// * [String] currency:
  ///   The currency of the authorization. If not provided, defaults to the currency of the card. Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [FleetTestmodeAuthorizationSpecs] fleet:
  ///
  /// * [FuelSpecs] fuel:
  ///
  /// * [bool] isAmountControllable:
  ///   If set `true`, you may provide [amount](https://stripe.com/docs/api/issuing/authorizations/approve#approve_issuing_authorization-amount) to control how much to hold for the authorization.
  ///
  /// * [int] merchantAmount:
  ///   The total amount to attempt to authorize. This amount is in the provided merchant currency, and in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal).
  ///
  /// * [String] merchantCurrency:
  ///   The currency of the authorization. If not provided, defaults to the currency of the card. Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [MerchantDataSpecs] merchantData:
  ///
  /// * [NetworkDataSpecs] networkData:
  ///
  /// * [VerificationDataSpecs] verificationData:
  ///
  /// * [String] wallet:
  ///   The digital wallet used for this transaction. One of `apple_pay`, `google_pay`, or `samsung_pay`. Will populate as `null` when no digital wallet was utilized.
  Future<Response> postTestHelpersIssuingAuthorizationsWithHttpInfo(String card, { int? amount, AmountDetailsSpecs? amountDetails, String? authorizationMethod, String? currency, List<String>? expand, FleetTestmodeAuthorizationSpecs? fleet, FuelSpecs? fuel, bool? isAmountControllable, int? merchantAmount, String? merchantCurrency, MerchantDataSpecs? merchantData, NetworkDataSpecs? networkData, VerificationDataSpecs? verificationData, String? wallet, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/issuing/authorizations';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (amountDetails != null) {
      formParams[r'amount_details'] = parameterToString(amountDetails);
    }
    if (authorizationMethod != null) {
      formParams[r'authorization_method'] = parameterToString(authorizationMethod);
    }
    if (card != null) {
      formParams[r'card'] = parameterToString(card);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (fleet != null) {
      formParams[r'fleet'] = parameterToString(fleet);
    }
    if (fuel != null) {
      formParams[r'fuel'] = parameterToString(fuel);
    }
    if (isAmountControllable != null) {
      formParams[r'is_amount_controllable'] = parameterToString(isAmountControllable);
    }
    if (merchantAmount != null) {
      formParams[r'merchant_amount'] = parameterToString(merchantAmount);
    }
    if (merchantCurrency != null) {
      formParams[r'merchant_currency'] = parameterToString(merchantCurrency);
    }
    if (merchantData != null) {
      formParams[r'merchant_data'] = parameterToString(merchantData);
    }
    if (networkData != null) {
      formParams[r'network_data'] = parameterToString(networkData);
    }
    if (verificationData != null) {
      formParams[r'verification_data'] = parameterToString(verificationData);
    }
    if (wallet != null) {
      formParams[r'wallet'] = parameterToString(wallet);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a test-mode authorization
  ///
  /// <p>Create a test-mode authorization.</p>
  ///
  /// Parameters:
  ///
  /// * [String] card (required):
  ///   Card associated with this authorization.
  ///
  /// * [int] amount:
  ///   The total amount to attempt to authorize. This amount is in the provided currency, or defaults to the card's currency, and in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal).
  ///
  /// * [AmountDetailsSpecs] amountDetails:
  ///
  /// * [String] authorizationMethod:
  ///   How the card details were provided. Defaults to online.
  ///
  /// * [String] currency:
  ///   The currency of the authorization. If not provided, defaults to the currency of the card. Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [FleetTestmodeAuthorizationSpecs] fleet:
  ///
  /// * [FuelSpecs] fuel:
  ///
  /// * [bool] isAmountControllable:
  ///   If set `true`, you may provide [amount](https://stripe.com/docs/api/issuing/authorizations/approve#approve_issuing_authorization-amount) to control how much to hold for the authorization.
  ///
  /// * [int] merchantAmount:
  ///   The total amount to attempt to authorize. This amount is in the provided merchant currency, and in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal).
  ///
  /// * [String] merchantCurrency:
  ///   The currency of the authorization. If not provided, defaults to the currency of the card. Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [MerchantDataSpecs] merchantData:
  ///
  /// * [NetworkDataSpecs] networkData:
  ///
  /// * [VerificationDataSpecs] verificationData:
  ///
  /// * [String] wallet:
  ///   The digital wallet used for this transaction. One of `apple_pay`, `google_pay`, or `samsung_pay`. Will populate as `null` when no digital wallet was utilized.
  Future<IssuingAuthorization?> postTestHelpersIssuingAuthorizations(String card, { int? amount, AmountDetailsSpecs? amountDetails, String? authorizationMethod, String? currency, List<String>? expand, FleetTestmodeAuthorizationSpecs? fleet, FuelSpecs? fuel, bool? isAmountControllable, int? merchantAmount, String? merchantCurrency, MerchantDataSpecs? merchantData, NetworkDataSpecs? networkData, VerificationDataSpecs? verificationData, String? wallet, }) async {
    final response = await postTestHelpersIssuingAuthorizationsWithHttpInfo(card,  amount: amount, amountDetails: amountDetails, authorizationMethod: authorizationMethod, currency: currency, expand: expand, fleet: fleet, fuel: fuel, isAmountControllable: isAmountControllable, merchantAmount: merchantAmount, merchantCurrency: merchantCurrency, merchantData: merchantData, networkData: networkData, verificationData: verificationData, wallet: wallet, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingAuthorization',) as IssuingAuthorization;
    
    }
    return null;
  }

  /// Capture a test-mode authorization
  ///
  /// <p>Capture a test-mode authorization.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] authorization (required):
  ///
  /// * [int] captureAmount:
  ///   The amount to capture from the authorization. If not provided, the full amount of the authorization will be captured. This amount is in the authorization currency and in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal).
  ///
  /// * [bool] closeAuthorization:
  ///   Whether to close the authorization after capture. Defaults to true. Set to false to enable multi-capture flows.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PurchaseDetailsSpecs] purchaseDetails:
  Future<Response> postTestHelpersIssuingAuthorizationsAuthorizationCaptureWithHttpInfo(String authorization, { int? captureAmount, bool? closeAuthorization, List<String>? expand, PurchaseDetailsSpecs? purchaseDetails, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/issuing/authorizations/{authorization}/capture'
      .replaceAll('{authorization}', authorization);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (captureAmount != null) {
      formParams[r'capture_amount'] = parameterToString(captureAmount);
    }
    if (closeAuthorization != null) {
      formParams[r'close_authorization'] = parameterToString(closeAuthorization);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (purchaseDetails != null) {
      formParams[r'purchase_details'] = parameterToString(purchaseDetails);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Capture a test-mode authorization
  ///
  /// <p>Capture a test-mode authorization.</p>
  ///
  /// Parameters:
  ///
  /// * [String] authorization (required):
  ///
  /// * [int] captureAmount:
  ///   The amount to capture from the authorization. If not provided, the full amount of the authorization will be captured. This amount is in the authorization currency and in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal).
  ///
  /// * [bool] closeAuthorization:
  ///   Whether to close the authorization after capture. Defaults to true. Set to false to enable multi-capture flows.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PurchaseDetailsSpecs] purchaseDetails:
  Future<IssuingAuthorization?> postTestHelpersIssuingAuthorizationsAuthorizationCapture(String authorization, { int? captureAmount, bool? closeAuthorization, List<String>? expand, PurchaseDetailsSpecs? purchaseDetails, }) async {
    final response = await postTestHelpersIssuingAuthorizationsAuthorizationCaptureWithHttpInfo(authorization,  captureAmount: captureAmount, closeAuthorization: closeAuthorization, expand: expand, purchaseDetails: purchaseDetails, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingAuthorization',) as IssuingAuthorization;
    
    }
    return null;
  }

  /// Expire a test-mode authorization
  ///
  /// <p>Expire a test-mode Authorization.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] authorization (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postTestHelpersIssuingAuthorizationsAuthorizationExpireWithHttpInfo(String authorization, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/issuing/authorizations/{authorization}/expire'
      .replaceAll('{authorization}', authorization);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Expire a test-mode authorization
  ///
  /// <p>Expire a test-mode Authorization.</p>
  ///
  /// Parameters:
  ///
  /// * [String] authorization (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<IssuingAuthorization?> postTestHelpersIssuingAuthorizationsAuthorizationExpire(String authorization, { List<String>? expand, }) async {
    final response = await postTestHelpersIssuingAuthorizationsAuthorizationExpireWithHttpInfo(authorization,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingAuthorization',) as IssuingAuthorization;
    
    }
    return null;
  }

  /// Finalize a test-mode authorization's amount
  ///
  /// <p>Finalize the amount on an Authorization prior to capture, when the initial authorization was for an estimated amount.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] authorization (required):
  ///
  /// * [int] finalAmount (required):
  ///   The final authorization amount that will be captured by the merchant. This amount is in the authorization currency and in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [FleetSpecs1] fleet:
  ///
  /// * [FuelSpecs] fuel:
  Future<Response> postTestHelpersIssuingAuthorizationsAuthorizationFinalizeAmountWithHttpInfo(String authorization, int finalAmount, { List<String>? expand, FleetSpecs1? fleet, FuelSpecs? fuel, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/issuing/authorizations/{authorization}/finalize_amount'
      .replaceAll('{authorization}', authorization);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (finalAmount != null) {
      formParams[r'final_amount'] = parameterToString(finalAmount);
    }
    if (fleet != null) {
      formParams[r'fleet'] = parameterToString(fleet);
    }
    if (fuel != null) {
      formParams[r'fuel'] = parameterToString(fuel);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Finalize a test-mode authorization's amount
  ///
  /// <p>Finalize the amount on an Authorization prior to capture, when the initial authorization was for an estimated amount.</p>
  ///
  /// Parameters:
  ///
  /// * [String] authorization (required):
  ///
  /// * [int] finalAmount (required):
  ///   The final authorization amount that will be captured by the merchant. This amount is in the authorization currency and in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [FleetSpecs1] fleet:
  ///
  /// * [FuelSpecs] fuel:
  Future<IssuingAuthorization?> postTestHelpersIssuingAuthorizationsAuthorizationFinalizeAmount(String authorization, int finalAmount, { List<String>? expand, FleetSpecs1? fleet, FuelSpecs? fuel, }) async {
    final response = await postTestHelpersIssuingAuthorizationsAuthorizationFinalizeAmountWithHttpInfo(authorization, finalAmount,  expand: expand, fleet: fleet, fuel: fuel, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingAuthorization',) as IssuingAuthorization;
    
    }
    return null;
  }

  /// Respond to fraud challenge
  ///
  /// <p>Respond to a fraud challenge on a testmode Issuing authorization, simulating either a confirmation of fraud or a correction of legitimacy.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] authorization (required):
  ///
  /// * [bool] confirmed (required):
  ///   Whether to simulate the user confirming that the transaction was legitimate (true) or telling Stripe that it was fraudulent (false).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postTestHelpersIssuingAuthorizationsAuthorizationFraudChallengesRespondWithHttpInfo(String authorization, bool confirmed, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/issuing/authorizations/{authorization}/fraud_challenges/respond'
      .replaceAll('{authorization}', authorization);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (confirmed != null) {
      formParams[r'confirmed'] = parameterToString(confirmed);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Respond to fraud challenge
  ///
  /// <p>Respond to a fraud challenge on a testmode Issuing authorization, simulating either a confirmation of fraud or a correction of legitimacy.</p>
  ///
  /// Parameters:
  ///
  /// * [String] authorization (required):
  ///
  /// * [bool] confirmed (required):
  ///   Whether to simulate the user confirming that the transaction was legitimate (true) or telling Stripe that it was fraudulent (false).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<IssuingAuthorization?> postTestHelpersIssuingAuthorizationsAuthorizationFraudChallengesRespond(String authorization, bool confirmed, { List<String>? expand, }) async {
    final response = await postTestHelpersIssuingAuthorizationsAuthorizationFraudChallengesRespondWithHttpInfo(authorization, confirmed,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingAuthorization',) as IssuingAuthorization;
    
    }
    return null;
  }

  /// Increment a test-mode authorization
  ///
  /// <p>Increment a test-mode Authorization.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] authorization (required):
  ///
  /// * [int] incrementAmount (required):
  ///   The amount to increment the authorization by. This amount is in the authorization currency and in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [bool] isAmountControllable:
  ///   If set `true`, you may provide [amount](https://stripe.com/docs/api/issuing/authorizations/approve#approve_issuing_authorization-amount) to control how much to hold for the authorization.
  Future<Response> postTestHelpersIssuingAuthorizationsAuthorizationIncrementWithHttpInfo(String authorization, int incrementAmount, { List<String>? expand, bool? isAmountControllable, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/issuing/authorizations/{authorization}/increment'
      .replaceAll('{authorization}', authorization);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (incrementAmount != null) {
      formParams[r'increment_amount'] = parameterToString(incrementAmount);
    }
    if (isAmountControllable != null) {
      formParams[r'is_amount_controllable'] = parameterToString(isAmountControllable);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Increment a test-mode authorization
  ///
  /// <p>Increment a test-mode Authorization.</p>
  ///
  /// Parameters:
  ///
  /// * [String] authorization (required):
  ///
  /// * [int] incrementAmount (required):
  ///   The amount to increment the authorization by. This amount is in the authorization currency and in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [bool] isAmountControllable:
  ///   If set `true`, you may provide [amount](https://stripe.com/docs/api/issuing/authorizations/approve#approve_issuing_authorization-amount) to control how much to hold for the authorization.
  Future<IssuingAuthorization?> postTestHelpersIssuingAuthorizationsAuthorizationIncrement(String authorization, int incrementAmount, { List<String>? expand, bool? isAmountControllable, }) async {
    final response = await postTestHelpersIssuingAuthorizationsAuthorizationIncrementWithHttpInfo(authorization, incrementAmount,  expand: expand, isAmountControllable: isAmountControllable, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingAuthorization',) as IssuingAuthorization;
    
    }
    return null;
  }

  /// Reverse a test-mode authorization
  ///
  /// <p>Reverse a test-mode Authorization.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] authorization (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] reverseAmount:
  ///   The amount to reverse from the authorization. If not provided, the full amount of the authorization will be reversed. This amount is in the authorization currency and in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal).
  Future<Response> postTestHelpersIssuingAuthorizationsAuthorizationReverseWithHttpInfo(String authorization, { List<String>? expand, int? reverseAmount, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/issuing/authorizations/{authorization}/reverse'
      .replaceAll('{authorization}', authorization);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (reverseAmount != null) {
      formParams[r'reverse_amount'] = parameterToString(reverseAmount);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Reverse a test-mode authorization
  ///
  /// <p>Reverse a test-mode Authorization.</p>
  ///
  /// Parameters:
  ///
  /// * [String] authorization (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] reverseAmount:
  ///   The amount to reverse from the authorization. If not provided, the full amount of the authorization will be reversed. This amount is in the authorization currency and in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal).
  Future<IssuingAuthorization?> postTestHelpersIssuingAuthorizationsAuthorizationReverse(String authorization, { List<String>? expand, int? reverseAmount, }) async {
    final response = await postTestHelpersIssuingAuthorizationsAuthorizationReverseWithHttpInfo(authorization,  expand: expand, reverseAmount: reverseAmount, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingAuthorization',) as IssuingAuthorization;
    
    }
    return null;
  }

  /// Deliver a testmode card
  ///
  /// <p>Updates the shipping status of the specified Issuing <code>Card</code> object to <code>delivered</code>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] card (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postTestHelpersIssuingCardsCardShippingDeliverWithHttpInfo(String card, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/issuing/cards/{card}/shipping/deliver'
      .replaceAll('{card}', card);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Deliver a testmode card
  ///
  /// <p>Updates the shipping status of the specified Issuing <code>Card</code> object to <code>delivered</code>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] card (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<IssuingCard?> postTestHelpersIssuingCardsCardShippingDeliver(String card, { List<String>? expand, }) async {
    final response = await postTestHelpersIssuingCardsCardShippingDeliverWithHttpInfo(card,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingCard',) as IssuingCard;
    
    }
    return null;
  }

  /// Fail a testmode card
  ///
  /// <p>Updates the shipping status of the specified Issuing <code>Card</code> object to <code>failure</code>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] card (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postTestHelpersIssuingCardsCardShippingFailWithHttpInfo(String card, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/issuing/cards/{card}/shipping/fail'
      .replaceAll('{card}', card);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Fail a testmode card
  ///
  /// <p>Updates the shipping status of the specified Issuing <code>Card</code> object to <code>failure</code>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] card (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<IssuingCard?> postTestHelpersIssuingCardsCardShippingFail(String card, { List<String>? expand, }) async {
    final response = await postTestHelpersIssuingCardsCardShippingFailWithHttpInfo(card,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingCard',) as IssuingCard;
    
    }
    return null;
  }

  /// Return a testmode card
  ///
  /// <p>Updates the shipping status of the specified Issuing <code>Card</code> object to <code>returned</code>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] card (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postTestHelpersIssuingCardsCardShippingReturnWithHttpInfo(String card, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/issuing/cards/{card}/shipping/return'
      .replaceAll('{card}', card);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Return a testmode card
  ///
  /// <p>Updates the shipping status of the specified Issuing <code>Card</code> object to <code>returned</code>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] card (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<IssuingCard?> postTestHelpersIssuingCardsCardShippingReturn(String card, { List<String>? expand, }) async {
    final response = await postTestHelpersIssuingCardsCardShippingReturnWithHttpInfo(card,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingCard',) as IssuingCard;
    
    }
    return null;
  }

  /// Ship a testmode card
  ///
  /// <p>Updates the shipping status of the specified Issuing <code>Card</code> object to <code>shipped</code>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] card (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postTestHelpersIssuingCardsCardShippingShipWithHttpInfo(String card, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/issuing/cards/{card}/shipping/ship'
      .replaceAll('{card}', card);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Ship a testmode card
  ///
  /// <p>Updates the shipping status of the specified Issuing <code>Card</code> object to <code>shipped</code>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] card (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<IssuingCard?> postTestHelpersIssuingCardsCardShippingShip(String card, { List<String>? expand, }) async {
    final response = await postTestHelpersIssuingCardsCardShippingShipWithHttpInfo(card,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingCard',) as IssuingCard;
    
    }
    return null;
  }

  /// Submit a testmode card
  ///
  /// <p>Updates the shipping status of the specified Issuing <code>Card</code> object to <code>submitted</code>. This method requires Stripe Version ‘2024-09-30.acacia’ or later.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] card (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postTestHelpersIssuingCardsCardShippingSubmitWithHttpInfo(String card, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/issuing/cards/{card}/shipping/submit'
      .replaceAll('{card}', card);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Submit a testmode card
  ///
  /// <p>Updates the shipping status of the specified Issuing <code>Card</code> object to <code>submitted</code>. This method requires Stripe Version ‘2024-09-30.acacia’ or later.</p>
  ///
  /// Parameters:
  ///
  /// * [String] card (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<IssuingCard?> postTestHelpersIssuingCardsCardShippingSubmit(String card, { List<String>? expand, }) async {
    final response = await postTestHelpersIssuingCardsCardShippingSubmitWithHttpInfo(card,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingCard',) as IssuingCard;
    
    }
    return null;
  }

  /// Activate a testmode personalization design
  ///
  /// <p>Updates the <code>status</code> of the specified testmode personalization design object to <code>active</code>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] personalizationDesign (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postTestHelpersIssuingPersonalizationDesignsPersonalizationDesignActivateWithHttpInfo(String personalizationDesign, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/issuing/personalization_designs/{personalization_design}/activate'
      .replaceAll('{personalization_design}', personalizationDesign);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Activate a testmode personalization design
  ///
  /// <p>Updates the <code>status</code> of the specified testmode personalization design object to <code>active</code>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] personalizationDesign (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<IssuingPersonalizationDesign?> postTestHelpersIssuingPersonalizationDesignsPersonalizationDesignActivate(String personalizationDesign, { List<String>? expand, }) async {
    final response = await postTestHelpersIssuingPersonalizationDesignsPersonalizationDesignActivateWithHttpInfo(personalizationDesign,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingPersonalizationDesign',) as IssuingPersonalizationDesign;
    
    }
    return null;
  }

  /// Deactivate a testmode personalization design
  ///
  /// <p>Updates the <code>status</code> of the specified testmode personalization design object to <code>inactive</code>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] personalizationDesign (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postTestHelpersIssuingPersonalizationDesignsPersonalizationDesignDeactivateWithHttpInfo(String personalizationDesign, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/issuing/personalization_designs/{personalization_design}/deactivate'
      .replaceAll('{personalization_design}', personalizationDesign);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Deactivate a testmode personalization design
  ///
  /// <p>Updates the <code>status</code> of the specified testmode personalization design object to <code>inactive</code>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] personalizationDesign (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<IssuingPersonalizationDesign?> postTestHelpersIssuingPersonalizationDesignsPersonalizationDesignDeactivate(String personalizationDesign, { List<String>? expand, }) async {
    final response = await postTestHelpersIssuingPersonalizationDesignsPersonalizationDesignDeactivateWithHttpInfo(personalizationDesign,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingPersonalizationDesign',) as IssuingPersonalizationDesign;
    
    }
    return null;
  }

  /// Reject a testmode personalization design
  ///
  /// <p>Updates the <code>status</code> of the specified testmode personalization design object to <code>rejected</code>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] personalizationDesign (required):
  ///
  /// * [RejectionReasonsParam] rejectionReasons (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postTestHelpersIssuingPersonalizationDesignsPersonalizationDesignRejectWithHttpInfo(String personalizationDesign, RejectionReasonsParam rejectionReasons, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/issuing/personalization_designs/{personalization_design}/reject'
      .replaceAll('{personalization_design}', personalizationDesign);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (rejectionReasons != null) {
      formParams[r'rejection_reasons'] = parameterToString(rejectionReasons);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Reject a testmode personalization design
  ///
  /// <p>Updates the <code>status</code> of the specified testmode personalization design object to <code>rejected</code>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] personalizationDesign (required):
  ///
  /// * [RejectionReasonsParam] rejectionReasons (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<IssuingPersonalizationDesign?> postTestHelpersIssuingPersonalizationDesignsPersonalizationDesignReject(String personalizationDesign, RejectionReasonsParam rejectionReasons, { List<String>? expand, }) async {
    final response = await postTestHelpersIssuingPersonalizationDesignsPersonalizationDesignRejectWithHttpInfo(personalizationDesign, rejectionReasons,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingPersonalizationDesign',) as IssuingPersonalizationDesign;
    
    }
    return null;
  }

  /// Create a test-mode settlement
  ///
  /// <p>Allows the user to create an Issuing settlement.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] bin (required):
  ///   The Bank Identification Number reflecting this settlement record.
  ///
  /// * [int] clearingDate (required):
  ///   The date that the transactions are cleared and posted to user's accounts.
  ///
  /// * [String] currency (required):
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [int] netTotalAmount (required):
  ///   The total net amount required to settle with the network.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] interchangeFeesAmount:
  ///   The total interchange received as reimbursement for the transactions.
  ///
  /// * [String] network:
  ///   The card network for this settlement. One of [\\\"visa\\\", \\\"maestro\\\"]
  ///
  /// * [String] networkSettlementIdentifier:
  ///   The Settlement Identification Number assigned by the network.
  ///
  /// * [int] transactionAmount:
  ///   The total transaction amount reflected in this settlement.
  ///
  /// * [int] transactionCount:
  ///   The total number of transactions reflected in this settlement.
  Future<Response> postTestHelpersIssuingSettlementsWithHttpInfo(String bin, int clearingDate, String currency, int netTotalAmount, { List<String>? expand, int? interchangeFeesAmount, String? network, String? networkSettlementIdentifier, int? transactionAmount, int? transactionCount, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/issuing/settlements';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (bin != null) {
      formParams[r'bin'] = parameterToString(bin);
    }
    if (clearingDate != null) {
      formParams[r'clearing_date'] = parameterToString(clearingDate);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (interchangeFeesAmount != null) {
      formParams[r'interchange_fees_amount'] = parameterToString(interchangeFeesAmount);
    }
    if (netTotalAmount != null) {
      formParams[r'net_total_amount'] = parameterToString(netTotalAmount);
    }
    if (network != null) {
      formParams[r'network'] = parameterToString(network);
    }
    if (networkSettlementIdentifier != null) {
      formParams[r'network_settlement_identifier'] = parameterToString(networkSettlementIdentifier);
    }
    if (transactionAmount != null) {
      formParams[r'transaction_amount'] = parameterToString(transactionAmount);
    }
    if (transactionCount != null) {
      formParams[r'transaction_count'] = parameterToString(transactionCount);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a test-mode settlement
  ///
  /// <p>Allows the user to create an Issuing settlement.</p>
  ///
  /// Parameters:
  ///
  /// * [String] bin (required):
  ///   The Bank Identification Number reflecting this settlement record.
  ///
  /// * [int] clearingDate (required):
  ///   The date that the transactions are cleared and posted to user's accounts.
  ///
  /// * [String] currency (required):
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [int] netTotalAmount (required):
  ///   The total net amount required to settle with the network.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] interchangeFeesAmount:
  ///   The total interchange received as reimbursement for the transactions.
  ///
  /// * [String] network:
  ///   The card network for this settlement. One of [\\\"visa\\\", \\\"maestro\\\"]
  ///
  /// * [String] networkSettlementIdentifier:
  ///   The Settlement Identification Number assigned by the network.
  ///
  /// * [int] transactionAmount:
  ///   The total transaction amount reflected in this settlement.
  ///
  /// * [int] transactionCount:
  ///   The total number of transactions reflected in this settlement.
  Future<IssuingSettlement?> postTestHelpersIssuingSettlements(String bin, int clearingDate, String currency, int netTotalAmount, { List<String>? expand, int? interchangeFeesAmount, String? network, String? networkSettlementIdentifier, int? transactionAmount, int? transactionCount, }) async {
    final response = await postTestHelpersIssuingSettlementsWithHttpInfo(bin, clearingDate, currency, netTotalAmount,  expand: expand, interchangeFeesAmount: interchangeFeesAmount, network: network, networkSettlementIdentifier: networkSettlementIdentifier, transactionAmount: transactionAmount, transactionCount: transactionCount, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingSettlement',) as IssuingSettlement;
    
    }
    return null;
  }

  /// Complete a test-mode settlement
  ///
  /// <p>Allows the user to mark an Issuing settlement as complete.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] settlement (required):
  ///   The settlement token to mark as complete.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postTestHelpersIssuingSettlementsSettlementCompleteWithHttpInfo(String settlement, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/issuing/settlements/{settlement}/complete'
      .replaceAll('{settlement}', settlement);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Complete a test-mode settlement
  ///
  /// <p>Allows the user to mark an Issuing settlement as complete.</p>
  ///
  /// Parameters:
  ///
  /// * [String] settlement (required):
  ///   The settlement token to mark as complete.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<IssuingSettlement?> postTestHelpersIssuingSettlementsSettlementComplete(String settlement, { List<String>? expand, }) async {
    final response = await postTestHelpersIssuingSettlementsSettlementCompleteWithHttpInfo(settlement,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingSettlement',) as IssuingSettlement;
    
    }
    return null;
  }

  /// Create a test-mode force capture
  ///
  /// <p>Allows the user to capture an arbitrary amount, also known as a forced capture.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [int] amount (required):
  ///   The total amount to attempt to capture. This amount is in the provided currency, or defaults to the cards currency, and in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal).
  ///
  /// * [String] card (required):
  ///   Card associated with this transaction.
  ///
  /// * [String] currency:
  ///   The currency of the capture. If not provided, defaults to the currency of the card. Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [MerchantDataSpecs] merchantData:
  ///
  /// * [PurchaseDetailsSpecs] purchaseDetails:
  Future<Response> postTestHelpersIssuingTransactionsCreateForceCaptureWithHttpInfo(int amount, String card, { String? currency, List<String>? expand, MerchantDataSpecs? merchantData, PurchaseDetailsSpecs? purchaseDetails, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/issuing/transactions/create_force_capture';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (card != null) {
      formParams[r'card'] = parameterToString(card);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (merchantData != null) {
      formParams[r'merchant_data'] = parameterToString(merchantData);
    }
    if (purchaseDetails != null) {
      formParams[r'purchase_details'] = parameterToString(purchaseDetails);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a test-mode force capture
  ///
  /// <p>Allows the user to capture an arbitrary amount, also known as a forced capture.</p>
  ///
  /// Parameters:
  ///
  /// * [int] amount (required):
  ///   The total amount to attempt to capture. This amount is in the provided currency, or defaults to the cards currency, and in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal).
  ///
  /// * [String] card (required):
  ///   Card associated with this transaction.
  ///
  /// * [String] currency:
  ///   The currency of the capture. If not provided, defaults to the currency of the card. Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [MerchantDataSpecs] merchantData:
  ///
  /// * [PurchaseDetailsSpecs] purchaseDetails:
  Future<IssuingTransaction?> postTestHelpersIssuingTransactionsCreateForceCapture(int amount, String card, { String? currency, List<String>? expand, MerchantDataSpecs? merchantData, PurchaseDetailsSpecs? purchaseDetails, }) async {
    final response = await postTestHelpersIssuingTransactionsCreateForceCaptureWithHttpInfo(amount, card,  currency: currency, expand: expand, merchantData: merchantData, purchaseDetails: purchaseDetails, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingTransaction',) as IssuingTransaction;
    
    }
    return null;
  }

  /// Create a test-mode unlinked refund
  ///
  /// <p>Allows the user to refund an arbitrary amount, also known as a unlinked refund.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [int] amount (required):
  ///   The total amount to attempt to refund. This amount is in the provided currency, or defaults to the cards currency, and in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal).
  ///
  /// * [String] card (required):
  ///   Card associated with this unlinked refund transaction.
  ///
  /// * [String] currency:
  ///   The currency of the unlinked refund. If not provided, defaults to the currency of the card. Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [MerchantDataSpecs] merchantData:
  ///
  /// * [PurchaseDetailsSpecs] purchaseDetails:
  Future<Response> postTestHelpersIssuingTransactionsCreateUnlinkedRefundWithHttpInfo(int amount, String card, { String? currency, List<String>? expand, MerchantDataSpecs? merchantData, PurchaseDetailsSpecs? purchaseDetails, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/issuing/transactions/create_unlinked_refund';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (card != null) {
      formParams[r'card'] = parameterToString(card);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (merchantData != null) {
      formParams[r'merchant_data'] = parameterToString(merchantData);
    }
    if (purchaseDetails != null) {
      formParams[r'purchase_details'] = parameterToString(purchaseDetails);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a test-mode unlinked refund
  ///
  /// <p>Allows the user to refund an arbitrary amount, also known as a unlinked refund.</p>
  ///
  /// Parameters:
  ///
  /// * [int] amount (required):
  ///   The total amount to attempt to refund. This amount is in the provided currency, or defaults to the cards currency, and in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal).
  ///
  /// * [String] card (required):
  ///   Card associated with this unlinked refund transaction.
  ///
  /// * [String] currency:
  ///   The currency of the unlinked refund. If not provided, defaults to the currency of the card. Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [MerchantDataSpecs] merchantData:
  ///
  /// * [PurchaseDetailsSpecs] purchaseDetails:
  Future<IssuingTransaction?> postTestHelpersIssuingTransactionsCreateUnlinkedRefund(int amount, String card, { String? currency, List<String>? expand, MerchantDataSpecs? merchantData, PurchaseDetailsSpecs? purchaseDetails, }) async {
    final response = await postTestHelpersIssuingTransactionsCreateUnlinkedRefundWithHttpInfo(amount, card,  currency: currency, expand: expand, merchantData: merchantData, purchaseDetails: purchaseDetails, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingTransaction',) as IssuingTransaction;
    
    }
    return null;
  }

  /// Refund a test-mode transaction
  ///
  /// <p>Refund a test-mode Transaction.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] transaction (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] refundAmount:
  ///   The total amount to attempt to refund. This amount is in the provided currency, or defaults to the cards currency, and in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal).
  Future<Response> postTestHelpersIssuingTransactionsTransactionRefundWithHttpInfo(String transaction, { List<String>? expand, int? refundAmount, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/issuing/transactions/{transaction}/refund'
      .replaceAll('{transaction}', transaction);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (refundAmount != null) {
      formParams[r'refund_amount'] = parameterToString(refundAmount);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Refund a test-mode transaction
  ///
  /// <p>Refund a test-mode Transaction.</p>
  ///
  /// Parameters:
  ///
  /// * [String] transaction (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [int] refundAmount:
  ///   The total amount to attempt to refund. This amount is in the provided currency, or defaults to the cards currency, and in the [smallest currency unit](https://stripe.com/docs/currencies#zero-decimal).
  Future<IssuingTransaction?> postTestHelpersIssuingTransactionsTransactionRefund(String transaction, { List<String>? expand, int? refundAmount, }) async {
    final response = await postTestHelpersIssuingTransactionsTransactionRefundWithHttpInfo(transaction,  expand: expand, refundAmount: refundAmount, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'IssuingTransaction',) as IssuingTransaction;
    
    }
    return null;
  }

  /// Expire a pending refund.
  ///
  /// <p>Expire a refund with a status of <code>requires_action</code>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] refund (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postTestHelpersRefundsRefundExpireWithHttpInfo(String refund, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/refunds/{refund}/expire'
      .replaceAll('{refund}', refund);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Expire a pending refund.
  ///
  /// <p>Expire a refund with a status of <code>requires_action</code>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] refund (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Refund?> postTestHelpersRefundsRefundExpire(String refund, { List<String>? expand, }) async {
    final response = await postTestHelpersRefundsRefundExpireWithHttpInfo(refund,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Refund',) as Refund;
    
    }
    return null;
  }

  /// Simulate presenting a payment method
  ///
  /// <p>Presents a payment method on a simulated reader. Can be used to simulate accepting a payment, saving a card or refunding a transaction.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] reader (required):
  ///
  /// * [int] amountTip:
  ///   Simulated on-reader tip amount.
  ///
  /// * [CardPresent] cardPresent:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [InteracPresent] interacPresent:
  ///
  /// * [String] type:
  ///   Simulated payment type.
  Future<Response> postTestHelpersTerminalReadersReaderPresentPaymentMethodWithHttpInfo(String reader, { int? amountTip, CardPresent? cardPresent, List<String>? expand, InteracPresent? interacPresent, String? type, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/terminal/readers/{reader}/present_payment_method'
      .replaceAll('{reader}', reader);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amountTip != null) {
      formParams[r'amount_tip'] = parameterToString(amountTip);
    }
    if (cardPresent != null) {
      formParams[r'card_present'] = parameterToString(cardPresent);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (interacPresent != null) {
      formParams[r'interac_present'] = parameterToString(interacPresent);
    }
    if (type != null) {
      formParams[r'type'] = parameterToString(type);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Simulate presenting a payment method
  ///
  /// <p>Presents a payment method on a simulated reader. Can be used to simulate accepting a payment, saving a card or refunding a transaction.</p>
  ///
  /// Parameters:
  ///
  /// * [String] reader (required):
  ///
  /// * [int] amountTip:
  ///   Simulated on-reader tip amount.
  ///
  /// * [CardPresent] cardPresent:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [InteracPresent] interacPresent:
  ///
  /// * [String] type:
  ///   Simulated payment type.
  Future<TerminalReader?> postTestHelpersTerminalReadersReaderPresentPaymentMethod(String reader, { int? amountTip, CardPresent? cardPresent, List<String>? expand, InteracPresent? interacPresent, String? type, }) async {
    final response = await postTestHelpersTerminalReadersReaderPresentPaymentMethodWithHttpInfo(reader,  amountTip: amountTip, cardPresent: cardPresent, expand: expand, interacPresent: interacPresent, type: type, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TerminalReader',) as TerminalReader;
    
    }
    return null;
  }

  /// Simulate a successful input collection
  ///
  /// <p>Use this endpoint to trigger a successful input collection on a simulated reader.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] reader (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] skipNonRequiredInputs:
  ///   This parameter defines the skip behavior for input collection.
  Future<Response> postTestHelpersTerminalReadersReaderSucceedInputCollectionWithHttpInfo(String reader, { List<String>? expand, String? skipNonRequiredInputs, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/terminal/readers/{reader}/succeed_input_collection'
      .replaceAll('{reader}', reader);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (skipNonRequiredInputs != null) {
      formParams[r'skip_non_required_inputs'] = parameterToString(skipNonRequiredInputs);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Simulate a successful input collection
  ///
  /// <p>Use this endpoint to trigger a successful input collection on a simulated reader.</p>
  ///
  /// Parameters:
  ///
  /// * [String] reader (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] skipNonRequiredInputs:
  ///   This parameter defines the skip behavior for input collection.
  Future<TerminalReader?> postTestHelpersTerminalReadersReaderSucceedInputCollection(String reader, { List<String>? expand, String? skipNonRequiredInputs, }) async {
    final response = await postTestHelpersTerminalReadersReaderSucceedInputCollectionWithHttpInfo(reader,  expand: expand, skipNonRequiredInputs: skipNonRequiredInputs, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TerminalReader',) as TerminalReader;
    
    }
    return null;
  }

  /// Simulate an input collection timeout
  ///
  /// <p>Use this endpoint to complete an input collection with a timeout error on a simulated reader.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] reader (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postTestHelpersTerminalReadersReaderTimeoutInputCollectionWithHttpInfo(String reader, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/terminal/readers/{reader}/timeout_input_collection'
      .replaceAll('{reader}', reader);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Simulate an input collection timeout
  ///
  /// <p>Use this endpoint to complete an input collection with a timeout error on a simulated reader.</p>
  ///
  /// Parameters:
  ///
  /// * [String] reader (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TerminalReader?> postTestHelpersTerminalReadersReaderTimeoutInputCollection(String reader, { List<String>? expand, }) async {
    final response = await postTestHelpersTerminalReadersReaderTimeoutInputCollectionWithHttpInfo(reader,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TerminalReader',) as TerminalReader;
    
    }
    return null;
  }

  /// Create a test clock
  ///
  /// <p>Creates a new test clock that can be attached to new customers and quotes.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [int] frozenTime (required):
  ///   The initial frozen time for this test clock.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] name:
  ///   The name for this test clock.
  Future<Response> postTestHelpersTestClocksWithHttpInfo(int frozenTime, { List<String>? expand, String? name, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/test_clocks';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (frozenTime != null) {
      formParams[r'frozen_time'] = parameterToString(frozenTime);
    }
    if (name != null) {
      formParams[r'name'] = parameterToString(name);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a test clock
  ///
  /// <p>Creates a new test clock that can be attached to new customers and quotes.</p>
  ///
  /// Parameters:
  ///
  /// * [int] frozenTime (required):
  ///   The initial frozen time for this test clock.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [String] name:
  ///   The name for this test clock.
  Future<TestHelpersTestClock?> postTestHelpersTestClocks(int frozenTime, { List<String>? expand, String? name, }) async {
    final response = await postTestHelpersTestClocksWithHttpInfo(frozenTime,  expand: expand, name: name, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TestHelpersTestClock',) as TestHelpersTestClock;
    
    }
    return null;
  }

  /// Advance a test clock
  ///
  /// <p>Starts advancing a test clock to a specified time in the future. Advancement is done when status changes to <code>Ready</code>.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] testClock (required):
  ///
  /// * [int] frozenTime (required):
  ///   The time to advance the test clock. Must be after the test clock's current frozen time. Cannot be more than two intervals in the future from the shortest subscription in this test clock. If there are no subscriptions in this test clock, it cannot be more than two years in the future.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postTestHelpersTestClocksTestClockAdvanceWithHttpInfo(String testClock, int frozenTime, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/test_clocks/{test_clock}/advance'
      .replaceAll('{test_clock}', testClock);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (frozenTime != null) {
      formParams[r'frozen_time'] = parameterToString(frozenTime);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Advance a test clock
  ///
  /// <p>Starts advancing a test clock to a specified time in the future. Advancement is done when status changes to <code>Ready</code>.</p>
  ///
  /// Parameters:
  ///
  /// * [String] testClock (required):
  ///
  /// * [int] frozenTime (required):
  ///   The time to advance the test clock. Must be after the test clock's current frozen time. Cannot be more than two intervals in the future from the shortest subscription in this test clock. If there are no subscriptions in this test clock, it cannot be more than two years in the future.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TestHelpersTestClock?> postTestHelpersTestClocksTestClockAdvance(String testClock, int frozenTime, { List<String>? expand, }) async {
    final response = await postTestHelpersTestClocksTestClockAdvanceWithHttpInfo(testClock, frozenTime,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TestHelpersTestClock',) as TestHelpersTestClock;
    
    }
    return null;
  }

  /// Test mode: Fail an InboundTransfer
  ///
  /// <p>Transitions a test mode created InboundTransfer to the <code>failed</code> status. The InboundTransfer must already be in the <code>processing</code> state.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [FailureDetailsParam] failureDetails:
  Future<Response> postTestHelpersTreasuryInboundTransfersIdFailWithHttpInfo(String id, { List<String>? expand, FailureDetailsParam? failureDetails, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/treasury/inbound_transfers/{id}/fail'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (failureDetails != null) {
      formParams[r'failure_details'] = parameterToString(failureDetails);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Test mode: Fail an InboundTransfer
  ///
  /// <p>Transitions a test mode created InboundTransfer to the <code>failed</code> status. The InboundTransfer must already be in the <code>processing</code> state.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [FailureDetailsParam] failureDetails:
  Future<TreasuryInboundTransfer?> postTestHelpersTreasuryInboundTransfersIdFail(String id, { List<String>? expand, FailureDetailsParam? failureDetails, }) async {
    final response = await postTestHelpersTreasuryInboundTransfersIdFailWithHttpInfo(id,  expand: expand, failureDetails: failureDetails, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryInboundTransfer',) as TreasuryInboundTransfer;
    
    }
    return null;
  }

  /// Test mode: Return an InboundTransfer
  ///
  /// <p>Marks the test mode InboundTransfer object as returned and links the InboundTransfer to a ReceivedDebit. The InboundTransfer must already be in the <code>succeeded</code> state.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postTestHelpersTreasuryInboundTransfersIdReturnWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/treasury/inbound_transfers/{id}/return'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Test mode: Return an InboundTransfer
  ///
  /// <p>Marks the test mode InboundTransfer object as returned and links the InboundTransfer to a ReceivedDebit. The InboundTransfer must already be in the <code>succeeded</code> state.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TreasuryInboundTransfer?> postTestHelpersTreasuryInboundTransfersIdReturn(String id, { List<String>? expand, }) async {
    final response = await postTestHelpersTreasuryInboundTransfersIdReturnWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryInboundTransfer',) as TreasuryInboundTransfer;
    
    }
    return null;
  }

  /// Test mode: Succeed an InboundTransfer
  ///
  /// <p>Transitions a test mode created InboundTransfer to the <code>succeeded</code> status. The InboundTransfer must already be in the <code>processing</code> state.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postTestHelpersTreasuryInboundTransfersIdSucceedWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/treasury/inbound_transfers/{id}/succeed'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Test mode: Succeed an InboundTransfer
  ///
  /// <p>Transitions a test mode created InboundTransfer to the <code>succeeded</code> status. The InboundTransfer must already be in the <code>processing</code> state.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TreasuryInboundTransfer?> postTestHelpersTreasuryInboundTransfersIdSucceed(String id, { List<String>? expand, }) async {
    final response = await postTestHelpersTreasuryInboundTransfersIdSucceedWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryInboundTransfer',) as TreasuryInboundTransfer;
    
    }
    return null;
  }

  /// Test mode: Update an OutboundPayment
  ///
  /// <p>Updates a test mode created OutboundPayment with tracking details. The OutboundPayment must not be cancelable, and cannot be in the <code>canceled</code> or <code>failed</code> states.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [TrackingDetailsParams] trackingDetails (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postTestHelpersTreasuryOutboundPaymentsIdWithHttpInfo(String id, TrackingDetailsParams trackingDetails, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/treasury/outbound_payments/{id}'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (trackingDetails != null) {
      formParams[r'tracking_details'] = parameterToString(trackingDetails);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Test mode: Update an OutboundPayment
  ///
  /// <p>Updates a test mode created OutboundPayment with tracking details. The OutboundPayment must not be cancelable, and cannot be in the <code>canceled</code> or <code>failed</code> states.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [TrackingDetailsParams] trackingDetails (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TreasuryOutboundPayment?> postTestHelpersTreasuryOutboundPaymentsId(String id, TrackingDetailsParams trackingDetails, { List<String>? expand, }) async {
    final response = await postTestHelpersTreasuryOutboundPaymentsIdWithHttpInfo(id, trackingDetails,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryOutboundPayment',) as TreasuryOutboundPayment;
    
    }
    return null;
  }

  /// Test mode: Fail an OutboundPayment
  ///
  /// <p>Transitions a test mode created OutboundPayment to the <code>failed</code> status. The OutboundPayment must already be in the <code>processing</code> state.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postTestHelpersTreasuryOutboundPaymentsIdFailWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/treasury/outbound_payments/{id}/fail'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Test mode: Fail an OutboundPayment
  ///
  /// <p>Transitions a test mode created OutboundPayment to the <code>failed</code> status. The OutboundPayment must already be in the <code>processing</code> state.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TreasuryOutboundPayment?> postTestHelpersTreasuryOutboundPaymentsIdFail(String id, { List<String>? expand, }) async {
    final response = await postTestHelpersTreasuryOutboundPaymentsIdFailWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryOutboundPayment',) as TreasuryOutboundPayment;
    
    }
    return null;
  }

  /// Test mode: Post an OutboundPayment
  ///
  /// <p>Transitions a test mode created OutboundPayment to the <code>posted</code> status. The OutboundPayment must already be in the <code>processing</code> state.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postTestHelpersTreasuryOutboundPaymentsIdPostWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/treasury/outbound_payments/{id}/post'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Test mode: Post an OutboundPayment
  ///
  /// <p>Transitions a test mode created OutboundPayment to the <code>posted</code> status. The OutboundPayment must already be in the <code>processing</code> state.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TreasuryOutboundPayment?> postTestHelpersTreasuryOutboundPaymentsIdPost(String id, { List<String>? expand, }) async {
    final response = await postTestHelpersTreasuryOutboundPaymentsIdPostWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryOutboundPayment',) as TreasuryOutboundPayment;
    
    }
    return null;
  }

  /// Test mode: Return an OutboundPayment
  ///
  /// <p>Transitions a test mode created OutboundPayment to the <code>returned</code> status. The OutboundPayment must already be in the <code>processing</code> state.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [ReturnedDetailsParams] returnedDetails:
  Future<Response> postTestHelpersTreasuryOutboundPaymentsIdReturnWithHttpInfo(String id, { List<String>? expand, ReturnedDetailsParams? returnedDetails, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/treasury/outbound_payments/{id}/return'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (returnedDetails != null) {
      formParams[r'returned_details'] = parameterToString(returnedDetails);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Test mode: Return an OutboundPayment
  ///
  /// <p>Transitions a test mode created OutboundPayment to the <code>returned</code> status. The OutboundPayment must already be in the <code>processing</code> state.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [ReturnedDetailsParams] returnedDetails:
  Future<TreasuryOutboundPayment?> postTestHelpersTreasuryOutboundPaymentsIdReturn(String id, { List<String>? expand, ReturnedDetailsParams? returnedDetails, }) async {
    final response = await postTestHelpersTreasuryOutboundPaymentsIdReturnWithHttpInfo(id,  expand: expand, returnedDetails: returnedDetails, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryOutboundPayment',) as TreasuryOutboundPayment;
    
    }
    return null;
  }

  /// Test mode: Update an OutboundTransfer
  ///
  /// <p>Updates a test mode created OutboundTransfer with tracking details. The OutboundTransfer must not be cancelable, and cannot be in the <code>canceled</code> or <code>failed</code> states.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] outboundTransfer (required):
  ///
  /// * [TrackingDetailsParams] trackingDetails (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postTestHelpersTreasuryOutboundTransfersOutboundTransferWithHttpInfo(String outboundTransfer, TrackingDetailsParams trackingDetails, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}'
      .replaceAll('{outbound_transfer}', outboundTransfer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (trackingDetails != null) {
      formParams[r'tracking_details'] = parameterToString(trackingDetails);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Test mode: Update an OutboundTransfer
  ///
  /// <p>Updates a test mode created OutboundTransfer with tracking details. The OutboundTransfer must not be cancelable, and cannot be in the <code>canceled</code> or <code>failed</code> states.</p>
  ///
  /// Parameters:
  ///
  /// * [String] outboundTransfer (required):
  ///
  /// * [TrackingDetailsParams] trackingDetails (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TreasuryOutboundTransfer?> postTestHelpersTreasuryOutboundTransfersOutboundTransfer(String outboundTransfer, TrackingDetailsParams trackingDetails, { List<String>? expand, }) async {
    final response = await postTestHelpersTreasuryOutboundTransfersOutboundTransferWithHttpInfo(outboundTransfer, trackingDetails,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryOutboundTransfer',) as TreasuryOutboundTransfer;
    
    }
    return null;
  }

  /// Test mode: Fail an OutboundTransfer
  ///
  /// <p>Transitions a test mode created OutboundTransfer to the <code>failed</code> status. The OutboundTransfer must already be in the <code>processing</code> state.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] outboundTransfer (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postTestHelpersTreasuryOutboundTransfersOutboundTransferFailWithHttpInfo(String outboundTransfer, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/fail'
      .replaceAll('{outbound_transfer}', outboundTransfer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Test mode: Fail an OutboundTransfer
  ///
  /// <p>Transitions a test mode created OutboundTransfer to the <code>failed</code> status. The OutboundTransfer must already be in the <code>processing</code> state.</p>
  ///
  /// Parameters:
  ///
  /// * [String] outboundTransfer (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TreasuryOutboundTransfer?> postTestHelpersTreasuryOutboundTransfersOutboundTransferFail(String outboundTransfer, { List<String>? expand, }) async {
    final response = await postTestHelpersTreasuryOutboundTransfersOutboundTransferFailWithHttpInfo(outboundTransfer,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryOutboundTransfer',) as TreasuryOutboundTransfer;
    
    }
    return null;
  }

  /// Test mode: Post an OutboundTransfer
  ///
  /// <p>Transitions a test mode created OutboundTransfer to the <code>posted</code> status. The OutboundTransfer must already be in the <code>processing</code> state.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] outboundTransfer (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postTestHelpersTreasuryOutboundTransfersOutboundTransferPostWithHttpInfo(String outboundTransfer, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/post'
      .replaceAll('{outbound_transfer}', outboundTransfer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Test mode: Post an OutboundTransfer
  ///
  /// <p>Transitions a test mode created OutboundTransfer to the <code>posted</code> status. The OutboundTransfer must already be in the <code>processing</code> state.</p>
  ///
  /// Parameters:
  ///
  /// * [String] outboundTransfer (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TreasuryOutboundTransfer?> postTestHelpersTreasuryOutboundTransfersOutboundTransferPost(String outboundTransfer, { List<String>? expand, }) async {
    final response = await postTestHelpersTreasuryOutboundTransfersOutboundTransferPostWithHttpInfo(outboundTransfer,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryOutboundTransfer',) as TreasuryOutboundTransfer;
    
    }
    return null;
  }

  /// Test mode: Return an OutboundTransfer
  ///
  /// <p>Transitions a test mode created OutboundTransfer to the <code>returned</code> status. The OutboundTransfer must already be in the <code>processing</code> state.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] outboundTransfer (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [ReturnedDetailsParams1] returnedDetails:
  Future<Response> postTestHelpersTreasuryOutboundTransfersOutboundTransferReturnWithHttpInfo(String outboundTransfer, { List<String>? expand, ReturnedDetailsParams1? returnedDetails, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/treasury/outbound_transfers/{outbound_transfer}/return'
      .replaceAll('{outbound_transfer}', outboundTransfer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (returnedDetails != null) {
      formParams[r'returned_details'] = parameterToString(returnedDetails);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Test mode: Return an OutboundTransfer
  ///
  /// <p>Transitions a test mode created OutboundTransfer to the <code>returned</code> status. The OutboundTransfer must already be in the <code>processing</code> state.</p>
  ///
  /// Parameters:
  ///
  /// * [String] outboundTransfer (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [ReturnedDetailsParams1] returnedDetails:
  Future<TreasuryOutboundTransfer?> postTestHelpersTreasuryOutboundTransfersOutboundTransferReturn(String outboundTransfer, { List<String>? expand, ReturnedDetailsParams1? returnedDetails, }) async {
    final response = await postTestHelpersTreasuryOutboundTransfersOutboundTransferReturnWithHttpInfo(outboundTransfer,  expand: expand, returnedDetails: returnedDetails, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryOutboundTransfer',) as TreasuryOutboundTransfer;
    
    }
    return null;
  }

  /// Test mode: Create a ReceivedCredit
  ///
  /// <p>Use this endpoint to simulate a test mode ReceivedCredit initiated by a third party. In live mode, you can’t directly create ReceivedCredits initiated by third parties.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [int] amount (required):
  ///   Amount (in cents) to be transferred.
  ///
  /// * [String] currency (required):
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] financialAccount (required):
  ///   The FinancialAccount to send funds to.
  ///
  /// * [String] network (required):
  ///   Specifies the network rails to be used. If not set, will default to the PaymentMethod's preferred network. See the [docs](https://stripe.com/docs/treasury/money-movement/timelines) to learn more about money movement timelines for each network type.
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [SourceParams] initiatingPaymentMethodDetails:
  Future<Response> postTestHelpersTreasuryReceivedCreditsWithHttpInfo(int amount, String currency, String financialAccount, String network, { String? description, List<String>? expand, SourceParams? initiatingPaymentMethodDetails, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/treasury/received_credits';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (financialAccount != null) {
      formParams[r'financial_account'] = parameterToString(financialAccount);
    }
    if (initiatingPaymentMethodDetails != null) {
      formParams[r'initiating_payment_method_details'] = parameterToString(initiatingPaymentMethodDetails);
    }
    if (network != null) {
      formParams[r'network'] = parameterToString(network);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Test mode: Create a ReceivedCredit
  ///
  /// <p>Use this endpoint to simulate a test mode ReceivedCredit initiated by a third party. In live mode, you can’t directly create ReceivedCredits initiated by third parties.</p>
  ///
  /// Parameters:
  ///
  /// * [int] amount (required):
  ///   Amount (in cents) to be transferred.
  ///
  /// * [String] currency (required):
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] financialAccount (required):
  ///   The FinancialAccount to send funds to.
  ///
  /// * [String] network (required):
  ///   Specifies the network rails to be used. If not set, will default to the PaymentMethod's preferred network. See the [docs](https://stripe.com/docs/treasury/money-movement/timelines) to learn more about money movement timelines for each network type.
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [SourceParams] initiatingPaymentMethodDetails:
  Future<TreasuryReceivedCredit?> postTestHelpersTreasuryReceivedCredits(int amount, String currency, String financialAccount, String network, { String? description, List<String>? expand, SourceParams? initiatingPaymentMethodDetails, }) async {
    final response = await postTestHelpersTreasuryReceivedCreditsWithHttpInfo(amount, currency, financialAccount, network,  description: description, expand: expand, initiatingPaymentMethodDetails: initiatingPaymentMethodDetails, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryReceivedCredit',) as TreasuryReceivedCredit;
    
    }
    return null;
  }

  /// Test mode: Create a ReceivedDebit
  ///
  /// <p>Use this endpoint to simulate a test mode ReceivedDebit initiated by a third party. In live mode, you can’t directly create ReceivedDebits initiated by third parties.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [int] amount (required):
  ///   Amount (in cents) to be transferred.
  ///
  /// * [String] currency (required):
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] financialAccount (required):
  ///   The FinancialAccount to pull funds from.
  ///
  /// * [String] network (required):
  ///   Specifies the network rails to be used. If not set, will default to the PaymentMethod's preferred network. See the [docs](https://stripe.com/docs/treasury/money-movement/timelines) to learn more about money movement timelines for each network type.
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [SourceParams] initiatingPaymentMethodDetails:
  Future<Response> postTestHelpersTreasuryReceivedDebitsWithHttpInfo(int amount, String currency, String financialAccount, String network, { String? description, List<String>? expand, SourceParams? initiatingPaymentMethodDetails, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/test_helpers/treasury/received_debits';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (financialAccount != null) {
      formParams[r'financial_account'] = parameterToString(financialAccount);
    }
    if (initiatingPaymentMethodDetails != null) {
      formParams[r'initiating_payment_method_details'] = parameterToString(initiatingPaymentMethodDetails);
    }
    if (network != null) {
      formParams[r'network'] = parameterToString(network);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Test mode: Create a ReceivedDebit
  ///
  /// <p>Use this endpoint to simulate a test mode ReceivedDebit initiated by a third party. In live mode, you can’t directly create ReceivedDebits initiated by third parties.</p>
  ///
  /// Parameters:
  ///
  /// * [int] amount (required):
  ///   Amount (in cents) to be transferred.
  ///
  /// * [String] currency (required):
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] financialAccount (required):
  ///   The FinancialAccount to pull funds from.
  ///
  /// * [String] network (required):
  ///   Specifies the network rails to be used. If not set, will default to the PaymentMethod's preferred network. See the [docs](https://stripe.com/docs/treasury/money-movement/timelines) to learn more about money movement timelines for each network type.
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [SourceParams] initiatingPaymentMethodDetails:
  Future<TreasuryReceivedDebit?> postTestHelpersTreasuryReceivedDebits(int amount, String currency, String financialAccount, String network, { String? description, List<String>? expand, SourceParams? initiatingPaymentMethodDetails, }) async {
    final response = await postTestHelpersTreasuryReceivedDebitsWithHttpInfo(amount, currency, financialAccount, network,  description: description, expand: expand, initiatingPaymentMethodDetails: initiatingPaymentMethodDetails, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryReceivedDebit',) as TreasuryReceivedDebit;
    
    }
    return null;
  }

  /// Create a CVC update token
  ///
  /// <p>Creates a single-use token that represents a bank account’s details. You can use this token with any v1 API method in place of a bank account dictionary. You can only use this token once. To do so, attach it to a <a href=\"#accounts\">connected account</a> where <a href=\"/api/accounts/object#account_object-controller-requirement_collection\">controller.requirement_collection</a> is <code>application</code>, which includes Custom accounts.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [ConnectJsAccountTokenSpecs] account:
  ///
  /// * [TokenCreateBankAccount] bankAccount:
  ///
  /// * [PostTokensRequestCard] card:
  ///
  /// * [String] customer:
  ///   Create a token for the customer, which is owned by the application's account. You can only use this with an [OAuth access token](https://stripe.com/docs/connect/standard-accounts) or [Stripe-Account header](https://stripe.com/docs/connect/authentication). Learn more about [cloning saved payment methods](https://stripe.com/docs/connect/cloning-saved-payment-methods).
  ///
  /// * [CvcParams] cvcUpdate:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PersonTokenSpecs] person:
  ///
  /// * [PiiTokenSpecs] pii:
  Future<Response> postTokensWithHttpInfo({ ConnectJsAccountTokenSpecs? account, TokenCreateBankAccount? bankAccount, PostTokensRequestCard? card, String? customer, CvcParams? cvcUpdate, List<String>? expand, PersonTokenSpecs? person, PiiTokenSpecs? pii, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/tokens';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (account != null) {
      formParams[r'account'] = parameterToString(account);
    }
    if (bankAccount != null) {
      formParams[r'bank_account'] = parameterToString(bankAccount);
    }
    if (card != null) {
      formParams[r'card'] = parameterToString(card);
    }
    if (customer != null) {
      formParams[r'customer'] = parameterToString(customer);
    }
    if (cvcUpdate != null) {
      formParams[r'cvc_update'] = parameterToString(cvcUpdate);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (person != null) {
      formParams[r'person'] = parameterToString(person);
    }
    if (pii != null) {
      formParams[r'pii'] = parameterToString(pii);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a CVC update token
  ///
  /// <p>Creates a single-use token that represents a bank account’s details. You can use this token with any v1 API method in place of a bank account dictionary. You can only use this token once. To do so, attach it to a <a href=\"#accounts\">connected account</a> where <a href=\"/api/accounts/object#account_object-controller-requirement_collection\">controller.requirement_collection</a> is <code>application</code>, which includes Custom accounts.</p>
  ///
  /// Parameters:
  ///
  /// * [ConnectJsAccountTokenSpecs] account:
  ///
  /// * [TokenCreateBankAccount] bankAccount:
  ///
  /// * [PostTokensRequestCard] card:
  ///
  /// * [String] customer:
  ///   Create a token for the customer, which is owned by the application's account. You can only use this with an [OAuth access token](https://stripe.com/docs/connect/standard-accounts) or [Stripe-Account header](https://stripe.com/docs/connect/authentication). Learn more about [cloning saved payment methods](https://stripe.com/docs/connect/cloning-saved-payment-methods).
  ///
  /// * [CvcParams] cvcUpdate:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PersonTokenSpecs] person:
  ///
  /// * [PiiTokenSpecs] pii:
  Future<Token?> postTokens({ ConnectJsAccountTokenSpecs? account, TokenCreateBankAccount? bankAccount, PostTokensRequestCard? card, String? customer, CvcParams? cvcUpdate, List<String>? expand, PersonTokenSpecs? person, PiiTokenSpecs? pii, }) async {
    final response = await postTokensWithHttpInfo( account: account, bankAccount: bankAccount, card: card, customer: customer, cvcUpdate: cvcUpdate, expand: expand, person: person, pii: pii, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Token',) as Token;
    
    }
    return null;
  }

  /// Create a top-up
  ///
  /// <p>Top up the balance of an account</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [int] amount (required):
  ///   A positive integer representing how much to transfer.
  ///
  /// * [String] currency (required):
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] source_:
  ///   The ID of a source to transfer funds from. For most users, this should be left unspecified which will use the bank account that was set up in the dashboard for the specified currency. In test mode, this can be a test bank token (see [Testing Top-ups](https://stripe.com/docs/connect/testing#testing-top-ups)).
  ///
  /// * [String] statementDescriptor:
  ///   Extra information about a top-up for the source's bank statement. Limited to 15 ASCII characters.
  ///
  /// * [String] transferGroup:
  ///   A string that identifies this top-up as part of a group.
  Future<Response> postTopupsWithHttpInfo(int amount, String currency, { String? description, List<String>? expand, PostAccountsRequestMetadata? metadata, String? source_, String? statementDescriptor, String? transferGroup, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/topups';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (source_ != null) {
      formParams[r'source'] = parameterToString(source_);
    }
    if (statementDescriptor != null) {
      formParams[r'statement_descriptor'] = parameterToString(statementDescriptor);
    }
    if (transferGroup != null) {
      formParams[r'transfer_group'] = parameterToString(transferGroup);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a top-up
  ///
  /// <p>Top up the balance of an account</p>
  ///
  /// Parameters:
  ///
  /// * [int] amount (required):
  ///   A positive integer representing how much to transfer.
  ///
  /// * [String] currency (required):
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] source_:
  ///   The ID of a source to transfer funds from. For most users, this should be left unspecified which will use the bank account that was set up in the dashboard for the specified currency. In test mode, this can be a test bank token (see [Testing Top-ups](https://stripe.com/docs/connect/testing#testing-top-ups)).
  ///
  /// * [String] statementDescriptor:
  ///   Extra information about a top-up for the source's bank statement. Limited to 15 ASCII characters.
  ///
  /// * [String] transferGroup:
  ///   A string that identifies this top-up as part of a group.
  Future<Topup?> postTopups(int amount, String currency, { String? description, List<String>? expand, PostAccountsRequestMetadata? metadata, String? source_, String? statementDescriptor, String? transferGroup, }) async {
    final response = await postTopupsWithHttpInfo(amount, currency,  description: description, expand: expand, metadata: metadata, source_: source_, statementDescriptor: statementDescriptor, transferGroup: transferGroup, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Topup',) as Topup;
    
    }
    return null;
  }

  /// Update a top-up
  ///
  /// <p>Updates the metadata of a top-up. Other top-up details are not editable by design.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] topup (required):
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<Response> postTopupsTopupWithHttpInfo(String topup, { String? description, List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/topups/{topup}'
      .replaceAll('{topup}', topup);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a top-up
  ///
  /// <p>Updates the metadata of a top-up. Other top-up details are not editable by design.</p>
  ///
  /// Parameters:
  ///
  /// * [String] topup (required):
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<Topup?> postTopupsTopup(String topup, { String? description, List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    final response = await postTopupsTopupWithHttpInfo(topup,  description: description, expand: expand, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Topup',) as Topup;
    
    }
    return null;
  }

  /// Cancel a top-up
  ///
  /// <p>Cancels a top-up. Only pending top-ups can be canceled.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] topup (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postTopupsTopupCancelWithHttpInfo(String topup, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/topups/{topup}/cancel'
      .replaceAll('{topup}', topup);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Cancel a top-up
  ///
  /// <p>Cancels a top-up. Only pending top-ups can be canceled.</p>
  ///
  /// Parameters:
  ///
  /// * [String] topup (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Topup?> postTopupsTopupCancel(String topup, { List<String>? expand, }) async {
    final response = await postTopupsTopupCancelWithHttpInfo(topup,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Topup',) as Topup;
    
    }
    return null;
  }

  /// Create a transfer
  ///
  /// <p>To send funds from your Stripe account to a connected account, you create a new transfer object. Your <a href=\"#balance\">Stripe balance</a> must be able to cover the transfer amount, or you’ll receive an “Insufficient Funds” error.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] currency (required):
  ///   Three-letter [ISO code for currency](https://www.iso.org/iso-4217-currency-codes.html) in lowercase. Must be a [supported currency](https://docs.stripe.com/currencies).
  ///
  /// * [String] destination (required):
  ///   The ID of a connected Stripe account. <a href=\\\"/docs/connect/separate-charges-and-transfers\\\">See the Connect documentation</a> for details.
  ///
  /// * [int] amount:
  ///   A positive integer in cents (or local equivalent) representing how much to transfer.
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] sourceTransaction:
  ///   You can use this parameter to transfer funds from a charge before they are added to your available balance. A pending balance will transfer immediately but the funds will not become available until the original charge becomes available. [See the Connect documentation](https://stripe.com/docs/connect/separate-charges-and-transfers#transfer-availability) for details.
  ///
  /// * [String] sourceType:
  ///   The source balance to use for this transfer. One of `bank_account`, `card`, or `fpx`. For most users, this will default to `card`.
  ///
  /// * [String] transferGroup:
  ///   A string that identifies this transaction as part of a group. See the [Connect documentation](https://stripe.com/docs/connect/separate-charges-and-transfers#transfer-options) for details.
  Future<Response> postTransfersWithHttpInfo(String currency, String destination, { int? amount, String? description, List<String>? expand, Map<String, String>? metadata, String? sourceTransaction, String? sourceType, String? transferGroup, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/transfers';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (destination != null) {
      formParams[r'destination'] = parameterToString(destination);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (sourceTransaction != null) {
      formParams[r'source_transaction'] = parameterToString(sourceTransaction);
    }
    if (sourceType != null) {
      formParams[r'source_type'] = parameterToString(sourceType);
    }
    if (transferGroup != null) {
      formParams[r'transfer_group'] = parameterToString(transferGroup);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a transfer
  ///
  /// <p>To send funds from your Stripe account to a connected account, you create a new transfer object. Your <a href=\"#balance\">Stripe balance</a> must be able to cover the transfer amount, or you’ll receive an “Insufficient Funds” error.</p>
  ///
  /// Parameters:
  ///
  /// * [String] currency (required):
  ///   Three-letter [ISO code for currency](https://www.iso.org/iso-4217-currency-codes.html) in lowercase. Must be a [supported currency](https://docs.stripe.com/currencies).
  ///
  /// * [String] destination (required):
  ///   The ID of a connected Stripe account. <a href=\\\"/docs/connect/separate-charges-and-transfers\\\">See the Connect documentation</a> for details.
  ///
  /// * [int] amount:
  ///   A positive integer in cents (or local equivalent) representing how much to transfer.
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] sourceTransaction:
  ///   You can use this parameter to transfer funds from a charge before they are added to your available balance. A pending balance will transfer immediately but the funds will not become available until the original charge becomes available. [See the Connect documentation](https://stripe.com/docs/connect/separate-charges-and-transfers#transfer-availability) for details.
  ///
  /// * [String] sourceType:
  ///   The source balance to use for this transfer. One of `bank_account`, `card`, or `fpx`. For most users, this will default to `card`.
  ///
  /// * [String] transferGroup:
  ///   A string that identifies this transaction as part of a group. See the [Connect documentation](https://stripe.com/docs/connect/separate-charges-and-transfers#transfer-options) for details.
  Future<Transfer?> postTransfers(String currency, String destination, { int? amount, String? description, List<String>? expand, Map<String, String>? metadata, String? sourceTransaction, String? sourceType, String? transferGroup, }) async {
    final response = await postTransfersWithHttpInfo(currency, destination,  amount: amount, description: description, expand: expand, metadata: metadata, sourceTransaction: sourceTransaction, sourceType: sourceType, transferGroup: transferGroup, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Transfer',) as Transfer;
    
    }
    return null;
  }

  /// Create a transfer reversal
  ///
  /// <p>When you create a new reversal, you must specify a transfer to create it on.</p>  <p>When reversing transfers, you can optionally reverse part of the transfer. You can do so as many times as you wish until the entire transfer has been reversed.</p>  <p>Once entirely reversed, a transfer can’t be reversed again. This method will return an error when called on an already-reversed transfer, or when trying to reverse more money than is left on a transfer.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [int] amount:
  ///   A positive integer in cents (or local equivalent) representing how much of this transfer to reverse. Can only reverse up to the unreversed amount remaining of the transfer. Partial transfer reversals are only allowed for transfers to Stripe Accounts. Defaults to the entire transfer amount.
  ///
  /// * [String] description:
  ///   An arbitrary string which you can attach to a reversal object. This will be unset if you POST an empty value.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [bool] refundApplicationFee:
  ///   Boolean indicating whether the application fee should be refunded when reversing this transfer. If a full transfer reversal is given, the full application fee will be refunded. Otherwise, the application fee will be refunded with an amount proportional to the amount of the transfer reversed.
  Future<Response> postTransfersIdReversalsWithHttpInfo(String id, { int? amount, String? description, List<String>? expand, PostAccountsRequestMetadata? metadata, bool? refundApplicationFee, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/transfers/{id}/reversals'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (refundApplicationFee != null) {
      formParams[r'refund_application_fee'] = parameterToString(refundApplicationFee);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a transfer reversal
  ///
  /// <p>When you create a new reversal, you must specify a transfer to create it on.</p>  <p>When reversing transfers, you can optionally reverse part of the transfer. You can do so as many times as you wish until the entire transfer has been reversed.</p>  <p>Once entirely reversed, a transfer can’t be reversed again. This method will return an error when called on an already-reversed transfer, or when trying to reverse more money than is left on a transfer.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [int] amount:
  ///   A positive integer in cents (or local equivalent) representing how much of this transfer to reverse. Can only reverse up to the unreversed amount remaining of the transfer. Partial transfer reversals are only allowed for transfers to Stripe Accounts. Defaults to the entire transfer amount.
  ///
  /// * [String] description:
  ///   An arbitrary string which you can attach to a reversal object. This will be unset if you POST an empty value.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [bool] refundApplicationFee:
  ///   Boolean indicating whether the application fee should be refunded when reversing this transfer. If a full transfer reversal is given, the full application fee will be refunded. Otherwise, the application fee will be refunded with an amount proportional to the amount of the transfer reversed.
  Future<TransferReversal?> postTransfersIdReversals(String id, { int? amount, String? description, List<String>? expand, PostAccountsRequestMetadata? metadata, bool? refundApplicationFee, }) async {
    final response = await postTransfersIdReversalsWithHttpInfo(id,  amount: amount, description: description, expand: expand, metadata: metadata, refundApplicationFee: refundApplicationFee, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TransferReversal',) as TransferReversal;
    
    }
    return null;
  }

  /// Update a transfer
  ///
  /// <p>Updates the specified transfer by setting the values of the parameters passed. Any parameters not provided will be left unchanged.</p>  <p>This request accepts only metadata as an argument.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] transfer (required):
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<Response> postTransfersTransferWithHttpInfo(String transfer, { String? description, List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/transfers/{transfer}'
      .replaceAll('{transfer}', transfer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a transfer
  ///
  /// <p>Updates the specified transfer by setting the values of the parameters passed. Any parameters not provided will be left unchanged.</p>  <p>This request accepts only metadata as an argument.</p>
  ///
  /// Parameters:
  ///
  /// * [String] transfer (required):
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<Transfer?> postTransfersTransfer(String transfer, { String? description, List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    final response = await postTransfersTransferWithHttpInfo(transfer,  description: description, expand: expand, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'Transfer',) as Transfer;
    
    }
    return null;
  }

  /// Update a reversal
  ///
  /// <p>Updates the specified reversal by setting the values of the parameters passed. Any parameters not provided will be left unchanged.</p>  <p>This request only accepts metadata and description as arguments.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [String] transfer (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<Response> postTransfersTransferReversalsIdWithHttpInfo(String id, String transfer, { List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/transfers/{transfer}/reversals/{id}'
      .replaceAll('{id}', id)
      .replaceAll('{transfer}', transfer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a reversal
  ///
  /// <p>Updates the specified reversal by setting the values of the parameters passed. Any parameters not provided will be left unchanged.</p>  <p>This request only accepts metadata and description as arguments.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [String] transfer (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<TransferReversal?> postTransfersTransferReversalsId(String id, String transfer, { List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    final response = await postTransfersTransferReversalsIdWithHttpInfo(id, transfer,  expand: expand, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TransferReversal',) as TransferReversal;
    
    }
    return null;
  }

  /// Create a CreditReversal
  ///
  /// <p>Reverses a ReceivedCredit and creates a CreditReversal object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] receivedCredit (required):
  ///   The ReceivedCredit to reverse.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  Future<Response> postTreasuryCreditReversalsWithHttpInfo(String receivedCredit, { List<String>? expand, Map<String, String>? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/treasury/credit_reversals';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (receivedCredit != null) {
      formParams[r'received_credit'] = parameterToString(receivedCredit);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a CreditReversal
  ///
  /// <p>Reverses a ReceivedCredit and creates a CreditReversal object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] receivedCredit (required):
  ///   The ReceivedCredit to reverse.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  Future<TreasuryCreditReversal?> postTreasuryCreditReversals(String receivedCredit, { List<String>? expand, Map<String, String>? metadata, }) async {
    final response = await postTreasuryCreditReversalsWithHttpInfo(receivedCredit,  expand: expand, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryCreditReversal',) as TreasuryCreditReversal;
    
    }
    return null;
  }

  /// Create a DebitReversal
  ///
  /// <p>Reverses a ReceivedDebit and creates a DebitReversal object.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] receivedDebit (required):
  ///   The ReceivedDebit to reverse.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  Future<Response> postTreasuryDebitReversalsWithHttpInfo(String receivedDebit, { List<String>? expand, Map<String, String>? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/treasury/debit_reversals';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (receivedDebit != null) {
      formParams[r'received_debit'] = parameterToString(receivedDebit);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a DebitReversal
  ///
  /// <p>Reverses a ReceivedDebit and creates a DebitReversal object.</p>
  ///
  /// Parameters:
  ///
  /// * [String] receivedDebit (required):
  ///   The ReceivedDebit to reverse.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  Future<TreasuryDebitReversal?> postTreasuryDebitReversals(String receivedDebit, { List<String>? expand, Map<String, String>? metadata, }) async {
    final response = await postTreasuryDebitReversalsWithHttpInfo(receivedDebit,  expand: expand, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryDebitReversal',) as TreasuryDebitReversal;
    
    }
    return null;
  }

  /// Create a FinancialAccount
  ///
  /// <p>Creates a new FinancialAccount. Each connected account can have up to three FinancialAccounts by default.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [List<String>] supportedCurrencies (required):
  ///   The currencies the FinancialAccount can hold a balance in.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [FeatureAccess] features:
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [PostTreasuryFinancialAccountsRequestNickname] nickname:
  ///
  /// * [PlatformRestrictions] platformRestrictions:
  Future<Response> postTreasuryFinancialAccountsWithHttpInfo(List<String> supportedCurrencies, { List<String>? expand, FeatureAccess? features, Map<String, String>? metadata, PostTreasuryFinancialAccountsRequestNickname? nickname, PlatformRestrictions? platformRestrictions, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/treasury/financial_accounts';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (features != null) {
      formParams[r'features'] = parameterToString(features);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (nickname != null) {
      formParams[r'nickname'] = parameterToString(nickname);
    }
    if (platformRestrictions != null) {
      formParams[r'platform_restrictions'] = parameterToString(platformRestrictions);
    }
    if (supportedCurrencies != null) {
      formParams[r'supported_currencies'] = parameterToString(supportedCurrencies);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a FinancialAccount
  ///
  /// <p>Creates a new FinancialAccount. Each connected account can have up to three FinancialAccounts by default.</p>
  ///
  /// Parameters:
  ///
  /// * [List<String>] supportedCurrencies (required):
  ///   The currencies the FinancialAccount can hold a balance in.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [FeatureAccess] features:
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [PostTreasuryFinancialAccountsRequestNickname] nickname:
  ///
  /// * [PlatformRestrictions] platformRestrictions:
  Future<TreasuryFinancialAccount?> postTreasuryFinancialAccounts(List<String> supportedCurrencies, { List<String>? expand, FeatureAccess? features, Map<String, String>? metadata, PostTreasuryFinancialAccountsRequestNickname? nickname, PlatformRestrictions? platformRestrictions, }) async {
    final response = await postTreasuryFinancialAccountsWithHttpInfo(supportedCurrencies,  expand: expand, features: features, metadata: metadata, nickname: nickname, platformRestrictions: platformRestrictions, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryFinancialAccount',) as TreasuryFinancialAccount;
    
    }
    return null;
  }

  /// Update a FinancialAccount
  ///
  /// <p>Updates the details of a FinancialAccount.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] financialAccount (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [FeatureAccess1] features:
  ///
  /// * [ForwardingSettings] forwardingSettings:
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [PostTreasuryFinancialAccountsRequestNickname] nickname:
  ///
  /// * [PlatformRestrictions] platformRestrictions:
  Future<Response> postTreasuryFinancialAccountsFinancialAccountWithHttpInfo(String financialAccount, { List<String>? expand, FeatureAccess1? features, ForwardingSettings? forwardingSettings, Map<String, String>? metadata, PostTreasuryFinancialAccountsRequestNickname? nickname, PlatformRestrictions? platformRestrictions, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/treasury/financial_accounts/{financial_account}'
      .replaceAll('{financial_account}', financialAccount);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (features != null) {
      formParams[r'features'] = parameterToString(features);
    }
    if (forwardingSettings != null) {
      formParams[r'forwarding_settings'] = parameterToString(forwardingSettings);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (nickname != null) {
      formParams[r'nickname'] = parameterToString(nickname);
    }
    if (platformRestrictions != null) {
      formParams[r'platform_restrictions'] = parameterToString(platformRestrictions);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a FinancialAccount
  ///
  /// <p>Updates the details of a FinancialAccount.</p>
  ///
  /// Parameters:
  ///
  /// * [String] financialAccount (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [FeatureAccess1] features:
  ///
  /// * [ForwardingSettings] forwardingSettings:
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [PostTreasuryFinancialAccountsRequestNickname] nickname:
  ///
  /// * [PlatformRestrictions] platformRestrictions:
  Future<TreasuryFinancialAccount?> postTreasuryFinancialAccountsFinancialAccount(String financialAccount, { List<String>? expand, FeatureAccess1? features, ForwardingSettings? forwardingSettings, Map<String, String>? metadata, PostTreasuryFinancialAccountsRequestNickname? nickname, PlatformRestrictions? platformRestrictions, }) async {
    final response = await postTreasuryFinancialAccountsFinancialAccountWithHttpInfo(financialAccount,  expand: expand, features: features, forwardingSettings: forwardingSettings, metadata: metadata, nickname: nickname, platformRestrictions: platformRestrictions, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryFinancialAccount',) as TreasuryFinancialAccount;
    
    }
    return null;
  }

  /// Close a FinancialAccount
  ///
  /// <p>Closes a FinancialAccount. A FinancialAccount can only be closed if it has a zero balance, has no pending InboundTransfers, and has canceled all attached Issuing cards.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] financialAccount (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [ForwardingSettings] forwardingSettings:
  Future<Response> postTreasuryFinancialAccountsFinancialAccountCloseWithHttpInfo(String financialAccount, { List<String>? expand, ForwardingSettings? forwardingSettings, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/treasury/financial_accounts/{financial_account}/close'
      .replaceAll('{financial_account}', financialAccount);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (forwardingSettings != null) {
      formParams[r'forwarding_settings'] = parameterToString(forwardingSettings);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Close a FinancialAccount
  ///
  /// <p>Closes a FinancialAccount. A FinancialAccount can only be closed if it has a zero balance, has no pending InboundTransfers, and has canceled all attached Issuing cards.</p>
  ///
  /// Parameters:
  ///
  /// * [String] financialAccount (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [ForwardingSettings] forwardingSettings:
  Future<TreasuryFinancialAccount?> postTreasuryFinancialAccountsFinancialAccountClose(String financialAccount, { List<String>? expand, ForwardingSettings? forwardingSettings, }) async {
    final response = await postTreasuryFinancialAccountsFinancialAccountCloseWithHttpInfo(financialAccount,  expand: expand, forwardingSettings: forwardingSettings, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryFinancialAccount',) as TreasuryFinancialAccount;
    
    }
    return null;
  }

  /// Update FinancialAccount Features
  ///
  /// <p>Updates the Features associated with a FinancialAccount.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] financialAccount (required):
  ///
  /// * [Access1] cardIssuing:
  ///
  /// * [Access2] depositInsurance:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [FinancialAddresses1] financialAddresses:
  ///
  /// * [InboundTransfers2] inboundTransfers:
  ///
  /// * [Access3] intraStripeFlows:
  ///
  /// * [OutboundPayments1] outboundPayments:
  ///
  /// * [OutboundTransfers1] outboundTransfers:
  Future<Response> postTreasuryFinancialAccountsFinancialAccountFeaturesWithHttpInfo(String financialAccount, { Access1? cardIssuing, Access2? depositInsurance, List<String>? expand, FinancialAddresses1? financialAddresses, InboundTransfers2? inboundTransfers, Access3? intraStripeFlows, OutboundPayments1? outboundPayments, OutboundTransfers1? outboundTransfers, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/treasury/financial_accounts/{financial_account}/features'
      .replaceAll('{financial_account}', financialAccount);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (cardIssuing != null) {
      formParams[r'card_issuing'] = parameterToString(cardIssuing);
    }
    if (depositInsurance != null) {
      formParams[r'deposit_insurance'] = parameterToString(depositInsurance);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (financialAddresses != null) {
      formParams[r'financial_addresses'] = parameterToString(financialAddresses);
    }
    if (inboundTransfers != null) {
      formParams[r'inbound_transfers'] = parameterToString(inboundTransfers);
    }
    if (intraStripeFlows != null) {
      formParams[r'intra_stripe_flows'] = parameterToString(intraStripeFlows);
    }
    if (outboundPayments != null) {
      formParams[r'outbound_payments'] = parameterToString(outboundPayments);
    }
    if (outboundTransfers != null) {
      formParams[r'outbound_transfers'] = parameterToString(outboundTransfers);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update FinancialAccount Features
  ///
  /// <p>Updates the Features associated with a FinancialAccount.</p>
  ///
  /// Parameters:
  ///
  /// * [String] financialAccount (required):
  ///
  /// * [Access1] cardIssuing:
  ///
  /// * [Access2] depositInsurance:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [FinancialAddresses1] financialAddresses:
  ///
  /// * [InboundTransfers2] inboundTransfers:
  ///
  /// * [Access3] intraStripeFlows:
  ///
  /// * [OutboundPayments1] outboundPayments:
  ///
  /// * [OutboundTransfers1] outboundTransfers:
  Future<TreasuryFinancialAccountFeatures?> postTreasuryFinancialAccountsFinancialAccountFeatures(String financialAccount, { Access1? cardIssuing, Access2? depositInsurance, List<String>? expand, FinancialAddresses1? financialAddresses, InboundTransfers2? inboundTransfers, Access3? intraStripeFlows, OutboundPayments1? outboundPayments, OutboundTransfers1? outboundTransfers, }) async {
    final response = await postTreasuryFinancialAccountsFinancialAccountFeaturesWithHttpInfo(financialAccount,  cardIssuing: cardIssuing, depositInsurance: depositInsurance, expand: expand, financialAddresses: financialAddresses, inboundTransfers: inboundTransfers, intraStripeFlows: intraStripeFlows, outboundPayments: outboundPayments, outboundTransfers: outboundTransfers, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryFinancialAccountFeatures',) as TreasuryFinancialAccountFeatures;
    
    }
    return null;
  }

  /// Create an InboundTransfer
  ///
  /// <p>Creates an InboundTransfer.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [int] amount (required):
  ///   Amount (in cents) to be transferred.
  ///
  /// * [String] currency (required):
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] financialAccount (required):
  ///   The FinancialAccount to send funds to.
  ///
  /// * [String] originPaymentMethod (required):
  ///   The origin payment method to be debited for the InboundTransfer.
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] statementDescriptor:
  ///   The complete description that appears on your customers' statements. Maximum 10 characters.
  Future<Response> postTreasuryInboundTransfersWithHttpInfo(int amount, String currency, String financialAccount, String originPaymentMethod, { String? description, List<String>? expand, Map<String, String>? metadata, String? statementDescriptor, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/treasury/inbound_transfers';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (financialAccount != null) {
      formParams[r'financial_account'] = parameterToString(financialAccount);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (originPaymentMethod != null) {
      formParams[r'origin_payment_method'] = parameterToString(originPaymentMethod);
    }
    if (statementDescriptor != null) {
      formParams[r'statement_descriptor'] = parameterToString(statementDescriptor);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create an InboundTransfer
  ///
  /// <p>Creates an InboundTransfer.</p>
  ///
  /// Parameters:
  ///
  /// * [int] amount (required):
  ///   Amount (in cents) to be transferred.
  ///
  /// * [String] currency (required):
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] financialAccount (required):
  ///   The FinancialAccount to send funds to.
  ///
  /// * [String] originPaymentMethod (required):
  ///   The origin payment method to be debited for the InboundTransfer.
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] statementDescriptor:
  ///   The complete description that appears on your customers' statements. Maximum 10 characters.
  Future<TreasuryInboundTransfer?> postTreasuryInboundTransfers(int amount, String currency, String financialAccount, String originPaymentMethod, { String? description, List<String>? expand, Map<String, String>? metadata, String? statementDescriptor, }) async {
    final response = await postTreasuryInboundTransfersWithHttpInfo(amount, currency, financialAccount, originPaymentMethod,  description: description, expand: expand, metadata: metadata, statementDescriptor: statementDescriptor, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryInboundTransfer',) as TreasuryInboundTransfer;
    
    }
    return null;
  }

  /// Cancel an InboundTransfer
  ///
  /// <p>Cancels an InboundTransfer.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] inboundTransfer (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postTreasuryInboundTransfersInboundTransferCancelWithHttpInfo(String inboundTransfer, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/treasury/inbound_transfers/{inbound_transfer}/cancel'
      .replaceAll('{inbound_transfer}', inboundTransfer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Cancel an InboundTransfer
  ///
  /// <p>Cancels an InboundTransfer.</p>
  ///
  /// Parameters:
  ///
  /// * [String] inboundTransfer (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TreasuryInboundTransfer?> postTreasuryInboundTransfersInboundTransferCancel(String inboundTransfer, { List<String>? expand, }) async {
    final response = await postTreasuryInboundTransfersInboundTransferCancelWithHttpInfo(inboundTransfer,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryInboundTransfer',) as TreasuryInboundTransfer;
    
    }
    return null;
  }

  /// Create an OutboundPayment
  ///
  /// <p>Creates an OutboundPayment.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [int] amount (required):
  ///   Amount (in cents) to be transferred.
  ///
  /// * [String] currency (required):
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] financialAccount (required):
  ///   The FinancialAccount to pull funds from.
  ///
  /// * [String] customer:
  ///   ID of the customer to whom the OutboundPayment is sent. Must match the Customer attached to the `destination_payment_method` passed in.
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [String] destinationPaymentMethod:
  ///   The PaymentMethod to use as the payment instrument for the OutboundPayment. Exclusive with `destination_payment_method_data`.
  ///
  /// * [PaymentMethodData] destinationPaymentMethodData:
  ///
  /// * [PaymentMethodOptions2] destinationPaymentMethodOptions:
  ///
  /// * [EndUserDetailsParams] endUserDetails:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] statementDescriptor:
  ///   The description that appears on the receiving end for this OutboundPayment (for example, bank statement for external bank transfer). Maximum 10 characters for `ach` payments, 140 characters for `us_domestic_wire` payments, or 500 characters for `stripe` network transfers. The default value is \\\"payment\\\".
  Future<Response> postTreasuryOutboundPaymentsWithHttpInfo(int amount, String currency, String financialAccount, { String? customer, String? description, String? destinationPaymentMethod, PaymentMethodData? destinationPaymentMethodData, PaymentMethodOptions2? destinationPaymentMethodOptions, EndUserDetailsParams? endUserDetails, List<String>? expand, Map<String, String>? metadata, String? statementDescriptor, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/treasury/outbound_payments';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (customer != null) {
      formParams[r'customer'] = parameterToString(customer);
    }
    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (destinationPaymentMethod != null) {
      formParams[r'destination_payment_method'] = parameterToString(destinationPaymentMethod);
    }
    if (destinationPaymentMethodData != null) {
      formParams[r'destination_payment_method_data'] = parameterToString(destinationPaymentMethodData);
    }
    if (destinationPaymentMethodOptions != null) {
      formParams[r'destination_payment_method_options'] = parameterToString(destinationPaymentMethodOptions);
    }
    if (endUserDetails != null) {
      formParams[r'end_user_details'] = parameterToString(endUserDetails);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (financialAccount != null) {
      formParams[r'financial_account'] = parameterToString(financialAccount);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (statementDescriptor != null) {
      formParams[r'statement_descriptor'] = parameterToString(statementDescriptor);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create an OutboundPayment
  ///
  /// <p>Creates an OutboundPayment.</p>
  ///
  /// Parameters:
  ///
  /// * [int] amount (required):
  ///   Amount (in cents) to be transferred.
  ///
  /// * [String] currency (required):
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] financialAccount (required):
  ///   The FinancialAccount to pull funds from.
  ///
  /// * [String] customer:
  ///   ID of the customer to whom the OutboundPayment is sent. Must match the Customer attached to the `destination_payment_method` passed in.
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [String] destinationPaymentMethod:
  ///   The PaymentMethod to use as the payment instrument for the OutboundPayment. Exclusive with `destination_payment_method_data`.
  ///
  /// * [PaymentMethodData] destinationPaymentMethodData:
  ///
  /// * [PaymentMethodOptions2] destinationPaymentMethodOptions:
  ///
  /// * [EndUserDetailsParams] endUserDetails:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] statementDescriptor:
  ///   The description that appears on the receiving end for this OutboundPayment (for example, bank statement for external bank transfer). Maximum 10 characters for `ach` payments, 140 characters for `us_domestic_wire` payments, or 500 characters for `stripe` network transfers. The default value is \\\"payment\\\".
  Future<TreasuryOutboundPayment?> postTreasuryOutboundPayments(int amount, String currency, String financialAccount, { String? customer, String? description, String? destinationPaymentMethod, PaymentMethodData? destinationPaymentMethodData, PaymentMethodOptions2? destinationPaymentMethodOptions, EndUserDetailsParams? endUserDetails, List<String>? expand, Map<String, String>? metadata, String? statementDescriptor, }) async {
    final response = await postTreasuryOutboundPaymentsWithHttpInfo(amount, currency, financialAccount,  customer: customer, description: description, destinationPaymentMethod: destinationPaymentMethod, destinationPaymentMethodData: destinationPaymentMethodData, destinationPaymentMethodOptions: destinationPaymentMethodOptions, endUserDetails: endUserDetails, expand: expand, metadata: metadata, statementDescriptor: statementDescriptor, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryOutboundPayment',) as TreasuryOutboundPayment;
    
    }
    return null;
  }

  /// Cancel an OutboundPayment
  ///
  /// <p>Cancel an OutboundPayment.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postTreasuryOutboundPaymentsIdCancelWithHttpInfo(String id, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/treasury/outbound_payments/{id}/cancel'
      .replaceAll('{id}', id);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Cancel an OutboundPayment
  ///
  /// <p>Cancel an OutboundPayment.</p>
  ///
  /// Parameters:
  ///
  /// * [String] id (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TreasuryOutboundPayment?> postTreasuryOutboundPaymentsIdCancel(String id, { List<String>? expand, }) async {
    final response = await postTreasuryOutboundPaymentsIdCancelWithHttpInfo(id,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryOutboundPayment',) as TreasuryOutboundPayment;
    
    }
    return null;
  }

  /// Create an OutboundTransfer
  ///
  /// <p>Creates an OutboundTransfer.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [int] amount (required):
  ///   Amount (in cents) to be transferred.
  ///
  /// * [String] currency (required):
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] financialAccount (required):
  ///   The FinancialAccount to pull funds from.
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [String] destinationPaymentMethod:
  ///   The PaymentMethod to use as the payment instrument for the OutboundTransfer.
  ///
  /// * [PaymentMethodData1] destinationPaymentMethodData:
  ///
  /// * [PaymentMethodOptions3] destinationPaymentMethodOptions:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] statementDescriptor:
  ///   Statement descriptor to be shown on the receiving end of an OutboundTransfer. Maximum 10 characters for `ach` transfers or 140 characters for `us_domestic_wire` transfers. The default value is \\\"transfer\\\".
  Future<Response> postTreasuryOutboundTransfersWithHttpInfo(int amount, String currency, String financialAccount, { String? description, String? destinationPaymentMethod, PaymentMethodData1? destinationPaymentMethodData, PaymentMethodOptions3? destinationPaymentMethodOptions, List<String>? expand, Map<String, String>? metadata, String? statementDescriptor, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/treasury/outbound_transfers';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (amount != null) {
      formParams[r'amount'] = parameterToString(amount);
    }
    if (currency != null) {
      formParams[r'currency'] = parameterToString(currency);
    }
    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (destinationPaymentMethod != null) {
      formParams[r'destination_payment_method'] = parameterToString(destinationPaymentMethod);
    }
    if (destinationPaymentMethodData != null) {
      formParams[r'destination_payment_method_data'] = parameterToString(destinationPaymentMethodData);
    }
    if (destinationPaymentMethodOptions != null) {
      formParams[r'destination_payment_method_options'] = parameterToString(destinationPaymentMethodOptions);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (financialAccount != null) {
      formParams[r'financial_account'] = parameterToString(financialAccount);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (statementDescriptor != null) {
      formParams[r'statement_descriptor'] = parameterToString(statementDescriptor);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create an OutboundTransfer
  ///
  /// <p>Creates an OutboundTransfer.</p>
  ///
  /// Parameters:
  ///
  /// * [int] amount (required):
  ///   Amount (in cents) to be transferred.
  ///
  /// * [String] currency (required):
  ///   Three-letter [ISO currency code](https://www.iso.org/iso-4217-currency-codes.html), in lowercase. Must be a [supported currency](https://stripe.com/docs/currencies).
  ///
  /// * [String] financialAccount (required):
  ///   The FinancialAccount to pull funds from.
  ///
  /// * [String] description:
  ///   An arbitrary string attached to the object. Often useful for displaying to users.
  ///
  /// * [String] destinationPaymentMethod:
  ///   The PaymentMethod to use as the payment instrument for the OutboundTransfer.
  ///
  /// * [PaymentMethodData1] destinationPaymentMethodData:
  ///
  /// * [PaymentMethodOptions3] destinationPaymentMethodOptions:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [Map<String, String>] metadata:
  ///   Set of [key-value pairs](https://stripe.com/docs/api/metadata) that you can attach to an object. This can be useful for storing additional information about the object in a structured format. Individual keys can be unset by posting an empty value to them. All keys can be unset by posting an empty value to `metadata`.
  ///
  /// * [String] statementDescriptor:
  ///   Statement descriptor to be shown on the receiving end of an OutboundTransfer. Maximum 10 characters for `ach` transfers or 140 characters for `us_domestic_wire` transfers. The default value is \\\"transfer\\\".
  Future<TreasuryOutboundTransfer?> postTreasuryOutboundTransfers(int amount, String currency, String financialAccount, { String? description, String? destinationPaymentMethod, PaymentMethodData1? destinationPaymentMethodData, PaymentMethodOptions3? destinationPaymentMethodOptions, List<String>? expand, Map<String, String>? metadata, String? statementDescriptor, }) async {
    final response = await postTreasuryOutboundTransfersWithHttpInfo(amount, currency, financialAccount,  description: description, destinationPaymentMethod: destinationPaymentMethod, destinationPaymentMethodData: destinationPaymentMethodData, destinationPaymentMethodOptions: destinationPaymentMethodOptions, expand: expand, metadata: metadata, statementDescriptor: statementDescriptor, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryOutboundTransfer',) as TreasuryOutboundTransfer;
    
    }
    return null;
  }

  /// Cancel an OutboundTransfer
  ///
  /// <p>An OutboundTransfer can be canceled if the funds have not yet been paid out.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] outboundTransfer (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<Response> postTreasuryOutboundTransfersOutboundTransferCancelWithHttpInfo(String outboundTransfer, { List<String>? expand, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/treasury/outbound_transfers/{outbound_transfer}/cancel'
      .replaceAll('{outbound_transfer}', outboundTransfer);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Cancel an OutboundTransfer
  ///
  /// <p>An OutboundTransfer can be canceled if the funds have not yet been paid out.</p>
  ///
  /// Parameters:
  ///
  /// * [String] outboundTransfer (required):
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  Future<TreasuryOutboundTransfer?> postTreasuryOutboundTransfersOutboundTransferCancel(String outboundTransfer, { List<String>? expand, }) async {
    final response = await postTreasuryOutboundTransfersOutboundTransferCancelWithHttpInfo(outboundTransfer,  expand: expand, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'TreasuryOutboundTransfer',) as TreasuryOutboundTransfer;
    
    }
    return null;
  }

  /// Create a webhook endpoint
  ///
  /// <p>A webhook endpoint must have a <code>url</code> and a list of <code>enabled_events</code>. You may optionally specify the Boolean <code>connect</code> parameter. If set to true, then a Connect webhook endpoint that notifies the specified <code>url</code> about events from all connected accounts is created; otherwise an account webhook endpoint that notifies the specified <code>url</code> only about events from your account is created. You can also create webhook endpoints in the <a href=\"https://dashboard.stripe.com/account/webhooks\">webhooks settings</a> section of the Dashboard.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [List<String>] enabledEvents (required):
  ///   The list of events to enable for this endpoint. You may specify `['*']` to enable all events, except those that require explicit selection.
  ///
  /// * [String] url (required):
  ///   The URL of the webhook endpoint.
  ///
  /// * [String] apiVersion:
  ///   Events sent to this endpoint will be generated with this Stripe Version instead of your account's default Stripe Version.
  ///
  /// * [bool] connect:
  ///   Whether this endpoint should receive events from connected accounts (`true`), or from your account (`false`). Defaults to `false`.
  ///
  /// * [PostWebhookEndpointsRequestDescription] description:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<Response> postWebhookEndpointsWithHttpInfo(List<String> enabledEvents, String url, { String? apiVersion, bool? connect, PostWebhookEndpointsRequestDescription? description, List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/webhook_endpoints';

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (apiVersion != null) {
      formParams[r'api_version'] = parameterToString(apiVersion);
    }
    if (connect != null) {
      formParams[r'connect'] = parameterToString(connect);
    }
    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (enabledEvents != null) {
      formParams[r'enabled_events'] = parameterToString(enabledEvents);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (url != null) {
      formParams[r'url'] = parameterToString(url);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Create a webhook endpoint
  ///
  /// <p>A webhook endpoint must have a <code>url</code> and a list of <code>enabled_events</code>. You may optionally specify the Boolean <code>connect</code> parameter. If set to true, then a Connect webhook endpoint that notifies the specified <code>url</code> about events from all connected accounts is created; otherwise an account webhook endpoint that notifies the specified <code>url</code> only about events from your account is created. You can also create webhook endpoints in the <a href=\"https://dashboard.stripe.com/account/webhooks\">webhooks settings</a> section of the Dashboard.</p>
  ///
  /// Parameters:
  ///
  /// * [List<String>] enabledEvents (required):
  ///   The list of events to enable for this endpoint. You may specify `['*']` to enable all events, except those that require explicit selection.
  ///
  /// * [String] url (required):
  ///   The URL of the webhook endpoint.
  ///
  /// * [String] apiVersion:
  ///   Events sent to this endpoint will be generated with this Stripe Version instead of your account's default Stripe Version.
  ///
  /// * [bool] connect:
  ///   Whether this endpoint should receive events from connected accounts (`true`), or from your account (`false`). Defaults to `false`.
  ///
  /// * [PostWebhookEndpointsRequestDescription] description:
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  Future<WebhookEndpoint?> postWebhookEndpoints(List<String> enabledEvents, String url, { String? apiVersion, bool? connect, PostWebhookEndpointsRequestDescription? description, List<String>? expand, PostAccountsRequestMetadata? metadata, }) async {
    final response = await postWebhookEndpointsWithHttpInfo(enabledEvents, url,  apiVersion: apiVersion, connect: connect, description: description, expand: expand, metadata: metadata, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'WebhookEndpoint',) as WebhookEndpoint;
    
    }
    return null;
  }

  /// Update a webhook endpoint
  ///
  /// <p>Updates the webhook endpoint. You may edit the <code>url</code>, the list of <code>enabled_events</code>, and the status of your endpoint.</p>
  ///
  /// Note: This method returns the HTTP [Response].
  ///
  /// Parameters:
  ///
  /// * [String] webhookEndpoint (required):
  ///
  /// * [PostWebhookEndpointsRequestDescription] description:
  ///
  /// * [bool] disabled:
  ///   Disable the webhook endpoint if set to true.
  ///
  /// * [List<String>] enabledEvents:
  ///   The list of events to enable for this endpoint. You may specify `['*']` to enable all events, except those that require explicit selection.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] url:
  ///   The URL of the webhook endpoint.
  Future<Response> postWebhookEndpointsWebhookEndpointWithHttpInfo(String webhookEndpoint, { PostWebhookEndpointsRequestDescription? description, bool? disabled, List<String>? enabledEvents, List<String>? expand, PostAccountsRequestMetadata? metadata, String? url, }) async {
    // ignore: prefer_const_declarations
    final path = r'/v1/webhook_endpoints/{webhook_endpoint}'
      .replaceAll('{webhook_endpoint}', webhookEndpoint);

    // ignore: prefer_final_locals
    Object? postBody;

    final queryParams = <QueryParam>[];
    final headerParams = <String, String>{};
    final formParams = <String, String>{};

    const contentTypes = <String>['application/x-www-form-urlencoded'];

    if (description != null) {
      formParams[r'description'] = parameterToString(description);
    }
    if (disabled != null) {
      formParams[r'disabled'] = parameterToString(disabled);
    }
    if (enabledEvents != null) {
      formParams[r'enabled_events'] = parameterToString(enabledEvents);
    }
    if (expand != null) {
      formParams[r'expand'] = parameterToString(expand);
    }
    if (metadata != null) {
      formParams[r'metadata'] = parameterToString(metadata);
    }
    if (url != null) {
      formParams[r'url'] = parameterToString(url);
    }

    return apiClient.invokeAPI(
      path,
      'POST',
      queryParams,
      postBody,
      headerParams,
      formParams,
      contentTypes.isEmpty ? null : contentTypes.first,
    );
  }

  /// Update a webhook endpoint
  ///
  /// <p>Updates the webhook endpoint. You may edit the <code>url</code>, the list of <code>enabled_events</code>, and the status of your endpoint.</p>
  ///
  /// Parameters:
  ///
  /// * [String] webhookEndpoint (required):
  ///
  /// * [PostWebhookEndpointsRequestDescription] description:
  ///
  /// * [bool] disabled:
  ///   Disable the webhook endpoint if set to true.
  ///
  /// * [List<String>] enabledEvents:
  ///   The list of events to enable for this endpoint. You may specify `['*']` to enable all events, except those that require explicit selection.
  ///
  /// * [List<String>] expand:
  ///   Specifies which fields in the response should be expanded.
  ///
  /// * [PostAccountsRequestMetadata] metadata:
  ///
  /// * [String] url:
  ///   The URL of the webhook endpoint.
  Future<WebhookEndpoint?> postWebhookEndpointsWebhookEndpoint(String webhookEndpoint, { PostWebhookEndpointsRequestDescription? description, bool? disabled, List<String>? enabledEvents, List<String>? expand, PostAccountsRequestMetadata? metadata, String? url, }) async {
    final response = await postWebhookEndpointsWebhookEndpointWithHttpInfo(webhookEndpoint,  description: description, disabled: disabled, enabledEvents: enabledEvents, expand: expand, metadata: metadata, url: url, );
    if (response.statusCode >= HttpStatus.badRequest) {
      throw ApiException(response.statusCode, await _decodeBodyBytes(response));
    }
    // When a remote server returns no body with a status of 204, we shall not decode it.
    // At the time of writing this, `dart:convert` will throw an "Unexpected end of input"
    // FormatException when trying to decode an empty string.
    if (response.body.isNotEmpty && response.statusCode != HttpStatus.noContent) {
      return await apiClient.deserializeAsync(await _decodeBodyBytes(response), 'WebhookEndpoint',) as WebhookEndpoint;
    
    }
    return null;
  }
}
